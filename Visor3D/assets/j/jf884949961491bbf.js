(function(){'use strict';async function bimConfirm(message,options){if(window.BimDialogs&&typeof window.BimDialogs.confirm==='function'){return await window.BimDialogs.confirm(message,options||{});}return typeof window.confirm==='function'?window.confirm(message):false;}async function bimPrompt(message,defaultValue,options){if(window.BimDialogs&&typeof window.BimDialogs.prompt==='function'){return await window.BimDialogs.prompt(message,defaultValue,options||{});}return typeof window.prompt==='function'?window.prompt(message,defaultValue):defaultValue;}const utils=window.BimIssuesUtils;const persistenceFactory=window.BimIssuePersistenceService;const attachmentFactory=window.BimIssueAttachmentService;const visualizationFactory=window.BimIssueVisualizationService;const eventBus=window.BimEventBus;const appState=window.BimAppState;if(!utils||!persistenceFactory||!attachmentFactory||!visualizationFactory){throw new Error('El panel de Incidencias no pudo inicializar sus servicios.');}const PANEL_WIDTH_KEY='bimingenieros.exportmodel.issues.panelWidth.v2';const DEFAULT_PANEL_WIDTH=430;const MIN_PANEL_WIDTH=360;const MAX_ISSUE_IMAGES=40;function resolveLegacyModelIdentity(model){const manifest=model&&model.manifest||{};const projects=model&&Array.isArray(model.projects)?model.projects:[];if(manifest.ProjectId)return String(manifest.ProjectId);if(manifest.ProjectName)return String(manifest.ProjectName);if(projects.length===1&&projects[0]&&projects[0].ProjectId)return String(projects[0].ProjectId);return projects.map(project=>project&&project.ProjectId||'').filter(Boolean).sort().join('|');}class IssuePanelController{constructor(){this.persistence=persistenceFactory.create();this.attachments=attachmentFactory.create();this.visualization=visualizationFactory.create();this.cacheService=null;this.sharedPersistTimer=0;this.sharedPersistPromise=Promise.resolve(true);this.sharedPersistInFlight=false;this.sharedPersistPendingRequest=null;this.projectLoadGeneration=0;this.identity='';this.legacyIdentity='';this.model=null;this.issues=[];this.team=[];this.labelsCatalog=[];this.selectedIssueId='';this.draft=null;this.search='';this.statusFilter='all';this.priorityFilter='all';this.labelFilter='all';this.assigneeFilter='all';this.persistTimer=0;this.pendingCommentAttachments=[];this.commentMutationInProgress=false;this.lastCommentMutationSignature='';this.lastCommentMutationAt=0;this.editingCommentId='';this.editingCommentOriginalAttachmentIds=new Set();this.previewUrl='';this.previewScale=1;this.previewOffsetX=0;this.previewOffsetY=0;this.previewDragging=false;this.previewPointerId=null;this.previewLastX=0;this.previewLastY=0;this.lastValidDueDateText='';this.attachmentUrls=new Map();this.commentUrls=new Map();this.historyUrls=new Map();this.pendingUrls=new Map();this.attachmentRenderToken=0;this.commentRenderToken=0;this.historyRenderToken=0;this.pendingRenderToken=0;this.initialized=false;this.activeMenu='list';this.panelEntryViewSnapshot=null;this.panelOpenProgressGeneration=0;this.panelOpenProgressPromise=null;this.issueFocusGeneration=0;this.issueFocusPromise=null;this.pendingIssueFocusRequest=null;this.issueDetailRenderGeneration=0;this.issueDetailRenderFrame=0;this.nodes=this.collectNodes();this.bindEvents();this.populateOptions();this.initPanelSplitter();this.visualization.attach(window.__exportModelViewer,this.nodes.markerLayer);this.visualization.setMarkersVisible(!!(this.nodes.markerToggle&&this.nodes.markerToggle.checked));this.visualization.setIssueProvider(()=>this.getVisibleMarkerIssues());this.visualization.setMarkerClickHandler(id=>this.selectIssue(id,true));this.bindLifecycle();this.restoreCurrentProjectState();this.renderAll();this.initialized=true;window.BimIssuesPanel=this;}collectNodes(){const get=id=>document.getElementById(id);return{workspace:get('workspaceShell'),layout:get('appLayout'),panel:get('issuesPanel'),splitter:get('issuesSplitter'),openButton:get('mobileIssuesPanelButton'),listMenuButton:get('issuesNewMenuButton'),detailsMenuButton:get('issuesDetailsMenuButton'),historyMenuButton:get('issuesHistoryMenuButton'),settingsMenuButton:get('issuesSettingsMenuButton'),listView:get('issuesListView'),detailsView:get('issuesDetailsView'),historyView:get('issuesHistoryView'),settingsView:get('issuesSettingsView'),newButton:get('issuesNewButton'),teamPanel:get('issuesTeamPanel'),teamName:get('issuesTeamNameInput'),teamRole:get('issuesTeamRoleInput'),teamEmail:get('issuesTeamEmailInput'),teamAddButton:get('issuesTeamAddButton'),teamList:get('issuesTeamList'),labelsPanel:get('issuesLabelsPanel'),labelName:get('issuesLabelNameInput'),labelColor:get('issuesLabelColorInput'),labelAddButton:get('issuesLabelAddButton'),labelsList:get('issuesLabelsList'),markerToggle:get('issuesMarkerToggle'),searchInput:get('issuesSearchInput'),statusFilter:get('issuesStatusFilter'),priorityFilter:get('issuesPriorityFilter'),labelFilter:get('issuesLabelFilter'),assigneeFilter:get('issuesAssigneeFilter'),stats:get('issuesStats'),list:get('issuesList'),listEmpty:get('issuesListEmpty'),editor:get('issuesEditor'),editorEmpty:get('issuesEditorEmpty'),editorTitle:get('issuesEditorTitle'),issueLabel:get('issueLabelInput'),title:get('issueTitleInput'),description:get('issueDescriptionInput'),type:get('issueTypeInput'),priority:get('issuePriorityInput'),status:get('issueStatusInput'),statusSwatch:get('issueStatusColorSwatch'),assignee:get('issueAssigneeInput'),dueDate:get('issueDueDateInput'),dueDatePickerButton:get('issueDueDatePickerButton'),dueDatePickerNative:get('issueDueDatePickerNative'),linkedElements:get('issueLinkedElements'),saveButton:get('issueSaveButton'),cancelButton:get('issueCancelButton'),focusButton:get('issueFocusButton'),relocateButton:get('issueRelocateButton'),imageInput:get('issueImageInput'),captureButton:get('issueCaptureButton'),attachmentsList:get('issueAttachmentsList'),commentInput:get('issueCommentInput'),commentImageInput:get('issueCommentImageInput'),commentCaptureButton:get('issueCommentCaptureButton'),commentPending:get('issueCommentPendingAttachments'),addCommentButton:get('issueAddCommentButton'),cancelCommentEditButton:get('issueCancelCommentEditButton'),commentsList:get('issueCommentsList'),historyCount:get('issueHistoryCount'),historyList:get('issueHistoryList'),historyEmpty:get('issuesHistoryEmpty'),historyContent:get('issuesHistoryContent'),historyTitle:get('issuesHistoryTitle'),historyFocusButton:get('issuesHistoryFocusButton'),placementBanner:get('issuesPlacementBanner'),placementCancel:get('issuesPlacementCancel'),markerLayer:get('issuesMarkerLayer'),preview:get('issuesImagePreview'),previewViewport:get('issuesImagePreviewViewport'),previewImage:get('issuesImagePreviewImage'),previewClose:get('issuesImagePreviewClose'),previewZoomIn:get('issuesImagePreviewZoomIn'),previewZoomOut:get('issuesImagePreviewZoomOut'),previewReset:get('issuesImagePreviewReset'),previewZoomLabel:get('issuesImagePreviewZoomLabel')};}bindEvents(){const n=this.nodes;if(n.openButton)n.openButton.addEventListener('click',()=>this.togglePanel());if(n.listMenuButton)n.listMenuButton.addEventListener('click',()=>this.switchMenu('list'));if(n.detailsMenuButton)n.detailsMenuButton.addEventListener('click',()=>this.switchMenu('details'));if(n.historyMenuButton)n.historyMenuButton.addEventListener('click',()=>this.switchMenu('history'));if(n.settingsMenuButton)n.settingsMenuButton.addEventListener('click',()=>this.switchMenu('settings'));if(n.newButton)n.newButton.addEventListener('click',()=>this.startNewIssue());if(n.teamAddButton)n.teamAddButton.addEventListener('click',()=>this.addTeamMember());if(n.labelAddButton)n.labelAddButton.addEventListener('click',()=>this.addLabelDefinition());if(n.labelName)n.labelName.addEventListener('keydown',event=>{if(event.key==='Enter')this.addLabelDefinition();});for(const input of[n.teamName,n.teamRole,n.teamEmail]){if(input)input.addEventListener('keydown',event=>{if(event.key==='Enter')this.addTeamMember();});}if(n.markerToggle)n.markerToggle.addEventListener('change',()=>{this.visualization.setMarkersVisible(n.markerToggle.checked);});if(n.searchInput)n.searchInput.addEventListener('input',()=>{this.search=n.searchInput.value||'';this.renderList();this.visualization.refreshMarkers();});if(n.statusFilter)n.statusFilter.addEventListener('change',()=>{this.statusFilter=n.statusFilter.value||'all';this.updateStatusFilterVisual();this.renderList();this.visualization.refreshMarkers();});if(n.priorityFilter)n.priorityFilter.addEventListener('change',()=>{this.priorityFilter=n.priorityFilter.value||'all';this.renderList();this.visualization.refreshMarkers();});if(n.labelFilter)n.labelFilter.addEventListener('change',()=>{this.labelFilter=n.labelFilter.value||'all';this.renderList();this.visualization.refreshMarkers();});if(n.assigneeFilter)n.assigneeFilter.addEventListener('change',()=>{this.assigneeFilter=n.assigneeFilter.value||'all';this.renderList();this.visualization.refreshMarkers();});if(n.status)n.status.addEventListener('change',()=>this.updateStatusVisual());if(n.saveButton)n.saveButton.addEventListener('click',()=>this.saveEditor());if(n.cancelButton)n.cancelButton.addEventListener('click',()=>this.cancelEditor());if(n.focusButton)n.focusButton.addEventListener('click',()=>this.focusSelectedIssue());if(n.historyFocusButton)n.historyFocusButton.addEventListener('click',()=>this.focusSelectedIssue());if(n.relocateButton)n.relocateButton.addEventListener('click',()=>this.relocateSelectedIssue());if(n.dueDate){n.dueDate.addEventListener('input',()=>this.handleDueDateTextInput());n.dueDate.addEventListener('blur',()=>this.validateDueDateField(true));}if(n.dueDatePickerButton)n.dueDatePickerButton.addEventListener('click',event=>{event.preventDefault();this.openDueDatePicker();});if(n.dueDatePickerNative)n.dueDatePickerNative.addEventListener('change',()=>this.applyDueDatePickerValue());if(n.imageInput)n.imageInput.addEventListener('change',event=>{this.addImageFiles(event.target.files).finally(()=>{event.target.value='';});});if(n.captureButton)n.captureButton.addEventListener('click',()=>this.captureViewerEvidence());if(n.commentImageInput)n.commentImageInput.addEventListener('change',event=>{this.addCommentImageFiles(event.target.files).finally(()=>{event.target.value='';});});if(n.commentCaptureButton)n.commentCaptureButton.addEventListener('click',()=>this.captureCommentEvidence());if(n.addCommentButton)n.addCommentButton.addEventListener('click',()=>this.addComment());if(n.cancelCommentEditButton)n.cancelCommentEditButton.addEventListener('click',()=>this.cancelCommentEdit());if(n.commentInput)n.commentInput.addEventListener('keydown',event=>{if((event.ctrlKey||event.metaKey)&&event.key==='Enter')this.addComment();});if(n.placementCancel)n.placementCancel.addEventListener('click',()=>this.cancelPlacement());if(n.previewClose)n.previewClose.addEventListener('click',()=>this.closeImagePreview());if(n.previewZoomIn)n.previewZoomIn.addEventListener('click',()=>this.zoomImagePreview(0.25));if(n.previewZoomOut)n.previewZoomOut.addEventListener('click',()=>this.zoomImagePreview(-0.25));if(n.previewReset)n.previewReset.addEventListener('click',()=>this.resetImagePreviewTransform());if(n.preview)n.preview.addEventListener('click',event=>{if(event.target===n.preview)this.closeImagePreview();});if(n.previewViewport){n.previewViewport.addEventListener('wheel',event=>{if(!this.isImagePreviewOpen())return;event.preventDefault();this.zoomImagePreview(event.deltaY<0?0.2:-0.2);},{passive:false});n.previewViewport.addEventListener('pointerdown',event=>this.startImagePreviewDrag(event));n.previewViewport.addEventListener('pointermove',event=>this.moveImagePreviewDrag(event));n.previewViewport.addEventListener('pointerup',event=>this.endImagePreviewDrag(event));n.previewViewport.addEventListener('pointercancel',event=>this.endImagePreviewDrag(event));n.previewViewport.addEventListener('dblclick',()=>this.resetImagePreviewTransform());}document.addEventListener('keydown',event=>this.handleImagePreviewKeydown(event));}populateOptions(){const appendOptions=(select,items,includeAll,allLabel)=>{if(!select)return;select.innerHTML='';if(includeAll){const option=document.createElement('option');option.value='all';option.textContent=allLabel||'Todos';select.appendChild(option);}for(const item of items){const option=document.createElement('option');option.value=typeof item==='string'?item:item.value;option.textContent=typeof item==='string'?item:item.label;if(item&&typeof item==='object'&&item.color){option.style.backgroundColor=item.color;option.style.color='#ffffff';option.dataset.color=item.color;}select.appendChild(option);}};appendOptions(this.nodes.statusFilter,utils.STATUSES,true,'Todos los estados');appendOptions(this.nodes.priorityFilter,utils.PRIORITIES,true,'Todas las prioridades');appendOptions(this.nodes.status,utils.STATUSES,false);appendOptions(this.nodes.priority,utils.PRIORITIES,false);appendOptions(this.nodes.type,utils.TYPES,false);this.updateStatusVisual();this.updateStatusFilterVisual();this.populateDynamicFilters();this.populateAssigneeOptions(null);this.populateLabelOptions(null);}updateStatusVisual(){const status=utils.getStatus(this.nodes.status&&this.nodes.status.value);if(this.nodes.statusSwatch){this.nodes.statusSwatch.style.setProperty('--issue-current-status-color',status.color);this.nodes.statusSwatch.title=status.label;}if(this.nodes.status){this.nodes.status.style.setProperty('--issue-current-status-color',status.color);this.nodes.status.style.backgroundColor=status.color;this.nodes.status.style.borderColor=status.color;this.nodes.status.style.color='#ffffff';}}updateStatusFilterVisual(){const select=this.nodes.statusFilter;if(!select)return;if(!select.value||select.value==='all'){select.style.removeProperty('background-color');select.style.removeProperty('border-color');select.style.removeProperty('color');return;}const status=utils.getStatus(select.value);select.style.backgroundColor=status.color;select.style.borderColor=status.color;select.style.color='#ffffff';}initPanelSplitter(){const splitter=this.nodes.splitter;if(!splitter||splitter.__bimIssuesSplitterBound)return;splitter.__bimIssuesSplitterBound=true;let saved=DEFAULT_PANEL_WIDTH;try{const stored=Number(localStorage.getItem(PANEL_WIDTH_KEY));if(Number.isFinite(stored)&&stored>=MIN_PANEL_WIDTH)saved=stored;}catch(_){}this.applyPanelWidth(saved);let dragging=false;const getClientX=event=>event&&event.touches&&event.touches.length?event.touches[0].clientX:event.clientX;const start=event=>{if(!this.isPanelOpen()||window.innerWidth<=900)return;dragging=true;splitter.classList.add('dragging');if(this.nodes.workspace)this.nodes.workspace.classList.add('resizing');document.body.style.cursor='col-resize';document.body.style.userSelect='none';if(event&&event.preventDefault)event.preventDefault();};const move=event=>{if(!dragging)return;const clientX=getClientX(event);if(!Number.isFinite(clientX))return;const bounds=this.nodes.workspace?this.nodes.workspace.getBoundingClientRect():{right:window.innerWidth};this.applyPanelWidth(bounds.right-clientX);this.requestViewerResize();if(event&&event.cancelable)event.preventDefault();};const end=()=>{if(!dragging)return;dragging=false;splitter.classList.remove('dragging');if(this.nodes.workspace)this.nodes.workspace.classList.remove('resizing');document.body.style.cursor='';document.body.style.userSelect='';const width=this.getPanelWidth();try{localStorage.setItem(PANEL_WIDTH_KEY,String(width));}catch(_){}this.requestViewerResize(true);};splitter.addEventListener('mousedown',start);splitter.addEventListener('touchstart',start,{passive:false});window.addEventListener('mousemove',move);window.addEventListener('touchmove',move,{passive:false});window.addEventListener('mouseup',end);window.addEventListener('touchend',end);window.addEventListener('resize',()=>{this.applyPanelWidth(this.getPanelWidth());if(this.isPanelOpen())this.requestViewerResize(true);});}getPanelWidth(){const value=Number.parseFloat(getComputedStyle(document.documentElement).getPropertyValue('--issues-width'));return Number.isFinite(value)&&value>=MIN_PANEL_WIDTH?value:DEFAULT_PANEL_WIDTH;}applyPanelWidth(value){const numeric=Number(value);const width=Math.max(MIN_PANEL_WIDTH,Math.round(Number.isFinite(numeric)?numeric:DEFAULT_PANEL_WIDTH));document.documentElement.style.setProperty('--issues-width',width+'px');return width;}requestViewerResize(deep){if(deep&&window.__requestExportModelViewerResizeDeep){window.__requestExportModelViewerResizeDeep();return;}requestAnimationFrame(()=>{const viewer=window.__exportModelViewer;if(viewer&&viewer.resize)viewer.resize();});}isPanelOpen(){return!!(this.nodes.panel&&!this.nodes.panel.classList.contains('hidden'));}bindLifecycle(){if(!eventBus)return;eventBus.on('project:opened',payload=>{this.setProject(payload&&payload.identity,payload&&payload.model,payload&&payload.cacheService);});eventBus.on('project:closing',()=>this.flushPersistence());eventBus.on('project:closed',()=>this.clearProject());eventBus.on('project:transient-reset',()=>this.visualization.cancelPlacement('project-reset'));}restoreCurrentProjectState(){const state=appState&&appState.snapshot?appState.snapshot('project'):null;if(state&&state.status==='open'&&state.identity){const cacheService=window.ExportModelApp&&window.ExportModelApp.getActiveCacheService?window.ExportModelApp.getActiveCacheService():null;this.setProject(state.identity,window.__exportModelUI&&window.__exportModelUI.model,cacheService);}}setProject(identity,model,cacheService){this.flushPersistence();this.cancelEditor(true);const generation=++this.projectLoadGeneration;this.identity=String(identity||'').trim()||'proyecto-local';this.model=model||null;this.legacyIdentity=resolveLegacyModelIdentity(this.model);this.cacheService=cacheService||null;if(this.attachments.setProjectIdentity)this.attachments.setProjectIdentity(this.identity);if(this.attachments.setSharedStorage)this.attachments.setSharedStorage(this.cacheService);if(this.attachments.clearSharedReferences)this.attachments.clearSharedReferences();this.persistence.setProjectIdentity(this.identity);this.issues=[];this.team=[];this.labelsCatalog=[];this.labelsCatalog=utils.upgradeDefaultLabelDefinitions(this.labelsCatalog);if(!this.labelsCatalog.length)this.labelsCatalog=utils.createDefaultLabelDefinitions();this.sanitizeAllIssues();this.ensureIssueNumbers();this.reconcileAssignees();this.reconcileIssueLabels();if(this.attachments.setSharedStorage)this.attachments.setSharedStorage(this.cacheService);if(this.attachments.registerSharedReferences)this.attachments.registerSharedReferences(this.issues);this.selectedIssueId='';if(this.nodes.newButton)this.nodes.newButton.disabled=false;if(this.nodes.settingsMenuButton)this.nodes.settingsMenuButton.disabled=false;this.visualization.attach(window.__exportModelViewer,this.nodes.markerLayer);this.renderAll();this.visualization.refreshMarkers();this.loadSharedState(this.cacheService,this.identity,generation);}clearProject(){this.flushPersistence();this.cancelEditor(true);++this.projectLoadGeneration;this.identity='';this.legacyIdentity='';this.model=null;this.cacheService=null;if(this.attachments.setProjectIdentity)this.attachments.setProjectIdentity('');if(this.attachments.setSharedStorage)this.attachments.setSharedStorage(null);if(this.attachments.clearSharedReferences)this.attachments.clearSharedReferences();this.issues=[];this.team=[];this.labelsCatalog=[];this.selectedIssueId='';this.panelEntryViewSnapshot=null;if(this.nodes.newButton)this.nodes.newButton.disabled=true;if(this.nodes.settingsMenuButton)this.nodes.settingsMenuButton.disabled=true;this.activeMenu='list';this.renderAll();this.visualization.refreshMarkers();}buildSharedStatePayload(){return{schemaVersion:5,identity:this.identity,savedAt:new Date().toISOString(),issues:utils.normalizeIssues(this.issues),team:utils.normalizeTeam(this.team),labels:utils.normalizeLabelDefinitions(this.labelsCatalog)};}normalizeSharedStatePayload(payload){const source=payload&&payload.state?payload.state:(payload||{});return{issues:utils.normalizeIssues(source.issues),team:utils.normalizeTeam(source.team),labels:utils.normalizeLabelDefinitions(source.labels||source.labelDefinitions)};}getSharedStateIdentity(payload){const source=payload&&payload.state?payload.state:(payload||{});return String(source&&source.identity||payload&&payload.identity||'').trim();}async loadSharedState(cacheService,identity,generation){if(!cacheService||(!cacheService.readIssuesBundle&&!cacheService.readIssuesState&&!cacheService.readStateFile))return;try{let payload=null;if(cacheService.readIssuesBundle)payload=await cacheService.readIssuesBundle();if(!payload&&cacheService.readIssuesState)payload=await cacheService.readIssuesState();if(!payload&&cacheService.readStateFile)payload=await cacheService.readStateFile('issues_cache.json');if(generation!==this.projectLoadGeneration||identity!==this.identity||cacheService!==this.cacheService)return;if(!payload){await this.offerLegacyMigration(cacheService,identity,generation);return;}const payloadIdentity=this.getSharedStateIdentity(payload);const isScopedPayload=payloadIdentity.indexOf('local-folder:')===0||payloadIdentity.indexOf('cloud|')===0||payloadIdentity.indexOf('local-package:')===0;const localFolderMismatch=payloadIdentity.indexOf('local-folder:')===0&&String(identity||'').indexOf('local-folder:')===0&&payloadIdentity!==identity;const samePhysicalLocalCache=!!(localFolderMismatch&&cacheService&&typeof cacheService.ownsLocalStatePayload==='function'&&cacheService.ownsLocalStatePayload(payloadIdentity));if(isScopedPayload&&payloadIdentity!==identity&&!samePhysicalLocalCache){console.warn('Se ignoró una caché de incidencias perteneciente a otra carpeta/proyecto.',{expected:identity,received:payloadIdentity});return;}if(samePhysicalLocalCache){console.info('Se recuperó una caché de incidencias de la misma carpeta con una identidad local anterior.',{expected:identity,received:payloadIdentity});}this.applyPersistedState(payload);if(payloadIdentity!==identity)this.writeSharedState(false);}catch(error){console.warn('No se pudieron cargar las incidencias del caché compartido.',error);}}async offerLegacyMigration(cacheService,identity,generation){if(!identity||identity.indexOf('local-folder:')!==0||!this.legacyIdentity)return false;if(!this.persistence||typeof this.persistence.readLegacyPayload!=='function')return false;const legacy=this.persistence.readLegacyPayload(this.legacyIdentity);const hasLegacyData=!!(legacy.issues.length||legacy.team.length||legacy.labels.length);if(!hasLegacyData)return false;const accepted=await bimConfirm('Se encontraron incidencias antiguas guardadas en este navegador para el mismo modelo.\n\n'+'¿Deseas importarlas únicamente a la carpeta que acabas de abrir?');if(!accepted)return false;if(generation!==this.projectLoadGeneration||identity!==this.identity||cacheService!==this.cacheService)return false;this.applyPersistedState(legacy);const saved=await this.writeSharedState(true);if(saved&&typeof this.persistence.removeLegacyPayload==='function'){this.persistence.removeLegacyPayload(this.legacyIdentity);}return!!saved;}applyPersistedState(payload){if(!this.identity)return false;const state=this.normalizeSharedStatePayload(payload);this.cancelEditor(true);this.issues=state.issues;this.team=state.team;this.labelsCatalog=utils.upgradeDefaultLabelDefinitions(state.labels);if(!this.labelsCatalog.length)this.labelsCatalog=utils.createDefaultLabelDefinitions();this.sanitizeAllIssues();this.ensureIssueNumbers();this.reconcileAssignees();this.reconcileIssueLabels();if(this.attachments.setSharedStorage)this.attachments.setSharedStorage(this.cacheService);if(this.attachments.registerSharedReferences)this.attachments.registerSharedReferences(this.issues);this.selectedIssueId='';this.renderAll();this.visualization.refreshMarkers();return true;}sanitizeIssueInPlace(issue){if(!issue)return null;const normalized=utils.normalizeIssue(issue);for(const key of Object.keys(issue))delete issue[key];Object.assign(issue,normalized);return issue;}sanitizeAllIssues(){this.issues=utils.normalizeIssues(this.issues);if(this.selectedIssueId&&!this.issues.some(issue=>issue.id===this.selectedIssueId))this.selectedIssueId='';return this.issues;}reconcileAssignees(){const byName=new Map(this.team.map(member=>[utils.normalizeText(member.name),member]));const byId=new Map(this.team.map(member=>[member.id,member]));for(const issue of this.issues){let member=issue.assigneeId?byId.get(issue.assigneeId):null;if(!member&&issue.assignee)member=byName.get(utils.normalizeText(issue.assignee));if(member){issue.assigneeId=member.id;issue.assignee=member.name;}}}reconcileIssueLabels(){if(!this.labelsCatalog.length)this.labelsCatalog=utils.createDefaultLabelDefinitions();const byId=new Map(this.labelsCatalog.map(label=>[label.id,label]));const byName=new Map(this.labelsCatalog.map(label=>[utils.normalizeText(label.name),label]));const legacyIds=new Map([['label-pci','label-aci'],['label-arquitectura','label-arq'],['label-telecom','label-dt'],['label-electricas','label-ie'],['label-mecanicas','label-im'],['label-sanitarias','label-is'],['label-estructuras','label-st'],['label-hvac','label-hvac'],['label-coordinacion','label-coordinacion']]);const legacyNames=new Map([['proteccion contra incendios','ACI'],['arquitectura','ARQ'],['telecomunicaciones','DT'],['instalaciones electricas','IE'],['instalaciones mecanicas','IM'],['instalaciones sanitarias','IS'],['estructuras','ST'],['climatizacion / hvac','HVAC']]);for(const issue of this.issues){const mappedId=legacyIds.get(issue.labelId)||issue.labelId;let label=mappedId?byId.get(mappedId):null;const mappedName=legacyNames.get(utils.normalizeText(issue.label))||issue.label;if(!label&&mappedName)label=byName.get(utils.normalizeText(mappedName));if(!label&&issue.label){label=utils.normalizeLabelDefinition({id:utils.createId('label-'),name:issue.label,color:issue.labelColor||issue.markerColor||'#2563eb'});this.labelsCatalog.push(label);byId.set(label.id,label);byName.set(utils.normalizeText(label.name),label);}if(!label)label=this.labelsCatalog[0]||null;this.applyLabelToIssue(issue,label);}}getLabelById(id){return this.labelsCatalog.find(label=>label.id===id)||null;}applyLabelToIssue(issue,label){if(!issue)return;issue.labelId=label?label.id:'';issue.label=label?label.name:'';issue.labelColor=label?label.color:utils.getStatus(issue.status).color;issue.labels=issue.label?[issue.label]:[];issue.markerColorMode='label';issue.markerColor=issue.labelColor;}populateLabelOptions(issue){const select=this.nodes.issueLabel;if(!select)return;const currentId=issue&&issue.labelId||'';select.innerHTML='';for(const label of this.labelsCatalog.slice().sort((a,b)=>a.name.localeCompare(b.name,'es'))){const option=document.createElement('option');option.value=label.id;option.textContent=label.name;option.dataset.labelColor=label.color;select.appendChild(option);}if(!this.labelsCatalog.length){const option=document.createElement('option');option.value='';option.textContent='Crea una etiqueta primero';select.appendChild(option);}select.value=this.labelsCatalog.some(label=>label.id===currentId)?currentId:(this.labelsCatalog[0]&&this.labelsCatalog[0].id||'');}ensureIssueNumbers(){const used=new Set();let next=1;for(const issue of this.issues){const number=String(issue.number||'').trim();const key=utils.normalizeText(number);if(number&&!used.has(key)){issue.number=number;used.add(key);const auto=/^INC-(\d+)$/i.exec(number);if(auto)next=Math.max(next,Number(auto[1])+1);}else{issue.number='';}}for(const issue of this.issues){if(issue.number)continue;let candidate=utils.buildIssueNumber(next++);while(used.has(utils.normalizeText(candidate)))candidate=utils.buildIssueNumber(next++);issue.number=candidate;used.add(utils.normalizeText(candidate));}}getNextIssueNumber(){let max=0;const used=new Set(this.issues.map(issue=>utils.normalizeText(issue.number)));for(const issue of this.issues){const match=/^INC-(\d+)$/i.exec(issue.number||'');if(match)max=Math.max(max,Number(match[1])||0);}let candidate=utils.buildIssueNumber(max+1);while(used.has(utils.normalizeText(candidate)))candidate=utils.buildIssueNumber(++max+1);return candidate;}togglePanel(force){const panel=this.nodes.panel;if(!panel)return;const wasOpen=!panel.classList.contains('hidden');const open=typeof force==='boolean'?force:panel.classList.contains('hidden');if(typeof force==='boolean'&&open===wasOpen)return;const generation=++this.panelOpenProgressGeneration;if(open&&!wasOpen){this.panelEntryViewSnapshot=this.visualization.captureViewState();}panel.classList.toggle('hidden',!open);panel.setAttribute('aria-hidden',open?'false':'true');if(this.nodes.splitter)this.nodes.splitter.classList.toggle('hidden',!open);if(this.nodes.layout)this.nodes.layout.classList.toggle('issues-panel-visible',open);if(this.nodes.workspace)this.nodes.workspace.classList.toggle('issues-panel-open',open);if(this.nodes.openButton){this.nodes.openButton.classList.toggle('active',open);this.nodes.openButton.setAttribute('aria-expanded',open?'true':'false');}if(open){const ui=window.__exportModelUI||null;const runOpen=async report=>{if(generation!==this.panelOpenProgressGeneration)return false;report({message:'Abriendo Incidencias...',current:0,total:0});if(ui&&ui.waitForLoadingProgressPaint)await ui.waitForLoadingProgressPaint();this.applyPanelWidth(this.getPanelWidth());this.visualization.attach(window.__exportModelViewer,this.nodes.markerLayer);if(generation!==this.panelOpenProgressGeneration)return false;if(typeof this.visualization.refreshMarkersAsync==='function'){await this.visualization.refreshMarkersAsync({onProgress:update=>report(update),batchSize:120,isCancelled:()=>generation!==this.panelOpenProgressGeneration||panel.classList.contains('hidden')});}else{report({message:'Sincronizando marcadores de Incidencias...',current:0,total:0});if(ui&&ui.waitForLoadingProgressPaint)await ui.waitForLoadingProgressPaint();this.visualization.refreshMarkers();}if(generation!==this.panelOpenProgressGeneration)return false;report({message:'Preparando la vista de Incidencias...',current:0,total:0});if(ui&&ui.waitForLoadingProgressPaint)await ui.waitForLoadingProgressPaint();this.switchMenu(this.activeMenu||'list',{keepPanelState:true});this.requestViewerResize(true);report({message:'Panel Incidencias listo.',current:1,total:1});return true;};if(ui&&typeof ui.runLoadingOperation==='function'&&!wasOpen){const promise=ui.runLoadingOperation({message:'Abriendo Incidencias...',total:0,completionMessage:'Panel Incidencias listo.',completionStatus:'Modelo cargado',errorStatus:'No se pudo preparar el panel Incidencias.',completeWhenFalse:false},runOpen).catch(error=>{console.warn('No se pudo completar la apertura de Incidencias.',error);return false;});this.panelOpenProgressPromise=promise;Promise.resolve(promise).finally(()=>{if(this.panelOpenProgressPromise===promise)this.panelOpenProgressPromise=null;});}else{this.applyPanelWidth(this.getPanelWidth());this.visualization.attach(window.__exportModelViewer,this.nodes.markerLayer);this.visualization.refreshMarkers();this.switchMenu(this.activeMenu||'list',{keepPanelState:true});this.requestViewerResize(true);}}else if(wasOpen){this.cancelPendingIssueFocus('panel-closed');this.cancelIssueDetailRender();this.cancelPlacement();const entrySnapshot=this.panelEntryViewSnapshot;this.panelEntryViewSnapshot=null;const ui=window.__exportModelUI||null;if(ui&&typeof ui.restoreViewerAfterPanelClose==='function'){Promise.resolve(ui.restoreViewerAfterPanelClose('incidencias',{resetViewer:false,restoreSnapshot:entrySnapshot?report=>(typeof this.visualization.restoreViewStateAsync==='function'?this.visualization.restoreViewStateAsync(entrySnapshot,{onProgress:report}):this.visualization.restoreViewState(entrySnapshot)):null,fitToModel:false})).catch(error=>console.warn('No se pudo completar la restauración al cerrar Incidencias.',error));}else if(entrySnapshot){this.visualization.restoreViewState(entrySnapshot);}this.requestViewerResize(true);}else{this.requestViewerResize(true);}}switchMenu(menu,options){const valid=['list','details','history','settings'];let next=valid.includes(menu)?menu:'list';const issue=this.getSelectedIssue();if((next==='details'||next==='history')&&!issue)next='list';if(next==='settings'&&!this.identity)next='list';this.activeMenu=next;const views={list:this.nodes.listView,details:this.nodes.detailsView,history:this.nodes.historyView,settings:this.nodes.settingsView};const buttons={list:this.nodes.listMenuButton,details:this.nodes.detailsMenuButton,history:this.nodes.historyMenuButton,settings:this.nodes.settingsMenuButton};for(const name of valid){const active=name===next;if(views[name])views[name].classList.toggle('hidden',!active);if(buttons[name]){buttons[name].classList.toggle('active',active);buttons[name].setAttribute('aria-selected',active?'true':'false');}}const skipRender=!!(options&&options.skipRender);if(!skipRender){if(next==='settings'){this.renderTeam();this.renderLabelsManager();}else if(next==='details'){this.renderEditor(issue,!!this.draft);}else if(next==='history'){this.renderHistory(issue);}}this.updateMenuAvailability();if(!(options&&options.keepPanelState))this.togglePanel(true);}updateMenuAvailability(){const hasIssue=!!this.getSelectedIssue();if(this.nodes.detailsMenuButton)this.nodes.detailsMenuButton.disabled=!hasIssue;if(this.nodes.historyMenuButton)this.nodes.historyMenuButton.disabled=!hasIssue;if(this.nodes.settingsMenuButton)this.nodes.settingsMenuButton.disabled=!this.identity;if(this.nodes.newButton)this.nodes.newButton.disabled=!this.identity;}addLabelDefinition(){if(!this.identity)return;const name=String(this.nodes.labelName&&this.nodes.labelName.value||'').trim();const color=utils.normalizeHexColor(this.nodes.labelColor&&this.nodes.labelColor.value,'#2563eb');if(!name){alert('Escribe el nombre de la etiqueta.');if(this.nodes.labelName)this.nodes.labelName.focus();return;}if(this.labelsCatalog.some(label=>utils.normalizeText(label.name)===utils.normalizeText(name))){alert('Ya existe una etiqueta con ese nombre.');return;}this.labelsCatalog.push(utils.normalizeLabelDefinition({id:utils.createId('label-'),name,color}));if(this.nodes.labelName)this.nodes.labelName.value='';this.persistSoon();this.renderLabelsManager();this.populateLabelOptions(this.getSelectedIssue());this.populateDynamicFilters();}updateLabelDefinition(labelId,name,color){const label=this.getLabelById(labelId);if(!label)return;const nextName=String(name||'').trim();if(!nextName){alert('La etiqueta debe tener un nombre.');return;}const duplicate=this.labelsCatalog.some(item=>item.id!==labelId&&utils.normalizeText(item.name)===utils.normalizeText(nextName));if(duplicate){alert('Ya existe otra etiqueta con ese nombre.');return;}const previousName=label.name;label.name=nextName;label.color=utils.normalizeHexColor(color,label.color);label.updatedAt=new Date().toISOString();for(const issue of this.issues.concat(this.draft?[this.draft]:[])){if(!issue||(issue.labelId!==labelId&&utils.normalizeText(issue.label)!==utils.normalizeText(previousName)))continue;this.applyLabelToIssue(issue,label);issue.updatedAt=new Date().toISOString();}this.persistSoon();this.renderLabelsManager();this.populateLabelOptions(this.getSelectedIssue());this.populateDynamicFilters();this.renderList();this.visualization.refreshMarkers();}async removeLabelDefinition(labelId){const label=this.getLabelById(labelId);if(!label)return;if(this.labelsCatalog.length<=1){alert('Debe existir al menos una etiqueta disponible.');return;}const used=this.issues.filter(issue=>issue.labelId===labelId).length+(this.draft&&this.draft.labelId===labelId?1:0);const message=used?'¿Eliminar la etiqueta "'+label.name+'"? Las '+used+' incidencia(s) asociadas pasarán a otra etiqueta disponible.':'¿Eliminar la etiqueta "'+label.name+'"?';if(!await bimConfirm(message,{danger:true,acceptText:'Eliminar'}))return;this.labelsCatalog=this.labelsCatalog.filter(item=>item.id!==labelId);const fallback=this.labelsCatalog[0]||null;for(const issue of this.issues.concat(this.draft?[this.draft]:[])){if(issue&&issue.labelId===labelId)this.applyLabelToIssue(issue,fallback);}this.persistSoon();this.renderLabelsManager();this.populateLabelOptions(this.getSelectedIssue());this.populateDynamicFilters();this.renderList();this.visualization.refreshMarkers();}renderLabelsManager(){const container=this.nodes.labelsList;if(!container)return;container.innerHTML='';if(!this.labelsCatalog.length){const empty=document.createElement('div');empty.className='issues-subtle';empty.textContent='Todavía no existen etiquetas.';container.appendChild(empty);return;}for(const label of this.labelsCatalog.slice().sort((a,b)=>a.name.localeCompare(b.name,'es'))){const card=document.createElement('div');card.className='issues-label-definition';const color=document.createElement('input');color.type='color';color.className='issues-color-input';color.value=label.color;color.setAttribute('aria-label','Color de '+label.name);const name=document.createElement('input');name.type='text';name.className='issues-input';name.maxLength=80;name.value=label.name;const save=document.createElement('button');save.type='button';save.className='issues-secondary-button';save.textContent='Guardar';save.addEventListener('click',()=>this.updateLabelDefinition(label.id,name.value,color.value));const remove=document.createElement('button');remove.type='button';remove.className='issues-icon-button danger';remove.textContent='×';remove.title='Eliminar etiqueta';remove.addEventListener('click',()=>this.removeLabelDefinition(label.id));name.addEventListener('keydown',event=>{if(event.key==='Enter')this.updateLabelDefinition(label.id,name.value,color.value);});card.append(color,name,save,remove);container.appendChild(card);}}addTeamMember(){if(!this.identity)return;const name=String(this.nodes.teamName&&this.nodes.teamName.value||'').trim();const role=String(this.nodes.teamRole&&this.nodes.teamRole.value||'').trim();const email=String(this.nodes.teamEmail&&this.nodes.teamEmail.value||'').trim();if(!name){alert('Escribe el nombre de la persona.');if(this.nodes.teamName)this.nodes.teamName.focus();return;}const duplicate=this.team.some(member=>utils.normalizeText(member.name)===utils.normalizeText(name)&&utils.normalizeText(member.email)===utils.normalizeText(email));if(duplicate){alert('Esta persona ya está registrada en el equipo.');return;}this.team.push(utils.normalizeTeamMember({id:utils.createId('member-'),name,role,email}));if(this.nodes.teamName)this.nodes.teamName.value='';if(this.nodes.teamRole)this.nodes.teamRole.value='';if(this.nodes.teamEmail)this.nodes.teamEmail.value='';this.persistSoon();this.renderTeam();this.populateAssigneeOptions(this.getSelectedIssue());this.populateDynamicFilters();}updateTeamMember(memberId,nameValue,roleValue,emailValue){const member=this.team.find(item=>item.id===memberId);if(!member)return;const name=String(nameValue||'').trim();const role=String(roleValue||'').trim();const email=String(emailValue||'').trim();if(!name){alert('El nombre de la persona no puede quedar vacío.');return;}const duplicate=this.team.some(item=>item.id!==memberId&&utils.normalizeText(item.name)===utils.normalizeText(name)&&utils.normalizeText(item.email)===utils.normalizeText(email));if(duplicate){alert('Ya existe otra persona con el mismo nombre y correo.');return;}const previousName=member.name;member.name=name;member.role=role;member.email=email;member.updatedAt=new Date().toISOString();for(const issue of this.issues.concat(this.draft?[this.draft]:[])){if(issue.assigneeId===memberId)issue.assignee=name;else if(!issue.assigneeId&&utils.normalizeText(issue.assignee)===utils.normalizeText(previousName)){issue.assigneeId=memberId;issue.assignee=name;}}this.persistSoon();this.renderTeam();this.populateAssigneeOptions(this.getSelectedIssue());this.populateDynamicFilters();this.renderList();}async removeTeamMember(memberId){const member=this.team.find(item=>item.id===memberId);if(!member)return;const assigned=this.issues.filter(issue=>issue.assigneeId===memberId).length;const message=assigned?'¿Quitar a '+member.name+' del equipo? Su nombre se conservará en '+assigned+' incidencia(s) existentes.':'¿Quitar a '+member.name+' del equipo?';if(!await bimConfirm(message,{danger:true,acceptText:'Quitar'}))return;this.team=this.team.filter(item=>item.id!==memberId);for(const issue of this.issues){if(issue.assigneeId===memberId)issue.assigneeId='';}const current=this.getSelectedIssue();if(current&&current.assigneeId===memberId)current.assigneeId='';this.persistSoon();this.renderTeam();this.populateAssigneeOptions(current);this.populateDynamicFilters();this.renderList();}renderTeam(){const container=this.nodes.teamList;if(!container)return;container.innerHTML='';if(!this.team.length){const empty=document.createElement('div');empty.className='issues-subtle';empty.textContent='Todavía no se registraron personas.';container.appendChild(empty);return;}for(const member of this.team.slice().sort((a,b)=>a.name.localeCompare(b.name,'es'))){const card=document.createElement('div');card.className='issues-team-member editable';const name=document.createElement('input');name.type='text';name.className='issues-input';name.maxLength=100;name.value=member.name;name.setAttribute('aria-label','Nombre de la persona');const role=document.createElement('input');role.type='text';role.className='issues-input';role.maxLength=100;role.value=member.role||'';role.placeholder='Cargo o especialidad';role.setAttribute('aria-label','Cargo o especialidad');const email=document.createElement('input');email.type='email';email.className='issues-input';email.maxLength=160;email.value=member.email||'';email.placeholder='Correo (opcional)';email.setAttribute('aria-label','Correo');const save=document.createElement('button');save.type='button';save.className='issues-secondary-button';save.textContent='Guardar';save.addEventListener('click',()=>this.updateTeamMember(member.id,name.value,role.value,email.value));const remove=document.createElement('button');remove.type='button';remove.className='issues-icon-button danger';remove.textContent='×';remove.title='Quitar del equipo';remove.addEventListener('click',()=>this.removeTeamMember(member.id));for(const input of[name,role,email]){input.addEventListener('keydown',event=>{if(event.key==='Enter')this.updateTeamMember(member.id,name.value,role.value,email.value);});}card.append(name,role,email,save,remove);container.appendChild(card);}}populateAssigneeOptions(issue){const select=this.nodes.assignee;if(!select)return;const currentId=issue&&issue.assigneeId||'';const currentName=issue&&issue.assignee||'';select.innerHTML='';const empty=document.createElement('option');empty.value='';empty.textContent='Sin asignar';empty.dataset.memberName='';select.appendChild(empty);for(const member of this.team.slice().sort((a,b)=>a.name.localeCompare(b.name,'es'))){const option=document.createElement('option');option.value=member.id;option.textContent=member.name+(member.role?' · '+member.role:'');option.dataset.memberName=member.name;select.appendChild(option);}let value=currentId;if(currentName&&!this.team.some(member=>member.id===currentId)){const legacy=document.createElement('option');legacy.value='legacy:'+utils.hashText(currentName);legacy.textContent=currentName+' · no registrado';legacy.dataset.memberName=currentName;select.appendChild(legacy);value=legacy.value;}select.value=value||'';}populateDynamicFilters(){const populate=(select,values,allLabel,current)=>{if(!select)return'all';select.innerHTML='';const all=document.createElement('option');all.value='all';all.textContent=allLabel;select.appendChild(all);for(const item of values){const option=document.createElement('option');option.value=typeof item==='string'?item:item.value;option.textContent=typeof item==='string'?item:item.label;select.appendChild(option);}const available=values.map(item=>typeof item==='string'?item:item.value);const next=current!=='all'&&available.includes(current)?current:'all';select.value=next;return next;};const labels=this.labelsCatalog.slice().sort((a,b)=>a.name.localeCompare(b.name,'es')).map(label=>({value:label.id,label:label.name}));const assignees=utils.uniqueStrings(this.team.map(member=>member.name).concat(this.issues.map(issue=>issue.assignee))).sort((a,b)=>a.localeCompare(b,'es'));this.labelFilter=populate(this.nodes.labelFilter,labels,'Todas las etiquetas',this.labelFilter);this.assigneeFilter=populate(this.nodes.assigneeFilter,assignees,'Todos los responsables',this.assigneeFilter);}startNewIssue(){const viewer=window.__exportModelViewer;if(!this.identity||!viewer||!viewer.meshes||!viewer.meshes.length){alert('Carga un proyecto BIM antes de crear una incidencia.');return;}this.togglePanel(true);this.cancelEditor(true);const defaultLabel=this.labelsCatalog[0]||null;this.draft=utils.normalizeIssue({id:utils.createId('issue-'),number:this.getNextIssueNumber(),labelId:defaultLabel&&defaultLabel.id,label:defaultLabel&&defaultLabel.name,labelColor:defaultLabel&&defaultLabel.color,title:'',type:utils.TYPES[0],priority:'medium',status:'active',createdBy:'Usuario local',attachments:[],comments:[],history:[]});this.applyLabelToIssue(this.draft,defaultLabel);this.selectedIssueId='';this.switchMenu('details');this.enterPlacement(this.draft,true);}enterPlacement(issue,isNew){if(!issue)return;if(this.nodes.placementBanner)this.nodes.placementBanner.classList.remove('hidden');this.visualization.enterPlacement(async payload=>{issue.anchor=payload.anchor;issue.elementKeys=payload.elementKeys;issue.viewSnapshot=payload.viewSnapshot;issue.updatedAt=new Date().toISOString();if(!isNew&&!this.draft){this.addHistory(issue,'Marcador reubicado',payload.elementKeys.length+' elemento(s) BIM vinculados.');this.persistSoon();}if(this.nodes.placementBanner)this.nodes.placementBanner.classList.add('hidden');this.renderEditor(issue,isNew);if(isNew&&!issue.attachments.length){try{const metadata=await this.attachments.captureViewer(window.__exportModelViewer,issue.id);const current=this.getSelectedIssue();const stillCurrentDraft=!!(current&&current.id===issue.id&&this.draft&&this.draft.id===issue.id);if(!stillCurrentDraft){if(metadata&&metadata.id)this.attachments.delete(metadata.id).catch(()=>{});return;}issue.attachments.push(metadata);this.renderAttachments(issue);}catch(error){console.warn('No se pudo crear la captura inicial de la incidencia.',error);}}},()=>{if(this.nodes.placementBanner)this.nodes.placementBanner.classList.add('hidden');});}cancelPlacement(){this.visualization.cancelPlacement('user');if(this.nodes.placementBanner)this.nodes.placementBanner.classList.add('hidden');}getSelectedIssue(){if(this.draft)return this.draft;return this.issues.find(issue=>issue.id===this.selectedIssueId)||null;}isValidCalendarDate(day,month,year){const date=new Date(year,month-1,day);return date.getFullYear()===year&&date.getMonth()===month-1&&date.getDate()===day;}formatDueDateForEditor(isoValue){const match=/^(\d{4})-(\d{2})-(\d{2})$/.exec(String(isoValue||'').trim());return match?match[3]+'/'+match[2]+'/'+match[1]:'';}parseDueDateFromEditor(value){const text=String(value||'').trim();if(!text)return'';const match=/^(\d{2})\/(\d{2})\/(\d{4})$/.exec(text);if(!match)return null;const day=Number(match[1]);const month=Number(match[2]);const year=Number(match[3]);if(year<1000||!this.isValidCalendarDate(day,month,year))return null;return match[3]+'-'+match[2]+'-'+match[1];}formatDueDateDigits(value){const digits=String(value||'').replace(/\D/g,'').slice(0,8);if(!digits)return'';if(digits.length<=2)return digits;if(digits.length<=4)return digits.slice(0,2)+'/'+digits.slice(2);return digits.slice(0,2)+'/'+digits.slice(2,4)+'/'+digits.slice(4);}isValidPartialDueDate(value){const digits=String(value||'').replace(/\D/g,'');if(!digits)return true;if(digits.length>=1&&Number(digits[0])>3)return false;if(digits.length>=2){const day=Number(digits.slice(0,2));if(day<1||day>31)return false;}if(digits.length>=3&&Number(digits[2])>1)return false;if(digits.length>=4){const month=Number(digits.slice(2,4));if(month<1||month>12)return false;}if(digits.length===8&&this.parseDueDateFromEditor(this.formatDueDateDigits(digits))===null)return false;return true;}handleDueDateTextInput(){const input=this.nodes.dueDate;if(!input)return;const formatted=this.formatDueDateDigits(input.value);if(!this.isValidPartialDueDate(formatted)){input.value=this.lastValidDueDateText;return;}input.value=formatted;this.lastValidDueDateText=formatted;input.setCustomValidity('');if(this.nodes.dueDatePickerNative){const iso=this.parseDueDateFromEditor(formatted);this.nodes.dueDatePickerNative.value=iso||'';}}validateDueDateField(report){const input=this.nodes.dueDate;if(!input)return true;const parsed=this.parseDueDateFromEditor(input.value);const valid=parsed!==null;input.setCustomValidity(valid?'':'Usa una fecha válida con el formato DD/MM/AAAA.');if(!valid&&report)input.reportValidity();return valid;}openDueDatePicker(){const nativeInput=this.nodes.dueDatePickerNative;if(!nativeInput)return;const parsed=this.parseDueDateFromEditor(this.nodes.dueDate&&this.nodes.dueDate.value||'');nativeInput.value=parsed||'';try{if(typeof nativeInput.showPicker==='function')nativeInput.showPicker();else nativeInput.click();}catch(_){nativeInput.click();}}applyDueDatePickerValue(){const nativeInput=this.nodes.dueDatePickerNative;const input=this.nodes.dueDate;if(!nativeInput||!input)return;input.value=this.formatDueDateForEditor(nativeInput.value);this.lastValidDueDateText=input.value;input.setCustomValidity('');}readEditorIntoIssue(issue){if(!issue)return null;const n=this.nodes;if(!this.validateDueDateField(true))return null;issue.title=String(n.title&&n.title.value||'').trim();issue.description=String(n.description&&n.description.value||'').trim();issue.type=String(n.type&&n.type.value||utils.TYPES[0]);issue.priority=String(n.priority&&n.priority.value||'medium');issue.status=String(n.status&&n.status.value||'active');const option=n.assignee&&n.assignee.selectedOptions?n.assignee.selectedOptions[0]:null;const selectedValue=String(n.assignee&&n.assignee.value||'');issue.assigneeId=selectedValue&&!selectedValue.startsWith('legacy:')?selectedValue:'';issue.assignee=option&&option.dataset?String(option.dataset.memberName||''):'';issue.dueDate=this.parseDueDateFromEditor(n.dueDate&&n.dueDate.value||'')||'';const selectedLabel=this.getLabelById(String(n.issueLabel&&n.issueLabel.value||''))||this.labelsCatalog[0]||null;this.applyLabelToIssue(issue,selectedLabel);return issue;}async saveEditor(){const issue=this.getSelectedIssue();if(!issue)return;const isDraft=!!this.draft;const previous=isDraft?null:utils.normalizeIssue(issue);if(!this.readEditorIntoIssue(issue))return;if(!issue.title){alert('Escribe un título para la incidencia.');if(this.nodes.title)this.nodes.title.focus();return;}if(!issue.anchor){alert('Ubica la incidencia dentro del modelo 3D antes de guardarla.');this.enterPlacement(issue,isDraft);return;}const latestViewSnapshot=this.visualization.captureViewState();if(latestViewSnapshot)issue.viewSnapshot=latestViewSnapshot;this.dedupeIssueAttachmentReferences(issue);const now=new Date().toISOString();issue.updatedAt=now;issue.revision=Math.max(1,Number(issue.revision)||1)+(isDraft?0:1);if(isDraft){issue.createdAt=issue.createdAt||now;this.addHistory(issue,'Incidencia creada',this.describeIssueAssignment(issue));const savedIssue=utils.normalizeIssue(issue);this.issues.unshift(savedIssue);this.selectedIssueId=savedIssue.id;this.draft=null;this.emit('issues:created',savedIssue);}else{const changes=this.describeChanges(previous,issue);this.addHistory(issue,'Modificaciones guardadas',changes||'Se validaron los datos actuales sin agregar nuevas evidencias.');this.emit('issues:updated',issue);}this.renderAll();this.visualization.refreshMarkers();const saveButton=this.nodes.saveButton;if(saveButton)saveButton.disabled=true;try{const saved=await this.flushSharedPersistence(true);if(!saved)throw new Error('El visor conservó los cambios en memoria, pero no pudo confirmarlos en la carpeta o caché del proyecto.');}catch(error){if(window.BimPersistencePolicy)window.BimPersistencePolicy.notifyError('Incidencias',error);alert('No se pudo confirmar el guardado de la incidencia:\n'+(error&&error.message?error.message:error));}finally{if(saveButton)saveButton.disabled=false;}}cancelEditor(silent){this.cancelPendingIssueFocus('editor-cancelled');this.cancelIssueDetailRender();this.cancelPlacement();const draftId=this.draft&&this.draft.id;this.cancelCommentEdit(true);this.draft=null;this.selectedIssueId='';this.clearAllImageUrls();if(this.nodes.commentInput)this.nodes.commentInput.value='';this.renderEditor(null,false);this.updateMenuAvailability();if(!silent){this.renderList();this.switchMenu('list',{skipRender:true});}if(draftId)this.attachments.deleteIssue(draftId).catch(()=>{});}cancelPendingIssueFocus(reason){this.issueFocusGeneration=(this.issueFocusGeneration||0)+1;this.pendingIssueFocusRequest=null;if(reason&&window.BimDebugLog&&typeof window.BimDebugLog.log==='function'){try{window.BimDebugLog.log('debug','issues:focus-cancelled',{reason:String(reason)});}catch(_){}}}async executeIssueFocusRequest(request){const issue=request&&request.issue;const generation=request&&request.generation;if(!issue||!generation)return false;const ui=window.__exportModelUI||null;const execute=async report=>{const isCancelled=()=>generation!==this.issueFocusGeneration||this.selectedIssueId!==issue.id;if(isCancelled())return false;if(ui&&typeof ui.waitForLoadingProgressPaint==='function')await ui.waitForLoadingProgressPaint();if(isCancelled())return false;if(typeof this.visualization.focusIssueAsync==='function'){return await this.visualization.focusIssueAsync(issue,{onProgress:update=>report(update),isCancelled,deferEdgeRefresh:true});}return this.visualization.focusIssue(issue);};let promise;if(ui&&typeof ui.runLoadingOperation==='function'){promise=ui.runLoadingOperation({message:'Dirigiendo a la incidencia...',total:0,completionMessage:'Incidencia localizada.',completionStatus:'Modelo cargado',errorStatus:'No se pudo recuperar completamente la vista de la incidencia.',completeWhenFalse:false},execute);}else{promise=Promise.resolve(execute(()=>{}));}return Promise.resolve(promise).catch(error=>{console.warn('No se pudo dirigir el visor a la incidencia.',error);return false;});}focusIssueWithProgress(issue){if(!issue)return Promise.resolve(false);const generation=(this.issueFocusGeneration||0)+1;this.issueFocusGeneration=generation;this.pendingIssueFocusRequest={issue,generation};if(this.issueFocusPromise)return this.issueFocusPromise;const drain=async()=>{let result=false;while(this.pendingIssueFocusRequest){const request=this.pendingIssueFocusRequest;this.pendingIssueFocusRequest=null;if(!request||!request.issue||this.selectedIssueId!==request.issue.id)continue;result=await this.executeIssueFocusRequest(request);}return result;};const guarded=Promise.resolve().then(drain).finally(()=>{if(this.issueFocusPromise===guarded)this.issueFocusPromise=null;});this.issueFocusPromise=guarded;return guarded;}selectIssue(id,focus){const issue=this.issues.find(item=>item.id===id);if(!issue)return;this.cancelPendingIssueFocus('issue-selection-changed');this.cancelIssueDetailRender();this.cancelPlacement();this.cancelCommentEdit(true);this.draft=null;this.selectedIssueId=id;if(this.nodes.commentInput)this.nodes.commentInput.value='';this.togglePanel(true);this.renderList();this.switchMenu('details');if(focus!==false){const panelOpenPromise=this.panelOpenProgressPromise;if(panelOpenPromise){Promise.resolve(panelOpenPromise).finally(()=>{if(this.selectedIssueId===issue.id)this.focusIssueWithProgress(issue);});}else{this.focusIssueWithProgress(issue);}}this.emit('issues:selected',issue);}focusSelectedIssue(){const issue=this.getSelectedIssue();if(issue)this.focusIssueWithProgress(issue);}relocateSelectedIssue(){const issue=this.getSelectedIssue();if(!issue)return;if(!this.readEditorIntoIssue(issue))return;this.enterPlacement(issue,!!this.draft);}async deleteIssueFromList(issueId){const issue=this.issues.find(item=>item.id===issueId);if(!issue)return;const label=String(issue.label||'Sin etiqueta');const title=String(issue.title||'Sin nombre');const message='¿Deseas eliminar esta incidencia?\n\nEtiqueta: '+label+'\nNombre: '+title+'\n\nTambién se eliminarán sus comentarios e imágenes asociadas.';if(!await bimConfirm(message,{danger:true,acceptText:'Eliminar'}))return;const wasSelected=this.selectedIssueId===issue.id;if(wasSelected){this.cancelPlacement();this.cancelCommentEdit(true);this.selectedIssueId='';this.draft=null;this.clearAllImageUrls();if(this.nodes.commentInput)this.nodes.commentInput.value='';}this.issues=this.issues.filter(item=>item.id!==issue.id);try{await this.attachments.deleteIssue(issue.id);}catch(error){console.warn(error);}this.emit('issues:deleted',issue);this.persistSoon();this.renderAll();if(wasSelected)this.switchMenu('list');this.visualization.refreshMarkers();}getIssueAttachmentCount(issue){if(!issue)return 0;const general=Array.isArray(issue.attachments)?issue.attachments.length:0;if(this.editingCommentId){const otherComments=Array.isArray(issue.comments)?issue.comments.reduce((sum,comment)=>sum+(comment.id===this.editingCommentId?0:(Array.isArray(comment.attachments)?comment.attachments.length:0)),0):0;return general+otherComments+this.pendingCommentAttachments.length;}const comments=Array.isArray(issue.comments)?issue.comments.reduce((sum,comment)=>sum+(Array.isArray(comment.attachments)?comment.attachments.length:0),0):0;return general+comments+this.pendingCommentAttachments.length;}getAttachmentReferenceKey(attachment){if(!attachment)return'';const fingerprint=String(attachment.fingerprint||'').trim();if(fingerprint)return'fingerprint:'+fingerprint;const id=String(attachment.id||'').trim();return id?'id:'+id:'';}getIssueAttachmentReferences(issue,includePending){const result=[];if(!issue)return result;result.push(...(Array.isArray(issue.attachments)?issue.attachments:[]));for(const comment of Array.isArray(issue.comments)?issue.comments:[]){result.push(...(Array.isArray(comment.attachments)?comment.attachments:[]));}if(includePending)result.push(...this.pendingCommentAttachments);return result;}hasDuplicateAttachment(issue,attachment,includePending){const key=this.getAttachmentReferenceKey(attachment);if(!key)return false;return this.getIssueAttachmentReferences(issue,includePending).some(item=>this.getAttachmentReferenceKey(item)===key);}dedupeAttachmentArray(items,used){const result=[];for(const item of Array.isArray(items)?items:[]){const key=this.getAttachmentReferenceKey(item);if(!key||used.has(key))continue;used.add(key);result.push(item);}return result;}dedupeIssueAttachmentReferences(issue){if(!issue)return;const used=new Set();issue.attachments=this.dedupeAttachmentArray(issue.attachments,used);for(const comment of Array.isArray(issue.comments)?issue.comments:[]){comment.attachments=this.dedupeAttachmentArray(comment.attachments,used);}}filterImageFiles(fileList,availableSlots){return Array.from(fileList||[]).filter(file=>file&&String(file.type||'').startsWith('image/')&&Number(file.size||0)<=20*1024*1024).slice(0,Math.min(10,Math.max(0,availableSlots)));}async addImageFiles(fileList){const issue=this.getSelectedIssue();if(!issue||!fileList||!fileList.length)return;const availableSlots=Math.max(0,MAX_ISSUE_IMAGES-this.getIssueAttachmentCount(issue));if(!availableSlots){alert('Esta incidencia alcanzó el límite local de '+MAX_ISSUE_IMAGES+' imágenes.');return;}const files=this.filterImageFiles(fileList,availableSlots);if(!files.length){alert('Selecciona imágenes válidas de hasta 20 MB cada una.');return;}this.setAttachmentBusy(true,'Optimizando imágenes...');try{let added=0;for(const file of files){const metadata=await this.attachments.saveFile(issue.id,file,'upload');if(this.hasDuplicateAttachment(issue,metadata,true)){await this.attachments.delete(metadata.id).catch(()=>{});continue;}issue.attachments.push(metadata);added++;}if(added){this.addHistory(issue,'Evidencias generales agregadas',added+' imagen(es) optimizada(s).');issue.updatedAt=new Date().toISOString();if(!this.draft)this.persistSoon();}this.renderAttachments(issue);this.renderHistory(issue);}catch(error){console.error(error);alert('No se pudieron agregar todas las imágenes:\n'+(error.message||error));}finally{this.setAttachmentBusy(false);}}async captureViewerEvidence(){const issue=this.getSelectedIssue();if(!issue)return;if(this.getIssueAttachmentCount(issue)>=MAX_ISSUE_IMAGES){alert('Esta incidencia alcanzó el límite local de '+MAX_ISSUE_IMAGES+' imágenes.');return;}this.setAttachmentBusy(true,'Capturando visor...');try{const metadata=await this.attachments.captureViewer(window.__exportModelViewer,issue.id);if(this.hasDuplicateAttachment(issue,metadata,true)){await this.attachments.delete(metadata.id).catch(()=>{});}else{issue.attachments.push(metadata);issue.viewSnapshot=this.visualization.captureViewState()||issue.viewSnapshot;this.addHistory(issue,'Captura general agregada','Se guardó una nueva evidencia vinculada a la vista 3D.');issue.updatedAt=new Date().toISOString();if(!this.draft)this.persistSoon();}this.renderAttachments(issue);this.renderHistory(issue);}catch(error){console.error(error);alert('No se pudo capturar el visor:\n'+(error.message||error));}finally{this.setAttachmentBusy(false);}}async addCommentImageFiles(fileList){const issue=this.getSelectedIssue();if(!issue||!fileList||!fileList.length)return;const availableSlots=Math.max(0,MAX_ISSUE_IMAGES-this.getIssueAttachmentCount(issue));if(!availableSlots){alert('Esta incidencia alcanzó el límite local de '+MAX_ISSUE_IMAGES+' imágenes.');return;}const files=this.filterImageFiles(fileList,availableSlots);if(!files.length){alert('Selecciona imágenes válidas de hasta 20 MB cada una.');return;}this.setCommentAttachmentBusy(true,'Optimizando...');try{for(const file of files){const metadata=await this.attachments.saveFile(issue.id,file,'comment-upload');if(this.hasDuplicateAttachment(issue,metadata,true)){await this.attachments.delete(metadata.id).catch(()=>{});continue;}this.pendingCommentAttachments.push(metadata);}this.renderPendingCommentAttachments();}catch(error){console.error(error);alert('No se pudieron preparar las imágenes del comentario:\n'+(error.message||error));}finally{this.setCommentAttachmentBusy(false);}}async captureCommentEvidence(){const issue=this.getSelectedIssue();if(!issue)return;if(this.getIssueAttachmentCount(issue)>=MAX_ISSUE_IMAGES){alert('Esta incidencia alcanzó el límite local de '+MAX_ISSUE_IMAGES+' imágenes.');return;}this.setCommentAttachmentBusy(true,'Capturando...');try{const metadata=await this.attachments.captureViewer(window.__exportModelViewer,issue.id,{kind:'comment-viewer-capture',name:'comentario-vista-'+new Date().toISOString().replace(/[:.]/g,'-')+'.webp'});if(this.hasDuplicateAttachment(issue,metadata,true)){await this.attachments.delete(metadata.id).catch(()=>{});}else{this.pendingCommentAttachments.push(metadata);}this.renderPendingCommentAttachments();}catch(error){console.error(error);alert('No se pudo capturar la vista para el comentario:\n'+(error.message||error));}finally{this.setCommentAttachmentBusy(false);}}setAttachmentBusy(busy,text){if(this.nodes.captureButton){this.nodes.captureButton.disabled=!!busy;this.nodes.captureButton.textContent=busy?(text||'Procesando...'):'Capturar vista';}if(this.nodes.imageInput)this.nodes.imageInput.disabled=!!busy;}setCommentAttachmentBusy(busy,text){if(this.nodes.commentCaptureButton){this.nodes.commentCaptureButton.disabled=!!busy;this.nodes.commentCaptureButton.textContent=busy?(text||'Procesando...'):'Capturar';}if(this.nodes.commentImageInput)this.nodes.commentImageInput.disabled=!!busy;if(this.nodes.addCommentButton)this.nodes.addCommentButton.disabled=!!busy;}async removeAttachment(issue,attachmentId){if(!issue)return;issue.attachments=issue.attachments.filter(item=>item.id!==attachmentId);try{await this.attachments.delete(attachmentId);}catch(error){console.warn(error);}this.addHistory(issue,'Evidencia general eliminada','Se retiró una imagen de la incidencia.');if(!this.draft)this.persistSoon();this.renderAttachments(issue);this.renderHistory(issue);}async removePendingCommentAttachment(attachmentId){this.pendingCommentAttachments=this.pendingCommentAttachments.filter(item=>item.id!==attachmentId);if(!this.editingCommentOriginalAttachmentIds.has(attachmentId)){try{await this.attachments.delete(attachmentId);}catch(_){}}this.renderPendingCommentAttachments();}clearPendingCommentAttachments(deleteRecords){const items=this.pendingCommentAttachments.splice(0);const protectedIds=new Set(this.editingCommentOriginalAttachmentIds);this.revokeUrlMap(this.pendingUrls);if(this.nodes.commentPending)this.nodes.commentPending.innerHTML='';if(deleteRecords){for(const item of items){if(!protectedIds.has(item.id))this.attachments.delete(item.id).catch(()=>{});}}}async renderPendingCommentAttachments(){const container=this.nodes.commentPending;if(!container||this.activeMenu!=='details')return;const token=++this.pendingRenderToken;this.revokeUrlMap(this.pendingUrls);container.innerHTML='';for(const attachment of this.pendingCommentAttachments){if(token!==this.pendingRenderToken)return;const card=document.createElement('div');card.className='issue-pending-evidence';const image=document.createElement('img');image.alt=attachment.name||'Imagen para comentario';const remove=document.createElement('button');remove.type='button';remove.className='issues-icon-button danger';remove.textContent='×';remove.title='Quitar imagen del comentario';remove.addEventListener('click',()=>this.removePendingCommentAttachment(attachment.id));card.append(image,remove);container.appendChild(card);try{const record=await this.attachments.getRecord(attachment.id);const blob=record&&(record.thumbnailBlob||record.blob);if(blob&&token===this.pendingRenderToken&&card.isConnected){const url=URL.createObjectURL(blob);this.pendingUrls.set(attachment.id,url);image.src=url;image.addEventListener('click',()=>this.previewAttachment(attachment.id));}}catch(error){console.warn(error);}if(token!==this.pendingRenderToken)return;}}async renderAttachments(issue){const container=this.nodes.attachmentsList;const current=this.getSelectedIssue();if(!container||this.activeMenu!=='details'||!issue||!current||current.id!==issue.id)return;const renderToken=++this.attachmentRenderToken;const issueId=issue&&issue.id;this.revokeUrlMap(this.attachmentUrls);container.innerHTML='';const items=issue&&Array.isArray(issue.attachments)?issue.attachments:[];if(!items.length){const empty=document.createElement('div');empty.className='issues-subtle';empty.textContent='Sin imágenes generales adjuntas.';container.appendChild(empty);return;}for(const attachment of items){if(renderToken!==this.attachmentRenderToken)return;const card=document.createElement('div');card.className='issue-attachment-card';const image=document.createElement('img');image.alt=attachment.name||'Evidencia de incidencia';image.loading='lazy';const info=document.createElement('div');info.className='issue-attachment-info';const name=document.createElement('strong');name.textContent=attachment.kind==='viewer-capture'?'Captura del visor':(attachment.name||'Imagen');const size=document.createElement('span');size.textContent=this.formatFileSize(attachment.size);info.append(name,size);const remove=document.createElement('button');remove.type='button';remove.className='issues-icon-button danger';remove.textContent='×';remove.title='Eliminar evidencia';remove.addEventListener('click',()=>this.removeAttachment(issue,attachment.id));card.append(image,info,remove);container.appendChild(card);try{const record=await this.attachments.getRecord(attachment.id);const blob=record&&(record.thumbnailBlob||record.blob);if(blob&&card.isConnected&&renderToken===this.attachmentRenderToken&&this.getSelectedIssue()&&this.getSelectedIssue().id===issueId){const url=URL.createObjectURL(blob);this.attachmentUrls.set(attachment.id,url);image.src=url;image.addEventListener('click',()=>this.previewAttachment(attachment.id));}}catch(error){console.warn('No se pudo cargar una miniatura.',error);}if(renderToken!==this.attachmentRenderToken)return;}}revokeUrlMap(map){for(const url of map.values())URL.revokeObjectURL(url);map.clear();}clearAllImageUrls(){this.revokeUrlMap(this.attachmentUrls);this.revokeUrlMap(this.commentUrls);this.revokeUrlMap(this.historyUrls);this.revokeUrlMap(this.pendingUrls);}isImagePreviewOpen(){return!!(this.nodes.preview&&!this.nodes.preview.classList.contains('hidden'));}applyImagePreviewTransform(){if(!this.nodes.previewImage)return;this.nodes.previewImage.style.transform='translate('+this.previewOffsetX+'px, '+this.previewOffsetY+'px) scale('+this.previewScale+')';if(this.nodes.previewZoomLabel)this.nodes.previewZoomLabel.textContent=Math.round(this.previewScale*100)+'%';if(this.nodes.previewViewport)this.nodes.previewViewport.classList.toggle('zoomed',this.previewScale>1.001);}resetImagePreviewTransform(){this.previewScale=1;this.previewOffsetX=0;this.previewOffsetY=0;this.previewDragging=false;this.previewPointerId=null;this.applyImagePreviewTransform();}zoomImagePreview(delta){if(!this.isImagePreviewOpen())return;const next=Math.min(6,Math.max(0.25,Number((this.previewScale+Number(delta||0)).toFixed(2))));this.previewScale=next;if(next<=1){this.previewOffsetX=0;this.previewOffsetY=0;}this.applyImagePreviewTransform();}startImagePreviewDrag(event){if(!this.isImagePreviewOpen()||!event||event.button!==0||this.previewScale<=1)return;this.previewDragging=true;this.previewPointerId=event.pointerId;this.previewLastX=event.clientX;this.previewLastY=event.clientY;if(this.nodes.previewViewport&&this.nodes.previewViewport.setPointerCapture){try{this.nodes.previewViewport.setPointerCapture(event.pointerId);}catch(_){}}event.preventDefault();}moveImagePreviewDrag(event){if(!this.previewDragging||!event||event.pointerId!==this.previewPointerId)return;this.previewOffsetX+=event.clientX-this.previewLastX;this.previewOffsetY+=event.clientY-this.previewLastY;this.previewLastX=event.clientX;this.previewLastY=event.clientY;this.applyImagePreviewTransform();event.preventDefault();}endImagePreviewDrag(event){if(!this.previewDragging||(event&&event.pointerId!==this.previewPointerId))return;this.previewDragging=false;this.previewPointerId=null;if(this.nodes.previewViewport&&event&&this.nodes.previewViewport.releasePointerCapture){try{this.nodes.previewViewport.releasePointerCapture(event.pointerId);}catch(_){}}}handleImagePreviewKeydown(event){if(!this.isImagePreviewOpen()||!event)return;if(event.key==='Escape'){event.preventDefault();this.closeImagePreview();}else if(event.key==='+'||event.key==='='){event.preventDefault();this.zoomImagePreview(0.25);}else if(event.key==='-'){event.preventDefault();this.zoomImagePreview(-0.25);}else if(event.key==='0'){event.preventDefault();this.resetImagePreviewTransform();}}async previewAttachment(id){this.closeImagePreview();const record=await this.attachments.getRecord(id);if(!record||!record.blob||!this.nodes.preview||!this.nodes.previewImage)return;this.previewUrl=URL.createObjectURL(record.blob);this.nodes.previewImage.src=this.previewUrl;this.resetImagePreviewTransform();this.nodes.preview.classList.remove('hidden');if(this.nodes.previewClose)this.nodes.previewClose.focus({preventScroll:true});}closeImagePreview(){this.endImagePreviewDrag();if(this.previewUrl)URL.revokeObjectURL(this.previewUrl);this.previewUrl='';this.resetImagePreviewTransform();if(this.nodes.previewImage)this.nodes.previewImage.removeAttribute('src');if(this.nodes.preview)this.nodes.preview.classList.add('hidden');}updateCommentComposerMode(){const editing=!!this.editingCommentId;if(this.nodes.addCommentButton)this.nodes.addCommentButton.textContent=editing?'Guardar cambios':'Agregar comentario';if(this.nodes.cancelCommentEditButton)this.nodes.cancelCommentEditButton.classList.toggle('hidden',!editing);if(this.nodes.commentInput){this.nodes.commentInput.placeholder=editing?'Modifica el comentario o sus imágenes asociadas...':'Agregar comentario y, opcionalmente, imágenes asociadas...';}}async startEditComment(commentId){const issue=this.getSelectedIssue();const comment=issue&&Array.isArray(issue.comments)?issue.comments.find(item=>item.id===commentId):null;if(!comment)return;const hasUnsaved=!this.editingCommentId&&((this.nodes.commentInput&&this.nodes.commentInput.value.trim())||this.pendingCommentAttachments.length);if(hasUnsaved&&!await bimConfirm('Se descartará el comentario que todavía no has agregado. ¿Continuar?',{type:'warning',acceptText:'Continuar'}))return;this.clearPendingCommentAttachments(true);this.editingCommentId=comment.id;this.editingCommentOriginalAttachmentIds=new Set((comment.attachments||[]).map(item=>item.id));this.pendingCommentAttachments=(comment.attachments||[]).slice();if(this.nodes.commentInput)this.nodes.commentInput.value=comment.text||'';this.updateCommentComposerMode();this.renderPendingCommentAttachments();if(this.nodes.commentInput){this.nodes.commentInput.scrollIntoView({behavior:'smooth',block:'center'});this.nodes.commentInput.focus({preventScroll:true});}}cancelCommentEdit(silent){this.clearPendingCommentAttachments(true);this.editingCommentId='';this.editingCommentOriginalAttachmentIds=new Set();if(this.nodes.commentInput)this.nodes.commentInput.value='';this.updateCommentComposerMode();if(!silent)this.renderPendingCommentAttachments();}collectReferencedAttachmentIds(issue){const result=new Set();const add=values=>{for(const item of Array.isArray(values)?values:[]){const id=String(item&&item.id||'');if(id)result.add(id);}};if(!issue)return result;add(issue.attachments);for(const comment of Array.isArray(issue.comments)?issue.comments:[])add(comment&&comment.attachments);for(const entry of Array.isArray(issue.history)?issue.history:[])add(entry&&entry.attachments);return result;}async deleteComment(commentId){const issue=this.getSelectedIssue();if(!issue||!Array.isArray(issue.comments))return false;this.sanitizeIssueInPlace(issue);const index=issue.comments.findIndex(item=>item.id===commentId);if(index<0)return false;const comment=issue.comments[index];const preview=String(comment.text||'').trim();const evidenceCount=Array.isArray(comment.attachments)?comment.attachments.length:0;const label=preview?(preview.length>120?preview.slice(0,117)+'...':preview):(evidenceCount?'Comentario con '+evidenceCount+' imagen(es).':'Comentario sin texto.');const confirmed=await bimConfirm('¿Eliminar este comentario?\n\n'+label+(evidenceCount?'\n\nTambién se retirarán sus imágenes asociadas cuando no estén utilizadas en otra parte de la incidencia.':''),{danger:true,acceptText:'Eliminar'});if(!confirmed)return false;const candidateAttachmentIds=new Set();for(const item of comment.attachments||[])if(item&&item.id)candidateAttachmentIds.add(String(item.id));const relatedHistory=(issue.history||[]).filter(entry=>String(entry&&entry.commentId||'')===String(comment.id));for(const entry of relatedHistory){for(const item of entry.attachments||[])if(item&&item.id)candidateAttachmentIds.add(String(item.id));}issue.comments.splice(index,1);issue.history=(issue.history||[]).filter(entry=>String(entry&&entry.commentId||'')!==String(comment.id));if(this.editingCommentId===comment.id)this.cancelCommentEdit(true);const now=new Date().toISOString();issue.updatedAt=now;issue.revision=Math.max(1,Number(issue.revision)||1)+(this.draft?0:1);if(!this.draft)this.addHistory(issue,'Comentario eliminado',label);this.sanitizeIssueInPlace(issue);const referenced=this.collectReferencedAttachmentIds(issue);for(const id of candidateAttachmentIds){if(!referenced.has(id)){try{await this.attachments.delete(id);}catch(error){console.warn('No se pudo eliminar la imagen del comentario.',error);}}}if(this.attachments.registerSharedReferences)this.attachments.registerSharedReferences(this.issues);if(!this.draft)this.persistSoon();await this.renderComments(issue);await this.renderHistory(issue);this.renderEditor(issue,!!this.draft);return true;}addComment(silent){if(this.commentMutationInProgress)return false;const issue=this.getSelectedIssue();const input=this.nodes.commentInput;if(!issue||!input)return false;const text=String(input.value||'').trim();const evidence=utils.dedupeAttachments(this.pendingCommentAttachments.slice());if(!text&&!evidence.length)return false;const attachmentSignature=evidence.map(item=>item.fingerprint||item.id).sort().join('|');const mutationSignature=[this.editingCommentId||'new',utils.normalizeText(text),attachmentSignature].join('::');const nowMs=Date.now();if(mutationSignature===this.lastCommentMutationSignature&&nowMs-this.lastCommentMutationAt<1500)return false;this.commentMutationInProgress=true;try{const now=new Date(nowMs).toISOString();let comment;let changed=true;if(this.editingCommentId){comment=issue.comments.find(item=>item.id===this.editingCommentId);if(!comment){this.cancelCommentEdit();return false;}const previousText=String(comment.text||'');const previousIds=utils.dedupeAttachments(comment.attachments).map(item=>item.fingerprint||item.id).sort().join('|');const nextIds=evidence.map(item=>item.fingerprint||item.id).sort().join('|');changed=previousText!==text||previousIds!==nextIds;if(changed){comment.text=text;comment.attachments=evidence;comment.updatedAt=now;const detail=text?(text.length>100?text.slice(0,97)+'...':text):'Se modificaron las evidencias del comentario.';this.addHistory(issue,'Comentario modificado',detail,{commentId:comment.id,attachments:evidence});}}else{comment=utils.normalizeComment({id:utils.createId('comment-'),text,author:'Usuario local',createdAt:now,updatedAt:now,attachments:evidence});issue.comments.push(comment);const summary=text?(text.length>100?text.slice(0,97)+'...':text):'Comentario con evidencia gráfica.';this.addHistory(issue,'Comentario agregado',summary,{commentId:comment.id,attachments:evidence});}this.sanitizeIssueInPlace(issue);if(changed)issue.updatedAt=now;input.value='';this.pendingCommentAttachments=[];this.editingCommentId='';this.editingCommentOriginalAttachmentIds=new Set();this.revokeUrlMap(this.pendingUrls);if(this.nodes.commentPending)this.nodes.commentPending.innerHTML='';this.updateCommentComposerMode();this.lastCommentMutationSignature=mutationSignature;this.lastCommentMutationAt=nowMs;if(changed&&!this.draft)this.persistSoon();if(!silent){this.renderComments(issue);this.renderHistory(issue);}return true;}finally{this.commentMutationInProgress=false;}}async appendEvidenceGallery(container,attachments,urlMap,token,tokenGetter){const items=Array.isArray(attachments)?attachments:[];if(!items.length||token!==tokenGetter())return;const gallery=document.createElement('div');gallery.className='issue-evidence-gallery';container.appendChild(gallery);for(const attachment of items){if(token!==tokenGetter())return;const image=document.createElement('img');image.className='issue-evidence-thumb';image.loading='lazy';image.alt=attachment.name||'Evidencia vinculada';gallery.appendChild(image);try{const record=await this.attachments.getRecord(attachment.id);const blob=record&&(record.thumbnailBlob||record.blob);if(blob&&image.isConnected&&token===tokenGetter()){const key=attachment.id+':'+urlMap.size;const url=URL.createObjectURL(blob);urlMap.set(key,url);image.src=url;image.addEventListener('click',()=>this.previewAttachment(attachment.id));}}catch(error){console.warn(error);}if(token!==tokenGetter())return;}}async renderComments(issue){if(issue)this.sanitizeIssueInPlace(issue);const container=this.nodes.commentsList;const current=this.getSelectedIssue();if(!container||this.activeMenu!=='details'||!issue||!current||current.id!==issue.id)return;const token=++this.commentRenderToken;this.revokeUrlMap(this.commentUrls);container.innerHTML='';const comments=issue&&Array.isArray(issue.comments)?issue.comments.slice().reverse():[];if(!comments.length){const empty=document.createElement('div');empty.className='issues-subtle';empty.textContent='Sin comentarios.';container.appendChild(empty);return;}for(const comment of comments){if(token!==this.commentRenderToken)return;const card=document.createElement('div');card.className='issue-comment';const header=document.createElement('div');header.className='issue-comment-header';const meta=document.createElement('div');meta.className='issue-comment-meta';const evidenceCount=Array.isArray(comment.attachments)?comment.attachments.length:0;const modified=comment.updatedAt&&comment.updatedAt!==comment.createdAt?' · editado '+utils.formatDate(comment.updatedAt,true):'';meta.textContent=(comment.author||'Usuario')+' · '+utils.formatDate(comment.createdAt,true)+modified+(evidenceCount?' · '+evidenceCount+' imagen(es)':'');const actions=document.createElement('div');actions.className='issue-comment-card-actions';const edit=document.createElement('button');edit.type='button';edit.className='issue-comment-edit-button';edit.textContent='Editar';edit.addEventListener('click',()=>this.startEditComment(comment.id));const remove=document.createElement('button');remove.type='button';remove.className='issue-comment-delete-button';remove.textContent='−';remove.title='Eliminar comentario';remove.setAttribute('aria-label','Eliminar comentario');remove.addEventListener('click',()=>this.deleteComment(comment.id));actions.append(edit,remove);header.append(meta,actions);card.appendChild(header);if(comment.text){const text=document.createElement('div');text.className='issue-comment-text';text.textContent=comment.text;card.appendChild(text);}container.appendChild(card);await this.appendEvidenceGallery(card,comment.attachments,this.commentUrls,token,()=>this.commentRenderToken);if(token!==this.commentRenderToken)return;}}getFilteredIssues(){const query=utils.normalizeText(this.search);const assigneeFilter=utils.normalizeText(this.assigneeFilter);return this.issues.filter(issue=>{if(this.statusFilter!=='all'&&issue.status!==this.statusFilter)return false;if(this.priorityFilter!=='all'&&issue.priority!==this.priorityFilter)return false;if(this.labelFilter!=='all'&&issue.labelId!==this.labelFilter)return false;if(this.assigneeFilter!=='all'&&utils.normalizeText(issue.assignee)!==assigneeFilter)return false;if(!query)return true;const haystack=utils.normalizeText([issue.number,issue.label,issue.title,issue.description,issue.assignee,issue.type,(issue.comments||[]).map(comment=>comment.text).join(' ')].join(' '));return haystack.includes(query);});}getVisibleMarkerIssues(){return this.getFilteredIssues();}renderAll(){this.populateDynamicFilters();this.renderStats();this.renderList();const issue=this.getSelectedIssue();if(this.activeMenu==='details')this.renderEditor(issue,!!this.draft);else if(this.activeMenu==='history')this.renderHistory(issue);else if(this.activeMenu==='settings'){this.renderTeam();this.renderLabelsManager();}this.updateMenuAvailability();this.switchMenu(this.activeMenu||'list',{keepPanelState:true,skipRender:true});this.visualization.refreshMarkers();}renderStats(){const node=this.nodes.stats;if(!node)return;const counts={total:this.issues.length,active:this.issues.filter(issue=>issue.status==='active'||issue.status==='reopened').length,review:this.issues.filter(issue=>issue.status==='review').length,resolved:this.issues.filter(issue=>issue.status==='resolved'||issue.status==='approved').length};node.innerHTML='';[['Total',counts.total],['Activas',counts.active],['Revisión',counts.review],['Cerradas',counts.resolved]].forEach(([label,value])=>{const card=document.createElement('div');card.className='issues-stat-card';const strong=document.createElement('strong');strong.textContent=String(value);const span=document.createElement('span');span.textContent=label;card.append(strong,span);node.appendChild(card);});}renderList(){const container=this.nodes.list;if(!container)return;container.innerHTML='';const items=this.getFilteredIssues().slice().sort((a,b)=>String(b.updatedAt).localeCompare(String(a.updatedAt)));if(this.nodes.listEmpty)this.nodes.listEmpty.classList.toggle('hidden',items.length>0);for(const issue of items){const row=document.createElement('div');row.className='issue-list-row';const card=document.createElement('button');card.type='button';card.className='issue-list-card';card.classList.toggle('active',issue.id===this.selectedIssueId);const status=utils.getStatus(issue.status);card.style.setProperty('--issue-status-color',status.color);const top=document.createElement('div');top.className='issue-list-card-top';const number=document.createElement('strong');number.textContent=issue.number||'SIN CÓDIGO';const badge=document.createElement('span');badge.className='issue-status-badge';badge.textContent=status.label;top.append(number,badge);const title=document.createElement('div');title.className='issue-list-title';title.textContent=issue.title;const meta=document.createElement('div');meta.className='issue-list-meta';const priority=utils.getPriority(issue.priority).label;meta.textContent=priority+(issue.assignee?' · '+issue.assignee:' · Sin responsable')+' · '+utils.formatDate(issue.updatedAt,false);card.append(top,title,meta);if(issue.label){const labels=document.createElement('div');labels.className='issue-list-labels';const tag=document.createElement('span');tag.className='issue-list-label';tag.textContent=issue.label;tag.style.setProperty('--issue-label-color',issue.labelColor||'#2563eb');labels.appendChild(tag);card.appendChild(labels);}card.addEventListener('click',()=>this.selectIssue(issue.id,true));const deleteButton=document.createElement('button');deleteButton.type='button';deleteButton.className='issue-list-delete-button';deleteButton.textContent='−';deleteButton.title='Eliminar incidencia';deleteButton.setAttribute('aria-label','Eliminar '+(issue.title||issue.label||'incidencia'));deleteButton.addEventListener('click',event=>{event.preventDefault();event.stopPropagation();this.deleteIssueFromList(issue.id);});row.append(card,deleteButton);container.appendChild(row);}}cancelIssueDetailRender(){this.issueDetailRenderGeneration=(this.issueDetailRenderGeneration||0)+1;if(this.issueDetailRenderFrame){const cancel=typeof cancelAnimationFrame==='function'?cancelAnimationFrame:clearTimeout;try{cancel(this.issueDetailRenderFrame);}catch(_){}this.issueDetailRenderFrame=0;}this.attachmentRenderToken++;this.commentRenderToken++;this.pendingRenderToken++;}scheduleIssueDetailRender(issue){if(!issue)return;const issueId=String(issue.id||'');const generation=(this.issueDetailRenderGeneration||0)+1;this.issueDetailRenderGeneration=generation;if(this.issueDetailRenderFrame){const cancel=typeof cancelAnimationFrame==='function'?cancelAnimationFrame:clearTimeout;try{cancel(this.issueDetailRenderFrame);}catch(_){}}const schedule=typeof requestAnimationFrame==='function'?requestAnimationFrame:callback=>setTimeout(callback,0);this.issueDetailRenderFrame=schedule(()=>{this.issueDetailRenderFrame=0;if(generation!==this.issueDetailRenderGeneration)return;const current=this.getSelectedIssue();if(!current||String(current.id||'')!==issueId||this.activeMenu!=='details')return;this.renderAttachments(current);this.renderPendingCommentAttachments();this.renderComments(current);});}renderEditor(issue,isDraft){const n=this.nodes;const visible=!!issue;if(n.editor)n.editor.classList.toggle('hidden',!visible);if(n.editorEmpty)n.editorEmpty.classList.toggle('hidden',visible);if(!visible){this.cancelIssueDetailRender();if(n.historyCount)n.historyCount.textContent='0';this.updateMenuAvailability();return;}if(n.editorTitle)n.editorTitle.textContent=isDraft?'Nueva incidencia':((issue.number||'')+' · Detalles');this.populateLabelOptions(issue);if(n.title)n.title.value=issue.title||'';if(n.description)n.description.value=issue.description||'';if(n.type)n.type.value=issue.type||utils.TYPES[0];if(n.priority)n.priority.value=issue.priority||'medium';if(n.status)n.status.value=issue.status||'active';this.updateStatusVisual();this.populateAssigneeOptions(issue);if(n.dueDate){n.dueDate.value=this.formatDueDateForEditor(issue.dueDate||'');this.lastValidDueDateText=n.dueDate.value;n.dueDate.setCustomValidity('');}if(n.dueDatePickerNative)n.dueDatePickerNative.value=issue.dueDate||'';if(n.linkedElements){const count=(issue.elementKeys||[]).length;n.linkedElements.textContent=issue.anchor?count+' elemento'+(count===1?'':'s')+' vinculado'+(count===1?'':'s')+' · marcador ubicado':'Marcador pendiente de ubicar';n.linkedElements.classList.toggle('ready',!!issue.anchor);}if(n.saveButton)n.saveButton.textContent=isDraft?'Crear incidencia':'Guardar modificaciones';if(n.historyCount)n.historyCount.textContent=String(Array.isArray(issue.history)?issue.history.length:0);this.updateCommentComposerMode();this.scheduleIssueDetailRender(issue);this.updateMenuAvailability();}addHistory(issue,action,detail,options){if(!issue)return;if(!Array.isArray(issue.history))issue.history=[];const opts=options&&typeof options==='object'?options:{};const createdAt=new Date().toISOString();const attachments=utils.dedupeAttachments(opts.attachments);const attachmentSignature=attachments.map(item=>item.fingerprint||item.id).sort().join('|');const recentDuplicate=issue.history.slice(-8).some(entry=>{const delta=Math.abs(new Date(createdAt).getTime()-new Date(entry.createdAt||0).getTime());const currentSignature=utils.dedupeAttachments(entry.attachments).map(item=>item.fingerprint||item.id).sort().join('|');return delta<=1500&&String(entry.action||'')===String(action||'Actualización')&&String(entry.detail||'')===String(detail||'')&&String(entry.commentId||'')===String(opts.commentId||'')&&currentSignature===attachmentSignature;});if(recentDuplicate)return;issue.history.push(utils.normalizeHistoryEntry({id:utils.createId('history-'),action:String(action||'Actualización'),detail:String(detail||''),author:'Usuario local',createdAt,commentId:String(opts.commentId||''),attachments}));}describeIssueAssignment(issue){const parts=['Código: '+issue.number,'Etiqueta: '+(issue.label||'sin etiqueta'),utils.getStatus(issue.status).label,utils.getPriority(issue.priority).label];if(issue.assignee)parts.push('Responsable: '+issue.assignee);return parts.join(' · ');}describeChanges(previous,issue){if(!previous||!issue)return'';const changes=[];if(previous.status!==issue.status)changes.push('Estado: '+utils.getStatus(previous.status).label+' → '+utils.getStatus(issue.status).label);if(previous.priority!==issue.priority)changes.push('Prioridad: '+utils.getPriority(previous.priority).label+' → '+utils.getPriority(issue.priority).label);if(previous.assignee!==issue.assignee)changes.push('Responsable: '+(previous.assignee||'sin asignar')+' → '+(issue.assignee||'sin asignar'));if(previous.dueDate!==issue.dueDate)changes.push('Fecha límite modificada');if(previous.title!==issue.title||previous.description!==issue.description||previous.type!==issue.type)changes.push('Información general modificada');if(previous.labelId!==issue.labelId||previous.label!==issue.label||previous.labelColor!==issue.labelColor)changes.push('Etiqueta modificada: '+(previous.label||'sin etiqueta')+' → '+(issue.label||'sin etiqueta'));return changes.join(' · ');}async renderHistory(issue){if(issue)this.sanitizeIssueInPlace(issue);if(this.activeMenu!=='history'){if(this.nodes.historyCount)this.nodes.historyCount.textContent=String(issue&&Array.isArray(issue.history)?issue.history.length:0);return;}const container=this.nodes.historyList;if(!container)return;const token=++this.historyRenderToken;this.revokeUrlMap(this.historyUrls);container.innerHTML='';const visible=!!issue;if(this.nodes.historyEmpty)this.nodes.historyEmpty.classList.toggle('hidden',visible);if(this.nodes.historyContent)this.nodes.historyContent.classList.toggle('hidden',!visible);if(!visible){if(this.nodes.historyCount)this.nodes.historyCount.textContent='0';if(this.nodes.historyTitle)this.nodes.historyTitle.textContent='Incidencia seleccionada';return;}if(this.nodes.historyTitle){this.nodes.historyTitle.textContent=(issue.number||'SIN CÓDIGO')+' · '+(issue.title||'Incidencia sin título');}const history=Array.isArray(issue.history)?issue.history.slice().reverse():[];if(this.nodes.historyCount)this.nodes.historyCount.textContent=String(history.length);if(!history.length){const empty=document.createElement('div');empty.className='issues-subtle';empty.textContent='La actividad aparecerá después de crear o modificar la incidencia.';container.appendChild(empty);return;}for(const entry of history){if(token!==this.historyRenderToken)return;const card=document.createElement('div');card.className='issue-history-entry';const dot=document.createElement('span');dot.className='issue-history-dot';const body=document.createElement('div');const title=document.createElement('strong');title.textContent=entry.action||'Actualización';const detail=document.createElement('div');detail.className='issue-history-detail';detail.textContent=entry.detail||'';const meta=document.createElement('div');meta.className='issue-history-meta';const evidenceCount=Array.isArray(entry.attachments)?entry.attachments.length:0;meta.textContent=(entry.author||'Usuario')+' · '+utils.formatDate(entry.createdAt,true)+(evidenceCount?' · '+evidenceCount+' evidencia(s) vinculada(s)':'');body.append(title);if(entry.detail)body.append(detail);body.append(meta);card.append(dot,body);container.appendChild(card);await this.appendEvidenceGallery(body,entry.attachments,this.historyUrls,token,()=>this.historyRenderToken);if(token!==this.historyRenderToken)return;}}formatFileSize(bytes){const value=Number(bytes)||0;if(value>=1048576)return(value/1048576).toFixed(1)+' MB';if(value>=1024)return Math.round(value/1024)+' KB';return value+' B';}persistSoon(){clearTimeout(this.persistTimer);this.persistTimer=setTimeout(()=>this.flushPersistence(),250);clearTimeout(this.sharedPersistTimer);this.sharedPersistTimer=setTimeout(()=>this.writeSharedState(false),420);if(eventBus)eventBus.emit('issues:sync-required',{identity:this.identity,count:this.issues.length});}writeSharedState(forceDirty){clearTimeout(this.sharedPersistTimer);this.sharedPersistTimer=0;const service=this.cacheService;const identity=this.identity;if(!identity)return Promise.resolve(false);if(!service||(!service.saveIssuesBundle&&!service.saveIssuesState&&!service.writeStateFile))return Promise.resolve(true);this.sanitizeAllIssues();const previousPending=this.sharedPersistPendingRequest;this.sharedPersistPendingRequest={service,identity,payload:this.buildSharedStatePayload(),attachmentReader:this.attachments.createScopedReader?this.attachments.createScopedReader(identity):this.attachments,forceDirty:!!forceDirty||!!(previousPending&&previousPending.forceDirty)};if(this.sharedPersistInFlight&&this.sharedPersistPromise)return this.sharedPersistPromise;this.sharedPersistInFlight=true;const drain=async()=>{let lastResult=true;while(this.sharedPersistPendingRequest){const request=this.sharedPersistPendingRequest;this.sharedPersistPendingRequest=null;try{const currentService=request.service;if(currentService.saveIssuesBundle){lastResult=await currentService.saveIssuesBundle(request.payload,request.attachmentReader,{forceDirty:!!request.forceDirty,changeCount:1});}else if(currentService.saveIssuesState){lastResult=await currentService.saveIssuesState(request.payload,{forceDirty:!!request.forceDirty,changeCount:1});}else{lastResult=await currentService.writeStateFile('issues_cache.json',{version:1,savedAt:new Date().toISOString(),state:request.payload},{forceDirty:!!request.forceDirty,changeCount:1});}}catch(error){lastResult=false;console.warn('No se pudieron guardar las incidencias en el caché del proyecto.',error);if(window.BimPersistencePolicy)window.BimPersistencePolicy.notifyError('Incidencias',error);else if(eventBus)eventBus.emit('cache:write-error',{area:'Incidencias',error:error});}}return lastResult;};const promise=Promise.resolve().then(drain).finally(()=>{if(this.sharedPersistPromise===promise){this.sharedPersistInFlight=false;this.sharedPersistPromise=Promise.resolve(true);}});this.sharedPersistPromise=promise;return promise;}async flushSharedPersistence(forceDirty){clearTimeout(this.persistTimer);clearTimeout(this.sharedPersistTimer);this.persistTimer=0;this.sharedPersistTimer=0;if(!this.identity)return false;const localSaved=this.persistence.save(this.issues,this.team,this.labelsCatalog);if(!localSaved)return false;const sharedSaved=await this.writeSharedState(!!forceDirty);return sharedSaved!==false;}async reloadSharedBundle(){const service=this.cacheService;if(!service||!service.readIssuesBundle)return false;const payload=await service.readIssuesBundle({forceRemote:true});return payload?this.applyPersistedState(payload):false;}flushPersistence(){clearTimeout(this.persistTimer);clearTimeout(this.sharedPersistTimer);this.persistTimer=0;this.sharedPersistTimer=0;if(!this.identity)return false;this.sanitizeAllIssues();const localSaved=this.persistence.save(this.issues,this.team,this.labelsCatalog);this.writeSharedState(false);return localSaved;}emit(name,issue){if(eventBus)eventBus.emit(name,{issue:utils.normalizeIssue(issue),identity:this.identity});}}function init(){if(window.BimIssuesPanel||!document.getElementById('issuesPanel'))return;try{new IssuePanelController();}catch(error){console.error('No se pudo iniciar el panel de Incidencias.',error);}}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init);else init();})();