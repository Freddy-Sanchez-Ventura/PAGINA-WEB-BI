(function(){'use strict';function normalizeBaseUrl(value){const text=String(value||'').trim();if(!text)return'';return text.replace(/\/+$/,'');}function isLocalHostname(hostname){const value=String(hostname||'').toLowerCase();return value==='localhost'||value==='127.0.0.1'||value==='::1'||value==='[::1]';}const runtime=window.BIM_RUNTIME_CONFIG||{};const configuredBaseUrl=normalizeBaseUrl(runtime.aiApiBaseUrl);const local=isLocalHostname(window.location&&window.location.hostname);const forceConfigured=runtime.forceConfiguredApiOnLocalhost===true;const apiBaseUrl=local&&!forceConfigured?'':configuredBaseUrl;function url(pathname){const path='/'+String(pathname||'').replace(/^\/+/,'');return apiBaseUrl?apiBaseUrl+path:path;}function unavailableMessage(){if(local&&!forceConfigured){return'El servidor local de IA no está activo. Inicia la plataforma con INICIAR_PLATAFORMA_BIM_IA.bat o ejecuta npm start.';}if(apiBaseUrl){return'El Cloudflare Worker de IA no está disponible. Verifica la URL del Worker, su despliegue y la configuración CORS.';}return'Este alojamiento no expone el backend /api/ai. Si publicas el frontend en un hosting estático como GitHub Pages, configura BIM_AI_API_BASE_URL al generar el dist para apuntar al backend de producción.';}window.BimAiEndpoint=Object.freeze({apiBaseUrl,configuredBaseUrl,isLocal:local,mode:local&&!forceConfigured?'local-same-origin':(apiBaseUrl?'configured-production':'same-origin'),url,unavailableMessage});})();