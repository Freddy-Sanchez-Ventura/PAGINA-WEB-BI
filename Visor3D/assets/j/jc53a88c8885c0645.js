(function(){'use strict';class BimAiPanel{constructor(){this.catalog=new window.BimAiCatalog();this.engine=new window.BimAiQueryEngine(this.catalog);this.reportService=window.BimAiReportService?new window.BimAiReportService(this.catalog):null;this.reportAttachments=new Map();this.diagnostics=window.BimAiDiagnostics?new window.BimAiDiagnostics():null;this.providerId=sessionStorage.getItem('bim-ai-provider')||'gemini';this.pendingClarification=null;this.abortController=null;this.busy=false;this.proxyReady=null;this.proxyCapabilities=null;this.recognition=null;this.recognitionActive=false;this.recognitionWanted=false;this.speechRestartForEdit=false;this.microphoneStream=null;this.speechInterimText='';this.speechRenderedValue='';this.speechLastResultCount=0;this.speechIgnoreThroughIndex=-1;this.speechResultTranscripts=new Map();this.speechDiscardedTranscripts=new Map();this.readResponsesEnabled=sessionStorage.getItem('bim-ai-read-responses')==='1';this.activeSpeechUtterance=null;this.conversations=[];this.currentConversationId='';this.currentLastResultSnapshot=null;this.restoringConversation=false;this.usageStats=this.loadUsageStats();this.lastUsageModelByProvider={};this.nodes={};this.build();this.bind();this.syncProvider();this.checkProxyHealth();this.restoreHistory();window.BimAiPanel=this;}build(){const buttonHost=document.querySelector('.mobile-panel-buttons');if(buttonHost&&!document.getElementById('mobileAiPanelButton')){const button=document.createElement('button');button.id='mobileAiPanelButton';button.type='button';button.className='mobile-panel-button';button.textContent='IA';button.setAttribute('aria-expanded','false');button.setAttribute('aria-controls','aiPanel');buttonHost.appendChild(button);}const panel=document.createElement('aside');panel.id='aiPanel';panel.className='ai-panel hidden';panel.setAttribute('aria-hidden','true');panel.innerHTML=['<div id="aiPanelSplitter" class="ai-panel-splitter" role="separator" aria-orientation="vertical" aria-label="Cambiar ancho del Panel IA" title="Arrastra para cambiar el ancho"></div>','<header class="ai-header"><div><strong>Asistente IA</strong><small>Consultas BIM locales y controladas</small></div><button id="aiCloseButton" type="button" aria-label="Cerrar">×</button></header>','<details id="aiSettings" class="ai-settings"><summary><span>Conexión IA</span><small id="aiConnectionSummary">Sin configurar</small></summary>','<div class="ai-settings-body">','<label>Proveedor<select id="aiProviderSelect"><option value="gemini">Google Gemini</option><option value="openai">OpenAI</option><option value="nvidia">NVIDIA NIM</option></select></label>','<label>Modelo<select id="aiModelSelect"></select></label>','<label id="aiApiKeyLabel">API key<input id="aiApiKeyInput" type="password" autocomplete="off" placeholder="Se conserva solo durante esta sesión" /></label>','<div id="aiKeyNote" class="ai-key-note">La clave no se guarda en el proyecto ni se envía a los archivos BIM.</div>','<div class="ai-usage-summary"><strong id="aiUsageTitle">Consumo de esta IA</strong><span id="aiUsageDetail">Consultas: 0 · Entrada: 0 · Salida: 0</span><small id="aiUsageProviderTotal">Total del proveedor: 0 consultas</small></div>','</div>','</details>','<div class="ai-catalog"><span id="aiCatalogDot"></span><div><strong id="aiCatalogTitle">Catálogo en espera</strong><small id="aiCatalogDetail">Abre el panel con un proyecto cargado.</small></div></div>','<details id="aiConversations" class="ai-conversations"><summary><span>Conversaciones</span><small id="aiConversationCount">0</small></summary><div id="aiConversationList" class="ai-conversation-list"></div></details>','<div id="aiMessages" class="ai-messages" aria-live="polite"></div>','<div id="aiClarification" class="ai-clarification hidden"></div>','<div id="aiProgress" class="ai-progress hidden"><div><span id="aiProgressText">Procesando...</span><b id="aiProgressValue">0%</b></div><progress id="aiProgressBar" max="100" value="0"></progress></div>','<footer class="ai-compose"><textarea id="aiPromptInput" rows="3" placeholder="Ej.: Aísla las tuberías del Nivel 02 que contienen AF"></textarea><div><span class="ai-compose-tools"><button id="aiClearButton" class="ai-secondary" type="button">Nueva conversación</button><button id="aiDiagnosticButton" class="ai-secondary" type="button" title="Descargar el diagnóstico de interpretación y ejecución local">Diagnóstico</button></span><span class="ai-compose-actions"><button id="aiMicButton" class="ai-voice-button ai-mic-button" type="button" title="Dictar consulta" aria-label="Activar micrófono" aria-pressed="false"></button><button id="aiSpeakButton" class="ai-voice-button ai-speak-button" type="button" title="Leer las respuestas" aria-label="Activar lectura de respuestas" aria-pressed="false"></button><button id="aiSendButton" type="button">Enviar</button></span></div></footer>'].join('');document.body.appendChild(panel);this.nodes={panel,splitter:document.getElementById('aiPanelSplitter'),toggle:document.getElementById('mobileAiPanelButton'),close:document.getElementById('aiCloseButton'),provider:document.getElementById('aiProviderSelect'),model:document.getElementById('aiModelSelect'),key:document.getElementById('aiApiKeyInput'),keyLabel:document.getElementById('aiApiKeyLabel'),keyNote:document.getElementById('aiKeyNote'),summary:document.getElementById('aiConnectionSummary'),usageTitle:document.getElementById('aiUsageTitle'),usageDetail:document.getElementById('aiUsageDetail'),usageProviderTotal:document.getElementById('aiUsageProviderTotal'),catalogDot:document.getElementById('aiCatalogDot'),catalogTitle:document.getElementById('aiCatalogTitle'),catalogDetail:document.getElementById('aiCatalogDetail'),conversations:document.getElementById('aiConversations'),conversationCount:document.getElementById('aiConversationCount'),conversationList:document.getElementById('aiConversationList'),messages:document.getElementById('aiMessages'),clarification:document.getElementById('aiClarification'),progress:document.getElementById('aiProgress'),progressText:document.getElementById('aiProgressText'),progressValue:document.getElementById('aiProgressValue'),progressBar:document.getElementById('aiProgressBar'),prompt:document.getElementById('aiPromptInput'),mic:document.getElementById('aiMicButton'),speak:document.getElementById('aiSpeakButton'),send:document.getElementById('aiSendButton'),clear:document.getElementById('aiClearButton'),diagnostic:document.getElementById('aiDiagnosticButton')};this.nodes.provider.value=this.providerId;const savedWidth=Math.max(320,Math.min(900,Number(sessionStorage.getItem('bim-ai-panel-width'))||430));document.documentElement.style.setProperty('--ai-panel-width',savedWidth+'px');}bind(){this.nodes.toggle.addEventListener('click',()=>this.toggle());this.nodes.close.addEventListener('click',()=>this.toggle(false));this.nodes.provider.addEventListener('change',()=>{this.providerId=this.nodes.provider.value;sessionStorage.setItem('bim-ai-provider',this.providerId);this.syncProvider();});this.nodes.model.addEventListener('change',()=>{sessionStorage.setItem('bim-ai-model-'+this.providerId,this.nodes.model.value);this.lastUsageModelByProvider[this.providerId]=this.nodes.model.value;this.updateUsageSummary();});this.nodes.key.addEventListener('input',()=>{sessionStorage.setItem('bim-ai-key-'+this.providerId,this.nodes.key.value);this.updateConnectionSummary();});this.nodes.send.addEventListener('click',()=>this.send());this.nodes.prompt.addEventListener('keydown',event=>{if(event.key==='Enter'&&!event.shiftKey){event.preventDefault();this.send();}});this.nodes.prompt.addEventListener('input',()=>this.handlePromptEdited());this.nodes.clear.addEventListener('click',()=>this.newConversation());this.nodes.diagnostic.addEventListener('click',()=>this.downloadDiagnostics());this.nodes.mic.addEventListener('click',()=>this.toggleRecognition());this.nodes.speak.addEventListener('click',()=>this.toggleReadResponses());this.nodes.conversations.addEventListener('toggle',()=>{if(this.nodes.conversations.open)this.renderConversationList();});this.nodes.conversationList.addEventListener('click',event=>{const deleteButton=event.target&&event.target.closest?event.target.closest('[data-ai-conversation-delete]'):null;if(deleteButton){event.preventDefault();event.stopPropagation();this.deleteConversation(deleteButton.getAttribute('data-ai-conversation-delete'));return;}const openButton=event.target&&event.target.closest?event.target.closest('[data-ai-conversation-open]'):null;if(openButton)this.openConversation(openButton.getAttribute('data-ai-conversation-open'));});this.bindSplitter();if(!(window.SpeechRecognition||window.webkitSpeechRecognition)){this.nodes.mic.disabled=true;this.nodes.mic.title='El reconocimiento de voz no está disponible en este navegador.';}if(!('speechSynthesis'in window)||typeof window.SpeechSynthesisUtterance!=='function'){this.nodes.speak.disabled=true;this.nodes.speak.title='La lectura de respuestas no está disponible en este navegador.';}this.setMicrophoneUi(false);this.setReadResponsesUi(this.readResponsesEnabled);window.addEventListener('pagehide',()=>{this.releaseMicrophone();this.stopSpeaking();this.releaseReportAttachments();},{once:true});if(window.BimEventBus&&typeof window.BimEventBus.on==='function'){window.BimEventBus.on('project:opened',()=>this.handleProjectChanged());window.BimEventBus.on('project:closed',()=>this.handleProjectChanged());window.BimEventBus.on('project:structure-changed',()=>this.invalidateCatalog());}}toggle(force){const open=force==null?this.nodes.panel.classList.contains('hidden'):!!force;this.nodes.panel.classList.toggle('hidden',!open);this.nodes.panel.setAttribute('aria-hidden',open?'false':'true');this.nodes.toggle.classList.toggle('active',open);this.nodes.toggle.setAttribute('aria-expanded',open?'true':'false');document.body.classList.toggle('ai-panel-open',open);if(!open){this.stopRecognition();this.stopSpeaking();}if(open){this.checkProxyHealth();this.prepareCatalog();setTimeout(()=>this.nodes.prompt.focus(),30);}}bindSplitter(){const splitter=this.nodes.splitter;if(!splitter||splitter.__bimAiBound)return;splitter.__bimAiBound=true;let pointerId=null;let mouseDragging=false;const applyWidth=clientX=>{const maximum=Math.max(320,Math.min(900,window.innerWidth-280));const width=Math.max(320,Math.min(maximum,window.innerWidth-clientX));document.documentElement.style.setProperty('--ai-panel-width',Math.round(width)+'px');sessionStorage.setItem('bim-ai-panel-width',String(Math.round(width)));this.requestViewerResize('ai-panel-resize');};const move=event=>{if(pointerId==null||event.pointerId!==pointerId)return;applyWidth(event.clientX);event.preventDefault();};const stop=event=>{if(pointerId==null||(event&&event.pointerId!==pointerId))return;const activePointerId=pointerId;pointerId=null;try{splitter.releasePointerCapture(activePointerId);}catch(_){}splitter.classList.remove('dragging');document.body.style.cursor='';document.body.style.userSelect='';this.requestViewerResize('ai-panel-resize-end');};splitter.addEventListener('pointerdown',event=>{if(window.innerWidth<=1100)return;pointerId=event.pointerId;splitter.classList.add('dragging');document.body.style.cursor='col-resize';document.body.style.userSelect='none';try{splitter.setPointerCapture(pointerId);}catch(_){}});splitter.addEventListener('pointermove',move);splitter.addEventListener('pointerup',stop);splitter.addEventListener('pointercancel',stop);document.addEventListener('pointermove',move);document.addEventListener('pointerup',stop);document.addEventListener('pointercancel',stop);splitter.addEventListener('mousedown',event=>{if(event.button!==0||window.innerWidth<=1100)return;mouseDragging=true;splitter.classList.add('dragging');document.body.style.cursor='col-resize';document.body.style.userSelect='none';event.preventDefault();});document.addEventListener('mousemove',event=>{if(!mouseDragging)return;applyWidth(event.clientX);event.preventDefault();});document.addEventListener('mouseup',()=>{if(!mouseDragging)return;mouseDragging=false;splitter.classList.remove('dragging');document.body.style.cursor='';document.body.style.userSelect='';this.requestViewerResize('ai-panel-resize-end');});splitter.addEventListener('dblclick',()=>{document.documentElement.style.setProperty('--ai-panel-width','430px');sessionStorage.setItem('bim-ai-panel-width','430');this.requestViewerResize('ai-panel-resize-reset');});}requestViewerResize(reason){if(window.__exportModelRequestWorkspaceLayout)window.__exportModelRequestWorkspaceLayout(reason);const viewer=window.__exportModelViewer;if(viewer&&typeof viewer.resize==='function')viewer.resize({reason});}syncProvider(){const provider=window.BimAiProviders[this.providerId]||window.BimAiProviders.gemini;this.providerId=provider.id;this.nodes.provider.value=provider.id;this.nodes.model.innerHTML='';provider.models.forEach(model=>{const option=document.createElement('option');option.value=model;option.textContent=this.modelLabel(model);this.nodes.model.appendChild(option);});const savedModel=sessionStorage.getItem('bim-ai-model-'+provider.id);if(savedModel&&provider.models.includes(savedModel))this.nodes.model.value=savedModel;this.nodes.model.disabled=provider.id==='gemini';this.nodes.model.title=provider.id==='gemini'?'Gemini usa automáticamente 3.5 → 2.5 → 3.1 Flash-Lite.':'Modelo utilizado por el proveedor.';this.nodes.key.value=sessionStorage.getItem('bim-ai-key-'+provider.id)||'';this.updateKeyUi();this.updateConnectionSummary();this.updateUsageSummary();}loadUsageStats(){try{const parsed=JSON.parse(localStorage.getItem('bim-ai-usage-v1')||'{}');return parsed&&parsed.version===1&&parsed.providers&&typeof parsed.providers==='object'?parsed:{version:1,providers:{}};}catch(_){return{version:1,providers:{}};}}saveUsageStats(){try{localStorage.setItem('bim-ai-usage-v1',JSON.stringify(this.usageStats));}catch(_){}}emptyUsage(){return{queries:0,inputTokens:0,outputTokens:0,totalTokens:0,models:{}};}formatUsageNumber(value){return Math.max(0,Number(value)||0).toLocaleString('es-PE');}recordProviderUsage(response){if(!response)return;const providerId=String(response.providerId||this.providerId||'unknown');const model=String(response.model||this.nodes.model.value||'sin-modelo');const usage=response.usage||{};const providers=this.usageStats.providers||(this.usageStats.providers={});const providerStats=providers[providerId]||(providers[providerId]=this.emptyUsage());const models=providerStats.models||(providerStats.models={});const modelStats=models[model]||(models[model]=this.emptyUsage());const queries=Math.max(1,Number(usage.requests)||1);const inputTokens=Math.max(0,Number(usage.inputTokens)||0);const outputTokens=Math.max(0,Number(usage.outputTokens)||0);const totalTokens=Math.max(0,Number(usage.totalTokens)||inputTokens+outputTokens);[providerStats,modelStats].forEach(target=>{target.queries=Math.max(0,Number(target.queries)||0)+queries;target.inputTokens=Math.max(0,Number(target.inputTokens)||0)+inputTokens;target.outputTokens=Math.max(0,Number(target.outputTokens)||0)+outputTokens;target.totalTokens=Math.max(0,Number(target.totalTokens)||0)+totalTokens;target.updatedAt=new Date().toISOString();});this.lastUsageModelByProvider[providerId]=model;this.saveUsageStats();this.updateUsageSummary(model);}updateUsageSummary(modelOverride){if(!this.nodes.usageTitle)return;const provider=window.BimAiProviders[this.providerId];const providerStats=this.usageStats.providers&&this.usageStats.providers[this.providerId]||this.emptyUsage();const model=String(modelOverride||this.lastUsageModelByProvider[this.providerId]||this.nodes.model.value||'');const modelStats=providerStats.models&&providerStats.models[model]||this.emptyUsage();this.nodes.usageTitle.textContent='Consumo · '+provider.label+(model?' · '+this.modelLabel(model):'');this.nodes.usageDetail.textContent='Consultas: '+this.formatUsageNumber(modelStats.queries)+' · Entrada: '+this.formatUsageNumber(modelStats.inputTokens)+' · Salida: '+this.formatUsageNumber(modelStats.outputTokens)+' tokens';this.nodes.usageProviderTotal.textContent='Total del proveedor: '+this.formatUsageNumber(providerStats.queries)+' consultas · '+this.formatUsageNumber(providerStats.totalTokens)+' tokens';}responseUsageLabel(response){const usage=response&&response.usage;if(!usage)return'';return' · Entrada '+this.formatUsageNumber(usage.inputTokens)+' · Salida '+this.formatUsageNumber(usage.outputTokens)+' tokens';}modelLabel(model){const labels={'gemini-3.5-flash':'Gemini 3.5 Flash (principal)','gemini-2.5-flash':'Gemini 2.5 Flash (respaldo 1)','gemini-3.1-flash-lite':'Gemini 3.1 Flash-Lite (respaldo 2)','gpt-5.6-luna':'GPT-5.6 Luna','gpt-5.6-terra':'GPT-5.6 Terra','gpt-5.6-sol':'GPT-5.6 Sol','nvidia/nemotron-3-super-120b-a12b':'Nemotron 3 Super 120B','meta/llama-3.3-70b-instruct':'Llama 3.3 70B'};return labels[model]||model;}providerRequiresClientKey(providerId){const providerAuth=this.proxyCapabilities&&this.proxyCapabilities.providerAuth;const item=providerAuth&&providerAuth[providerId];return!(item&&item.requiresClientKey===false);}updateKeyUi(){if(!this.nodes.key)return;const provider=window.BimAiProviders[this.providerId]||window.BimAiProviders.gemini;const requiresClientKey=this.providerRequiresClientKey(provider.id);this.nodes.key.disabled=!requiresClientKey;if(this.nodes.keyLabel)this.nodes.keyLabel.classList.toggle('ai-server-key',!requiresClientKey);if(requiresClientKey){this.nodes.key.placeholder='Se conserva solo durante esta sesión';if(this.nodes.keyNote)this.nodes.keyNote.textContent='La clave no se guarda en el proyecto ni se envía a los archivos BIM.';}else{this.nodes.key.placeholder='Gestionada de forma segura por el servidor';if(this.nodes.keyNote)this.nodes.keyNote.textContent='La API key de '+provider.label+' está configurada en el backend y no se expone en el navegador.';}}updateConnectionSummary(){const provider=window.BimAiProviders[this.providerId]||window.BimAiProviders.gemini;let status;if(this.proxyReady===false)status='Backend IA no disponible';else if(!this.providerRequiresClientKey(provider.id))status='Conexión lista · clave del servidor';else status=this.nodes.key.value?'Clave disponible':'Falta API key';this.nodes.summary.textContent=provider.label+' · '+status;}async checkProxyHealth(){const endpoint=window.BimAiEndpoint;const url=endpoint&&typeof endpoint.url==='function'?endpoint.url('/api/ai/health'):'/api/ai/health';try{const response=await fetch(url,{method:'GET',cache:'no-store'});const payload=await response.json();this.proxyReady=!!(response.ok&&payload&&payload.ok&&payload.service==='bim-ai-proxy');this.proxyCapabilities=this.proxyReady?payload:null;}catch(_){this.proxyReady=false;this.proxyCapabilities=null;}this.updateKeyUi();this.updateConnectionSummary();return this.proxyReady;}invalidateCatalog(){this.catalog.model=null;this.catalog.fingerprint='';this.catalog.summary=null;this.catalog.store=null;this.catalog.basicStore=null;this.catalog.sourceCache.clear();this.nodes.catalogDot.className='';this.nodes.catalogTitle.textContent='Catálogo en espera';this.nodes.catalogDetail.textContent='Abre el panel para actualizarlo.';}handleProjectChanged(){this.invalidateCatalog();this.engine.lastResult=null;this.currentLastResultSnapshot=null;this.conversations=[];this.currentConversationId='';this.nodes.messages.innerHTML='';this.restoreHistory();if(!this.nodes.panel.classList.contains('hidden'))this.prepareCatalog();}async prepareCatalog(){try{this.nodes.catalogDot.className='working';const summary=await this.catalog.prepare((message,current,total)=>this.setCatalogProgress(message,current,total));this.nodes.catalogDot.className='ready';this.nodes.catalogTitle.textContent='Proyecto preparado';this.nodes.catalogDetail.textContent=summary.totalElements.toLocaleString('es-PE')+' elementos · '+summary.projects.length+' modelos · '+Number(summary.structure&&summary.structure.folderCount||0).toLocaleString('es-PE')+' carpetas · '+summary.sources.issues+' incidencias';}catch(error){this.nodes.catalogDot.className='error';this.nodes.catalogTitle.textContent='Sin proyecto disponible';this.nodes.catalogDetail.textContent=error.message;}}setCatalogProgress(message,current,total){this.nodes.catalogTitle.textContent='Preparando información';const percent=total>0?Math.round((current/total)*100):0;this.nodes.catalogDetail.textContent=message+(total>1?' · '+percent+'%':'');}setProgress(visible,message,current,total){this.nodes.progress.classList.toggle('hidden',!visible);if(!visible)return;const percent=total>0?Math.max(0,Math.min(100,Math.round((current/total)*100))):0;this.nodes.progressText.textContent=message||'Procesando...';this.nodes.progressValue.textContent=percent+'%';this.nodes.progressBar.value=percent;}addMessage(role,text,meta,tableData,attachmentData){const row=document.createElement('article');row.className='ai-message '+role;const bubble=document.createElement('div');bubble.className='ai-message-bubble';const messageText=document.createElement('div');messageText.className='ai-message-text';messageText.textContent=text;bubble.appendChild(messageText);const table=this.normalizeTable(tableData,{preserveLive:true});if(table){row.classList.add('has-table');this.renderInteractiveResultTable(bubble,table);}const attachments=this.normalizeAttachments(attachmentData);if(attachments.length){const attachmentHost=document.createElement('div');attachmentHost.className='ai-message-attachments';attachments.forEach(attachment=>{const live=this.reportAttachments.get(attachment.id);if(live&&live.url){const link=document.createElement('a');link.className='ai-report-download';link.href=live.url;link.download=attachment.filename;link.title='Descargar '+attachment.filename;link.innerHTML='<svg viewBox="0 0 24 24" aria-hidden="true"><path d="M12 3v12m0 0 4-4m-4 4-4-4M5 20h14"/></svg><span></span>';link.querySelector('span').textContent='Descargar Excel';attachmentHost.appendChild(link);}else{const unavailable=document.createElement('span');unavailable.className='ai-report-unavailable';unavailable.textContent='Excel de una sesión anterior · vuelve a solicitarlo para regenerarlo';attachmentHost.appendChild(unavailable);}const detail=document.createElement('small');detail.textContent=attachment.filename+' · '+Number(attachment.rowCount||0).toLocaleString('es-PE')+' filas'+(attachment.size?' · '+this.formatFileSize(attachment.size):'');attachmentHost.appendChild(detail);});bubble.appendChild(attachmentHost);}row.appendChild(bubble);if(meta){const small=document.createElement('small');small.textContent=meta;row.appendChild(small);}row.__bimAiHistory={role,text:String(text||''),meta:String(meta||''),table:this.normalizeTable(table),attachments};this.nodes.messages.appendChild(row);this.nodes.messages.scrollTop=this.nodes.messages.scrollHeight;this.saveHistory();if(role==='assistant'&&!this.restoringConversation)this.speakAssistantMessage(text,!!table);}normalizeAttachments(value){const values=Array.isArray(value)?value:(value?[value]:[]);return values.slice(0,5).map(item=>({id:String(item&&item.id||''),filename:String(item&&item.filename||'Reporte_BIM.xlsx').slice(0,180),mimeType:String(item&&item.mimeType||'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet'),size:Math.max(0,Number(item&&item.size)||0),rowCount:Math.max(0,Number(item&&item.rowCount)||0),totalRowCount:Math.max(0,Number(item&&item.totalRowCount)||Number(item&&item.rowCount)||0),truncated:!!(item&&item.truncated)})).filter(item=>item.id);}registerReportAttachment(reportFile){if(!reportFile||!reportFile.blob)return null;const id='ai-report-'+Date.now()+'-'+Math.random().toString(36).slice(2,8);const url=URL.createObjectURL(reportFile.blob);this.reportAttachments.set(id,{url,createdAt:Date.now()});while(this.reportAttachments.size>5){const oldest=this.reportAttachments.keys().next().value;const entry=this.reportAttachments.get(oldest);if(entry&&entry.url)URL.revokeObjectURL(entry.url);this.reportAttachments.delete(oldest);}return Object.assign({id},reportFile,{blob:undefined});}releaseReportAttachments(){for(const entry of this.reportAttachments.values()){if(entry&&entry.url)URL.revokeObjectURL(entry.url);}this.reportAttachments.clear();}formatFileSize(bytes){const size=Math.max(0,Number(bytes)||0);if(size<1024)return size+' B';if(size<1024*1024)return(size/1024).toLocaleString('es-PE',{maximumFractionDigits:1})+' KB';return(size/(1024*1024)).toLocaleString('es-PE',{maximumFractionDigits:1})+' MB';}normalizeTable(table,options){if(!table||!Array.isArray(table.headers)||!table.headers.length)return null;const opts=options&&typeof options==='object'?options:{};const preserveLive=opts.preserveLive===true;const headers=table.headers.map(value=>String(value==null?'':value).slice(0,180));const sourceRows=Array.isArray(table.rows)?table.rows:[];const totalRows=Math.max(sourceRows.length,Math.max(0,Number(table.totalRows)||0));if(!totalRows)return null;const copyCount=preserveLive?sourceRows.length:Math.min(sourceRows.length,40);const rows=sourceRows.slice(0,copyCount).map(values=>headers.map((_header,index)=>{const value=values&&values[index]!=null?values[index]:'';return preserveLive?value:String(value).slice(0,600);}));const sourceRowKeys=Array.isArray(table.rowKeys)?table.rowKeys:[];const rowKeys=[];for(let index=0;index<rows.length;index++){let keys=sourceRowKeys[index];if((!keys||!keys.length)&&typeof table.getRowKeys==='function'){try{keys=table.getRowKeys(index);}catch(_){keys=[];}}rowKeys.push(Array.from(new Set(Array.from(keys||[]).map(value=>String(value||'')).filter(Boolean))).slice(0,preserveLive?100000:250));}return{headers,rows,rowKeys,totalRows:preserveLive?totalRows:rows.length,getRow:preserveLive&&typeof table.getRow==='function'?table.getRow:null,getRowKeys:preserveLive&&typeof table.getRowKeys==='function'?table.getRowKeys:null,selectable:table.selectable!==false};}smartTableCompare(left,right){if(left==null&&right==null)return 0;if(left==null)return-1;if(right==null)return 1;if(typeof left==='number'&&typeof right==='number')return left-right;const a=String(left).trim();const b=String(right).trim();const numeric=value=>{const text=String(value||'').trim().replace(/\s/g,'');if(!text||!/^-?[\d.,]+(?:[eE][+-]?\d+)?$/.test(text))return NaN;const comma=text.lastIndexOf(',');const dot=text.lastIndexOf('.');let normalized=text;if(comma>=0&&dot>=0){normalized=comma>dot?text.replace(/\./g,'').replace(',','.'):text.replace(/,/g,'');}else if(comma>=0){const decimals=text.length-comma-1;normalized=decimals>0&&decimals<=3?text.replace(',','.'):text.replace(/,/g,'');}return Number(normalized);};const na=numeric(a);const nb=numeric(b);if(Number.isFinite(na)&&Number.isFinite(nb))return na-nb;const da=/^\d{4}-\d{2}-\d{2}(?:[T\s].*)?$/.test(a)?Date.parse(a):NaN;const db=/^\d{4}-\d{2}-\d{2}(?:[T\s].*)?$/.test(b)?Date.parse(b):NaN;if(Number.isFinite(da)&&Number.isFinite(db))return da-db;return a.localeCompare(b,'es',{numeric:true,sensitivity:'base'});}parseAiTableNumericValue(rawValue){if(rawValue==null)return null;if(typeof rawValue==='number')return Number.isFinite(rawValue)?{value:rawValue,unit:''}:null;const text=String(rawValue).trim();if(!text)return null;const match=text.match(/^([+-]?(?:\d{1,3}(?:[.,]\d{3})+(?:[.,]\d+)?|\d+(?:[.,]\d+)?|[.,]\d+))(?:\s*([^0-9.,+\-].*))?$/);if(!match)return null;let numericText=match[1];const comma=numericText.lastIndexOf(',');const dot=numericText.lastIndexOf('.');if(comma>=0&&dot>=0){numericText=comma>dot?numericText.replace(/\./g,'').replace(',','.'):numericText.replace(/,/g,'');}else if(comma>=0){const decimals=numericText.length-comma-1;numericText=decimals>0&&decimals<=4?numericText.replace(',','.'):numericText.replace(/,/g,'');}const value=Number(numericText);if(!Number.isFinite(value))return null;return{value,unit:String(match[2]||'').trim()};}formatAiTableNumber(value){const numeric=Number(value);if(!Number.isFinite(numeric))return'';return numeric.toLocaleString('es-PE',{maximumFractionDigits:4});}renderInteractiveResultTable(bubble,table){if(!bubble||!table)return null;const totalRows=Math.max(0,Number(table.totalRows)||(table.rows&&table.rows.length)||0);if(!totalRows)return null;const rowCache=new Map();const rowKeyCache=new Map();const getRow=index=>{if(rowCache.has(index))return rowCache.get(index);let values=index<(table.rows||[]).length?table.rows[index]:null;if(typeof table.getRow==='function'){try{values=table.getRow(index);}catch(_){values=values||[];}}const normalized=table.headers.map((_header,columnIndex)=>values&&values[columnIndex]!=null?values[columnIndex]:'');rowCache.set(index,normalized);return normalized;};const getRowKeys=index=>{if(rowKeyCache.has(index))return rowKeyCache.get(index);let keys=index<(table.rowKeys||[]).length?table.rowKeys[index]:null;if(typeof table.getRowKeys==='function'){try{keys=table.getRowKeys(index);}catch(_){keys=keys||[];}}const normalized=Array.from(new Set(Array.from(keys||[]).map(value=>String(value||'')).filter(Boolean)));rowKeyCache.set(index,normalized);return normalized;};const getFilterValue=value=>String(value==null?'':value).trim();const getFilterLabel=value=>value===''?'(Vacío)':value;let selectable=table.selectable!==false&&(table.rowKeys||[]).some(keys=>keys&&keys.length);if(!selectable&&table.selectable!==false&&typeof table.getRowKeys==='function'){for(let index=0;index<Math.min(totalRows,20);index++){if(getRowKeys(index).length){selectable=true;break;}}}const controls=document.createElement('div');controls.className='ai-table-controls';const selectionLabel=document.createElement('label');selectionLabel.className='ai-table-viewer-toggle'+(selectable?'':' is-disabled');const selectionCheckbox=document.createElement('input');selectionCheckbox.type='checkbox';selectionCheckbox.disabled=!selectable;selectionCheckbox.setAttribute('aria-label','Seleccionar elementos en visor 3D');const selectionText=document.createElement('span');selectionText.textContent='Seleccionar elementos en visor 3D';selectionLabel.appendChild(selectionCheckbox);selectionLabel.appendChild(selectionText);if(!selectable)selectionLabel.title='Este resultado no tiene elementos BIM asociados.';controls.appendChild(selectionLabel);const actions=document.createElement('div');actions.className='ai-table-actions';const status=document.createElement('small');status.className='ai-table-status';const toggle=document.createElement('button');toggle.type='button';toggle.className='ai-table-toggle';actions.appendChild(status);actions.appendChild(toggle);controls.appendChild(actions);bubble.appendChild(controls);const wrapper=document.createElement('div');wrapper.className='ai-table-wrap ai-interactive-table-wrap';const tableNode=document.createElement('table');tableNode.className='ai-result-table';const head=document.createElement('thead');const headRow=document.createElement('tr');const sortIndicators=[];const filterButtons=[];const totalLabels=[];table.headers.forEach((header,columnIndex)=>{const cell=document.createElement('th');cell.className='ai-table-header-cell';const headerMain=document.createElement('div');headerMain.className='ai-table-header-main';const button=document.createElement('button');button.type='button';button.className='ai-table-sort';button.title='Ordenar por '+header;const label=document.createElement('span');label.className='ai-table-header-label';label.textContent=header;const indicator=document.createElement('span');indicator.className='ai-table-sort-indicator';indicator.textContent='↕';button.appendChild(label);button.appendChild(indicator);sortIndicators.push(indicator);const filterButton=document.createElement('button');filterButton.type='button';filterButton.className='ai-table-filter-button';filterButton.title='Filtrar valores de '+header;filterButton.setAttribute('aria-label','Filtrar '+header);filterButton.textContent='▼';filterButtons.push(filterButton);headerMain.appendChild(button);headerMain.appendChild(filterButton);cell.appendChild(headerMain);const totalLine=document.createElement('small');totalLine.className='ai-table-column-total hidden';cell.appendChild(totalLine);totalLabels.push(totalLine);headRow.appendChild(cell);button.addEventListener('click',event=>{event.preventDefault();event.stopPropagation();sortBy(columnIndex);});filterButton.addEventListener('click',event=>{event.preventDefault();event.stopPropagation();toggleFilterPopup(columnIndex,filterButton);});});head.appendChild(headRow);tableNode.appendChild(head);const body=document.createElement('tbody');tableNode.appendChild(body);wrapper.appendChild(tableNode);bubble.appendChild(wrapper);const state={expanded:false,viewIndices:null,sourceToDisplay:null,sortColumn:-1,sortDirection:1,selected:new Set(),anchor:null,filters:new Map(),filterGeneration:0,summaryGeneration:0,virtualRowHeight:31,virtualFirst:-1,virtualLast:-1,lastScrollTop:0,activeFilterPopup:null,activeFilterColumn:-1};const displayCount=()=>state.viewIndices?state.viewIndices.length:totalRows;const sourceIndexAt=displayIndex=>state.viewIndices?state.viewIndices[displayIndex]:displayIndex;const rebuildSourceToDisplay=()=>{if(!state.viewIndices){state.sourceToDisplay=null;return;}const map=new Map();for(let index=0;index<state.viewIndices.length;index++)map.set(state.viewIndices[index],index);state.sourceToDisplay=map;};const displayIndexOf=sourceIndex=>state.sourceToDisplay?(state.sourceToDisplay.get(sourceIndex)??-1):sourceIndex;const isFilterValueSelected=(filter,value)=>{if(!filter)return true;if(filter.mode==='include')return filter.values.has(value);return!filter.values.has(value);};const rowMatchesFilters=sourceIndex=>{if(!state.filters.size)return true;const values=getRow(sourceIndex);for(const[columnIndex,filter]of state.filters.entries()){if(!isFilterValueSelected(filter,getFilterValue(values[columnIndex])))return false;}return true;};const updateFilterButtons=()=>{filterButtons.forEach((button,columnIndex)=>button.classList.toggle('active',state.filters.has(columnIndex)));};const updateStatus=(temporaryText)=>{if(temporaryText){status.textContent=temporaryText;return;}const filtered=displayCount();const visible=state.expanded?filtered:Math.min(10,filtered);let text='Mostrando '+visible.toLocaleString('es-PE')+' de '+filtered.toLocaleString('es-PE')+' resultado'+(filtered===1?'':'s');if(filtered!==totalRows)text+=' · '+totalRows.toLocaleString('es-PE')+' originales';status.textContent=text;toggle.textContent=state.expanded?'Ver menos':'Ver más';toggle.classList.toggle('hidden',filtered<=10);wrapper.classList.toggle('is-expanded',state.expanded);};const updateSortIndicators=()=>{sortIndicators.forEach((indicator,index)=>{indicator.textContent=index===state.sortColumn?(state.sortDirection>0?'↑':'↓'):'↕';});};const syncViewerSelection=()=>{if(!selectionCheckbox.checked||!selectable)return;const keys=new Set();for(const sourceIndex of state.selected)for(const key of getRowKeys(sourceIndex))keys.add(key);const viewer=window.__exportModelViewer||null;if(!viewer||typeof viewer.selectElementKeys!=='function')return;viewer.selectElementKeys(Array.from(keys),true,{source:'ai-result-table'});};const selectRow=(event,sourceIndex)=>{const useCtrl=!!(event.ctrlKey||event.metaKey);const anchorDisplay=state.anchor==null?-1:displayIndexOf(state.anchor);const useShift=!!event.shiftKey&&anchorDisplay>=0;if(useShift){const currentDisplay=Math.max(0,displayIndexOf(sourceIndex));if(!useCtrl)state.selected.clear();const from=Math.min(anchorDisplay,currentDisplay);const to=Math.max(anchorDisplay,currentDisplay);for(let displayIndex=from;displayIndex<=to;displayIndex++)state.selected.add(sourceIndexAt(displayIndex));}else if(useCtrl){if(state.selected.has(sourceIndex))state.selected.delete(sourceIndex);else state.selected.add(sourceIndex);state.anchor=sourceIndex;}else{state.selected.clear();state.selected.add(sourceIndex);state.anchor=sourceIndex;}renderBody({preserveScroll:true});syncViewerSelection();};const createSpacer=(height,position)=>{if(!(height>0))return null;const row=document.createElement('tr');row.className='ai-table-virtual-spacer '+position;const cell=document.createElement('td');cell.colSpan=Math.max(1,table.headers.length);cell.style.height=Math.round(height)+'px';cell.style.padding='0';cell.style.border='0';row.appendChild(cell);return row;};const createDataRow=(sourceIndex,displayIndex)=>{const values=getRow(sourceIndex);const row=document.createElement('tr');row.dataset.aiSourceIndex=String(sourceIndex);row.dataset.aiDisplayIndex=String(displayIndex);row.classList.toggle('is-selected',state.selected.has(sourceIndex));table.headers.forEach((_header,index)=>{const cell=document.createElement('td');const value=values[index]==null?'':String(values[index]);cell.textContent=value;if(value.length>80)cell.title=value;row.appendChild(cell);});row.addEventListener('click',event=>selectRow(event,sourceIndex));return row;};const restoreTableScroll=(top,left)=>{wrapper.scrollTop=Math.max(0,Number(top)||0);wrapper.scrollLeft=Math.max(0,Number(left)||0);state.lastScrollTop=wrapper.scrollTop;requestAnimationFrame(()=>{if(Math.abs(wrapper.scrollTop-top)>1)wrapper.scrollTop=Math.max(0,Number(top)||0);if(Math.abs(wrapper.scrollLeft-left)>1)wrapper.scrollLeft=Math.max(0,Number(left)||0);state.lastScrollTop=wrapper.scrollTop;});};const renderBody=options=>{const opts=options&&typeof options==='object'?options:{};const preserveScroll=opts.preserveScroll!==false;const savedTop=wrapper.scrollTop;const savedLeft=wrapper.scrollLeft;const countRows=displayCount();const fragment=document.createDocumentFragment();if(!state.expanded){state.virtualFirst=-1;state.virtualLast=-1;for(let displayIndex=0;displayIndex<Math.min(10,countRows);displayIndex++)fragment.appendChild(createDataRow(sourceIndexAt(displayIndex),displayIndex));body.replaceChildren(fragment);if(preserveScroll)restoreTableScroll(savedTop,savedLeft);return;}const useVirtual=countRows>180;if(!useVirtual){state.virtualFirst=-1;state.virtualLast=-1;for(let displayIndex=0;displayIndex<countRows;displayIndex++)fragment.appendChild(createDataRow(sourceIndexAt(displayIndex),displayIndex));body.replaceChildren(fragment);if(preserveScroll)restoreTableScroll(savedTop,savedLeft);return;}const rowHeight=state.virtualRowHeight;const viewportHeight=Math.max(240,wrapper.clientHeight||380);const first=Math.max(0,Math.floor(savedTop/rowHeight)-12);const visibleCount=Math.ceil(viewportHeight/rowHeight)+24;const last=Math.min(countRows,first+visibleCount);if(opts.onlyIfRangeChanged&&first===state.virtualFirst&&last===state.virtualLast)return;state.virtualFirst=first;state.virtualLast=last;const topSpacer=createSpacer(first*rowHeight,'top');if(topSpacer)fragment.appendChild(topSpacer);for(let displayIndex=first;displayIndex<last;displayIndex++)fragment.appendChild(createDataRow(sourceIndexAt(displayIndex),displayIndex));const bottomSpacer=createSpacer((countRows-last)*rowHeight,'bottom');if(bottomSpacer)fragment.appendChild(bottomSpacer);body.replaceChildren(fragment);if(preserveScroll)restoreTableScroll(savedTop,savedLeft);};const sortCurrentView=()=>{if(state.sortColumn<0)return;const countRows=displayCount();const indices=state.viewIndices?state.viewIndices.slice():Array.from({length:countRows},(_value,index)=>index);const columnIndex=state.sortColumn;const direction=state.sortDirection;indices.sort((leftIndex,rightIndex)=>{const compared=this.smartTableCompare(getRow(leftIndex)[columnIndex],getRow(rightIndex)[columnIndex]);return compared===0?leftIndex-rightIndex:compared*direction;});state.viewIndices=indices;rebuildSourceToDisplay();};const scheduleColumnSummaries=()=>{const generation=++state.summaryGeneration;totalLabels.forEach(label=>{label.textContent='';label.classList.add('hidden');});const countRows=displayCount();if(!countRows)return;const summaries=table.headers.map(()=>({sum:0,numericCount:0,units:new Map()}));let cursor=0;const process=()=>{if(generation!==state.summaryGeneration)return;const end=Math.min(countRows,cursor+700);for(;cursor<end;cursor++){const values=getRow(sourceIndexAt(cursor));for(let columnIndex=0;columnIndex<table.headers.length;columnIndex++){const parsed=this.parseAiTableNumericValue(values[columnIndex]);if(!parsed)continue;const summary=summaries[columnIndex];summary.sum+=parsed.value;summary.numericCount++;const unit=String(parsed.unit||'').trim();const unitKey=unit.toLocaleLowerCase?unit.toLocaleLowerCase():unit.toLowerCase();if(!summary.units.has(unitKey))summary.units.set(unitKey,unit);}}if(cursor<countRows){requestAnimationFrame(process);return;}summaries.forEach((summary,columnIndex)=>{if(!summary.numericCount)return;const header=String(table.headers[columnIndex]||'').trim();if(/^(?:element\s*id|elementid|unique\s*id|uniqueid)$/i.test(header))return;if(summary.units.size>1)return;const unit=summary.units.size===1?Array.from(summary.units.values())[0]:'';const text='Suma válida: '+this.formatAiTableNumber(summary.sum)+(unit?' '+unit:'');totalLabels[columnIndex].textContent=text;totalLabels[columnIndex].classList.remove('hidden');});};requestAnimationFrame(process);};const applyFiltersBatched=()=>{const generation=++state.filterGeneration;state.summaryGeneration++;updateStatus('Filtrando información...');const filtersActive=state.filters.size>0;if(!filtersActive){state.viewIndices=null;state.sourceToDisplay=null;if(state.sortColumn>=0)sortCurrentView();state.selected.clear();state.anchor=null;wrapper.scrollTop=0;state.virtualFirst=-1;state.virtualLast=-1;updateFilterButtons();updateStatus();renderBody({preserveScroll:false});scheduleColumnSummaries();if(selectionCheckbox.checked)syncViewerSelection();return;}const matches=[];let cursor=0;const process=()=>{if(generation!==state.filterGeneration)return;const end=Math.min(totalRows,cursor+900);for(;cursor<end;cursor++)if(rowMatchesFilters(cursor))matches.push(cursor);if(cursor<totalRows){requestAnimationFrame(process);return;}state.viewIndices=matches;rebuildSourceToDisplay();if(state.sortColumn>=0)sortCurrentView();state.selected.clear();state.anchor=null;wrapper.scrollTop=0;state.virtualFirst=-1;state.virtualLast=-1;updateFilterButtons();updateStatus();renderBody({preserveScroll:false});scheduleColumnSummaries();if(selectionCheckbox.checked)syncViewerSelection();};requestAnimationFrame(process);};const sortBy=columnIndex=>{if(state.sortColumn===columnIndex)state.sortDirection*=-1;else{state.sortColumn=columnIndex;state.sortDirection=1;}sortCurrentView();wrapper.scrollTop=0;state.virtualFirst=-1;state.virtualLast=-1;updateSortIndicators();renderBody({preserveScroll:false});};const closeFilterPopup=()=>{if(state.activeFilterPopup&&state.activeFilterPopup.parentNode)state.activeFilterPopup.parentNode.removeChild(state.activeFilterPopup);state.activeFilterPopup=null;state.activeFilterColumn=-1;document.removeEventListener('pointerdown',outsideFilterPointerDown,true);window.removeEventListener('resize',closeFilterPopup);};const outsideFilterPointerDown=event=>{const popup=state.activeFilterPopup;if(!popup)return;if(popup.contains(event.target))return;if(filterButtons.some(button=>button&&button.contains(event.target)))return;closeFilterPopup();};const positionFilterPopup=(popup,anchor)=>{if(!popup||!anchor)return;const rect=anchor.getBoundingClientRect();const width=Math.min(330,Math.max(240,popup.offsetWidth||290));const maxLeft=Math.max(8,window.innerWidth-width-8);let left=Math.max(8,Math.min(maxLeft,rect.right-width));let top=rect.bottom+5;const height=Math.min(390,popup.offsetHeight||320);if(top+height>window.innerHeight-8)top=Math.max(8,rect.top-height-5);popup.style.left=Math.round(left)+'px';popup.style.top=Math.round(top)+'px';};const buildDistinctOptionsBatched=(columnIndex,popup,onDone)=>{const counts=new Map();let cursor=0;const generation=state.filterGeneration;const process=()=>{if(!popup||!popup.isConnected||state.activeFilterColumn!==columnIndex)return;const end=Math.min(totalRows,cursor+1000);for(;cursor<end;cursor++){const value=getFilterValue(getRow(cursor)[columnIndex]);counts.set(value,(counts.get(value)||0)+1);}if(cursor<totalRows){requestAnimationFrame(process);return;}const options=Array.from(counts.entries()).map(([value,count])=>({value,label:getFilterLabel(value),count}));options.sort((a,b)=>this.smartTableCompare(a.label,b.label));if(generation!==state.filterGeneration&&!popup.isConnected)return;onDone(options);};requestAnimationFrame(process);};const toggleFilterPopup=(columnIndex,anchorButton)=>{if(state.activeFilterPopup&&state.activeFilterColumn===columnIndex){closeFilterPopup();return;}closeFilterPopup();const popup=document.createElement('div');popup.className='ai-table-filter-popup';popup.setAttribute('role','dialog');popup.setAttribute('aria-label','Filtrar '+String(table.headers[columnIndex]||'columna'));const search=document.createElement('input');search.type='search';search.className='ai-table-filter-search';search.placeholder='Buscar valores...';popup.appendChild(search);const allRow=document.createElement('label');allRow.className='ai-table-filter-option ai-table-filter-all';const allInput=document.createElement('input');allInput.type='checkbox';const allText=document.createElement('span');allText.textContent='(Seleccionar todo)';allRow.appendChild(allInput);allRow.appendChild(allText);popup.appendChild(allRow);const list=document.createElement('div');list.className='ai-table-filter-list';const loading=document.createElement('div');loading.className='ai-table-filter-loading';loading.textContent='Cargando valores...';list.appendChild(loading);popup.appendChild(list);document.body.appendChild(popup);state.activeFilterPopup=popup;state.activeFilterColumn=columnIndex;document.addEventListener('pointerdown',outsideFilterPointerDown,true);window.addEventListener('resize',closeFilterPopup,{once:true});positionFilterPopup(popup,anchorButton);buildDistinctOptionsBatched(columnIndex,popup,options=>{if(!popup.isConnected)return;const ITEM_HEIGHT=28;const OVERSCAN=5;let filteredOptions=options;const isChecked=option=>isFilterValueSelected(state.filters.get(columnIndex),option.value);const updateAll=()=>{let selectedCount=0;for(const option of options)if(isChecked(option))selectedCount++;allInput.checked=options.length>0&&selectedCount===options.length;allInput.indeterminate=selectedCount>0&&selectedCount<options.length;};const inner=document.createElement('div');inner.className='ai-table-filter-list-inner';list.replaceChildren(inner);const renderVirtualOptions=()=>{const top=list.scrollTop||0;const viewportHeight=Math.max(100,list.clientHeight||230);const first=Math.max(0,Math.floor(top/ITEM_HEIGHT)-OVERSCAN);const count=Math.ceil(viewportHeight/ITEM_HEIGHT)+OVERSCAN*2;const last=Math.min(filteredOptions.length,first+count);inner.style.height=Math.max(1,filteredOptions.length*ITEM_HEIGHT)+'px';const fragment=document.createDocumentFragment();for(let index=first;index<last;index++){const option=filteredOptions[index];const row=document.createElement('label');row.className='ai-table-filter-option';row.style.top=(index*ITEM_HEIGHT)+'px';const checkbox=document.createElement('input');checkbox.type='checkbox';checkbox.checked=isChecked(option);const text=document.createElement('span');text.textContent=option.label;const countNode=document.createElement('small');countNode.textContent=option.count.toLocaleString('es-PE');row.appendChild(checkbox);row.appendChild(text);row.appendChild(countNode);checkbox.addEventListener('change',()=>{let filter=state.filters.get(columnIndex);const nowChecked=checkbox.checked;if(!filter){if(!nowChecked)state.filters.set(columnIndex,{mode:'exclude',values:new Set([option.value])});}else if(filter.mode==='exclude'){if(nowChecked)filter.values.delete(option.value);else filter.values.add(option.value);if(!filter.values.size)state.filters.delete(columnIndex);}else{if(nowChecked)filter.values.add(option.value);else filter.values.delete(option.value);}updateAll();renderVirtualOptions();updateFilterButtons();applyFiltersBatched();});fragment.appendChild(row);}inner.replaceChildren(fragment);};search.addEventListener('input',()=>{const query=String(search.value||'').trim().toLocaleLowerCase();filteredOptions=query?options.filter(option=>option.label.toLocaleLowerCase().includes(query)):options;list.scrollTop=0;renderVirtualOptions();});allInput.addEventListener('change',()=>{if(allInput.checked)state.filters.delete(columnIndex);else state.filters.set(columnIndex,{mode:'include',values:new Set()});updateAll();renderVirtualOptions();updateFilterButtons();applyFiltersBatched();});list.addEventListener('scroll',()=>requestAnimationFrame(renderVirtualOptions),{passive:true});updateAll();renderVirtualOptions();positionFilterPopup(popup,anchorButton);requestAnimationFrame(()=>search.focus());});};toggle.addEventListener('click',()=>{state.expanded=!state.expanded;wrapper.scrollTop=0;state.lastScrollTop=0;state.virtualFirst=-1;state.virtualLast=-1;updateStatus();renderBody({preserveScroll:false});});selectionCheckbox.addEventListener('change',()=>{if(selectionCheckbox.checked)syncViewerSelection();});let scrollFrame=0;wrapper.addEventListener('scroll',()=>{if(!state.expanded||displayCount()<=180)return;const currentTop=wrapper.scrollTop;if(Math.abs(currentTop-state.lastScrollTop)<0.5)return;state.lastScrollTop=currentTop;if(scrollFrame)cancelAnimationFrame(scrollFrame);scrollFrame=requestAnimationFrame(()=>{scrollFrame=0;renderBody({preserveScroll:true,onlyIfRangeChanged:true});});},{passive:true});updateStatus();updateSortIndicators();updateFilterButtons();renderBody({preserveScroll:false});scheduleColumnSummaries();return{wrapper,tableNode,controls,state};}referencesPreviousResult(prompt){const value=String(prompt||'').normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase();if(/\b(consulta anterior|resultado anterior|elementos anteriormente (?:aislados|seleccionados)|que ya tenemos|del mensaje anterior)\b/.test(value))return true;const beginning=value.replace(/^[¿?¡!.,;:\s]+/,'');return/^(?:(?:bien|ahora|entonces|luego|por favor|quiero que)\s+){0,4}(?:con esos|con estos|de esos|de estos|esos mismos|estos mismos|los mismos|dichos elementos|sobre ellos|a esos|a estos|elementos aislados|elementos seleccionados|ponlos|pintalos|colorealos|ocultalos|aislalos|seleccionalos|transparentalos)\b/.test(beginning);}buildProviderContext(){const context=Object.assign({},this.catalog.compactContext());const recent=Array.from(this.nodes.messages.querySelectorAll('.ai-message')).slice(-7).map(row=>{const item=row.__bimAiHistory||{};return{role:row.classList.contains('user')?'user':'assistant',text:String(item.text||'').replace(/\s+/g,' ').slice(0,420)};});const previous=this.engine.lastResult;context.conversation={recent,previousResult:previous?{available:true,source:previous.source,action:previous.plan&&previous.plan.action,count:Number(previous.count)||0,groupBy:previous.plan&&previous.plan.groupBy||''}:{available:false}};return context;}enrichFollowUpPlan(prompt,plan){const source=plan&&typeof plan==='object'?plan:{};const next=Array.isArray(source.steps)?source:{steps:[source],explanation:String(source.explanation||'')};if(!next.steps.length)next.steps=[{}];const normalized=String(prompt||'').normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase();const referencesPrevious=this.referencesPreviousResult(prompt)&&this.engine.lastResult&&Array.isArray(this.engine.lastResult.keys);next.steps.forEach((step,index)=>{if(!step||typeof step!=='object')return;if(!step.numericOperation)step.numericOperation='none';if(step.numericFields==null)step.numericFields='';if(!Array.isArray(step.percentageBaseFilters))step.percentageBaseFilters=[];if(!step.percentageMode)step.percentageMode='auto';if(!step.reportMode)step.reportMode='detail';if(step.reportTitle==null)step.reportTitle='';if(step.reportFields==null)step.reportFields='';if(step.reportEmptyFields==null)step.reportEmptyFields='';if(index===0){if(referencesPrevious)step.scope='previous_result';else if(step.scope==='previous_result')step.scope='all';}else if(step.scope==='previous_result'){step.scope='previous_step';}if(/\b(carpeta|carpetas|ruta|directorio|directorios)\b/.test(normalized)){(step.filters||[]).forEach(filter=>{const field=String(filter&&filter.field||'').normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase();if(field==='carpeta'||field==='folder'||/^(?:instancia|tipo)::carpeta$/.test(field))filter.field='Ruta de carpeta';});const groupField=String(step.groupBy||'').normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase();if(groupField==='carpeta'||/^(?:instancia|tipo)::carpeta$/.test(groupField))step.groupBy='Ruta de carpeta';}});if(/\b(por|cada|segun)\s+(el\s+)?modelo\b|\ben que modelos\b/.test(normalized)){const target=next.steps[next.steps.length-1];if(!target.groupBy)target.groupBy='Modelo';if(/\b(cuant|cantidad|distribu|lista|dime)\w*/.test(normalized)&&!/\b(color|pint)/.test(normalized))target.action='summarize';}if(/\b(color|pint)\w*[\s\S]*\b(diferente|distinto)\w*/.test(normalized))next.steps[next.steps.length-1].colorMode='by_group';if(/\b(reporte|informe|excel|xlsx)\b/.test(normalized)){const reportTarget=next.steps[next.steps.length-1];reportTarget.action='export_excel';if(/\b(sin valor|vacio|incomplet|observacion)\w*/.test(normalized))reportTarget.reportMode='observations';}if(next.steps.length===1&&/\baisl\w*/.test(normalized)&&/\b(color|pint)\w*/.test(normalized))next.steps[0].action='isolate_color';if(next.steps.length===1&&/\btransparen\w*/.test(normalized)&&/\b(resalt|destac|color|pint)\w*/.test(normalized))next.steps[0].action='transparency_highlight';const numericTarget=next.steps.slice().reverse().find(step=>step&&['summarize','count','list'].includes(step.action));if(numericTarget&&(!numericTarget.numericOperation||numericTarget.numericOperation==='none')){if(/\b(porcentaje|porcentual|porcentajes)\b|%/.test(normalized))numericTarget.numericOperation='percentage';else if(/\b(promedio|media)\b/.test(normalized))numericTarget.numericOperation='average';else if(/\b(minimo|menor)\b/.test(normalized))numericTarget.numericOperation='minimum';else if(/\b(maximo|mayor)\b/.test(normalized))numericTarget.numericOperation='maximum';else if(/\b(resta|restar|diferencia)\b/.test(normalized))numericTarget.numericOperation='subtract';else if(/\b(multiplica|multiplicar|producto)\b/.test(normalized))numericTarget.numericOperation='multiply';else if(/\b(divide|dividir|division|cociente)\b/.test(normalized))numericTarget.numericOperation='divide';else if(/\b(suma|sumar|total)\b/.test(normalized)&&/\b(volumen|area|longitud|costo|precio|cantidad|peso)\b/.test(normalized))numericTarget.numericOperation='sum';}if(numericTarget&&numericTarget.numericOperation!=='none'&&numericTarget.numericOperation!=='percentage'&&!String(numericTarget.numericFields||'').trim()){if(/\bvolumen\b/.test(normalized))numericTarget.numericFields='Volumen';else if(/\barea\b/.test(normalized))numericTarget.numericFields='Área';else if(/\blongitud\b/.test(normalized))numericTarget.numericFields='Longitud';else if(/\bpeso\b/.test(normalized))numericTarget.numericFields='Peso';else if(/\b(costo|precio)\b/.test(normalized))numericTarget.numericFields=/\bprecio\b/.test(normalized)?'Precio':'Costo';}if(numericTarget&&numericTarget.numericOperation==='percentage'&&(!Array.isArray(numericTarget.percentageBaseFilters)||!numericTarget.percentageBaseFilters.length)&&Array.isArray(numericTarget.filters)&&numericTarget.filters.length){const base=[];const numerator=[];for(const filter of numericTarget.filters){const valueToken=String(filter&&filter.value||'').normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase().trim();if(!valueToken||['is_empty','is_not_empty'].includes(filter&&filter.operator)){numerator.push(filter);continue;}const valueIndex=normalized.indexOf(valueToken);const before=valueIndex>=0?normalized.slice(Math.max(0,valueIndex-48),valueIndex):'';const explicitBase=/(?:\bde\s+(?:los|las|el|la)\s+|\bdel\s+|\bentre\s+(?:los|las|el|la)?\s*|\bdentro\s+de\s+(?:los|las|el|la)?\s*)$/.test(before);if(explicitBase)base.push(filter);else numerator.push(filter);}if(base.length){numericTarget.percentageBaseFilters=base;numericTarget.filters=numerator;}}if(numericTarget&&numericTarget.numericOperation!=='none')numericTarget.action='summarize';return next;}isLocalGreeting(prompt){const normalized=String(prompt||'').normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase().replace(/[¿?¡!.,;:]/g,' ').replace(/\s+/g,' ').trim();return/^(hola|buenos dias|buenas tardes|buenas noches|hey|saludos)$/.test(normalized);}async ensureMicrophone(){if(this.microphoneStream&&this.microphoneStream.getAudioTracks().some(track=>track.readyState==='live'))return true;if(!navigator.mediaDevices||typeof navigator.mediaDevices.getUserMedia!=='function')return true;this.microphoneStream=await navigator.mediaDevices.getUserMedia({audio:{echoCancellation:true,noiseSuppression:true,autoGainControl:true},video:false});return true;}setMicrophoneEnabled(enabled){if(!this.microphoneStream)return;this.microphoneStream.getAudioTracks().forEach(track=>{if(track.readyState==='live')track.enabled=!!enabled;});}releaseMicrophone(){this.stopRecognition();if(!this.microphoneStream)return;try{this.microphoneStream.getTracks().forEach(track=>track.stop());}catch(_){}this.microphoneStream=null;}setMicrophoneUi(active){this.nodes.mic.classList.toggle('recording',!!active);this.nodes.mic.innerHTML=active?'<svg viewBox="0 0 24 24" aria-hidden="true"><rect x="7" y="7" width="10" height="10" rx="1.5"></rect></svg>':'<svg viewBox="0 0 24 24" aria-hidden="true"><rect x="9" y="3.5" width="6" height="11" rx="3"></rect><path d="M6.5 11.5a5.5 5.5 0 0 0 11 0M12 17v3.5M8.5 20.5h7"></path></svg>';this.nodes.mic.title=active?'Detener dictado':'Dictar consulta';this.nodes.mic.setAttribute('aria-label',active?'Detener micrófono':'Activar micrófono');this.nodes.mic.setAttribute('aria-pressed',active?'true':'false');}setReadResponsesUi(active){if(!this.nodes.speak)return;this.readResponsesEnabled=!!active&&!this.nodes.speak.disabled;this.nodes.speak.classList.toggle('active',this.readResponsesEnabled);this.nodes.speak.innerHTML=this.readResponsesEnabled?'<svg viewBox="0 0 24 24" aria-hidden="true"><path d="M4 9v6h4l5 4V5L8 9H4z"></path><path d="M16 9a4 4 0 0 1 0 6M18.5 6.5a7.5 7.5 0 0 1 0 11"></path></svg>':'<svg viewBox="0 0 24 24" aria-hidden="true"><path d="M4 9v6h4l5 4V5L8 9H4z"></path><path d="m16.5 9 4 6M20.5 9l-4 6"></path></svg>';this.nodes.speak.title=this.readResponsesEnabled?'Desactivar lectura de respuestas':'Leer las respuestas';this.nodes.speak.setAttribute('aria-label',this.readResponsesEnabled?'Desactivar lectura de respuestas':'Activar lectura de respuestas');this.nodes.speak.setAttribute('aria-pressed',this.readResponsesEnabled?'true':'false');}toggleReadResponses(){if(!this.nodes.speak||this.nodes.speak.disabled)return;const enabled=!this.readResponsesEnabled;sessionStorage.setItem('bim-ai-read-responses',enabled?'1':'0');this.setReadResponsesUi(enabled);if(!enabled)this.stopSpeaking();}stopSpeaking(){if('speechSynthesis'in window){try{window.speechSynthesis.cancel();}catch(_){}}this.activeSpeechUtterance=null;}sanitizeSpeechText(text,hasTable){let value=String(text||'').replace(/```[\s\S]*?```/g,' ').replace(/https?:\/\/\S+/gi,' ').replace(/[`*_#~>|{}\[\]<>]/g,' ').replace(/[•▪◦→←↑↓✓✔✕·…©®™]/g,' ').replace(/[\p{So}\p{Sk}]/gu,' ').replace(/^\s*\d+[.)]\s*/gm,'').replace(/\s+/g,' ').trim();if(hasTable&&value)value+='. Los detalles se muestran en la tabla.';return value;}speakAssistantMessage(text,hasTable){if(!this.readResponsesEnabled||!('speechSynthesis'in window)||typeof window.SpeechSynthesisUtterance!=='function')return;const spokenText=this.sanitizeSpeechText(text,hasTable);if(!spokenText)return;this.stopSpeaking();const utterance=new window.SpeechSynthesisUtterance(spokenText);utterance.lang=navigator.language&&/^es(?:-|$)/i.test(navigator.language)?navigator.language:'es-PE';const voices=window.speechSynthesis.getVoices?window.speechSynthesis.getVoices():[];const spanishVoice=voices.find(voice=>/^es(?:-|_)/i.test(String(voice.lang||'')));if(spanishVoice)utterance.voice=spanishVoice;utterance.rate=1;utterance.onend=utterance.onerror=()=>{if(this.activeSpeechUtterance===utterance)this.activeSpeechUtterance=null;};this.activeSpeechUtterance=utterance;window.speechSynthesis.speak(utterance);}joinSpeechText(baseText,addition){const base=String(baseText||'').replace(/[ \t]+$/g,'');const next=String(addition||'').replace(/\s+/g,' ').trim();if(!next)return base;if(!base)return next;return base+(/\s$/.test(base)?'':' ')+next;}stripTrackedSpeechInterim(value){const text=String(value||'');const interim=String(this.speechInterimText||'').trim();if(!interim)return text;const rightTrimmed=text.replace(/\s+$/g,'');if(!rightTrimmed.endsWith(interim))return text;const start=rightTrimmed.length-interim.length;if(start>0&&!/\s/.test(rightTrimmed.charAt(start-1)))return text;return rightTrimmed.slice(0,start).replace(/[ \t]+$/g,'');}resetSpeechRecognitionCycle(){this.speechInterimText='';this.speechRenderedValue=this.nodes.prompt?this.nodes.prompt.value:'';this.speechLastResultCount=0;this.speechIgnoreThroughIndex=-1;this.speechResultTranscripts=new Map();this.speechDiscardedTranscripts=new Map();}handlePromptEdited(){if(!this.recognitionWanted)return;this.speechInterimText='';this.speechRenderedValue=this.nodes.prompt.value;if(this.recognitionActive&&this.recognition&&typeof this.recognition.abort==='function'){this.speechRestartForEdit=true;try{this.recognition.abort();}catch(_){this.speechRestartForEdit=false;this.recognitionActive=false;this.resetSpeechRecognitionCycle();setTimeout(()=>this.startRecognitionEngine(),40);}}else{this.resetSpeechRecognitionCycle();}}handleRecognitionResult(event){if(!this.recognitionWanted||this.speechRestartForEdit||!event||!event.results)return;const results=event.results;const start=Math.max(0,Number(event.resultIndex)||0);this.speechLastResultCount=Math.max(this.speechLastResultCount,Number(results.length)||0);let finalText='';let interimText='';for(let index=start;index<results.length;index++){const result=results[index];const rawTranscript=result&&result[0]?String(result[0].transcript||'').trim():'';const discardedTranscript=index<=this.speechIgnoreThroughIndex?String(this.speechDiscardedTranscripts&&this.speechDiscardedTranscripts.get(index)||'').trim():'';if(this.speechResultTranscripts)this.speechResultTranscripts.set(index,rawTranscript);let transcript=rawTranscript;if(index<=this.speechIgnoreThroughIndex){if(!discardedTranscript||!rawTranscript.toLocaleLowerCase().startsWith(discardedTranscript.toLocaleLowerCase()))continue;transcript=rawTranscript.slice(discardedTranscript.length).trim();}if(!transcript)continue;if(result.isFinal)finalText=this.joinSpeechText(finalText,transcript);else interimText=this.joinSpeechText(interimText,transcript);}const currentText=this.stripTrackedSpeechInterim(this.nodes.prompt.value);const committedText=this.joinSpeechText(currentText,finalText);const renderedText=this.joinSpeechText(committedText,interimText);this.nodes.prompt.value=renderedText;this.speechInterimText=interimText;this.speechRenderedValue=renderedText;this.nodes.prompt.selectionStart=this.nodes.prompt.selectionEnd=renderedText.length;}async toggleRecognition(){if(this.busy||this.nodes.mic.disabled)return;if(this.recognitionWanted){this.stopRecognition();return;}const Recognition=window.SpeechRecognition||window.webkitSpeechRecognition;if(!Recognition)return;this.recognitionWanted=true;this.setMicrophoneUi(true);try{await this.ensureMicrophone();this.setMicrophoneEnabled(true);}catch(_){this.recognitionWanted=false;this.setMicrophoneUi(false);this.addMessage('assistant','No pude acceder al micrófono. Habilita el permiso para este sitio y vuelve a intentarlo.','Reconocimiento de voz');return;}if(!this.recognition){this.recognition=new Recognition();this.recognition.lang=navigator.language||'es-PE';this.recognition.interimResults=true;this.recognition.continuous=true;this.recognition.maxAlternatives=1;this.recognition.onresult=event=>this.handleRecognitionResult(event);this.recognition.onerror=event=>{const code=String(event&&event.error||'');if(code==='not-allowed'||code==='service-not-allowed'){this.recognitionWanted=false;this.setMicrophoneUi(false);this.setMicrophoneEnabled(false);this.addMessage('assistant','El navegador bloqueó el micrófono. Habilita su permiso para esta página.','Reconocimiento de voz');}};this.recognition.onend=()=>{this.recognitionActive=false;const restartingAfterEdit=this.speechRestartForEdit;this.speechRestartForEdit=false;if(this.recognitionWanted&&!this.busy){this.resetSpeechRecognitionCycle();setTimeout(()=>this.startRecognitionEngine(),restartingAfterEdit?40:220);}else{this.setMicrophoneUi(false);this.setMicrophoneEnabled(false);}};}this.resetSpeechRecognitionCycle();this.startRecognitionEngine();}startRecognitionEngine(){if(!this.recognitionWanted||this.recognitionActive||!this.recognition)return;try{this.recognition.lang=navigator.language||'es-PE';this.recognition.start();this.recognitionActive=true;}catch(_){}}stopRecognition(){this.recognitionWanted=false;this.speechRestartForEdit=false;this.resetSpeechRecognitionCycle();if(this.recognition&&this.recognitionActive){try{this.recognition.stop();}catch(_){try{this.recognition.abort();}catch(_){}}}this.recognitionActive=false;this.setMicrophoneEnabled(false);if(this.nodes.mic)this.setMicrophoneUi(false);}async send(){const prompt=this.nodes.prompt.value.trim();if(!prompt||this.busy)return;if(this.recognitionWanted)this.stopRecognition();this.nodes.prompt.value='';const provider=window.BimAiProviders[this.providerId];if(this.isLocalGreeting(prompt)){this.addMessage('user',prompt);this.addMessage('assistant','Hola. Puedo consultar el modelo, generar reportes Excel y usar acciones controladas del visor, como seleccionar, aislar, colorear, medir o consultar elevaciones.','Respuesta local · sin consumo de API');return;}let key=this.nodes.key.value.trim();if(!key&&this.providerRequiresClientKey(provider.id)&&this.proxyReady!==true){await this.checkProxyHealth();key=this.nodes.key.value.trim();}if(!key&&this.providerRequiresClientKey(provider.id)){document.getElementById('aiSettings').open=true;this.addMessage('assistant','Ingresa la API key del proveedor seleccionado para continuar.');return;}this.addMessage('user',prompt);this.busy=true;this.nodes.send.disabled=true;this.abortController=new AbortController();let diagnosticEntry=this.diagnostics?this.diagnostics.start({prompt,provider:provider.id,model:this.nodes.model.value,context:this.catalog.compactContext?this.catalog.compactContext():{}}):null;let diagnosticStage='catalog';try{this.setProgress(true,'Interpretando la consulta...',0,1);await this.catalog.prepare((message,current,total)=>this.setProgress(true,message,current,total));diagnosticStage='interpretation';const response=await provider.createPlan(prompt,this.buildProviderContext(),{key,model:this.nodes.model.value,signal:this.abortController.signal});this.recordProviderUsage(response);response.plan=this.enrichFollowUpPlan(prompt,response.plan);if(this.diagnostics)this.diagnostics.recordInterpretation(diagnosticEntry,response,response.plan);diagnosticStage='local-execution';this.setProgress(true,'Ejecutando consulta local...',0,1);const result=await this.engine.execute(response.plan,{onProgress:(message,current,total)=>this.setProgress(true,message,current,total)});if(this.diagnostics)this.diagnostics.recordExecution(diagnosticEntry,result);if(result.needsClarification){this.pendingClarification=result;this.renderClarification(result.clarification,response);this.addMessage('assistant',result.clarification.prompt,response.provider+' · '+this.modelLabel(response.model)+this.responseUsageLabel(response));if(this.diagnostics)this.diagnostics.complete(diagnosticEntry,'needs-clarification');}else{this.rememberConversationResult(result);await this.addResultMessage(result,response,prompt);if(this.diagnostics)this.diagnostics.complete(diagnosticEntry,'completed');}}catch(error){if(this.diagnostics)this.diagnostics.fail(diagnosticEntry,diagnosticStage,error);this.addMessage('assistant','No pude completar la consulta: '+error.message,provider.label);}finally{this.busy=false;this.nodes.send.disabled=false;this.setProgress(false);this.abortController=null;}}renderClarification(clarification,response){const host=this.nodes.clarification;host.innerHTML='';host.classList.remove('hidden');const header=document.createElement('div');header.className='ai-clarification-header';const title=document.createElement('strong');const options=Array.isArray(clarification.options)?clarification.options:[];title.textContent=clarification.kind==='value'?'Elige el valor correcto':(options.length?'Elige el parámetro correcto':'La consulta necesita mayor precisión');const cancel=document.createElement('button');cancel.type='button';cancel.className='ai-clarification-cancel';cancel.textContent='×';cancel.title='Cancelar esta aclaración';cancel.setAttribute('aria-label','Cancelar aclaración');cancel.addEventListener('click',()=>this.cancelClarification());header.appendChild(title);header.appendChild(cancel);host.appendChild(header);if(!options.length){const note=document.createElement('span');note.textContent=clarification.prompt||'Reformula la consulta indicando el parámetro o valor que deseas utilizar.';host.appendChild(note);const close=document.createElement('button');close.type='button';close.textContent='Entendido';close.addEventListener('click',()=>this.cancelClarification());host.appendChild(close);return;}const select=document.createElement('select');options.forEach(option=>{const node=document.createElement('option');node.value=option.value;node.textContent=option.label+(option.detail?' · '+option.detail:'');select.appendChild(node);});host.appendChild(select);const button=document.createElement('button');button.type='button';button.textContent='Continuar';button.addEventListener('click',()=>this.continueClarification(select.value,response));host.appendChild(button);}cancelClarification(){this.pendingClarification=null;if(this.nodes.clarification){this.nodes.clarification.innerHTML='';this.nodes.clarification.classList.add('hidden');}if(this.nodes.prompt)this.nodes.prompt.focus();}replaceClarifiedField(plan,requested,fieldKey){const steps=Array.isArray(plan.steps)?plan.steps:[plan];steps.forEach(step=>{if(!step||typeof step!=='object')return;(step.filters||[]).forEach(filter=>{if(filter.field===requested)filter.field=fieldKey;});if(step.groupBy===requested)step.groupBy=fieldKey;else if(String(step.groupBy||'').includes(requested)){step.groupBy=String(step.groupBy).split(/\s*(?:>|\|)\s*/).map(item=>item.trim()===requested?fieldKey:item.trim()).filter(Boolean).join(' > ');}if(step.numericFields===requested)step.numericFields=fieldKey;else if(String(step.numericFields||'').includes(requested)){step.numericFields=String(step.numericFields).split(/\s*\|\s*/).map(item=>item.trim()===requested?fieldKey:item.trim()).filter(Boolean).join(' | ');}for(const property of['reportFields','reportEmptyFields']){if(step[property]===requested)step[property]=fieldKey;else if(String(step[property]||'').includes(requested)){step[property]=String(step[property]).split(/\s*\|\s*/).map(item=>item.trim()===requested?fieldKey:item.trim()).filter(Boolean).join(' | ');}}if(step.parameter===requested)step.parameter=fieldKey;if(step.target===requested)step.target=fieldKey;});}async continueClarification(fieldKey,response){const pending=this.pendingClarification;if(!pending||this.busy)return;const requested=pending.clarification.field;const plan=JSON.parse(JSON.stringify(pending.plan));const step=Array.isArray(plan.steps)?plan.steps[Math.max(0,Number(pending.clarificationStepIndex)||0)]:plan;const kind=pending.clarification.kind||'field';if(kind==='value'){const filterIndex=Math.max(0,Number(pending.clarification.filterIndex)||0);if(step&&Array.isArray(step.filters)&&step.filters[filterIndex])step.filters[filterIndex].value=fieldKey;}else if(kind==='numeric_field'){if(step)step.numericFields=fieldKey;}else if(kind==='report_empty_field'){if(step){step.reportFields=fieldKey;step.reportEmptyFields=fieldKey;step.reportMode='observations';}}else if(kind==='edit_parameter'){if(step)step.parameter=fieldKey;}else if(kind==='tree_target'){if(step)step.target=fieldKey;}else{this.replaceClarifiedField(plan,requested,fieldKey);}this.nodes.clarification.classList.add('hidden');this.pendingClarification=null;this.busy=true;let diagnosticEntry=this.diagnostics?this.diagnostics.start({prompt:'Continuar aclaración con: '+fieldKey,provider:response&&response.provider,model:response&&response.model,context:this.catalog.compactContext?this.catalog.compactContext():{}}):null;try{if(this.diagnostics)this.diagnostics.recordInterpretation(diagnosticEntry,response,plan);this.setProgress(true,'Continuando consulta local...',0,1);const result=await this.engine.execute(plan,{onProgress:(message,current,total)=>this.setProgress(true,message,current,total)});if(this.diagnostics)this.diagnostics.recordExecution(diagnosticEntry,result);if(result.needsClarification){this.pendingClarification=result;this.renderClarification(result.clarification,response);if(this.diagnostics)this.diagnostics.complete(diagnosticEntry,'needs-clarification');}else{this.rememberConversationResult(result);await this.addResultMessage(result,response,'Continuar aclaración');if(this.diagnostics)this.diagnostics.complete(diagnosticEntry,'completed');}}catch(error){if(this.diagnostics)this.diagnostics.fail(diagnosticEntry,'clarification-execution',error);this.addMessage('assistant','No pude completar la consulta: '+error.message,'Motor local');}finally{this.busy=false;this.setProgress(false);}}async addResultMessage(result,response,prompt){const action=result.plan.action;const reportOutput=await this.generateReportAttachments(result,prompt);let text='Encontré '+Number(result.count||0).toLocaleString('es-PE')+' resultado(s).';if(result.isSequence&&result.sequenceCount>1){text='Ejecuté '+result.sequenceCount+' acciones en el orden solicitado.';text+='\n'+result.steps.map((step,index)=>{const label=this.actionLabel(step.plan&&step.plan.action);return(index+1)+'. '+label+' · '+Number(step.count||0).toLocaleString('es-PE')+' resultado(s)';}).join('\n');}if(result.calculation&&result.calculation.operation&&result.calculation.operation!=='none'){if(result.calculation.operation==='percentage'){const numerator=Number(result.calculation.numeratorCount||0);const denominator=Number(result.calculation.denominatorCount||0);const formatted=result.calculation.formatted||(Number.isFinite(result.calculation.percentage)?Number(result.calculation.percentage).toLocaleString('es-PE',{maximumFractionDigits:2})+' %':'0 %');text+='\nPorcentaje local: '+formatted+' ('+numerator.toLocaleString('es-PE')+' de '+denominator.toLocaleString('es-PE')+').';}else{text+='\nCálculo local: '+this.numericOperationLabel(result.calculation.operation)+' de '+(result.calculation.requestedFields||[]).join(' y ')+'.';}}if(!(result.isSequence&&result.sequenceCount>1)){if(action==='select')text+=' Los seleccioné en el visor 3D.';else if(action==='isolate')text+=' Los aislé en el visor 3D.';else if(action==='isolate_color')text+=' Los aislé y apliqué los colores solicitados.';else if(action==='hide')text+=' Los oculté en el visor 3D.';else if(action==='tree_control'){const info=result.treeExecution||{};const target=info.target?' “'+info.target+'”':'';const labels={show:'Mostré',hide:'Oculté',show_only:'Dejé visible únicamente',show_all:'Mostré todos los nodos de',hide_all:'Oculté todos los nodos de',expand:'Expandí',collapse:'Contraje'};text=(labels[info.operation]||'Actualicé')+target+' en el árbol de '+(info.targetType==='category'?'Categorías':'Proyecto')+'.';}else if(action==='set_parameter'||action==='replace_parameter'){const edit=result.editExecution||{};if(edit.cancelled)text='La edición fue cancelada. No se modificó ningún valor.';else if(!edit.changedCells)text='No encontré valores que necesiten modificarse en “'+(edit.parameterLabel||result.plan.parameter||'el parámetro')+'”.';else text='Actualicé '+edit.changedCells+' celda'+(edit.changedCells===1?'':'s')+' en '+edit.changedElements+' elemento'+(edit.changedElements===1?'':'s')+' para “'+(edit.parameterLabel||result.plan.parameter||'el parámetro')+'”.';}else if(action==='color')text+=result.plan.colorMode==='by_group'?' Apliqué colores diferentes por grupo.':' Apliqué el color solicitado.';else if(action==='transparency')text+=' Apliqué la transparencia solicitada.';else if(action==='transparency_highlight')text+=' Apliqué transparencia al contexto y mantuve resaltados los elementos solicitados.';else if(action==='export_excel')text+=' Generé el reporte Excel solicitado; puedes descargarlo en este mensaje.';else if(action==='select_same_type')text='Seleccioné los elementos del mismo tipo mediante la herramienta del visor.';else if(action==='zoom_selection')text+=' Encuadré los elementos solicitados.';else if(action==='fit_view')text='Encuadré el modelo completo.';else if(action==='area_zoom')text='Activé el zoom por ventana.';else if(action==='section_box')text='Actualicé la caja de corte.';else if(action==='measurement_tool')text='Activé la herramienta de medición.';else if(action==='elevation_tool')text='Activé la consulta de elevación.';else if(action==='toggle_level_grid')text='Actualicé la rejilla de niveles.';else if(action==='restore_visibility')text='Restablecí los elementos ocultos o aislados.';else if(action==='reset_view')text='Restablecí la vista general.';else if(action==='fade_selection')text='Apliqué la transparencia del visor conservando la selección.';else if(action==='clear')text='Restablecí los efectos visuales solicitados.';}if(reportOutput.truncated)text+='\nEl archivo alcanzó el límite seguro de Excel; se exportaron '+Number(reportOutput.exportedRows||0).toLocaleString('es-PE')+' filas.';const table=reportOutput.previewTable||this.createResultTable(result);const attempts=response.attempts&&response.attempts.length>1?' · respaldo automático: '+response.attempts.map(item=>this.modelLabel(item)).join(' → '):'';this.addMessage('assistant',text,response.provider+' · '+this.modelLabel(response.model)+attempts+this.responseUsageLabel(response),table,reportOutput.attachments);}async generateReportAttachments(result,prompt){const reportResults=result&&result.isSequence&&Array.isArray(result.steps)?result.steps.filter(step=>step&&step.plan&&step.plan.action==='export_excel'):(result&&result.plan&&result.plan.action==='export_excel'?[result]:[]);if(!reportResults.length)return{attachments:[],previewTable:null,truncated:false,exportedRows:0};if(!this.reportService)throw new Error('El generador de reportes Excel no está disponible en esta versión.');const attachments=[];let previewTable=null;let truncated=false;let exportedRows=0;for(let index=0;index<Math.min(3,reportResults.length);index++){const reportResult=reportResults[index];this.setProgress(true,'Generando reporte Excel '+(index+1)+' de '+Math.min(3,reportResults.length)+'...',index,Math.max(1,reportResults.length));const file=await this.reportService.create(reportResult,{prompt,onProgress:(message,current,total)=>this.setProgress(true,message,current,total)});const attachment=this.registerReportAttachment(file);if(attachment)attachments.push(attachment);if(!previewTable)previewTable=file.previewTable;truncated=truncated||!!file.truncated;exportedRows+=Number(file.rowCount)||0;}return{attachments,previewTable,truncated,exportedRows};}actionLabel(action){const labels={count:'Contar',list:'Listar',export_excel:'Generar reporte Excel',summarize:'Resumir',select:'Seleccionar',select_same_type:'Seleccionar mismo tipo',isolate:'Aislar',isolate_color:'Aislar y colorear',hide:'Ocultar',color:'Aplicar color',transparency:'Aplicar transparencia',transparency_highlight:'Transparencia y resaltado',zoom_selection:'Encuadrar resultados',fit_view:'Encuadrar modelo',area_zoom:'Zoom por ventana',section_box:'Caja de corte',measurement_tool:'Herramienta de medición',elevation_tool:'Consulta de elevación',toggle_level_grid:'Rejilla de niveles',restore_visibility:'Restaurar visibilidad',reset_view:'Restablecer visor',fade_selection:'Transparentar contexto',clear:'Limpiar efectos'};return labels[action]||String(action||'Acción');}numericOperationLabel(operation){const labels={sum:'Suma',average:'Promedio',minimum:'Mínimo',maximum:'Máximo',subtract:'Resta',multiply:'Multiplicación',divide:'División',percentage:'Porcentaje'};return labels[operation]||String(operation||'Cálculo');}extractTableElementKeysFromRecord(record){const keys=new Set();const add=value=>{if(Array.isArray(value)||value instanceof Set){for(const item of value)add(item);return;}const text=String(value==null?'':value).trim();if(text&&text.includes(':'))keys.add(text);};const source=record&&typeof record==='object'?record:{};['elementKey','ElementKey','elementAKey','elementBKey','ElementAKey','ElementBKey','selectedElementKeys','elementKeys','keys'].forEach(name=>add(source[name]));return Array.from(keys);}createResultTable(result){if(!result||!result.plan)return null;if(result.calculation&&Array.isArray(result.groups)&&result.groups.length){if(result.calculation.operation==='percentage'){const rows=result.groups.map(item=>[item.value,item.formattedValue||'0 %',Number(item.count||0)]);return{headers:[result.plan.groupBy||'Resultado','Porcentaje','Elementos'],rows,totalRows:rows.length,rowKeys:result.groups.map(item=>Array.from(item&&item.keys||[])),selectable:result.source==='model'};}const fields=(result.calculation.requestedFields||[]).join(' | ');const rows=result.groups.map(item=>[item.value,item.formattedValue||'Sin datos numéricos',Number(item.numericCount||0),Number(item.count||0)]);return{headers:[result.plan.groupBy||'Grupo',this.numericOperationLabel(result.calculation.operation)+(fields?': '+fields:''),'Valores válidos','Elementos'],rows,totalRows:rows.length,rowKeys:result.groups.map(item=>Array.from(item&&item.keys||[])),selectable:result.source==='model'};}if(result.isSequence&&Array.isArray(result.steps)&&result.steps.length>1){const rows=result.steps.map((step,index)=>[index+1,this.actionLabel(step.plan&&step.plan.action),step.plan&&step.plan.scope||'all',Number(step.count||0),step.viewerExecution&&step.viewerExecution.api||(step.localExecution&&step.localExecution.directViewerAction?'Comando compartido del visor':'Consulta local')]);return{headers:['Paso','Acción','Alcance','Resultados','Ejecución optimizada'],rows,totalRows:rows.length,rowKeys:result.steps.map(step=>Array.from(step&&step.keys||[])),selectable:result.steps.some(step=>step&&Array.isArray(step.keys)&&step.keys.length)};}if(result.groups&&result.groups.length){const rows=result.groups.map(item=>[item.value,Number(item.count||0)]);return{headers:[result.plan.groupBy||'Valor','Cantidad'],rows,totalRows:rows.length,rowKeys:result.groups.map(item=>Array.from(item&&item.keys||[])),selectable:result.source==='model'};}if(!['list','summarize'].includes(result.plan.action))return null;if(result.source==='model'&&Array.isArray(result.keys)&&result.keys.length){const store=this.catalog&&(this.catalog.store||this.catalog.basicStore);const headers=['Proyecto','Modelo','Ruta de carpeta','Archivo de origen','ElementId','UniqueId','Categoría','Familia','Tipo','Nivel'];if(store&&store.elementByKey instanceof Map){const keys=result.keys.slice();const getRow=index=>{const element=store.elementByKey.get(keys[index])||{};const structure=this.catalog&&this.catalog.getProjectStructureForElement?this.catalog.getProjectStructureForElement(element):null;return[element.ProjectName||element.ModelId||'',element.ModelId||element.ProjectName||'',structure&&structure.folderPath||'',structure&&structure.sourceDocument||'',element.ElementId||'',element.UniqueId||'',element.CategoryName||'',element.FamilyName||'',element.TypeName||'',element.LevelName||''];};return{headers,rows:keys.slice(0,Math.min(60,keys.length)).map((_key,index)=>getRow(index)),totalRows:keys.length,getRow,getRowKeys:index=>keys[index]?[keys[index]]:[],selectable:true};}}const records=Array.isArray(result.__tableRecords)?result.__tableRecords:(Array.isArray(result.samples)?result.samples:[]);const sourceRecords=Array.isArray(result.__tableSourceRecords)?result.__tableSourceRecords:records;if(!records.length)return null;if(result.source==='model'){const headers=['Proyecto','Modelo','Ruta de carpeta','Archivo de origen','ElementId','Categoría','Familia','Tipo','Nivel'];return{headers,rows:records.map(item=>[item.project,item.project,item.folder,item.sourceDocument,item.elementId,item.category,item.family,item.type,item.level]),totalRows:records.length,selectable:false};}const headers=Array.from(records.reduce((keys,item)=>{Object.keys(item||{}).forEach(key=>keys.add(key));return keys;},new Set()));if(!headers.length)return null;const getRow=index=>headers.map(key=>records[index]&&records[index][key]);const getRowKeys=index=>this.extractTableElementKeysFromRecord(sourceRecords[index]);return{headers,rows:records.slice(0,Math.min(60,records.length)).map((_item,index)=>getRow(index)),totalRows:records.length,getRow,getRowKeys,selectable:sourceRecords.slice(0,Math.min(sourceRecords.length,50)).some(record=>this.extractTableElementKeysFromRecord(record).length)};}downloadDiagnostics(){if(!this.diagnostics){this.addMessage('assistant','El diagnóstico local no está disponible en esta sesión.','Diagnóstico IA');return;}const count=this.diagnostics.download();this.addMessage('assistant','Descargué el diagnóstico de '+count+' consulta(s). El archivo separa el plan interpretado, el filtrado local y la acción aplicada al visor.','Diagnóstico IA · no incluye API keys');}conversationFingerprint(){const model=this.catalog.getModel?this.catalog.getModel():null;return(model&&this.catalog.createFingerprint?this.catalog.createFingerprint(model):this.catalog.fingerprint)||'current';}historyKey(){return'bim-ai-history:'+this.conversationFingerprint();}conversationsKey(){return'bim-ai-conversations:'+this.conversationFingerprint();}createConversation(messages){const now=new Date().toISOString();return{id:'conversation-'+Date.now()+'-'+Math.random().toString(36).slice(2,8),title:'Nueva conversación',createdAt:now,updatedAt:now,messages:Array.isArray(messages)?messages:[],lastResult:null};}normalizeStoredMessage(item){const source=item&&typeof item==='object'?item:{};return{role:source.role==='user'?'user':'assistant',text:String(source.text||'').slice(0,12000),meta:String(source.meta||'').slice(0,1000),table:this.normalizeTable(source.table),attachments:this.normalizeAttachments(source.attachments)};}normalizeConversation(item){const source=item&&typeof item==='object'?item:{};const createdAt=source.createdAt||new Date().toISOString();return{id:String(source.id||('conversation-'+Date.now()+'-'+Math.random().toString(36).slice(2,8))),title:String(source.title||'Nueva conversación').slice(0,90),createdAt,updatedAt:source.updatedAt||createdAt,messages:(Array.isArray(source.messages)?source.messages:[]).slice(-80).map(value=>this.normalizeStoredMessage(value)),lastResult:source.lastResult&&typeof source.lastResult==='object'?source.lastResult:null};}compressElementKeys(keys){const groups=new Map();for(const rawKey of keys||[]){const key=String(rawKey||'');if(!key)continue;const separator=key.lastIndexOf(':');const prefix=separator>=0?key.slice(0,separator+1):'';const suffix=separator>=0?key.slice(separator+1):key;if(!groups.has(prefix))groups.set(prefix,[]);groups.get(prefix).push(suffix);}return Array.from(groups,([prefix,values])=>[prefix,values]);}decompressElementKeys(groups){const keys=[];for(const group of groups||[]){if(!Array.isArray(group)||!Array.isArray(group[1]))continue;const prefix=String(group[0]||'');for(const suffix of group[1])keys.push(prefix+String(suffix==null?'':suffix));}return keys;}createLastResultSnapshot(result){const candidate=result&&result.isSequence&&Array.isArray(result.steps)?result.steps.slice().reverse().find(step=>step&&Array.isArray(step.keys)):result;if(!candidate||!Array.isArray(candidate.keys)||!candidate.plan)return null;return{source:String(candidate.source||'model'),plan:JSON.parse(JSON.stringify(candidate.plan)),count:Number(candidate.count)||0,keyGroups:this.compressElementKeys(candidate.keys)};}restoreLastResultSnapshot(snapshot){if(!snapshot||!snapshot.plan||!Array.isArray(snapshot.keyGroups)){this.engine.lastResult=null;return null;}const restored={source:snapshot.source||'model',plan:JSON.parse(JSON.stringify(snapshot.plan)),count:Number(snapshot.count)||0,keys:this.decompressElementKeys(snapshot.keyGroups),samples:[],groups:[]};this.engine.lastResult=restored;return restored;}rememberConversationResult(result){const snapshot=this.createLastResultSnapshot(result);if(!snapshot)return;this.currentLastResultSnapshot=snapshot;const conversation=this.getCurrentConversation();if(conversation)conversation.lastResult=snapshot;}getCurrentConversation(){return this.conversations.find(item=>item.id===this.currentConversationId)||null;}ensureCurrentConversation(){let conversation=this.getCurrentConversation();if(conversation)return conversation;conversation=this.createConversation([]);this.conversations.push(conversation);this.currentConversationId=conversation.id;return conversation;}loadConversationCollection(){let state=null;try{state=JSON.parse(localStorage.getItem(this.conversationsKey())||'null');}catch(_){}let conversations=state&&Array.isArray(state.conversations)?state.conversations.map(item=>this.normalizeConversation(item)):[];if(!conversations.length){let legacyMessages=[];try{legacyMessages=JSON.parse(localStorage.getItem(this.historyKey())||'[]');}catch(_){}if(Array.isArray(legacyMessages)&&legacyMessages.length){const migrated=this.createConversation(legacyMessages.map(item=>this.normalizeStoredMessage(item)));const firstUser=migrated.messages.find(item=>item.role==='user'&&item.text.trim());if(firstUser)migrated.title=firstUser.text.replace(/\s+/g,' ').trim().slice(0,64);conversations.push(migrated);state={activeId:migrated.id};}}this.conversations=conversations;const requestedActive=state&&String(state.activeId||'');const fallback=conversations.slice().sort((a,b)=>String(b.updatedAt).localeCompare(String(a.updatedAt)))[0];this.currentConversationId=conversations.some(item=>item.id===requestedActive)?requestedActive:(fallback&&fallback.id||'');this.currentLastResultSnapshot=this.getCurrentConversation()&&this.getCurrentConversation().lastResult||null;this.restoreLastResultSnapshot(this.currentLastResultSnapshot);}conversationStatePayload(options){const opts=options&&typeof options==='object'?options:{};const ordered=this.conversations.slice().sort((a,b)=>String(a.updatedAt).localeCompare(String(b.updatedAt))).slice(-30);return{version:2,activeId:this.currentConversationId,conversations:ordered.map(item=>({id:item.id,title:item.title,createdAt:item.createdAt,updatedAt:item.updatedAt,messages:item.messages.slice(opts.compact?-50:-80).map(message=>({role:message.role,text:message.text,meta:message.meta,table:opts.compact?null:message.table,attachments:message.attachments})),lastResult:opts.dropOldResults&&item.id!==this.currentConversationId?null:item.lastResult}))};}persistConversations(){const key=this.conversationsKey();try{localStorage.setItem(key,JSON.stringify(this.conversationStatePayload()));return true;}catch(_){}try{localStorage.setItem(key,JSON.stringify(this.conversationStatePayload({compact:true,dropOldResults:true})));return true;}catch(_){return false;}}conversationTitle(messages){const firstUser=(messages||[]).find(item=>item.role==='user'&&String(item.text||'').trim());return firstUser?String(firstUser.text).replace(/\s+/g,' ').trim().slice(0,64):'Nueva conversación';}saveHistory(){if(this.restoringConversation)return;const messages=Array.from(this.nodes.messages.querySelectorAll('.ai-message')).slice(-80).map(row=>row.__bimAiHistory||({role:row.classList.contains('user')?'user':'assistant',text:row.querySelector('.ai-message-text')?row.querySelector('.ai-message-text').textContent:'',meta:row.querySelector('small')?row.querySelector('small').textContent:'',table:null})).map(item=>this.normalizeStoredMessage(item));const conversation=this.ensureCurrentConversation();conversation.messages=messages;conversation.title=this.conversationTitle(messages);conversation.updatedAt=new Date().toISOString();conversation.lastResult=this.currentLastResultSnapshot;this.persistConversations();this.renderConversationList();}restoreHistory(){this.loadConversationCollection();let conversation=this.getCurrentConversation();if(!conversation){conversation=this.createConversation([]);this.conversations.push(conversation);this.currentConversationId=conversation.id;}this.nodes.messages.innerHTML='';this.restoringConversation=true;conversation.messages.forEach(item=>this.addMessage(item.role,item.text,item.meta,item.table,item.attachments));this.restoringConversation=false;if(!conversation.messages.length){this.addMessage('assistant','Puedo consultar elementos, parámetros, interferencias, incidencias y láminas, generar reportes Excel y ejecutar acciones seguras del visor. La IA interpreta tu pedido; la plataforma calcula el resultado localmente.');}this.renderConversationList();}renderConversationList(){if(!this.nodes.conversationList)return;const list=this.conversations.slice().sort((a,b)=>String(b.updatedAt).localeCompare(String(a.updatedAt)));this.nodes.conversationCount.textContent=String(list.length);this.nodes.conversationList.innerHTML='';if(!list.length){const empty=document.createElement('div');empty.className='ai-conversation-empty';empty.textContent='No hay conversaciones guardadas.';this.nodes.conversationList.appendChild(empty);return;}list.forEach(conversation=>{const row=document.createElement('div');row.className='ai-conversation-item'+(conversation.id===this.currentConversationId?' active':'');const open=document.createElement('button');open.type='button';open.className='ai-conversation-open';open.setAttribute('data-ai-conversation-open',conversation.id);const title=document.createElement('strong');title.textContent=conversation.title||'Nueva conversación';const detail=document.createElement('small');const date=new Date(conversation.updatedAt||conversation.createdAt||Date.now());detail.textContent=conversation.messages.length+' mensajes · '+date.toLocaleString('es-PE',{dateStyle:'short',timeStyle:'short'});open.appendChild(title);open.appendChild(detail);const remove=document.createElement('button');remove.type='button';remove.className='ai-conversation-delete';remove.setAttribute('data-ai-conversation-delete',conversation.id);remove.setAttribute('aria-label','Eliminar conversación');remove.title='Eliminar conversación';remove.textContent='×';row.appendChild(open);row.appendChild(remove);this.nodes.conversationList.appendChild(row);});}renderCurrentConversation(){const conversation=this.getCurrentConversation();this.nodes.messages.innerHTML='';this.nodes.clarification.classList.add('hidden');this.pendingClarification=null;this.currentLastResultSnapshot=conversation&&conversation.lastResult||null;this.restoreLastResultSnapshot(this.currentLastResultSnapshot);this.restoringConversation=true;for(const item of conversation&&conversation.messages||[])this.addMessage(item.role,item.text,item.meta,item.table,item.attachments);this.restoringConversation=false;if(!conversation||!conversation.messages.length){this.addMessage('assistant','Puedo consultar elementos, parámetros, interferencias, incidencias y láminas, generar reportes Excel y ejecutar acciones seguras del visor. La IA interpreta tu pedido; la plataforma calcula el resultado localmente.');}this.nodes.messages.scrollTop=this.nodes.messages.scrollHeight;this.renderConversationList();}newConversation(){if(this.busy)return;if(this.recognitionWanted)this.stopRecognition();this.saveHistory();const conversation=this.createConversation([]);this.conversations.push(conversation);this.currentConversationId=conversation.id;this.currentLastResultSnapshot=null;this.engine.lastResult=null;this.renderCurrentConversation();this.persistConversations();if(this.nodes.conversations)this.nodes.conversations.open=false;this.nodes.prompt.focus();}openConversation(id){if(this.busy||!id||id===this.currentConversationId){if(this.nodes.conversations)this.nodes.conversations.open=false;return;}if(this.recognitionWanted)this.stopRecognition();this.saveHistory();if(!this.conversations.some(item=>item.id===id))return;this.currentConversationId=id;this.renderCurrentConversation();this.persistConversations();if(this.nodes.conversations)this.nodes.conversations.open=false;}async deleteConversation(id){if(this.busy||!id)return;const conversation=this.conversations.find(item=>item.id===id);if(!conversation)return;if(!window.BimDialogs||typeof window.BimDialogs.confirm!=='function')return;const confirmed=await window.BimDialogs.confirm('¿Deseas eliminar la conversación “'+conversation.title+'”? Esta acción no se puede deshacer.',{type:'warning',title:'Eliminar conversación',acceptText:'Eliminar',cancelText:'Cancelar'});if(!confirmed)return;const deletingCurrent=id===this.currentConversationId;this.conversations=this.conversations.filter(item=>item.id!==id);if(deletingCurrent){const next=this.conversations.slice().sort((a,b)=>String(b.updatedAt).localeCompare(String(a.updatedAt)))[0];if(next)this.currentConversationId=next.id;else{const created=this.createConversation([]);this.conversations.push(created);this.currentConversationId=created.id;}this.renderCurrentConversation();}this.persistConversations();this.renderConversationList();}clearHistory(){this.newConversation();}}window.BimAiPanelController=BimAiPanel;function init(){if(window.BimAiPanel||!window.BimAiCatalog||!window.BimAiProviders||!document.querySelector('.mobile-panel-buttons'))return;new BimAiPanel();}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init,{once:true});else init();})();