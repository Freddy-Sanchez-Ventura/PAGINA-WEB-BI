(function(){'use strict';async function bimConfirm(message,options){const safeMessage=sanitizeCloudUiMessage(message);if(window.BimDialogs&&typeof window.BimDialogs.confirm==='function'){return await window.BimDialogs.confirm(safeMessage,options||{});}return false;}async function bimPrompt(message,defaultValue,options){const safeMessage=sanitizeCloudUiMessage(message);if(window.BimDialogs&&typeof window.BimDialogs.prompt==='function'){return await window.BimDialogs.prompt(safeMessage,defaultValue,options||{});}return defaultValue==null?null:String(defaultValue);}function sanitizeCloudUiMessage(message,fallback){const text=String(message==null?'':message).trim();if(!text)return String(fallback||'No se pudo completar la operación en la nube.');if(/(google cloud storage|google sheets?|apps script|script properties|spreadsheet|\bgcs\b|\bbucket\b|\bcors\b|signed urls?|\/exec)/i.test(text)){return String(fallback||'El servicio en la nube no pudo completar la operación. Inténtalo nuevamente o contacta al administrador.');}return text.replace(/API cloud/gi,'servicio en la nube').replace(/servidor cloud/gi,'servicio en la nube').replace(/proyecto cloud/gi,'proyecto en la nube');}const DEFAULT_CONFIG={apiUrl:'',requestMode:'postAction',requestTimeoutMs:120000,transferTimeoutMs:1800000,maxParallelDownloads:3,maxParallelUploads:2,openAutoSelectAllModels:true,requestPersistentStorage:true,heartbeatIntervalMs:60000,actions:{login:'webLogin',currentUser:'webCurrentUser',heartbeat:'webHeartbeat',logout:'webLogout',closeSession:'webCloseSession',listProjects:'listProjects',openProject:'openProject',createUploadSession:'createUploadSession',finalizeUpload:'finalizeUpload',validateBiPackage:'validateBiPackage',readProjectState:'readProjectState',saveProjectState:'saveProjectState',createProjectFolder:'createProjectFolder',moveProjectItems:'moveProjectItems',deleteProjectItems:'deleteProjectItems',listProjectMembers:'listProjectMembers',saveProjectMember:'saveProjectMember',removeProjectMember:'removeProjectMember'}};const CONFIG=Object.assign({},DEFAULT_CONFIG,window.BIM_CLOUD_CONFIG||{});CONFIG.actions=Object.assign({},DEFAULT_CONFIG.actions,CONFIG.actions||{});const LEGACY_SESSION_KEY='bim_cloud_session_v1';const DEVICE_KEY='bim_cloud_device_v1';const DB_NAME='BimCloudFileCacheDB';const DB_VERSION=2;const FILE_STORE='files';const META_STORE='meta';const STATE_STORE='state_fallback';const BI_FILE_PATTERN=/\.bi$/i;const JSON_FILE_PATTERN=/\.json$/i;const ISSUE_IMAGE_PATTERN=/\.(webp|png|jpe?g)$/i;const CACHE_FOLDER_PATTERN=/(^|\/)ExportModel_Web_Cach[eé](\/|$)/i;const CACHE_FOLDER_NAME='ExportModel_Web_Cache';const ISSUES_BUNDLE_FOLDER=CACHE_FOLDER_NAME+'/Incidencias';const ISSUE_BUNDLE_STORE_PREFIX='issues-bundle|';const DEPRECATED_PROJECT_CACHE_NAMES={'mass_info_config.json':true,'viewer_state.json':true};const CORE_CACHE_STATE_NAMES=['cache_metadata.json','saved_configurations.json','works_saved_configurations.json','works_state.json','parameter_overrides.json','interferences_cache.json','plans_2d_cache.json'];const NON_SYNC_STATE_NAMES={'works_state.json':true};const CACHE_NAMES_TO_KEEP_ON_BI_UPDATE={'saved_configurations.json':true,'works_saved_configurations.json':true,'works_state.json':true,'cache_metadata.json':true};const CACHE_NAMES_INVALIDATED_BY_3D_BI=['parameter_overrides.json','interferences_cache.json'];const CACHE_NAMES_INVALIDATED_BY_2D_BI=['plans_2d_cache.json','plans_annotations.json','plan_annotations.json','annotations_2d.json','plans_folder_cache.json','plans_folder_structure.json','plans_state.json'];const PLANS_FOLDER_LOCAL_STORAGE_KEY='bimingenieros.exportmodel.plans.folderStructure.v1';const PLANS_ANNOTATION_LOCAL_STORAGE_KEY='bimingenieros.exportmodel.plans.annotations.v1';const INTERFERENCES_LOCAL_BACKUP_PREFIX='bimingenieros.exportmodel.interferences.cloud.v1:';const PLANS_2D_LOCAL_BACKUP_PREFIX='bimingenieros.exportmodel.plans.state.backup.v2:';const INTERFERENCES_LOCAL_BACKUP_MAX_BYTES=2000000;const stateDefaults={user:null,projects:[],selectedProjectId:'',busy:false,heartbeatTimer:0,selectedFolderPaths:{},selectedModelPaths:{},knownModelPaths:{},collapsedFolderPaths:{},activeStateService:null,activeCloudProject:null,cacheToolbarNode:null,showTechnicalFilesByProject:{}};const nodes={};const core=window.BimCore;const eventBus=window.BimEventBus;const appState=window.BimAppState;const storageServices=window.BimStorageServices;const cloudTransferService=window.BimCloudTransferService;const securityPolicy=window.BimSecurityPolicy;if(!core||!eventBus||!appState||!storageServices||!window.BimCloudApiClient||!cloudTransferService||!securityPolicy||!window.BimCloudUiKit||!window.BimCloudDashboardPanel||!window.BimCloudSessionPanel||!window.BimCloudProjectTree||!window.BimCloudChangesPanel){throw new Error('No se cargaron los módulos base requeridos por cloudAccess.js.');}const state=appState.createMutableSlice('cloud',stateDefaults);let sessionToken='';let activeSessionId='';let legacySessionCleared=false;const localStore=storageServices.local;const indexedDbService=storageServices.indexedDb;const cloudApiClient=window.BimCloudApiClient.create({getApiUrl:function(){return CONFIG.apiUrl;},getTimeoutMs:function(){return CONFIG.requestTimeoutMs;},getToken:function(){return sessionToken||'';},getSessionId:function(){return activeSessionId||'';},getClientInfo:getWebClientInfo,createApiError:createApiError});const cloudUiKit=window.BimCloudUiKit.create({core});const cloudChangesApi=window.BimCloudChangesPanel;const projectTree=window.BimCloudProjectTree.create({normalizePath,lastPathPart,escapeHtml,isBiFilePath,getSelectedFolderPath,isFolderOpen:isCloudFolderOpen,renderSelectionCheckbox:renderTreeSelectionCheckbox,canEdit:function(project){return getProjectAccess(project).canEdit;},canDelete:function(project){return getProjectAccess(project).canDelete;}});const dashboardPanel=window.BimCloudDashboardPanel.create({state,nodes,ui:cloudUiKit,localStore,eventBus,callbacks:{logout:function(){return logoutCloudSession(true);},showLogin:showLoginModal,loadProjects:loadProjectList,showUploadPicker,openSelectedProject,renderProjects,getSelectedProject,getSelectedBiPaths,canCreateProjects:canCurrentAccountCreateProjects,showTeamPermissions:function(){const project=getSelectedProject();if(project&&getProjectAccess(project).canManageMembers)showProjectMembersPanel(project);},toggleTechnicalFiles:function(checked){const project=getSelectedProject();if(!project||!getProjectAccess(project).canViewTechnicalFiles)return;state.showTechnicalFilesByProject[String(project.projectId||'')]=!!checked;renderSelectedProject();}}});const changesPanel=cloudChangesApi.create({getService:function(){return state.activeStateService;},discardAll:discardActiveProjectCacheChanges,discardFile:discardActiveProjectCacheFile,discardChange:discardActiveProjectCacheSingleChange});const sessionPanel=window.BimCloudSessionPanel.create({state,ui:cloudUiKit,core,eventBus,callbacks:{attemptLogin,closeRemoteSession:async function(session,loginContext,message){setBusy(true);setMessage(message,'Cerrando sesión seleccionada...','info');try{await callApi(CONFIG.actions.closeSession,{username:loginContext.username,password:loginContext.password,targetSessionId:session.sessionId||session.id||'',reason:'Cierre remoto desde intento de ingreso.'},{token:''});setMessage(message,'Sesión cerrada. Intentando ingresar nuevamente...','success');await sleep(350);await attemptLogin(loginContext.username,loginContext.password,message);}catch(closeError){console.error(closeError);setMessage(message,closeError.message||'No se pudo cerrar la sesión seleccionada.','error');}finally{setBusy(false);}}}});function init(){const loginButton=document.getElementById('cloudLoginButton');if(!loginButton||loginButton.__bimCloudBound)return;loginButton.__bimCloudBound=true;loginButton.addEventListener('click',function(){restoreSession();if(sessionToken)showDashboard(true);else showLoginModal();});restoreSession();if(sessionToken){loginButton.classList.add('cloud-logged');loginButton.textContent='Mi nube';loginButton.title='Abrir panel de proyectos en la nube';startHeartbeat();}window.addEventListener('online',function(){if(sessionToken)sendHeartbeat(true);});document.addEventListener('visibilitychange',function(){if(!document.hidden&&sessionToken)sendHeartbeat(true);});eventBus.emit('cloud:initialized',{authenticated:!!sessionToken});}function clearLegacyPersistedSession(){if(legacySessionCleared)return;legacySessionCleared=true;try{localStore.remove(LEGACY_SESSION_KEY);}catch(_){}try{window.sessionStorage.removeItem(LEGACY_SESSION_KEY);}catch(_){}}function restoreSession(){clearLegacyPersistedSession();if(!sessionToken){activeSessionId='';state.user=null;}eventBus.emit('cloud:session-restored',{authenticated:!!sessionToken,user:state.user,storage:'memory-only'});}function sanitizeSessionUser(user){if(!user||typeof user!=='object')return null;const safeUser=Object.assign({},user);['token','sessionToken','accessToken','refreshToken','password','passwordHash','sessionId','webSessionId'].forEach(function(key){delete safeUser[key];});return safeUser;}function saveSession(token,user,sessionId){clearLegacyPersistedSession();sessionToken=String(token||'');activeSessionId=String(sessionId||'');state.user=sanitizeSessionUser(user);if(sessionToken){startHeartbeat();}else{stopHeartbeat();removeCloudCacheSyncToolbar();}const loginButton=document.getElementById('cloudLoginButton');if(loginButton){loginButton.classList.toggle('cloud-logged',!!sessionToken);loginButton.textContent=sessionToken?'Mi nube':'Login';loginButton.title=sessionToken?'Abrir panel de proyectos en la nube':'Ingresar con cuenta y abrir proyectos guardados en la nube';}eventBus.emit('cloud:session-changed',{authenticated:!!sessionToken,user:state.user,storage:'memory-only'});}function showLoginModal(){return sessionPanel.showLogin();}async function attemptLogin(username,password,messageNode){setBusy(true);setMessage(messageNode,'Validando acceso...','info');try{const response=await callApi(CONFIG.actions.login,{username,password});const token=response.token||response.sessionToken||response.accessToken||(response.data&&response.data.token);const sessionId=response.sessionId||response.webSessionId||(response.data&&(response.data.sessionId||response.data.webSessionId))||'';if(!token)throw new Error('El servidor no devolvió sessionToken.');saveSession(token,response.user||(response.data&&response.data.user)||{username},sessionId);setMessage(messageNode,'Acceso correcto. Cargando proyectos...','success');showDashboard(true);Promise.resolve(requestStoragePersistence()).catch(function(error){console.warn('No se pudo solicitar almacenamiento persistente.',error);});return true;}catch(error){console.error(error);if(isSessionLimitError(error)){showSessionLimitModal(error,{username,password});}else{setMessage(messageNode,error.message||'No se pudo iniciar sesión.','error');}return false;}finally{setBusy(false);}}function showDashboard(refresh){return dashboardPanel.show(refresh);}function bindDashboardResizer(body,splitter){return dashboardPanel.bindResizer(body,splitter);}async function loadProjectList(force){if(!sessionToken)return showLoginModal();if(state.busy&&!force)return;setBusy(true);if(nodes.projectList)nodes.projectList.innerHTML='<div class="cloud-empty">Consultando proyectos...</div>';setDashboardMessage('Consultando proyectos vinculados a la cuenta...','info');try{const response=await callApi(CONFIG.actions.listProjects,{});const projects=Array.isArray(response.projects)?response.projects:(response.data&&Array.isArray(response.data.projects)?response.data.projects:[]);const account=response.account||(response.data&&response.data.account)||null;if(account&&state.user)state.user=Object.assign({},state.user,{account:account});state.projects=projects.map(normalizeProject).filter(Boolean);if(!state.selectedProjectId&&state.projects.length)state.selectedProjectId=state.projects[0].projectId;if(state.selectedProjectId&&!state.projects.some(p=>p.projectId===state.selectedProjectId)){state.selectedProjectId=state.projects.length?state.projects[0].projectId:'';}renderProjects();eventBus.emit('cloud:projects-loaded',{projects:state.projects.slice(),selectedProjectId:state.selectedProjectId});setDashboardMessage(state.projects.length?'Proyectos cargados correctamente.':'No hay proyectos asociados a esta cuenta.',state.projects.length?'success':'info');}catch(error){console.error(error);renderProjects();setDashboardProgress(100,'Error consultando proyectos',false);setDashboardMessage(formatApiActionError(error,'listProjects')||error.message||'No se pudo consultar la lista de proyectos.','error');}finally{setBusy(false);}}function normalizeProject(project){if(!project||typeof project!=='object')return null;const projectId=String(project.projectId||project.id||project.ProjectId||'').trim();if(!projectId)return null;const name=String(project.name||project.projectName||project.Name||projectId).trim()||projectId;const files=normalizeFileList(project.files||project.Files||[]);const folders=normalizeFolderList(project.folders||project.Folders||[]);return Object.assign({},project,{projectId,name,description:String(project.description||project.Description||''),ownerUserId:String(project.ownerUserId||project.OwnerUserId||''),ownerUser:String(project.ownerUser||project.OwnerUser||''),access:normalizeProjectAccess(project.access||project.Access||null),updatedAt:project.updatedAt||project.UpdatedAt||project.modifiedAt||'',sizeBytes:Number(project.sizeBytes||project.SizeBytes||files.reduce((sum,file)=>sum+(Number(file.sizeBytes)||0),0))||0,files,folders});}function normalizeProjectAccess(access){access=access&&typeof access==='object'?access:{};const role=String(access.role||'MASTER').toUpperCase();const legacyFullAccess=!Object.keys(access).length;return{role:role==='EDITOR'||role==='VIEWER'?role:'MASTER',isOwner:legacyFullAccess?true:!!access.isOwner,isShared:legacyFullAccess?false:!!access.isShared,canView:access.canView!==false,canEdit:legacyFullAccess?true:!!access.canEdit,canDelete:legacyFullAccess?true:!!access.canDelete,canManageMembers:legacyFullAccess?true:!!access.canManageMembers,canViewTechnicalFiles:legacyFullAccess?true:!!access.canViewTechnicalFiles};}function getProjectAccess(project){return normalizeProjectAccess(project&&project.access);}function getCurrentAccount(){const account=state.user&&state.user.account&&typeof state.user.account==='object'?state.user.account:{};const type=String(account.accountType||'OWNER').toUpperCase();return{accountType:type,canCreateProjects:account.canCreateProjects==null?type!=='COLLABORATOR':!!account.canCreateProjects,maxOwnedProjects:Number(account.maxOwnedProjects)||0,maxStorageBytes:Number(account.maxStorageBytes)||0};}function canCurrentAccountCreateProjects(){return getCurrentAccount().canCreateProjects;}function isTechnicalProjectPath(path){return CACHE_FOLDER_PATTERN.test(normalizePath(path||''));}function shouldShowTechnicalFiles(project){const access=getProjectAccess(project);return!!(access.canViewTechnicalFiles&&state.showTechnicalFilesByProject[String(project.projectId||'')]);}function getVisibleProjectFiles(project){const files=normalizeFileList(project&&project.files||[]);return shouldShowTechnicalFiles(project)?files:files.filter(function(file){return!isTechnicalProjectPath(file.relativePath||file.name||'');});}function getVisibleProjectFolders(project){const folders=normalizeFolderList(project&&project.folders||[]);return shouldShowTechnicalFiles(project)?folders:folders.filter(function(folder){return!isTechnicalProjectPath(folder.path||folder.name||'');});}function projectRoleLabel(project){const access=getProjectAccess(project);if(access.role==='MASTER')return'MASTER';if(access.role==='EDITOR')return'EDITOR';return'SOLO LECTURA';}function normalizeFileList(files){return Array.isArray(files)?files.map(normalizeCloudFile).filter(Boolean).filter(file=>{const path=normalizePath(file.relativePath||file.name||'');return!isCacheJsonPath(path)||isActiveProjectCacheJsonPath(path);}):[];}function normalizeCloudFile(file){if(!file||typeof file!=='object')return null;const rawPath=file.relativePath||file.path||file.name||file.Name||'';const relativePath=tryNormalizeCloudPath(rawPath,'archivo');if(!relativePath)return null;return Object.assign({},file,{relativePath,name:String(file.name||lastPathPart(relativePath)||'archivo.bi'),sizeBytes:Number(file.sizeBytes||file.size||file.SizeBytes||0)||0,generation:String(file.generation||file.Generation||file.versionGeneration||''),sha256:String(file.sha256||file.hash||file.Hash||file.crc32c||''),biPackageHash:String(file.biPackageHash||''),biPackageId:String(file.biPackageId||''),biCertificateId:String(file.biCertificateId||''),biSignatureStatus:String(file.biSignatureStatus||''),biVerifiedUtc:file.biVerifiedUtc||'',biVerificationExpiresUtc:file.biVerificationExpiresUtc||'',updatedAt:file.updatedAt||file.updatedUtc||file.UpdatedAt||'',downloadUrl:String(file.downloadUrl||file.url||file.signedUrl||file.readUrl||''),uploadUrl:String(file.uploadUrl||file.writeUrl||'')});}function normalizeFolderList(folders){return Array.isArray(folders)?folders.map(normalizeCloudFolder).filter(Boolean):[];}function normalizeCloudFolder(folder){if(typeof folder==='string'){const path=tryNormalizeCloudPath(folder,'carpeta');return path?{path,name:lastPathPart(path),folderId:''}:null;}if(!folder||typeof folder!=='object')return null;const rawPath=folder.path||folder.relativePath||folder.folderPath||folder.name||'';const path=tryNormalizeCloudPath(rawPath,'carpeta');if(!path)return null;return Object.assign({},folder,{path,name:String(folder.name||lastPathPart(path)||path),folderId:String(folder.folderId||folder.id||'')});}function renderProjects(){if(!nodes.projectList)return;if(!state.projects.length){nodes.projectList.innerHTML=canCurrentAccountCreateProjects()?'<div class="cloud-empty">No hay proyectos. Usa “Agregar / subir proyecto” para crear uno.</div>':'<div class="cloud-empty">No tienes proyectos compartidos. Un MASTER debe agregarte desde “Equipo y permisos”.</div>';renderSelectedProject();return;}nodes.projectList.innerHTML='';for(const project of state.projects){const access=getProjectAccess(project);const item=el('button','cloud-project-item'+(project.projectId===state.selectedProjectId?' selected':''));item.type='button';item.innerHTML=['<span class="cloud-project-item-main">','<strong>'+escapeHtml(project.name)+'</strong>','<span>'+escapeHtml(formatProjectSubtitle(project))+'</span>','<small>'+escapeHtml(formatBytes(project.sizeBytes))+'</small>','</span>',access.canDelete?'<button type="button" class="cloud-mini-danger" data-cloud-delete-project="'+encodeURIComponent(project.projectId)+'" title="Eliminar proyecto">−</button>':''].join('');item.addEventListener('click',function(event){if(event.target&&event.target.closest&&event.target.closest('[data-cloud-delete-project]'))return;state.selectedProjectId=project.projectId;eventBus.emit('cloud:project-selected',{projectId:project.projectId,project:project});ensureSelectedFolder(project);renderProjects();});const deleteProjectButton=item.querySelector('[data-cloud-delete-project]');if(deleteProjectButton)deleteProjectButton.addEventListener('click',function(event){event.preventDefault();event.stopPropagation();deleteProjectItem(project,'project','');});nodes.projectList.appendChild(item);}renderSelectedProject();}function renderSelectedProject(){if(!nodes.projectDetail)return;const project=getSelectedProject();if(!project){nodes.projectDetail.innerHTML='<div class="cloud-empty">Selecciona un proyecto.</div>';updateDashboardProjectControls(null);if(nodes.openProjectButton)nodes.openProjectButton.disabled=true;return;}ensureSelectedFolder(project);ensureProjectModelSelection(project);const access=getProjectAccess(project);const files=getVisibleProjectFiles(project);const folders=getVisibleProjectFolders(project);const selectedFolder=getSelectedFolderPath(project);const selectedTargetLabel=formatFolderTarget(project,selectedFolder);updateDashboardProjectControls(project);const toolbar=[];if(access.canEdit){toolbar.push('<button type="button" class="cloud-secondary" data-cloud-create-folder>Crear carpeta</button>');toolbar.push('<button type="button" class="cloud-secondary" data-cloud-add-folder>Subir carpeta aquí</button>');toolbar.push('<button type="button" class="cloud-secondary" data-cloud-update-folder>Actualizar carpeta seleccionada</button>');toolbar.push('<button type="button" class="cloud-secondary" data-cloud-add-bi-files>Agregar archivo aquí</button>');}if(access.canDelete){toolbar.push('<button type="button" class="cloud-secondary cloud-danger-soft" data-cloud-replace-project>Reemplazar proyecto</button>');toolbar.push('<button type="button" class="cloud-secondary cloud-danger-soft" data-cloud-delete-current-project>Eliminar proyecto</button>');}nodes.projectDetail.innerHTML=['<div class="cloud-detail-scroll cloud-detail-scroll-compact">','<div class="cloud-project-access-summary"><strong>'+escapeHtml(projectRoleLabel(project))+'</strong><span>'+escapeHtml(access.isOwner?'Proyecto propio':'Compartido por '+(project.ownerUser||'otro usuario'))+'</span></div>',toolbar.length?'<div class="cloud-project-manage-toolbar cloud-project-manage-toolbar-compact">'+toolbar.join('')+'</div>':'',access.canEdit?'<form class="cloud-create-folder-panel" data-cloud-create-folder-panel hidden><div><span>Nueva carpeta dentro de:</span><strong data-cloud-selected-folder-label>'+escapeHtml(selectedTargetLabel)+'</strong></div><label class="cloud-inline-field"><span>Nombre de carpeta</span><input type="text" data-cloud-folder-name-input maxlength="120" placeholder="Ejemplo: Comunicaciones" autocomplete="off" /></label><div class="cloud-inline-actions"><button type="submit" class="cloud-primary">Crear</button><button type="button" class="cloud-secondary" data-cloud-cancel-create-folder>Cancelar</button></div></form>':'','<div class="cloud-project-tree-toolbar cloud-project-tree-toolbar-compact"><span>Árbol del proyecto</span></div>','</div>','<div class="cloud-project-tree-wrap"><div class="cloud-project-tree">'+(files.length||folders.length?renderProjectFileTree(project,files,folders):renderEmptyProjectRoot(project))+'</div></div>'].join('');bindProjectTreeActions(project);bindTreeDragAndDrop(project);if(nodes.openProjectButton)nodes.openProjectButton.disabled=getSelectedBiPaths(project).length===0;}function updateDashboardProjectControls(project){const access=project?getProjectAccess(project):null;const canManageMembers=!!(project&&access&&access.canManageMembers);const canViewTechnicalFiles=!!(project&&access&&access.canViewTechnicalFiles);if(nodes.teamPermissionsButton){nodes.teamPermissionsButton.hidden=!canManageMembers;nodes.teamPermissionsButton.disabled=state.busy||!canManageMembers;}if(nodes.technicalFilesControl)nodes.technicalFilesControl.hidden=!canViewTechnicalFiles;if(nodes.technicalFilesCheckbox){nodes.technicalFilesCheckbox.checked=canViewTechnicalFiles&&shouldShowTechnicalFiles(project);nodes.technicalFilesCheckbox.disabled=state.busy||!canViewTechnicalFiles;}}function renderProjectFileTree(project,files,folders){return projectTree.renderProjectFileTree(project,files,folders);}function renderEmptyProjectRoot(project){return projectTree.renderEmptyProjectRoot(project);}function buildPathTree(files,folders){return projectTree.buildPathTree(files,folders);}function renderProjectRootNode(project,root,emptyHtml){return projectTree.renderProjectRootNode(project,root,emptyHtml);}function renderTreeChildren(project,childrenMap,depth){return projectTree.renderTreeChildren(project,childrenMap,depth);}function renderFolderNode(project,folder,depth){return projectTree.renderFolderNode(project,folder,depth);}function renderFileNode(project,file,depth){return projectTree.renderFileNode(project,file,depth);}function collectBiPathsFromTreeNode(node){return projectTree.collectBiPathsFromTreeNode(node);}function renderTreeSelectionCheckbox(project,candidatePaths,itemType,itemPath,disabled){candidatePaths=Array.isArray(candidatePaths)?candidatePaths:[];disabled=!!disabled||candidatePaths.length===0;const selection=getProjectModelSelectionState(project,candidatePaths);const encodedPath=encodeURIComponent(normalizePath(itemPath||''));return'<input type="checkbox" class="cloud-tree-select" data-cloud-select-type="'+escapeHtml(itemType||'')+'" data-cloud-select-path="'+encodedPath+'"'+(selection.checked?' checked':'')+(selection.indeterminate?' data-cloud-indeterminate="1"':'')+(disabled?' disabled title="Archivo de soporte; se obtiene junto con el proyecto"':' title="Incluir en la carga inicial"')+' />';}function ensureProjectModelSelection(project){if(!project||!project.projectId)return new Set();const projectId=String(project.projectId);const currentPaths=getProjectBiPaths(project);const currentSet=new Set(currentPaths);let selected=state.selectedModelPaths[projectId];let known=state.knownModelPaths[projectId];if(!(selected instanceof Set))selected=new Set();if(!(known instanceof Set))known=new Set();currentPaths.forEach(function(path){if(!known.has(path))selected.add(path);});Array.from(selected).forEach(function(path){if(!currentSet.has(path))selected.delete(path);});state.selectedModelPaths[projectId]=selected;state.knownModelPaths[projectId]=currentSet;return selected;}function getProjectBiPaths(project){return normalizeFileList(project&&project.files||[]).map(function(file){return normalizePath(file.relativePath||file.name||'');}).filter(isBiFilePath).sort(function(a,b){return a.localeCompare(b,undefined,{sensitivity:'base',numeric:true});});}function getSelectedBiPaths(project){const selected=ensureProjectModelSelection(project);return getProjectBiPaths(project).filter(function(path){return selected.has(path);});}function getProjectModelSelectionState(project,candidatePaths){const paths=Array.from(new Set((candidatePaths||[]).map(normalizePath).filter(Boolean)));const selected=ensureProjectModelSelection(project);if(!paths.length)return{checked:true,indeterminate:false,selectedCount:0,total:0};const selectedCount=paths.reduce(function(count,path){return count+(selected.has(path)?1:0);},0);return{checked:selectedCount===paths.length,indeterminate:selectedCount>0&&selectedCount<paths.length,selectedCount:selectedCount,total:paths.length};}function setProjectModelSelection(project,itemType,itemPath,checked){if(!project||!project.projectId)return;const selected=ensureProjectModelSelection(project);const normalizedPath=normalizePath(itemPath||'');let affected=[];if(itemType==='file'){if(isBiFilePath(normalizedPath))affected=[normalizedPath];}else{affected=getProjectBiPaths(project).filter(function(path){if(!normalizedPath)return true;const lower=path.toLowerCase();const prefix=normalizedPath.toLowerCase()+'/';return lower.indexOf(prefix)===0;});}affected.forEach(function(path){if(checked)selected.add(path);else selected.delete(path);});state.selectedModelPaths[String(project.projectId)]=selected;}function cloudFolderStateKey(folderPath){const normalized=normalizePath(folderPath||'').toLowerCase();return normalized||'__project_root__';}function getCollapsedFolderSet(project){if(!project||!project.projectId)return new Set();const projectId=String(project.projectId);let set=state.collapsedFolderPaths[projectId];if(!(set instanceof Set)){set=new Set();state.collapsedFolderPaths[projectId]=set;}return set;}function isCloudFolderOpen(project,folderPath){return!getCollapsedFolderSet(project).has(cloudFolderStateKey(folderPath));}function rememberCloudFolderOpenState(project,folderPath,isOpen){const set=getCollapsedFolderSet(project);const key=cloudFolderStateKey(folderPath);if(isOpen)set.delete(key);else set.add(key);}function updateSelectedFolderVisuals(project){if(!nodes.projectDetail||!project)return;const selectedPath=normalizePath(getSelectedFolderPath(project)).toLowerCase();nodes.projectDetail.querySelectorAll('[data-cloud-folder-container]').forEach(function(details){const path=normalizePath(details.getAttribute('data-cloud-folder-container')||'').toLowerCase();details.classList.toggle('selected-folder',path===selectedPath);});const label=nodes.projectDetail.querySelector('[data-cloud-selected-folder-label]');if(label)label.textContent=formatFolderTarget(project,getSelectedFolderPath(project));}function bindProjectTreeActions(project){if(!nodes.projectDetail)return;nodes.projectDetail.querySelectorAll('details[data-cloud-folder-container]').forEach(function(details){details.addEventListener('toggle',function(){rememberCloudFolderOpenState(project,details.getAttribute('data-cloud-folder-container')||'',details.open);});});nodes.projectDetail.querySelectorAll('.cloud-tree-select[data-cloud-indeterminate="1"]').forEach(function(checkbox){checkbox.indeterminate=true;});nodes.projectDetail.querySelectorAll('.cloud-tree-select[data-cloud-select-type]').forEach(function(checkbox){checkbox.addEventListener('click',function(event){event.stopPropagation();});checkbox.addEventListener('change',function(event){event.stopPropagation();if(checkbox.disabled)return;const type=checkbox.getAttribute('data-cloud-select-type')||'';const path=decodeURIComponent(checkbox.getAttribute('data-cloud-select-path')||'');setProjectModelSelection(project,type,path,!!checkbox.checked);renderSelectedProject();});});nodes.projectDetail.querySelectorAll('[data-cloud-folder-path]').forEach(function(node){node.addEventListener('click',function(event){if(event.target&&event.target.closest&&(event.target.closest('button')||event.target.closest('input')))return;const path=decodeURIComponent(node.getAttribute('data-cloud-folder-path')||'');setSelectedFolderPath(project,path,{render:false});event.stopPropagation();});});const createFolderPanel=nodes.projectDetail.querySelector('[data-cloud-create-folder-panel]');const createFolderInput=nodes.projectDetail.querySelector('[data-cloud-folder-name-input]');const createFolderButton=nodes.projectDetail.querySelector('[data-cloud-create-folder]');if(createFolderButton&&createFolderPanel&&createFolderInput){createFolderButton.addEventListener('click',function(){createFolderPanel.hidden=!createFolderPanel.hidden?true:false;if(!createFolderPanel.hidden){createFolderInput.value='';setTimeout(function(){try{createFolderInput.focus();}catch(_){}},0);}});createFolderPanel.addEventListener('submit',function(event){event.preventDefault();createFolderInProject(project,createFolderInput.value);});}const cancelCreateFolderButton=nodes.projectDetail.querySelector('[data-cloud-cancel-create-folder]');if(cancelCreateFolderButton&&createFolderPanel){cancelCreateFolderButton.addEventListener('click',function(){createFolderPanel.hidden=true;});}const addFolderButton=nodes.projectDetail.querySelector('[data-cloud-add-folder]');if(addFolderButton){addFolderButton.addEventListener('click',function(){const target=getSelectedFolderPath(project);pickFolderForExistingProject(project,{mode:'replaceFiles',targetFolder:target,keepSelectedRoot:true,stripSelectedRoot:false,confirmText:'Se cargará la carpeta seleccionada dentro de: '+formatFolderTarget(project,target)+'. Se mantendrá la estructura interna de esa carpeta y se subirán los archivos .bi y la carpeta ExportModel_Web_Cache con sus JSON, incidencias e imágenes optimizadas. Si alguno ya existe con la misma ruta, será reemplazado.\n\n¿Continuar?'});});}const updateFolderButton=nodes.projectDetail.querySelector('[data-cloud-update-folder]');if(updateFolderButton){updateFolderButton.addEventListener('click',async function(){const target=getSelectedFolderPath(project);if(!target){const okRoot=await bimConfirm('Tienes seleccionado el proyecto raíz: '+formatFolderTarget(project,'')+'.\n\nSi continúas, el contenido de la carpeta local seleccionada se mapeará desde el nivel principal del proyecto.\n\n¿Continuar desde el proyecto raíz?');if(!okRoot)return;}pickFolderForExistingProject(project,{mode:'replaceFiles',targetFolder:target,keepSelectedRoot:false,stripSelectedRoot:true,confirmText:'Selecciona la carpeta local que contiene los archivos actualizados. El contenido se mapeará dentro de: '+formatFolderTarget(project,target)+'.\n\nEsto evita volver a subir todo el proyecto.\n\n¿Continuar?'});});}const addBiFilesButton=nodes.projectDetail.querySelector('[data-cloud-add-bi-files]');if(addBiFilesButton){addBiFilesButton.addEventListener('click',function(){pickIndividualBiFilesForExistingProject(project,getSelectedFolderPath(project));});}const replaceProjectButton=nodes.projectDetail.querySelector('[data-cloud-replace-project]');if(replaceProjectButton){replaceProjectButton.addEventListener('click',function(){pickFolderForExistingProject(project,{mode:'fullProject',targetFolder:'',keepSelectedRoot:false,stripSelectedRoot:false,confirmText:'Esta opción reemplazará el conjunto completo de archivos .bi registrados para este proyecto. Úsala solo si estás seleccionando nuevamente la carpeta principal exportada desde Revit.\n\n¿Continuar?'});});}const deleteCurrentProjectButton=nodes.projectDetail.querySelector('[data-cloud-delete-current-project]');if(deleteCurrentProjectButton){deleteCurrentProjectButton.addEventListener('click',function(){deleteProjectItem(project,'project','');});}const deleteRootButton=nodes.projectDetail.querySelector('[data-cloud-delete-project-root]');if(deleteRootButton){deleteRootButton.addEventListener('click',function(event){event.preventDefault();event.stopPropagation();deleteProjectItem(project,'project','');});}nodes.projectDetail.querySelectorAll('[data-cloud-delete-folder]').forEach(function(button){button.addEventListener('click',function(event){event.preventDefault();event.stopPropagation();const folderPath=decodeURIComponent(button.getAttribute('data-cloud-delete-folder')||'');deleteProjectItem(project,'folder',folderPath);});});nodes.projectDetail.querySelectorAll('[data-cloud-delete-file]').forEach(function(button){button.addEventListener('click',function(event){event.preventDefault();event.stopPropagation();const relativePath=decodeURIComponent(button.getAttribute('data-cloud-delete-file')||'');deleteProjectItem(project,'file',relativePath);});});nodes.projectDetail.querySelectorAll('[data-cloud-update-file]').forEach(function(button){button.addEventListener('click',function(event){event.preventDefault();event.stopPropagation();const relativePath=decodeURIComponent(button.getAttribute('data-cloud-update-file')||'');const fileInfo=normalizeFileList(project.files||[]).find(function(file){return normalizePath(file.relativePath||file.name)===normalizePath(relativePath);});if(!fileInfo){setDashboardMessage('No se encontró el archivo seleccionado dentro del proyecto.','error');return;}updateSingleProjectFile(project,fileInfo);});});}function bindTreeDragAndDrop(project){if(!nodes.projectDetail||!getProjectAccess(project).canEdit)return;let currentDrag=null;nodes.projectDetail.querySelectorAll('[data-cloud-drag-type]').forEach(function(node){node.addEventListener('dragstart',function(event){const type=node.getAttribute('data-cloud-drag-type')||'';const path=decodeURIComponent(node.getAttribute('data-cloud-drag-path')||'');if(!type||!path)return;currentDrag={type,path};try{event.dataTransfer.effectAllowed='move';event.dataTransfer.setData('application/json',JSON.stringify(currentDrag));event.dataTransfer.setData('text/plain',path);}catch(_){}node.classList.add('cloud-dragging');});node.addEventListener('dragend',function(){currentDrag=null;node.classList.remove('cloud-dragging');nodes.projectDetail.querySelectorAll('.cloud-drop-target').forEach(n=>n.classList.remove('cloud-drop-target'));});});nodes.projectDetail.querySelectorAll('[data-cloud-drop-folder]').forEach(function(target){target.addEventListener('dragover',function(event){const drag=currentDrag||readDragData(event);if(!drag||!canDropOnFolder(drag,decodeURIComponent(target.getAttribute('data-cloud-folder-path')||'')))return;event.preventDefault();target.classList.add('cloud-drop-target');try{event.dataTransfer.dropEffect='move';}catch(_){}});target.addEventListener('dragleave',function(){target.classList.remove('cloud-drop-target');});target.addEventListener('drop',async function(event){event.preventDefault();target.classList.remove('cloud-drop-target');const drag=currentDrag||readDragData(event);const targetFolder=decodeURIComponent(target.getAttribute('data-cloud-folder-path')||'');if(!drag||!canDropOnFolder(drag,targetFolder))return;await moveProjectItem(project,drag.type,drag.path,targetFolder);});});}function readDragData(event){try{const raw=event.dataTransfer&&event.dataTransfer.getData('application/json');return raw?JSON.parse(raw):null;}catch(_){return null;}}function canDropOnFolder(drag,targetFolder){if(!drag||!drag.path)return false;const source=normalizePath(drag.path);const target=normalizePath(targetFolder);if(drag.type==='file'){return normalizePath(parentPath(source)).toLowerCase()!==target.toLowerCase();}if(drag.type==='folder'){if(!target)return!!source;const s=source.toLowerCase();const t=target.toLowerCase();return s!==t&&!t.startsWith(s+'/');}return false;}async function deleteProjectItem(project,itemType,itemPath){if(!project||!project.projectId||state.busy)return;itemType=String(itemType||'').toLowerCase();itemPath=normalizePath(itemPath||'');const targetName=itemType==='project'?project.name:(lastPathPart(itemPath)||itemPath||'elemento');const title=itemType==='project'?'Eliminar proyecto':(itemType==='folder'?'Eliminar carpeta':'Eliminar archivo');const message=itemType==='project'?'Se eliminará el proyecto completo y toda su información asociada en la nube para liberar almacenamiento.':(itemType==='folder'?'Se eliminará la carpeta seleccionada, sus subcarpetas y los archivos que contiene en la nube.':'Se eliminará este archivo del proyecto y de la nube.');const confirmed=await confirmCloudAction({title:title,target:targetName,message:message,confirmText:'Sí, eliminar',danger:true});if(!confirmed)return;setBusy(true);setDashboardMessage('Eliminando '+(itemType==='project'?'proyecto':itemType==='folder'?'carpeta':'archivo')+'...','info');setDashboardProgress(12,'Eliminando...',true);try{const response=await callApi(CONFIG.actions.deleteProjectItems,{projectId:project.projectId,itemType:itemType,itemPath:itemPath});setDashboardProgress(72,'Actualizando lista...',true);if(itemType==='project'){state.selectedProjectId='';}else if(itemType==='folder'){const selected=getSelectedFolderPath(project).toLowerCase();const deleted=itemPath.toLowerCase();if(selected===deleted||selected.indexOf(deleted+'/')===0){state.selectedFolderPaths[project.projectId]=parentPath(itemPath);}}await loadProjectList(true);setDashboardProgress(100,'Eliminación lista',false);setDashboardMessage((response&&response.message)||'Elemento eliminado correctamente.','success');clearDashboardProgressSoon();}catch(error){console.error(error);setDashboardProgress(100,'Error eliminando',false);setDashboardMessage(error.message||'No se pudo eliminar el elemento.','error');}finally{setBusy(false);}}async function createFolderInProject(project,rawName){const base=getSelectedFolderPath(project);const safe=normalizeFolderName(rawName);if(!safe){setDashboardMessage('Nombre de carpeta inválido. Escribe un nombre antes de crearla.','warning');return;}const folderPath=normalizePath(base?base+'/'+safe:safe);setBusy(true);setDashboardMessage('Creando carpeta lógica dentro de '+formatFolderTarget(project,base)+': '+safe,'info');setDashboardProgress(15,'Creando carpeta...',true);try{await callApi(CONFIG.actions.createProjectFolder,{projectId:project.projectId,folderPath:folderPath});setDashboardProgress(70,'Carpeta creada. Actualizando árbol...',true);setDashboardMessage('Carpeta creada correctamente.','success');state.selectedFolderPaths[project.projectId]=folderPath;await loadProjectList(true);setDashboardProgress(100,'Carpeta lista',false);clearDashboardProgressSoon();}catch(error){console.error(error);setDashboardProgress(100,'Error creando carpeta',false);setDashboardMessage(error.message||'No se pudo crear la carpeta.','error');}finally{setBusy(false);}}async function moveProjectItem(project,type,sourcePath,targetFolder){sourcePath=normalizePath(sourcePath);targetFolder=normalizePath(targetFolder);const label=type==='folder'?'carpeta':'archivo';const ok=await bimConfirm('Mover '+label+':\n'+sourcePath+'\n\nHacia:\n'+formatFolderTarget(project,targetFolder)+'\n\nEsto solo reorganiza la estructura lógica del proyecto y no vuelve a subir archivos. ¿Continuar?');if(!ok)return;setBusy(true);setDashboardMessage('Moviendo '+label+' dentro del proyecto...','info');setDashboardProgress(15,'Moviendo '+label+'...',true);try{const response=await callApi(CONFIG.actions.moveProjectItems,{projectId:project.projectId,itemType:type,sourcePath:sourcePath,targetFolder:targetFolder});const movedFolder=response&&response.newPath?normalizePath(response.newPath):targetFolder;state.selectedFolderPaths[project.projectId]=type==='folder'?movedFolder:targetFolder;setDashboardProgress(70,'Movimiento registrado. Actualizando árbol...',true);setDashboardMessage('Estructura actualizada correctamente.','success');await loadProjectList(true);setDashboardProgress(100,'Estructura lista',false);clearDashboardProgressSoon();}catch(error){console.error(error);setDashboardProgress(100,'Error moviendo elemento',false);setDashboardMessage(error.message||'No se pudo mover el elemento.','error');}finally{setBusy(false);}}function ensureSelectedFolder(project){if(!project||!project.projectId)return'';const current=normalizePath(state.selectedFolderPaths[project.projectId]||'');const known=collectKnownFolderPaths(project);if(!current||known[current.toLowerCase()])return current;state.selectedFolderPaths[project.projectId]='';return'';}function getSelectedFolderPath(project){if(!project||!project.projectId)return'';return normalizePath(state.selectedFolderPaths[project.projectId]||'');}function setSelectedFolderPath(project,folderPath,options){if(!project||!project.projectId)return;state.selectedFolderPaths[project.projectId]=normalizePath(folderPath||'');if(options&&options.render===false)updateSelectedFolderVisuals(project);else renderSelectedProject();}function collectKnownFolderPaths(project){const map={'':true};normalizeFolderList(project.folders||[]).forEach(function(folder){const p=normalizePath(folder.path);if(p)map[p.toLowerCase()]=true;});normalizeFileList(project.files||[]).forEach(function(file){const parts=normalizePath(file.relativePath||file.name).split('/').filter(Boolean);for(let i=1;i<parts.length;i++){const folder=parts.slice(0,i).join('/');map[folder.toLowerCase()]=true;}});return map;}function getSelectedProject(){return state.projects.find(project=>project.projectId===state.selectedProjectId)||null;}async function openSelectedProject(){const project=getSelectedProject();if(!project||state.busy)return;if(!window.ExportModelApp||typeof window.ExportModelApp.loadCloudFiles!=='function'){setDashboardMessage('El visor todavía no está listo para recibir proyectos de la nube.','error');return;}setBusy(true);setDashboardMessage('Solicitando permiso de lectura...','info');setDashboardProgress(8,'Solicitando permiso de lectura...',true);try{await requestStoragePersistence();const response=await callApi(CONFIG.actions.openProject,{projectId:project.projectId});const serverProject=normalizeProject(response.project||(response.data&&response.data.project)||project);const files=normalizeFileList(response.files||(response.data&&response.data.files)||(serverProject&&serverProject.files)||project.files||[]);if(serverProject)serverProject.files=files;const allBiFiles=files.filter(file=>isBiFilePath(file.relativePath||file.name));if(!allBiFiles.length)throw new Error('El proyecto no tiene archivos .bi para cargar.');const selectedPathSet=new Set(getSelectedBiPaths(serverProject||project).map(function(path){return normalizePath(path).toLowerCase();}));const biFiles=allBiFiles.filter(function(file){return selectedPathSet.has(normalizePath(file.relativePath||file.name).toLowerCase());});if(!biFiles.length)throw new Error('Selecciona al menos un archivo .bi antes de abrir el proyecto.');const deferredProjectDescriptors=buildCloudDeferredProjectDescriptors(serverProject||project,allBiFiles.filter(function(file){const path=normalizePath(file.relativePath||file.name);return!selectedPathSet.has(path.toLowerCase())&&!/_2d\.bi$/i.test(path);}));const cache=new CloudFileCache();const loadedFiles=[];const totalBytes=biFiles.reduce((sum,file)=>sum+(Number(file.sizeBytes)||0),0);let loadedBytes=0;let cachedCount=0;let downloadedCount=0;setDashboardMessage('Preparando caché de modelos .bi...','info');setDashboardProgress(18,'Preparando caché de modelos .bi...',true);await runWithLimit(biFiles,CONFIG.maxParallelDownloads,async function(file,index){const result=await cache.getOrDownload(serverProject||project,file,function(progress){const currentLoaded=loadedBytes+(progress.loaded||0);const percent=totalBytes?Math.max(18,Math.min(92,Math.round((currentLoaded/totalBytes)*74)+18)):Math.round(((index+1)/biFiles.length)*74)+18;const label='Preparando '+(index+1)+'/'+biFiles.length+': '+file.name+' · '+formatBytes(currentLoaded)+' / '+formatBytes(totalBytes);setDashboardProgress(percent,label,false);setDashboardMessage(label,'info');if(window.ExportModelApp&&window.ExportModelApp.setStatus)window.ExportModelApp.setStatus(label);});loadedBytes+=Number(file.sizeBytes||(result.blob&&result.blob.size)||0)||0;if(result.fromCache)cachedCount++;else downloadedCount++;loadedFiles.push(createFileFromCloudBlob(result.blob,file,serverProject||project));});loadedFiles.sort((a,b)=>String(a.webkitRelativePath||a.name).localeCompare(String(b.webkitRelativePath||b.name),undefined,{sensitivity:'base'}));const stateCache=new CloudProjectStateCacheService(serverProject||project);state.activeStateService=stateCache;state.activeCloudProject=serverProject||project;setDashboardProgress(100,'Proyecto listo. Abriendo visor...',false);setDashboardMessage('Abriendo proyecto en el visor... Caché: '+cachedCount+' · descargados: '+downloadedCount,'success');if(window.ExportModelApp&&window.ExportModelApp.setStatus)window.ExportModelApp.setStatus('Abriendo proyecto cloud: '+project.name);closeOverlay();installCloudCacheSyncToolbar(stateCache);await window.ExportModelApp.loadCloudFiles(loadedFiles,stateCache,{autoSelectAllProjects:false,preselectDiscoveredProjects:true,deferredProjectDescriptors:deferredProjectDescriptors,replaceCurrentModel:true});eventBus.emit('cloud:project-opened',{project:state.activeCloudProject,loadedFiles:loadedFiles.length,cachedFiles:cachedCount,downloadedFiles:downloadedCount});}catch(error){console.error(error);setDashboardMessage(error.message||'No se pudo abrir el proyecto.','error');}finally{setBusy(false);}}function createFileFromCloudBlob(blob,fileInfo,project){const fileName=fileInfo.name||lastPathPart(fileInfo.relativePath)||'archivo.bi';let file;try{file=new File([blob],fileName,{type:'application/octet-stream',lastModified:Date.now()});}catch(_){file=blob;file.name=fileName;file.lastModified=Date.now();}const rootName=sanitizeRootName(project&&(project.rootName||project.name||project.projectName||project.projectId)||'ProyectoCloud');const relative=rootName+'/'+normalizePath(fileInfo.relativePath||fileName);defineReadOnlyProperty(file,'webkitRelativePath',relative);file.__exportModelRelativePath=relative;file.__bimCloudFileInfo=fileInfo;file.__bimCloudProjectId=project&&project.projectId;file.__bimCloudVirtualRootPath=getCloudModelVirtualRootPath(project,fileInfo);return file;}function buildCloudDeferredProjectDescriptors(project,files){return(files||[]).map(function(file){const folderPath=getCloudModelVirtualRootPath(project,file);const modelName=stripBiExtension(lastPathPart(file.relativePath||file.name||'Modelo.bi'));return{ProjectId:makeCloudDeferredProjectId(folderPath),Name:modelName||'Modelo',FolderPath:folderPath,SourceDocument:file.relativePath||file.name||'',ExportDate:file.updatedAt||'',ElementCount:0,GeometryCount:0,Signature:file.generation||file.sha256||String(file.sizeBytes||''),__folderPath:folderPath,__signature:file.generation||file.sha256||String(file.sizeBytes||''),__fileMap:null,__cloudDeferredOnly:true,__cloudRelativePath:normalizePath(file.relativePath||file.name||''),__loadDeferredDescriptor:function(onStatus,options){return loadDeferredCloudProjectDescriptor(project.projectId,file.relativePath||file.name,onStatus,options);}};});}async function loadDeferredCloudProjectDescriptor(projectId,relativePath,onStatus,options){const isCancelled=options&&typeof options.isCancelled==='function'?options.isCancelled:null;const throwIfCancelled=function(){if(isCancelled&&isCancelled()){const error=new Error('Carga de modelo cancelada.');error.name='AbortError';throw error;}};throwIfCancelled();if(onStatus)onStatus({current:0,total:100,message:'Consultando modelo en la nube...'});const response=await callApi(CONFIG.actions.openProject,{projectId:projectId});const project=normalizeProject(response.project||(response.data&&response.data.project)||state.activeCloudProject||{projectId:projectId});const files=normalizeFileList(response.files||(response.data&&response.data.files)||(project&&project.files)||[]);if(project){project.files=files;state.activeCloudProject=project;}const targetPath=normalizePath(relativePath).toLowerCase();const fileInfo=files.find(function(file){return normalizePath(file.relativePath||file.name).toLowerCase()===targetPath;});if(!fileInfo)throw new Error('El modelo ya no existe en el proyecto cloud: '+relativePath);throwIfCancelled();const cache=new CloudFileCache();const result=await cache.getOrDownload(project,fileInfo,function(progress){const total=Math.max(1,Number(progress&&progress.total)||Number(fileInfo.sizeBytes)||1);const loaded=Math.max(0,Number(progress&&progress.loaded)||0);const percent=Math.min(70,Math.round((loaded/total)*70));if(onStatus)onStatus({current:percent,total:100,message:'Descargando modelo: '+(fileInfo.name||lastPathPart(fileInfo.relativePath))+'...'});});throwIfCancelled();const packageFile=createFileFromCloudBlob(result.blob,fileInfo,project);if(!window.BimBiPackageReader||!window.ExportModelLoader)throw new Error('El lector de modelos .bi no está disponible.');const expanded=await window.BimBiPackageReader.expandBiPackage(packageFile,function(status){const current=Number(status&&status.current)||0;const total=Math.max(1,Number(status&&status.total)||3);if(onStatus)onStatus({current:70+Math.round((current/total)*20),total:100,message:status&&status.message||'Abriendo modelo .bi...'});},{isCancelled:isCancelled,virtualRootPath:packageFile.__bimCloudVirtualRootPath});throwIfCancelled();const discovery=await window.ExportModelLoader.discoverExportModelProjects(expanded.files||[],function(status){if(onStatus)onStatus({current:95,total:100,message:status&&status.message||'Preparando modelo...'});},{isCancelled:isCancelled});const descriptor=discovery&&discovery.projects&&discovery.projects[0];if(!descriptor)throw new Error('No se pudo preparar el modelo descargado: '+relativePath);if(onStatus)onStatus({current:100,total:100,message:'Modelo descargado y preparado.'});return descriptor;}function getCloudModelVirtualRootPath(project,fileInfo){const rootName=sanitizeRootName(project&&(project.rootName||project.name||project.projectName||project.projectId)||'ProyectoCloud');const relative=normalizePath(fileInfo&&(fileInfo.relativePath||fileInfo.name)||'Modelo.bi').replace(/\.bi$/i,'');return normalizePath(rootName+'/'+relative);}function makeCloudDeferredProjectId(folderPath){const normalized=normalizePath(folderPath||'root').replace(/[^a-zA-Z0-9_\-]+/g,'_')||'root';return normalized+'_cloud_'+hashCloudString(folderPath).slice(0,6);}function hashCloudString(value){let hash=2166136261;const text=String(value||'');for(let i=0;i<text.length;i++){hash^=text.charCodeAt(i);hash=Math.imul(hash,16777619);}return(hash>>>0).toString(16);}async function showProjectMembersPanel(project){if(!project||!getProjectAccess(project).canManageMembers)return;const overlay=createOverlay('cloud-team-overlay',function(){try{overlay.remove();}catch(_){}});const card=el('div','cloud-card cloud-team-card');const close=function(){try{overlay.remove();}catch(_){}};card.appendChild(createHeader('Equipo y permisos',project.name,close));const body=el('div','cloud-team-body');body.innerHTML=['<form class="cloud-team-form" data-cloud-member-form>','<input type="hidden" data-cloud-member-user-id />','<label class="cloud-field"><span>Usuario existente</span><input type="text" data-cloud-member-user autocomplete="off" placeholder="Nombre exacto del usuario" required /></label>','<label class="cloud-field"><span>Rol</span><select data-cloud-member-role><option value="EDITOR">EDITOR</option><option value="VIEWER">SOLO LECTURA</option></select></label>','<div class="cloud-team-permissions">','<label><input type="checkbox" data-cloud-member-edit checked /> Puede editar y subir archivos</label>','<label><input type="checkbox" data-cloud-member-delete /> Puede eliminar archivos o proyectos</label>','<label><input type="checkbox" data-cloud-member-manage /> Puede administrar miembros</label>','<label><input type="checkbox" data-cloud-member-technical /> Puede ver archivos técnicos</label>','</div>','<div class="cloud-actions"><button type="submit" class="cloud-primary">Guardar acceso</button><button type="button" class="cloud-secondary" data-cloud-member-clear>Limpiar</button></div>','<div class="cloud-message" data-cloud-team-message></div>','</form>','<div class="cloud-team-list-wrap"><div class="cloud-panel-title">Usuarios con acceso</div><div class="cloud-team-list" data-cloud-team-list><div class="cloud-empty">Cargando equipo...</div></div></div>'].join('');card.appendChild(body);overlay.appendChild(card);document.body.appendChild(overlay);const form=body.querySelector('[data-cloud-member-form]');const userInput=body.querySelector('[data-cloud-member-user]');const userIdInput=body.querySelector('[data-cloud-member-user-id]');const roleInput=body.querySelector('[data-cloud-member-role]');const editInput=body.querySelector('[data-cloud-member-edit]');const deleteInput=body.querySelector('[data-cloud-member-delete]');const manageInput=body.querySelector('[data-cloud-member-manage]');const technicalInput=body.querySelector('[data-cloud-member-technical]');const list=body.querySelector('[data-cloud-team-list]');const message=body.querySelector('[data-cloud-team-message]');function clearForm(){userInput.value='';userIdInput.value='';userInput.disabled=false;roleInput.value='EDITOR';editInput.checked=true;deleteInput.checked=false;manageInput.checked=false;technicalInput.checked=false;}roleInput.addEventListener('change',function(){const editor=roleInput.value==='EDITOR';editInput.checked=editor;if(!editor){deleteInput.checked=false;manageInput.checked=false;technicalInput.checked=false;}});body.querySelector('[data-cloud-member-clear]').addEventListener('click',clearForm);async function loadMembers(){list.innerHTML='<div class="cloud-empty">Cargando equipo...</div>';try{const response=await callApi(CONFIG.actions.listProjectMembers,{projectId:project.projectId});const members=Array.isArray(response.members)?response.members:[];if(!members.length){list.innerHTML='<div class="cloud-empty">Todavía no hay colaboradores.</div>';return;}list.innerHTML=members.map(function(member){const permissions=[member.canEdit?'Editar':'',member.canDelete?'Eliminar':'',member.canManageMembers?'Administrar':'',member.canViewTechnicalFiles?'Técnicos':''].filter(Boolean).join(' · ')||'Solo lectura';return'<div class="cloud-team-member" data-cloud-team-member="'+encodeURIComponent(member.userId||'')+'">'+'<div><strong>'+escapeHtml(member.user||member.userId||'Usuario')+'</strong><span>'+escapeHtml(member.role||'VIEWER')+' · '+escapeHtml(permissions)+'</span></div>'+(member.isOwner?'<small>Propietario</small>':'<span class="cloud-team-member-actions"><button type="button" class="cloud-secondary" data-cloud-member-edit-action>Editar</button><button type="button" class="cloud-secondary cloud-danger-soft" data-cloud-member-remove>Quitar</button></span>')+'</div>';}).join('');members.forEach(function(member){const row=list.querySelector('[data-cloud-team-member="'+encodeURIComponent(member.userId||'')+'"]');if(!row||member.isOwner)return;row.querySelector('[data-cloud-member-edit-action]').addEventListener('click',function(){userInput.value=member.user||'';userIdInput.value=member.userId||'';userInput.disabled=true;roleInput.value=member.role==='EDITOR'?'EDITOR':'VIEWER';editInput.checked=!!member.canEdit;deleteInput.checked=!!member.canDelete;manageInput.checked=!!member.canManageMembers;technicalInput.checked=!!member.canViewTechnicalFiles;});row.querySelector('[data-cloud-member-remove]').addEventListener('click',async function(){const ok=await bimConfirm('¿Quitar el acceso de '+(member.user||'este usuario')+' al proyecto?');if(!ok)return;try{await callApi(CONFIG.actions.removeProjectMember,{projectId:project.projectId,userId:member.userId});setMessage(message,'Acceso retirado correctamente.','success');clearForm();await loadMembers();await loadProjectList(true);}catch(error){setMessage(message,error.message||'No se pudo retirar el acceso.','error');}});});}catch(error){list.innerHTML='<div class="cloud-empty">'+escapeHtml(error.message||'No se pudo cargar el equipo.')+'</div>';}}form.addEventListener('submit',async function(event){event.preventDefault();const username=String(userInput.value||'').trim();if(!username)return;try{setMessage(message,'Guardando permisos...','info');await callApi(CONFIG.actions.saveProjectMember,{projectId:project.projectId,user:username,role:roleInput.value,canEdit:!!editInput.checked,canDelete:!!deleteInput.checked,canManageMembers:!!manageInput.checked,canViewTechnicalFiles:!!technicalInput.checked});setMessage(message,'Permisos guardados correctamente.','success');clearForm();await loadMembers();await loadProjectList(true);}catch(error){setMessage(message,error.message||'No se pudieron guardar los permisos.','error');}});clearForm();await loadMembers();setTimeout(function(){try{userInput.focus();}catch(_){}},0);}function showUploadPicker(){if(!sessionToken)return showLoginModal();closeUploadOptionsOverlay();const overlay=createOverlay('cloud-upload-options-overlay');const card=el('div','cloud-card cloud-upload-options-card');const header=createHeader('Agregar o actualizar proyectos','Elige cómo quieres subir archivos .bi y la carpeta ExportModel_Web_Cache a la nube.',closeUploadOptionsOverlay);const content=el('div','cloud-upload-options');content.innerHTML=['<button type="button" id="cloudUploadFolderButton" class="cloud-upload-option"><strong>Cargar carpeta completa</strong><span>Selecciona la carpeta principal exportada desde Revit. Se buscarán todos los .bi, los JSON activos y la carpeta Incidencias con sus imágenes optimizadas.</span></button>','<button type="button" id="cloudUploadFilesButton" class="cloud-upload-option"><strong>Cargar archivos individuales</strong><span>Útil para subir .bi sueltos, JSON de caché o archivos específicos de Incidencias.</span></button>','<div class="cloud-actions"><button type="button" class="cloud-secondary" id="cloudUploadOptionsCancel">Cancelar</button></div>'].join('');card.appendChild(header);card.appendChild(content);overlay.appendChild(card);document.body.appendChild(overlay);document.getElementById('cloudUploadFolderButton').addEventListener('click',function(){closeUploadOptionsOverlay();pickProjectFolderForUpload();});document.getElementById('cloudUploadFilesButton').addEventListener('click',function(){closeUploadOptionsOverlay();pickIndividualBiFilesForUpload();});document.getElementById('cloudUploadOptionsCancel').addEventListener('click',closeUploadOptionsOverlay);}function closeUploadOptionsOverlay(){const existing=document.querySelector('.cloud-upload-options-overlay');if(existing)existing.remove();}async function pickProjectFolderForUpload(){const selection=await pickProjectDirectory('bim-new-project-folder');if(!selection)return;const allFiles=selection.files;const biFiles=collectProjectFilesFromFolder(allFiles);if(!biFiles.length){const selectedCount=allFiles.length;setDashboardMessage(selectedCount?'La carpeta seleccionada contiene '+selectedCount+' archivo(s), pero no se encontró ningún .bi ni contenido válido dentro de ExportModel_Web_Cache. Selecciona la carpeta principal exportada desde Revit, no una subcarpeta vacía ni un ZIP.':'No se seleccionó ninguna carpeta. Usa “Agregar / subir proyecto” y elige la carpeta principal exportada desde Revit.','warning');return;}const rootName=selection.rootName||inferRootName(allFiles.length?allFiles:biFiles);const projectName=await bimPrompt('Nombre del proyecto en la nube:',rootName||'Nuevo proyecto');if(!projectName)return;await uploadProjectFiles(projectName,rootName,biFiles,allFiles.length,{mode:'fullProject'});}async function pickProjectDirectory(pickerId){if(!window.isSecureContext||typeof window.showDirectoryPicker!=='function'){if(window.BimDialogs&&typeof window.BimDialogs.alert==='function'){await window.BimDialogs.alert('La selección profesional de carpetas requiere abrir el visor desde una dirección segura y utilizar una versión reciente de Chrome o Microsoft Edge. También puedes usar “Cargar archivos individuales”.',{title:'Selección de carpeta no disponible',type:'warning'});}else{setDashboardMessage('La selección de carpetas no está disponible en este navegador. Usa Chrome o Microsoft Edge y abre el visor desde una dirección segura.','warning');}return null;}try{const rootHandle=await window.showDirectoryPicker({id:String(pickerId||'bim-project-folder'),mode:'read'});const files=[];await collectDirectoryHandleFiles(rootHandle,rootHandle.name||'Proyecto','',files);return{rootName:String(rootHandle.name||'Nuevo proyecto'),files:files};}catch(error){if(error&&(error.name==='AbortError'||error.name==='NotAllowedError'))return null;console.error(error);if(window.BimDialogs&&typeof window.BimDialogs.alert==='function'){await window.BimDialogs.alert('No se pudo leer la carpeta seleccionada. Verifica que tengas permiso de lectura e inténtalo nuevamente.',{type:'error'});}else{setDashboardMessage('No se pudo leer la carpeta seleccionada.','error');}return null;}}async function collectDirectoryHandleFiles(directoryHandle,rootName,relativeDirectory,output){for await(const entry of directoryHandle.values()){const relativePath=normalizePath(relativeDirectory?relativeDirectory+'/'+entry.name:entry.name);if(entry.kind==='directory'){await collectDirectoryHandleFiles(entry,rootName,relativePath,output);continue;}if(entry.kind!=='file')continue;const file=await entry.getFile();const fullRelativePath=normalizePath(rootName+'/'+relativePath);defineReadOnlyProperty(file,'webkitRelativePath',fullRelativePath);defineReadOnlyProperty(file,'__exportModelRelativePath',fullRelativePath);output.push(file);}}function pickIndividualBiFilesForUpload(){const input=document.createElement('input');input.type='file';input.multiple=true;input.accept='.bi,.json,.webp,.png,.jpg,.jpeg,application/json,image/webp,image/png,image/jpeg,application/octet-stream';prepareHiddenInput(input);input.addEventListener('change',async function(){try{const files=Array.from(input.files||[]).filter(function(file){return isProjectUploadFile(file.name||'',file.name||'');}).sort(function(a,b){return a.name.localeCompare(b.name,undefined,{sensitivity:'base'});});if(!files.length){setDashboardMessage('No seleccionaste archivos válidos. Usa archivos .bi o contenido válido dentro de ExportModel_Web_Cache, incluida la carpeta Incidencias.','warning');return;}const defaultName=files.length===1?stripBiExtension(files[0].name):'Nuevo proyecto';const projectName=await bimPrompt('Nombre del proyecto en la nube:',defaultName);if(!projectName)return;await uploadProjectFiles(projectName,projectName,files,files.length,{mode:'individualFiles',relativePaths:files.map(function(file){return file.name;})});}finally{removeInputLater(input);}});document.body.appendChild(input);input.click();}async function pickFolderForExistingProject(project,config){if(!project)return;config=config||{};const selection=await pickProjectDirectory('bim-existing-project-folder');if(!selection)return;const allFiles=selection.files;const biFiles=collectProjectFilesFromFolder(allFiles);if(!biFiles.length){setDashboardMessage('La carpeta seleccionada no contiene archivos .bi ni contenido válido dentro de ExportModel_Web_Cache. Selecciona una carpeta exportada desde Revit.','warning');return;}const selectedRoot=selection.rootName||inferRootName(allFiles.length?allFiles:biFiles);const targetFolder=normalizePath(config.targetFolder||'');const ok=await bimConfirm((config.confirmText||'Se subirán los archivos válidos encontrados en la carpeta seleccionada. ¿Continuar?')+'\n\nDestino: '+formatFolderTarget(project,targetFolder)+'\nCarpeta local detectada: '+(selectedRoot||'selección')+'\nArchivos válidos encontrados: '+biFiles.length);if(!ok)return;const relativePaths=biFiles.map(function(file){const localPath=normalizePath(file.webkitRelativePath||file.__exportModelRelativePath||file.name);let mappedPath;if(config.stripSelectedRoot){mappedPath=stripFirstPathPart(localPath);}else if(config.keepSelectedRoot!==false){mappedPath=localPath;}else{mappedPath=stripFirstPathPart(localPath);}return normalizePath(targetFolder?targetFolder+'/'+mappedPath:mappedPath);});await uploadProjectFiles(project.name,project.rootName||project.name||project.projectId,biFiles,allFiles.length,{mode:config.mode||'replaceFiles',projectId:project.projectId,relativePaths:relativePaths,forceNames:relativePaths.map(function(path){return lastPathPart(path)||'archivo.bi';})});}function pickIndividualBiFilesForExistingProject(project,folderPrefix){if(!project)return;const input=document.createElement('input');input.type='file';input.multiple=true;input.accept='.bi,.json,.webp,.png,.jpg,.jpeg,application/json,image/webp,image/png,image/jpeg,application/octet-stream';prepareHiddenInput(input);input.addEventListener('change',async function(){try{const files=Array.from(input.files||[]).filter(function(file){return isProjectUploadFile(file.name||'',file.name||'');}).sort(function(a,b){return a.name.localeCompare(b.name,undefined,{sensitivity:'base'});});if(!files.length){setDashboardMessage('No seleccionaste archivos válidos.','warning');return;}const target=normalizePath(folderPrefix||getSelectedFolderPath(project));const relativePaths=files.map(function(file){return normalizePath(target?target+'/'+file.name:file.name);});const ok=await bimConfirm('Se agregarán o actualizarán '+files.length+' archivo(s) en:\n\n'+formatFolderTarget(project,target)+'\n\nSi ya existe un archivo con la misma ruta, será reemplazado. ¿Continuar?');if(!ok)return;await uploadProjectFiles(project.name,project.rootName||project.name||project.projectId,files,files.length,{mode:'replaceFiles',projectId:project.projectId,relativePaths:relativePaths,forceNames:files.map(function(file){return file.name;})});}finally{removeInputLater(input);}});document.body.appendChild(input);input.click();}function updateSingleProjectFile(project,fileInfo){if(!project||!fileInfo)return;const input=document.createElement('input');input.type='file';input.multiple=false;input.accept='.bi,.json,.webp,.png,.jpg,.jpeg,application/json,image/webp,image/png,image/jpeg,application/octet-stream';prepareHiddenInput(input);input.addEventListener('change',async function(){try{const file=input.files&&input.files[0];if(!file)return;const targetPath=normalizePath(fileInfo.relativePath||fileInfo.name||file.name);if(isBiFilePath(targetPath)&&!isBiFilePath(file.name||'')){setDashboardMessage('El archivo seleccionado no es .bi. Selecciona el modelo .bi actualizado.','warning');return;}if(isCacheJsonPath(targetPath)&&!isActiveProjectCacheJsonPath(targetPath)){setDashboardMessage('Este archivo de caché ya no se utiliza. No se actualiza ni se sincroniza.','warning');return;}if(isCacheJsonPath(targetPath)&&!JSON_FILE_PATTERN.test(file.name||'')){setDashboardMessage('El archivo seleccionado no es .json. Selecciona el archivo de caché actualizado.','warning');return;}if(!isBiFilePath(targetPath)&&!isCacheJsonPath(targetPath)&&!isProjectUploadFile(file.name||'',file.name||'')){setDashboardMessage('El archivo seleccionado no corresponde al tipo esperado.','warning');return;}const ok=await bimConfirm('Se actualizará únicamente este archivo en la nube:\n\n'+targetPath+'\n\nArchivo seleccionado:\n'+file.name+'\n\n¿Continuar?');if(!ok)return;await uploadProjectFiles(project.name,project.rootName||project.name||project.projectId,[file],1,{mode:'replaceFiles',projectId:project.projectId,relativePaths:[targetPath],forceNames:[lastPathPart(targetPath)||file.name]});}finally{removeInputLater(input);}});document.body.appendChild(input);input.click();}function prepareHiddenInput(input){input.style.position='fixed';input.style.left='-9999px';input.style.top='-9999px';input.style.opacity='0';}function removeInputLater(input){setTimeout(function(){try{input.remove();}catch(_){}},0);}function isBiFilePath(path){return BI_FILE_PATTERN.test(String(path||''));}function isCacheJsonPath(path){path=normalizePath(path||'');return JSON_FILE_PATTERN.test(path)&&CACHE_FOLDER_PATTERN.test(path);}function isIssueBundleFilePath(path){const normalized=normalizePath(path||'');const lower=normalized.toLowerCase();const prefix=ISSUES_BUNDLE_FOLDER.toLowerCase()+'/';return lower.indexOf(prefix)>=0&&(JSON_FILE_PATTERN.test(normalized)||ISSUE_IMAGE_PATTERN.test(normalized));}function isProjectUploadFile(path,name){const normalizedPath=normalizePath(path||name||'');return isBiFilePath(normalizedPath)||isBiFilePath(name||'')||isActiveProjectCacheJsonPath(normalizedPath)||isIssueBundleFilePath(normalizedPath);}function getProjectFileKind(fileOrPath){const path=typeof fileOrPath==='string'?normalizePath(fileOrPath):normalizePath((fileOrPath&&(fileOrPath.relativePath||fileOrPath.name))||'');if(isIssueBundleFilePath(path))return ISSUE_IMAGE_PATTERN.test(path)?'issue-image':'issue-cache';if(isActiveProjectCacheJsonPath(path))return'cache';if(isBiFilePath(path))return'bi';return'file';}function getProjectFileKindLabel(fileOrPath){const kind=getProjectFileKind(fileOrPath);if(kind==='issue-image')return'imagen de incidencia';if(kind==='issue-cache')return'datos de incidencia';if(kind==='cache')return'caché JSON';if(kind==='bi')return'modelo .bi';return'archivo';}function inferProjectContentType(file){const path=normalizePath((file&&(file.webkitRelativePath||file.__exportModelRelativePath||file.name))||'');if(isCacheJsonPath(path))return'application/json';return(file&&file.type)||'application/octet-stream';}function collectProjectFilesFromFolder(allFiles){return(allFiles||[]).filter(function(file){const path=normalizePath(file.webkitRelativePath||file.__exportModelRelativePath||file.name||'');return isProjectUploadFile(path,file.name||'');}).sort(function(a,b){const ap=normalizePath(a.webkitRelativePath||a.__exportModelRelativePath||a.name||'');const bp=normalizePath(b.webkitRelativePath||b.__exportModelRelativePath||b.name||'');return ap.localeCompare(bp,undefined,{sensitivity:'base'});});}function countFileKinds(files){return(files||[]).reduce(function(acc,file){const kind=getProjectFileKind(file);if(kind==='cache')acc.cache++;else if(kind==='bi')acc.bi++;else acc.other++;return acc;},{bi:0,cache:0,other:0});}async function verifyBiPackagesForUpload(files,fileDescriptors){const verifier=window.BimBiSignatureVerifier;if(!verifier||typeof verifier.inspectAndVerify!=='function'){throw new Error('No se cargó el verificador de autenticidad de archivos .bi.');}const result=new Map();for(let index=0;index<files.length;index++){const file=files[index];const descriptor=fileDescriptors[index]||{};const relativePath=normalizePath(descriptor.relativePath||file.webkitRelativePath||file.name||'');if(!isBiFilePath(relativePath))continue;setDashboardMessage('Verificando firma de '+(file.name||relativePath)+'...','info');setDashboardProgress(5,'Verificando autenticidad del archivo .bi...',true);const security=await verifier.inspectAndVerify(file,null,{});if(!security||!security.signed||!security.verified){throw new Error('El archivo '+(file.name||relativePath)+' no tiene una firma oficial válida.');}result.set(relativePath,security);}return result;}async function approveBiPackagesForUpload(projectId,securityByPath){if(!securityByPath||!securityByPath.size)return;const packages=[];securityByPath.forEach(function(security,relativePath){packages.push({relativePath:relativePath,packageHash:security.contentHash,packageId:security.packageId,certificateId:security.certificateId,certificate:security.certificate});});setDashboardMessage('Validando autorización de los archivos .bi...','info');setDashboardProgress(16,'Validando archivos firmados...',true);await callApi(CONFIG.actions.validateBiPackage,{projectId:projectId,packages:packages});}async function uploadProjectFiles(projectName,rootName,files,selectedFileCount,options){options=options||{};if(state.busy)return;setBusy(true);try{securityPolicy.assertCount(Array.isArray(files)?files.length:0,securityPolicy.limits.maxInputFiles,'Los archivos seleccionados para subir');(files||[]).forEach(function(file){const path=file&&(file.webkitRelativePath||file.__exportModelRelativePath||file.name)||'';if(isBiFilePath(path))securityPolicy.assertFileSize(file,securityPolicy.limits.maxBiPackageBytes,'El archivo .bi "'+(file.name||'sin nombre')+'"');});const forcedPaths=Array.isArray(options.relativePaths)?options.relativePaths:[];const forcedNames=Array.isArray(options.forceNames)?options.forceNames:[];const fileDescriptors=files.map(function(file,index){const defaultRelativePath=stripFirstPathPart(normalizePath(file.webkitRelativePath||file.__exportModelRelativePath||file.name));const relativePath=normalizePath(forcedPaths[index]||defaultRelativePath||file.name);return{name:forcedNames[index]||lastPathPart(relativePath)||file.name,relativePath:relativePath,sourceRelativePath:normalizePath(file.webkitRelativePath||file.__exportModelRelativePath||file.name),sizeBytes:file.size,lastModified:file.lastModified||0,contentType:inferProjectContentType(file)};});const biSecurityByPath=await verifyBiPackagesForUpload(files,fileDescriptors);const isUpdate=normalizePath(options.mode||'').toLowerCase()==='replacefiles';setDashboardMessage(isUpdate?'Preparando actualización de '+files.length+' archivo(s)...':'Carpeta/selección detectada: '+(selectedFileCount||files.length)+' archivo(s) revisados · '+files.length+' archivo(s) válidos encontrados. Preparando subida...','info');setDashboardProgress(8,isUpdate?'Preparando actualización...':'Preparando subida...',true);const session=await callApi(CONFIG.actions.createUploadSession,{projectId:options.projectId||'',updateMode:options.mode||'fullProject',projectName:String(projectName||'').trim(),rootName:rootName||'',selectedFileCount:selectedFileCount||files.length,files:fileDescriptors});await approveBiPackagesForUpload(session.projectId||options.projectId||'',biSecurityByPath);const uploadFiles=normalizeUploadFiles(session.files||(session.data&&session.data.files)||[]);if(!uploadFiles.length)throw new Error('El servicio en la nube no pudo preparar la subida. Inténtalo nuevamente o contacta al administrador.');setDashboardProgress(20,'URLs temporales creadas. Iniciando subida...',false);setDashboardMessage('Subiendo '+uploadFiles.length+' archivo(s) a la nube...','info');const uploadByPath=new Map(uploadFiles.map(item=>[normalizePath(item.relativePath),item]));const uploadedMetaByPath=new Map();let completed=0;await runWithLimit(files,CONFIG.maxParallelUploads,async function(file,index){const relativePath=normalizePath(fileDescriptors[index].relativePath);const uploadInfo=uploadByPath.get(relativePath)||uploadByPath.get(file.name);if(!uploadInfo||!uploadInfo.uploadUrl)throw new Error('No hay URL de subida para '+relativePath);const uploadedMeta=await uploadFileToSignedUrl(file,uploadInfo.uploadUrl,uploadInfo.headers||null);uploadedMetaByPath.set(relativePath,uploadedMeta||{});completed++;const percent=20+Math.round((completed/Math.max(files.length,1))*60);const label=(isUpdate?'Actualizando archivo: ':'Subiendo archivos: ')+completed+'/'+files.length;setDashboardProgress(percent,label,false);setDashboardMessage(label,'info');});setDashboardProgress(86,isUpdate?'Registrando actualización...':'Registrando proyecto...',true);setDashboardMessage(isUpdate?'Registrando actualización...':'Registrando proyecto...','info');await callApi(CONFIG.actions.finalizeUpload,{uploadId:session.uploadId||session.sessionId||'',projectId:session.projectId||options.projectId||'',updateMode:options.mode||'fullProject',projectName,rootName:rootName||projectName,files:uploadFiles.map(file=>{const meta=uploadedMetaByPath.get(normalizePath(file.relativePath))||{};const relativePath=normalizePath(file.relativePath);const biSecurity=biSecurityByPath.get(relativePath)||null;return{relativePath:file.relativePath,name:file.name||lastPathPart(file.relativePath),objectPath:file.objectPath||'',sizeBytes:file.sizeBytes||0,generation:meta.generation||file.generation||file.version||new Date().toISOString(),sha256:file.sha256||'',biPackageHash:biSecurity?biSecurity.contentHash:'',biPackageId:biSecurity?biSecurity.packageId:'',biCertificateId:biSecurity?biSecurity.certificateId:'',biSignatureStatus:biSecurity?'VERIFIED':'',etag:meta.etag||'',gcsHash:meta.gcsHash||'',contentType:file.contentType||'application/octet-stream'};})});if(isUpdate&&files.some(file=>isBiFilePath(file&&(file.name||file.webkitRelativePath||file.__exportModelRelativePath)))){await invalidateProjectCacheAfterBiUpdate(options.projectId||session.projectId||'',files);}setDashboardProgress(96,isUpdate?'Actualizando árbol...':'Actualizando lista...',true);setDashboardMessage(isUpdate?'Archivo actualizado correctamente. Actualizando árbol...':'Proyecto subido correctamente. Actualizando lista...','success');await loadProjectList(true);setDashboardProgress(100,isUpdate?'Actualización lista':'Proyecto listo',false);clearDashboardProgressSoon();}catch(error){console.error(error);setDashboardProgress(100,'Error durante la subida',false);setDashboardMessage(error.message||'No se pudo subir el proyecto.','error');}finally{setBusy(false);}}function stripBiExtension(name){return String(name||'').replace(/\.bi$/i,'')||'Nuevo proyecto';}function normalizeUploadFiles(files){return Array.isArray(files)?files.map(item=>{const relativePath=normalizePath(item.relativePath||item.path||item.name||'');if(!relativePath)return null;return Object.assign({},item,{relativePath,name:String(item.name||lastPathPart(relativePath)||'archivo.bi'),sizeBytes:Number(item.sizeBytes||item.size||0)||0,generation:String(item.generation||item.version||''),sha256:String(item.sha256||item.hash||''),contentType:String(item.contentType||'application/octet-stream'),uploadUrl:String(item.uploadUrl||item.writeUrl||item.signedUrl||''),headers:item.headers||null});}).filter(Boolean):[];}function verifyRemoteUploadRegistration(remoteFiles,descriptors,label){const byPath=new Map(normalizeFileList(remoteFiles||[]).map(file=>[normalizePath(file.relativePath||file.name||'').toLowerCase(),file]));const missing=[];const wrongSize=[];for(const descriptor of descriptors||[]){const path=normalizePath(descriptor&&descriptor.relativePath||'').toLowerCase();if(!path)continue;const remote=byPath.get(path);if(!remote){missing.push(descriptor.relativePath);continue;}const expectedSize=Math.max(0,Number(descriptor.sizeBytes)||0);const actualSize=Math.max(0,Number(remote.sizeBytes)||0);if(expectedSize&&actualSize&&expectedSize!==actualSize)wrongSize.push(descriptor.relativePath+' ('+actualSize+'/'+expectedSize+' bytes)');}if(missing.length||wrongSize.length){const details=[];if(missing.length)details.push('no registrados: '+missing.slice(0,5).join(', '));if(wrongSize.length)details.push('tamaño distinto: '+wrongSize.slice(0,5).join(', '));throw new Error('La nube no confirmó correctamente '+String(label||'los archivos guardados')+'. '+details.join(' · '));}return true;}async function uploadFileToSignedUrl(file,uploadUrl,headers){try{return await cloudTransferService.upload({url:uploadUrl,headers:Object.assign({},headers||{}),body:file,timeoutMs:CONFIG.transferTimeoutMs});}catch(error){if(error&&error.code==='HTTP_ERROR'){throw new Error('Error subiendo '+(file.name||'archivo')+': HTTP '+error.status+(error.details?' · '+error.details:''));}throw new Error('No se pudo subir "'+(file.name||'archivo')+'" a la nube. '+'La carpeta fue leída correctamente, pero la transferencia no pudo completarse. '+'Verifica tu conexión e inténtalo nuevamente. Si el problema continúa, contacta al administrador.');}}function getCurrentOriginForCors(){if(location.protocol==='file:')return'file:// no permitido para esta prueba';return location.origin||(location.protocol+'//'+location.host);}function getUrlHost(url){try{return new URL(url).host;}catch(_){return'';}}async function callApi(action,payload,options){return cloudApiClient.request(action,payload,options||{});}function formatApiActionError(error,actionName){const text=String(error&&error.message||'');if(/Action no soportado/i.test(text)){return'El servicio en la nube todavía no admite esta operación. Contacta al administrador para actualizar el sistema.';}return'';}function createApiError(data){const error=new Error(sanitizeCloudUiMessage(data.message||data.error,'El servicio en la nube rechazó la operación.'));error.name='CloudApiError';error.code=data.code||data.errorCode||'';error.data=data;error.activeSessions=data.activeSessions||data.sessions||(data.data&&(data.data.activeSessions||data.data.sessions))||[];error.maxWebSessions=data.maxWebSessions||(data.data&&data.data.maxWebSessions)||0;return error;}function isSessionLimitError(error){const code=String(error&&error.code||'').toUpperCase();return code==='WEB_SESSION_LIMIT'||code==='SESSION_LIMIT'||Array.isArray(error&&error.activeSessions)&&error.activeSessions.length>0;}function getWebClientInfo(){let payload=localStore.getJson(DEVICE_KEY,null,{removeInvalid:true});if(!payload||!payload.deviceHash){payload={deviceHash:createDeviceHash(),deviceName:inferDeviceName(),createdAt:new Date().toISOString()};localStore.setJson(DEVICE_KEY,payload);}if(!payload.deviceName)payload.deviceName=inferDeviceName();payload.timeZone=Intl&&Intl.DateTimeFormat?(Intl.DateTimeFormat().resolvedOptions().timeZone||''):'';return payload;}function createDeviceHash(){const random=new Uint8Array(32);if(window.crypto&&crypto.getRandomValues)crypto.getRandomValues(random);else for(let i=0;i<random.length;i++)random[i]=Math.floor(Math.random()*256);const raw=Array.from(random).map(x=>x.toString(16).padStart(2,'0')).join('')+'_'+Date.now();return'web_'+raw;}function inferDeviceName(){const ua=navigator.userAgent||'';const platform=navigator.platform||'';let browser='Navegador';if(/Edg\//i.test(ua))browser='Edge';else if(/Chrome\//i.test(ua)&&!/Edg\//i.test(ua))browser='Chrome';else if(/Firefox\//i.test(ua))browser='Firefox';else if(/Safari\//i.test(ua)&&!/Chrome\//i.test(ua))browser='Safari';let device=platform||'Dispositivo';if(/Android/i.test(ua))device='Android';else if(/iPhone/i.test(ua))device='iPhone';else if(/iPad/i.test(ua))device='iPad';else if(/Windows/i.test(ua))device='Windows';else if(/Macintosh|Mac OS/i.test(ua))device='Mac';return browser+' · '+device;}function startHeartbeat(){stopHeartbeat();const interval=Math.max(30000,Number(CONFIG.heartbeatIntervalMs)||60000);state.heartbeatTimer=setInterval(function(){sendHeartbeat(false);},interval);}function stopHeartbeat(){if(state.heartbeatTimer){clearInterval(state.heartbeatTimer);state.heartbeatTimer=0;}}async function sendHeartbeat(immediate){if(!sessionToken||!CONFIG.actions.heartbeat)return false;try{await callApi(CONFIG.actions.heartbeat,{sessionId:activeSessionId||''});return true;}catch(error){console.warn('Heartbeat cloud falló.',error);const code=String(error&&error.code||'').toUpperCase();if(code==='WEB_SESSION_CLOSED'||code==='WEB_SESSION_INVALID'||code==='WEB_SESSION_EXPIRED'){saveSession('',null,'');if(immediate)showLoginModal();}return false;}}async function logoutCloudSession(notifyServer){const token=sessionToken;const sessionId=activeSessionId;saveSession('',null,'');if(!notifyServer||!token||!CONFIG.actions.logout)return;try{await callApi(CONFIG.actions.logout,{sessionId},{token});}catch(error){console.warn('No se pudo cerrar la sesión cloud en servidor.',error);}}function showSessionLimitModal(error,loginContext){return sessionPanel.showSessionLimit(error,loginContext);}function formatDateTime(value){return sessionPanel.formatDateTime(value);}class CloudFileCache{async getOrDownload(project,fileInfo,onProgress){const key=makeFileCacheKey(project,fileInfo);const cached=await this.readFileRecord(key);const expectedSize=Number(fileInfo.sizeBytes)||0;const expectedSha256=cloudTransferService.normalizeSha256?cloudTransferService.normalizeSha256(fileInfo.sha256||''):String(fileInfo.sha256||'').toLowerCase();if(cached&&cached.blob&&cached.blob.size>securityPolicy.limits.maxBiPackageBytes){await this.deleteFileRecord(key);throw securityPolicy.resourceError('El archivo .bi almacenado en caché',securityPolicy.limits.maxBiPackageBytes,cached.blob.size);}const cachedSizeMatches=!!(cached&&cached.blob&&(!expectedSize||cached.blob.size===expectedSize));const cachedHashMatches=!expectedSha256||String(cached&&cached.verifiedSha256||'').toLowerCase()===expectedSha256;if(cachedSizeMatches&&cachedHashMatches){if(onProgress)onProgress({loaded:expectedSize||cached.blob.size||0,total:expectedSize||cached.blob.size||0,fromCache:true});return{blob:cached.blob,fromCache:true,key};}if(cached&&cached.blob&&(!cachedSizeMatches||!cachedHashMatches))await this.deleteFileRecord(key);if(!fileInfo.downloadUrl)throw new Error('No hay URL temporal de descarga para '+fileInfo.relativePath);if(expectedSize>securityPolicy.limits.maxBiPackageBytes){throw securityPolicy.resourceError('El archivo cloud "'+(fileInfo.name||fileInfo.relativePath||'Proyecto.bi')+'"',securityPolicy.limits.maxBiPackageBytes,expectedSize);}const blob=await downloadBlob(fileInfo.downloadUrl,onProgress,securityPolicy.limits.maxBiPackageBytes,fileInfo);await this.writeFileRecord(key,{key,projectId:project.projectId,relativePath:fileInfo.relativePath,generation:fileInfo.generation||'',sha256:fileInfo.sha256||'',verifiedSha256:String(blob&&blob.__bimTransferIntegrity&&blob.__bimTransferIntegrity.sha256||''),verifiedGcsHash:String(blob&&blob.__bimTransferIntegrity&&blob.__bimTransferIntegrity.gcsHash||''),sizeBytes:blob.size,cachedAt:new Date().toISOString(),blob});await this.rememberCurrentFile(project,fileInfo,key);return{blob,fromCache:false,key};}async readFileRecord(key){try{const db=await openCloudDb();const record=await idbGet(db,FILE_STORE,key);db.close();return record||null;}catch(_){return null;}}async writeFileRecord(key,record){const db=await openCloudDb();await idbPut(db,FILE_STORE,key,record);db.close();}async deleteFileRecord(key){try{const db=await openCloudDb();await idbDelete(db,FILE_STORE,key);db.close();}catch(_){}}async rememberCurrentFile(project,fileInfo,key){try{const db=await openCloudDb();const metaKey=makeCurrentMetaKey(project,fileInfo);const previous=await idbGet(db,META_STORE,metaKey);await idbPut(db,META_STORE,metaKey,{key,savedAt:new Date().toISOString()});if(previous&&previous.key&&previous.key!==key){try{await idbDelete(db,FILE_STORE,previous.key);}catch(_){}}db.close();}catch(_){}}}function installCloudCacheSyncToolbar(service){if(!service||!service.projectId)return;removeCloudCacheSyncToolbar();const toolbar=el('div','cloud-cache-sync-toolbar');toolbar.setAttribute('role','group');toolbar.setAttribute('aria-label','Sincronización del caché del proyecto');toolbar.innerHTML=['<button type="button" class="cloud-cache-sync-button" data-cloud-cache-action="pull" title="Traer el último ExportModel_Web_Cache sincronizado desde la nube">Actualizar desde nube</button>','<button type="button" class="cloud-cache-sync-button cloud-cache-sync-primary" data-cloud-cache-action="push" title="Subir a la nube los cambios locales del ExportModel_Web_Cache">Sincronizar a nube</button>','<button type="button" class="cloud-cache-sync-button cloud-cache-discard-button" data-cloud-cache-action="discard" title="Restaurar la última versión sincronizada y eliminar cambios locales pendientes">Descartar cambios</button>','<span class="cloud-cache-sync-status" data-cloud-cache-status role="button" tabindex="0" aria-haspopup="dialog" aria-expanded="false" title="Ver detalle de cambios pendientes">Sin cambios pendientes</span>'].join('');const host=document.getElementById('cloudCacheSyncHost');const topActions=document.querySelector('.top-actions');const appShell=document.querySelector('.app-shell');if(host){host.appendChild(toolbar);}else{toolbar.classList.add('cloud-cache-sync-toolbar-floating');document.body.appendChild(toolbar);}if(topActions)topActions.classList.add('cloud-cache-sync-visible');if(appShell)appShell.classList.add('cloud-cache-sync-visible');state.cacheToolbarNode=toolbar;toolbar.querySelector('[data-cloud-cache-action="push"]').addEventListener('click',function(){syncActiveProjectCacheToCloud();});toolbar.querySelector('[data-cloud-cache-action="pull"]').addEventListener('click',function(){refreshActiveProjectCacheFromCloud();});toolbar.querySelector('[data-cloud-cache-action="discard"]').addEventListener('click',function(){discardActiveProjectCacheChanges();});const statusNode=toolbar.querySelector('[data-cloud-cache-status]');if(statusNode){const openDetails=function(){statusNode.setAttribute('aria-expanded','true');changesPanel.open(statusNode);};statusNode.addEventListener('click',openDetails);statusNode.addEventListener('keydown',function(event){if(event.key==='Enter'||event.key===' '){event.preventDefault();openDetails();}});}refreshCloudCacheToolbarState(service);}function removeCloudCacheSyncToolbar(){if(changesPanel)changesPanel.close();if(state.cacheToolbarNode){try{state.cacheToolbarNode.remove();}catch(_){}state.cacheToolbarNode=null;}const topActions=document.querySelector('.top-actions');const appShell=document.querySelector('.app-shell');if(topActions)topActions.classList.remove('cloud-cache-sync-visible');if(appShell)appShell.classList.remove('cloud-cache-sync-visible');}function markCloudCacheDirty(service,name){if(service&&state.activeStateService===service){refreshCloudCacheToolbarState(service,name);}}async function refreshCloudCacheToolbarState(service){const toolbar=state.cacheToolbarNode;if(!toolbar||!service)return;const status=toolbar.querySelector('[data-cloud-cache-status]');try{const dirty=await service.listDirtyStateEntries();const actionCount=dirty.reduce((sum,entry)=>sum+Math.max(1,Number(entry&&entry.revisionCount)||1),0);if(status){status.textContent=dirty.length?dirty.length+' archivo(s) modificado(s) · '+actionCount+' cambio(s) pendiente(s)':'Sin cambios pendientes';status.classList.toggle('is-dirty',dirty.length>0);status.title=status.textContent+' · Clic para ver el detalle';}const discardButton=toolbar.querySelector('[data-cloud-cache-action="discard"]');const pushButton=toolbar.querySelector('[data-cloud-cache-action="push"]');const canEditCloud=!state.activeCloudProject||getProjectAccess(state.activeCloudProject).canEdit;if(discardButton)discardButton.disabled=dirty.length===0;if(pushButton){pushButton.disabled=dirty.length===0||!canEditCloud;pushButton.title=canEditCloud?'Subir a la nube los cambios locales del ExportModel_Web_Cache':'Tu rol es de solo lectura';}if(changesPanel&&changesPanel.opened)await changesPanel.refresh();}catch(_){if(status){status.textContent='Estado del caché no disponible';status.title=status.textContent;}}}function setCloudCacheToolbarBusy(message,busy){const toolbar=state.cacheToolbarNode;if(!toolbar)return;toolbar.classList.toggle('is-busy',!!busy);const status=toolbar.querySelector('[data-cloud-cache-status]');if(status&&message){status.textContent=message;status.title=message;}toolbar.querySelectorAll('button').forEach(button=>{button.disabled=!!busy;});}async function syncActiveProjectCacheToCloud(){const service=state.activeStateService;if(!service)return alert('Abre primero un proyecto desde Mi nube.');try{eventBus.emit('cloud:sync-started',{direction:'upload',project:state.activeCloudProject});setCloudCacheToolbarBusy('Preparando sincronización...',true);await flushPendingProjectCacheWrites();const result=await service.syncDirtyStateToCloud(function(progress){const total=Math.max(1,Number(progress.total)||1);const current=Math.max(0,Number(progress.current)||0);setCloudCacheToolbarBusy('Sincronizando caché '+current+'/'+total+'...',true);});setCloudCacheToolbarBusy(result.message||'Caché sincronizado.',false);eventBus.emit('cloud:sync-completed',{direction:'upload',project:state.activeCloudProject,result:result});await refreshCloudCacheToolbarState(service);}catch(error){console.error(error);eventBus.emit('cloud:sync-error',{direction:'upload',project:state.activeCloudProject,error:error});setCloudCacheToolbarBusy('Error al sincronizar: '+(error.message||error),false);alert('No se pudo sincronizar el caché del proyecto:\n'+(error.message||error));}}async function refreshActiveProjectCacheFromCloud(){const service=state.activeStateService;if(!service)return alert('Abre primero un proyecto desde Mi nube.');const ok=await bimConfirm('Se traerá el último ExportModel_Web_Cache publicado en la nube. Si tienes cambios locales sin sincronizar podrían ser reemplazados.\n\n¿Continuar?');if(!ok)return;try{eventBus.emit('cloud:sync-started',{direction:'download',project:state.activeCloudProject});setCloudCacheToolbarBusy('Leyendo caché desde nube...',true);const result=await service.refreshSharedStateFromCloud(function(progress){const total=Math.max(1,Number(progress.total)||1);const current=Math.max(0,Number(progress.current)||0);setCloudCacheToolbarBusy('Actualizando caché '+current+'/'+total+'...',true);});setCloudCacheToolbarBusy((result&&result.message)||'Caché actualizado desde nube.',false);eventBus.emit('cloud:sync-completed',{direction:'download',project:state.activeCloudProject,result:result});await refreshCloudCacheToolbarState(service);alert('Caché actualizado desde nube. Para aplicar todos los cambios visuales ya cargados, vuelve a abrir el panel correspondiente o vuelve a abrir el proyecto si es necesario.');}catch(error){console.error(error);eventBus.emit('cloud:sync-error',{direction:'download',project:state.activeCloudProject,error:error});setCloudCacheToolbarBusy('Error al actualizar desde nube: '+(error.message||error),false);alert('No se pudo actualizar el caché desde nube:\n'+(error.message||error));}}async function flushPendingProjectCacheWrites(){const ui=window.__exportModelUI;const massPanel=window.__exportModelMassPanel;if(ui&&ui.persistClashStateNow)await ui.persistClashStateNow();if(massPanel&&massPanel.persistParameterOverridesNow)await massPanel.persistParameterOverridesNow();if(window.BimPlansPanel&&window.BimPlansPanel.flushPersistedState)await window.BimPlansPanel.flushPersistedState();if(window.BimIssuesPanel&&window.BimIssuesPanel.flushSharedPersistence){const issuesSaved=await window.BimIssuesPanel.flushSharedPersistence(false);if(issuesSaved===false)throw new Error('No se pudo confirmar el guardado local de las incidencias antes de sincronizar.');}return true;}async function discardActiveProjectCacheChanges(){const service=state.activeStateService;if(!service)return alert('Abre primero un proyecto desde Mi nube.');await flushPendingProjectCacheWrites();const dirty=await service.listDirtyStateEntries();if(!dirty.length){refreshCloudCacheToolbarState(service);return;}const actionCount=dirty.reduce((sum,entry)=>sum+Math.max(1,Number(entry&&entry.revisionCount)||1),0);const ok=await bimConfirm('Se descartarán '+actionCount+' cambio(s) pendiente(s) en '+dirty.length+' archivo(s). Se restaurará la última versión sincronizada.\n\nEsta acción no se puede deshacer. ¿Continuar?');if(!ok)return;try{eventBus.emit('cloud:discard-started',{project:state.activeCloudProject,entries:dirty.length,changes:actionCount});setCloudCacheToolbarBusy('Descartando cambios locales...',true);const result=await service.discardDirtyStateChanges(function(progress){const total=Math.max(1,Number(progress.total)||1);const current=Math.max(0,Number(progress.current)||0);setCloudCacheToolbarBusy('Restaurando caché '+current+'/'+total+'...',true);});setCloudCacheToolbarBusy(result.message||'Cambios descartados.',false);eventBus.emit('cloud:discard-completed',{project:state.activeCloudProject,result});await refreshCloudCacheToolbarState(service);}catch(error){console.error(error);eventBus.emit('cloud:discard-error',{project:state.activeCloudProject,error});setCloudCacheToolbarBusy('Error al descartar cambios: '+(error.message||error),false);alert('No se pudieron descartar los cambios locales:\n'+(error.message||error));}}async function discardActiveProjectCacheFile(name){const service=state.activeStateService;if(!service)return;await flushPendingProjectCacheWrites();const entry=(await service.listDirtyStateEntries()).find(item=>item&&item.name===name);if(!entry)return;const ok=await bimConfirm('Se descartarán todos los cambios locales de '+name+' y se restaurará la última versión sincronizada.\n\n¿Continuar?');if(!ok)return;setCloudCacheToolbarBusy('Descartando cambios de '+name+'...',true);try{await service.discardDirtyStateFile(name);await refreshCloudCacheToolbarState(service);}finally{setCloudCacheToolbarBusy('',false);}}async function discardActiveProjectCacheSingleChange(name,change){const service=state.activeStateService;if(!service||!change)return;await flushPendingProjectCacheWrites();const ok=await bimConfirm('Se descartará únicamente esta modificación:\n\n'+(change.label||change.pathText||'Cambio seleccionado')+'\n\n¿Continuar?');if(!ok)return;setCloudCacheToolbarBusy('Descartando modificación...',true);try{await service.discardDirtyStateChange(name,change);await refreshCloudCacheToolbarState(service);}finally{setCloudCacheToolbarBusy('',false);}}async function uploadStateEntriesToCloud(service,entries,onProgress){const project=service.project||{};const projectId=service.projectId||project.projectId||'';if(!projectId)throw new Error('No se encontró projectId para sincronizar caché.');entries=(entries||[]).filter(entry=>entry&&!isDeprecatedProjectCacheName(entry.name));if(!entries.length)return;const now=Date.now();const files=entries.map(function(entry,index){const name=normalizeStateFileNameForCloud(entry.name);const payload=entry.payload==null?null:entry.payload;const blob=new Blob([JSON.stringify(payload,null,2)],{type:'application/json'});let file;try{file=new File([blob],name,{type:'application/json',lastModified:now+index});}catch(_){file=blob;file.name=name;file.lastModified=now+index;}const relativePath=CACHE_FOLDER_NAME+'/'+name;file.__exportModelRelativePath=(project.rootName||project.name||'ProyectoCloud')+'/'+relativePath;return file;});const relativePaths=entries.map(entry=>CACHE_FOLDER_NAME+'/'+normalizeStateFileNameForCloud(entry.name));const forcedNames=entries.map(entry=>normalizeStateFileNameForCloud(entry.name));const fileDescriptors=files.map(function(file,index){const relativePath=normalizePath(relativePaths[index]);return{name:forcedNames[index]||lastPathPart(relativePath)||file.name,relativePath:relativePath,sourceRelativePath:normalizePath(file.__exportModelRelativePath||file.name),sizeBytes:file.size,lastModified:file.lastModified||0,contentType:'application/json'};});const session=await callApi(CONFIG.actions.createUploadSession,{projectId,updateMode:'replaceFiles',projectName:String(project.name||project.projectName||projectId),rootName:project.rootName||project.name||projectId,selectedFileCount:files.length,files:fileDescriptors});const uploadFiles=normalizeUploadFiles(session.files||(session.data&&session.data.files)||[]);if(!uploadFiles.length)throw new Error('El servidor no devolvió URLs de subida para sincronizar caché.');const uploadByPath=new Map(uploadFiles.map(item=>[normalizePath(item.relativePath),item]));const uploadedMetaByPath=new Map();let completed=0;await runWithLimit(files,CONFIG.maxParallelUploads,async function(file,index){const relativePath=normalizePath(fileDescriptors[index].relativePath);const uploadInfo=uploadByPath.get(relativePath);if(!uploadInfo||!uploadInfo.uploadUrl)throw new Error('No hay URL de subida para '+relativePath);const meta=await uploadFileToSignedUrl(file,uploadInfo.uploadUrl,uploadInfo.headers||null);uploadedMetaByPath.set(relativePath,meta||{});completed++;if(onProgress)onProgress({current:completed,total:files.length});});await callApi(CONFIG.actions.finalizeUpload,{uploadId:session.uploadId||session.sessionId||'',projectId:session.projectId||projectId,updateMode:'replaceFiles',projectName:project.name||project.projectName||projectId,rootName:project.rootName||project.name||projectId,files:uploadFiles.map(file=>{const meta=uploadedMetaByPath.get(normalizePath(file.relativePath))||{};return{relativePath:file.relativePath,name:file.name||lastPathPart(file.relativePath),objectPath:file.objectPath||'',sizeBytes:file.sizeBytes||0,generation:meta.generation||file.generation||file.version||new Date().toISOString(),sha256:file.sha256||'',etag:meta.etag||'',gcsHash:meta.gcsHash||'',contentType:'application/json'};})});const response=await callApi(CONFIG.actions.openProject,{projectId});const serverProject=normalizeProject(response.project||(response.data&&response.data.project)||project);const nextFiles=normalizeFileList(response.files||(response.data&&response.data.files)||(serverProject&&serverProject.files)||[]);verifyRemoteUploadRegistration(nextFiles,fileDescriptors,'los archivos de configuración y caché');if(serverProject)serverProject.files=nextFiles;service.project=serverProject||Object.assign({},service.project,{files:nextFiles});updateProjectInState(service.project);}function normalizeIssueBundleCloudPath(path){const normalized=normalizePath(path||'');if(!normalized)return'';const lower=normalized.toLowerCase();if(lower.indexOf(CACHE_FOLDER_NAME.toLowerCase()+'/')===0)return normalized;return normalizePath(CACHE_FOLDER_NAME+'/'+normalized);}function makeIssueBundleStoreKey(storageIdentity,relativePath){return ISSUE_BUNDLE_STORE_PREFIX+String(storageIdentity||'')+'|'+normalizeIssueBundleCloudPath(relativePath).toLowerCase();}async function readIssueBundleStoreEntry(storageIdentity,relativePath){try{const db=await openCloudDb();const value=await idbGet(db,FILE_STORE,makeIssueBundleStoreKey(storageIdentity,relativePath));db.close();return value||null;}catch(_){return null;}}async function writeIssueBundleStoreEntry(storageIdentity,relativePath,record){const path=normalizeIssueBundleCloudPath(relativePath);const key=makeIssueBundleStoreKey(storageIdentity,path);const db=await openCloudDb();await idbPut(db,FILE_STORE,key,Object.assign({},record||{},{key,storageIdentity:String(storageIdentity||''),relativePath:path,issueBundle:true,updatedAt:new Date().toISOString()}));db.close();return key;}async function deleteIssueBundleStoreEntry(storageIdentity,relativePath){try{const db=await openCloudDb();await idbDelete(db,FILE_STORE,makeIssueBundleStoreKey(storageIdentity,relativePath));db.close();}catch(_){}}async function listIssueBundleStoreEntries(storageIdentity){const identity=String(storageIdentity||'');const prefix=ISSUE_BUNDLE_STORE_PREFIX+identity+'|';const result=[];try{const db=await openCloudDb();await new Promise((resolve,reject)=>{const transaction=db.transaction(FILE_STORE,'readonly');const request=transaction.objectStore(FILE_STORE).openCursor();request.onsuccess=event=>{const cursor=event.target.result;if(!cursor)return;const key=String(cursor.key||'');const value=cursor.value;if(key.indexOf(prefix)===0&&value&&value.issueBundle)result.push(value);cursor.continue();};transaction.oncomplete=()=>resolve();transaction.onerror=()=>reject(transaction.error||new Error('No se pudo listar la caché de incidencias.'));});db.close();}catch(error){console.warn('No se pudo listar la caché binaria de incidencias.',error);}return result;}function hashIssueBundleText(text){const value=String(text||'');let hash=2166136261;for(let index=0;index<value.length;index++){hash^=value.charCodeAt(index);hash=Math.imul(hash,16777619);}return'txt-'+(hash>>>0).toString(16)+'-'+value.length.toString(16);}async function getIssueBundleDescriptorSignature(descriptor){if(!descriptor||!descriptor.blob)return String(descriptor&&descriptor.fingerprint||'');if(descriptor.fingerprint)return String(descriptor.fingerprint)+':'+descriptor.blob.size;if(String(descriptor.contentType||descriptor.blob.type||'').indexOf('application/json')>=0){return hashIssueBundleText(await descriptor.blob.text());}return'blob-'+descriptor.blob.size+'-'+String(descriptor.blob.type||descriptor.contentType||'');}async function uploadIssueBundleEntriesToCloud(service,entries,onProgress){const project=service.project||{};const projectId=service.projectId||project.projectId||'';if(!projectId)throw new Error('No se encontró projectId para sincronizar las incidencias.');const uploadEntries=(entries||[]).filter(entry=>entry&&entry.dirty&&!entry.deleted&&entry.blob);const deleteEntries=(entries||[]).filter(entry=>entry&&entry.dirty&&entry.deleted);const total=uploadEntries.length+deleteEntries.length;let completed=0;let uploadedDescriptors=[];for(const entry of deleteEntries){try{await callApi(CONFIG.actions.deleteProjectItems,{projectId,itemType:'file',itemPath:entry.relativePath});}catch(error){const message=String(error&&error.message||error||'');if(!/no existe|not found|404/i.test(message))throw error;}await deleteIssueBundleStoreEntry(service.storageIdentity,entry.relativePath);completed++;if(onProgress)onProgress({current:completed,total,name:entry.relativePath});}if(uploadEntries.length){const now=Date.now();const files=uploadEntries.map((entry,index)=>{const name=lastPathPart(entry.relativePath)||'archivo';let file;try{file=new File([entry.blob],name,{type:entry.contentType||entry.blob.type||'application/octet-stream',lastModified:now+index});}catch(_){file=entry.blob;file.name=name;file.lastModified=now+index;}file.__exportModelRelativePath=(project.rootName||project.name||'ProyectoCloud')+'/'+entry.relativePath;return file;});const descriptors=uploadEntries.map((entry,index)=>({name:lastPathPart(entry.relativePath)||files[index].name,relativePath:normalizePath(entry.relativePath),sourceRelativePath:normalizePath(files[index].__exportModelRelativePath||files[index].name),sizeBytes:files[index].size,lastModified:files[index].lastModified||0,contentType:entry.contentType||files[index].type||'application/octet-stream'}));uploadedDescriptors=descriptors;const session=await callApi(CONFIG.actions.createUploadSession,{projectId,updateMode:'replaceFiles',projectName:String(project.name||project.projectName||projectId),rootName:project.rootName||project.name||projectId,selectedFileCount:files.length,files:descriptors});const uploadFiles=normalizeUploadFiles(session.files||(session.data&&session.data.files)||[]);if(!uploadFiles.length)throw new Error('El servidor no devolvió URLs para subir los archivos de incidencias.');const uploadByPath=new Map(uploadFiles.map(item=>[normalizePath(item.relativePath),item]));const uploadedMetaByPath=new Map();await runWithLimit(files,CONFIG.maxParallelUploads,async(file,index)=>{const relativePath=normalizePath(descriptors[index].relativePath);const uploadInfo=uploadByPath.get(relativePath);if(!uploadInfo||!uploadInfo.uploadUrl)throw new Error('No hay URL de subida para '+relativePath);const meta=await uploadFileToSignedUrl(file,uploadInfo.uploadUrl,uploadInfo.headers||null);uploadedMetaByPath.set(relativePath,meta||{});completed++;if(onProgress)onProgress({current:completed,total,name:relativePath});});await callApi(CONFIG.actions.finalizeUpload,{uploadId:session.uploadId||session.sessionId||'',projectId:session.projectId||projectId,updateMode:'replaceFiles',projectName:project.name||project.projectName||projectId,rootName:project.rootName||project.name||projectId,files:uploadFiles.map(file=>{const relativePath=normalizePath(file.relativePath);const meta=uploadedMetaByPath.get(relativePath)||{};const descriptor=descriptors.find(item=>normalizePath(item.relativePath)===relativePath)||{};return{relativePath:file.relativePath,name:file.name||lastPathPart(file.relativePath),objectPath:file.objectPath||'',sizeBytes:file.sizeBytes||descriptor.sizeBytes||0,generation:meta.generation||file.generation||file.version||new Date().toISOString(),sha256:file.sha256||'',etag:meta.etag||'',gcsHash:meta.gcsHash||'',contentType:descriptor.contentType||'application/octet-stream'};})});}if(total){const response=await callApi(CONFIG.actions.openProject,{projectId});const serverProject=normalizeProject(response.project||(response.data&&response.data.project)||project);const nextFiles=normalizeFileList(response.files||(response.data&&response.data.files)||(serverProject&&serverProject.files)||[]);if(uploadEntries.length)verifyRemoteUploadRegistration(nextFiles,uploadedDescriptors,'las incidencias y evidencias');if(serverProject)serverProject.files=nextFiles;service.project=serverProject||Object.assign({},service.project,{files:nextFiles});state.activeCloudProject=service.project;updateProjectInState(service.project);for(const entry of uploadEntries){await writeIssueBundleStoreEntry(service.storageIdentity,entry.relativePath,Object.assign({},entry,{dirty:false,deleted:false,syncedAt:new Date().toISOString()}));}}return{uploaded:uploadEntries.length,deleted:deleteEntries.length};}function updateProjectInState(project){if(!project||!project.projectId)return;const normalized=normalizeProject(project);const index=state.projects.findIndex(item=>item&&item.projectId===normalized.projectId);if(index>=0)state.projects[index]=Object.assign({},state.projects[index],normalized);else state.projects.push(normalized);}function applySpecialStateToBrowser(name,payload){const normalized=normalizeStateFileNameForCloud(name).toLowerCase();if(isDeprecatedProjectCacheName(normalized))return;try{if(normalized==='plans_2d_cache.json'){const statePayload=payload&&payload.state?payload.state:(payload||{});if(window.BimPlansPanel&&window.BimPlansPanel.applyPersistedState){window.BimPlansPanel.applyPersistedState(statePayload);}localStore.setJson(PLANS_FOLDER_LOCAL_STORAGE_KEY,statePayload.folderStructure||{version:1,groups:{}});localStore.setJson(PLANS_ANNOTATION_LOCAL_STORAGE_KEY,statePayload.annotations||{version:1,plans:{}});}else if(normalized==='interferences_cache.json'){const ui=window.__exportModelUI;const clashState=payload&&payload.state?payload.state:(payload||{});if(ui&&ui.loadPersistedClashState){const previousSuspend=ui.suspendPersistence;ui.suspendPersistence=true;try{ui.loadPersistedClashState(clashState);}finally{ui.suspendPersistence=previousSuspend;}}}else if(normalized==='saved_configurations.json'){const massPanel=window.__exportModelMassPanel;if(massPanel&&massPanel.refreshSavedConfigurationsList)massPanel.refreshSavedConfigurationsList();}else if(normalized==='works_saved_configurations.json'){const ui=window.__exportModelUI;if(ui&&ui.refreshWorksSavedConfigurationsList)ui.refreshWorksSavedConfigurationsList();}else if(normalized==='parameter_overrides.json'){applyParameterOverridesStateToBrowser(payload||{items:[]});}else if(normalized==='issues_cache.json'){const issuesState=payload&&payload.state?payload.state:(payload||{});if(window.BimIssuesPanel&&window.BimIssuesPanel.applyPersistedState){window.BimIssuesPanel.applyPersistedState(issuesState);}}else if(normalized==='plans_folder_cache.json'||normalized==='plans_folder_structure.json'){localStore.setJson(PLANS_FOLDER_LOCAL_STORAGE_KEY,payload||{version:1,groups:{}});}else if(normalized==='plans_annotations.json'||normalized==='plan_annotations.json'||normalized==='annotations_2d.json'){localStore.setJson(PLANS_ANNOTATION_LOCAL_STORAGE_KEY,payload||{version:1,plans:{}});}}catch(error){console.warn('No se pudo aplicar el estado restaurado:',normalized,error);}}function applyParameterOverridesStateToBrowser(payload){const model=window.ExportModelApp&&window.ExportModelApp.getActiveModel?window.ExportModelApp.getActiveModel():null;if(!model||!window.BimDataStore)return;const store=new window.BimDataStore(model);if(store.replaceParameterOverrides)store.replaceParameterOverrides(payload||{items:[]});const massPanel=window.__exportModelMassPanel;if(massPanel){if(massPanel.clearCustomParameterValueCache)massPanel.clearCustomParameterValueCache();if(massPanel.applyFilters)massPanel.applyFilters();if(massPanel.render)massPanel.render();}const ui=window.__exportModelUI;if(ui&&ui.selectedElement&&ui.selectElement)ui.selectElement(ui.selectedElement);}function normalizeStateFileNameForCloud(name){const text=lastPathPart(String(name||'').replace(/\\/g,'/')).trim();if(!text)return'';return text.toLowerCase().endsWith('.json')?text:text+'.json';}function isDeprecatedProjectCacheName(name){const normalized=normalizeStateFileNameForCloud(name).toLowerCase();return!!DEPRECATED_PROJECT_CACHE_NAMES[normalized];}function isActiveProjectCacheJsonPath(path){if(!isCacheJsonPath(path))return false;return!isDeprecatedProjectCacheName(lastPathPart(path));}function isTopLevelCacheStateJsonPath(path){const normalized=normalizePath(path||'');if(!isActiveProjectCacheJsonPath(normalized))return false;const lower=normalized.toLowerCase();const marker=CACHE_FOLDER_NAME.toLowerCase()+'/';const index=lower.lastIndexOf(marker);if(index<0)return false;const relative=normalized.slice(index+marker.length);return relative.indexOf('/')<0;}function getCacheStateNamesToInvalidateAfterBiUpdate(updatedFiles,project){const has2D=(updatedFiles||[]).some(file=>/_2d\.bi$/i.test(String(file&&(file.name||file.webkitRelativePath||file.__exportModelRelativePath)||'')));const has3D=(updatedFiles||[]).some(file=>isBiFilePath(file&&(file.name||file.webkitRelativePath||file.__exportModelRelativePath))&&!/_2d\.bi$/i.test(String(file&&(file.name||file.webkitRelativePath||file.__exportModelRelativePath)||'')));const names=new Set();if(has3D)CACHE_NAMES_INVALIDATED_BY_3D_BI.forEach(name=>names.add(name));if(has2D)CACHE_NAMES_INVALIDATED_BY_2D_BI.forEach(name=>names.add(name));Object.keys(DEPRECATED_PROJECT_CACHE_NAMES).forEach(name=>names.add(name));normalizeFileList(project&&project.files||[]).forEach(file=>{const path=normalizePath(file.relativePath||file.name||'');if(!isCacheJsonPath(path))return;const name=lastPathPart(path);if(!name)return;if(isDeprecatedProjectCacheName(name)){names.add(name);return;}if(CACHE_NAMES_TO_KEEP_ON_BI_UPDATE[name.toLowerCase()])return;const lower=name.toLowerCase();if(has3D&&(lower.indexOf('parameter')>=0||lower.indexOf('override')>=0))names.add(name);if(has2D&&(lower.indexOf('annotation')>=0||lower.indexOf('plan')>=0||lower.indexOf('2d')>=0))names.add(name);});return Array.from(names);}async function invalidateProjectCacheAfterBiUpdate(projectId,updatedFiles){const service=state.activeStateService&&state.activeStateService.projectId===projectId?state.activeStateService:null;const project=service?service.project:getSelectedProject();const names=getCacheStateNamesToInvalidateAfterBiUpdate(updatedFiles,project);if(!names.length)return;const storageIdentity=service?service.storageIdentity:makeCloudStateStorageIdentity(project||{projectId:projectId});for(const name of names)await markStateFallbackInvalidated(storageIdentity,name);await deleteCacheJsonFilesFromCloud(projectId,project,names);if(service)refreshCloudCacheToolbarState(service);}async function deleteCacheJsonFilesFromCloud(projectId,project,names){const files=normalizeFileList(project&&project.files||[]);for(const name of names||[]){const target=normalizePath(CACHE_FOLDER_NAME+'/'+normalizeStateFileNameForCloud(name)).toLowerCase();const file=files.find(item=>{const path=normalizePath(item.relativePath||item.name||'').toLowerCase();return path===target||path.endsWith('/'+target);});if(!file)continue;try{await callApi(CONFIG.actions.deleteProjectItems,{projectId,itemType:'file',itemPath:file.relativePath||file.name});}catch(error){console.warn('No se pudo invalidar caché cloud:',name,error);}}}function makeCloudStateStorageIdentity(project){const userId=String((state.user&&(state.user.userId||state.user.id||state.user.user||state.user.username))||'user');const projectId=String(project&&(project.projectId||project.id||project.ProjectId)||'project');return'cloud|'+encodeURIComponent(userId)+'|'+encodeURIComponent(projectId);}function getLegacyProjectIdFromStorageIdentity(identity){const text=String(identity||'');if(text.indexOf('cloud|')!==0)return'';const parts=text.split('|');try{return decodeURIComponent(parts.slice(2).join('|'));}catch(_){return parts.slice(2).join('|');}}function unwrapInterferencePayload(payload){return payload&&payload.state?payload.state:(payload||{});}function getInterferenceEvaluationCount(payload){const statePayload=unwrapInterferencePayload(payload);return Array.isArray(statePayload.clashEvaluations)?statePayload.clashEvaluations.length:0;}function mergeLegacyInterferencePayload(cloudPayload,localPayload){const cloudWrapped=cloudPayload&&cloudPayload.state?cloudPayload:{version:1,state:unwrapInterferencePayload(cloudPayload)};const localState=unwrapInterferencePayload(localPayload);const mergedState=Object.assign({},unwrapInterferencePayload(cloudWrapped),localState);const byId=new Map();(unwrapInterferencePayload(cloudWrapped).clashEvaluations||[]).forEach(item=>{if(item&&item.id)byId.set(String(item.id),item);});(localState.clashEvaluations||[]).forEach(item=>{if(item&&item.id)byId.set(String(item.id),item);});mergedState.clashEvaluations=Array.from(byId.values());return Object.assign({},cloudWrapped,{state:mergedState,savedAt:new Date().toISOString()});}function reconcileInterferencePayload(cloudPayload,localPayload,baselinePayload){if(baselinePayload!=null){return cloudChangesApi.rebasePayload(baselinePayload,localPayload,cloudPayload,{fileName:'interferences_cache.json',maxChanges:1000});}return mergeLegacyInterferencePayload(cloudPayload,localPayload);}class CloudProjectStateCacheService{constructor(project){this.project=project||{};this.rootName=this.project.name||this.project.projectId||'ProyectoCloud';this.readOnly=false;this.projectId=String(this.project.projectId||'');this.storageIdentity=makeCloudStateStorageIdentity(this.project);}getIdentityInfo(){return{mode:'cloud',projectId:this.projectId,projectName:this.rootName,userId:String((state.user&&(state.user.userId||state.user.id||state.user.user||state.user.username))||''),storageIdentity:this.storageIdentity};}async readProjectCache(){return null;}async writeProjectCache(){}async writeMetadata(){}async readMassInfoConfig(){return null;}async saveMassInfoConfig(){}async readSavedConfigurations(){return await this.readStateFile('saved_configurations.json');}async readClassificationColorRules(){return await this.readStateFile('classification_color_rules.json');}async saveClassificationColorRules(payload,options){const safePayload=payload&&typeof payload==='object'?payload:{};await this.writeStateFile('classification_color_rules.json',{version:1,savedAt:new Date().toISOString(),rules:Array.isArray(safePayload.rules)?safePayload.rules:[]},options||{forceDirty:true,changeCount:1});return true;}async saveSavedConfigurations(payload,options){const safePayload=payload&&typeof payload==='object'?payload:{};await this.writeStateFile('saved_configurations.json',{version:1,savedAt:new Date().toISOString(),activeName:safePayload.activeName||'',configurations:Array.isArray(safePayload.configurations)?safePayload.configurations:[]},options);}async saveNamedConfiguration(name,statePayload){const safeName=String(name||'').trim();if(!safeName)throw new Error('El nombre de la configuración está vacío.');const payload=await this.readSavedConfigurations()||{configurations:[]};const configurations=Array.isArray(payload.configurations)?payload.configurations.slice():[];const index=configurations.findIndex(item=>item&&item.name===safeName);const entry={name:safeName,savedAt:new Date().toISOString(),viewState:statePayload&&statePayload.viewState?statePayload.viewState:null,massState:statePayload&&statePayload.massState?statePayload.massState:null};if(index>=0)configurations[index]=entry;else configurations.push(entry);configurations.sort((a,b)=>String(a.name||'').localeCompare(String(b.name||''),undefined,{sensitivity:'base'}));const nextPayload={activeName:safeName,configurations};await this.saveSavedConfigurations(nextPayload,{forceDirty:true,changeCount:1});return Object.assign({version:1,savedAt:new Date().toISOString()},nextPayload);}async setActiveSavedConfigurationName(){}async readWorksState(){return await this.readStateFile('works_state.json');}async saveWorksState(payload){const source=payload&&payload.state?payload.state:(payload||{});return await this.writeTransientStateFile('works_state.json',{version:1,savedAt:new Date().toISOString(),state:source});}async readWorksSavedConfigurations(){return await this.readStateFile('works_saved_configurations.json');}async saveWorksSavedConfigurations(payload,options){const safePayload=payload&&typeof payload==='object'?payload:{};await this.writeStateFile('works_saved_configurations.json',{version:1,savedAt:new Date().toISOString(),activeName:safePayload.activeName||'',configurations:Array.isArray(safePayload.configurations)?safePayload.configurations:[]},options);}async saveNamedWorksConfiguration(name,statePayload){const safeName=String(name||'').trim();if(!safeName)throw new Error('El nombre de la configuración de Obras está vacío.');const payload=await this.readWorksSavedConfigurations()||{configurations:[]};const configurations=Array.isArray(payload.configurations)?payload.configurations.slice():[];const index=configurations.findIndex(item=>item&&item.name===safeName);const entry={name:safeName,savedAt:new Date().toISOString(),worksState:statePayload&&typeof statePayload==='object'?statePayload:null};if(index>=0)configurations[index]=entry;else configurations.push(entry);configurations.sort((a,b)=>String(a.name||'').localeCompare(String(b.name||''),undefined,{sensitivity:'base'}));const nextPayload={activeName:safeName,configurations};await this.saveWorksSavedConfigurations(nextPayload,{forceDirty:true,changeCount:1});return Object.assign({version:1,savedAt:new Date().toISOString()},nextPayload);}async setActiveWorksSavedConfigurationName(){}async readParameterOverrides(){return await this.readStateFile('parameter_overrides.json');}async saveParameterOverrides(payload){const safePayload=payload&&typeof payload==='object'?payload:{};await this.writeStateFile('parameter_overrides.json',{version:1,savedAt:new Date().toISOString(),modelFingerprint:String(safePayload.modelFingerprint||safePayload.parameterOverridesModelFingerprint||''),items:Array.isArray(safePayload.items)?safePayload.items:(Array.isArray(safePayload.parameterOverrides)?safePayload.parameterOverrides:[])});}async readInterferencesState(){const name='interferences_cache.json';const projectId=this.projectId||this.project.projectId||'';const storageIdentity=this.storageIdentity||projectId;const indexedEntry=await readStateFallbackEntry(storageIdentity,name);const backupEntry=readInterferencesLocalBackup(storageIdentity);let localEntry=indexedEntry;if(backupEntry&&backupEntry.dirty&&!backupEntry.invalidated){const backupRevision=Math.max(0,Number(backupEntry.revisionCount)||0);const indexedRevision=Math.max(0,Number(indexedEntry&&indexedEntry.revisionCount)||0);if(!indexedEntry||!indexedEntry.dirty||backupRevision>indexedRevision||String(backupEntry.updatedAt||'')>String(indexedEntry.updatedAt||'')){localEntry=backupEntry;}}let cloudPayload=null;try{cloudPayload=await this.readUploadedCacheJson(name);}catch(error){console.warn('No se pudo comprobar la copia cloud de Interferencias.',error);}if(localEntry&&localEntry.dirty&&!localEntry.invalidated&&localEntry.payload!=null){let resolvedPayload=localEntry.payload;let diagnostics=localEntry.diagnostics||null;if(cloudPayload!=null){resolvedPayload=reconcileInterferencePayload(cloudPayload,localEntry.payload,Object.prototype.hasOwnProperty.call(localEntry,'baselinePayload')?localEntry.baselinePayload:null);const cloudEvaluations=getInterferenceEvaluationCount(cloudPayload);const localEvaluations=getInterferenceEvaluationCount(localEntry.payload);const resolvedEvaluations=getInterferenceEvaluationCount(resolvedPayload);const reconciled=!statePayloadEquals(resolvedPayload,localEntry.payload)||!statePayloadEquals(localEntry.baselinePayload,cloudPayload);diagnostics=reconciled?{reconciledAt:new Date().toISOString(),reason:localEntry.baselinePayload==null?'legacy-local-shadow':'cloud-baseline-updated',cloudEvaluationCount:cloudEvaluations,localEvaluationCount:localEvaluations,resolvedEvaluationCount:resolvedEvaluations}:diagnostics;await writeStateFallback(storageIdentity,name,resolvedPayload,{dirty:true,source:reconciled?'localCloudReconciled':(localEntry.source||'local'),revisionCount:Math.max(1,Number(localEntry.revisionCount)||1),baselinePayload:cloudPayload,changeLog:localEntry.changeLog||[],diagnostics});markCloudCacheDirty(this,name);}else if(localEntry===backupEntry&&(!indexedEntry||!indexedEntry.dirty)){try{await writeStateFallback(storageIdentity,name,resolvedPayload,{dirty:true,source:'localBackupRecovery',revisionCount:Math.max(1,Number(localEntry.revisionCount)||1),baselinePayload:Object.prototype.hasOwnProperty.call(localEntry,'baselinePayload')?localEntry.baselinePayload:null,changeLog:localEntry.changeLog||[],diagnostics});markCloudCacheDirty(this,name);}catch(error){console.warn('La caché de Interferencias se restauró desde el respaldo local, pero IndexedDB no pudo actualizarse.',error);}}return resolvedPayload;}if(cloudPayload!=null){const entry=await writeStateFallback(storageIdentity,name,cloudPayload,{dirty:false,source:'gcs',revisionCount:0,baselinePayload:cloudPayload,changeLog:[]});writeInterferencesLocalBackup(storageIdentity,entry);return cloudPayload;}if(localEntry&&!localEntry.invalidated&&Object.prototype.hasOwnProperty.call(localEntry,'payload')){return localEntry.payload;}try{const response=await callApi(CONFIG.actions.readProjectState,{projectId,name});const legacyPayload=response.payload||response.state||response.data||null;if(legacyPayload){await writeStateFallback(storageIdentity,name,legacyPayload,{dirty:false,source:'legacyProjectState',revisionCount:0,baselinePayload:legacyPayload,changeLog:[]});return legacyPayload;}}catch(_){}return null;}saveInterferencesEmergencyBackup(payload,options){const source=payload&&payload.state?payload.state:(payload||{});const wrappedPayload={version:1,savedAt:new Date().toISOString(),state:source};const projectId=this.projectId||this.project.projectId||'';const storageIdentity=this.storageIdentity||projectId;const previous=readInterferencesLocalBackup(storageIdentity);const changeCount=Math.max(1,Number(options&&options.changeCount)||1);const revisionCount=(previous&&previous.dirty?Math.max(0,Number(previous.revisionCount)||0):0)+changeCount;const baselinePayload=previous&&previous.dirty?previous.baselinePayload:(previous&&Object.prototype.hasOwnProperty.call(previous,'payload')?previous.payload:null);const changeLog=cloudChangesApi.buildLog(previous&&previous.payload,wrappedPayload,'interferences_cache.json',previous&&previous.changeLog,changeCount);const written=writeInterferencesLocalBackup(storageIdentity,{payload:wrappedPayload,dirty:true,source:'emergencyBackup',revisionCount,baselinePayload,changeLog,diagnostics:previous&&previous.diagnostics});if(written)markCloudCacheDirty(this,'interferences_cache.json');return written;}async saveInterferencesState(payload,options){const source=payload&&payload.state?payload.state:(payload||{});const wrappedPayload={version:1,savedAt:new Date().toISOString(),state:source};if(!(options&&options.emergencyBackupDone))this.saveInterferencesEmergencyBackup(wrappedPayload,options);try{return await this.writeStateFile('interferences_cache.json',wrappedPayload,options);}catch(error){error.interferencesBackupPreserved=true;throw error;}}async readPlans2DState(){const current=await this.readStateFile('plans_2d_cache.json');if(current)return current;const folderStructure=await this.readStateFile('plans_folder_cache.json')||await this.readStateFile('plans_folder_structure.json');const annotations=await this.readStateFile('plans_annotations.json')||await this.readStateFile('plan_annotations.json')||await this.readStateFile('annotations_2d.json');if(!folderStructure&&!annotations)return null;return{version:1,state:{folderStructure:folderStructure||{version:1,groups:{}},annotations:annotations||{version:1,plans:{}}}};}async savePlans2DState(payload,options){const source=payload&&payload.state?payload.state:(payload||{});return await this.writeStateFile('plans_2d_cache.json',{version:1,savedAt:new Date().toISOString(),state:{folderStructure:source.folderStructure&&typeof source.folderStructure==='object'?source.folderStructure:{version:1,groups:{}},annotations:source.annotations&&typeof source.annotations==='object'?source.annotations:{version:1,plans:{}}}},options);}findIssueBundleCloudFile(bundlePath){const fullPath=normalizeIssueBundleCloudPath(bundlePath).toLowerCase();return normalizeFileList(this.project.files||[]).find(file=>{const candidate=normalizePath(file.relativePath||file.name||'').toLowerCase();return candidate===fullPath||candidate.endsWith('/'+fullPath);})||null;}async readIssueBundleFile(bundlePath,options){const fullPath=normalizeIssueBundleCloudPath(bundlePath);const local=await readIssueBundleStoreEntry(this.storageIdentity,fullPath);const file=this.findIssueBundleCloudFile(fullPath);const remoteVersion=file?String(file.generation||file.sha256||file.etag||file.updatedAt||('size-'+(Number(file.sizeBytes)||0))):'';if(!(options&&options.forceRemote)&&local){if(local.deleted)return null;if(local.dirty&&local.blob)return local.blob;if(local.blob&&(!file||!remoteVersion||String(local.remoteVersion||'')===remoteVersion))return local.blob;}if(!file||!file.downloadUrl)return local&&local.blob?local.blob:null;const blob=await downloadBlob(file.downloadUrl,null,0,file);await writeIssueBundleStoreEntry(this.storageIdentity,fullPath,{blob,contentType:file.contentType||blob.type||inferProjectContentType(file),sizeBytes:blob.size,signature:String(file.sha256||file.generation||('remote-'+blob.size)),remoteVersion,dirty:false,deleted:false,source:'gcs',syncedAt:new Date().toISOString()});return blob;}async readIssueBundleJson(bundlePath,options){const blob=await this.readIssueBundleFile(bundlePath,options);if(!blob)return null;try{const text=await blob.text();return text?JSON.parse(text):null;}catch(error){console.warn('No se pudo leer el JSON de incidencias:',bundlePath,error);return null;}}async listIssueBundlePaths(bundlePrefix){const prefix=normalizeIssueBundleCloudPath(bundlePrefix||'Incidencias').toLowerCase();const paths=new Set();const deleted=new Set();for(const file of normalizeFileList(this.project.files||[])){const path=normalizePath(file.relativePath||file.name||'');const lower=path.toLowerCase();const marker=lower.indexOf(prefix);if(marker>=0&&(marker===0||lower.charAt(marker-1)==='/'))paths.add(path.slice(marker));}for(const entry of await listIssueBundleStoreEntries(this.storageIdentity)){const path=normalizePath(entry.relativePath||'');if(path.toLowerCase().indexOf(prefix)!==0)continue;if(entry.deleted)deleted.add(path.toLowerCase());else paths.add(path);}const cachePrefix=CACHE_FOLDER_NAME+'/';return Array.from(paths).filter(path=>!deleted.has(path.toLowerCase())).map(path=>path.toLowerCase().indexOf(cachePrefix.toLowerCase())===0?path.slice(cachePrefix.length):path).sort();}async readIssuesBundle(options){const codec=window.BimIssueBundleService;if(!codec)return null;return await codec.readBundle({readJson:path=>this.readIssueBundleJson(path,options),listPaths:prefix=>this.listIssueBundlePaths(prefix)});}async stageIssueBundleFile(bundlePath,descriptor){const fullPath=normalizeIssueBundleCloudPath(bundlePath);if(!descriptor||!descriptor.blob)return false;const signature=await getIssueBundleDescriptorSignature(descriptor);const current=await readIssueBundleStoreEntry(this.storageIdentity,fullPath);if(current&&!current.deleted&&current.signature===signature&&current.blob&&current.blob.size===descriptor.blob.size)return false;if(!current){const remote=this.findIssueBundleCloudFile(fullPath);if(remote&&(descriptor.kind==='image'||descriptor.kind==='thumbnail')&&Number(remote.sizeBytes||0)===descriptor.blob.size){return false;}if(remote&&descriptor.contentType==='application/json'){const remoteBlob=await this.readIssueBundleFile(fullPath);if(remoteBlob){const remoteSignature=hashIssueBundleText(await remoteBlob.text());if(remoteSignature===signature)return false;}}}await writeIssueBundleStoreEntry(this.storageIdentity,fullPath,{blob:descriptor.blob,contentType:descriptor.contentType||descriptor.blob.type||'application/octet-stream',sizeBytes:descriptor.blob.size,signature,fingerprint:String(descriptor.fingerprint||''),kind:String(descriptor.kind||'file'),issueId:String(descriptor.issueId||''),attachmentId:String(descriptor.attachmentId||''),dirty:true,deleted:false,source:'local'});return true;}async stageIssueBundleDeletion(fullPath){const path=normalizeIssueBundleCloudPath(fullPath);await writeIssueBundleStoreEntry(this.storageIdentity,path,{blob:null,contentType:'',sizeBytes:0,signature:'',dirty:true,deleted:true,source:'local-delete'});}async saveIssuesBundle(payload,attachmentService){const codec=window.BimIssueBundleService;if(!codec)return false;const source=payload&&payload.state?payload.state:(payload||{});const previousIndex=await this.readIssueBundleJson(codec.INDEX_FILE)||{issues:[],deletedIssueIds:[]};const previousTeam=await this.readIssueBundleJson(codec.TEAM_FILE)||{team:[],deletedTeamIds:[]};const previousLabels=await this.readIssueBundleJson(codec.LABELS_FILE)||{labels:[],deletedLabelIds:[]};const currentIds=new Set((Array.isArray(source.issues)?source.issues:[]).map(item=>String(item&&item.id||'')).filter(Boolean));const currentTeamIds=new Set((Array.isArray(source.team)?source.team:[]).map(item=>String(item&&item.id||'')).filter(Boolean));const currentLabelIds=new Set((Array.isArray(source.labels)?source.labels:[]).map(item=>String(item&&item.id||'')).filter(Boolean));const deleted=new Set((previousIndex.deletedIssueIds||[]).map(String));const deletedTeamIds=new Set((previousTeam.deletedTeamIds||[]).map(String));const deletedLabelIds=new Set((previousLabels.deletedLabelIds||[]).map(String));const deletedFolders=[];for(const item of previousIndex.issues||[]){const id=String(item&&item.id||'');if(id&&!currentIds.has(id)){deleted.add(id);if(item.folder)deletedFolders.push(String(item.folder));}}for(const item of previousTeam.team||[]){const id=String(item&&item.id||'');if(id&&!currentTeamIds.has(id))deletedTeamIds.add(id);}for(const item of previousLabels.labels||[]){const id=String(item&&item.id||'');if(id&&!currentLabelIds.has(id))deletedLabelIds.add(id);}const bundle=await codec.buildFiles(source,attachmentService,{deletedIssueIds:Array.from(deleted),deletedTeamIds:Array.from(deletedTeamIds),deletedLabelIds:Array.from(deletedLabelIds)});const desired=new Set(Array.from(bundle.files.keys()).map(path=>normalizeIssueBundleCloudPath(path).toLowerCase()));let changed=0;for(const[path,descriptor]of bundle.files.entries()){if(!descriptor||!descriptor.blob)continue;if(await this.stageIssueBundleFile(path,descriptor))changed++;}const localEntries=await listIssueBundleStoreEntries(this.storageIdentity);for(const entry of localEntries){const path=normalizePath(entry.relativePath||'');if(!path||entry.deleted||desired.has(path.toLowerCase()))continue;if(path.toLowerCase().indexOf(ISSUES_BUNDLE_FOLDER.toLowerCase()+'/')!==0)continue;await this.stageIssueBundleDeletion(path);changed++;}for(const folder of deletedFolders){const prefix=normalizeIssueBundleCloudPath(folder).toLowerCase()+'/';for(const file of normalizeFileList(this.project.files||[])){const path=normalizePath(file.relativePath||file.name||'');if(path.toLowerCase().indexOf(prefix)!==0)continue;await this.stageIssueBundleDeletion(path);changed++;}}const manifestChanged=await this.writeStateFile('issues_cache.json',{version:2,savedAt:new Date().toISOString(),bundlePath:codec.ROOT,indexPath:codec.INDEX_FILE,state:source},{forceDirty:changed>0,changeCount:Math.max(1,changed)});return changed>0||!!manifestChanged;}async readIssueAttachmentRecord(metadata){const source=metadata&&typeof metadata==='object'?metadata:{};const blob=await this.readIssueBundleFile(source.filePath);if(!blob)return null;const thumbnailBlob=await this.readIssueBundleFile(source.thumbnailPath)||blob;return{id:String(source.id||''),issueId:String(source.issueId||''),blob,thumbnailBlob,name:String(source.name||lastPathPart(source.filePath)||'evidencia.webp'),kind:String(source.kind||'image'),mimeType:String(source.mimeType||blob.type||'image/webp'),width:Math.max(0,Number(source.width)||0),height:Math.max(0,Number(source.height)||0),size:blob.size,createdAt:String(source.createdAt||new Date().toISOString()),fingerprint:String(source.fingerprint||'')};}async listDirtyIssueBundleEntries(){return(await listIssueBundleStoreEntries(this.storageIdentity)).filter(entry=>entry&&entry.dirty);}async mergeRemoteIssueBundleCatalog(bundlePath,arrayKey){const fullPath=normalizeIssueBundleCloudPath(bundlePath);const localEntry=await readIssueBundleStoreEntry(this.storageIdentity,fullPath);if(!localEntry||!localEntry.dirty||localEntry.deleted||!localEntry.blob)return;const remoteFile=this.findIssueBundleCloudFile(fullPath);if(!remoteFile||!remoteFile.downloadUrl)return;let localPayload=null;let remotePayload=null;try{localPayload=JSON.parse(await localEntry.blob.text());}catch(_){return;}try{const remoteBlob=await downloadBlob(remoteFile.downloadUrl,null,0,remoteFile);const text=await remoteBlob.text();remotePayload=text?JSON.parse(text):null;}catch(_){return;}const deletedKey=arrayKey==='team'?'deletedTeamIds':'deletedLabelIds';const deleted=new Set([].concat(remotePayload&&remotePayload[deletedKey]||[],localPayload&&localPayload[deletedKey]||[]).map(String));const remoteItems=remotePayload&&Array.isArray(remotePayload[arrayKey])?remotePayload[arrayKey]:[];const localItems=localPayload&&Array.isArray(localPayload[arrayKey])?localPayload[arrayKey]:[];const byKey=new Map();const getKey=item=>{const id=String(item&&item.id||'').trim();if(id)return'id:'+id;const name=String(item&&item.name||'').trim().toLowerCase();const email=String(item&&item.email||'').trim().toLowerCase();return'name:'+name+'|'+email;};const choose=item=>{if(!item)return;const id=String(item.id||'');if(id&&deleted.has(id))return;const key=getKey(item);const current=byKey.get(key);if(!current){byKey.set(key,item);return;}const currentTime=String(current.updatedAt||current.createdAt||'');const nextTime=String(item.updatedAt||item.createdAt||'');if(nextTime>=currentTime)byKey.set(key,item);};remoteItems.forEach(choose);localItems.forEach(choose);const merged=Object.assign({},remotePayload||{},localPayload||{},{schemaVersion:Math.max(Number(remotePayload&&remotePayload.schemaVersion)||0,Number(localPayload&&localPayload.schemaVersion)||0,2),savedAt:new Date().toISOString(),[arrayKey]:Array.from(byKey.values()),[deletedKey]:Array.from(deleted)});const blob=new Blob([JSON.stringify(merged,null,2)],{type:'application/json'});await writeIssueBundleStoreEntry(this.storageIdentity,fullPath,Object.assign({},localEntry,{blob,sizeBytes:blob.size,signature:hashIssueBundleText(await blob.text()),dirty:true,deleted:false}));}async mergeRemoteIssueBundleIndex(){const codec=window.BimIssueBundleService;if(!codec)return;const fullPath=normalizeIssueBundleCloudPath(codec.INDEX_FILE);const localEntry=await readIssueBundleStoreEntry(this.storageIdentity,fullPath);if(!localEntry||!localEntry.dirty||localEntry.deleted||!localEntry.blob)return;let localIndex=null;try{localIndex=JSON.parse(await localEntry.blob.text());}catch(_){return;}const remoteFile=this.findIssueBundleCloudFile(fullPath);if(!remoteFile||!remoteFile.downloadUrl)return;let remoteIndex=null;try{const remoteBlob=await downloadBlob(remoteFile.downloadUrl,null,0,remoteFile);const text=await remoteBlob.text();remoteIndex=text?JSON.parse(text):null;}catch(_){return;}const merged=codec.mergeIssueIndexes(remoteIndex,localIndex);const blob=new Blob([JSON.stringify(merged,null,2)],{type:'application/json'});await writeIssueBundleStoreEntry(this.storageIdentity,fullPath,Object.assign({},localEntry,{blob,sizeBytes:blob.size,signature:hashIssueBundleText(await blob.text()),dirty:true,deleted:false}));}async mergeRemoteDirtyIssueEntities(){const entries=await this.listDirtyIssueBundleEntries();for(const entry of entries){if(!entry||entry.deleted||!entry.blob||!['issue','comment','history'].includes(String(entry.kind||'')))continue;const remoteFile=this.findIssueBundleCloudFile(entry.relativePath);if(!remoteFile||!remoteFile.downloadUrl)continue;let localPayload=null;let remotePayload=null;try{localPayload=JSON.parse(await entry.blob.text());}catch(_){continue;}try{const remoteBlob=await downloadBlob(remoteFile.downloadUrl,null,0,remoteFile);const text=await remoteBlob.text();remotePayload=text?JSON.parse(text):null;}catch(_){continue;}if(!remotePayload||!localPayload)continue;let selected=localPayload;if(entry.kind==='issue'){const localRevision=Math.max(0,Number(localPayload.revision)||0);const remoteRevision=Math.max(0,Number(remotePayload.revision)||0);if(remoteRevision>localRevision)selected=remotePayload;else if(remoteRevision===localRevision&&String(remotePayload.updatedAt||'')>String(localPayload.updatedAt||''))selected=remotePayload;selected=Object.assign({},selected,{updatedAt:String(localPayload.updatedAt||'')>String(remotePayload.updatedAt||'')?localPayload.updatedAt:remotePayload.updatedAt});}else if(entry.kind==='comment'){const localTime=String(localPayload.updatedAt||localPayload.createdAt||'');const remoteTime=String(remotePayload.updatedAt||remotePayload.createdAt||'');if(remoteTime>localTime)selected=remotePayload;}else{const localTime=String(localPayload.createdAt||'');const remoteTime=String(remotePayload.createdAt||'');if(remoteTime>localTime)selected=remotePayload;}const blob=new Blob([JSON.stringify(selected,null,2)],{type:'application/json'});const signature=hashIssueBundleText(await blob.text());if(signature===entry.signature)continue;await writeIssueBundleStoreEntry(this.storageIdentity,entry.relativePath,Object.assign({},entry,{blob,sizeBytes:blob.size,signature,dirty:true,deleted:false}));}}async applyIssueBundleTombstones(){const codec=window.BimIssueBundleService;if(!codec)return;const indexEntry=await readIssueBundleStoreEntry(this.storageIdentity,normalizeIssueBundleCloudPath(codec.INDEX_FILE));if(!indexEntry||!indexEntry.blob||indexEntry.deleted)return;let index=null;try{index=JSON.parse(await indexEntry.blob.text());}catch(_){return;}const deleted=new Set((index&&index.deletedIssueIds||[]).map(String));if(!deleted.size)return;for(const entry of await this.listDirtyIssueBundleEntries()){if(!entry||entry.deleted||!entry.issueId||!deleted.has(String(entry.issueId)))continue;await this.stageIssueBundleDeletion(entry.relativePath);}}async syncDirtyIssueBundleToCloud(onProgress){const codec=window.BimIssueBundleService;if(codec){await this.mergeRemoteIssueBundleCatalog(codec.TEAM_FILE,'team');await this.mergeRemoteIssueBundleCatalog(codec.LABELS_FILE,'labels');}await this.mergeRemoteIssueBundleIndex();await this.applyIssueBundleTombstones();await this.mergeRemoteDirtyIssueEntities();const entries=await this.listDirtyIssueBundleEntries();if(!entries.length)return{uploaded:0,deleted:0};return await uploadIssueBundleEntriesToCloud(this,entries,onProgress);}async clearIssueBundleLocalCache(includeDirty){const entries=await listIssueBundleStoreEntries(this.storageIdentity);for(const entry of entries){if(!includeDirty&&entry.dirty)continue;await deleteIssueBundleStoreEntry(this.storageIdentity,entry.relativePath);}}async readIssuesState(){const bundle=await this.readIssuesBundle();if(bundle)return{version:2,bundlePath:'Incidencias',state:bundle};return await this.readStateFile('issues_cache.json');}async saveIssuesState(payload,options){const source=payload&&payload.state?payload.state:(payload||{});return await this.writeStateFile('issues_cache.json',{version:1,savedAt:new Date().toISOString(),state:source},options);}async readViewState(){return null;}async saveViewState(){}async readStateFile(name){name=normalizeStateFileNameForCloud(name);if(isDeprecatedProjectCacheName(name))return null;const projectId=this.projectId||this.project.projectId||'';const localEntry=await readStateFallbackEntry(this.storageIdentity||projectId,name);if(localEntry&&localEntry.invalidated)return null;if(localEntry&&localEntry.dirty)return localEntry.payload;const uploadedCachePayload=await this.readUploadedCacheJson(name);if(uploadedCachePayload){await writeStateFallback(this.storageIdentity||projectId,name,uploadedCachePayload,{dirty:false,source:'gcs',revisionCount:0,baselinePayload:uploadedCachePayload});return uploadedCachePayload;}if(localEntry&&localEntry.hasOwnProperty('payload'))return localEntry.payload;try{const response=await callApi(CONFIG.actions.readProjectState,{projectId,name});const payload=response.payload||response.state||response.data||null;if(payload){await writeStateFallback(this.storageIdentity||projectId,name,payload,{dirty:false,source:'legacyProjectState',revisionCount:0,baselinePayload:payload});return payload;}}catch(_){}return null;}async readUploadedCacheJson(name){if(isDeprecatedProjectCacheName(name))return null;const file=this.findCacheJsonFile(name);if(!file||!file.downloadUrl)return null;try{const blob=await downloadBlob(file.downloadUrl,null,0,file);const text=await blob.text();return text?JSON.parse(text):null;}catch(error){console.warn('No se pudo leer cache JSON subido:',name,error);return null;}}findCacheJsonFile(name){if(isDeprecatedProjectCacheName(name))return null;const files=normalizeFileList(this.project.files||[]);const target=normalizePath(CACHE_FOLDER_NAME+'/'+normalizeStateFileNameForCloud(name)).toLowerCase();return files.find(function(item){const path=normalizePath(item.relativePath||item.name||'').toLowerCase();return path===target||path.endsWith('/'+target);})||null;}async writeTransientStateFile(name,payload){name=normalizeStateFileNameForCloud(name);if(!name||isDeprecatedProjectCacheName(name))return false;const projectId=this.projectId||this.project.projectId||'';const storageIdentity=this.storageIdentity||projectId;const current=await readStateFallbackEntry(storageIdentity,name);if(current&&statePayloadEquals(current.payload,payload)&&!current.dirty)return false;await writeStateFallback(storageIdentity,name,payload,{dirty:false,source:'localTransient',revisionCount:0,baselinePayload:current&&Object.prototype.hasOwnProperty.call(current,'baselinePayload')?current.baselinePayload:(current&&Object.prototype.hasOwnProperty.call(current,'payload')?current.payload:null),changeLog:[],diagnostics:current&&current.diagnostics});return true;}async writeStateFile(name,payload,options){name=normalizeStateFileNameForCloud(name);if(isDeprecatedProjectCacheName(name))return false;if(NON_SYNC_STATE_NAMES[name.toLowerCase()])return await this.writeTransientStateFile(name,payload);const projectId=this.projectId||this.project.projectId||'';const current=await readStateFallbackEntry(this.storageIdentity||projectId,name);const forceDirty=!!(options&&options.forceDirty);if(!forceDirty&&current&&statePayloadEquals(current.payload,payload))return false;const changeCount=Math.max(1,Number(options&&options.changeCount)||1);const revisionCount=(current&&current.dirty?Math.max(0,Number(current.revisionCount)||0):0)+changeCount;const baselinePayload=current&&current.dirty?current.baselinePayload:(current&&Object.prototype.hasOwnProperty.call(current,'payload')?current.payload:null);const changeLog=cloudChangesApi.buildLog(current&&current.payload,payload,name,current&&current.changeLog,changeCount);await writeStateFallback(this.storageIdentity||projectId,name,payload,{dirty:true,source:'local',revisionCount,baselinePayload,changeLog,diagnostics:current&&current.diagnostics});markCloudCacheDirty(this,name);return true;}async listLocalStateEntries(){return await listStateFallbackEntries(this.storageIdentity||this.projectId||this.project.projectId||'');}async listDirtyStateEntries(){const entries=await this.listLocalStateEntries();return entries.filter(entry=>{if(!entry||!entry.dirty||entry.invalidated||entry.payload==null||isDeprecatedProjectCacheName(entry.name))return false;const normalized=normalizeStateFileNameForCloud(entry.name).toLowerCase();return!NON_SYNC_STATE_NAMES[normalized];});}async syncDirtyStateToCloud(onProgress){await this.purgeDeprecatedStateFiles();const entries=await this.listDirtyStateEntries();const bundleEntries=await this.listDirtyIssueBundleEntries();if(!entries.length&&!bundleEntries.length)return{synced:0,message:'No hay cambios locales pendientes por sincronizar.'};const estimatedTotal=entries.length+bundleEntries.length;let offset=0;const bundleResult=await this.syncDirtyIssueBundleToCloud(progress=>{if(onProgress)onProgress({current:offset+(Number(progress.current)||0),total:estimatedTotal,name:progress.name});});offset+=(bundleResult.uploaded||0)+(bundleResult.deleted||0);if(entries.length){await uploadStateEntriesToCloud(this,entries,progress=>{if(onProgress)onProgress({current:offset+(Number(progress.current)||0),total:estimatedTotal,name:progress.name});});for(const entry of entries){await writeStateFallback(this.storageIdentity||this.projectId,entry.name,entry.payload,{dirty:false,source:'gcs',syncedAt:new Date().toISOString(),revisionCount:0,baselinePayload:entry.payload,changeLog:[],diagnostics:null});}}refreshCloudCacheToolbarState(this);const synced=entries.length+(bundleResult.uploaded||0)+(bundleResult.deleted||0);return{synced,message:'Incidencias, comentarios e imágenes sincronizados correctamente.'};}async refreshSharedStateFromCloud(onProgress){const projectId=this.projectId||this.project.projectId||'';if(!projectId)throw new Error('No se encontró el proyecto activo.');const response=await callApi(CONFIG.actions.openProject,{projectId});const serverProject=normalizeProject(response.project||(response.data&&response.data.project)||this.project);const files=normalizeFileList(response.files||(response.data&&response.data.files)||(serverProject&&serverProject.files)||[]);if(serverProject)serverProject.files=files;this.project=serverProject||Object.assign({},this.project,{files});this.rootName=this.project.name||this.project.projectId||this.rootName;state.activeCloudProject=this.project;updateProjectInState(this.project);await this.purgeDeprecatedStateFiles();const cacheFiles=files.filter(file=>isTopLevelCacheStateJsonPath(file.relativePath||file.name));let count=0;for(const file of cacheFiles){const name=lastPathPart(file.relativePath||file.name);if(!name||isDeprecatedProjectCacheName(name))continue;if(onProgress)onProgress({current:count,total:cacheFiles.length,name});const blob=await downloadBlob(file.downloadUrl,null,0,file);const text=await blob.text();const payload=text?JSON.parse(text):null;await writeStateFallback(this.storageIdentity||projectId,name,payload,{dirty:false,source:'gcs',syncedAt:new Date().toISOString(),revisionCount:0,baselinePayload:payload,changeLog:[],diagnostics:null});applySpecialStateToBrowser(name,payload);count++;}await this.clearIssueBundleLocalCache(true);const issuesBundle=await this.readIssuesBundle({forceRemote:true});if(issuesBundle&&window.BimIssuesPanel&&window.BimIssuesPanel.applyPersistedState){window.BimIssuesPanel.applyPersistedState(issuesBundle);count++;}refreshCloudCacheToolbarState(this);return{refreshed:count,message:count?'Caché e incidencias actualizados desde nube.':'El proyecto no tiene caché compartido en la nube.'};}async discardDirtyStateChanges(onProgress){const entries=await this.listDirtyStateEntries();const issueBundleEntries=await this.listDirtyIssueBundleEntries();if(!entries.length&&!issueBundleEntries.length)return{discarded:0,changes:0,message:'No hay cambios locales pendientes.'};const projectId=this.projectId||this.project.projectId||'';const response=await callApi(CONFIG.actions.openProject,{projectId});const serverProject=normalizeProject(response.project||(response.data&&response.data.project)||this.project);const files=normalizeFileList(response.files||(response.data&&response.data.files)||(serverProject&&serverProject.files)||[]);if(serverProject)serverProject.files=files;this.project=serverProject||Object.assign({},this.project,{files});state.activeCloudProject=this.project;updateProjectInState(this.project);let completed=0;let changes=0;for(const entry of entries){changes+=Math.max(1,Number(entry.revisionCount)||1);let payload=Object.prototype.hasOwnProperty.call(entry,'baselinePayload')?entry.baselinePayload:null;const file=this.findCacheJsonFile(entry.name);if(file&&file.downloadUrl){const blob=await downloadBlob(file.downloadUrl,null,0,file);const text=await blob.text();payload=text?JSON.parse(text):null;}await writeStateFallback(this.storageIdentity||projectId,entry.name,payload,{dirty:false,source:file?'gcs':'discarded',syncedAt:file?new Date().toISOString():'',revisionCount:0,baselinePayload:payload,changeLog:[],diagnostics:null});applySpecialStateToBrowser(entry.name,payload);completed++;if(onProgress)onProgress({current:completed,total:entries.length,name:entry.name});}if(issueBundleEntries.length){changes+=issueBundleEntries.length;await this.clearIssueBundleLocalCache(true);const issuesBundle=await this.readIssuesBundle({forceRemote:true});if(issuesBundle&&window.BimIssuesPanel&&window.BimIssuesPanel.applyPersistedState){window.BimIssuesPanel.applyPersistedState(issuesBundle);}}refreshCloudCacheToolbarState(this);return{discarded:entries.length+issueBundleEntries.length,changes,message:'Cambios locales descartados correctamente.'};}async discardDirtyStateFile(name){name=normalizeStateFileNameForCloud(name);const entry=await readStateFallbackEntry(this.storageIdentity||this.projectId,name);if(!entry||!entry.dirty)return{discarded:0,changes:0};let payload=Object.prototype.hasOwnProperty.call(entry,'baselinePayload')?entry.baselinePayload:null;const cloudPayload=await this.readUploadedCacheJson(name);if(cloudPayload!=null)payload=cloudPayload;await writeStateFallback(this.storageIdentity||this.projectId,name,payload,{dirty:false,source:cloudPayload!=null?'gcs':'discarded',syncedAt:cloudPayload!=null?new Date().toISOString():'',revisionCount:0,baselinePayload:payload,changeLog:[],diagnostics:null});applySpecialStateToBrowser(name,payload);if(name==='issues_cache.json'){await this.clearIssueBundleLocalCache(true);const issuesBundle=await this.readIssuesBundle({forceRemote:true});if(issuesBundle&&window.BimIssuesPanel&&window.BimIssuesPanel.applyPersistedState)window.BimIssuesPanel.applyPersistedState(issuesBundle);}refreshCloudCacheToolbarState(this);return{discarded:1,changes:Math.max(1,Number(entry.revisionCount)||1),name};}async discardDirtyStateChange(name,change){name=normalizeStateFileNameForCloud(name);const entry=await readStateFallbackEntry(this.storageIdentity||this.projectId,name);if(!entry||!entry.dirty||!change)return{discarded:0,changes:0};const nextPayload=cloudChangesApi.revertChange(entry.payload,change);let baselinePayload=Object.prototype.hasOwnProperty.call(entry,'baselinePayload')?entry.baselinePayload:null;if(baselinePayload==null){const cloudPayload=await this.readUploadedCacheJson(name);if(cloudPayload!=null)baselinePayload=cloudPayload;}const stillDirty=!cloudChangesApi.equals(nextPayload,baselinePayload);const remainingChanges=stillDirty?cloudChangesApi.diffPayloads(baselinePayload,nextPayload,{fileName:name,maxChanges:500}):[];const nextLog=Array.isArray(entry.changeLog)?entry.changeLog.filter(log=>!(log&&change.id&&log.id===change.id)).slice(-250):[];await writeStateFallback(this.storageIdentity||this.projectId,name,nextPayload,{dirty:stillDirty,source:stillDirty?'localSelectiveDiscard':'discarded',syncedAt:stillDirty?'':new Date().toISOString(),revisionCount:stillDirty?Math.max(1,Math.min(Math.max(1,Number(entry.revisionCount)||1)-1,remainingChanges.length||1)):0,baselinePayload,changeLog:stillDirty?nextLog:[],diagnostics:stillDirty?entry.diagnostics:null});applySpecialStateToBrowser(name,nextPayload);refreshCloudCacheToolbarState(this);return{discarded:1,changes:1,name,remaining:remainingChanges.length};}async purgeDeprecatedStateFiles(){const projectId=this.projectId||this.project.projectId||'';if(!projectId)return{removed:0};let removed=0;for(const name of Object.keys(DEPRECATED_PROJECT_CACHE_NAMES)){await markStateFallbackInvalidated(this.storageIdentity||projectId,name);removed++;}try{await deleteCacheJsonFilesFromCloud(projectId,this.project,Object.keys(DEPRECATED_PROJECT_CACHE_NAMES));}catch(_){}return{removed};}async invalidateAfterBiUpdate(updatedFiles){const projectId=this.projectId||this.project.projectId||'';if(!projectId)return{invalidated:0};const names=getCacheStateNamesToInvalidateAfterBiUpdate(updatedFiles,this.project);let invalidated=0;for(const name of names){await markStateFallbackInvalidated(this.storageIdentity||projectId,name);invalidated++;}await deleteCacheJsonFilesFromCloud(projectId,this.project,names);refreshCloudCacheToolbarState(this);return{invalidated};}}async function downloadBlob(url,onProgress,maxBytes,integrityInfo){const info=integrityInfo&&typeof integrityInfo==='object'?integrityInfo:{};const safeMaxBytes=Math.max(0,Number(maxBytes)||securityPolicy.limits.maxJsonCompressedBytes);try{return await cloudTransferService.download({url:url,timeoutMs:CONFIG.transferTimeoutMs,onProgress:onProgress,maxBytes:safeMaxBytes,expectedSize:Math.max(0,Number(info.sizeBytes||info.size||0)||0),expectedSha256:String(info.sha256||info.sha256Hash||''),expectedGcsHash:String(info.gcsHash||info.googleHash||'')});}catch(error){if(error&&error.code==='HTTP_ERROR'){throw new Error('Descarga fallida desde la nube: HTTP '+error.status);}if(error&&error.code==='DOWNLOAD_SIZE_LIMIT'){throw securityPolicy.resourceError('La descarga desde la nube',safeMaxBytes,Number(error.actual)||0);}if(error&&['TRANSFER_DOMAIN_NOT_ALLOWED','TRANSFER_URL_INVALID','TRANSFER_URL_PROTOCOL','TRANSFER_URL_CREDENTIALS'].includes(error.code)){throw new Error('La descarga fue bloqueada porque la URL no pertenece a un dominio autorizado. Detalle: '+(error.message||error.code));}if(error&&['DOWNLOAD_SIZE_MISMATCH','DOWNLOAD_INCOMPLETE','DOWNLOAD_HASH_MISMATCH','DOWNLOAD_GCS_HASH_MISMATCH'].includes(error.code)){throw new Error('La descarga fue descartada porque el archivo está incompleto o no coincide con la versión registrada. Detalle: '+(error.message||error.code));}throw new Error('No se pudo descargar el archivo desde la nube. Verifica tu conexión e inténtalo nuevamente. Si el problema continúa, contacta al administrador.');}}function getStableFileIdentity(fileInfo){return normalizePath(fileInfo.fileId||fileInfo.objectPath||fileInfo.storagePath||fileInfo.gcsPath||fileInfo.relativePath||fileInfo.name||'file');}function makeFileCacheKey(project,fileInfo){const projectId=String(project&&project.projectId||'project');const identity=getStableFileIdentity(fileInfo);const version=String(fileInfo.generation||fileInfo.sha256||fileInfo.version||fileInfo.updatedAt||fileInfo.sizeBytes||'v0');return[projectId,identity,version].join('|');}function makeCurrentMetaKey(project,fileInfo){const projectId=String(project&&project.projectId||'project');const identity=getStableFileIdentity(fileInfo);return[projectId,identity,'current'].join('|');}async function readStateFallback(storageIdentity,name){const entry=await readStateFallbackEntry(storageIdentity,name);return entry&&Object.prototype.hasOwnProperty.call(entry,'payload')?entry.payload:null;}function normalizeStoredStateEntry(value){if(!value)return null;if(value&&typeof value==='object'&&value.__bimCloudStateEntry===true)return value;return{__bimCloudStateEntry:true,payload:value,dirty:false,source:'legacy',updatedAt:'',revisionCount:0,baselinePayload:value,changeLog:[]};}async function readStateFallbackEntry(storageIdentity,name){const currentKey=makeStateStoreKey(storageIdentity,name);const legacyProjectId=getLegacyProjectIdFromStorageIdentity(storageIdentity);try{const db=await openCloudDb();let value=await idbGet(db,STATE_STORE,currentKey);if(!value&&legacyProjectId){value=await idbGet(db,STATE_STORE,makeStateStoreKey(legacyProjectId,name));if(value){try{await idbPut(db,STATE_STORE,currentKey,value);}catch(_){}}}db.close();return normalizeStoredStateEntry(value);}catch(_){return null;}}async function writeStateFallback(storageIdentity,name,payload,options){options=options||{};const entry={__bimCloudStateEntry:true,storageIdentity:String(storageIdentity||''),payload:payload==null?null:payload,dirty:options.dirty===true,source:options.source||(options.dirty?'local':'cache'),updatedAt:new Date().toISOString(),syncedAt:options.syncedAt||'',revisionCount:Math.max(0,Number(options.revisionCount)||0),baselinePayload:Object.prototype.hasOwnProperty.call(options,'baselinePayload')?options.baselinePayload:(payload==null?null:payload),changeLog:Array.isArray(options.changeLog)?options.changeLog.slice(-250):[],diagnostics:options.diagnostics&&typeof options.diagnostics==='object'?options.diagnostics:null,invalidated:false};if(normalizeStateFileNameForCloud(name).toLowerCase()==='interferences_cache.json'){writeInterferencesLocalBackup(storageIdentity,entry);}const db=await openCloudDb();try{await idbPut(db,STATE_STORE,makeStateStoreKey(storageIdentity,name),entry);}finally{try{db.close();}catch(_){}}return entry;}async function markStateFallbackInvalidated(storageIdentity,name){const entry={__bimCloudStateEntry:true,storageIdentity:String(storageIdentity||''),payload:null,dirty:false,source:'invalidated',updatedAt:new Date().toISOString(),syncedAt:'',revisionCount:0,baselinePayload:null,changeLog:[],diagnostics:null,invalidated:true};if(normalizeStateFileNameForCloud(name).toLowerCase()==='interferences_cache.json'){writeInterferencesLocalBackup(storageIdentity,entry);}const db=await openCloudDb();try{await idbPut(db,STATE_STORE,makeStateStoreKey(storageIdentity,name),entry);}finally{try{db.close();}catch(_){}}}async function collectStateEntriesForPrefix(db,prefix){const result=[];await new Promise(function(resolve,reject){const tx=db.transaction(STATE_STORE,'readonly');const store=tx.objectStore(STATE_STORE);const request=store.openCursor();request.onsuccess=function(){const cursor=request.result;if(!cursor)return resolve();const key=String(cursor.key||'');if(key.indexOf(prefix)===0){const name=key.slice(prefix.length);result.push(Object.assign({name},normalizeStoredStateEntry(cursor.value)));}cursor.continue();};request.onerror=()=>reject(request.error);});return result;}async function listStateFallbackEntries(storageIdentity){const currentPrefix=String(storageIdentity||'')+':';const legacyProjectId=getLegacyProjectIdFromStorageIdentity(storageIdentity);const byName=new Map();try{const db=await openCloudDb();const currentEntries=await collectStateEntriesForPrefix(db,currentPrefix);currentEntries.forEach(entry=>byName.set(entry.name,entry));if(legacyProjectId){const legacyEntries=await collectStateEntriesForPrefix(db,legacyProjectId+':');for(const entry of legacyEntries){if(byName.has(entry.name))continue;const migrated=Object.assign({},entry,{storageIdentity:String(storageIdentity),source:entry.source==='legacy'?'legacyMigrated':entry.source});byName.set(entry.name,migrated);try{await idbPut(db,STATE_STORE,makeStateStoreKey(storageIdentity,entry.name),migrated);}catch(_){}}}db.close();}catch(error){console.warn('No se pudieron listar completamente los cambios locales de IndexedDB.',error);}const result=Array.from(byName.values());const backup=readInterferencesLocalBackup(storageIdentity);if(backup&&!backup.invalidated&&backup.payload!=null&&!result.some(entry=>entry&&entry.name==='interferences_cache.json')){result.push(Object.assign({name:'interferences_cache.json'},backup));}return result;}function makeInterferencesLocalBackupKey(storageIdentity){return INTERFERENCES_LOCAL_BACKUP_PREFIX+encodeURIComponent(String(storageIdentity||'cloud|user|project'));}function readInterferencesLocalBackup(storageIdentity){let value=localStore.getJson(makeInterferencesLocalBackupKey(storageIdentity),null,{removeInvalid:true});const legacyProjectId=getLegacyProjectIdFromStorageIdentity(storageIdentity);if(!value&&legacyProjectId){value=localStore.getJson(makeInterferencesLocalBackupKey(legacyProjectId),null,{removeInvalid:true});if(value){try{localStore.setJson(makeInterferencesLocalBackupKey(storageIdentity),Object.assign({},value,{storageIdentity:String(storageIdentity)}));}catch(_){}}}if(!value||typeof value!=='object'||value.__bimInterferencesBackup!==true)return null;return value;}function writeInterferencesLocalBackup(storageIdentity,entry){if(!storageIdentity||!entry||typeof entry!=='object')return false;const existing=readInterferencesLocalBackup(storageIdentity);const incomingRevision=Math.max(0,Number(entry.revisionCount)||0);const existingRevision=Math.max(0,Number(existing&&existing.revisionCount)||0);if(existing&&existing.dirty&&entry.dirty===true&&existingRevision>incomingRevision){return true;}const backup={__bimInterferencesBackup:true,storageIdentity:String(storageIdentity),projectId:getLegacyProjectIdFromStorageIdentity(storageIdentity)||String(storageIdentity),payload:Object.prototype.hasOwnProperty.call(entry,'payload')?entry.payload:null,dirty:entry.dirty===true,source:entry.source||(entry.dirty?'local':'cache'),updatedAt:entry.updatedAt||new Date().toISOString(),syncedAt:entry.syncedAt||'',revisionCount:Math.max(0,Number(entry.revisionCount)||0),baselinePayload:Object.prototype.hasOwnProperty.call(entry,'baselinePayload')?entry.baselinePayload:null,changeLog:Array.isArray(entry.changeLog)?entry.changeLog.slice(-250):[],diagnostics:entry.diagnostics||null,invalidated:entry.invalidated===true};let serialized='';try{serialized=JSON.stringify(backup);}catch(error){console.warn('No se pudo serializar el respaldo local de Interferencias.',error);return false;}if(serialized.length>INTERFERENCES_LOCAL_BACKUP_MAX_BYTES){console.warn('El respaldo local de Interferencias supera el tamaño permitido del almacenamiento local; IndexedDB seguirá siendo el almacenamiento principal.');return false;}return localStore.setRaw(makeInterferencesLocalBackupKey(storageIdentity),serialized);}function statePayloadEquals(a,b){try{return JSON.stringify(statePayloadComparable(a))===JSON.stringify(statePayloadComparable(b));}catch(_){return false;}}function statePayloadComparable(value){if(Array.isArray(value))return value.map(statePayloadComparable);if(!value||typeof value!=='object')return value;const result={};Object.keys(value).sort().forEach(function(key){if(key==='savedAt'||key==='updatedAt')return;result[key]=statePayloadComparable(value[key]);});return result;}function makeStateStoreKey(projectId,name){return String(projectId||'')+':'+normalizeStateFileNameForCloud(name);}async function openCloudDb(){const db=await indexedDbService.open({name:DB_NAME,version:DB_VERSION,stores:[FILE_STORE,META_STORE,STATE_STORE],onBlocked:function(){console.warn('La actualización de la caché local está bloqueada por otra pestaña del visor.');}});const requiredStores=[FILE_STORE,META_STORE,STATE_STORE];const missing=requiredStores.filter(storeName=>!db.objectStoreNames.contains(storeName));if(missing.length){try{db.close();}catch(_){}const error=new Error('La base local del visor está desactualizada y no contiene: '+missing.join(', ')+'. Cierra las demás pestañas y vuelve a abrir el proyecto.');error.code='IDB_SCHEMA_INCOMPLETE';throw error;}return db;}function idbGet(db,storeName,key){return indexedDbService.get(db,storeName,key);}function idbPut(db,storeName,key,value){return indexedDbService.put(db,storeName,key,value);}function idbDelete(db,storeName,key){return indexedDbService.delete(db,storeName,key);}function confirmCloudAction(options){return cloudUiKit.confirmAction(options);}function createOverlay(className){return cloudUiKit.createOverlay(className);}function createHeader(title,subtitle,onClose){return cloudUiKit.createHeader(title,subtitle,onClose);}function closeOverlay(){return cloudUiKit.closeOverlay();}function setBusy(value){return dashboardPanel.setBusy(value);}function setDashboardMessage(message,type){return dashboardPanel.setMessage(sanitizeCloudUiMessage(message),type);}function setDashboardProgress(percent,label,indeterminate){return dashboardPanel.setProgress(percent,sanitizeCloudUiMessage(label,'Procesando...'),indeterminate);}function clearDashboardProgressSoon(delay){return dashboardPanel.clearProgressSoon(delay);}function setMessage(node,message,type){return cloudUiKit.setMessage(node,sanitizeCloudUiMessage(message),type);}function el(tag,className){return cloudUiKit.el(tag,className);}function defineReadOnlyProperty(target,name,value){try{Object.defineProperty(target,name,{value,configurable:true});}catch(_){try{target[name]=value;}catch(_){}}}function formatFolderTarget(project,folderPath){const path=normalizePath(folderPath||'');if(path)return path;return String(project&&(project.name||project.projectName||project.rootName)||'Raíz del proyecto');}function parentPath(path){return core.path.parent(path);}function normalizeFolderName(name){return core.path.normalizeFolderName(name);}function tryNormalizeCloudPath(path,kind){const raw=String(path==null?'':path);const normalized=core.path.tryNormalize(raw,'');if(!normalized&&raw.trim()){console.warn('Se ignoró una ruta cloud insegura de '+(kind||'elemento')+'.');}return normalized;}function normalizePath(path){return core.path.normalize(path);}function lastPathPart(path){return core.path.lastPart(path);}function sanitizeRootName(value){return core.path.sanitizeFileName(value,'ProyectoCloud');}function escapeHtml(value){return core.format.escapeHtml(value);}function formatBytes(bytes){return core.format.bytes(bytes);}function formatDate(value){return core.format.date(value);}function formatProjectSubtitle(project){const parts=[projectRoleLabel(project)];const files=normalizeFileList(project.files||[]);const kindCounts=countFileKinds(files);if(kindCounts.bi)parts.push(kindCounts.bi+' archivo(s) .bi');if(project.updatedAt)parts.push('Actualizado: '+formatDate(project.updatedAt));return parts.join(' · ');}function shortVersion(value){const text=String(value||'');return text.length>12?text.slice(0,6)+'…'+text.slice(-4):text;}function inferRootName(files){const first=files&&files[0];const rel=normalizePath(first&&(first.webkitRelativePath||first.__exportModelRelativePath)||'');return rel.split('/').filter(Boolean)[0]||'Nuevo proyecto';}function stripFirstPathPart(path){return core.path.stripFirstPart(path);}async function requestStoragePersistence(){if(!CONFIG.requestPersistentStorage||!navigator.storage||!navigator.storage.persist)return false;try{return await navigator.storage.persist();}catch(_){return false;}}function sleep(ms){return core.async.sleep(ms);}async function runWithLimit(items,limit,worker){return core.async.runWithLimit(items,limit,worker);}window.BimCloudAccess={showLoginModal,showDashboard,openProject:function(projectId){restoreSession();state.selectedProjectId=String(projectId||'');return showDashboard(true);},config:CONFIG,events:eventBus,getState:function(){const snapshot=appState.snapshot('cloud');snapshot.authenticated=!!sessionToken;snapshot.sessionStorage='memory-only';return snapshot;},syncActiveProjectCache:syncActiveProjectCacheToCloud,refreshActiveProjectCache:refreshActiveProjectCacheFromCloud,openPendingChanges:function(){const statusNode=state.cacheToolbarNode&&state.cacheToolbarNode.querySelector('[data-cloud-cache-status]');if(statusNode)return changesPanel.open(statusNode);},getActiveProjectIdentity:function(){return state.activeStateService&&state.activeStateService.getIdentityInfo?state.activeStateService.getIdentityInfo():null;},getPendingChanges:async function(){return state.activeStateService?state.activeStateService.listDirtyStateEntries():[];},checkLocalCache:async function(){const db=await openCloudDb();try{return{name:DB_NAME,version:Number(db.version)||DB_VERSION,stores:Array.from(db.objectStoreNames||[]),ready:db.objectStoreNames.contains(FILE_STORE)&&db.objectStoreNames.contains(META_STORE)&&db.objectStoreNames.contains(STATE_STORE)};}finally{try{db.close();}catch(_){}}},clearLocalFileCache:async function(){localStore.removePrefix(INTERFERENCES_LOCAL_BACKUP_PREFIX);localStore.removePrefix(PLANS_2D_LOCAL_BACKUP_PREFIX);return indexedDbService.deleteDatabase(DB_NAME);}};if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init);else init();})();