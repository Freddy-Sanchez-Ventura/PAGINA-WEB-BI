(function(){'use strict';function create(){const state={disposers:[],boundKeys:new Set(),generation:0};function bindOnce(key,target,eventName,handler,options){if(!target||!eventName||typeof handler!=='function')return false;const id=String(key||eventName);if(state.boundKeys.has(id))return false;state.boundKeys.add(id);target.addEventListener(eventName,handler,options);state.disposers.push(function(){try{target.removeEventListener(eventName,handler,options);}catch(_){}state.boundKeys.delete(id);});return true;}function addDisposer(disposer){if(typeof disposer==='function')state.disposers.push(disposer);}function disposeAll(){state.generation+=1;const disposers=state.disposers.splice(0);disposers.reverse().forEach(disposer=>{try{disposer();}catch(_){}});state.boundKeys.clear();}return Object.freeze({state,bindOnce,addDisposer,disposeAll});}window.BimPlansInteractionService=Object.freeze({create});})();