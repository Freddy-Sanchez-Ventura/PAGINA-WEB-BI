(function(){'use strict';const DB_NAME='bim-ingenieros-ai';const DB_VERSION=1;const STORE_NAME='catalogs';const GENERAL_FIELDS=[['General::ProjectName','Proyecto',['proyecto','project']],['General::ModelId','Modelo',['modelo','model']],['General::ElementId','ElementId',['elementid','id de elemento','elemento']],['General::UniqueId','UniqueId',['uniqueid','guid']],['General::CategoryName','Categoría',['categoria','categoría','category']],['General::SubcategoryName','Subcategoría',['subcategoria','subcategoría','subcategory']],['General::FamilyName','Familia',['familia','family']],['General::TypeName','Tipo',['tipo','type']],['General::LevelName','Nivel',['nivel','level']],['General::GeometryId','Geometría',['geometria','geometría','geometry']],['Structure::FolderPath','Ruta de carpeta',['carpeta','ruta','ruta de carpeta','folder','folder path','estructura de carpetas']],['Structure::FolderName','Nombre de carpeta',['nombre de carpeta','carpeta exacta','folder name']],['Structure::ParentFolder','Carpeta superior',['carpeta padre','carpeta superior','parent folder']],['Structure::RootFolder','Carpeta raíz',['carpeta raiz','carpeta raíz','root folder']],['Structure::SourceDocument','Archivo de origen',['archivo','archivo de origen','documento de origen','source document']],['Structure::ProjectId','Id de proyecto',['id de proyecto','project id']]];const OPERATORS=new Set(['equals','not_equals','contains','not_contains','starts_with','ends_with','greater_than','greater_or_equal','less_than','less_or_equal','is_empty','is_not_empty']);const NUMERIC_OPERATIONS=new Set(['none','sum','average','minimum','maximum','subtract','multiply','divide','percentage']);const REPORT_MODES=new Set(['detail','observations','summary']);const PLAN_SOURCES=new Set(['model','structure','interferences','issues','plans']);const PLAN_ACTIONS=new Set(['count','list','export_excel','select','select_same_type','isolate','isolate_color','hide','color','transparency','transparency_highlight','zoom_selection','fit_view','area_zoom','section_box','measurement_tool','elevation_tool','toggle_level_grid','restore_visibility','reset_view','fade_selection','clear','summarize','tree_control','set_parameter','replace_parameter']);const PLAN_SCOPES=new Set(['all','previous_result','previous_step','current_selection']);const DIRECT_VIEWER_ACTIONS=new Set(['select_same_type','fit_view','area_zoom','measurement_tool','elevation_tool','toggle_level_grid','restore_visibility','reset_view','fade_selection','clear']);const LOCAL_OPERATION_ACTIONS=new Set(['tree_control','set_parameter','replace_parameter']);const TREE_TARGET_TYPES=new Set(['project','category']);const TREE_OPERATIONS=new Set(['show','hide','show_only','show_all','hide_all','expand','collapse']);const RECORD_TYPES=new Set(['auto','evaluations','clashes']);const PERCENTAGE_MODES=new Set(['auto','share_of_base','within_group']);function normalize(value){return String(value==null?'':value).normalize('NFD').replace(/[\u0300-\u036f]/g,'').trim().toLowerCase();}function safeNumber(value){const original=String(value==null?'':value).replace(/[\u00a0\s]/g,'');const match=original.match(/[-+]?(?:\d[\d.,]*|[.,]\d+)(?:[eE][-+]?\d+)?/);if(!match)return null;let text=match[0];const comma=text.lastIndexOf(',');const dot=text.lastIndexOf('.');if(comma>=0&&dot>=0){const decimal=comma>dot?',':'.';const thousands=decimal===','?/\./g:/,/g;text=text.replace(thousands,'').replace(decimal,'.');}else if(comma>=0){text=text.replace(/,/g,'.');}const result=Number(text);return Number.isFinite(result)?result:null;}function parseMeasurement(value){const number=safeNumber(value);if(number==null)return null;const original=String(value==null?'':value);const text=normalize(original).replace(/[²]/g,'2').replace(/[³]/g,'3').replace(/\^\s*([23])/g,'$1');const rules=[{pattern:/(?:mm\s*3|milimetro(?:s)?\s+cubico)/,factor:1e-9,unit:'m³',dimension:'volume'},{pattern:/(?:cm\s*3|centimetro(?:s)?\s+cubico)/,factor:1e-6,unit:'m³',dimension:'volume'},{pattern:/(?:ft\s*3|pie(?:s)?\s+cubico|cubic\s+feet)/,factor:0.028316846592,unit:'m³',dimension:'volume'},{pattern:/(?:in\s*3|pulgada(?:s)?\s+cubica)/,factor:0.000016387064,unit:'m³',dimension:'volume'},{pattern:/(?:m\s*3|metro(?:s)?\s+cubico)/,factor:1,unit:'m³',dimension:'volume'},{pattern:/(?:mm\s*2|milimetro(?:s)?\s+cuadrado)/,factor:1e-6,unit:'m²',dimension:'area'},{pattern:/(?:cm\s*2|centimetro(?:s)?\s+cuadrado)/,factor:1e-4,unit:'m²',dimension:'area'},{pattern:/(?:ft\s*2|pie(?:s)?\s+cuadrado|square\s+feet)/,factor:0.09290304,unit:'m²',dimension:'area'},{pattern:/(?:in\s*2|pulgada(?:s)?\s+cuadrada)/,factor:0.00064516,unit:'m²',dimension:'area'},{pattern:/(?:m\s*2|metro(?:s)?\s+cuadrado)/,factor:1,unit:'m²',dimension:'area'},{pattern:/(?:^|[^a-z])mm(?:[^a-z]|$)|milimetro(?:s)?/,factor:0.001,unit:'m',dimension:'length'},{pattern:/(?:^|[^a-z])cm(?:[^a-z]|$)|centimetro(?:s)?/,factor:0.01,unit:'m',dimension:'length'},{pattern:/(?:^|[^a-z])(?:ft|feet|foot|pie|pies)(?:[^a-z]|$)|'/,factor:0.3048,unit:'m',dimension:'length'},{pattern:/(?:^|[^a-z])(?:in|inch|inches|pulgada|pulgadas)(?:[^a-z]|$)|"/,factor:0.0254,unit:'m',dimension:'length'},{pattern:/(?:^|[^a-z])m(?:[^a-z]|$)|metro(?:s)?/,factor:1,unit:'m',dimension:'length'},{pattern:/(?:^|[^a-z])kg(?:[^a-z]|$)|kilogramo(?:s)?/,factor:1,unit:'kg',dimension:'mass'},{pattern:/(?:^|[^a-z])g(?:[^a-z]|$)|gramo(?:s)?/,factor:0.001,unit:'kg',dimension:'mass'}];const rule=rules.find(item=>item.pattern.test(text));return rule?{value:number*rule.factor,unit:rule.unit,dimension:rule.dimension,raw:original}:{value:number,unit:'',dimension:'',raw:original};}function formatNumericValue(value){if(!Number.isFinite(value))return'';const absolute=Math.abs(value);const maximumFractionDigits=absolute>0&&absolute<0.001?9:(absolute<1?6:3);return value.toLocaleString('es-PE',{maximumFractionDigits});}function yieldBrowser(){return new Promise(resolve=>setTimeout(resolve,0));}function normalizeStructurePath(value){return String(value==null?'':value).replace(/\\/g,'/').replace(/^\/+|\/+$/g,'').replace(/\/{2,}/g,'/');}function unwrapState(payload){return payload&&payload.state?payload.state:(payload||{});}class BimAiCatalog{constructor(){this.model=null;this.store=null;this.basicStore=null;this.fingerprint='';this.summary=null;this.parametersReady=false;this.preparePromise=null;this.parameterPromise=null;this.sourceCache=new Map();this.structureRecords=[];this.projectStructureById=new Map();this.projectStructureByName=new Map();}getModel(){return window.ExportModelApp&&window.ExportModelApp.getActiveModel?window.ExportModelApp.getActiveModel():null;}findProjectDescriptor(model,project){const descriptors=model&&model.__projectDescriptorsByFolderPath instanceof Map?Array.from(new Set(model.__projectDescriptorsByFolderPath.values())):[];const projectId=String(project&&project.ProjectId||'');const folderPath=normalizeStructurePath(project&&(project.FolderPath||project.__folderPath)||'');return descriptors.find(item=>String(item&&item.ProjectId||'')===projectId)||descriptors.find(item=>normalizeStructurePath(item&&(item.FolderPath||item.__folderPath)||'')===folderPath)||null;}buildProjectStructure(model){const projects=Array.isArray(model&&model.projects)?model.projects:[];const folderPaths=new Set();const folderModels=new Map();const records=[];this.projectStructureById.clear();this.projectStructureByName.clear();for(const project of projects){if(!project)continue;const descriptor=this.findProjectDescriptor(model,project);const folderPath=normalizeStructurePath(project.FolderPath||project.__folderPath||project.Name||'');const parts=folderPath.split('/').filter(Boolean);const folderName=parts[parts.length-1]||String(project.Name||'Proyecto ExportModel');const parentFolder=parts.length>1?parts.slice(0,-1).join('/'):'';const rootFolder=parts[0]||folderName;const sourceDocument=String(project.SourceDocument||(descriptor&&descriptor.SourceDocument)||'');const fileMap=descriptor&&descriptor.__fileMap instanceof Map?descriptor.__fileMap:null;const files=fileMap?Array.from(fileMap.entries()).map(([name,file])=>({name:String(name||(file&&file.name)||''),path:normalizeStructurePath(file&&(file.webkitRelativePath||file.__exportModelRelativePath)||(folderPath?folderPath+'/'+name:name)),size:Math.max(0,Number(file&&file.size)||0)})).filter(item=>item.name):[];const metadata={projectId:String(project.ProjectId||''),model:String(project.Name||folderName),folderPath,folderName,parentFolder,rootFolder,sourceDocument,exportDate:String(project.ExportDate||''),elementCount:Math.max(0,Number(project.ElementCount)||0),geometryCount:Math.max(0,Number(project.GeometryCount)||0),loaded:project.IsLoaded!==false&&!project.DeferredProject,files};if(metadata.projectId)this.projectStructureById.set(metadata.projectId,metadata);[metadata.model,folderName,sourceDocument].filter(Boolean).forEach(value=>this.projectStructureByName.set(normalize(value),metadata));for(let depth=1;depth<=parts.length;depth++){const currentPath=parts.slice(0,depth).join('/');folderPaths.add(currentPath);if(!folderModels.has(currentPath))folderModels.set(currentPath,new Set());folderModels.get(currentPath).add(metadata.model);}records.push({Tipo:'Modelo',Ruta:folderPath,Carpeta:folderName,CarpetaPadre:parentFolder,CarpetaRaiz:rootFolder,Modelo:metadata.model,ProjectId:metadata.projectId,Archivo:sourceDocument,FechaExportacion:metadata.exportDate,Elementos:metadata.elementCount,Geometrias:metadata.geometryCount,Archivos:files.length,Estado:metadata.loaded?'Cargado':'No cargado'});files.forEach(file=>records.push({Tipo:'Archivo',Ruta:file.path,Carpeta:folderName,CarpetaPadre:folderPath,CarpetaRaiz:rootFolder,Modelo:metadata.model,ProjectId:metadata.projectId,Archivo:file.name,DocumentoOrigen:sourceDocument,TamanoBytes:file.size,Estado:metadata.loaded?'Cargado':'No cargado'}));}Array.from(folderPaths).sort((a,b)=>a.localeCompare(b)).forEach(path=>{const parts=path.split('/').filter(Boolean);const models=folderModels.get(path)||new Set();records.push({Tipo:'Carpeta',Ruta:path,Carpeta:parts[parts.length-1]||path,CarpetaPadre:parts.length>1?parts.slice(0,-1).join('/'):'',CarpetaRaiz:parts[0]||'',Nivel:parts.length,Modelos:models.size,Contenido:Array.from(models).sort().join(', ')});});this.structureRecords=records;const modelRecords=records.filter(item=>item.Tipo==='Modelo');const fileRecords=records.filter(item=>item.Tipo==='Archivo');return{rootFolders:Array.from(new Set(modelRecords.map(item=>item.CarpetaRaiz).filter(Boolean))).sort(),folderCount:folderPaths.size,modelCount:modelRecords.length,fileCount:fileRecords.length,folderPaths:Array.from(folderPaths).sort(),modelLocations:modelRecords.map(item=>({model:item.Modelo,folderPath:item.Ruta,sourceDocument:item.Archivo,loaded:item.Estado==='Cargado'}))};}getProjectStructureForElement(element){if(!element)return null;const projectId=String(element.ProjectId||'');if(projectId&&this.projectStructureById.has(projectId))return this.projectStructureById.get(projectId);for(const candidate of[element.ProjectName,element.OriginalModelId,element.ModelId]){const key=normalize(candidate);if(key&&this.projectStructureByName.has(key))return this.projectStructureByName.get(key);}return null;}getElementStructureValue(element,key){const metadata=this.getProjectStructureForElement(element);if(!metadata)return'';const field=String(key||'').replace(/^Structure::/,'');const values={FolderPath:metadata.folderPath,FolderName:metadata.folderName,ParentFolder:metadata.parentFolder,RootFolder:metadata.rootFolder,SourceDocument:metadata.sourceDocument,ProjectId:metadata.projectId};return values[field]==null?'':values[field];}createFingerprint(model){const projects=(model&&model.projects)||[];return projects.map(item=>[item.ProjectId,item.Signature,item.ElementCount,item.Name,item.FolderPath,item.SourceDocument].join(':')).sort().join('|')||'empty';}async prepare(onProgress){const model=this.getModel();if(!model)throw new Error('Primero carga un proyecto BIM.');const fingerprint=this.createFingerprint(model);if(this.model===model&&this.fingerprint===fingerprint&&this.summary)return this.summary;if(this.preparePromise)return this.preparePromise;this.preparePromise=(async()=>{if(onProgress)onProgress('Catalogando proyectos y categorías...',0,1);this.model=model;this.fingerprint=fingerprint;this.store=null;this.basicStore=null;this.parametersReady=false;this.sourceCache.clear();const structure=this.buildProjectStructure(model);const projectMap=new Map();const categoryMap=new Map();const elements=Array.isArray(model.elements)?model.elements:[];for(let i=0;i<elements.length;i++){const element=elements[i]||{};const project=String(element.ProjectName||element.ModelId||'Sin proyecto');const category=String(element.CategoryName||'Sin categoría');projectMap.set(project,(projectMap.get(project)||0)+1);categoryMap.set(category,(categoryMap.get(category)||0)+1);if(i&&i%12000===0){if(onProgress)onProgress('Catalogando información BIM...',i,elements.length);await yieldBrowser();}}this.summary={fingerprint,generatedAt:new Date().toISOString(),totalElements:elements.length,projects:Array.from(projectMap,([name,count])=>({name,count})).sort((a,b)=>a.name.localeCompare(b.name)),categories:Array.from(categoryMap,([name,count])=>({name,count})).sort((a,b)=>a.name.localeCompare(b.name)),parameterNames:[],parametersDeferred:model.__parametersLoaded!==true,sources:await this.getSourceCounts(),structure};this.writeCatalog(this.summary).catch(()=>{});if(onProgress)onProgress('Catálogo IA preparado.',1,1);return this.summary;})().finally(()=>{this.preparePromise=null;});return this.preparePromise;}async ensureParameters(onProgress){await this.prepare(onProgress);if(this.store&&this.parametersReady)return this.store;if(this.parameterPromise)return this.parameterPromise;this.parameterPromise=(async()=>{if(this.model&&typeof this.model.loadDeferredParameters==='function'&&this.model.__parametersLoaded!==true){await this.model.loadDeferredParameters(status=>{if(onProgress)onProgress(status.message||'Leyendo parámetros...',status.current||0,status.total||1);});}if(!window.BimDataStore)throw new Error('El motor de datos BIM no está disponible.');if(onProgress)onProgress('Preparando índice de parámetros...',0,1);await yieldBrowser();this.store=new window.BimDataStore(this.model);this.parametersReady=true;if(this.summary){this.summary.parameterNames=this.store.getParameters().map(item=>({key:item.key,name:item.display,source:item.source}));this.summary.parametersDeferred=false;this.writeCatalog(this.summary).catch(()=>{});}if(onProgress)onProgress('Índice de parámetros preparado.',1,1);return this.store;})().finally(()=>{this.parameterPromise=null;});return this.parameterPromise;}async ensureBasicStore(onProgress){await this.prepare(onProgress);if(this.store)return this.store;if(this.basicStore)return this.basicStore;if(!window.BimDataStore)throw new Error('El motor de datos BIM no está disponible.');if(onProgress)onProgress('Preparando índice general del proyecto...',0,1);await yieldBrowser();this.basicStore=new window.BimDataStore(this.model);if(onProgress)onProgress('Índice general preparado.',1,1);return this.basicStore;}async getSourceCounts(){const[interferences,issues,plans]=await Promise.all([this.readInterferences(),this.readIssues(),this.readPlans()]);return{structure:this.structureRecords.length,interferences:interferences.reduce((sum,item)=>sum+((item.clashes||[]).length),0),evaluations:interferences.length,issues:issues.length,plans:plans.length};}async readInterferences(){if(this.sourceCache.has('interferences'))return this.sourceCache.get('interferences');let values=[];const cache=window.ExportModelApp&&window.ExportModelApp.getActiveCacheService?window.ExportModelApp.getActiveCacheService():null;try{const payload=cache&&cache.readInterferencesState?await cache.readInterferencesState():null;const state=unwrapState(payload);values=Array.isArray(state.clashEvaluations)?state.clashEvaluations:[];}catch(_){}if(!values.length&&window.__exportModelUI&&Array.isArray(window.__exportModelUI.clashEvaluations))values=window.__exportModelUI.clashEvaluations;this.sourceCache.set('interferences',values);return values;}async readIssues(){if(this.sourceCache.has('issues'))return this.sourceCache.get('issues');let values=[];const cache=window.ExportModelApp&&window.ExportModelApp.getActiveCacheService?window.ExportModelApp.getActiveCacheService():null;try{const payload=cache&&cache.readIssuesBundle?await cache.readIssuesBundle():(cache&&cache.readIssuesState?await cache.readIssuesState():null);const state=unwrapState(payload);values=Array.isArray(state.issues)?state.issues:[];}catch(_){}if(!values.length&&window.BimIssuesPanel&&Array.isArray(window.BimIssuesPanel.issues))values=window.BimIssuesPanel.issues;this.sourceCache.set('issues',values);return values;}async readPlans(){if(this.sourceCache.has('plans'))return this.sourceCache.get('plans');let values=[];if(window.BimPlansPanel&&typeof window.BimPlansPanel.getPlans==='function')values=window.BimPlansPanel.getPlans()||[];const cache=window.ExportModelApp&&window.ExportModelApp.getActiveCacheService?window.ExportModelApp.getActiveCacheService():null;if(!values.length&&cache&&cache.readPlans2DState){try{const state=unwrapState(await cache.readPlans2DState());const candidates=state.plans||state.documents||[];values=Array.isArray(candidates)?candidates:[];}catch(_){}}this.sourceCache.set('plans',values);return values;}async readStructure(){if(!this.structureRecords.length&&this.model)this.buildProjectStructure(this.model);return this.structureRecords.slice();}openDb(){return new Promise((resolve,reject)=>{if(!window.indexedDB)return resolve(null);const request=indexedDB.open(DB_NAME,DB_VERSION);request.onupgradeneeded=()=>{if(!request.result.objectStoreNames.contains(STORE_NAME))request.result.createObjectStore(STORE_NAME,{keyPath:'fingerprint'});};request.onsuccess=()=>resolve(request.result);request.onerror=()=>reject(request.error);});}async writeCatalog(catalog){const db=await this.openDb();if(!db)return;await new Promise((resolve,reject)=>{const tx=db.transaction(STORE_NAME,'readwrite');tx.objectStore(STORE_NAME).put(catalog);tx.oncomplete=resolve;tx.onerror=()=>reject(tx.error);});db.close();}compactContext(){const summary=this.summary||{};const structure=summary.structure||{};const ui=window.__exportModelUI||null;const viewer=window.__exportModelViewer||null;const selectedProjectIds=ui&&ui.selectedProjectIds?Array.from(ui.selectedProjectIds).filter(Boolean):[];const selectedCategoryKeys=ui&&ui.selectedCategoryKeys?Array.from(ui.selectedCategoryKeys).filter(Boolean):[];const selectedElementKeys=viewer&&viewer.selectedKeys?Array.from(viewer.selectedKeys).filter(Boolean):[];return{totalElements:summary.totalElements||0,projects:(summary.projects||[]).slice(0,80),categories:(summary.categories||[]).slice(0,160),parametersAvailable:(summary.parameterNames||[]).map(item=>item.name).slice(0,240),parametersDeferred:!!summary.parametersDeferred,sources:summary.sources||{},currentSelection:{elementCount:selectedElementKeys.length,projectIds:selectedProjectIds.slice(0,20),categoryKeys:selectedCategoryKeys.slice(0,40)},controlTree:{visibleProjectCount:ui&&ui.visibleProjects?ui.visibleProjects.size:0,visibleCategoryCount:ui&&ui.visibleCategoryKeys?ui.visibleCategoryKeys.size:0},structure:{available:!!Number(structure.modelCount||0),rootFolders:(structure.rootFolders||[]).slice(0,20),folderCount:Number(structure.folderCount||0),modelCount:Number(structure.modelCount||0),fileCount:Number(structure.fileCount||0),fields:['Tipo','Ruta','Carpeta','CarpetaPadre','CarpetaRaiz','Modelo','ProjectId','Archivo','DocumentoOrigen','Elementos','Geometrias','Archivos','Estado']}};}}class BimAiViewerActionService{getViewer(){return window.__exportModelViewer||null;}parseColor(value,fallback){const parsed=parseInt(String(value||'').replace('#',''),16);return Number.isFinite(parsed)?parsed:Number(fallback==null?0x5DA3F6:fallback);}buildColorMap(result){const keys=result&&Array.isArray(result.keys)?result.keys:[];const plan=result&&result.plan||{};if(plan.colorMode==='by_group'&&result.groupByElementKey&&result.groupByElementKey.size){const palette=window.BimGeneralSettings&&typeof window.BimGeneralSettings.getPaletteNumbers==='function'?window.BimGeneralSettings.getPaletteNumbers():[0x5DA3F6,0xF58AAE,0x79CFA6,0xFFD166,0xA78BFA,0xF59E0B,0x22D3EE,0xFB7185];const values=Array.from(new Set(result.groupByElementKey.values())).sort((a,b)=>String(a).localeCompare(String(b)));const colorByValue=new Map(values.map((value,index)=>[value,palette[index%palette.length]]));return new Map(Array.from(result.groupByElementKey,([key,value])=>[key,colorByValue.get(value)]));}const color=this.parseColor(plan.color,0x5DA3F6);return new Map(keys.map(key=>[key,color]));}getExistingColorMap(viewer){const result=new Map();const current=viewer&&viewer.visualColorOverrides instanceof Map?viewer.visualColorOverrides:new Map();for(const[key,value]of current.entries()){const color=value&&typeof value==='object'?value.color:value;if(key&&Number.isFinite(Number(color)))result.set(key,Number(color));}return result;}applyPlatformDataVisualComposition(viewer,transparencyKeys,colorByKey){const massPanel=window.__exportModelMassPanel;if(!massPanel||massPanel.viewer!==viewer||typeof massPanel.applyClassificationVisualComposition!=='function')return false;const scopeKeys=new Set(transparencyKeys||[]);if(!scopeKeys.size)return false;massPanel.applyClassificationVisualComposition({colorState:{colorByKey:colorByKey instanceof Map?colorByKey:this.getExistingColorMap(viewer),isolationKeys:new Set(scopeKeys)},transparencyState:{keys:scopeKeys,paths:new Set(['ai-panel'])}});return true;}updateAiVisualState(viewer,patch){if(!viewer)return;const current=viewer.aiVisualState&&viewer.aiVisualState.active?viewer.aiVisualState:{active:true,colorKeys:new Set(),isolationKeys:new Set(),transparencyKeys:new Set(),actions:[]};const next=Object.assign({},current,{active:true});next.colorKeys=patch&&patch.colorKeys!=null?new Set(patch.colorKeys||[]):new Set(current.colorKeys||[]);next.isolationKeys=patch&&patch.isolationKeys!=null?new Set(patch.isolationKeys||[]):new Set(current.isolationKeys||[]);next.transparencyKeys=patch&&patch.transparencyKeys!=null?new Set(patch.transparencyKeys||[]):new Set(current.transparencyKeys||[]);next.actions=Array.from(new Set([].concat(current.actions||[],patch&&patch.action||[]).filter(Boolean))).slice(-12);next.updatedAt=Date.now();next.active=!!(next.colorKeys.size||next.isolationKeys.size||next.transparencyKeys.size);viewer.aiVisualState=next.active?next:null;}async apply(result,onProgress){const viewer=this.getViewer();const action=result&&result.plan?result.plan.action:'';if(!viewer||['count','list','summarize','export_excel'].includes(action)||LOCAL_OPERATION_ACTIONS.has(action))return false;if(onProgress)onProgress('Aplicando resultado en el visor 3D...',0,1);const transactionName='ai-viewer-action-'+action;const run=()=>this.applyCore(viewer,result,transactionName);const applied=viewer.runVisualTransaction?viewer.runVisualTransaction(transactionName,run):run();const resolved=applied&&typeof applied.then==='function'?await applied:applied;if(viewer.syncViewerToolButtons)viewer.syncViewerToolButtons();if(onProgress)onProgress('Resultado aplicado en el visor.',1,1);return resolved!==false;}async applySequence(results,onProgress){const viewer=this.getViewer();const visualResults=(results||[]).filter(result=>result&&!['count','list','summarize','export_excel'].includes(result.plan&&result.plan.action)&&!LOCAL_OPERATION_ACTIONS.has(result.plan&&result.plan.action));if(!viewer||!visualResults.length)return false;const coloredKeys=new Set();const colorResults=visualResults.filter(result=>['color','isolate_color','transparency_highlight'].includes(result.plan&&result.plan.action));const composedColorMap=this.getExistingColorMap(viewer);colorResults.forEach(result=>{if(!['color','isolate_color','transparency_highlight'].includes(result.plan&&result.plan.action))return;(result.keys||[]).forEach(key=>coloredKeys.add(key));for(const[key,color]of this.buildColorMap(result))composedColorMap.set(key,color);});colorResults.forEach((result,index)=>{result.sequenceSkipColor=index<colorResults.length-1;if(!result.sequenceSkipColor)result.sequenceColorMap=composedColorMap;});visualResults.forEach(result=>{if(['transparency','transparency_highlight'].includes(result.plan&&result.plan.action)){result.sequenceVisualColorMap=composedColorMap;}});const execute=async()=>{for(let index=0;index<visualResults.length;index++){const result=visualResults[index];if(onProgress)onProgress('Aplicando acción '+(index+1)+' de '+visualResults.length+': '+result.plan.action+'...',index,visualResults.length);if(result.plan.action==='transparency'&&coloredKeys.size){const transparentKeys=new Set(result.keys||[]);result.sequenceTransparencyActiveKeys=new Set(Array.from(coloredKeys).filter(key=>transparentKeys.has(key)));}const value=this.applyCore(viewer,result,'ai-viewer-action-sequence');if(value&&typeof value.then==='function')await value;}};if(viewer.runVisualTransaction)await viewer.runVisualTransaction('ai-viewer-action-sequence',execute);else await execute();if(viewer.syncViewerToolButtons)viewer.syncViewerToolButtons();if(onProgress)onProgress('Secuencia aplicada en el visor.',visualResults.length,visualResults.length);return true;}executeViewerCommand(viewer,action){const commands={select_same_type:'SA',fit_view:'FIT',area_zoom:'ZR',section_box:'BX',measurement_tool:'MEASURE',elevation_tool:'ELEVATION',toggle_level_grid:'GRID',restore_visibility:'HR',reset_view:'RR',fade_selection:'FADE',clear:'RR'};const command=commands[action];const ui=window.__exportModelUI;if(command&&ui&&typeof ui.executeViewerToolCommand==='function'){return ui.executeViewerToolCommand(command,{source:'ai-panel'});}if(action==='fit_view'&&viewer.fitToModel)return viewer.fitToModel();if(action==='area_zoom'&&viewer.requestAreaZoom)return viewer.requestAreaZoom();if(action==='section_box'&&viewer.toggleSectionBox)return viewer.toggleSectionBox();if(action==='restore_visibility'){if(viewer.resetHiddenAndIsolationWithProgress)return viewer.resetHiddenAndIsolationWithProgress({source:'ai-panel'});if(viewer.resetHiddenAndIsolation)return viewer.resetHiddenAndIsolation();}if((action==='reset_view'||action==='clear')&&viewer.resetAllViewModifications)return viewer.resetAllViewModifications({source:'ai-panel'});if(action==='fade_selection'&&viewer.fadeUnselected)return viewer.fadeUnselected();return false;}applyCore(viewer,result,transactionName){const action=result.plan.action;const keys=Array.isArray(result.keys)?result.keys:[];const keySet=new Set(keys);const highlightColor=this.parseColor(result.plan.color,0x5DA3F6);const fadedColor=viewer.getConfiguredTransparencyColor?viewer.getConfiguredTransparencyColor():0xbebebe;const platformTransparency=viewer.getConfiguredTransparencyPercent?viewer.getConfiguredTransparencyPercent(result.plan.transparency):result.plan.transparency;result.viewerExecution={action,elementCount:keys.length,transaction:transactionName||'ai-viewer-action',reusedViewerApi:true,api:''};if(['transparency','transparency_highlight'].includes(action)){result.viewerExecution.requestedTransparency=result.plan.transparency;result.viewerExecution.appliedTransparency=platformTransparency;result.viewerExecution.transparencySource=viewer.getConfiguredTransparencyPercent?'platform-settings':'ai-plan';}if(DIRECT_VIEWER_ACTIONS.has(action)){result.viewerExecution.api='UI.executeViewerToolCommand';return this.executeViewerCommand(viewer,action);}if(!keys.length)return false;if(['select','hide','zoom_selection'].includes(action)&&viewer.selectElementKeys){result.viewerExecution.api='selectElementKeys';viewer.selectElementKeys(keys,true,{source:'ai-panel'});}if((action==='isolate'||action==='isolate_color')&&viewer.setBaseIsolationKeys){result.viewerExecution.api='setBaseIsolationKeys';viewer.setBaseIsolationKeys(keySet,{source:'ai-panel',preserveVisualAppearance:true});this.updateAiVisualState(viewer,{action,isolationKeys:viewer.baseIsolationKeys||keySet});}else if((action==='isolate'||action==='isolate_color')&&viewer.isolateSelected&&viewer.selectElementKeys){result.viewerExecution.api='selectElementKeys+isolateSelected(fallback)';viewer.selectElementKeys(keys,true,{source:'ai-panel-fallback'});viewer.isolateSelected();}else if(action==='hide'&&viewer.hideSelected){result.viewerExecution.api+='+hideSelected';viewer.hideSelected();}else if(action==='zoom_selection'&&viewer.fitToSelectedOrModel){result.viewerExecution.api+='+fitToSelectedOrModel';viewer.fitToSelectedOrModel();}else if(action==='section_box'){if(viewer.enableSectionBoxForElementKeys){result.viewerExecution.api+='+enableSectionBoxForElementKeys';return viewer.enableSectionBoxForElementKeys(keySet,{source:'ai-panel'});}if(viewer.selectElementKeys)viewer.selectElementKeys(keys,true,{source:'ai-panel-section-fallback'});result.viewerExecution.api+='+UI.executeViewerToolCommand';return this.executeViewerCommand(viewer,action);}if(['color','isolate_color','transparency_highlight'].includes(action)&&viewer.applyColorOverrides){if(result.sequenceSkipColor){result.viewerExecution.api+=(result.viewerExecution.api?'+':'')+'deferred-to-composed-color-map';}else{result.viewerExecution.api+=(result.viewerExecution.api?'+':'')+'applyColorOverrides';const colorMap=result.sequenceColorMap instanceof Map?result.sequenceColorMap:this.getExistingColorMap(viewer);if(!(result.sequenceColorMap instanceof Map)){for(const[key,color]of this.buildColorMap(result))colorMap.set(key,color);}viewer.applyColorOverrides(colorMap);this.updateAiVisualState(viewer,{action,colorKeys:colorMap.keys()});}}if(action==='transparency'&&viewer.applyMassTransparencyHighlight){const composedActiveKeys=result.sequenceTransparencyActiveKeys instanceof Set?result.sequenceTransparencyActiveKeys:new Set();const usedPlatformComposition=this.applyPlatformDataVisualComposition(viewer,keySet,result.sequenceVisualColorMap);result.viewerExecution.api=usedPlatformComposition?'MassInfoPanel.applyClassificationVisualComposition':(composedActiveKeys.size?'applyMassTransparencyHighlight(composed-color-active-set)':'applyMassTransparencyHighlight');if(!usedPlatformComposition){viewer.applyMassTransparencyHighlight(composedActiveKeys,highlightColor,fadedColor,platformTransparency,{owner:'data',allowEmptyActive:!composedActiveKeys.size,allElementKeys:keySet,preserveActiveAppearance:true});}this.updateAiVisualState(viewer,{action,transparencyKeys:keySet});}else if(action==='transparency_highlight'&&viewer.applyMassTransparencyHighlight){const transparencyScope=viewer.getBaseVisibleElementKeySet?viewer.getBaseVisibleElementKeySet():keySet;const usedPlatformComposition=this.applyPlatformDataVisualComposition(viewer,transparencyScope,result.sequenceVisualColorMap);result.viewerExecution.api+=usedPlatformComposition?'+MassInfoPanel.applyClassificationVisualComposition':'+applyMassTransparencyHighlight';if(!usedPlatformComposition){viewer.applyMassTransparencyHighlight(keySet,highlightColor,fadedColor,platformTransparency,{owner:'data',preserveActiveAppearance:true});}this.updateAiVisualState(viewer,{action,transparencyKeys:transparencyScope});}return true;}}class BimAiQueryEngine{constructor(catalog){this.catalog=catalog;this.lastResult=null;this.viewerActions=new BimAiViewerActionService();}normalizePlan(plan){const source=plan&&typeof plan==='object'?plan:{};if(!PLAN_SOURCES.has(source.source))throw new Error('La IA no indicó una fuente BIM válida. Reformula la consulta especificando si deseas trabajar con el modelo, interferencias, incidencias o láminas.');if(!PLAN_ACTIONS.has(source.action))throw new Error('La IA no indicó una acción BIM válida. No se realizó ningún cambio en el visor.');if(!PLAN_SCOPES.has(source.scope))throw new Error('La IA no indicó un alcance válido para la acción. No se realizó ningún cambio en el visor.');if(source.numericOperation!=null&&!NUMERIC_OPERATIONS.has(source.numericOperation))throw new Error('La operación matemática solicitada no pudo interpretarse de forma segura.');if(Array.isArray(source.filters)&&source.filters.some(filter=>!OPERATORS.has(filter&&filter.operator))){throw new Error('La IA generó una condición de filtro no reconocida. No se realizó ningún cambio en el visor.');}if(Array.isArray(source.percentageBaseFilters)&&source.percentageBaseFilters.some(filter=>!OPERATORS.has(filter&&filter.operator))){throw new Error('La IA generó una condición base de porcentaje no reconocida. No se realizó ningún cálculo.');}let transparency=Number(source.transparency==null?85:source.transparency);if(!Number.isFinite(transparency))transparency=85;if(transparency>0&&transparency<=1)transparency*=100;if(['transparency','transparency_highlight'].includes(source.action)&&transparency<=0)transparency=85;return{source:source.source,action:source.action,scope:source.scope,filters:Array.isArray(source.filters)?source.filters.slice(0,24).map(filter=>({field:String(filter&&(filter.fieldKey||filter.field)||''),operator:OPERATORS.has(filter&&filter.operator)?filter.operator:'contains',value:filter&&filter.value!=null?filter.value:'',connector:String(filter&&filter.connector||'AND').toUpperCase()==='OR'?'OR':'AND'})):[],percentageBaseFilters:Array.isArray(source.percentageBaseFilters)?source.percentageBaseFilters.slice(0,24).map(filter=>({field:String(filter&&(filter.fieldKey||filter.field)||''),operator:OPERATORS.has(filter&&filter.operator)?filter.operator:'contains',value:filter&&filter.value!=null?filter.value:'',connector:String(filter&&filter.connector||'AND').toUpperCase()==='OR'?'OR':'AND'})):[],percentageMode:PERCENTAGE_MODES.has(source.percentageMode)?source.percentageMode:'auto',groupBy:String(source.groupBy||''),numericOperation:NUMERIC_OPERATIONS.has(source.numericOperation)?source.numericOperation:'none',numericFields:source.numericOperation==='percentage'?'':String(source.numericFields||''),reportTitle:String(source.reportTitle||'').trim().slice(0,140),reportMode:REPORT_MODES.has(source.reportMode)?source.reportMode:'detail',reportFields:String(source.reportFields||''),reportEmptyFields:String(source.reportEmptyFields||''),displayFields:String(source.displayFields||''),recordType:RECORD_TYPES.has(source.recordType)?source.recordType:'auto',colorMode:source.colorMode==='by_group'?'by_group':'single',color:String(source.color||'#5DA3F6'),transparency:Math.max(0,Math.min(100,transparency)),limit:Math.max(1,Math.min(500,Number(source.limit)||100)),explanation:String(source.explanation||''),treeTargetType:TREE_TARGET_TYPES.has(source.treeTargetType)?source.treeTargetType:'project',treeOperation:TREE_OPERATIONS.has(source.treeOperation)?source.treeOperation:'show',target:String(source.target||'').trim(),parameter:String(source.parameter||'').trim(),value:source.value==null?'':String(source.value),searchValue:source.searchValue==null?'':String(source.searchValue),replaceValue:source.replaceValue==null?'':String(source.replaceValue),matchCase:source.matchCase===true,wholeCell:source.wholeCell!==false};}getCurrentSelectionKeySet(){const viewer=window.__exportModelViewer||null;const massPanel=window.__exportModelMassPanel||null;const keys=new Set();if(viewer&&viewer.selectedKeys&&viewer.selectedKeys.size){for(const key of viewer.selectedKeys)if(key)keys.add(String(key));}if(!keys.size&&massPanel&&massPanel.selectedElementKeys&&massPanel.selectedElementKeys.size){for(const key of massPanel.selectedElementKeys)if(key)keys.add(String(key));}if(!keys.size&&viewer&&viewer.selectedMesh){const key=viewer.getMeshKey?viewer.getMeshKey(viewer.selectedMesh):(viewer.selectedMesh.userData&&viewer.selectedMesh.userData.elementKey);if(key)keys.add(String(key));}return keys;}getScopedKeySet(plan,options){const opts=options&&typeof options==='object'?options:{};if(!plan||plan.scope==='all')return null;if(plan.scope==='current_selection'){const current=this.getCurrentSelectionKeySet();if(!current.size)throw new Error('No hay elementos seleccionados. Selecciona uno o varios elementos antes de ejecutar esta acción.');return current;}const scopedResult=opts.scopeResult||this.lastResult;const keys=scopedResult&&Array.isArray(scopedResult.keys)?new Set(scopedResult.keys):null;if(!keys||!keys.size)throw new Error('No existe un resultado anterior disponible para aplicar esta consulta.');return keys;}resolveEditableField(store,requested){const raw=String(requested||'').trim();const token=normalize(raw);if(!raw)return{missing:true,requested:raw};const direct=store.parameterByKey&&store.parameterByKey.get(raw);if(direct){if(store.isEditable&&store.isEditable(direct.key))return{key:direct.key,label:direct.display};return{readonly:true,requested:raw,label:direct.display,key:direct.key};}const params=store.getParameters?store.getParameters():[];const displayToken=item=>normalize(String(item&&item.display||'').replace(/\s*\((?:Instancia|Tipo|Calculado)\)\s*$/i,''));const exact=params.filter(item=>displayToken(item)===token||normalize(item&&item.display)===token);const editableExact=exact.filter(item=>!store.isEditable||store.isEditable(item.key));if(editableExact.length===1)return{key:editableExact[0].key,label:editableExact[0].display};if(editableExact.length>1)return{ambiguous:true,requested:raw,options:editableExact.slice(0,20).map(item=>({value:item.key,label:item.display,detail:item.source==='type'?'Parámetro de tipo':(item.source==='instance'?'Parámetro de instancia':'Editable')}))};const partial=params.filter(item=>displayToken(item).includes(token)||token.includes(displayToken(item))).filter(item=>!store.isEditable||store.isEditable(item.key));if(partial.length===1)return{key:partial[0].key,label:partial[0].display};if(partial.length>1)return{ambiguous:true,requested:raw,options:partial.slice(0,20).map(item=>({value:item.key,label:item.display,detail:item.source==='type'?'Parámetro de tipo':(item.source==='instance'?'Parámetro de instancia':'Editable')}))};if(exact.length)return{readonly:true,requested:raw,label:exact[0].display,key:exact[0].key};return{missing:true,requested:raw};}async confirmParameterChanges(plan,parameterLabel,changes){const count=Array.isArray(changes)?changes.length:0;const requiresConfirmation=plan.action==='replace_parameter'?count>1:count>=20;if(!requiresConfirmation)return true;const uniqueElements=new Set(changes.map(change=>change.elementKey).filter(Boolean)).size;const verb=plan.action==='replace_parameter'?'reemplazar valores':'modificar valores';const message='Se van a '+verb+' en “'+parameterLabel+'”.\n\nCeldas afectadas: '+count+'\nElementos afectados: '+uniqueElements+(plan.action==='replace_parameter'?'\nBuscar: “'+plan.searchValue+'”\nReemplazar con: “'+plan.replaceValue+'”':'\nNuevo valor: “'+plan.value+'”')+'\n\n¿Deseas continuar?';if(window.BimDialogs&&typeof window.BimDialogs.confirm==='function'){return await window.BimDialogs.confirm(message,{type:'warning',title:'Confirmar edición masiva',acceptText:'Aplicar cambios',cancelText:'Cancelar'});}return typeof window.confirm==='function'?window.confirm(message):false;}async refreshAfterParameterEdits(store,changes){if(!changes||!changes.length)return;const massPanel=window.__exportModelMassPanel||null;const ui=window.__exportModelUI||null;if(massPanel){try{if(massPanel.recordTableHistory)massPanel.recordTableHistory(changes);}catch(_){}try{if(massPanel.clearCustomParameterValueCache)massPanel.clearCustomParameterValueCache();}catch(_){}try{if(massPanel.resetDataPerformanceCaches)massPanel.resetDataPerformanceCaches();}catch(_){}try{if(massPanel.applyFilters)massPanel.applyFilters();}catch(_){}try{if(massPanel.isDataPanelVisible&&massPanel.isDataPanelVisible()&&massPanel.renderDataPreserveTreeScroll)massPanel.renderDataPreserveTreeScroll();else if(massPanel.nodes&&massPanel.nodes.root&&!massPanel.nodes.root.classList.contains('hidden')&&massPanel.render)massPanel.render();}catch(_){}try{if(massPanel.persistParameterOverridesNow)await massPanel.persistParameterOverridesNow(store);else if(massPanel.persistExternalParameterOverridesDebounced)massPanel.persistExternalParameterOverridesDebounced(store);}catch(error){console.warn('No se pudieron persistir las ediciones realizadas por IA.',error);}}if(ui){try{if(ui.propertiesPanel&&ui.propertiesPanel.render)ui.propertiesPanel.render();}catch(_){}ui.worksPanelDirty=true;ui.clashPanelDirty=true;try{if(ui.persistStateDebounced)ui.persistStateDebounced();}catch(_){}}try{window.dispatchEvent(new CustomEvent('bim-ai-parameter-values-changed',{detail:{count:changes.length,keys:Array.from(new Set(changes.map(change=>change.elementKey).filter(Boolean)))}}));}catch(_){}}async executeParameterAction(plan,onProgress,options,startedAt){if(plan.source!=='model')throw new Error('La edición de parámetros sólo está disponible sobre elementos del modelo BIM.');const store=await this.catalog.ensureParameters(onProgress);const scopedKeys=this.getScopedKeySet(plan,options);const massPanel=window.__exportModelMassPanel||null;const requestedParameter=String(plan.parameter||'').trim()||(massPanel&&massPanel.tableActiveCell?String(massPanel.tableActiveCell.paramKey||''):'');const parameterMatch=this.resolveEditableField(store,requestedParameter);if(parameterMatch.ambiguous)return{needsClarification:true,plan,clarification:{kind:'edit_parameter',field:requestedParameter,prompt:'Encontré varios parámetros editables llamados “'+requestedParameter+'”. ¿Cuál deseas modificar?',options:parameterMatch.options}};if(parameterMatch.readonly)return{needsClarification:true,plan,clarification:{kind:'edit_parameter',field:requestedParameter,prompt:'El campo “'+(parameterMatch.label||requestedParameter)+'” es de solo lectura. Elige un parámetro editable o reformula la instrucción.',options:[]}};if(parameterMatch.missing||!parameterMatch.key){const token=normalize(requestedParameter);const optionsList=(store.getParameters?store.getParameters():[]).filter(item=>(!store.isEditable||store.isEditable(item.key))&&(!token||normalize(item.display).includes(token.slice(0,Math.max(2,Math.min(6,token.length)))))).slice(0,20).map(item=>({value:item.key,label:item.display,detail:item.source==='type'?'Parámetro de tipo':'Parámetro de instancia'}));return{needsClarification:true,plan,clarification:{kind:'edit_parameter',field:requestedParameter,prompt:requestedParameter?('No encontré un parámetro editable llamado “'+requestedParameter+'”.'):'Indica qué parámetro deseas modificar o activa una celda en la tabla de Datos.',options:optionsList}};}const filterResolved=new Map();for(const filter of plan.filters||[]){const match=this.resolveField(store,filter.field);if(match.ambiguous)return{needsClarification:true,plan,clarification:{field:filter.field,prompt:'Encontré varios parámetros llamados “'+filter.field+'”. ¿Cuál deseas usar para filtrar?',options:match.options}};if(match.missing)return{needsClarification:true,plan,clarification:{field:filter.field,prompt:'No encontré el parámetro “'+filter.field+'” usado como filtro.',options:[]}};filterResolved.set(filter.field,match.key);}const filters=(plan.filters||[]).map(filter=>Object.assign({},filter,{field:filterResolved.get(filter.field)||filter.field}));const elements=scopedKeys&&store.elementByKey instanceof Map?Array.from(scopedKeys,key=>store.elementByKey.get(key)).filter(Boolean):(store.elements||[]);const changes=[];const samples=[];let matchedElements=0;let readonlyOrUnavailable=0;const parameterKey=parameterMatch.key;const parameterLabel=parameterMatch.label||requestedParameter||parameterKey;const searchText=String(plan.searchValue==null?'':plan.searchValue);const replacement=String(plan.replaceValue==null?'':plan.replaceValue);const matchCase=!!plan.matchCase;const wholeCell=plan.wholeCell!==false;const needle=matchCase?searchText:searchText.toLowerCase();for(let index=0;index<elements.length;index++){const element=elements[index];if(!element)continue;const matches=this.matchesFilters(key=>this.getElementValue(store,element,key),filters);if(!matches)continue;matchedElements++;if(store.isEditable&&!store.isEditable(parameterKey,element)){readonlyOrUnavailable++;continue;}const oldValueRaw=store.getValue(element,parameterKey);const oldValue=String(oldValueRaw==null?'':oldValueRaw);let nextValue=String(plan.value==null?'':plan.value);if(plan.action==='replace_parameter'){const haystack=matchCase?oldValue:oldValue.toLowerCase();const isMatch=wholeCell?haystack===needle:(searchText.length>0&&haystack.includes(needle));if(!isMatch)continue;if(wholeCell)nextValue=replacement;else{const escaped=searchText.replace(/[.*+?^${}()|[\]\\]/g,'\\$&');nextValue=matchCase?oldValue.split(searchText).join(replacement):oldValue.replace(new RegExp(escaped,'gi'),replacement);}}if(nextValue===oldValue)continue;const elementKey=store.getElementKey(element);changes.push({element,key:parameterKey,oldValue:oldValueRaw,newValue:nextValue,elementKey});if(samples.length<plan.limit)samples.push(this.elementSummary(element));if(index&&index%5000===0){if(onProgress)onProgress('Preparando edición de parámetros...',index,elements.length);await yieldBrowser();}}if(!changes.length){return{source:'model',plan,count:0,keys:[],samples:[],groups:[],editExecution:{parameterKey,parameterLabel,matchedElements,changedCells:0,changedElements:0,readonlyOrUnavailable,cancelled:false},localExecution:{durationMs:Date.now()-(startedAt||Date.now()),resolvedFields:[parameterKey]}};}const confirmed=await this.confirmParameterChanges(plan,parameterLabel,changes);if(!confirmed){return{source:'model',plan,count:0,keys:[],samples,groups:[],editExecution:{parameterKey,parameterLabel,matchedElements,changedCells:0,changedElements:0,readonlyOrUnavailable,cancelled:true,proposedCells:changes.length},localExecution:{durationMs:Date.now()-(startedAt||Date.now()),resolvedFields:[parameterKey]}};}const applied=[];const changedKeys=new Set();const batchSize=400;for(let offset=0;offset<changes.length;offset+=batchSize){const end=Math.min(changes.length,offset+batchSize);for(let index=offset;index<end;index++){const change=changes[index];if(store.setValue(change.element,change.key,change.newValue)){applied.push(change);if(change.elementKey)changedKeys.add(change.elementKey);}}if(onProgress)onProgress('Aplicando cambios de parámetros...',end,changes.length);if(end<changes.length)await yieldBrowser();}await this.refreshAfterParameterEdits(store,applied);return{source:'model',plan,count:changedKeys.size,keys:Array.from(changedKeys),samples,groups:[],editExecution:{parameterKey,parameterLabel,matchedElements,changedCells:applied.length,changedElements:changedKeys.size,readonlyOrUnavailable,cancelled:false},localExecution:{durationMs:Date.now()-(startedAt||Date.now()),resolvedFields:[parameterKey]}};}resolveProjectTreeTarget(ui,target,scope){const raw=String(target||'').trim();const token=normalize(raw);if(!raw&&scope==='current_selection'){const ids=Array.from(ui.selectedProjectIds||[]).filter(Boolean);return ids.length?{ids,label:'selección actual'}:{missing:true,prompt:'No hay un proyecto seleccionado en el árbol de Proyecto.'};}if(/^nodePath:/i.test(raw)){const path=raw.slice('nodePath:'.length);const entry=ui.projectNodeByPath&&ui.projectNodeByPath.get(path);return entry?{ids:Array.from(entry.ids||[]),nodePath:path,label:entry.node&&entry.node.name||path}:{missing:true};}const allProjects=ui.model&&Array.isArray(ui.model.projects)?ui.model.projects:[];if(token==='all'||token==='todos'||token==='todo')return{ids:allProjects.map(project=>project.ProjectId).filter(Boolean),all:true,label:'todos los proyectos'};if(ui.renderProjects)ui.renderProjects();const candidates=[];for(const[path,entry]of(ui.projectNodeByPath||new Map()).entries()){const name=String(entry&&entry.node&&entry.node.name||'').trim();if(name)candidates.push({name,path,ids:Array.from(entry.ids||[])});}for(const project of allProjects){const name=String(project&&(project.Name||project.ProjectName||project.ProjectId)||'').trim();if(name)candidates.push({name,path:'',ids:[project.ProjectId]});}const directById=allProjects.find(project=>String(project&&project.ProjectId||'')===raw);if(directById)return{ids:[directById.ProjectId],label:directById.Name||directById.ProjectId};let matches=candidates.filter(item=>normalize(item.name)===token);if(!matches.length&&token)matches=candidates.filter(item=>normalize(item.name).includes(token)||token.includes(normalize(item.name)));const unique=[];const indexBySignature=new Map();for(const item of matches){const signature=normalize(item.name)+'|'+item.ids.slice().sort().join('|');if(indexBySignature.has(signature)){const existing=unique[indexBySignature.get(signature)];if(existing&&!existing.path&&item.path)existing.path=item.path;continue;}indexBySignature.set(signature,unique.length);unique.push(Object.assign({},item));}if(unique.length===1)return{ids:unique[0].ids,nodePath:unique[0].path||'',label:unique[0].name};if(unique.length>1)return{ambiguous:true,options:unique.slice(0,20).map(item=>({value:item.path?'nodePath:'+item.path:(item.ids[0]||item.name),label:item.name,detail:item.ids.length+' proyecto(s)'}))};return{missing:true,prompt:'No encontré el proyecto o carpeta “'+raw+'”.'};}resolveCategoryTreeTarget(ui,target,scope){const raw=String(target||'').trim();const token=normalize(raw);if(!raw&&scope==='current_selection'){const keys=Array.from(ui.selectedCategoryKeys||[]).filter(Boolean);return keys.length?{keys,label:'selección actual'}:{missing:true,prompt:'No hay una categoría seleccionada en el árbol de Categorías.'};}const groups=ui.getCategoryGroupsForAllProjects?ui.getCategoryGroupsForAllProjects():(ui.getCategoryGroups?ui.getCategoryGroups():[]);if(/^categoryKey:/i.test(raw)){const key=raw.slice('categoryKey:'.length);const child=groups.flatMap(group=>(group.children||[]).map(item=>({group,item}))).find(pair=>pair.item.key===key);return key?{keys:[key],groupName:child&&child.group&&child.group.name||'',label:child&&child.item&&child.item.name||key}:{missing:true};}if(/^categoryGroup:/i.test(raw)){const name=raw.slice('categoryGroup:'.length);const group=groups.find(item=>item&&item.name===name);return group?{keys:Array.from(group.keys||[]),groupName:group.name,label:group.name}:{missing:true};}if(token==='all'||token==='todos'||token==='todas'||token==='categorias'||token==='categorías'){return{keys:groups.flatMap(group=>group.keys||[]),all:true,groupName:'root',label:'todas las categorías'};}const candidates=[];for(const group of groups){if(!group)continue;candidates.push({label:group.name,value:'categoryGroup:'+group.name,keys:Array.from(group.keys||[]),groupName:group.name,detail:'Categoría'});for(const child of group.children||[]){candidates.push({label:child.name,value:'categoryKey:'+child.key,keys:[child.key],groupName:group.name,detail:group.name+' > '+child.name});}}let matches=candidates.filter(item=>normalize(item.label)===token||normalize(item.detail)===token);if(!matches.length&&token)matches=candidates.filter(item=>normalize(item.label).includes(token)||token.includes(normalize(item.label))||normalize(item.detail).includes(token));if(matches.length===1)return{keys:matches[0].keys,groupName:matches[0].groupName,label:matches[0].label};if(matches.length>1)return{ambiguous:true,options:matches.slice(0,20).map(item=>({value:item.value,label:item.label,detail:item.detail}))};return{missing:true,prompt:'No encontré la categoría “'+raw+'”.'};}async executeTreeControl(plan,onProgress,startedAt){const ui=window.__exportModelUI||null;if(!ui||!ui.model)throw new Error('El Panel de Control no está disponible para modificar el árbol del proyecto.');const isProject=plan.treeTargetType==='project';const operation=plan.treeOperation;const resolution=isProject?this.resolveProjectTreeTarget(ui,plan.target,plan.scope):this.resolveCategoryTreeTarget(ui,plan.target,plan.scope);const operationNeedsTarget=!['show_all','hide_all'].includes(operation);if(operationNeedsTarget&&resolution.ambiguous)return{needsClarification:true,plan,clarification:{kind:'tree_target',field:plan.target,prompt:'Encontré varios nodos llamados “'+plan.target+'”. Elige cuál deseas modificar.',options:resolution.options}};if(operationNeedsTarget&&resolution.missing)return{needsClarification:true,plan,clarification:{kind:'tree_target',field:plan.target,prompt:resolution.prompt||'No encontré el nodo solicitado en el Panel de Control.',options:[]}};if(onProgress)onProgress('Actualizando Panel de Control...',0,1);let affectedKeys=[];let affectedCount=0;if(isProject){const allIds=(ui.model.projects||[]).map(project=>project&&project.ProjectId).filter(Boolean);const ids=Array.from(new Set((resolution&&resolution.ids)||[]));if(operation==='show_all')await ui.setAllProjects(true);else if(operation==='hide_all')await ui.setAllProjects(false);else if(operation==='show'||operation==='hide'){await ui.applyProjectVisibilitySelection(ids,null,null,operation==='show',{exactIds:true,keepSelection:true});}else if(operation==='show_only'){if(ui.ensureProjectsLoadedForVisibility){const loaded=await ui.ensureProjectsLoadedForVisibility(ids);if(!loaded)throw new Error('No se pudieron cargar los proyectos solicitados.');}const previousCategoryKeys=ui.getCurrentCategoryKeySet?ui.getCurrentCategoryKeySet():new Set();ui.visibleProjects=new Set(ids);if(ui.invalidateWorksCaches)ui.invalidateWorksCaches({partidas:true,summary:true});if(ui.syncCategoriesAfterProjectVisibilityChange)ui.syncCategoriesAfterProjectVisibilityChange(previousCategoryKeys);if(ui.viewer&&ui.viewer.primeTreeVisibilityPredicate)ui.viewer.primeTreeVisibilityPredicate(element=>ui.isElementVisibleInTrees(element),'ai-project-show-only');if(ui.renderProjects)ui.renderProjects();if(ui.renderCategories)ui.renderCategories();if(ui.queueTreeVisibilityUpdate)ui.queueTreeVisibilityUpdate({affectedProjectIds:allIds,reason:'ai-project-show-only',immediatePanel:false});if(ui.renderElementList)ui.renderElementList();if(ui.scheduleDeferredPanelRefresh)ui.scheduleDeferredPanelRefresh('ai-project-show-only');}else if(operation==='expand'||operation==='collapse'){const expand=operation==='expand';if(resolution&&resolution.all){if(!ui.collapsedProjectNodePaths)ui.collapsedProjectNodePaths=new Set();if(expand)ui.collapsedProjectNodePaths.clear();else{if(ui.renderProjects)ui.renderProjects();for(const[path,entry]of(ui.projectNodeByPath||new Map()).entries()){if(entry&&entry.node&&entry.node.children&&entry.node.children.length)ui.collapsedProjectNodePaths.add(path);}}if(ui.renderProjects)ui.renderProjects();if(ui.persistStateDebounced)ui.persistStateDebounced();}else if(resolution&&resolution.nodePath&&ui.projectTreeController){ui.projectTreeController.dispatch('renderer','setProjectNodeContextExpansion',ui,[resolution.nodePath,expand]);}else{if(ui.renderProjects)ui.renderProjects();const match=Array.from(ui.projectNodeByPath||[]).find(([,entry])=>entry&&entry.ids&&ids.every(id=>entry.ids.includes(id)));if(match&&ui.projectTreeController)ui.projectTreeController.dispatch('renderer','setProjectNodeContextExpansion',ui,[match[0],expand]);}}affectedCount=['show_all','hide_all'].includes(operation)?allIds.length:ids.length;if(!['expand','collapse'].includes(operation)&&ui.getElementKeysForProjectIds)affectedKeys=ui.getElementKeysForProjectIds(['show_all','hide_all'].includes(operation)?allIds:ids);}else{const allCategoryKeys=(ui.getCategoryGroupsForAllProjects?ui.getCategoryGroupsForAllProjects():ui.getCategoryGroups()).flatMap(group=>group.keys||[]);const keys=Array.from(new Set((resolution&&resolution.keys)||[]));if(operation==='show_all')ui.setAllCategories(true);else if(operation==='hide_all')ui.setAllCategories(false);else if(operation==='show'||operation==='hide')ui.applyCategoryVisibilitySelection(keys,null,null,operation==='show');else if(operation==='show_only'){ui.visibleCategoryKeys=new Set(keys);if(ui.viewer&&ui.viewer.primeTreeVisibilityPredicate)ui.viewer.primeTreeVisibilityPredicate(element=>ui.isElementVisibleInTrees(element),'ai-category-show-only');if(ui.renderCategories)ui.renderCategories();if(ui.queueTreeVisibilityUpdate)ui.queueTreeVisibilityUpdate({affectedCategoryKeys:allCategoryKeys,reason:'ai-category-show-only',immediatePanel:false});if(ui.renderElementList)ui.renderElementList();if(ui.scheduleDeferredPanelRefresh)ui.scheduleDeferredPanelRefresh('ai-category-show-only');}else if(operation==='expand'||operation==='collapse'){const expand=operation==='expand';const nodeKey=resolution&&resolution.all?'root':(resolution&&resolution.groupName||'root');if(ui.projectTreeController)ui.projectTreeController.dispatch('renderer','setCategoryNodeContextExpansion',ui,[nodeKey,expand]);}affectedCount=['show_all','hide_all'].includes(operation)?allCategoryKeys.length:keys.length;if(!['expand','collapse'].includes(operation)&&ui.getElementKeysForCategoryKeys)affectedKeys=ui.getElementKeysForCategoryKeys(['show_all','hide_all'].includes(operation)?allCategoryKeys:keys,{visibleProjectsOnly:true});}try{if(ui.persistStateDebounced)ui.persistStateDebounced();}catch(_){}if(onProgress)onProgress('Panel de Control actualizado.',1,1);return{source:'model',plan,count:affectedKeys.length||affectedCount,keys:affectedKeys,samples:[],groups:[],treeExecution:{targetType:plan.treeTargetType,operation,target:(resolution&&resolution.label)||plan.target||'',affectedNodes:affectedCount},localExecution:{durationMs:Date.now()-(startedAt||Date.now()),directTreeAction:true,resolvedFields:[]}};}resolveField(store,requested){const token=normalize(requested);if(!token)return{key:''};const direct=store.parameterByKey&&store.parameterByKey.get(requested);if(direct)return{key:direct.key,label:direct.display};const generalMatches=GENERAL_FIELDS.filter(item=>normalize(item[1])===token||item[2].some(alias=>normalize(alias)===token));if(generalMatches.length)return{key:generalMatches[0][0],label:generalMatches[0][1]};const params=store.getParameters();let matches=params.filter(item=>normalize(item.display)===token||normalize(String(item.display).replace(/\s*\(tipo\)$/i,''))===token);if(!matches.length)matches=params.filter(item=>normalize(item.display).includes(token));if(matches.length===1)return{key:matches[0].key,label:matches[0].display};if(matches.length>1)return{ambiguous:true,requested,options:matches.slice(0,20).map(item=>({value:item.key,label:item.display,detail:item.source==='type'?'Parámetro de tipo':(item.source==='instance'?'Parámetro de instancia':'Información general')}))};return{missing:true,requested};}getElementValue(store,element,key){if(String(key||'').startsWith('Structure::')&&this.catalog&&this.catalog.getElementStructureValue){return this.catalog.getElementStructureValue(element,key);}return store.getValue(element,key);}isGeneralOrStructureField(requested){const raw=String(requested||'');if(!raw)return true;if(/^(?:General|Structure)::/.test(raw))return true;const token=normalize(raw);return GENERAL_FIELDS.some(item=>normalize(item[1])===token||item[2].some(alias=>normalize(alias)===token));}compare(actual,operator,expected){const left=normalize(actual);const right=normalize(expected);if(operator==='is_empty')return!left;if(operator==='is_not_empty')return!!left;if(operator==='equals')return left===right;if(operator==='not_equals')return left!==right;if(operator==='contains')return left.includes(right);if(operator==='not_contains')return!left.includes(right);if(operator==='starts_with')return left.startsWith(right);if(operator==='ends_with')return left.endsWith(right);const a=safeNumber(actual);const b=safeNumber(expected);if(a==null||b==null)return false;if(operator==='greater_than')return a>b;if(operator==='greater_or_equal')return a>=b;if(operator==='less_than')return a<b;if(operator==='less_or_equal')return a<=b;return false;}matchesFilters(getValue,filters){let result=null;for(const filter of filters){const current=this.compare(getValue(filter.field),filter.operator,filter.value);if(result==null)result=current;else result=filter.connector==='OR'?result||current:result&&current;}return result==null?true:result;}splitFieldList(value,separator){const pattern=separator==='numeric'?/\s*\|\s*/:/\s*(?:>|\|)\s*/;return String(value||'').split(pattern).map(item=>item.trim()).filter(Boolean);}createNumericStats(count){return Array.from({length:count},()=>({sum:0,count:0,min:Infinity,max:-Infinity,unit:'',dimension:'',incompatible:0}));}appendNumericValue(stat,rawValue){const parsed=parseMeasurement(rawValue);if(!parsed||!Number.isFinite(parsed.value))return false;if(stat.dimension&&parsed.dimension&&stat.dimension!==parsed.dimension){stat.incompatible+=1;return false;}if(!stat.dimension&&parsed.dimension&&stat.count>0){stat.incompatible+=1;return false;}if(!stat.dimension&&parsed.dimension){stat.dimension=parsed.dimension;stat.unit=parsed.unit;}stat.sum+=parsed.value;stat.count+=1;stat.min=Math.min(stat.min,parsed.value);stat.max=Math.max(stat.max,parsed.value);return true;}calculatePercentageResult(numeratorCount,denominatorCount){const numerator=Math.max(0,Number(numeratorCount)||0);const denominator=Math.max(0,Number(denominatorCount)||0);if(!(denominator>0))return null;const value=Math.max(0,Math.min(100,(numerator/denominator)*100));return{value,formatted:value.toLocaleString('es-PE',{minimumFractionDigits:0,maximumFractionDigits:2})+' %',unit:'%',numeratorCount:numerator,denominatorCount:denominator,numericCount:numerator,skipped:0};}calculateNumericResult(operation,stats){const available=(stats||[]).filter(item=>item&&item.count>0);if(!available.length)return null;let value=null;let unit=available[0].unit||'';if(operation==='sum'){const dimensions=new Set(available.map(item=>item.dimension).filter(Boolean));if(dimensions.size>1)return null;value=available.reduce((total,item)=>total+item.sum,0);}else if(operation==='average')value=available[0].sum/available[0].count;else if(operation==='minimum')value=available[0].min;else if(operation==='maximum')value=available[0].max;else if(operation==='subtract'){if(available.length<2)return null;const dimension=available[0].dimension;if(available.some(item=>item.dimension&&dimension&&item.dimension!==dimension))return null;value=available.slice(1).reduce((total,item)=>total-item.sum,available[0].sum);}else if(operation==='multiply'){if(available.length<2)return null;value=available.reduce((total,item)=>total*item.sum,1);unit=available.map(item=>item.unit).filter(Boolean).join(' × ');}else if(operation==='divide'){if(available.length<2||available.slice(1).some(item=>item.sum===0))return null;value=available.slice(1).reduce((total,item)=>total/item.sum,available[0].sum);const divisorUnit=available.slice(1).map(item=>item.unit).filter(Boolean).join(' × ');unit=divisorUnit?((available[0].unit||'')+' / '+divisorUnit).trim():available[0].unit;}if(!Number.isFinite(value))return null;return{value,formatted:formatNumericValue(value)+(unit?' '+unit:''),unit,numericCount:available.reduce((total,item)=>total+item.count,0),skipped:(stats||[]).reduce((total,item)=>total+Number(item&&item.incompatible||0),0)};}createNoMatchClarification(store,plan,filters,previousKeys){const candidates=(filters||[]).map((filter,index)=>({filter,index})).filter(item=>!['is_empty','is_not_empty'].includes(item.filter.operator));const target=candidates[candidates.length-1];if(!target)return{kind:'no_matches',prompt:'No encontré elementos que cumplan esta parte de la consulta. Indica otro parámetro o valor para continuar.',options:[]};const values=new Map();for(const element of store.elements||[]){const key=store.getElementKey(element);if(previousKeys&&!previousKeys.has(key))continue;const value=String(this.getElementValue(store,element,target.filter.field)||'').trim();if(!value)continue;const normalized=normalize(value);if(!values.has(normalized))values.set(normalized,value);if(values.size>=500)break;}const expected=normalize(target.filter.value);const ranked=Array.from(values.values()).sort((a,b)=>{const an=normalize(a);const bn=normalize(b);const as=an===expected?0:(an.includes(expected)||expected.includes(an)?1:2);const bs=bn===expected?0:(bn.includes(expected)||expected.includes(bn)?1:2);return as-bs||a.localeCompare(b);}).slice(0,20);return{kind:'value',field:target.filter.field,filterIndex:target.index,requestedValue:String(target.filter.value||''),prompt:'No encontré resultados con “'+target.filter.value+'” en “'+target.filter.field+'”. Elige un valor disponible o reformula la consulta:',options:ranked.map(value=>({value,label:value,detail:'Valor disponible'}))};}normalizeSequence(rawPlan){const source=rawPlan&&typeof rawPlan==='object'?rawPlan:{};const rawSteps=Array.isArray(source.steps)&&source.steps.length?source.steps.slice(0,8):[source];const steps=rawSteps.map(step=>this.normalizePlan(step));for(let index=1;index<steps.length;index++){if(steps[index].scope==='previous_result')steps[index].scope='previous_step';}return{steps,explanation:String(source.explanation||'')};}async execute(rawPlan,options){if(rawPlan&&Array.isArray(rawPlan.steps))return this.executeSequence(rawPlan,options);return this.executeSingle(rawPlan,options);}async executeSequence(rawPlan,options){const sequence=this.normalizeSequence(rawPlan);const opts=options&&typeof options==='object'?options:{};const onProgress=opts.onProgress;const conversationPrevious=this.lastResult;let previousStep=null;const results=[];for(let index=0;index<sequence.steps.length;index++){const step=sequence.steps[index];if(onProgress)onProgress('Evaluando acción '+(index+1)+' de '+sequence.steps.length+'...',index,sequence.steps.length);const scopeResult=step.scope==='previous_result'?conversationPrevious:(step.scope==='previous_step'?previousStep:null);const result=await this.executeSingle(step,Object.assign({},opts,{deferViewerAction:true,deferLastResult:true,scopeResult}));if(result.needsClarification){return{needsClarification:true,plan:sequence,clarification:result.clarification,clarificationStepIndex:index,completedSteps:results.length};}result.stepIndex=index;results.push(result);if((Array.isArray(result.keys)&&result.keys.length)||(result.source!=='model'&&(Array.isArray(result.__tableSourceRecords)||result.calculation||Number.isFinite(Number(result.count))))){previousStep=result;}}await this.viewerActions.applySequence(results,onProgress);const lastDataResult=results.slice().reverse().find(result=>(Array.isArray(result.keys)&&result.keys.length)||(result.source!=='model'&&(Array.isArray(result.__tableSourceRecords)||result.calculation||Number.isFinite(Number(result.count)))))||null;const displayResult=lastDataResult||results[results.length-1]||null;if(lastDataResult)this.lastResult=lastDataResult;else if(conversationPrevious)this.lastResult=conversationPrevious;return Object.assign({},displayResult||{source:'model',count:0,keys:[],samples:[],groups:[]},{isSequence:true,sequencePlan:sequence,steps:results,sequenceCount:results.length});}async executeSingle(rawPlan,options){const plan=this.normalizePlan(rawPlan);const opts=options&&typeof options==='object'?options:{};const onProgress=opts.onProgress;const startedAt=Date.now();await this.catalog.prepare(onProgress);if(plan.action==='tree_control')return this.executeTreeControl(plan,onProgress,startedAt);if(plan.action==='set_parameter'||plan.action==='replace_parameter')return this.executeParameterAction(plan,onProgress,opts,startedAt);if(plan.source!=='model')return this.executeRecordSource(plan,onProgress,opts);if(DIRECT_VIEWER_ACTIONS.has(plan.action)||(plan.action==='section_box'&&plan.scope==='all'&&!plan.filters.length)){const directResult={source:'model',plan,count:0,keys:[],samples:[],groupByElementKey:null,groups:[]};directResult.localExecution={durationMs:Date.now()-startedAt,directViewerAction:true,resolvedFields:[]};if(!opts.deferViewerAction)await this.applyViewerAction(directResult,onProgress);return directResult;}const requestedReportFields=this.splitFieldList(plan.reportFields,'numeric');const requestedReportEmptyFields=this.splitFieldList(plan.reportEmptyFields,'numeric');const requestedFieldsForStore=plan.filters.map(item=>item.field).concat((plan.percentageBaseFilters||[]).map(item=>item.field),this.splitFieldList(plan.groupBy,'group'),this.splitFieldList(plan.numericFields,'numeric'),requestedReportFields,requestedReportEmptyFields);const usesOnlyGeneralStructure=requestedFieldsForStore.every(field=>this.isGeneralOrStructureField(field));const store=usesOnlyGeneralStructure&&this.catalog.ensureBasicStore?await this.catalog.ensureBasicStore(onProgress):await this.catalog.ensureParameters(onProgress);const previousKeys=this.getScopedKeySet(plan,opts);const requestedGroupFields=this.splitFieldList(plan.groupBy,'group');const requestedNumericFields=this.splitFieldList(plan.numericFields,'numeric');if(plan.numericOperation!=='none'&&plan.numericOperation!=='percentage'&&!requestedNumericFields.length){const options=store.getParameters().filter(item=>/volumen|volume|area|área|longitud|length|costo|coste|precio|price|cantidad|quantity|peso|weight/i.test(item.display)).slice(0,20);return{needsClarification:true,plan,clarification:{kind:'numeric_field',field:'',prompt:'Indica qué parámetro numérico debo utilizar para realizar el cálculo.',options:options.map(item=>({value:item.key,label:item.display,detail:item.source}))}};}if(['subtract','multiply','divide'].includes(plan.numericOperation)&&requestedNumericFields.length<2){return{needsClarification:true,plan,clarification:{kind:'numeric_fields',field:requestedNumericFields[0]||'',prompt:'La operación “'+plan.numericOperation+'” necesita por lo menos dos parámetros numéricos. Reformula la consulta indicando ambos parámetros.',options:[]}};}if(plan.action==='export_excel'&&plan.reportMode==='observations'&&!requestedReportEmptyFields.length&&!requestedReportFields.length){return{needsClarification:true,plan,clarification:{kind:'report_empty_field',field:'',prompt:'Indica qué parámetros debo revisar para detectar valores vacíos en el reporte.',options:store.getParameters().slice(0,20).map(item=>({value:item.key,label:item.display,detail:item.source}))}};}const fields=new Set(plan.filters.map(item=>item.field).concat((plan.percentageBaseFilters||[]).map(item=>item.field),requestedGroupFields,requestedNumericFields,requestedReportFields,requestedReportEmptyFields));const resolved=new Map();for(const field of fields){const match=this.resolveField(store,field);if(match.ambiguous)return{needsClarification:true,plan,clarification:{field,prompt:'Encontré varios parámetros llamados “'+field+'”. ¿Cuál deseas usar?',options:match.options}};if(match.missing)return{needsClarification:true,plan,clarification:{field,prompt:'No encontré el parámetro “'+field+'”. Elige uno disponible:',options:store.getParameters().filter(item=>normalize(item.display).includes(normalize(field).slice(0,4))).slice(0,20).map(item=>({value:item.key,label:item.display,detail:item.source}))}};resolved.set(field,match.key);}const filters=plan.filters.map(item=>Object.assign({},item,{field:resolved.get(item.field)||item.field}));const percentageBaseFilters=(plan.percentageBaseFilters||[]).map(item=>Object.assign({},item,{field:resolved.get(item.field)||item.field}));const groupKeys=requestedGroupFields.map(field=>resolved.get(field)||field);const numericKeys=requestedNumericFields.map(field=>resolved.get(field)||field);const reportKeys=requestedReportFields.map(field=>resolved.get(field)||field);const reportEmptyKeys=(requestedReportEmptyFields.length?requestedReportEmptyFields:requestedReportFields).map(field=>resolved.get(field)||field);const groupKey=groupKeys.length===1?groupKeys[0]:'';const allElements=store.elements||[];const elements=previousKeys&&store.elementByKey instanceof Map?Array.from(previousKeys,key=>store.elementByKey.get(key)).filter(Boolean):allElements;const keys=[];const samples=[];const groups=new Map();const groupByElementKey=plan.colorMode==='by_group'&&groupKeys.length?new Map():null;const observationsOnly=plan.action==='export_excel'&&plan.reportMode==='observations'&&reportEmptyKeys.length>0;const percentageMode=plan.numericOperation==='percentage';const percentageGroupingMode=plan.percentageMode==='within_group'?'within_group':(plan.percentageMode==='share_of_base'?'share_of_base':(groupKeys.length&&filters.length?'within_group':'share_of_base'));const percentageGroupDenominators=new Map();let percentageDenominatorCount=0;for(let i=0;i<elements.length;i++){const element=elements[i];const elementKey=store.getElementKey(element);if(previousKeys&&!previousKeys.has(elementKey))continue;const matchesPercentageBase=!percentageMode||this.matchesFilters(key=>this.getElementValue(store,element,key),percentageBaseFilters);if(percentageMode&&matchesPercentageBase){percentageDenominatorCount+=1;if(groupKeys.length&&percentageGroupingMode==='within_group'){const baseGroupValue=groupKeys.map(key=>this.getElementValue(store,element,key)||'<Sin valor>').join(' > ');percentageGroupDenominators.set(baseGroupValue,(percentageGroupDenominators.get(baseGroupValue)||0)+1);}}if(!matchesPercentageBase)continue;const matchesBaseFilters=this.matchesFilters(key=>this.getElementValue(store,element,key),filters);const matchesObservation=!observationsOnly||reportEmptyKeys.some(key=>!normalize(this.getElementValue(store,element,key)));if(matchesBaseFilters&&matchesObservation){const key=elementKey;if(key)keys.push(key);if(samples.length<plan.limit)samples.push(this.elementSummary(element));if(groupKeys.length||plan.numericOperation!=='none'){const value=groupKeys.length?groupKeys.map(key=>this.getElementValue(store,element,key)||'<Sin valor>').join(' > '):'Total';let group=groups.get(value);if(!group){group={value,count:0,keys:[],numericStats:this.createNumericStats(numericKeys.length)};groups.set(value,group);}group.count+=1;if(key)group.keys.push(key);if(!percentageMode){numericKeys.forEach((numericKey,numericIndex)=>{this.appendNumericValue(group.numericStats[numericIndex],this.getElementValue(store,element,numericKey));});}if(groupByElementKey&&key)groupByElementKey.set(key,value);}}if(i&&i%6000===0){if(onProgress)onProgress('Consultando elementos BIM...',i,elements.length);await yieldBrowser();}}if(percentageMode&&!groupKeys.length&&!groups.size){groups.set('Total',{value:'Total',count:0,keys:[],numericStats:[]});}if(!keys.length&&plan.filters.length&&!['count','list'].includes(plan.action)&&plan.numericOperation!=='percentage'){return{needsClarification:true,plan,clarification:this.createNoMatchClarification(store,plan,filters,previousKeys)};}if(percentageMode&&!(percentageDenominatorCount>0)){return{needsClarification:true,plan,clarification:{kind:'percentage_base',field:(plan.percentageBaseFilters||[]).map(item=>item.field).join(' | '),prompt:'El conjunto base del porcentaje no contiene elementos. Revisa la condición usada como denominador.',options:[]}};}const groupResults=Array.from(groups.values()).map(group=>{const calculation=plan.numericOperation==='none'?null:(percentageMode?this.calculatePercentageResult(group.count,percentageGroupingMode==='within_group'&&groupKeys.length?Number(percentageGroupDenominators.get(group.value)||0):percentageDenominatorCount):this.calculateNumericResult(plan.numericOperation,group.numericStats));return Object.assign({value:group.value,count:group.count,keys:Array.isArray(group.keys)?group.keys:[]},calculation?{numericValue:calculation.value,formattedValue:calculation.formatted,unit:calculation.unit,numericCount:calculation.numericCount,numeratorCount:calculation.numeratorCount,denominatorCount:calculation.denominatorCount,skippedNumericValues:calculation.skipped}:{});}).sort((a,b)=>{if(plan.numericOperation!=='none')return Number(b.numericValue||0)-Number(a.numericValue||0);return b.count-a.count;});if(plan.numericOperation!=='none'&&plan.numericOperation!=='percentage'&&(!groupResults.length||!groupResults.some(item=>Number.isFinite(item.numericValue)))){return{needsClarification:true,plan,clarification:{kind:'numeric_data',field:requestedNumericFields.join(' | '),prompt:'Los parámetros seleccionados no contienen valores numéricos compatibles para realizar “'+plan.numericOperation+'”. Indica otros parámetros o verifica sus unidades.',options:[]}};}const result={source:'model',plan,count:keys.length,keys,samples,groupByElementKey,groups:groupResults,calculation:plan.numericOperation==='none'?null:(percentageMode?{operation:'percentage',requestedFields:[],resolvedFields:[],numeratorCount:keys.length,denominatorCount:percentageDenominatorCount,percentage:percentageDenominatorCount>0?(keys.length/percentageDenominatorCount)*100:null,formatted:percentageDenominatorCount>0?this.calculatePercentageResult(keys.length,percentageDenominatorCount).formatted:''}:{operation:plan.numericOperation,requestedFields:requestedNumericFields,resolvedFields:numericKeys})};if(plan.action==='export_excel'){result.report={title:plan.reportTitle||(plan.reportMode==='observations'?'Reporte de observaciones BIM':'Reporte de información BIM'),mode:plan.reportMode,requestedFields:requestedReportFields,resolvedFields:requestedReportFields.map((requested,index)=>({requested,key:reportKeys[index]})),emptyFields:(requestedReportEmptyFields.length?requestedReportEmptyFields:requestedReportFields),resolvedEmptyFields:reportEmptyKeys,rowCount:keys.length};}result.localExecution={durationMs:Date.now()-startedAt,scannedElements:elements.length,matchedElements:keys.length,scope:plan.scope,scopeElementCount:previousKeys?previousKeys.size:elements.length,resolvedFields:Array.from(resolved,([requested,key])=>({requested,key})),resolvedFilters:filters.map(item=>({field:item.field,operator:item.operator,value:item.value,connector:item.connector})),resolvedPercentageBaseFilters:percentageBaseFilters.map(item=>({field:item.field,operator:item.operator,value:item.value,connector:item.connector})),percentageDenominatorCount:percentageMode?percentageDenominatorCount:null,percentageMode:percentageMode?percentageGroupingMode:null,resolvedGroupBy:groupKeys,resolvedReportFields:reportKeys,resolvedReportEmptyFields:reportEmptyKeys,numericOperation:plan.numericOperation,resolvedNumericFields:numericKeys};if(!opts.deferViewerAction)await this.applyViewerAction(result,onProgress);if(!opts.deferLastResult)this.lastResult=result;return result;}getRecordValue(record,field,source,recordType){const raw=String(field||'').trim();if(!raw)return'';const wanted=normalize(raw);const sourceRecord=record&&typeof record==='object'?record:{};const aliasGroups={name:['name','nombre','title','titulo','título'],evaluation:['evaluation','evaluacion','evaluación','name','nombre'],totalclashes:['totalclashes','interferencias','choques','cantidad de interferencias','cantidad interferencias','total interferencias'],open:['abiertas','abiertos','abierto','activas','activos'],reviewed:['revisadas','revisados','revisado'],resolved:['resueltas','resueltos','resuelto'],date:['createdat','fecha','fecha de creacion','fecha de creación'],tolerance:['tolerance','tolerancia'],status:['status','estado'],number:['number','numero','número'],title:['title','titulo','título','nombre'],priority:['priority','prioridad'],assignee:['assignee','responsable','asignado','asignada'],due:['duedate','fecha limite','fecha límite','vencimiento'],description:['description','descripcion','descripción'],elementa:['elementakey','elementaname','elementoa','elemento a'],elementb:['elementbkey','elementbname','elementob','elemento b'],volume:['volumemm3','volumen','volumen de interferencia']};const normalizedKeys=Object.keys(sourceRecord).map(key=>({key,normalized:normalize(key)}));const exact=normalizedKeys.find(item=>item.normalized===wanted);if(exact)return sourceRecord[exact.key];let aliases=[];for(const candidates of Object.values(aliasGroups)){if(candidates.some(candidate=>normalize(candidate)===wanted)){aliases=candidates;break;}}if(source==='interferences'&&recordType==='evaluations'&&['nombre de evaluacion','nombre de evaluación'].includes(wanted)){aliases=['name','nombre'];}for(const alias of aliases){const normalizedAlias=normalize(alias);const match=normalizedKeys.find(item=>item.normalized===normalizedAlias);if(match)return sourceRecord[match.key];}const partial=normalizedKeys.find(item=>item.normalized.includes(wanted)||wanted.includes(item.normalized));return partial?sourceRecord[partial.key]:'';}createInterferenceEvaluationRecords(evaluations){return(evaluations||[]).map(evaluation=>{const clashes=Array.isArray(evaluation&&evaluation.clashes)?evaluation.clashes:[];let open=0,reviewed=0,resolved=0;for(const clash of clashes){const status=normalize(clash&&clash.status||'abierto');if(status==='resuelto')resolved++;else if(status==='revisado')reviewed++;else open++;}return{id:evaluation&&evaluation.id||'',name:evaluation&&(evaluation.name||evaluation.title||evaluation.id)||'',createdAt:evaluation&&evaluation.createdAt||'',tolerance:Number(evaluation&&evaluation.tolerance)||0,totalChecked:Number(evaluation&&evaluation.totalChecked)||0,totalPossiblePairs:Number(evaluation&&evaluation.totalPossiblePairs)||0,totalClashes:clashes.length,openClashes:open,reviewedClashes:reviewed,resolvedClashes:resolved};});}getDefaultRecordDisplayFields(plan,recordType){if(!plan)return[];if(plan.source==='interferences'&&recordType==='evaluations')return['Nombre','Interferencias'];if(plan.source==='interferences')return['Evaluación','Estado','Elemento A','Elemento B'];if(plan.source==='issues')return['Número','Título','Estado','Prioridad'];if(plan.source==='plans')return['Nombre'];if(plan.source==='structure')return['Tipo','Ruta'];return[];}projectRecordForDisplay(record,fields,source,recordType){const projected={};for(const field of fields||[])projected[field]=this.getRecordValue(record,field,source,recordType);return projected;}async executeRecordSource(plan,onProgress,options){const startedAt=Date.now();let records=[];let recordType=plan.recordType||'auto';if(plan.source==='interferences'){const evaluations=await this.catalog.readInterferences();if(recordType==='auto'){const requestedShape=normalize([plan.displayFields||'',plan.groupBy||'',...(plan.filters||[]).map(item=>item&&item.field||'')].join(' | '));recordType=requestedShape.includes('evaluacion')||(plan.action==='list'&&normalize(plan.displayFields)==='nombre')?'evaluations':'clashes';}if(recordType==='evaluations'){records=this.createInterferenceEvaluationRecords(evaluations);}else{records=evaluations.flatMap(evaluation=>(evaluation.clashes||[]).map(clash=>Object.assign({evaluation:evaluation.name||evaluation.title||evaluation.id,evaluationId:evaluation.id||''},clash)));}}else if(plan.source==='structure')records=await this.catalog.readStructure();else if(plan.source==='issues')records=await this.catalog.readIssues();else if(plan.source==='plans')records=await this.catalog.readPlans();if(plan.scope==='previous_result'||plan.scope==='previous_step'){const scopeResult=options&&options.scopeResult?options.scopeResult:(plan.scope==='previous_result'?this.lastResult:null);const scopedRecords=scopeResult&&scopeResult.source===plan.source&&Array.isArray(scopeResult.__tableSourceRecords)?scopeResult.__tableSourceRecords:null;if(scopedRecords)records=scopedRecords.slice();}const getValue=(record,field)=>this.getRecordValue(record,field,plan.source,recordType);const numericFields=this.splitFieldList(plan.numericFields,'numeric');if(plan.numericOperation!=='none'&&plan.numericOperation!=='percentage'&&!numericFields.length){return{needsClarification:true,plan,clarification:{kind:'numeric_field',field:'',prompt:'Indica qué dato numérico debo utilizar para realizar el cálculo.',options:[]}};}if(['subtract','multiply','divide'].includes(plan.numericOperation)&&numericFields.length<2){return{needsClarification:true,plan,clarification:{kind:'numeric_fields',field:numericFields[0]||'',prompt:'La operación “'+plan.numericOperation+'” necesita por lo menos dos datos numéricos.',options:[]}};}const filtered=[];const groups=new Map();const groupFields=this.splitFieldList(plan.groupBy,'group');const percentageMode=plan.numericOperation==='percentage';const percentageBaseFilters=plan.percentageBaseFilters||[];const percentageGroupingMode=plan.percentageMode==='within_group'?'within_group':(plan.percentageMode==='share_of_base'?'share_of_base':(groupFields.length&&plan.filters.length?'within_group':'share_of_base'));const percentageGroupDenominators=new Map();let percentageDenominatorCount=0;const reportFields=this.splitFieldList(plan.reportFields,'numeric');const reportEmptyFields=this.splitFieldList(plan.reportEmptyFields,'numeric');const explicitlyRequestedDisplayFields=this.splitFieldList(plan.displayFields,'numeric');const displayFields=explicitlyRequestedDisplayFields.length?explicitlyRequestedDisplayFields:this.getDefaultRecordDisplayFields(plan,recordType);const observationFields=reportEmptyFields.length?reportEmptyFields:reportFields;if(plan.action==='export_excel'&&plan.reportMode==='observations'&&!observationFields.length){return{needsClarification:true,plan,clarification:{kind:'report_empty_field',field:'',prompt:'Indica qué campos debo revisar para detectar valores vacíos en el reporte.',options:[]}};}const observationsOnly=plan.action==='export_excel'&&plan.reportMode==='observations'&&observationFields.length>0;for(let i=0;i<records.length;i++){const record=records[i];const matchesPercentageBase=!percentageMode||this.matchesFilters(field=>getValue(record,field),percentageBaseFilters);if(percentageMode&&matchesPercentageBase){percentageDenominatorCount+=1;if(groupFields.length&&percentageGroupingMode==='within_group'){const baseGroupValue=groupFields.map(field=>getValue(record,field)||'<Sin valor>').join(' > ');percentageGroupDenominators.set(baseGroupValue,(percentageGroupDenominators.get(baseGroupValue)||0)+1);}}if(!matchesPercentageBase)continue;const matchesBaseFilters=this.matchesFilters(field=>getValue(record,field),plan.filters);const matchesObservation=!observationsOnly||observationFields.some(field=>!normalize(getValue(record,field)));if(matchesBaseFilters&&matchesObservation){filtered.push(record);if(groupFields.length||plan.numericOperation!=='none'){const value=groupFields.length?groupFields.map(field=>getValue(record,field)||'<Sin valor>').join(' > '):'Total';let group=groups.get(value);if(!group){group={value,count:0,numericStats:this.createNumericStats(numericFields.length)};groups.set(value,group);}group.count+=1;if(!percentageMode)numericFields.forEach((field,index)=>this.appendNumericValue(group.numericStats[index],getValue(record,field)));}}if(i&&i%3000===0)await yieldBrowser();}if(percentageMode&&!groupFields.length&&!groups.size){groups.set('Total',{value:'Total',count:0,numericStats:[]});}if(onProgress)onProgress('Consulta completada.',1,1);if(percentageMode&&!(percentageDenominatorCount>0)){return{needsClarification:true,plan,clarification:{kind:'percentage_base',field:percentageBaseFilters.map(item=>item.field).join(' | '),prompt:'El conjunto base del porcentaje no contiene registros. Revisa la condición usada como denominador.',options:[]}};}const groupResults=Array.from(groups.values()).map(group=>{const calculation=plan.numericOperation==='none'?null:(percentageMode?this.calculatePercentageResult(group.count,percentageGroupingMode==='within_group'&&groupFields.length?Number(percentageGroupDenominators.get(group.value)||0):percentageDenominatorCount):this.calculateNumericResult(plan.numericOperation,group.numericStats));return Object.assign({value:group.value,count:group.count},calculation?{numericValue:calculation.value,formattedValue:calculation.formatted,unit:calculation.unit,numericCount:calculation.numericCount,numeratorCount:calculation.numeratorCount,denominatorCount:calculation.denominatorCount,skippedNumericValues:calculation.skipped}:{});}).sort((a,b)=>plan.numericOperation==='none'?b.count-a.count:Number(b.numericValue||0)-Number(a.numericValue||0));if(plan.numericOperation!=='none'&&plan.numericOperation!=='percentage'&&(!groupResults.length||!groupResults.some(item=>Number.isFinite(item.numericValue)))){return{needsClarification:true,plan,clarification:{kind:'numeric_data',field:numericFields.join(' | '),prompt:'Los datos seleccionados no contienen valores numéricos compatibles para realizar “'+plan.numericOperation+'”.',options:[]}};}const projected=displayFields.length?filtered.map(record=>this.projectRecordForDisplay(record,displayFields,plan.source,recordType)):filtered;const result={source:plan.source,plan,recordType,displayFields:displayFields.slice(),count:filtered.length,samples:filtered.slice(0,plan.limit),keys:[],groups:groupResults,calculation:plan.numericOperation==='none'?null:(percentageMode?{operation:'percentage',requestedFields:[],resolvedFields:[],numeratorCount:filtered.length,denominatorCount:percentageDenominatorCount,percentage:percentageDenominatorCount>0?(filtered.length/percentageDenominatorCount)*100:null,formatted:percentageDenominatorCount>0?this.calculatePercentageResult(filtered.length,percentageDenominatorCount).formatted:''}:{operation:plan.numericOperation,requestedFields:numericFields,resolvedFields:numericFields})};Object.defineProperty(result,'__tableRecords',{value:projected,enumerable:false,configurable:true});Object.defineProperty(result,'__tableSourceRecords',{value:filtered,enumerable:false,configurable:true});if(plan.action==='export_excel'){result.report={title:plan.reportTitle||(plan.reportMode==='observations'?'Reporte de observaciones BIM':'Reporte BIM'),mode:plan.reportMode,requestedFields:reportFields,resolvedFields:reportFields.map(field=>({requested:field,key:field})),emptyFields:observationFields,resolvedEmptyFields:observationFields,rowCount:filtered.length};Object.defineProperty(result,'__reportRecords',{value:filtered,enumerable:false,configurable:true});}result.localExecution={durationMs:Date.now()-startedAt,scannedRecords:records.length,matchedRecords:filtered.length,resolvedFilters:plan.filters,resolvedPercentageBaseFilters:percentageBaseFilters,percentageDenominatorCount:percentageMode?percentageDenominatorCount:null,percentageMode:percentageMode?percentageGroupingMode:null,recordType,displayFields:displayFields.slice(),resolvedReportFields:reportFields,resolvedReportEmptyFields:observationFields,numericOperation:plan.numericOperation,resolvedNumericFields:numericFields};if(!(options&&options.deferLastResult))this.lastResult=result;return result;}elementSummary(element){const structure=this.catalog&&this.catalog.getProjectStructureForElement?this.catalog.getProjectStructureForElement(element):null;return{project:element.ProjectName||element.ModelId||'',folder:structure&&structure.folderPath||'',sourceDocument:structure&&structure.sourceDocument||'',elementId:element.ElementId||'',category:element.CategoryName||'',family:element.FamilyName||'',type:element.TypeName||'',level:element.LevelName||''};}async applyViewerAction(result,onProgress){return this.viewerActions.apply(result,onProgress);}}window.BimAiCatalog=BimAiCatalog;window.BimAiViewerActionService=BimAiViewerActionService;window.BimAiQueryEngine=BimAiQueryEngine;window.BimAiUtils=Object.freeze({normalize,yieldBrowser});})();