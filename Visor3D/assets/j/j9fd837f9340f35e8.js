(function(){function debugLog(level,phase,details,forcePersist){try{if(typeof window!=='undefined'&&window.BimDebugLog){window.BimDebugLog.log(level||'info',phase,details||{},!!forcePersist);}}catch(_){}}function debugError(phase,error,details){try{if(typeof window!=='undefined'&&window.BimDebugLog){window.BimDebugLog.error(phase,error,details||{});}}catch(_){}}function getArrayLikeLength(value){return value&&typeof value.length==='number'?value.length:0;}function getGeometryVertexCount(geometryData){if(!geometryData)return 0;if(geometryData.PositionBuffer&&typeof geometryData.PositionBuffer.length==='number'){return Math.floor(geometryData.PositionBuffer.length/3);}return getArrayLikeLength(geometryData.Vertices);}function getGeometryStats(geometryData){if(!geometryData)return{};return{geometryId:geometryData.GeometryId||'',projectId:geometryData.ProjectId||'',vertices:getGeometryVertexCount(geometryData),indices:getArrayLikeLength(geometryData.Indices),lineIndices:getArrayLikeLength(geometryData.LineIndices),materialIds:getArrayLikeLength(geometryData.MaterialIds),optimizedBinary:!!geometryData.OptimizedBinary};}function getElementStats(element){if(!element)return{};return{elementId:element.ElementId||'',modelId:element.ModelId||'',projectId:element.ProjectId||'',categoryId:element.CategoryId||'',categoryName:element.CategoryName||element.Category||''};}const fallbackPalette=[0x2563eb,0x16a34a,0xf97316,0x9333ea,0xdc2626,0x0891b2,0xca8a04,0x4f46e5,0x059669,0xbe123c,0x7c3aed,0x0d9488,0xea580c,0x65a30d,0x0284c7];class BimCanvasViewer{constructor(canvas,onSelect){if(!window.THREE){throw new Error('No se pudo cargar Three.js. Revisa tu conexión a internet o usa una versión con Three.js local.');}this.canvas=canvas;this.onSelect=onSelect;this.meshes=[];this.edges=[];this.fullModelBounds=null;this.modelCoordinateOriginRevit=null;this.meshByElementKey=new Map();this.meshesByElementKey=new Map();this.meshesByProjectId=new Map();this.meshesByCategoryUiKey=new Map();this.meshesByProjectCategoryUiKey=new Map();this.elementKeyByCategoryUiKey=new Map();this.elementKeyByProjectId=new Map();this.categoryGroups=new Map();this.materialByKey=new Map();this.selectedMesh=null;this.selectedMeshes=[];this.selectedOriginalMaterials=new Map();this.fadedMaterialStates=new Map();this.fadedMeshMaterialStates=new Map();this.fadedEdgeStates=new Map();this.fadedMeshKeys=new Set();this.visualClashForceVisibleKeys=new Set();this.visualClashHiddenKeys=new Set();this.colorOverrideStates=new Map();this.simpleColorMaterialCache=new Map();this.simpleEdgeMaterialCache=new Map();this.baseMeshMaterialCache=new Map();this.sharedGeometryCache=new Map();this.singleDrawGeometryCache=new Map();this.generatedEdgeGeometryCache=new Map();this.generatedEdgeGeometryCacheHits=0;this.generatedEdgeGeometryCacheMisses=0;this.compactFallbackEdgeCount=0;this.compactFallbackEdgeSegmentCount=0;this.transientCompactFallbackEdgeCount=0;this.groupedTransparentEdgeBatches=[];this.groupedTransparentEdgeCacheSignature=null;this.groupedTransparentEdgeActiveKeysSnapshot=new Set();this.groupedTransparentEdgeInteractiveKeysSnapshot=new Set();this.groupedTransparentEdgeCache=new Map();this.groupedTransparentEdgeCacheLimit=6;this.groupedTransparentEdgeBuildCount=0;this.groupedTransparentEdgeReuseCount=0;this.groupedTransparentEdgeActiveKeysSnapshot=new Set();this.groupedTransparentEdgeInteractiveKeysSnapshot=new Set();this.massTransparencyChunkMeshes=[];this.massTransparencyChunkCacheSignature=null;this.massTransparencyChunkVisible=false;this.massTransparencyChunksByElementKey=new Map();this.massTransparencyChunkHiddenKeys=new Set();this.massTransparencyChunkActiveKeysSnapshot=new Set();this.massTransparencyChunkInteractiveKeysSnapshot=new Set();this.massTransparencyChunkBuildCount=0;this.massTransparencyChunkReuseCount=0;this.massTransparencyGeometryCache=new Map();this.massTransparencyGeometryCacheLimit=6;this.massTransparencyGeometryCacheHits=0;this.massTransparencyGeometryCacheMisses=0;this.skipNormalChunkRefreshDuringMassApply=false;this.visualCachePrewarmTimer=null;this.visualCachePrewarmInProgress=false;this.transparencyCachePrewarmDone=false;this.transparencyCachePrewarmCount=0;this.normalChunkRefreshDeferTimer=null;this.detachedNormalChunkCells=new Map();this.normalChunkCellRefreshKeys=new Set();this.normalChunkCellRefreshTimer=null;this.normalChunkCellRefreshIdleHandle=null;this.normalChunkCellRefreshGeneration=0;this.normalChunkMeshes=[];this.normalChunkEdgeBatches=[];this.adaptiveMediumDepthProxies=new Set();this.adaptiveMediumDepthMaterial=null;this.adaptiveMediumDepthBoxGeometry=null;this.normalChunkDepthStableMaterialCache=new WeakMap();this.normalChunkEdgesVisibleOnly=false;this.normalChunkCacheSignature=null;this.normalChunkVisible=false;this.normalChunkHiddenKeys=new Set();this.normalChunksByElementKey=new Map();this.selectionPeeledNormalChunks=new Set();this.selectionChunkRestoredSources=new Set();this.selectionMaskedNormalChunks=new Map();this.selectionMaskedNormalChunkSourceKeys=new Set();this.normalChunkEdgeBuildGeneration=0;this.normalChunkRevision=0;this.normalChunkBuildCount=0;this.normalChunkReuseCount=0;this.normalChunkOpaqueMeshCount=0;this.normalChunkTransparentMeshCount=0;this.normalChunkSourceMeshCount=0;this.normalChunkEnabled=true;this.spatialChunkingEnabled=true;this.spatialChunkMinimumMeshes=24;this.spatialChunkTargetMeshesPerCell=180;this.spatialChunkMaximumCells=512;this.normalChunkOpaqueMaxVertices=120000;this.normalChunkOpaqueMaxIndices=360000;this.normalChunkTransparentMaxVertices=60000;this.normalChunkTransparentMaxIndices=180000;this.normalChunkEdgeMaxFloatValues=240000;this.spatialChunkBuildCount=0;this.adaptiveMediumPixelThreshold=3.5;this.adaptiveMediumPixelHysteresis=1;this.adaptiveMediumSuppressedChunks=0;this.visualStateChunkMeshes=[];this.visualStateChunkEdgeBatches=[];this.visualStateChunkHiddenKeys=new Set();this.visualStateChunkCacheSignature=null;this.visualStateChunkVisible=false;this.visualStateChunkBuildCount=0;this.visualStateChunkReuseCount=0;this.visualStateChunksByElementKey=new Map();this.selectionMaskedVisualStateChunks=new Map();this.selectionMaskedVisualStateSourceKeys=new Set();this.visualStateUseStableEdges=false;this.massTransparencyUseStableEdges=false;this.stableNormalEdgeAppearanceMode='normal';this.treeVisibilityGuardActive=false;this.treeVisibilityGuardRevision=0;this.baseVisibilityRevision=0;this.visualColorOverrides=new Map();this.visualColorOverridesRevision=0;this.visualMassState=null;this.viewerMassTransparencyState=null;this.dataMassTransparencyState=null;this.aiVisualState=null;this.visualWorksStates=new Map();this.visualClashStates=new Map();this.visualFadeStates=new Map();this.visualSelectionPreserveAppearance=false;this.visualHoverKey=null;this.hoverOverlayGroup=null;this.hoverOverlayObjects=[];this.hoverOverlayKey=null;this.hoverOverlaySource=null;this.hoverOverlayGeometryCache=new Map();this.hoverVisualMutatesGeometry=false;this.visualInteractionOverlayOnly=true;this.visualSelectionOverlayOnly=false;this.visualSelectionUseSourceAppearance=false;this.selectionOverlayKeyThreshold=24;this.selectionOverlayMeshThreshold=48;this.selectionBatchedOverlayActive=false;this._selectionOverlayModeChanged=false;this.selectionOverlayDirectMeshLimit=12;this.selectionOverlayMaxVerticesPerChunk=120000;this.selectionOverlayMaxIndicesPerChunk=420000;this.selectionOverlaySignature=null;this.interactionOverlayGroup3D=null;this.interactionOverlayObjectsByKey=new Map();this.interactionOverlayActiveKeys=new Set();this.selectionOverlayGroup3D=null;this.selectionOverlayObjects3D=[];this.selectionOverlayKeys3D=new Set();this.rectanglePreviewOverlayBatchThreshold=120;this.rectanglePreviewOverlayGroup3D=null;this.rectanglePreviewOverlayObjects3D=[];this.rectanglePreviewOverlaySignature=null;this.rectanglePreviewUsesBatch=false;this.visualDirtySectionPending=false;this.sectionCutEdgeDeferredTimer=null;this.sectionCutEdgeDeferredTimerType=null;this.sectionCutEdgeDeferredToken=0;this.sectionBoxLastTargetHadSelection=false;this.massTransparencyMaterialStates=new Map();this.massTransparencyEdgeStates=new Map();this.worksProgressMaterialStates=new Map();this.worksProgressEdgeStates=new Map();this.worksProgressVisibilityStates=new Map();this.worksProgressFadedMeshKeys=new Set();this.worksSelectionAppearanceLock=false;this.clashMaterialStates=new Map();this.clashRenderOrderStates=new Map();this.clashEdgeStates=new Map();this.clashEdgeMaterialByKey=new Map();this.clashPriorityKeys=new Set();this.activeClashContextSignature=null;this.activeClashContextProjectIds=new Set();this.activeClashContextVisibleKeys=new Set();this.activeClashPairKeys=new Set();this.activeClashReleasedKeys=new Set();this.clashModeActive=false;this.clashNormalChunksSuspended=false;this.clashPreviousVisualSelectionOverlayOnly=null;this.clashPreviousVisualInteractionOverlayOnly=null;this.clashPreviousHoverVisualMutatesGeometry=null;this.clashBoxHelper=null;this.clashContactZoneGroup=null;this.fadeActive=false;this.sectionBoxActive=false;this.sectionBounds=null;this.sectionLocalBounds=null;this.sectionBoxFrame=null;this.sectionBoxSource=null;this.sectionPlanes=[];this.sectionBoxGroup=null;this.clearSectionCaps();this.sectionHandles=[];this.sectionRotationHandles=[];this.sectionRotationAxis='Z';this.sectionActiveRotationAxis=null;this.sectionDrag=null;this.sectionActiveFace=null;this.selectedKey=null;this.selectedKeys=new Set();this.hoveredMesh=null;this.hoveredOriginalMaterials=null;this.hoveredEdgeStates=null;this.preselectTimer=null;this.preselectPendingPoint=null;this.preselectLastRun=0;this.preselectDelayMs=90;this.isolatedKey=null;this.isolateActive=false;this.isolatedKeys=new Set();this.hiddenKeys=new Set();this.visibilityPredicate=null;this.dataPanelFilterActive=false;this.dataPanelFilterKeys=new Set();this.baseIsolationActive=false;this.baseIsolationKeys=new Set();this.visibleCategories=new Set();this.visualTransactionDepth=0;this.visualTransactionLabelStack=[];this.visualTransactionChangedKeys=new Set();this.visualTransactionRenderReason=null;this.visualTransactionRefreshAllChunks=false;this.visualTransactionStartedMs=0;this.responsiveVisualOperationSequence=0;this.activeResponsiveVisualOperationToken=0;this.responsiveVisualTransactionPromise=null;this.mouseMode=null;this.dragging=false;this.dragDistance=0;this.last={x:0,y:0};this.down={x:0,y:0};this.selectionBoxActive=false;this.selectionRect=null;this.selectionOperation='replace';this.areaZoomPending=false;this.areaZoomRect=null;this.selectionOverlayMode='selection';if(!window.BimSelectionController)throw new Error('No se pudo cargar el sistema modular de selección.');this.selectionController=window.BimSelectionController.create();this.selectionController.attach(this);this.selectionOverlay=this.createSelectionOverlay();this.viewerShortcutBuffer='';this.viewerShortcutTimer=null;this.viewerShortcutLastTime=0;this.viewerShortcutTimeoutMs=850;this.rectanglePreviewMeshes=new Set();this.rectanglePreviewKeys=new Set();this.rectanglePreviewTimer=null;this.rectanglePreviewPendingRect=null;this.rectanglePreviewLastRun=0;this.rectanglePreviewLargeModelLimit=Number.POSITIVE_INFINITY;this.rectanglePreviewDelayMs=220;this.selectionWorldBoxCache=new WeakMap();this.selectionCandidateCache=null;this.selectionCandidateCacheRevision=0;this.selectionCandidateCacheVersion=-1;this.raycastViewRevision=0;this.raycastScreenCandidateCache=null;this.raycastScreenPrefilterEnabled=true;this.raycastPrefilterMinCandidates=1200;this.raycastPrefilterCellSize=96;this.raycastPrefilterPaddingPx=6;this.rectanglePreviewMaxVisualKeys=Number.POSITIVE_INFINITY;this.measureActive=false;this.measurements=[];this.activeMeasurementIndex=-1;this.measurePoints=[];this.measurePreview=null;this.measureDragIndex=null;this.measureDragMeasurementIndex=null;this.measureSnapCycleIndex=0;this.measureLastMouse=null;this.measurePickCache=null;this.measureSurfaceCandidateCache=null;this.measureSurfaceCandidateCacheRevision=-1;this.measureDirectLineCandidateCache=null;this.measureDirectLineCandidateCacheRevision=-1;this.measureRaycastThrottleMs=12;this.measureLastRaycastAt=0;this.measurePreviewTimer=null;this.measurePreviewPending=null;this.measurePreviewLastRun=0;this.measureReferenceGroup=null;this.measureReferenceSignature='';this.measureHighlightTarget=null;this.measureHighlightStates=[];this.touchState=null;this.measureScene=new THREE.Scene();this.measureGroup=null;this.measureLine=null;this.measureMarkers=[];this.measureOverlay=this.createMeasureOverlay();this.measurementOverlayLastSignature='';this.measurementOverlayLastViewRevision=-1;this.elevationActive=false;this.elevationPlacementActive=false;this.elevationMarkers=[];this.activeElevationIndex=-1;this.elevationDragIndex=null;this.measureHighlightMaterial=new THREE.MeshBasicMaterial({color:0x90ee90,transparent:true,opacity:0.30,depthTest:true,depthWrite:false,side:THREE.DoubleSide});this.viewCube=null;this.loadOperationId=0;this.target=new THREE.Vector3(0,0,0);this.orbitPivot=null;this.lastSelectionOrbitPivot=null;this.lastSelectedElementKey=null;this.isolationRenderSnapshot=null;this.distance=30;this.theta=-0.75;this.phi=1.1;this.orthoScale=30;this.projectionMode='orthographic';this.navigationMode='orbit';this.allowWalkThrough=true;this.walkSpeed='medium';this.perspectiveFov=55;this.viewportFramingReferenceAspect=null;this.viewportFramingLastAspect=null;this.viewportFramingEnabled=true;this.freeLookYaw=0;this.freeLookPitch=0;this.freeLookSensitivity=0.0016;this.lastNavigationPointer=null;this.scene=new THREE.Scene();this.scene.background=new THREE.Color(0xffffff);this.plansOverlayScene=new THREE.Scene();this.plansOverlayCutPlane=null;this.sectionOverlayScene=new THREE.Scene();this.sectionCapScene=new THREE.Scene();this.sectionCapPlaneMeshes=[];this.sectionCapGroup=null;this.sectionCutEdgeGroup=null;this.sectionCutFillColor=0xa8a8a8;this.sectionCutFillOpacity=1;this.sectionCutEdgeColor=0x111827;this.sectionCutEdgeMaxSegments=180000;this.sectionHandleBaseSize=null;this.sectionEdgeReferenceOrthoScale=null;this.sectionEdgeReferenceRadius=null;this.sectionEdgeTargetPixels=null;this.sectionDragFastVisual=null;this.sectionDragFastVisualActive=false;this.sectionDragLastRenderRequestMs=0;this.sectionDragRenderSettleMs=420;this.levelGrid=null;this.levelGridVisible=false;this.camera=new THREE.OrthographicCamera(-1,1,1,-1,0.01,100000);this.renderer=new THREE.WebGLRenderer({canvas:this.canvas,antialias:true,stencil:true,powerPreference:'high-performance',preserveDrawingBuffer:false});this.baseRendererPixelRatio=Math.min(window.devicePixelRatio||1,2);this.renderer.setPixelRatio(this.baseRendererPixelRatio);this.navigationPerformanceActive=false;this.navigationPerformanceRestoreTimer=null;this.navigationEdgeVisibilityStates=new Map();this.renderer.setClearColor(0xffffff,1);this.renderer.localClippingEnabled=true;this.generalSettings=null;this.visualDetailPreference='auto';this.visualDetailLevel='high';this.currentModel=null;this.applyGeneralSettings(window.BimGeneralSettings?window.BimGeneralSettings.getSettings():null,true);window.addEventListener('bim-general-settings-changed',event=>this.applyGeneralSettings(event.detail));this.raycaster=new THREE.Raycaster();this.raycaster.params.Line.threshold=0.08;this.edgeDetectionAngle=2;this.specialtyEdgeDetectionAngle=18;this.performanceRenderMode=false;this.renderPerformanceStats=null;this.generatedEdgeObjectCount=0;this.embeddedLineObjectCount=0;this.generatedEdgeObjectBudget=Number.POSITIVE_INFINITY;this.embeddedLineObjectBudget=Number.POSITIVE_INFINITY;this.pointer=new THREE.Vector2();this.selectionMaterial=new THREE.MeshBasicMaterial({color:0xff4d4d,side:THREE.DoubleSide,transparent:true,opacity:0.5,depthWrite:false,depthTest:true});this.hoverMaterial=new THREE.MeshBasicMaterial({color:0xff4d4d,side:THREE.DoubleSide,transparent:true,opacity:0.38,depthWrite:false,depthTest:true});this.rectanglePreviewMaterial=new THREE.MeshBasicMaterial({color:0xff4d4d,side:THREE.DoubleSide,transparent:true,opacity:0.38,depthWrite:false,depthTest:true});for(const overlayMaterial of[this.selectionMaterial,this.hoverMaterial,this.rectanglePreviewMaterial]){if(!overlayMaterial)continue;overlayMaterial.polygonOffset=true;overlayMaterial.polygonOffsetFactor=-1;overlayMaterial.polygonOffsetUnits=-1;}this.edgeMaterial=new THREE.LineBasicMaterial({color:0x111827,transparent:true,opacity:0.92,depthTest:true,depthWrite:false});this.hoverEdgeMaterial=new THREE.LineBasicMaterial({color:0xb91c1c,transparent:true,opacity:1,depthTest:true,linewidth:3});this.rectanglePreviewEdgeMaterial=new THREE.LineBasicMaterial({color:0xb91c1c,transparent:true,opacity:1,depthTest:true,depthWrite:false});this.selectionEdgeMaterial=new THREE.LineBasicMaterial({color:0xb91c1c,transparent:true,opacity:1,depthTest:true});this.fadedEdgeMaterial=new THREE.LineBasicMaterial({color:0xbebebe,transparent:false,opacity:1,depthTest:true,depthWrite:false});this.noMaterialColor=0x9ca3af;this.transparentCurtainHostColor=0x9ca3af;this.transparentCurtainHostOpacity=0;this.transparentCurtainHostEdgeOpacity=0;this.nonRenderedDataElementMaterial=null;this.nonRenderedDataElementLineMaterial=null;this.lineMaterialByKey=new Map();this.uiInteractionUntil=0;this.uiInteractionIdleMs=220;this.uiRenderThrottleMs=48;this.lastUiThrottledRenderTime=0;this.renderPending=false;this.renderLoopRunning=false;this.renderRequestReason='initial';this.renderSettleUntil=0;this.renderSettleMs=90;this.renderLoopActivations=0;this.renderDemandReady=true;this.addSceneHelpers();this.initViewCube();this.bindEvents();this.resize();this.applyCamera();this.requestRender('initial');}getGeneralSettings(){if(window.BimGeneralSettings&&window.BimGeneralSettings.getSettings)return window.BimGeneralSettings.getSettings();return this.generalSettings||{viewerBackgroundColor:'#ffffff',projectionMode:'orthographic',navigationMode:'orbit',allowWalkThrough:true,walkSpeed:'medium',transparencyColor:'#bebebe',transparencyPercent:85,visualDetailLevel:'high',predefinedColors:[]};}getConfiguredTransparencyColor(fallbackColor){if(window.BimGeneralSettings&&window.BimGeneralSettings.getTransparencyColorNumber){return window.BimGeneralSettings.getTransparencyColorNumber();}const settings=this.getGeneralSettings();if(window.BimGeneralSettings&&window.BimGeneralSettings.hexToNumber){return window.BimGeneralSettings.hexToNumber(settings.transparencyColor,fallbackColor==null?0xbebebe:fallbackColor);}return fallbackColor==null?0xbebebe:fallbackColor;}getConfiguredTransparencyPercent(fallbackPercent){if(window.BimGeneralSettings&&window.BimGeneralSettings.getTransparencyPercent)return window.BimGeneralSettings.getTransparencyPercent();const settings=this.getGeneralSettings();const value=Number(settings&&settings.transparencyPercent);return Number.isFinite(value)?Math.max(0,Math.min(100,value)):(fallbackPercent==null?85:fallbackPercent);}getConfiguredTransparencyOpacity(minOpacity,fallbackTransparencyPercent){if(window.BimGeneralSettings&&window.BimGeneralSettings.getTransparencyOpacity){return window.BimGeneralSettings.getTransparencyOpacity(minOpacity==null?0.02:minOpacity);}const percent=this.getConfiguredTransparencyPercent(fallbackTransparencyPercent==null?85:fallbackTransparencyPercent);const minimum=Number.isFinite(Number(minOpacity))?Number(minOpacity):0.02;return Math.max(minimum,Math.min(1,1-(percent/100)));}getConfiguredViewerBackgroundColor(){if(window.BimGeneralSettings&&window.BimGeneralSettings.getViewerBackgroundColorNumber){return window.BimGeneralSettings.getViewerBackgroundColorNumber();}return 0xffffff;}normalizeProjectionMode(value){const text=String(value||'').toLowerCase();return text==='perspective'?'perspective':'orthographic';}normalizeNavigationMode(value){const text=String(value||'').toLowerCase();return text==='free'?'free':'orbit';}normalizeWalkSpeed(value){const text=String(value||'').toLowerCase();return text==='low'||text==='high'?text:'medium';}getProjectionModeFromSettings(settings){if(settings&&settings.projectionMode)return this.normalizeProjectionMode(settings.projectionMode);if(window.BimGeneralSettings&&window.BimGeneralSettings.getProjectionMode)return this.normalizeProjectionMode(window.BimGeneralSettings.getProjectionMode());return'orthographic';}getNavigationModeFromSettings(settings){if(settings&&settings.navigationMode)return this.normalizeNavigationMode(settings.navigationMode);if(window.BimGeneralSettings&&window.BimGeneralSettings.getNavigationMode)return this.normalizeNavigationMode(window.BimGeneralSettings.getNavigationMode());return'orbit';}getAllowWalkThroughFromSettings(settings){if(settings&&typeof settings.allowWalkThrough!=='undefined')return!!settings.allowWalkThrough;if(window.BimGeneralSettings&&window.BimGeneralSettings.getAllowWalkThrough)return!!window.BimGeneralSettings.getAllowWalkThrough();return true;}getWalkSpeedFromSettings(settings){if(settings&&settings.walkSpeed)return this.normalizeWalkSpeed(settings.walkSpeed);if(window.BimGeneralSettings&&window.BimGeneralSettings.getWalkSpeed)return this.normalizeWalkSpeed(window.BimGeneralSettings.getWalkSpeed());return'medium';}normalizeVisualDetailLevel(value){const text=String(value||'').toLowerCase();if(text==='low'||text==='bajo')return'medium';if(text==='medium'||text==='medio')return'medium';if(text==='high'||text==='alto')return'high';return'high';}normalizeVisualDetailPreference(value){const text=String(value||'').toLowerCase();if(text==='auto'||text==='automatic'||text==='automático'||text==='automatico')return'auto';return this.normalizeVisualDetailLevel(text);}resolveAdaptiveVisualDetailLevel(model){const geometries=model&&Array.isArray(model.geometries)?model.geometries:[];const elements=model&&Array.isArray(model.elements)?model.elements:[];let totalVertices=0;for(let i=0;i<geometries.length;i++){totalVertices+=getGeometryVertexCount(geometries[i]);if(totalVertices>6000000)break;}if((model&&model.largeModelMode)||geometries.length>8000||elements.length>8000||totalVertices>650000)return'medium';return'high';}getVisualDetailLevelFromSettings(settings){const preference=settings&&settings.visualDetailLevel?this.normalizeVisualDetailPreference(settings.visualDetailLevel):(window.BimGeneralSettings&&window.BimGeneralSettings.getVisualDetailLevel?this.normalizeVisualDetailPreference(window.BimGeneralSettings.getVisualDetailLevel()):'auto');this.visualDetailPreference=preference;if(preference==='auto')return this.resolveAdaptiveVisualDetailLevel(this.currentModel);return this.normalizeVisualDetailLevel(preference);}getVisualDetailLevel(){return this.normalizeVisualDetailLevel(this.visualDetailLevel||'high');}applyAdaptiveVisualDetailForModel(model){if(this.normalizeVisualDetailPreference(this.visualDetailPreference)!=='auto')return this.getVisualDetailLevel();const previous=this.getVisualDetailLevel();const next=this.resolveAdaptiveVisualDetailLevel(model);this.visualDetailLevel=next;if(previous!==next&&typeof window!=='undefined'){window.dispatchEvent(new CustomEvent('bim-adaptive-quality-changed',{detail:{preference:'auto',level:next,previousLevel:previous}}));}return next;}isPerspectiveMode(){return this.projectionMode==='perspective'||(this.camera&&this.camera.isPerspectiveCamera);}isFreeNavigationMode(){return this.isPerspectiveMode()&&this.navigationMode==='free';}getCanvasAspect(){const element=this.renderer?this.renderer.domElement:this.canvas;const width=Math.max(1,element&&(element.clientWidth||this.canvas.clientWidth)||1);const height=Math.max(1,element&&(element.clientHeight||this.canvas.clientHeight)||1);return width/height;}createCameraForProjection(mode){if(mode==='perspective'){return new THREE.PerspectiveCamera(this.perspectiveFov||55,this.getCanvasAspect(),0.01,100000);}return new THREE.OrthographicCamera(-1,1,1,-1,0.01,100000);}switchProjectionMode(mode){const nextMode=this.normalizeProjectionMode(mode);const currentIsPerspective=this.camera&&this.camera.isPerspectiveCamera;const previousMode=currentIsPerspective?'perspective':'orthographic';if((nextMode==='perspective')===!!currentIsPerspective){this.projectionMode=nextMode;this.updateCameraClipRange();this.updateCameraProjection();return{changed:false,previousMode,nextMode};}const oldCamera=this.camera;const position=oldCamera?oldCamera.position.clone():new THREE.Vector3(0,0,1);const up=oldCamera?oldCamera.up.clone():new THREE.Vector3(0,1,0);const target=this.target?this.target.clone():new THREE.Vector3();const near=oldCamera?oldCamera.near:0.01;const far=oldCamera?oldCamera.far:100000;this.camera=this.createCameraForProjection(nextMode);this.camera.position.copy(position);this.camera.up.copy(up.lengthSq&&up.lengthSq()>1e-8?up.normalize():new THREE.Vector3(0,1,0));this.camera.near=near;this.camera.far=far;this.camera.lookAt(target);this.projectionMode=nextMode;this.syncOrbitAnglesFromCamera();this.syncFreeLookAnglesFromCamera();this.updateCameraClipRange();this.updateCameraProjection();this.updateViewCubeOrientation();return{changed:true,previousMode,nextMode};}resetViewAfterProjectionModeChange(){this.orbitPivot=null;if(this.meshes&&this.meshes.length){this.fitToModel();}else{this.updateCameraClipRange();this.updateCameraProjection();this.updateViewCubeOrientation();}}applyNavigationSettings(settings){this.navigationMode=this.isPerspectiveMode()?this.getNavigationModeFromSettings(settings):'orbit';this.allowWalkThrough=this.getAllowWalkThroughFromSettings(settings);this.walkSpeed=this.getWalkSpeedFromSettings(settings);if(this.isFreeNavigationMode())this.syncFreeLookAnglesFromCamera();this.updateCursor();}applyGeneralSettings(settings,initial){this.generalSettings=settings||this.getGeneralSettings();const previousVisualDetailLevel=this.getVisualDetailLevel();this.visualDetailLevel=this.getVisualDetailLevelFromSettings(this.generalSettings);const visualDetailChanged=previousVisualDetailLevel!==this.visualDetailLevel;const projectionChange=this.switchProjectionMode(this.getProjectionModeFromSettings(this.generalSettings));this.applyNavigationSettings(this.generalSettings);if(!initial&&projectionChange&&projectionChange.changed&&projectionChange.previousMode==='perspective'&&projectionChange.nextMode==='orthographic'){this.resetViewAfterProjectionModeChange();}const backgroundColor=this.getConfiguredViewerBackgroundColor();if(this.scene)this.scene.background=new THREE.Color(backgroundColor);if(this.renderer&&this.renderer.setClearColor)this.renderer.setClearColor(backgroundColor,1);const transparentColor=this.getConfiguredTransparencyColor();const transparentOpacity=this.getConfiguredTransparencyOpacity(0.03);let changedKeys=new Set();if(this.visualFadeStates&&this.visualFadeStates.size){for(const[key,state]of this.visualFadeStates.entries()){if(!state)continue;state.color=transparentColor;state.opacity=transparentOpacity;changedKeys.add(key);}}if(this.visualMassState&&this.visualMassState.active){this.visualMassState.fadedColor=transparentColor;this.visualMassState.fadedOpacity=this.getConfiguredTransparencyOpacity(0.02);for(const key of this.visualMassState.allKeys||[])changedKeys.add(key);}if(this.visualClashStates&&this.visualClashStates.size){for(const[key,state]of this.visualClashStates.entries()){if(state&&state.generalTransparency){state.color=transparentColor;state.opacity=this.getConfiguredTransparencyOpacity(0.02);state.edgeColor=this.darkenColorValue(transparentColor,0.3);state.edgeOpacity=Math.max(0.04,state.opacity);changedKeys.add(key);}}}if(changedKeys.size)this.updateVisualAppearanceForKeys(changedKeys);if(!initial&&visualDetailChanged)this.rebuildGeneratedEdgesForVisualDetail(previousVisualDetailLevel);if(!initial)this.renderOnce();}addSceneHelpers(){}bindEvents(){window.addEventListener('resize',()=>this.resize());this.canvas.addEventListener('contextmenu',event=>event.preventDefault());if(!this.canvas.hasAttribute('tabindex'))this.canvas.setAttribute('tabindex','0');const markUiInteraction=(event)=>this.markExternalUiInteraction(event);document.addEventListener('pointerdown',markUiInteraction,true);document.addEventListener('focusin',markUiInteraction,true);document.addEventListener('input',markUiInteraction,true);document.addEventListener('keydown',markUiInteraction,true);document.addEventListener('wheel',markUiInteraction,true);document.addEventListener('scroll',markUiInteraction,true);this.canvas.addEventListener('mouseenter',()=>this.clearExternalUiInteraction());this.canvas.addEventListener('mousedown',(event)=>{this.clearExternalUiInteraction();this.beginNavigationPerformanceMode('mouse');this.focusViewerCanvas();event.preventDefault();this.dragging=true;this.dragDistance=0;this.last={x:event.clientX,y:event.clientY};this.down={x:event.clientX,y:event.clientY};const rect=this.canvas.getBoundingClientRect();this.updateLastNavigationPointer(event.clientX,event.clientY);if(event.button===0&&this.areaZoomPending){this.startAreaZoomInteraction(event.clientX,event.clientY);this.updateCursor();return;}const sectionHandle=event.button===0?this.pickSectionHandle(event.clientX-rect.left,event.clientY-rect.top):null;if(sectionHandle){const rotationAxis=sectionHandle.userData.sectionRotationAxis;if(rotationAxis){this.mouseMode='section-rotate';this.beginSectionRotation(rotationAxis,event.clientX-rect.left,event.clientY-rect.top);}else{this.mouseMode='section';this.sectionDrag={face:sectionHandle.userData.sectionFace};this.sectionActiveFace=sectionHandle.userData.sectionFace;this.updateSectionBoxVisual();}this.forceSectionDragRender&&this.forceSectionDragRender('section-start');this.updateCursor();return;}if(event.button===1){this.mouseMode=event.shiftKey?(this.isFreeNavigationMode()?'free-look':'orbit'):'pan';}else if(event.button===2){this.mouseMode=this.isFreeNavigationMode()?'free-look':'orbit';}else{if(this.elevationActive){const elevationHit=this.getElevationMarkerHitFromScreen(event.clientX-rect.left,event.clientY-rect.top);if(elevationHit){this.mouseMode='elevation-drag';this.elevationDragIndex=elevationHit.elevationIndex;this.activeElevationIndex=elevationHit.elevationIndex;}else if(this.elevationPlacementActive){this.mouseMode='elevation';this.updateElevationPreviewFromScreen(event.clientX-rect.left,event.clientY-rect.top);}else if(this.isFreeNavigationMode()){this.mouseMode='free-look';}else{this.mouseMode='select';this.selectionOperation=event.ctrlKey||event.metaKey?'add':(event.shiftKey?'remove':'replace');}}else if(this.measureActive){const pointHit=this.getMeasurePointHitFromScreen(event.clientX-rect.left,event.clientY-rect.top);if(pointHit){this.mouseMode='measure-drag';this.measureDragMeasurementIndex=pointHit.measurementIndex;this.measureDragIndex=pointHit.pointIndex;}else if(this.isMeasurePlacementActive()){this.mouseMode='measure';}else if(this.isFreeNavigationMode()){this.mouseMode='free-look';}else{this.mouseMode='select';this.selectionOperation=event.ctrlKey||event.metaKey?'add':(event.shiftKey?'remove':'replace');}}else if(this.isFreeNavigationMode()){this.mouseMode='free-look';}else{this.mouseMode='select';this.selectionOperation=event.ctrlKey||event.metaKey?'add':(event.shiftKey?'remove':'replace');}}this.updateCursor();});this.canvas.addEventListener('mousemove',(event)=>{this.clearExternalUiInteraction();if(this.dragging)return;const rect=this.canvas.getBoundingClientRect();this.updateLastNavigationPointer(event.clientX,event.clientY);if(this.elevationActive&&this.elevationPlacementActive){this.scheduleMeasurementPreviewFromScreen(event.clientX-rect.left,event.clientY-rect.top,'elevation');return;}if(this.measureActive&&this.isMeasurePlacementActive()){this.scheduleMeasurementPreviewFromScreen(event.clientX-rect.left,event.clientY-rect.top,'measure');return;}if(this.measureActive){this.clearMeasurePreview();this.restoreMeasureReferenceHighlight();}if(this.areaZoomPending){this.canvas.style.cursor='zoom-in';return;}const sectionHover=this.pickSectionHandle(event.clientX-rect.left,event.clientY-rect.top);if(sectionHover){if(this.cancelScheduledPreselect)this.cancelScheduledPreselect();this.restoreHoveredMaterial();this.canvas.style.cursor=sectionHover.userData.sectionRotationAxis?'grab':'move';return;}if(this.elevationActive&&!this.elevationPlacementActive&&this.getElevationMarkerHitFromScreen(event.clientX-rect.left,event.clientY-rect.top)){this.canvas.style.cursor='move';return;}this.canvas.style.cursor='default';if(this.schedulePreselectFromScreen)this.schedulePreselectFromScreen(event.clientX-rect.left,event.clientY-rect.top);else this.preselectFromScreen(event.clientX-rect.left,event.clientY-rect.top);});this.canvas.addEventListener('mouseleave',()=>{if(this.cancelScheduledMeasurementPreview)this.cancelScheduledMeasurementPreview();if(this.cancelScheduledPreselect)this.cancelScheduledPreselect();if(!this.dragging)this.restoreHoveredMaterial();if(!this.dragging&&this.measureActive){this.clearMeasurePreview();this.restoreMeasureReferenceHighlight();}if(!this.dragging&&this.elevationActive&&this.elevationPlacementActive){this.restoreMeasureReferenceHighlight();}});window.addEventListener('mouseup',()=>{this.scheduleNavigationPerformanceRestore();if(!this.dragging)return;if(this.mouseMode==='section'||this.mouseMode==='section-rotate'){this.finishSectionBoxInteraction();}else if(this.mouseMode==='area-zoom'){if(this.areaZoomRect)this.fitToAreaRectangle(this.areaZoomRect);this.cancelAreaZoomMode();}else if(this.mouseMode==='elevation'){const rect=this.canvas.getBoundingClientRect();if(this.dragDistance<4)this.commitElevationFromScreen(this.down.x-rect.left,this.down.y-rect.top);}else if(this.mouseMode==='elevation-drag'){this.elevationDragIndex=null;this.restoreMeasureReferenceHighlight();}else if(this.mouseMode==='select'){const rect=this.canvas.getBoundingClientRect();if(this.selectionBoxActive&&this.selectionRect){this.selectByRectangle(this.selectionRect,true);}else if(this.dragDistance<4){this.selectFromScreen(this.down.x-rect.left,this.down.y-rect.top);}}else if(this.mouseMode==='free-look'){const rect=this.canvas.getBoundingClientRect();if(this.dragDistance<4)this.selectFromScreen(this.down.x-rect.left,this.down.y-rect.top);}else if(this.mouseMode==='measure'){const rect=this.canvas.getBoundingClientRect();if(this.dragDistance<4)this.commitMeasurePointFromScreen(this.down.x-rect.left,this.down.y-rect.top);this.restoreMeasureReferenceHighlight();}else if(this.mouseMode==='measure-drag'){this.measureDragIndex=null;this.measureDragMeasurementIndex=null;this.clearMeasurePreview();this.restoreMeasureReferenceHighlight();}this.hideSelectionOverlay();this.dragging=false;this.mouseMode=null;this.updateCursor();});window.addEventListener('mousemove',(event)=>{if(!this.dragging)return;const dx=event.clientX-this.last.x;const dy=event.clientY-this.last.y;this.last={x:event.clientX,y:event.clientY};this.updateLastNavigationPointer(event.clientX,event.clientY);this.dragDistance+=Math.abs(dx)+Math.abs(dy);if(this.mouseMode==='section')this.dragSectionFace(dx,dy);else if(this.mouseMode==='section-rotate'){const rect=this.canvas.getBoundingClientRect();this.dragSectionRotation(event.clientX-rect.left,event.clientY-rect.top);}else if(this.mouseMode==='orbit')this.orbit(dx,dy);else if(this.mouseMode==='free-look')this.freeLook(dx,dy);else if(this.mouseMode==='pan')this.pan(dx,dy);else if(this.mouseMode==='area-zoom')this.updateAreaZoomOverlay(event.clientX,event.clientY);else if(this.mouseMode==='select')this.updateSelectionOverlay(event.clientX,event.clientY);else if(this.mouseMode==='elevation-drag'){const rect=this.canvas.getBoundingClientRect();this.dragElevationMarkerFromScreen(event.clientX-rect.left,event.clientY-rect.top);}else if(this.mouseMode==='measure-drag'){const rect=this.canvas.getBoundingClientRect();this.dragMeasurePointFromScreen(event.clientX-rect.left,event.clientY-rect.top);}});this.canvas.addEventListener('wheel',(event)=>{this.clearExternalUiInteraction();this.beginNavigationPerformanceMode('wheel');this.scheduleNavigationPerformanceRestore();event.preventDefault();this.updateLastNavigationPointer(event.clientX,event.clientY);const factor=event.deltaY>0?1.12:0.88;if(this.isPerspectiveMode()){if(this.isFreeNavigationMode()){this.zoomFreeCameraAtPointer(event.clientX,event.clientY,event.deltaY,event.shiftKey?2.5:1);}else{this.dollyPerspectiveAtPointer(event.clientX,event.clientY,factor);}}else{this.orthoScale=Math.max(0.05,this.orthoScale*factor);this.updateCameraClipRange();this.updateCameraProjection();}},{passive:false});this.canvas.addEventListener('dblclick',()=>{if(!this.measureActive)this.fitToSelectedOrModel();});window.addEventListener('keydown',(event)=>{const active=document.activeElement;const isTyping=active&&['INPUT','TEXTAREA','SELECT'].includes(active.tagName);if(!isTyping&&this.handleFreeNavigationKeydown(event))return;if(this.handleViewerShortcutKeydown(event,isTyping))return;if(event.key==='Tab'&&!isTyping&&((this.measureActive&&(this.isMeasurePlacementActive()||this.mouseMode==='measure-drag'))||this.elevationActive)){event.preventDefault();this.cycleMeasureSnapCandidate();return;}if(event.key==='Escape'&&!isTyping){event.preventDefault();if(this.measureActive){this.clearMeasurePreview();this.restoreMeasureReferenceHighlight();}this.cancelAreaZoomMode();this.hideSelectionOverlay();this.restoreRectanglePreview();this.clearSelection(true);}});this.canvas.addEventListener('touchstart',(event)=>this.handleTouchStart(event),{passive:false});this.canvas.addEventListener('touchmove',(event)=>this.handleTouchMove(event),{passive:false});this.canvas.addEventListener('touchend',(event)=>this.handleTouchEnd(event),{passive:false});this.canvas.addEventListener('touchcancel',(event)=>this.handleTouchEnd(event),{passive:false});window.addEventListener('blur',()=>this.restoreNavigationPerformanceMode());}markExternalUiInteraction(event){try{const target=event&&event.target;if(this.isViewerEventTarget(target))return;this.uiInteractionUntil=(typeof performance!=='undefined'?performance.now():Date.now())+this.uiInteractionIdleMs;}catch(_){}}clearExternalUiInteraction(){this.uiInteractionUntil=0;this.lastUiThrottledRenderTime=0;}isViewerEventTarget(target){if(!target)return false;if(target===this.canvas)return true;if(this.viewCube){if(this.viewCube.container&&this.viewCube.container.contains&&this.viewCube.container.contains(target))return true;if(this.viewCube.canvas&&this.viewCube.canvas.contains&&this.viewCube.canvas.contains(target))return true;}return false;}isExternalEditableFocused(){if(typeof document==='undefined')return false;const active=document.activeElement;if(!active||active===document.body||active===this.canvas)return false;if(this.isViewerEventTarget(active))return false;return this.isEditableShortcutTarget(active);}isViewerBusyWithDirectInteraction(){return!!(this.dragging||this.sectionDrag||this.mouseMode==='section'||this.mouseMode==='orbit'||this.mouseMode==='free-look'||this.mouseMode==='pan'||this.mouseMode==='select'||this.mouseMode==='area-zoom'||this.mouseMode==='measure'||this.mouseMode==='measure-drag'||this.mouseMode==='elevation'||this.mouseMode==='elevation-drag'||(this.measureActive&&this.isMeasurePlacementActive&&this.isMeasurePlacementActive())||(this.elevationActive&&this.elevationPlacementActive));}shouldThrottleRenderForUi(now){if(this.isViewerBusyWithDirectInteraction())return false;return this.isExternalEditableFocused()||now<this.uiInteractionUntil;}focusViewerCanvas(){try{if(this.canvas&&typeof this.canvas.focus==='function'){this.canvas.focus({preventScroll:true});}}catch(_){try{if(this.canvas&&typeof this.canvas.focus==='function')this.canvas.focus();}catch(_){}}}getWalkSpeedMultiplier(){if(this.walkSpeed==='low')return 0.45;if(this.walkSpeed==='high')return 2.25;return 1;}getFreeMoveStep(multiplier){const base=Math.max(0.18,Math.min(4.5,this.getFullModelDepthSize()*0.018));const extra=Number.isFinite(Number(multiplier))?Number(multiplier):1;return base*this.getWalkSpeedMultiplier()*extra;}getViewHeightAtDistance(distance){if(this.isPerspectiveMode()){const safeDistance=Math.max(0.05,Number(distance)||this.distance||1);const fov=THREE.MathUtils.degToRad((this.camera&&this.camera.fov)||this.perspectiveFov||55);return Math.max(0.001,2*Math.tan(fov/2)*safeDistance);}if(this.camera&&this.camera.isOrthographicCamera){const zoom=Math.max(0.000001,Number(this.camera.zoom)||1);const projectedHeight=Math.abs(Number(this.camera.top)-Number(this.camera.bottom))/zoom;if(Number.isFinite(projectedHeight)&&projectedHeight>0.000001)return projectedHeight;}return Math.max(0.05,Number(this.orthoScale)||1);}getWorldPerPixelAtPoint(point){const element=this.renderer?this.renderer.domElement:this.canvas;const height=Math.max(1,element&&(element.clientHeight||this.canvas.clientHeight)||1);let distance=this.distance;if(this.isPerspectiveMode()&&point&&this.camera){distance=Math.max(0.05,point.clone().sub(this.camera.position).length());}return this.getViewHeightAtDistance(distance)/height;}updateLastNavigationPointer(clientX,clientY){if(!this.canvas)return null;const rect=this.canvas.getBoundingClientRect();if(!rect||!rect.width||!rect.height)return null;const x=THREE.MathUtils.clamp(clientX-rect.left,0,rect.width);const y=THREE.MathUtils.clamp(clientY-rect.top,0,rect.height);this.lastNavigationPointer={x,y};return this.lastNavigationPointer;}getNavigationRaycastHitFromScreen(x,y){if(!this.camera||!this.canvas||!this.raycaster)return null;const rect=this.canvas.getBoundingClientRect();const width=Math.max(1,rect.width||this.canvas.clientWidth||1);const height=Math.max(1,rect.height||this.canvas.clientHeight||1);const sx=Number.isFinite(Number(x))?Number(x):width*0.5;const sy=Number.isFinite(Number(y))?Number(y):height*0.5;this.pointer.x=(sx/width)*2-1;this.pointer.y=-(sy/height)*2+1;this.raycaster.setFromCamera(this.pointer,this.camera);const surfaceObjects=(this.meshes||[]).filter(mesh=>mesh&&mesh.visible&&mesh.isMesh);const hits=this.filterRaycastHitsBySection(this.raycaster.intersectObjects(surfaceObjects,false));return this.getPreferredRaycastHit(hits)||(hits&&hits.length?hits[0]:null);}getPerspectiveNavigationReferenceDistance(){if(!this.camera)return Math.max(0.5,Number(this.distance)||1);const element=this.renderer?this.renderer.domElement:this.canvas;const width=Math.max(1,element&&(element.clientWidth||this.canvas.clientWidth)||1);const height=Math.max(1,element&&(element.clientHeight||this.canvas.clientHeight)||1);const pointer=this.lastNavigationPointer||{x:width*0.5,y:height*0.5};const hit=this.getNavigationRaycastHitFromScreen(pointer.x,pointer.y);if(hit&&hit.point)return Math.max(0.08,hit.point.distanceTo(this.camera.position));const targetDistance=Math.max(0.08,this.target?this.target.distanceTo(this.camera.position):Number(this.distance)||1);if(!this.isFreeNavigationMode())return targetDistance;const modelDepth=Math.max(1,this.getFullModelDepthSize());return Math.max(0.08,Math.min(targetDistance,modelDepth*0.08));}getPerspectiveWorldPerPixelForNavigation(){const element=this.renderer?this.renderer.domElement:this.canvas;const height=Math.max(1,element&&(element.clientHeight||this.canvas.clientHeight)||1);const distance=this.getPerspectiveNavigationReferenceDistance();return this.getViewHeightAtDistance(distance)/height;}syncFreeLookAnglesFromCamera(){if(!this.camera)return;const forward=this.camera.getWorldDirection(new THREE.Vector3()).normalize();if(forward.lengthSq()<1e-12)return;this.freeLookYaw=Math.atan2(forward.x,forward.z);this.freeLookPitch=Math.asin(THREE.MathUtils.clamp(forward.y,-0.985,0.985));}applyFreeLookCamera(){if(!this.camera)return;const maxPitch=Math.PI*0.495;this.freeLookPitch=THREE.MathUtils.clamp(this.freeLookPitch,-maxPitch,maxPitch);const cosPitch=Math.cos(this.freeLookPitch);const forward=new THREE.Vector3(Math.sin(this.freeLookYaw)*cosPitch,Math.sin(this.freeLookPitch),Math.cos(this.freeLookYaw)*cosPitch).normalize();const targetDistance=Math.max(0.5,Number(this.distance)||this.getFullModelDepthSize()*0.35||10);this.target.copy(this.camera.position).add(forward.clone().multiplyScalar(targetDistance));this.camera.up.set(0,1,0);this.camera.lookAt(this.target);this.syncOrbitAnglesFromCamera();this.syncFreeLookAnglesFromCamera();this.updateCameraClipRange();this.updateCameraProjection();this.updateViewCubeOrientation();}freeLook(dx,dy){if(!this.camera)return;if(!Number.isFinite(this.freeLookYaw)||!Number.isFinite(this.freeLookPitch))this.syncFreeLookAnglesFromCamera();const sensitivity=Number.isFinite(Number(this.freeLookSensitivity))?Number(this.freeLookSensitivity):0.0016;this.freeLookYaw-=dx*sensitivity;this.freeLookPitch-=dy*sensitivity;this.applyFreeLookCamera();}zoomFreeCameraAtPointer(clientX,clientY,deltaY,multiplier){if(!this.camera)return false;const pointer=this.updateLastNavigationPointer(clientX,clientY);const hit=pointer?this.getNavigationRaycastHitFromScreen(pointer.x,pointer.y):null;const zoomIn=deltaY<0;const directionSign=zoomIn?1:-1;const modelDepth=Math.max(1,this.getFullModelDepthSize());const speedFactor=this.getWalkSpeedMultiplier();const extra=Number.isFinite(Number(multiplier))?Number(multiplier):1;const forward=this.camera.getWorldDirection(new THREE.Vector3()).normalize();const direction=forward.multiplyScalar(directionSign);let referenceDistance=Math.max(0.08,this.target?this.target.distanceTo(this.camera.position):Number(this.distance)||1);if(hit&&hit.point){referenceDistance=Math.max(0.08,hit.point.distanceTo(this.camera.position));}const closeLimit=Math.max(0.04,modelDepth*0.0009);const farLimit=Math.max(closeLimit*8,modelDepth*0.045);let step=THREE.MathUtils.clamp(referenceDistance*(zoomIn?0.20:0.18),closeLimit,farLimit)*speedFactor*extra;if(zoomIn&&!this.allowWalkThrough&&hit&&hit.point){step=Math.min(step,Math.max(closeLimit,referenceDistance*0.55));}return this.moveCameraByDelta(direction.multiplyScalar(step),false);}dollyPerspectiveAtPointer(clientX,clientY,factor){if(!this.camera)return;const pointer=this.updateLastNavigationPointer(clientX,clientY);const hit=pointer?this.getNavigationRaycastHitFromScreen(pointer.x,pointer.y):null;if(!hit||!hit.point){this.dollyPerspectiveOrbit(factor);return;}const zoomIn=factor<1;const directionSign=zoomIn?1:-1;const toPoint=hit.point.clone().sub(this.camera.position);const referenceDistance=Math.max(0.08,toPoint.length());const direction=toPoint.normalize().multiplyScalar(directionSign);const modelDepth=Math.max(1,this.getFullModelDepthSize());const closeLimit=Math.max(0.04,modelDepth*0.0009);const farLimit=Math.max(closeLimit*8,modelDepth*0.045);let step=THREE.MathUtils.clamp(referenceDistance*Math.abs(1-factor),closeLimit,farLimit);if(zoomIn&&!this.allowWalkThrough){step=Math.min(step,Math.max(closeLimit,referenceDistance*0.55));}this.moveCameraByDelta(direction.multiplyScalar(step),false);this.syncOrbitAnglesFromCamera();}dollyPerspectiveOrbit(factor){if(!this.camera)return;const pivot=this.target.clone();const offset=this.camera.position.clone().sub(pivot);const currentDistance=Math.max(0.05,offset.length());const nextDistance=Math.max(0.05,currentDistance*factor);offset.normalize().multiplyScalar(nextDistance);const delta=pivot.clone().add(offset).sub(this.camera.position);this.moveCameraByDelta(delta,true);this.distance=nextDistance;this.camera.lookAt(this.target);this.syncOrbitAnglesFromCamera();this.updateCameraClipRange();this.updateCameraProjection();this.updateViewCubeOrientation();}handleFreeNavigationKeydown(event){if(!this.isFreeNavigationMode())return false;if(!this.isViewerShortcutScope(event))return false;const key=String(event.key||'').toLowerCase();let vector=null;if(key==='w'||key==='arrowup')vector=new THREE.Vector3(0,0,1);else if(key==='s'||key==='arrowdown')vector=new THREE.Vector3(0,0,-1);else if(key==='a'||key==='arrowleft')vector=new THREE.Vector3(-1,0,0);else if(key==='d'||key==='arrowright')vector=new THREE.Vector3(1,0,0);else if(key==='q'||key==='pagedown')vector=new THREE.Vector3(0,-1,0);else if(key==='e'||key==='pageup')vector=new THREE.Vector3(0,1,0);if(!vector)return false;event.preventDefault();this.beginNavigationPerformanceMode('keyboard');this.scheduleNavigationPerformanceRestore();this.moveFreeCamera(vector,event.shiftKey?2.5:1);return true;}moveFreeCamera(localVector,multiplier){if(!this.camera||!localVector)return false;this.camera.updateMatrixWorld(true);const forward=this.camera.getWorldDirection(new THREE.Vector3()).normalize();const right=new THREE.Vector3().setFromMatrixColumn(this.camera.matrixWorld,0).normalize();const up=new THREE.Vector3(0,1,0);const move=new THREE.Vector3();if(localVector.z)move.add(forward.multiplyScalar(localVector.z));if(localVector.x)move.add(right.multiplyScalar(localVector.x));if(localVector.y)move.add(up.multiplyScalar(localVector.y));if(move.lengthSq()<1e-12)return false;move.normalize().multiplyScalar(this.getFreeMoveStep(multiplier));return this.moveCameraByDelta(move,false);}moveCameraByDelta(delta,keepTargetFixed){if(!delta||delta.lengthSq()<1e-12||!this.camera)return false;if(!this.allowWalkThrough&&!this.canMoveCameraByDelta(delta))return false;this.camera.position.add(delta);if(!keepTargetFixed)this.target.add(delta);if(keepTargetFixed)this.camera.lookAt(this.target);else if(this.isFreeNavigationMode())this.applyFreeLookCamera();else this.applyCamera(false);return true;}canMoveCameraByDelta(delta){if(!this.camera||!delta||delta.lengthSq()<1e-12)return true;const distance=delta.length();const direction=delta.clone().normalize();const raycaster=new THREE.Raycaster(this.camera.position.clone(),direction,0.001,distance+Math.max(0.08,distance*0.2));try{const targets=this.meshes?this.meshes.filter(mesh=>mesh&&mesh.visible):[];if(!targets.length)return true;const hits=raycaster.intersectObjects(targets,true);if(!hits||!hits.length)return true;const first=hits.find(hit=>hit&&hit.distance>0.03);return!first;}catch(_){return true;}}isEditableShortcutTarget(target){if(!target)return false;const tag=String(target.tagName||'').toUpperCase();if(tag==='INPUT'||tag==='TEXTAREA'||tag==='SELECT'||tag==='OPTION')return true;if(target.isContentEditable)return true;const editableParent=typeof target.closest==='function'?target.closest('input, textarea, select, [contenteditable="true"], [contenteditable=""]'):null;return!!editableParent;}isViewerShortcutScope(event){if(!this.canvas||typeof document==='undefined')return false;if(this.isEditableShortcutTarget(event&&event.target))return false;const active=document.activeElement;return active===this.canvas;}resetViewerShortcutBuffer(){this.viewerShortcutBuffer='';this.viewerShortcutLastTime=0;if(this.viewerShortcutTimer){clearTimeout(this.viewerShortcutTimer);this.viewerShortcutTimer=null;}}setViewerToolButtonActive(buttonId,active){try{if(typeof document==='undefined')return;const button=document.getElementById(buttonId);if(button)button.classList.toggle('active',!!active);}catch(_){}}syncViewerToolButtons(){const aiIsolationActive=!!(this.aiVisualState&&this.aiVisualState.active&&this.aiVisualState.isolationKeys&&this.aiVisualState.isolationKeys.size);this.setViewerToolButtonActive('isolateButton',!!((this.isolateActive&&this.isolatedKeys&&this.isolatedKeys.size)||aiIsolationActive));this.setViewerToolButtonActive('hideSelectedButton',!!(this.hiddenKeys&&this.hiddenKeys.size));this.setViewerToolButtonActive('fadeUnselectedButton',!!(this.isViewerTransparencyActive&&this.isViewerTransparencyActive()));this.setViewerToolButtonActive('sectionBoxButton',!!this.sectionBoxActive);this.syncSectionRotationAxisButtons();}syncSectionRotationAxisButtons(){if(typeof document==='undefined')return;const activeAxis=String(this.sectionRotationAxis||'Z').toUpperCase();for(const axis of['X','Y','Z']){const button=document.getElementById('sectionRotate'+axis+'Button');if(!button)continue;button.classList.toggle('hidden',!this.sectionBoxActive);button.classList.toggle('active',!!this.sectionBoxActive&&activeAxis===axis);button.setAttribute('aria-pressed',activeAxis===axis?'true':'false');}}setSectionRotationAxis(axis){const normalized=String(axis||'').toUpperCase();if(!['X','Y','Z'].includes(normalized))return false;this.sectionRotationAxis=normalized;this.syncSectionRotationAxisButtons();if(this.sectionBoxActive&&this.updateSectionBoxVisual)this.updateSectionBoxVisual();if(this.requestRender)this.requestRender('section-rotation-axis');return true;}restoreHiddenKeysIncrementally(keys,options){const opts=options&&typeof options==='object'?options:{};const keySet=new Set(Array.from(keys||[]).filter(Boolean));if(!keySet.size)return{changedKeys:0,total:0};const metricsStart=this.getPerformanceNow?this.getPerformanceNow():Date.now();const massTransparencyActive=!!(this.visualMassState&&this.visualMassState.active);const stableMassEdges=massTransparencyActive&&this.massTransparencyUseStableEdges===true;const detachedSpatialKeysBeforeRestore=stableMassEdges&&this.getDetachedNormalChunkCellKeysForElementKeys?this.getDetachedNormalChunkCellKeysForElementKeys(keySet):new Set();if(!this.hiddenKeys)this.hiddenKeys=new Set();for(const key of keySet)this.hiddenKeys.delete(key);const targetMeshes=this.getMeshesForElementKeys?this.getMeshesForElementKeys(keySet):[];const changedKeys=new Set();for(const mesh of targetMeshes||[]){if(!mesh||!mesh.userData)continue;const key=this.getMeshKey?this.getMeshKey(mesh):mesh.userData.elementKey;const nextVisible=this.computeBaseVisibilityForMesh?this.computeBaseVisibilityForMesh(mesh):true;if(mesh.userData.baseVisible!==nextVisible){mesh.userData.baseVisible=nextVisible;if(key)changedKeys.add(key);}else if(nextVisible&&key){changedKeys.add(key);}}let restoredMassChunks=0;let rebuiltMassChunks=0;let restoredMassEdgeCells=0;if(changedKeys.size){this.baseVisibilityRevision=(this.baseVisibilityRevision||0)+1;if(this.updateVisualAppearanceForKeys)this.updateVisualAppearanceForKeys(changedKeys,{skipChunkRefresh:true,skipSectionRefresh:opts.skipSectionRefresh===true});if(massTransparencyActive&&this.visualMassState&&this.visualMassState.active){this.visualMassState.baseVisibilityRevision=this.baseVisibilityRevision||0;if(this.viewerMassTransparencyState&&this.viewerMassTransparencyState.active){this.viewerMassTransparencyState.baseVisibilityRevision=this.baseVisibilityRevision||0;}if(this.dataMassTransparencyState&&this.dataMassTransparencyState.active){this.dataMassTransparencyState.baseVisibilityRevision=this.baseVisibilityRevision||0;}const massRestore=this.restoreMassTransparencyChunkKeys?this.restoreMassTransparencyChunkKeys(changedKeys):{changedChunks:0,requiresRebuild:true};restoredMassChunks=Number(massRestore&&massRestore.changedChunks||0);if(massRestore&&massRestore.requiresRebuild){if(this.clearMassTransparencyChunks)this.clearMassTransparencyChunks(true);this.massTransparencyChunkCacheSignature=null;rebuiltMassChunks=this.rebuildMassTransparencyChunks?Number(this.rebuildMassTransparencyChunks(true)||0):0;}if(stableMassEdges&&detachedSpatialKeysBeforeRestore.size&&this.detachedNormalChunkCells&&this.rebuildNormalChunkEdgesFromMeshes){for(const spatialKey of detachedSpatialKeysBeforeRestore){const cellSources=this.detachedNormalChunkCells.get(spatialKey);if(!cellSources||!cellSources.size)continue;const visibleSources=Array.from(cellSources).filter(mesh=>mesh&&mesh.isMesh&&mesh.userData&&mesh.userData.baseVisible!==false&&!(this.isNonRenderedDataMesh&&this.isNonRenderedDataMesh(mesh)));if(!visibleSources.length)continue;this.rebuildNormalChunkEdgesFromMeshes(visibleSources,false,{appendExisting:true,forcedSpatialChunkKey:spatialKey});restoredMassEdgeCells++;}if(restoredMassEdgeCells&&this.applyStableNormalChunkEdgeAppearance){this.applyStableNormalChunkEdgeAppearance('mass');}}else if(!stableMassEdges&&this.rebuildGroupedTransparentEdges){this.groupedTransparentEdgeCacheSignature=null;this.rebuildGroupedTransparentEdges(true);}}}if(this.updateSelectionIndexesForVisibilityChange){this.updateSelectionIndexesForVisibilityChange(targetMeshes,true,'restore-hidden-incremental');}if(this.invalidateMeasurementPickCache)this.invalidateMeasurementPickCache('restore-hidden-incremental');if(changedKeys.size&&this.scheduleDetachedNormalChunkCellRefresh&&!(this.visualMassState&&this.visualMassState.active)){this.scheduleDetachedNormalChunkCellRefresh(changedKeys,'restore-hidden-incremental',180);}if(this.requestRender)this.requestRender('restore-hidden-incremental');if(this.recordPerformanceOperation)this.recordPerformanceOperation('restoreHidden:incremental',(this.getPerformanceNow?this.getPerformanceNow():Date.now())-metricsStart,{keys:keySet.size,changedKeys:changedKeys.size,targetMeshes:targetMeshes.length,massTransparencyActive,restoredMassChunks,rebuiltMassChunks,restoredMassEdgeCells});return{changedKeys:changedKeys.size,total:targetMeshes.length};}resetHiddenAndIsolation(){const hadAiBaseIsolation=!!(this.aiVisualState&&this.aiVisualState.active&&this.aiVisualState.isolationKeys&&this.aiVisualState.isolationKeys.size);const hadState=!!(this.isolateActive||(this.isolatedKeys&&this.isolatedKeys.size)||(this.hiddenKeys&&this.hiddenKeys.size)||hadAiBaseIsolation);const hadIsolation=!!(this.isolateActive||(this.isolatedKeys&&this.isolatedKeys.size));const hiddenBefore=new Set(this.hiddenKeys||[]);if(!hadIsolation&&hiddenBefore.size&&!hadAiBaseIsolation){this.restoreHiddenKeysIncrementally(hiddenBefore);this.syncViewerToolButtons();return false;}this.isolateActive=false;if(!this.isolatedKeys)this.isolatedKeys=new Set();this.isolatedKeys.clear();if(!this.hiddenKeys)this.hiddenKeys=new Set();if(hadAiBaseIsolation){this.baseIsolationActive=false;this.baseIsolationKeys=new Set();this.aiVisualState.isolationKeys=new Set();this.aiVisualState.active=!!((this.aiVisualState.colorKeys&&this.aiVisualState.colorKeys.size)||(this.aiVisualState.transparencyKeys&&this.aiVisualState.transparencyKeys.size));}if(hadIsolation&&this.restoreIsolationRenderSnapshotFast&&this.restoreIsolationRenderSnapshotFast('reset-hidden-isolation-fast')){if(hiddenBefore.size)this.restoreHiddenKeysIncrementally(hiddenBefore);this.syncViewerToolButtons();return false;}this.hiddenKeys.clear();if(hadState){this.applyVisibilityState(null,{forceChunkRebuild:true,reason:'reset-hidden-isolation'});if(this.endStrictIsolationVisibilityGuard)this.endStrictIsolationVisibilityGuard('reset-hidden-isolation');if(this.syncInteractiveNormalChunkPeel)this.syncInteractiveNormalChunkPeel();if(this.scheduleNormalChunkEdgesForVisibleChunks)this.scheduleNormalChunkEdgesForVisibleChunks('reset-hidden-isolation');}this.syncViewerToolButtons();return false;}async resetHiddenAndIsolationWithProgress(options){const opts=options&&typeof options==='object'?options:{};const report=typeof opts.onProgress==='function'?opts.onProgress:()=>{};const hadIsolation=!!(this.isolateActive||(this.isolatedKeys&&this.isolatedKeys.size));const hadHidden=!!(this.hiddenKeys&&this.hiddenKeys.size);const hadAiBaseIsolation=!!(this.aiVisualState&&this.aiVisualState.active&&this.aiVisualState.isolationKeys&&this.aiVisualState.isolationKeys.size);const hiddenBeforeIsolationReset=new Set(this.hiddenKeys||[]);if(!hadIsolation&&!hadHidden&&!hadAiBaseIsolation)return false;if(!hadIsolation&&hadHidden&&!hadAiBaseIsolation){const hiddenBefore=new Set(this.hiddenKeys||[]);const targetMeshes=this.getMeshesForElementKeys?this.getMeshesForElementKeys(hiddenBefore):[];report({message:'Restaurando elementos ocultos...',current:0,total:Math.max(1,targetMeshes.length)});if(opts.yieldBeforeWork!==false&&this.waitForResetProgressPaint)await this.waitForResetProgressPaint();const result=this.restoreHiddenKeysIncrementally(hiddenBefore);this.syncViewerToolButtons();report({message:'Visibilidad restablecida.',current:Math.max(1,targetMeshes.length),total:Math.max(1,targetMeshes.length)});return result;}const meshes=Array.isArray(this.meshes)?this.meshes:[];const totalMeshes=Math.max(1,meshes.length);this.isolateActive=false;this.isolatedKey=null;if(!this.isolatedKeys)this.isolatedKeys=new Set();this.isolatedKeys.clear();if(!this.hiddenKeys)this.hiddenKeys=new Set();if(hadAiBaseIsolation){this.baseIsolationActive=false;this.baseIsolationKeys=new Set();this.aiVisualState.isolationKeys=new Set();this.aiVisualState.active=!!((this.aiVisualState.colorKeys&&this.aiVisualState.colorKeys.size)||(this.aiVisualState.transparencyKeys&&this.aiVisualState.transparencyKeys.size));}const hadCachedIsolationSnapshot=!!this.isolationRenderSnapshot;const cachedObjectCount=this.isolationRenderSnapshot&&Array.isArray(this.isolationRenderSnapshot.objectStates)?this.isolationRenderSnapshot.objectStates.length:0;if(hadIsolation&&this.restoreIsolationRenderSnapshotFast&&this.restoreIsolationRenderSnapshotFast('shortcut-reset-hidden-isolation-fast')){if(hiddenBeforeIsolationReset.size)this.restoreHiddenKeysIncrementally(hiddenBeforeIsolationReset);this.syncViewerToolButtons();report({message:'Visibilidad restablecida desde cache.',current:Math.max(1,cachedObjectCount),total:Math.max(1,cachedObjectCount)});return false;}this.hiddenKeys.clear();report({message:(hadIsolation||hadAiBaseIsolation)?'Restaurando los elementos aislados...':'Restaurando elementos ocultos...',current:0,total:totalMeshes});if(opts.yieldBeforeWork!==false&&this.waitForResetProgressPaint){await this.waitForResetProgressPaint();}const visibilityOptions={forceChunkRebuild:hadCachedIsolationSnapshot,reason:'shortcut-reset-hidden-isolation',batchSize:Math.max(500,Number(opts.visibilityBatchSize)||3500),onProgress:(current,total,phase)=>{report({message:phase||'Restaurando visibilidad...',current:Math.max(0,Number(current)||0),total:Math.max(1,Number(total)||totalMeshes)});}};if(this.applyVisibilityStateBatched){await this.applyVisibilityStateBatched(null,visibilityOptions);}else{this.applyVisibilityState(null,visibilityOptions);report({message:'Visibilidad restaurada.',current:totalMeshes,total:totalMeshes});}if(this.endStrictIsolationVisibilityGuard)this.endStrictIsolationVisibilityGuard('reset-hidden-isolation-progress');report({message:'Sincronizando bloques optimizados y bordes visibles...',current:0,total:0});if(this.waitForResetProgressPaint)await this.waitForResetProgressPaint();if(this.syncInteractiveNormalChunkPeel)this.syncInteractiveNormalChunkPeel();if(this.refreshNormalChunkEdgesForVisibleChunksAsync){await this.refreshNormalChunkEdgesForVisibleChunksAsync('shortcut-reset-hidden-isolation',{onProgress:(current,total,phase)=>report({message:phase||'Sincronizando bordes visibles...',current:Math.max(0,Number(current)||0),total:Math.max(0,Number(total)||0)})});}else if(this.scheduleNormalChunkEdgesForVisibleChunks){this.scheduleNormalChunkEdgesForVisibleChunks('shortcut-reset-hidden-isolation');}if(this.invalidateSelectionCandidateCache)this.invalidateSelectionCandidateCache('shortcut-reset-hidden-isolation');if(this.invalidateRaycastScreenCandidateCache)this.invalidateRaycastScreenCandidateCache('shortcut-reset-hidden-isolation');if(this.invalidateMeasurementPickCache)this.invalidateMeasurementPickCache('shortcut-reset-hidden-isolation');if(this.requestRender)this.requestRender('shortcut-reset-hidden-isolation');this.syncViewerToolButtons();report({message:'Visibilidad restablecida.',current:1,total:1});return false;}runViewerShortcut(command,event){const code=String(command||'').toUpperCase();const ui=(typeof window!=='undefined')?window.__exportModelUI:null;if(ui&&typeof ui.executeViewerToolCommand==='function'){Promise.resolve(ui.executeViewerToolCommand(code,{source:'shortcut-'+code,shiftKey:!!(event&&event.shiftKey),event:event||null})).catch(error=>{if(typeof debugError==='function')debugError('viewer:shortcut:'+code+':error',error);else console.error('No se pudo ejecutar el acceso directo '+code+'.',error);});return true;}let result=false;if(code==='SA'){result=this.selectSameTypesFromSelection(true,{sameProjectOnly:!!(event&&event.shiftKey)});}else if(code==='HI'){result=this.toggleIsolateSelected?this.toggleIsolateSelected():false;}else if(code==='HH'){result=this.toggleHideSelected?this.toggleHideSelected():false;}else if(code==='HR'){if(this.resetHiddenAndIsolationWithProgress){Promise.resolve(this.resetHiddenAndIsolationWithProgress({source:'shortcut-'+code})).catch(error=>{if(typeof debugError==='function')debugError('viewer:shortcut:'+code+':fallback:error',error);else console.error('No se pudo restablecer la visibilidad.',error);});result=true;}else result=this.resetHiddenAndIsolation?this.resetHiddenAndIsolation():false;}else if(code==='BX'){result=this.toggleSectionBox?this.toggleSectionBox():false;}else if(code==='RR'){if(this.resetAllViewModifications){Promise.resolve(this.resetAllViewModifications({source:'shortcut-'+code})).then(()=>{if(this.fitToModel)this.fitToModel();}).catch(error=>{if(typeof debugError==='function')debugError('viewer:shortcut:'+code+':fallback:error',error);else console.error('No se pudo restablecer la vista.',error);});result=true;}}else if(code==='ZR'){result=this.requestAreaZoom?this.requestAreaZoom():false;}else{return false;}this.syncViewerToolButtons();return result!==false;}handleViewerShortcutKeydown(event,isTyping){if(!event||isTyping)return false;if(event.ctrlKey||event.altKey||event.metaKey)return false;if(!this.isViewerShortcutScope(event))return false;const key=String(event.key||'').toUpperCase();if(!/^[A-Z]$/.test(key)){if(key!=='SHIFT')this.resetViewerShortcutBuffer();return false;}event.preventDefault();if(event.repeat)return true;const now=(typeof performance!=='undefined'&&performance.now)?performance.now():Date.now();if(!this.viewerShortcutLastTime||(now-this.viewerShortcutLastTime)>this.viewerShortcutTimeoutMs){this.viewerShortcutBuffer='';}this.viewerShortcutLastTime=now;this.viewerShortcutBuffer=(this.viewerShortcutBuffer+key).slice(-4);const shortcutCommands=['SA','HI','HH','HR','BX','RR','ZR'];for(const command of shortcutCommands){if(this.viewerShortcutBuffer.endsWith(command)){this.resetViewerShortcutBuffer();this.runViewerShortcut(command,event);return true;}}if(this.viewerShortcutTimer)clearTimeout(this.viewerShortcutTimer);this.viewerShortcutTimer=setTimeout(()=>this.resetViewerShortcutBuffer(),this.viewerShortcutTimeoutMs);return true;}getTouchCanvasPoint(touch){const rect=this.canvas.getBoundingClientRect();return{x:touch.clientX,y:touch.clientY,canvasX:touch.clientX-rect.left,canvasY:touch.clientY-rect.top,rect};}getTouchDistance(touchA,touchB){const dx=touchB.clientX-touchA.clientX;const dy=touchB.clientY-touchA.clientY;return Math.max(1,Math.sqrt(dx*dx+dy*dy));}getTouchCenter(touchA,touchB){return{x:(touchA.clientX+touchB.clientX)/2,y:(touchA.clientY+touchB.clientY)/2};}resetTouchInteraction(){this.touchState=null;this.dragging=false;this.mouseMode=null;this.updateCursor();}setupTwoFingerTouch(touchA,touchB){const center=this.getTouchCenter(touchA,touchB);this.dragging=true;this.dragDistance=0;this.mouseMode='touch-two';this.touchState={mode:'two',aX:touchA.clientX,aY:touchA.clientY,bX:touchB.clientX,bY:touchB.clientY,centerX:center.x,centerY:center.y,distance:this.getTouchDistance(touchA,touchB)};this.updateCursor();}handleTouchStart(event){if(!event.touches||!event.touches.length)return;event.preventDefault();this.beginNavigationPerformanceMode('touch');this.focusViewerCanvas();const touches=event.touches;this.restoreHoveredMaterial();if(touches.length>=2){this.setupTwoFingerTouch(touches[0],touches[1]);return;}const point=this.getTouchCanvasPoint(touches[0]);const sectionHandle=this.areaZoomPending?null:this.pickSectionHandle(point.canvasX,point.canvasY);this.dragging=true;this.dragDistance=0;this.last={x:point.x,y:point.y};this.down={x:point.x,y:point.y};this.selectionOperation='replace';if(this.areaZoomPending){this.startAreaZoomInteraction(point.x,point.y);this.touchState={mode:'area-zoom',lastX:point.x,lastY:point.y,startX:point.x,startY:point.y};this.updateCursor();return;}if(sectionHandle){const rotationAxis=sectionHandle.userData.sectionRotationAxis;if(rotationAxis){this.mouseMode='section-rotate';this.beginSectionRotation(rotationAxis,point.canvasX,point.canvasY);this.touchState={mode:'section-rotate',lastX:point.x,lastY:point.y,startX:point.x,startY:point.y};}else{this.mouseMode='section';this.sectionDrag={face:sectionHandle.userData.sectionFace};this.sectionActiveFace=sectionHandle.userData.sectionFace;this.touchState={mode:'section',lastX:point.x,lastY:point.y,startX:point.x,startY:point.y};this.updateSectionBoxVisual();}this.updateCursor();return;}if(this.elevationActive){const elevationHit=this.getElevationMarkerHitFromScreen(point.canvasX,point.canvasY);if(elevationHit){this.mouseMode='elevation-drag';this.elevationDragIndex=elevationHit.elevationIndex;this.activeElevationIndex=elevationHit.elevationIndex;this.touchState={mode:'elevation-drag',lastX:point.x,lastY:point.y,startX:point.x,startY:point.y};this.updateCursor();return;}}if(this.elevationActive&&this.elevationPlacementActive){this.mouseMode='elevation';this.touchState={mode:'elevation',lastX:point.x,lastY:point.y,startX:point.x,startY:point.y};this.updateElevationPreviewFromScreen(point.canvasX,point.canvasY);this.updateCursor();return;}if(this.measureActive){const pointHit=this.getMeasurePointHitFromScreen(point.canvasX,point.canvasY);if(pointHit){this.mouseMode='measure-drag';this.measureDragMeasurementIndex=pointHit.measurementIndex;this.measureDragIndex=pointHit.pointIndex;this.touchState={mode:'measure-drag',lastX:point.x,lastY:point.y,startX:point.x,startY:point.y};this.updateCursor();return;}if(this.isMeasurePlacementActive()){this.mouseMode='measure';this.touchState={mode:'measure',lastX:point.x,lastY:point.y,startX:point.x,startY:point.y};this.updateMeasurePreviewFromScreen(point.canvasX,point.canvasY);this.updateCursor();return;}}this.mouseMode='touch-pan';this.touchState={mode:'pan',startX:point.x,startY:point.y,lastX:point.x,lastY:point.y};this.updateCursor();}handleTouchMove(event){if(!event.touches||!event.touches.length)return;event.preventDefault();const touches=event.touches;const state=this.touchState;if(!state)return;if(touches.length>=2){const touchA=touches[0];const touchB=touches[1];if(state.mode!=='two'){this.setupTwoFingerTouch(touchA,touchB);return;}const center=this.getTouchCenter(touchA,touchB);const distance=this.getTouchDistance(touchA,touchB);const centerDx=center.x-state.centerX;const centerDy=center.y-state.centerY;const aDx=touchA.clientX-state.aX;const aDy=touchA.clientY-state.aY;const bDx=touchB.clientX-state.bX;const bDy=touchB.clientY-state.bY;const aMove=Math.sqrt(aDx*aDx+aDy*aDy);const bMove=Math.sqrt(bDx*bDx+bDy*bDy);const distanceDelta=Math.abs(distance-state.distance);const anchoredThreshold=3;this.dragDistance+=Math.abs(centerDx)+Math.abs(centerDy)+distanceDelta;if(aMove<=anchoredThreshold&&bMove>anchoredThreshold){this.orbit(bDx,bDy);}else if(bMove<=anchoredThreshold&&aMove>anchoredThreshold){this.orbit(aDx,aDy);}else{if(Math.abs(centerDx)>0||Math.abs(centerDy)>0)this.pan(centerDx,centerDy);if(state.distance&&distance>0&&distanceDelta>0.5){const factor=state.distance/distance;if(this.isPerspectiveMode()){this.dollyPerspectiveOrbit(factor);}else{this.orthoScale=Math.max(0.05,this.orthoScale*factor);this.updateCameraClipRange();this.updateCameraProjection();}}}state.aX=touchA.clientX;state.aY=touchA.clientY;state.bX=touchB.clientX;state.bY=touchB.clientY;state.centerX=center.x;state.centerY=center.y;state.distance=distance;return;}const point=this.getTouchCanvasPoint(touches[0]);const dx=point.x-state.lastX;const dy=point.y-state.lastY;state.lastX=point.x;state.lastY=point.y;this.dragDistance+=Math.abs(dx)+Math.abs(dy);if(state.mode==='section'){this.dragSectionFace(dx,dy);}else if(state.mode==='section-rotate'){this.dragSectionRotation(point.canvasX,point.canvasY);}else if(state.mode==='area-zoom'){this.updateAreaZoomOverlay(point.x,point.y);}else if(state.mode==='elevation'){this.updateElevationPreviewFromScreen(point.canvasX,point.canvasY);}else if(state.mode==='elevation-drag'){this.dragElevationMarkerFromScreen(point.canvasX,point.canvasY);}else if(state.mode==='measure'){this.updateMeasurePreviewFromScreen(point.canvasX,point.canvasY);}else if(state.mode==='measure-drag'){this.dragMeasurePointFromScreen(point.canvasX,point.canvasY);}else if(state.mode==='pan'){this.pan(dx,dy);}}handleTouchEnd(event){if(event&&event.cancelable)event.preventDefault();const remainingTouches=event&&event.touches?event.touches.length:0;const state=this.touchState;if(!state){this.resetTouchInteraction();return;}if(remainingTouches>=2){this.setupTwoFingerTouch(event.touches[0],event.touches[1]);return;}if(remainingTouches===1){const point=this.getTouchCanvasPoint(event.touches[0]);this.dragging=true;this.dragDistance=0;this.mouseMode='touch-pan';this.touchState={mode:'pan',startX:point.x,startY:point.y,lastX:point.x,lastY:point.y};return;}if(state.mode==='section'||state.mode==='section-rotate'){this.finishSectionBoxInteraction();}else if(state.mode==='area-zoom'){if(this.areaZoomRect)this.fitToAreaRectangle(this.areaZoomRect);this.cancelAreaZoomMode();}else if(state.mode==='elevation'){const rect=this.canvas.getBoundingClientRect();if(this.dragDistance<8)this.commitElevationFromScreen(state.startX-rect.left,state.startY-rect.top);}else if(state.mode==='elevation-drag'){this.elevationDragIndex=null;this.restoreMeasureReferenceHighlight();}else if(state.mode==='measure'){const rect=this.canvas.getBoundingClientRect();if(this.dragDistance<8)this.commitMeasurePointFromScreen(state.startX-rect.left,state.startY-rect.top);this.restoreMeasureReferenceHighlight();}else if(state.mode==='measure-drag'){this.measureDragIndex=null;this.measureDragMeasurementIndex=null;this.clearMeasurePreview();this.restoreMeasureReferenceHighlight();}else if(state.mode==='pan'&&this.dragDistance<8){const rect=this.canvas.getBoundingClientRect();this.selectFromScreen(state.startX-rect.left,state.startY-rect.top);}this.hideSelectionOverlay();this.resetTouchInteraction();this.scheduleNavigationPerformanceRestore();}createSelectionOverlay(){return this.selectionController.dispatch('box','createSelectionOverlay',this,arguments);}createMeasureOverlay(){const parent=this.canvas.parentElement;if(!parent||typeof document==='undefined')return null;const root=document.createElement('div');root.className='measure-overlay-root';const axisSvg=document.createElementNS('http://www.w3.org/2000/svg','svg');axisSvg.classList.add('measure-projection-lines-layer');axisSvg.setAttribute('aria-hidden','true');axisSvg.style.position='absolute';axisSvg.style.inset='0';axisSvg.style.width='100%';axisSvg.style.height='100%';axisSvg.style.overflow='visible';axisSvg.style.pointerEvents='none';axisSvg.style.zIndex='0';root.appendChild(axisSvg);const pointA=document.createElement('div');pointA.className='measure-point-label hidden';pointA.textContent='1';root.appendChild(pointA);const pointB=document.createElement('div');pointB.className='measure-point-label hidden';pointB.textContent='2';root.appendChild(pointB);const distance=document.createElement('div');distance.className='measure-distance-label hidden';root.appendChild(distance);const axes=document.createElement('div');axes.className='measure-axis-box hidden';axes.innerHTML='<span class="measure-axis-x"><strong>X</strong> <span data-axis="x">0.000 m</span></span>'+'<span class="measure-axis-y"><strong>Y</strong> <span data-axis="y">0.000 m</span></span>'+'<span class="measure-axis-z"><strong>Z</strong> <span data-axis="z">0.000 m</span></span>';root.appendChild(axes);parent.appendChild(root);return{root,pointA,pointB,distance,axes,axisSvg,axisX:axes.querySelector('[data-axis="x"]'),axisY:axes.querySelector('[data-axis="y"]'),axisZ:axes.querySelector('[data-axis="z"]')};}requestAreaZoom(){this.areaZoomPending=true;this.areaZoomRect=null;this.restoreHoveredMaterial();this.restoreMeasureReferenceHighlight();this.hideSelectionOverlay();this.updateCursor();this.focusViewerCanvas();return true;}cancelAreaZoomMode(){this.areaZoomPending=false;this.areaZoomRect=null;if(this.selectionOverlay){this.selectionOverlay.classList.add('hidden');this.selectionOverlay.classList.remove('zoom-area');}this.selectionBoxActive=false;this.selectionRect=null;this.updateCursor();}startAreaZoomInteraction(clientX,clientY){this.areaZoomPending=false;this.mouseMode='area-zoom';this.areaZoomRect=null;this.selectionBoxActive=false;this.selectionRect=null;this.down={x:clientX,y:clientY};this.last={x:clientX,y:clientY};this.dragging=true;this.dragDistance=0;this.restoreHoveredMaterial();this.restoreMeasureReferenceHighlight();if(this.selectionOverlay){this.selectionOverlay.classList.remove('hidden');this.selectionOverlay.classList.add('zoom-area');this.selectionOverlay.classList.remove('crossing');this.selectionOverlay.style.left='0px';this.selectionOverlay.style.top='0px';this.selectionOverlay.style.width='0px';this.selectionOverlay.style.height='0px';}}updateAreaZoomOverlay(clientX,clientY){const rect=this.canvas.getBoundingClientRect();const startX=this.down.x-rect.left;const startY=this.down.y-rect.top;const currentX=clientX-rect.left;const currentY=clientY-rect.top;const left=Math.min(startX,currentX);const top=Math.min(startY,currentY);const width=Math.abs(currentX-startX);const height=Math.abs(currentY-startY);this.areaZoomRect={left,top,right:left+width,bottom:top+height,mode:'crossing'};this.selectionBoxActive=true;if(this.selectionOverlay){this.selectionOverlay.classList.remove('hidden');this.selectionOverlay.classList.add('zoom-area');this.selectionOverlay.classList.remove('crossing');this.selectionOverlay.style.left=left+'px';this.selectionOverlay.style.top=top+'px';this.selectionOverlay.style.width=width+'px';this.selectionOverlay.style.height=height+'px';}}fitToAreaRectangle(rect){if(!rect)return false;const width=Math.max(0,(rect.right||0)-(rect.left||0));const height=Math.max(0,(rect.bottom||0)-(rect.top||0));if(width<12||height<12)return false;const canvasRect=this.canvas.getBoundingClientRect();const canvasWidth=Math.max(1,canvasRect.width);const canvasHeight=Math.max(1,canvasRect.height);const rectCenterX=(rect.left+rect.right)*0.5;const rectCenterY=(rect.top+rect.bottom)*0.5;const centerX=canvasWidth*0.5;const centerY=canvasHeight*0.5;const oldScale=Math.max(0.05,Number(this.orthoScale)||1);this.pan(centerX-rectCenterX,centerY-rectCenterY);const horizontalRatio=width/canvasWidth;const verticalRatio=height/canvasHeight;const scaleFactor=THREE.MathUtils.clamp(Math.max(horizontalRatio,verticalRatio)*1.04,0.02,1);if(this.isPerspectiveMode()){this.dollyPerspectiveOrbit(scaleFactor);}else{this.orthoScale=Math.max(0.05,oldScale*scaleFactor);this.updateCameraClipRange();this.updateCameraProjection();this.updateViewCubeOrientation();}return true;}createElevationOverlay(){const parent=this.canvas&&this.canvas.parentElement;if(!parent||typeof document==='undefined')return null;const root=document.createElement('div');root.className='elevation-overlay-root hidden';root.style.position='absolute';root.style.left='0px';root.style.top='0px';root.style.transform='translate(-50%, -100%)';root.style.pointerEvents='none';root.style.zIndex='18';root.style.display='none';root.style.gridTemplateColumns='1fr';root.style.justifyItems='center';root.style.gap='2px';root.style.fontFamily='Inter, Segoe UI, Roboto, Arial, sans-serif';const value=document.createElement('div');value.className='elevation-value-label';value.style.background='rgba(15, 23, 42, 0.92)';value.style.color='#ffffff';value.style.border='1px solid rgba(255,255,255,0.35)';value.style.borderRadius='6px';value.style.padding='3px 6px';value.style.fontSize='12px';value.style.fontWeight='800';value.style.whiteSpace='nowrap';value.style.boxShadow='0 4px 12px rgba(15,23,42,0.25)';root.appendChild(value);const symbol=document.createElement('div');symbol.className='elevation-symbol-label';symbol.textContent='▼';symbol.style.color='#0ea5e9';symbol.style.fontSize='22px';symbol.style.lineHeight='18px';symbol.style.textShadow='0 1px 2px rgba(0,0,0,0.45)';root.appendChild(symbol);parent.appendChild(root);return{root,value,symbol};}updateSelectionOverlay(clientX,clientY){return this.selectionController.dispatch('box','updateSelectionOverlay',this,arguments);}hideSelectionOverlay(){return this.selectionController.dispatch('box','hideSelectionOverlay',this,arguments);}updateCursor(){if(!this.dragging){this.canvas.style.cursor=this.areaZoomPending?'zoom-in':((this.measureActive||this.elevationPlacementActive)?'crosshair':'default');return;}if(this.mouseMode==='section')this.canvas.style.cursor='ew-resize';else if(this.mouseMode==='section-rotate')this.canvas.style.cursor='grabbing';else if(this.mouseMode==='pan')this.canvas.style.cursor='move';else if(this.mouseMode==='free-look')this.canvas.style.cursor='grab';else if(this.mouseMode==='orbit')this.canvas.style.cursor='grab';else if(this.mouseMode==='area-zoom')this.canvas.style.cursor='zoom-in';else if(this.mouseMode==='elevation-drag')this.canvas.style.cursor='move';else if(this.mouseMode==='measure'||this.mouseMode==='measure-drag'||this.mouseMode==='elevation')this.canvas.style.cursor='crosshair';else if(this.isFreeNavigationMode&&this.isFreeNavigationMode())this.canvas.style.cursor='grab';else this.canvas.style.cursor='pointer';}getVisibleModelBounds(){const box=new THREE.Box3();for(const mesh of this.meshes){if(mesh&&mesh.visible)box.expandByObject(mesh);}return box;}getCurrentOrbitPivot(){if(this.orbitPivot)return this.orbitPivot;if(this.lastSelectionOrbitPivot)return this.lastSelectionOrbitPivot;const visibleBox=this.getVisibleModelBounds();if(visibleBox&&!visibleBox.isEmpty())return visibleBox.getCenter(new THREE.Vector3());return this.target;}orbit(dx,dy){const pivot=this.getCurrentOrbitPivot();const yaw=-dx*0.006;const pitch=-dy*0.006;this.camera.updateMatrixWorld(true);const right=new THREE.Vector3().setFromMatrixColumn(this.camera.matrixWorld,0).normalize();const yawQuaternion=new THREE.Quaternion().setFromAxisAngle(new THREE.Vector3(0,1,0),yaw);const pitchedRight=right.clone().applyQuaternion(yawQuaternion).normalize();const pitchQuaternion=new THREE.Quaternion().setFromAxisAngle(pitchedRight,pitch);const rotation=pitchQuaternion.multiply(yawQuaternion);this.camera.position.sub(pivot).applyQuaternion(rotation).add(pivot);this.target.sub(pivot).applyQuaternion(rotation).add(pivot);this.camera.up.applyQuaternion(rotation).normalize();this.camera.lookAt(this.target);this.syncOrbitAnglesFromCamera();this.updateCameraClipRange();this.updateCameraProjection();this.updateViewCubeOrientation();}syncOrbitAnglesFromCamera(){const offset=new THREE.Vector3().copy(this.camera.position).sub(this.target);const distance=Math.max(offset.length(),0.05);this.distance=distance;const polarEpsilon=0.001;this.phi=Math.max(polarEpsilon,Math.min(Math.PI-polarEpsilon,Math.acos(offset.y/distance)));this.theta=Math.atan2(offset.x,offset.z);}pan(dx,dy){let worldPerPixel=this.getWorldPerPixelAtPoint(this.target);let panSensitivity=1;if(this.isPerspectiveMode()){worldPerPixel=this.getPerspectiveWorldPerPixelForNavigation();panSensitivity=this.isFreeNavigationMode()?0.45:0.70;}const panX=dx*worldPerPixel*panSensitivity;const panY=dy*worldPerPixel*panSensitivity;const cameraMatrix=this.camera.matrix;const xAxis=new THREE.Vector3().setFromMatrixColumn(cameraMatrix,0).multiplyScalar(-panX);const yAxis=new THREE.Vector3().setFromMatrixColumn(cameraMatrix,1).multiplyScalar(panY);this.target.add(xAxis).add(yAxis);this.camera.position.add(xAxis).add(yAxis);if(this.isFreeNavigationMode())this.applyFreeLookCamera();else this.applyCamera(false);}updateCameraUpForView(){if(!this.camera)return;const viewDirection=new THREE.Vector3().copy(this.target).sub(this.camera.position);if(viewDirection.lengthSq()<1e-12){this.camera.up.set(0,1,0);return;}viewDirection.normalize();const verticalAmount=Math.abs(viewDirection.dot(new THREE.Vector3(0,1,0)));if(verticalAmount>0.985){this.camera.up.set(0,0,viewDirection.y<0?-1:1);}else{this.camera.up.set(0,1,0);}}applyCamera(recalculatePosition=true){if(recalculatePosition){const sinPhi=Math.sin(this.phi);const x=this.distance*sinPhi*Math.sin(this.theta);const y=this.distance*Math.cos(this.phi);const z=this.distance*sinPhi*Math.cos(this.theta);this.camera.position.set(this.target.x+x,this.target.y+y,this.target.z+z);this.updateCameraUpForView();}this.camera.lookAt(this.target);this.updateCameraClipRange();this.updateCameraProjection();this.updateViewCubeOrientation();this.requestRender&&this.requestRender('camera');}initViewCube(){const container=document.getElementById('viewCube');const canvas=document.getElementById('viewCubeCanvas');if(!container||!canvas||!window.THREE)return;const scene=new THREE.Scene();const camera=new THREE.OrthographicCamera(-1.9,1.9,1.9,-1.9,0.1,20);camera.position.set(0,0,6);camera.lookAt(0,0,0);const renderer=new THREE.WebGLRenderer({canvas,alpha:true,antialias:true});renderer.setPixelRatio(Math.min(window.devicePixelRatio||1,2));renderer.setSize(128,128,false);renderer.setClearColor(0x000000,0);scene.add(new THREE.AmbientLight(0xffffff,0.95));const light=new THREE.DirectionalLight(0xffffff,0.65);light.position.set(3,5,5);scene.add(light);const labels=['DERECHA','IZQUIERDA','SUPERIOR','INFERIOR','FRONTAL','POSTERIOR'];const faceMaterials=labels.map(label=>this.createViewCubeMaterial(label,false));const hoverMaterials=labels.map(label=>this.createViewCubeMaterial(label,true));const cube=new THREE.Mesh(new THREE.BoxGeometry(1.7,1.7,1.7),faceMaterials);const edges=new THREE.LineSegments(new THREE.EdgesGeometry(cube.geometry),new THREE.LineBasicMaterial({color:0x64748b,transparent:true,opacity:0.85}));cube.add(edges);scene.add(cube);const raycaster=new THREE.Raycaster();const pointer=new THREE.Vector2();const viewByMaterial=['right','left','top','bottom','front','back'];let hoveredFaceIndex=-1;const setHoveredFace=(index)=>{if(hoveredFaceIndex===index)return;hoveredFaceIndex=index;const nextMaterials=faceMaterials.slice();if(hoveredFaceIndex>=0)nextMaterials[hoveredFaceIndex]=hoverMaterials[hoveredFaceIndex];cube.material=nextMaterials;this.requestRender&&this.requestRender('viewcube-hover');};let dragging=false;let moved=false;let last={x:0,y:0};container.addEventListener('pointerdown',(event)=>{dragging=true;moved=false;setHoveredFace(-1);last={x:event.clientX,y:event.clientY};container.classList.add('dragging');container.setPointerCapture&&container.setPointerCapture(event.pointerId);event.preventDefault();});container.addEventListener('pointermove',(event)=>{if(!dragging){const hit=this.pickViewCubeHit(event,canvas,camera,raycaster,pointer,cube,viewByMaterial);setHoveredFace(hit?hit.index:-1);return;}const dx=event.clientX-last.x;const dy=event.clientY-last.y;if(Math.abs(dx)+Math.abs(dy)>8)moved=true;last={x:event.clientX,y:event.clientY};this.orbit(dx,dy);event.preventDefault();});container.addEventListener('pointerleave',()=>{if(!dragging)setHoveredFace(-1);});container.addEventListener('pointerup',(event)=>{if(!dragging)return;dragging=false;container.classList.remove('dragging');container.releasePointerCapture&&container.releasePointerCapture(event.pointerId);if(!moved){const hit=this.pickViewCubeHit(event,canvas,camera,raycaster,pointer,cube,viewByMaterial);if(hit&&hit.view)this.setStandardView(hit.view);}const hoverHit=this.pickViewCubeHit(event,canvas,camera,raycaster,pointer,cube,viewByMaterial);setHoveredFace(hoverHit?hoverHit.index:-1);event.preventDefault();});canvas.addEventListener('click',(event)=>{const hit=this.pickViewCubeHit(event,canvas,camera,raycaster,pointer,cube,viewByMaterial);if(hit&&hit.view)this.setStandardView(hit.view);});this.viewCube={container,canvas,scene,camera,renderer,cube};this.updateViewCubeOrientation();}pickViewCubeHit(event,canvas,camera,raycaster,pointer,cube,viewByMaterial){const rect=canvas.getBoundingClientRect();pointer.x=((event.clientX-rect.left)/rect.width)*2-1;pointer.y=-((event.clientY-rect.top)/rect.height)*2+1;raycaster.setFromCamera(pointer,camera);const hits=raycaster.intersectObject(cube,false);if(hits.length){const materialIndex=hits[0].face&&Number.isInteger(hits[0].face.materialIndex)?hits[0].face.materialIndex:4;return{index:materialIndex,view:viewByMaterial[materialIndex]||'front'};}const localX=(event.clientX-rect.left)/rect.width;const localY=(event.clientY-rect.top)/rect.height;let index=4;let view='front';if(localY<0.34){index=2;view='top';}else if(localY>0.72){index=3;view='bottom';}else if(localX<0.34){index=1;view='left';}else if(localX>0.66){index=0;view='right';}return{index,view};}createViewCubeMaterial(label,highlighted){const canvas=document.createElement('canvas');canvas.width=256;canvas.height=256;const ctx=canvas.getContext('2d');const gradient=ctx.createLinearGradient(0,0,256,256);gradient.addColorStop(0,'#ffffff');gradient.addColorStop(1,'#d1d5db');ctx.fillStyle=gradient;ctx.fillRect(0,0,256,256);ctx.strokeStyle=highlighted?'#ff0000':'#8b949e';ctx.lineWidth=highlighted?16:8;ctx.strokeRect(4,4,248,248);ctx.fillStyle='#475569';ctx.font='bold 42px Arial, sans-serif';ctx.textAlign='center';ctx.textBaseline='middle';const words=label.split(' ');const text=String(label||'').toUpperCase();if(text.length>8){ctx.font='bold 34px Arial, sans-serif';ctx.fillText(text.substring(0,8),128,128);}else{ctx.fillText(text,128,128);}const texture=new THREE.CanvasTexture(canvas);texture.anisotropy=4;return new THREE.MeshBasicMaterial({map:texture,side:THREE.DoubleSide});}updateViewCubeOrientation(){if(!this.viewCube||!this.viewCube.cube||!this.camera)return;this.camera.updateMatrixWorld(true);this.viewCube.cube.quaternion.copy(this.camera.quaternion).invert();this.requestRender&&this.requestRender('viewcube-orientation');}renderViewCube(){if(!this.viewCube)return;this.viewCube.renderer.render(this.viewCube.scene,this.viewCube.camera);}getFullModelBounds(){if(this.fullModelBounds&&!this.fullModelBounds.isEmpty())return this.fullModelBounds;const box=new THREE.Box3();for(const mesh of this.meshes){if(mesh)box.expandByObject(mesh);}if(!box.isEmpty())this.fullModelBounds=box.clone();return box;}getFullModelDepthSize(){const box=this.getFullModelBounds();if(!box||box.isEmpty())return 1;const size=box.getSize(new THREE.Vector3());return Math.max(size.x,size.y,size.z,size.length(),1);}updateCameraClipRange(){if(!this.camera)return;const box=this.getFullModelBounds();if(!box||box.isEmpty()){if(this.camera.isPerspectiveCamera){const focusDistance=Math.max(0.05,Number(this.distance)||1);this.camera.near=Math.max(0.01,Math.min(1,focusDistance*0.002));this.camera.far=Math.max(100,focusDistance*8,this.camera.near*1000);}else{this.camera.near=0.001;this.camera.far=Math.max(this.distance*50,1000);}this.camera.updateProjectionMatrix();return;}const min=box.min;const max=box.max;const corners=[new THREE.Vector3(min.x,min.y,min.z),new THREE.Vector3(min.x,min.y,max.z),new THREE.Vector3(min.x,max.y,min.z),new THREE.Vector3(min.x,max.y,max.z),new THREE.Vector3(max.x,min.y,min.z),new THREE.Vector3(max.x,min.y,max.z),new THREE.Vector3(max.x,max.y,min.z),new THREE.Vector3(max.x,max.y,max.z)];const forward=this.camera.getWorldDirection(new THREE.Vector3()).normalize();const offset=new THREE.Vector3();let minDepth=Infinity;let maxDepth=-Infinity;for(const corner of corners){const depth=offset.copy(corner).sub(this.camera.position).dot(forward);minDepth=Math.min(minDepth,depth);maxDepth=Math.max(maxDepth,depth);}const size=box.getSize(new THREE.Vector3());if(this.camera.isPerspectiveCamera){const diagonal=Math.max(1,size.length());const focusDistance=Math.max(0.05,this.target&&this.camera.position?this.camera.position.distanceTo(this.target):0,Number(this.distance)||0);const farMargin=Math.max(1,diagonal*0.06,focusDistance*0.04);const far=Math.max(10,maxDepth+farMargin,focusDistance+farMargin);const insideNear=Math.max(0.01,Math.min(1,focusDistance*0.002,diagonal*0.002));const outsideNear=minDepth>0?Math.max(insideNear,Math.min(minDepth*0.5,focusDistance*0.08)):insideNear;const near=Math.max(0.01,Math.min(outsideNear,far*0.2));this.camera.near=near;this.camera.far=Math.max(far,near+1);this.camera.updateProjectionMatrix();return;}const margin=Math.max(size.length()*0.35,this.distance*0.5,10);const near=Math.max(0.001,Math.min(Math.max(minDepth-margin,0.001),10));const far=Math.max(maxDepth+margin,near+margin,size.length()*4,1000);this.camera.near=near;this.camera.far=far;this.camera.updateProjectionMatrix();}getViewportAspect(){const element=this.renderer?this.renderer.domElement:this.canvas;const width=Math.max(1,element&&(element.clientWidth||this.canvas.clientWidth)||1);const height=Math.max(1,element&&(element.clientHeight||this.canvas.clientHeight)||1);return width/height;}resetViewportFramingReference(aspect){const numeric=Number(aspect);const next=Number.isFinite(numeric)&&numeric>0.0001?numeric:this.getViewportAspect();this.viewportFramingReferenceAspect=Math.max(0.0001,next);this.viewportFramingLastAspect=this.viewportFramingReferenceAspect;if(this.camera)this.updateCameraProjection();return this.viewportFramingReferenceAspect;}getViewportFramingReferenceAspect(currentAspect){const aspect=Math.max(0.0001,Number(currentAspect)||this.getViewportAspect()||1);const stored=Number(this.viewportFramingReferenceAspect);if(!this.viewportFramingEnabled||!Number.isFinite(stored)||stored<=0.0001){this.viewportFramingReferenceAspect=aspect;return aspect;}return stored;}getEffectiveOrthographicScale(aspect){const currentAspect=Math.max(0.0001,Number(aspect)||this.getViewportAspect()||1);const baseScale=Math.max(0.05,Number(this.orthoScale)||1);const referenceAspect=this.getViewportFramingReferenceAspect(currentAspect);return Math.max(0.05,baseScale*(referenceAspect/currentAspect));}getEffectivePerspectiveFov(aspect){const currentAspect=Math.max(0.0001,Number(aspect)||this.getViewportAspect()||1);const referenceAspect=this.getViewportFramingReferenceAspect(currentAspect);const baseFov=THREE.MathUtils.clamp(Number(this.perspectiveFov)||55,5,150);const baseHalfVertical=Math.tan(THREE.MathUtils.degToRad(baseFov)*0.5);const horizontalHalfTangent=baseHalfVertical*referenceAspect;const effective=THREE.MathUtils.radToDeg(2*Math.atan(horizontalHalfTangent/currentAspect));return THREE.MathUtils.clamp(effective,5,150);}updateCameraProjection(){if(!this.camera||!this.renderer)return;const element=this.renderer.domElement;const width=Math.max(1,element.clientWidth||this.canvas.clientWidth||1);const height=Math.max(1,element.clientHeight||this.canvas.clientHeight||1);const aspect=width/height;this.viewportFramingLastAspect=aspect;if(this.camera.isPerspectiveCamera){this.camera.aspect=aspect;this.camera.fov=this.getEffectivePerspectiveFov(aspect);}else{const halfHeight=this.getEffectiveOrthographicScale(aspect)/2;const halfWidth=halfHeight*aspect;this.camera.left=-halfWidth;this.camera.right=halfWidth;this.camera.top=halfHeight;this.camera.bottom=-halfHeight;}this.camera.updateProjectionMatrix();this.raycastViewRevision=(this.raycastViewRevision||0)+1;if(this.invalidateRaycastScreenCandidateCache)this.invalidateRaycastScreenCandidateCache('camera-projection');if(this.sectionBoxActive)this.buildSectionBoxVisual();this.requestRender&&this.requestRender('camera-projection');}resize(options){const opts=options&&typeof options==='object'?options:{};const width=Math.max(1,Math.floor(this.canvas.clientWidth||this.canvas.parentElement.clientWidth));const height=Math.max(1,Math.floor(this.canvas.clientHeight||this.canvas.parentElement.clientHeight));const current=this.renderer.getSize(new THREE.Vector2());if(opts.resetFramingReference===true){this.viewportFramingReferenceAspect=width/height;}if(current.x!==width||current.y!==height){this.renderer.setSize(width,height,false);}this.updateCameraProjection();this.requestRender&&this.requestRender(opts.reason||'resize');}requestRender(reason){if(this.renderDemandReady!==true)return;this.renderPending=true;this.renderRequestReason=reason||this.renderRequestReason||'scene-change';const now=this.getPerformanceNow?this.getPerformanceNow():(typeof performance!=='undefined'?performance.now():Date.now());this.renderSettleUntil=Math.max(this.renderSettleUntil||0,now+(this.renderSettleMs||90));if(!this.renderLoopRunning){this.renderLoopRunning=true;this.renderLoopActivations=(this.renderLoopActivations||0)+1;requestAnimationFrame(()=>this.animate());}}renderOnce(reason){this.requestRender(reason||'render-once');}shouldRenderContinuously(now){if(this.isViewerBusyWithDirectInteraction&&this.isViewerBusyWithDirectInteraction())return true;return!!(this.renderSettleUntil&&now<this.renderSettleUntil);}animate(){const now=typeof performance!=='undefined'?performance.now():Date.now();const shouldRender=!!this.renderPending||this.shouldRenderContinuously(now);if(shouldRender){if(this.shouldThrottleRenderForUi(now)){if(now-this.lastUiThrottledRenderTime>=this.uiRenderThrottleMs){this.lastUiThrottledRenderTime=now;this.renderPending=false;this.renderFrame();}}else{this.renderPending=false;this.renderFrame();}}const after=typeof performance!=='undefined'?performance.now():Date.now();if(this.renderPending||this.shouldRenderContinuously(after)){requestAnimationFrame(()=>this.animate());}else{this.renderLoopRunning=false;this.renderSettleUntil=0;}}renderFrame(){if(this.updateAdaptiveMediumChunkVisibility)this.updateAdaptiveMediumChunkVisibility();if(this.strictIsolationGuardActive&&this.enforceStrictIsolationVisibilityGuard){this.enforceStrictIsolationVisibilityGuard(false,'pre-render');}if(this.treeVisibilityGuardActive&&this.enforceTreeVisibilityMergedRenderGuard){this.enforceTreeVisibilityMergedRenderGuard(false,'pre-render');}this.renderer.clippingPlanes=this.getActiveClippingPlanes?this.getActiveClippingPlanes():(this.sectionPlanes||[]);this.renderer.render(this.scene,this.camera);if(this.sectionBoxActive){const savedClipping=this.renderer.clippingPlanes;this.renderer.clippingPlanes=[];this.renderer.autoClear=false;if(this.sectionCapGroup||this.sectionCutEdgeGroup){this.renderer.clearStencil();this.renderer.render(this.sectionCapScene,this.camera);}if(this.sectionBoxGroup)this.renderer.render(this.sectionOverlayScene,this.camera);this.renderer.autoClear=true;this.renderer.clippingPlanes=savedClipping;}if(this.plansOverlayScene&&this.plansOverlayScene.children&&this.plansOverlayScene.children.length){const savedClipping=this.renderer.clippingPlanes;this.renderer.clippingPlanes=[];this.renderer.autoClear=false;if(this.renderer.clearDepth)this.renderer.clearDepth();this.renderer.render(this.plansOverlayScene,this.camera);this.renderer.autoClear=true;this.renderer.clippingPlanes=savedClipping;}if(this.measureGroup||this.measureReferenceGroup){this.renderer.autoClear=false;if(this.renderer.clearDepth)this.renderer.clearDepth();this.renderer.render(this.measureScene,this.camera);this.renderer.autoClear=true;}this.updateMeasurementOverlay();this.updateElevationOverlay();this.renderViewCube();}getPerformanceNow(){return typeof performance!=='undefined'&&performance.now?performance.now():Date.now();}pushLimited(array,item,maxItems){if(!Array.isArray(array))return;array.push(item);const max=Math.max(1,Number(maxItems)||100);if(array.length>max)array.splice(0,array.length-max);}registerMesh(mesh){if(!mesh||!mesh.userData)return;if(mesh.userData.isNonRenderedDataElement==null){mesh.userData.isNonRenderedDataElement=this.isNonRenderedDataElement(mesh.userData.element);}if(!mesh.userData.baseMaterial)mesh.userData.baseMaterial=mesh.material;mesh.userData.baseRenderOrder=mesh.renderOrder||0;mesh.userData.baseVisible=mesh.visible!==false;if(this.visibilityPredicate||this.baseIsolationActive||this.isolateActive||(this.hiddenKeys&&this.hiddenKeys.size)){const nextBaseVisible=this.computeBaseVisibilityForMesh?this.computeBaseVisibilityForMesh(mesh):mesh.userData.baseVisible;mesh.userData.baseVisible=nextBaseVisible!==false;if(mesh.userData.baseVisible===false){mesh.visible=false;for(const child of mesh.children||[])if(child)child.visible=false;}}this.meshes.push(mesh);if(mesh.userData.elementKey){if(!this.meshByElementKey.has(mesh.userData.elementKey))this.meshByElementKey.set(mesh.userData.elementKey,mesh);if(!this.meshesByElementKey)this.meshesByElementKey=new Map();if(!this.meshesByElementKey.has(mesh.userData.elementKey))this.meshesByElementKey.set(mesh.userData.elementKey,[]);this.meshesByElementKey.get(mesh.userData.elementKey).push(mesh);}const projectId=mesh.userData.element&&mesh.userData.element.ProjectId;if(projectId){if(!this.meshesByProjectId.has(projectId))this.meshesByProjectId.set(projectId,[]);this.meshesByProjectId.get(projectId).push(mesh);}const categoryUiKey=this.getCategoryUiKeyForElement?this.getCategoryUiKeyForElement(mesh.userData.element):null;const elementKey=mesh.userData.elementKey;if(elementKey&&projectId){if(!this.elementKeyByProjectId.has(projectId))this.elementKeyByProjectId.set(projectId,new Set());this.elementKeyByProjectId.get(projectId).add(elementKey);}if(categoryUiKey){if(!this.meshesByCategoryUiKey.has(categoryUiKey))this.meshesByCategoryUiKey.set(categoryUiKey,[]);this.meshesByCategoryUiKey.get(categoryUiKey).push(mesh);if(!this.elementKeyByCategoryUiKey.has(categoryUiKey))this.elementKeyByCategoryUiKey.set(categoryUiKey,new Set());if(elementKey)this.elementKeyByCategoryUiKey.get(categoryUiKey).add(elementKey);if(projectId){const projectCategoryKey=projectId+'||'+categoryUiKey;if(!this.meshesByProjectCategoryUiKey.has(projectCategoryKey))this.meshesByProjectCategoryUiKey.set(projectCategoryKey,[]);this.meshesByProjectCategoryUiKey.get(projectCategoryKey).push(mesh);}}if(this.selectionCandidateCache||this.raycastScreenCandidateCache){this.invalidateSelectionCandidateCache&&this.invalidateSelectionCandidateCache('mesh-registered');}}refreshSelectionIndexesAfterGeometryChange(reason){return this.selectionController.dispatch('raycast','refreshSelectionIndexesAfterGeometryChange',this,arguments);}getCategoryGroupKeyForMesh(mesh){const categoryId=mesh&&mesh.userData?mesh.userData.categoryId:null;return categoryId||'__sin_categoria__';}getMeshesForElementKey(key){if(!key)return[];const grouped=this.meshesByElementKey&&this.meshesByElementKey.get(key);if(grouped&&grouped.length)return grouped.filter(Boolean);const mesh=this.meshByElementKey?this.meshByElementKey.get(key):null;return mesh?[mesh]:[];}forEachMeshForElementKey(key,callback){if(!key||typeof callback!=='function')return 0;const meshes=this.getMeshesForElementKey?this.getMeshesForElementKey(key):[];let count=0;for(const mesh of meshes){if(!mesh)continue;callback(mesh);count++;}return count;}getOrCreateCategoryGroup(mesh){const key=this.getCategoryGroupKeyForMesh(mesh);if(this.categoryGroups.has(key))return this.categoryGroups.get(key);const group=new THREE.Group();group.name='Categoria_'+key;group.userData.categoryId=key==='__sin_categoria__'?null:key;group.userData.isCategoryGroup=true;group.frustumCulled=false;this.categoryGroups.set(key,group);this.scene.add(group);return group;}addMeshToCategoryGroup(mesh){if(!mesh)return;const group=this.getOrCreateCategoryGroup(mesh);group.add(mesh);}removeMeshFromCategoryGroup(mesh){if(!mesh)return;if(mesh.parent)mesh.parent.remove(mesh);else this.scene.remove(mesh);}cleanupEmptyCategoryGroups(){for(const[key,group]of this.categoryGroups.entries()){if(!group||(group.children&&group.children.length))continue;if(group.parent)group.parent.remove(group);this.categoryGroups.delete(key);}}rebuildProjectMeshIndex(){this.meshesByProjectId=new Map();this.meshByElementKey=new Map();this.meshesByElementKey=new Map();this.meshesByCategoryUiKey=new Map();this.meshesByProjectCategoryUiKey=new Map();this.elementKeyByCategoryUiKey=new Map();this.elementKeyByProjectId=new Map();for(const mesh of this.meshes||[]){if(!mesh||!mesh.userData)continue;const element=mesh.userData.element;const projectId=element&&element.ProjectId;const elementKey=mesh.userData.elementKey;if(elementKey){if(!this.meshByElementKey.has(elementKey))this.meshByElementKey.set(elementKey,mesh);if(!this.meshesByElementKey.has(elementKey))this.meshesByElementKey.set(elementKey,[]);this.meshesByElementKey.get(elementKey).push(mesh);}if(projectId){if(!this.meshesByProjectId.has(projectId))this.meshesByProjectId.set(projectId,[]);this.meshesByProjectId.get(projectId).push(mesh);if(elementKey){if(!this.elementKeyByProjectId.has(projectId))this.elementKeyByProjectId.set(projectId,new Set());this.elementKeyByProjectId.get(projectId).add(elementKey);}}const categoryUiKey=this.getCategoryUiKeyForElement?this.getCategoryUiKeyForElement(element):null;if(categoryUiKey){if(!this.meshesByCategoryUiKey.has(categoryUiKey))this.meshesByCategoryUiKey.set(categoryUiKey,[]);this.meshesByCategoryUiKey.get(categoryUiKey).push(mesh);if(!this.elementKeyByCategoryUiKey.has(categoryUiKey))this.elementKeyByCategoryUiKey.set(categoryUiKey,new Set());if(elementKey)this.elementKeyByCategoryUiKey.get(categoryUiKey).add(elementKey);if(projectId){const projectCategoryKey=projectId+'||'+categoryUiKey;if(!this.meshesByProjectCategoryUiKey.has(projectCategoryKey))this.meshesByProjectCategoryUiKey.set(projectCategoryKey,[]);this.meshesByProjectCategoryUiKey.get(projectCategoryKey).push(mesh);}}}}getMeshesForProjectIds(projectIds){const ids=new Set(projectIds||[]);if(!ids.size||!this.meshesByProjectId)return null;const result=[];const seen=new Set();for(const id of ids){const meshes=this.meshesByProjectId.get(id)||[];for(const mesh of meshes){if(!mesh||seen.has(mesh))continue;seen.add(mesh);result.push(mesh);}}return result;}configureRenderPerformanceForModel(model,resetCounters){const geometries=model&&Array.isArray(model.geometries)?model.geometries:[];const elements=model&&Array.isArray(model.elements)?model.elements:[];const largeModel=this.isLargeViewerModel(model);const objectPressure=geometries.length>5000||elements.length>5000;this.performanceRenderMode=!!(largeModel||objectPressure);if(!this.performanceRenderMode&&this.navigationPerformanceActive)this.restoreNavigationPerformanceMode();if(resetCounters){this.generatedEdgeObjectCount=0;this.embeddedLineObjectCount=0;}else{this.generatedEdgeObjectCount=this.countGeneratedEdgeObjects();this.embeddedLineObjectCount=this.countEmbeddedLineObjects();}const visualDetailLevel=this.getVisualDetailLevel();if(visualDetailLevel==='high'){this.generatedEdgeObjectBudget=Number.POSITIVE_INFINITY;this.embeddedLineObjectBudget=Number.POSITIVE_INFINITY;}else if(this.performanceRenderMode){this.generatedEdgeObjectBudget=Math.max(1200,Math.min(8000,Math.floor(geometries.length*1.15)));this.embeddedLineObjectBudget=Math.max(300,Math.min(1800,Math.floor(geometries.length*0.18)));}else{this.generatedEdgeObjectBudget=Number.POSITIVE_INFINITY;this.embeddedLineObjectBudget=Number.POSITIVE_INFINITY;}this.renderPerformanceStats={active:this.performanceRenderMode,geometries:geometries.length,elements:elements.length,generatedEdgeObjectBudget:this.generatedEdgeObjectBudget,compactFallbackEdgeCount:this.compactFallbackEdgeCount||0,compactFallbackEdgeSegmentCount:this.compactFallbackEdgeSegmentCount||0,embeddedLineObjectBudget:this.embeddedLineObjectBudget,visualDetailPreference:this.visualDetailPreference||'auto',visualDetailLevel};debugLog('info','viewer:performanceRenderMode',this.renderPerformanceStats,true);}countGeneratedEdgeObjects(){let count=0;for(const mesh of this.meshes||[]){for(const child of mesh.children||[]){if(child&&child.isLineSegments&&child.userData&&child.userData.isGeneratedEdge)count++;}}return count;}countEmbeddedLineObjects(){let count=0;for(const mesh of this.meshes||[]){for(const child of mesh.children||[]){if(child&&child.isLineSegments&&child.userData&&child.userData.isLineGeometry)count++;}}return count;}getLoadProgressInterval(model){return this.isLargeViewerModel(model)?120:25;}getLoadProgressReportIntervalMs(model){return this.isLargeViewerModel(model)?220:120;}getLoadYieldIntervalMs(model){return this.isLargeViewerModel(model)?140:60;}isLargeViewerModel(model){if(!model)return false;if(model.largeModelMode)return true;const geometries=model.geometries||[];const elements=model.elements||[];if(geometries.length>8000||elements.length>8000)return true;let vertices=0;for(let i=0;i<geometries.length;i+=Math.max(1,Math.floor(geometries.length/250))){vertices+=getGeometryVertexCount(geometries[i]);if(vertices>650000)return true;}return false;}isPageHidden(){return typeof document!=='undefined'&&document.hidden;}yieldDuringLoad(){if(this.isPageHidden())return this.yieldWithMessageChannel();return new Promise(resolve=>setTimeout(resolve,0));}yieldWithMessageChannel(){if(typeof MessageChannel==='undefined')return new Promise(resolve=>setTimeout(resolve,0));return new Promise(resolve=>{const channel=new MessageChannel();channel.port1.onmessage=()=>{channel.port1.close();channel.port2.close();resolve();};channel.port2.postMessage(0);});}getBaseVisibleElementKeySet(){const keys=new Set();for(const mesh of this.meshes||[]){const key=this.getMeshKey?this.getMeshKey(mesh):null;const baseVisible=!(mesh&&mesh.userData&&mesh.userData.baseVisible===false);if(key&&baseVisible)keys.add(key);}return keys;}getDisplayVisibleElementKeySet(options){const opts=options&&typeof options==='object'?options:{};const keys=new Set();const includeClashForced=opts.includeClashForced===true;for(const mesh of this.meshes||[]){if(!mesh||!mesh.userData)continue;const key=this.getMeshKey?this.getMeshKey(mesh):mesh.userData.elementKey;if(!key)continue;let visible=mesh.userData.baseVisible!==false;if(this.dataPanelFilterActive&&this.dataPanelFilterKeys&&!this.dataPanelFilterKeys.has(key))visible=false;const works=this.visualWorksStates&&this.visualWorksStates.get(key);if(works&&works.hidden)visible=false;if(includeClashForced&&this.visualClashForceVisibleKeys&&this.visualClashForceVisibleKeys.has(key))visible=true;if(this.visualClashHiddenKeys&&this.visualClashHiddenKeys.has(key))visible=false;if(visible)keys.add(key);}return keys;}runWhenViewerIdle(callback,delayMs){const run=()=>{if(typeof requestIdleCallback==='function'){requestIdleCallback(()=>callback(),{timeout:1800});}else{setTimeout(callback,0);}};return setTimeout(run,Math.max(0,Number(delayMs)||0));}scheduleVisualCachePrewarm(reason){if(!this.meshes||!this.meshes.length)return;if(reason==='load-complete'){if(this.visualCachePrewarmTimer){clearTimeout(this.visualCachePrewarmTimer);this.visualCachePrewarmTimer=null;}if(this.recordPerformanceOperation)this.recordPerformanceOperation('prewarmTransparencyCaches:deferred',0,{reason});return;}if(this.visualCachePrewarmTimer)clearTimeout(this.visualCachePrewarmTimer);this.visualCachePrewarmTimer=this.runWhenViewerIdle(()=>this.prewarmTransparencyCaches(reason||'idle'),1200);}createViewerLoadBreakdown(model,totalGeometries,clearMs){const now=this.getPerformanceNow?this.getPerformanceNow():Date.now();return{version:'fase-1-fix-seleccion-carga-rapida-2026-06-30',startedAt:new Date().toISOString(),startedMs:now,totalMs:0,clearMs:+Math.max(0,Number(clearMs)||0).toFixed(3),totalGeometries:totalGeometries||0,elements:model&&model.elements?model.elements.length:0,largeModelMode:!!(model&&model.largeModelMode),visualDetailLevel:this.getVisualDetailLevel?this.getVisualDetailLevel():this.visualDetailLevel,memoryStart:this.getViewerLoadMemorySnapshot(),memoryEnd:null,counters:{visitedGeometries:0,processedGeometries:0,skippedNoElement:0,skippedEmptyGeometry:0,skippedNoDrawable:0,meshesCreated:0,lineObjectsCreated:0,edgeDecisionTrue:0,edgeDecisionFalse:0,edgesCreated:0,edgeCreateNull:0,edgeCreateErrors:0,edgeGeometryCacheHits:0,edgeGeometryCacheMisses:0,createMeshErrors:0,progressYields:0,progressUpdates:0,heavyGeometries:0},geometryTotals:{vertices:0,indices:0,lineIndices:0,maxVertices:0,maxIndices:0,maxLineIndices:0},phases:{},samples:[],final:{}};}getViewerLoadMemorySnapshot(){try{const memory=typeof performance!=='undefined'&&performance.memory?performance.memory:null;if(!memory)return null;return{usedMB:Math.round(memory.usedJSHeapSize/1048576),totalMB:Math.round(memory.totalJSHeapSize/1048576),limitMB:Math.round(memory.jsHeapSizeLimit/1048576)};}catch(_){return null;}}addViewerLoadPhaseMetric(breakdown,name,durationMs,details){if(!breakdown||!name)return;const duration=+Math.max(0,Number(durationMs)||0).toFixed(3);if(!breakdown.phases[name])breakdown.phases[name]={count:0,totalMs:0,avgMs:0,maxMs:0,lastMs:0};const phase=breakdown.phases[name];phase.count++;phase.totalMs=+(phase.totalMs+duration).toFixed(3);phase.lastMs=duration;phase.maxMs=Math.max(phase.maxMs||0,duration);phase.avgMs=+(phase.totalMs/Math.max(1,phase.count)).toFixed(3);if(details&&(duration>=12||details.forceSample)){this.pushLimited(breakdown.samples,Object.assign({t:+Math.max(0,(this.getPerformanceNow?this.getPerformanceNow():Date.now())-(breakdown.startedMs||0)).toFixed(3),phase:name,ms:duration},details||{}),180);}}recordActiveViewerLoadPhase(name,durationMs,details){if(!this.activeViewerLoadBreakdown)return;this.addViewerLoadPhaseMetric(this.activeViewerLoadBreakdown,name,durationMs,details||null);}finishViewerLoadBreakdown(breakdown,details){if(!breakdown)return null;const now=this.getPerformanceNow?this.getPerformanceNow():Date.now();breakdown.totalMs=+Math.max(0,now-(breakdown.startedMs||now)).toFixed(3);breakdown.finishedAt=new Date().toISOString();breakdown.memoryEnd=this.getViewerLoadMemorySnapshot();breakdown.final=Object.assign({},details||{});return breakdown;}prewarmTransparencyCaches(reason){if(this.visualCachePrewarmInProgress||!this.meshes||!this.meshes.length)return false;if(this.visualMassState&&this.visualMassState.active)return false;const metricsStart=this.getPerformanceNow?this.getPerformanceNow():Date.now();const allKeys=this.getBaseVisibleElementKeySet?this.getBaseVisibleElementKeySet():new Set();if(!allKeys.size)return false;this.visualCachePrewarmInProgress=true;const previousMassState=this.visualMassState||null;const previousFadedKeys=new Set(this.fadedMeshKeys||[]);const previousFadeActive=!!this.fadeActive;try{const percent=this.getConfiguredTransparencyPercent?this.getConfiguredTransparencyPercent(85):85;const opacity=this.getConfiguredTransparencyOpacity?this.getConfiguredTransparencyOpacity(0.02,percent):0.02;const fadedColor=this.getConfiguredTransparencyColor?this.getConfiguredTransparencyColor():0xbebebe;this.visualMassState={active:true,activeKeys:new Set(),allKeys,highlightColor:0x5DA3F6,highlightOpacity:0.7,fadedColor,fadedOpacity:opacity,prewarm:true};if(!this.fadedMeshKeys)this.fadedMeshKeys=new Set();this.fadedMeshKeys.clear();for(const key of allKeys)this.fadedMeshKeys.add(key);this.fadeActive=true;if(this.rebuildMassTransparencyChunks)this.rebuildMassTransparencyChunks(false);if(this.rebuildGroupedTransparentEdges)this.rebuildGroupedTransparentEdges(false);if(this.clearGroupedTransparentEdges)this.clearGroupedTransparentEdges(false);if(this.clearMassTransparencyChunks)this.clearMassTransparencyChunks(false);this.transparencyCachePrewarmDone=true;this.transparencyCachePrewarmCount=(this.transparencyCachePrewarmCount||0)+1;if(this.recordPerformanceOperation)this.recordPerformanceOperation('prewarmTransparencyCaches',(this.getPerformanceNow?this.getPerformanceNow():Date.now())-metricsStart,{reason:reason||'idle',allKeys:allKeys.size,massChunks:this.massTransparencyChunkMeshes?this.massTransparencyChunkMeshes.length:0,edgeBatches:this.groupedTransparentEdgeBatches?this.groupedTransparentEdgeBatches.length:0});return true;}finally{this.visualMassState=previousMassState;if(!this.fadedMeshKeys)this.fadedMeshKeys=new Set();this.fadedMeshKeys.clear();for(const key of previousFadedKeys)this.fadedMeshKeys.add(key);this.fadeActive=previousFadeActive;this.visualCachePrewarmInProgress=false;}}deferNormalChunkRefresh(reason,delayMs){if(this.clashModeActive===true){if(this.recordPerformanceOperation)this.recordPerformanceOperation('normalChunks:deferred-skip-clash-mode',0,{reason:reason||'deferred'});return;}if(this.normalChunkRefreshDeferTimer)clearTimeout(this.normalChunkRefreshDeferTimer);this.normalChunkRefreshDeferTimer=this.runWhenViewerIdle(()=>{this.normalChunkRefreshDeferTimer=null;if(this.clashModeActive===true){if(this.recordPerformanceOperation)this.recordPerformanceOperation('normalChunks:deferred-cancel-clash-mode',0,{reason:reason||'deferred'});return;}if(!this.visualMassState||!this.visualMassState.active){if(this.rebuildNormalChunks)this.rebuildNormalChunks(false);if(this.recordPerformanceOperation)this.recordPerformanceOperation('normalChunks:deferred-refresh',0,{reason:reason||'deferred'});}},Math.max(350,Number(delayMs)||350));}async load(model,onProgress){const loadMetricsStart=this.getPerformanceNow?this.getPerformanceNow():Date.now();const operationId=++this.loadOperationId;this.currentModel=model||null;const clearStart=this.getPerformanceNow?this.getPerformanceNow():Date.now();this.clear();const treeVisibilityRevisionAtLoadStart=this.treeVisibilityGuardRevision||0;this.modelCoordinateOriginRevit=model&&Array.isArray(model.ModelCoordinateOrigin)?model.ModelCoordinateOrigin.slice(0,3):null;const clearMs=(this.getPerformanceNow?this.getPerformanceNow():Date.now())-clearStart;const total=model.geometries.length||1;const loadBreakdown=this.createViewerLoadBreakdown?this.createViewerLoadBreakdown(model,total,clearMs):null;this.activeViewerLoadBreakdown=loadBreakdown;if(loadBreakdown)this.addViewerLoadPhaseMetric(loadBreakdown,'viewer.clearPreviousModel',clearMs,{forceSample:clearMs>=1});const configureStart=this.getPerformanceNow?this.getPerformanceNow():Date.now();this.applyAdaptiveVisualDetailForModel(model);this.configureRenderPerformanceForModel(model,true);if(loadBreakdown)this.addViewerLoadPhaseMetric(loadBreakdown,'viewer.configureRenderPerformance',(this.getPerformanceNow?this.getPerformanceNow():Date.now())-configureStart,{forceSample:true});const progressInterval=this.getLoadProgressInterval(model);const progressReportIntervalMs=this.getLoadProgressReportIntervalMs?this.getLoadProgressReportIntervalMs(model):200;const yieldIntervalMs=this.getLoadYieldIntervalMs?this.getLoadYieldIntervalMs(model):100;let lastProgressReportMs=loadMetricsStart;let lastLoadYieldMs=loadMetricsStart;debugLog('info','viewer:load:start',{totalGeometries:total,elements:model&&model.elements?model.elements.length:0,largeModelMode:!!(model&&model.largeModelMode),progressInterval,progressReportIntervalMs,yieldIntervalMs},true);for(let i=0;i<model.geometries.length;i++){if(operationId!==this.loadOperationId)return false;const geometryData=model.geometries[i];if(loadBreakdown)loadBreakdown.counters.visitedGeometries++;const element=model.elementsByGeometryId.get(geometryData.GeometryId);const vertexCount=getGeometryVertexCount(geometryData);const indexCount=getArrayLikeLength(geometryData.Indices);const lineIndexCount=getArrayLikeLength(geometryData.LineIndices);const hasTriangleIndices=indexCount>=3;const hasLineIndices=lineIndexCount>=2;if(loadBreakdown){loadBreakdown.geometryTotals.vertices+=vertexCount;loadBreakdown.geometryTotals.indices+=indexCount;loadBreakdown.geometryTotals.lineIndices+=lineIndexCount;loadBreakdown.geometryTotals.maxVertices=Math.max(loadBreakdown.geometryTotals.maxVertices||0,vertexCount);loadBreakdown.geometryTotals.maxIndices=Math.max(loadBreakdown.geometryTotals.maxIndices||0,indexCount);loadBreakdown.geometryTotals.maxLineIndices=Math.max(loadBreakdown.geometryTotals.maxLineIndices||0,lineIndexCount);if(vertexCount>8000)loadBreakdown.counters.heavyGeometries++;}if(!element){if(loadBreakdown)loadBreakdown.counters.skippedNoElement++;continue;}if(!vertexCount){if(loadBreakdown)loadBreakdown.counters.skippedEmptyGeometry++;continue;}if(!hasTriangleIndices&&!hasLineIndices){if(loadBreakdown)loadBreakdown.counters.skippedNoDrawable++;continue;}if(loadBreakdown)loadBreakdown.counters.processedGeometries++;if(i%progressInterval===0||vertexCount>8000){debugLog('info','viewer:geometry:beforeMesh',{index:i+1,total,geometryId:geometryData.GeometryId,elementId:element.ElementId,category:element.CategoryName||element.CategoryId,vertexCount},true);}let mesh=null;try{const createMeshStart=this.getPerformanceNow?this.getPerformanceNow():Date.now();mesh=this.createMesh(geometryData,element,model.materialsById);if(loadBreakdown)this.addViewerLoadPhaseMetric(loadBreakdown,'viewer.createMesh',(this.getPerformanceNow?this.getPerformanceNow():Date.now())-createMeshStart,{index:i+1,geometryId:geometryData.GeometryId||'',vertices:vertexCount,indices:indexCount,lineIndices:lineIndexCount});}catch(error){if(loadBreakdown)loadBreakdown.counters.createMeshErrors++;debugError('viewer:createMesh:error',error,{index:i+1,total,geometryId:geometryData.GeometryId,elementId:element&&element.ElementId,vertexCount});continue;}if(!mesh)continue;if(loadBreakdown){loadBreakdown.counters.meshesCreated++;if(mesh&&mesh.isLineSegments)loadBreakdown.counters.lineObjectsCreated++;}const registerStart=this.getPerformanceNow?this.getPerformanceNow():Date.now();this.addMeshToCategoryGroup(mesh);this.registerMesh(mesh);this.visibleCategories.add(element.CategoryId);if(loadBreakdown)this.addViewerLoadPhaseMetric(loadBreakdown,'viewer.registerMeshAndIndexes',(this.getPerformanceNow?this.getPerformanceNow():Date.now())-registerStart,{index:i+1});const edgeDecisionStart=this.getPerformanceNow?this.getPerformanceNow():Date.now();const shouldCreateEdges=this.shouldGenerateEdgesForGeometry(geometryData,element,model,total);if(loadBreakdown){this.addViewerLoadPhaseMetric(loadBreakdown,'viewer.shouldGenerateEdges',(this.getPerformanceNow?this.getPerformanceNow():Date.now())-edgeDecisionStart,{index:i+1});if(shouldCreateEdges)loadBreakdown.counters.edgeDecisionTrue++;else loadBreakdown.counters.edgeDecisionFalse++;}if(shouldCreateEdges){try{const edgeStart=this.getPerformanceNow?this.getPerformanceNow():Date.now();const edge=this.createEdges(mesh);if(loadBreakdown)this.addViewerLoadPhaseMetric(loadBreakdown,'viewer.createEdges',(this.getPerformanceNow?this.getPerformanceNow():Date.now())-edgeStart,{index:i+1,geometryId:geometryData.GeometryId||'',vertices:vertexCount,indices:indexCount});if(edge){mesh.add(edge);this.edges.push(edge);this.generatedEdgeObjectCount++;if(loadBreakdown)loadBreakdown.counters.edgesCreated++;}else if(loadBreakdown)loadBreakdown.counters.edgeCreateNull++;}catch(error){if(loadBreakdown)loadBreakdown.counters.edgeCreateErrors++;debugError('viewer:createEdges:error',error,{index:i+1,total,geometryId:geometryData.GeometryId,elementId:element&&element.ElementId});}}const hasDetailedEdge=this.hasDetailedGeneratedEdge?this.hasDetailedGeneratedEdge(mesh):false;if(!hasDetailedEdge&&this.ensureCompactFallbackEdgeCoverage){const fallbackCreated=this.ensureCompactFallbackEdgeCoverage(mesh,shouldCreateEdges?'detailed-edge-empty':'detailed-edge-budget');if(loadBreakdown&&fallbackCreated){loadBreakdown.counters.compactFallbackEdges=(loadBreakdown.counters.compactFallbackEdges||0)+1;}}const loopNow=this.getPerformanceNow?this.getPerformanceNow():Date.now();const shouldReportProgress=i===0||i+1===total||(loopNow-lastProgressReportMs)>=progressReportIntervalMs;const shouldYieldForResponsiveness=i===0||i+1===total||(loopNow-lastLoadYieldMs)>=yieldIntervalMs;if(shouldReportProgress||shouldYieldForResponsiveness){if(shouldReportProgress){const geometryPercent=Math.max(0,Math.min(88,Math.round(((i+1)/Math.max(1,total))*88)));debugLog('info','viewer:load:progress',{current:i+1,total,meshes:this.meshes.length,edges:this.edges.length,geometryPercent},true);if(onProgress)onProgress(i+1,total,{phase:'geometry',percent:geometryPercent,message:'Generando visor 3D: '+(i+1)+'/'+total});lastProgressReportMs=loopNow;if(loadBreakdown)loadBreakdown.counters.progressUpdates++;}if(shouldYieldForResponsiveness){const yieldStart=this.getPerformanceNow?this.getPerformanceNow():Date.now();await this.yieldDuringLoad();const yieldEnd=this.getPerformanceNow?this.getPerformanceNow():Date.now();lastLoadYieldMs=yieldEnd;if(loadBreakdown){loadBreakdown.counters.progressYields++;this.addViewerLoadPhaseMetric(loadBreakdown,'viewer.progressYield',yieldEnd-yieldStart,{current:i+1,total});}}}}if(onProgress)onProgress(total,total,{phase:'geometry-complete',percent:88,message:'Geometría 3D preparada.'});if(operationId!==this.loadOperationId)return false;this.fullModelBounds=null;const levelGridStart=this.getPerformanceNow?this.getPerformanceNow():Date.now();this.updateLevelGridToModel();if(loadBreakdown)this.addViewerLoadPhaseMetric(loadBreakdown,'viewer.updateLevelGridToModel',(this.getPerformanceNow?this.getPerformanceNow():Date.now())-levelGridStart,{forceSample:true});debugLog('info','viewer:load:beforeFit',{meshes:this.meshes.length,edges:this.edges.length},true);const fitStart=this.getPerformanceNow?this.getPerformanceNow():Date.now();this.fitToModel();if(loadBreakdown)this.addViewerLoadPhaseMetric(loadBreakdown,'viewer.fitToModel',(this.getPerformanceNow?this.getPerformanceNow():Date.now())-fitStart,{forceSample:true});if(onProgress)onProgress(total,total,{phase:'optimizing',percent:90,message:'Optimizando visor 3D...'});if(this.yieldDuringLoad)await this.yieldDuringLoad();const normalChunkStart=this.getPerformanceNow?this.getPerformanceNow():Date.now();let normalChunkCount=0;if(this.rebuildNormalChunksForLoad){const rebuilt=await this.rebuildNormalChunksForLoad(true,{operationId,onProgress:(current,progressTotal,message,subphase)=>{const ratio=progressTotal>0?Math.max(0,Math.min(1,current/progressTotal)):0;const isEdges=subphase==='edges'||String(message||'').toLowerCase().includes('borde');const isOptimizationComplete=subphase==='complete'||subphase==='fallback';const percent=isOptimizationComplete?99:(isEdges?Math.round(96+ratio*3):Math.round(90+ratio*6));if(onProgress)onProgress(total,total,{phase:isEdges?'optimizing-edges':'optimizing-chunks',percent:Math.max(90,Math.min(99,percent)),message:message||(isEdges?'Optimizando bordes del visor 3D...':'Optimizando geometría del visor 3D...')});}});if(rebuilt===false&&operationId!==this.loadOperationId)return false;normalChunkCount=Number(rebuilt)||0;}else if(this.rebuildNormalChunks){try{normalChunkCount=this.rebuildNormalChunks(true)||0;}catch(error){try{this.clearNormalChunks&&this.clearNormalChunks(true);}catch(_){}if(typeof debugError==='function')debugError('viewer:normalChunks:loadFallbackLegacy',error,{});normalChunkCount=0;}}if(loadBreakdown)this.addViewerLoadPhaseMetric(loadBreakdown,'viewer.rebuildNormalChunksLoadComplete',(this.getPerformanceNow?this.getPerformanceNow():Date.now())-normalChunkStart,{chunks:normalChunkCount,forceSample:true});if(this.visibilityPredicate&&(this.treeVisibilityGuardRevision||0)>treeVisibilityRevisionAtLoadStart){const reconcileStart=this.getPerformanceNow?this.getPerformanceNow():Date.now();this.applyVisibilityState(null,{forceChunkRebuild:true,strictTreeVisibility:true,skipChunkVisibilitySync:true,async:false,reason:'load-final-tree-visibility-reconcile'});if(this.enforceTreeVisibilityMergedRenderGuard)this.enforceTreeVisibilityMergedRenderGuard(true,'load-final-tree-visibility-reconcile');if(loadBreakdown)this.addViewerLoadPhaseMetric(loadBreakdown,'viewer.reconcileTreeVisibilityAfterLoad',(this.getPerformanceNow?this.getPerformanceNow():Date.now())-reconcileStart,{forceSample:true});}if(onProgress)onProgress(total,total,{phase:'finalizing',percent:99,message:'Finalizando visor 3D...'});if(this.refreshSelectionIndexesAfterGeometryChange)this.refreshSelectionIndexesAfterGeometryChange('load-complete');const renderRequestStart=this.getPerformanceNow?this.getPerformanceNow():Date.now();this.requestRender&&this.requestRender('load-complete');if(loadBreakdown)this.addViewerLoadPhaseMetric(loadBreakdown,'viewer.requestRenderLoadComplete',(this.getPerformanceNow?this.getPerformanceNow():Date.now())-renderRequestStart,{forceSample:true});const prewarmScheduleStart=this.getPerformanceNow?this.getPerformanceNow():Date.now();this.scheduleVisualCachePrewarm&&this.scheduleVisualCachePrewarm('load-complete');if(loadBreakdown)this.addViewerLoadPhaseMetric(loadBreakdown,'viewer.scheduleVisualCachePrewarm',(this.getPerformanceNow?this.getPerformanceNow():Date.now())-prewarmScheduleStart,{forceSample:true});const loadMetricsMs=this.getPerformanceNow?this.getPerformanceNow()-loadMetricsStart:0;if(loadBreakdown){this.finishViewerLoadBreakdown(loadBreakdown,{meshes:this.meshes.length,edges:this.edges.length,generatedEdgeObjectCount:this.generatedEdgeObjectCount||0,compactFallbackEdgeCount:this.compactFallbackEdgeCount||0,compactFallbackEdgeSegmentCount:this.compactFallbackEdgeSegmentCount||0,embeddedLineObjectCount:this.embeddedLineObjectCount||0,edgeGeometryCacheSize:this.generatedEdgeGeometryCache?this.generatedEdgeGeometryCache.size:0,edgeGeometryCacheHits:this.generatedEdgeGeometryCacheHits||0,edgeGeometryCacheMisses:this.generatedEdgeGeometryCacheMisses||0,loadMs:+loadMetricsMs.toFixed(3)});this.activeViewerLoadBreakdown=null;}if(this.recordModelPerformanceSnapshot)this.recordModelPerformanceSnapshot('viewer:load:complete');debugLog('info','viewer:load:complete',{meshes:this.meshes.length,edges:this.edges.length,loadMs:+loadMetricsMs.toFixed(3),normalChunkFallbacks:this.normalChunkLoadFallbackCount||0},true);if(onProgress)onProgress(total,total,{phase:'complete',percent:100,message:'Visor 3D listo.'});return true;}async reloadProjects(model,projectIds,onProgress,changedElementKeys){if(model&&Array.isArray(model.ModelCoordinateOrigin)){this.modelCoordinateOriginRevit=model.ModelCoordinateOrigin.slice(0,3);}const reloadMetricsStart=this.getPerformanceNow?this.getPerformanceNow():Date.now();const operationId=++this.loadOperationId;if(model)this.currentModel=model;const ids=new Set(projectIds||[]);if(!model||!ids.size)return;const useDiff=changedElementKeys!=null;const changedKeys=new Set(changedElementKeys||[]);debugLog('info','viewer:reloadProjects:start',{projectIds:Array.from(ids),useDiff,changedKeys:changedKeys.size},true);this.removeProjectMeshes(ids,useDiff?changedKeys:null);this.updateUnchangedProjectMeshElements(model,ids,changedKeys);this.configureRenderPerformanceForModel(model,false);const geometries=(model.geometries||[]).filter(geometry=>{if(!geometry||!ids.has(geometry.ProjectId))return false;if(!useDiff)return true;const element=model.elementsByGeometryId.get(geometry.GeometryId);const key=element?element.ModelId+':'+element.ElementId:'';return changedKeys.has(key);});const total=geometries.length||1;const progressInterval=this.getLoadProgressInterval(model);const progressReportIntervalMs=this.getLoadProgressReportIntervalMs?this.getLoadProgressReportIntervalMs(model):200;const yieldIntervalMs=this.getLoadYieldIntervalMs?this.getLoadYieldIntervalMs(model):100;let lastProgressReportMs=this.getPerformanceNow?this.getPerformanceNow():Date.now();let lastLoadYieldMs=lastProgressReportMs;for(let i=0;i<geometries.length;i++){if(operationId!==this.loadOperationId)return false;const geometryData=geometries[i];const element=model.elementsByGeometryId.get(geometryData.GeometryId);const hasTriangleIndices=getArrayLikeLength(geometryData.Indices)>=3;const hasLineIndices=getArrayLikeLength(geometryData.LineIndices)>=2;if(!element||!getGeometryVertexCount(geometryData)||(!hasTriangleIndices&&!hasLineIndices))continue;let mesh=null;try{mesh=this.createMesh(geometryData,element,model.materialsById);}catch(error){debugError('viewer:reloadProjects:createMesh:error',error,{index:i+1,total,geometry:getGeometryStats(geometryData),element:getElementStats(element)});continue;}if(!mesh)continue;this.addMeshToCategoryGroup(mesh);this.registerMesh(mesh);this.visibleCategories.add(element.CategoryId);if(this.shouldGenerateEdgesForGeometry(geometryData,element,model,total)){try{const edge=this.createEdges(mesh);if(edge){mesh.add(edge);this.edges.push(edge);this.generatedEdgeObjectCount++;}}catch(error){debugError('viewer:reloadProjects:createEdges:error',error,{index:i+1,total,geometry:getGeometryStats(geometryData),element:getElementStats(element)});}}if(this.ensureCompactFallbackEdgeCoverage){this.ensureCompactFallbackEdgeCoverage(mesh,'reload-detailed-edge-unavailable');}const loopNow=this.getPerformanceNow?this.getPerformanceNow():Date.now();const shouldReportProgress=i===0||i+1===total||(loopNow-lastProgressReportMs)>=progressReportIntervalMs;const shouldYieldForResponsiveness=i===0||i+1===total||(loopNow-lastLoadYieldMs)>=yieldIntervalMs;if(shouldReportProgress||shouldYieldForResponsiveness){if(shouldReportProgress){debugLog('info','viewer:reloadProjects:progress',{current:i+1,total,meshes:this.meshes.length,edges:this.edges.length},true);if(onProgress)onProgress(i+1,total);lastProgressReportMs=loopNow;}if(shouldYieldForResponsiveness){await this.yieldDuringLoad();lastLoadYieldMs=this.getPerformanceNow?this.getPerformanceNow():Date.now();}}}if(onProgress)onProgress(total,total);if(operationId!==this.loadOperationId)return false;this.fullModelBounds=null;this.edges=this.meshes.flatMap(mesh=>(mesh.children||[]).filter(child=>child&&child.isLineSegments));this.updateLevelGridToModel();this.applyVisibilityState();if(this.refreshSelectionIndexesAfterGeometryChange)this.refreshSelectionIndexesAfterGeometryChange('reload-complete');this.requestRender&&this.requestRender('reload-complete');const reloadMetricsMs=this.getPerformanceNow?this.getPerformanceNow()-reloadMetricsStart:0;if(this.recordPerformanceOperation)this.recordPerformanceOperation('reloadProjects',reloadMetricsMs,{projectCount:ids.size,useDiff,changedKeys:changedKeys.size,meshes:this.meshes.length,edges:this.edges.length});if(this.recordModelPerformanceSnapshot)this.recordModelPerformanceSnapshot('viewer:reloadProjects:complete');debugLog('info','viewer:reloadProjects:complete',{meshes:this.meshes.length,edges:this.edges.length,reloadMs:+reloadMetricsMs.toFixed(3)},true);return true;}updateUnchangedProjectMeshElements(model,projectIds,changedKeys){const ids=new Set(projectIds||[]);if(!model||!ids.size)return;const elementsByKey=new Map();for(const element of model.elements||[]){if(!element||!ids.has(element.ProjectId))continue;elementsByKey.set(element.ModelId+':'+element.ElementId,element);}for(const mesh of this.meshes||[]){const key=mesh&&mesh.userData?mesh.userData.elementKey:null;if(!key||(changedKeys&&changedKeys.has(key)))continue;const nextElement=elementsByKey.get(key);if(nextElement)mesh.userData.element=nextElement;}}removeProjectMeshes(projectIds,elementKeysToRemove){const ids=new Set(projectIds||[]);const removeKeys=elementKeysToRemove?new Set(elementKeysToRemove):null;if(!ids.size)return;const keep=[];for(const mesh of this.meshes||[]){const projectId=mesh&&mesh.userData&&mesh.userData.element?mesh.userData.element.ProjectId:null;const key=mesh&&mesh.userData?mesh.userData.elementKey:null;if(!ids.has(projectId)||(removeKeys&&!removeKeys.has(key))){keep.push(mesh);continue;}if(key){this.meshByElementKey.delete(key);this.selectedKeys.delete(key);this.hiddenKeys.delete(key);this.isolatedKeys.delete(key);if(this.baseIsolationKeys)this.baseIsolationKeys.delete(key);if(this.fadedMeshKeys)this.fadedMeshKeys.delete(key);}this.removeMeshFromCategoryGroup(mesh);if(this.clearCompactFallbackEdgeForMesh)this.clearCompactFallbackEdgeForMesh(mesh);mesh.traverse(child=>this.disposeObject(child));}this.meshes=keep;this.rebuildProjectMeshIndex();this.cleanupEmptyCategoryGroups();this.selectedMeshes=(this.selectedMeshes||[]).filter(mesh=>mesh&&!ids.has(mesh.userData&&mesh.userData.element&&mesh.userData.element.ProjectId));this.selectedMesh=this.selectedMeshes.length===1?this.selectedMeshes[0]:null;if(!this.selectedMeshes.length){this.selectedKey=null;this.selectedKeys.clear();}this.edges=this.meshes.flatMap(mesh=>(mesh.children||[]).filter(child=>child&&child.isLineSegments));if(this.refreshSelectionIndexesAfterGeometryChange)this.refreshSelectionIndexesAfterGeometryChange('project-meshes-removed');}disposeObject(object){if(!object)return;if(object.geometry&&object.geometry.dispose&&!(object.geometry.userData&&(object.geometry.userData.sharedCached||object.geometry.userData.singleDrawCached||object.geometry.userData.generatedEdgeCached)))object.geometry.dispose();if(object.material){if(Array.isArray(object.material))object.material.forEach(material=>this.disposeMaterialIfOwned(material));else this.disposeMaterialIfOwned(object.material);}}disposeSharedGeometryCache(){if(!this.sharedGeometryCache)return;for(const geometry of this.sharedGeometryCache.values()){if(geometry&&geometry.dispose)geometry.dispose();}this.sharedGeometryCache.clear();}disposeSingleDrawGeometryCache(){if(!this.singleDrawGeometryCache)return;for(const geometry of this.singleDrawGeometryCache.values()){if(geometry&&geometry.dispose)geometry.dispose();}this.singleDrawGeometryCache.clear();}disposeMaterialIfOwned(material){if(!material||!material.dispose||this.isSharedMaterial(material))return;material.dispose();}isSharedMaterial(material){if(!material)return false;if(material.userData&&(material.userData.adaptiveMediumDepthOnly||material.userData.normalChunkDepthStableMaterial))return true;if([this.selectionMaterial,this.hoverMaterial,this.rectanglePreviewMaterial,this.edgeMaterial,this.hoverEdgeMaterial,this.rectanglePreviewEdgeMaterial,this.selectionEdgeMaterial,this.fadedEdgeMaterial,this.adaptiveMediumDepthMaterial,this.nonRenderedDataElementMaterial,this.nonRenderedDataElementLineMaterial].includes(material))return true;if(this.materialMapContains(this.lineMaterialByKey,material))return true;if(this.materialMapContains(this.baseMeshMaterialCache,material))return true;if(this.materialMapContains(this.simpleColorMaterialCache,material))return true;return this.materialMapContains(this.simpleEdgeMaterialCache,material);}materialMapContains(materialMap,material){for(const candidate of materialMap.values()){if(candidate===material)return true;}return false;}disposeMaterialMap(materialMap){const disposed=new Set();for(const material of materialMap.values()){if(!material||disposed.has(material)||!material.dispose)continue;disposed.add(material);material.dispose();}materialMap.clear();}createMesh(geometryData,element,materialsById){const sourceGeometryData=geometryData&&geometryData.SharedGeometryInstance&&geometryData.SharedBaseGeometryRef?geometryData.SharedBaseGeometryRef:geometryData;const vertices=sourceGeometryData.Vertices||[];const indices=sourceGeometryData.Indices||[];const lineIndices=sourceGeometryData.LineIndices||[];const hasTriangles=getArrayLikeLength(indices)>=3;const hasLines=getArrayLikeLength(lineIndices)>=2;const vertexCount=getGeometryVertexCount(sourceGeometryData);if(!vertexCount||(!hasTriangles&&!hasLines))return null;let positions;if(sourceGeometryData.PositionBuffer instanceof Float32Array){positions=sourceGeometryData.PositionBuffer;}else{positions=new Float32Array(vertices.length*3);for(let i=0;i<vertices.length;i++){positions[i*3]=vertices[i][0];positions[i*3+1]=vertices[i][2];positions[i*3+2]=-vertices[i][1];}}let object;if(hasTriangles){const materialIds=this.getGeometryMaterialIds(sourceGeometryData);const materials=this.buildMaterials(materialIds,materialsById,element);const geometry=geometryData&&geometryData.SharedGeometryInstance?this.getOrCreateSharedIndexedTriangleGeometry(sourceGeometryData,positions,indices,materials.materialIndexById):this.createIndexedTriangleGeometry(positions,indices);if(!(geometry.userData&&geometry.userData.sharedCached)){this.applyGeometryGroups(geometry,sourceGeometryData,materials.materialIndexById);}object=new THREE.Mesh(geometry,materials.materials);if(geometryData&&geometryData.SharedGeometryInstance){object.userData.sharedGeometryInstance=true;object.userData.sharedGeometryId=geometryData.SharedGeometryId||'';}if(hasLines&&this.shouldCreateEmbeddedLineObject(sourceGeometryData,element,true)){const lineObject=this.createLineSegments(positions,lineIndices,element);if(lineObject){object.add(lineObject);this.embeddedLineObjectCount++;}}}else{object=this.createLineSegments(positions,lineIndices,element);}if(!object)return null;this.applyGeometryObjectTransforms(object,element&&element.ModelTransform,geometryData&&geometryData.SharedGeometryInstance?geometryData.GeometryTransform:null);object.userData.element=element;object.userData.elementKey=element.ModelId+':'+element.ElementId;object.userData.categoryId=element.CategoryId;object.userData.isNonRenderedDataElement=this.isNonRenderedDataElement(element);object.userData.optimizedBinary=!!geometryData.OptimizedBinary;object.userData.geometryData=sourceGeometryData||geometryData;if(object.isMesh&&object.geometry)object.userData.baseGeometry=object.geometry;object.frustumCulled=true;return object;}getOrCreateSharedIndexedTriangleGeometry(geometryData,positions,indices,materialIndexById){if(!this.sharedGeometryCache)this.sharedGeometryCache=new Map();const materialKey=(this.getGeometryMaterialIds(geometryData)||[]).join('|');const key=(geometryData.GeometryId||'')+'::triangles::'+materialKey;const cached=this.sharedGeometryCache.get(key);if(cached)return cached;const geometry=this.createIndexedTriangleGeometry(positions,indices);geometry.userData=geometry.userData||{};geometry.userData.sharedCached=true;this.applyGeometryGroups(geometry,geometryData,materialIndexById);this.sharedGeometryCache.set(key,geometry);return geometry;}applySharedGeometryTransform(object,transform){this.applyGeometryObjectTransforms(object,null,transform);}getModelCoordinateOriginRevit(){return Array.isArray(this.modelCoordinateOriginRevit)?this.modelCoordinateOriginRevit.slice(0,3):null;}applyGeometryObjectTransforms(object,modelTransform,geometryTransform){if(!object||!window.THREE)return;const hasModelTransform=Array.isArray(modelTransform)&&modelTransform.length>=16;const hasGeometryTransform=Array.isArray(geometryTransform)&&geometryTransform.length>=16;if(!hasModelTransform&&!hasGeometryTransform)return;const matrix=hasModelTransform?this.createThreeMatrixFromRevitTransform(modelTransform):new THREE.Matrix4().identity();if(hasGeometryTransform){matrix.multiply(this.createThreeMatrixFromRevitTransform(geometryTransform));}object.matrix.copy(matrix);object.matrixAutoUpdate=false;object.updateMatrixWorld(true);}createThreeMatrixFromRevitTransform(transform){const bx={x:Number(transform[0])||0,y:Number(transform[1])||0,z:Number(transform[2])||0};const by={x:Number(transform[4])||0,y:Number(transform[5])||0,z:Number(transform[6])||0};const bz={x:Number(transform[8])||0,y:Number(transform[9])||0,z:Number(transform[10])||0};const o={x:Number(transform[12])||0,y:Number(transform[13])||0,z:Number(transform[14])||0};const c1=[bx.x,bx.z,-bx.y];const c2=[bz.x,bz.z,-bz.y];const c3=[-by.x,-by.z,by.y];const t=[o.x,o.z,-o.y];const matrix=new THREE.Matrix4();matrix.set(c1[0],c2[0],c3[0],t[0],c1[1],c2[1],c3[1],t[1],c1[2],c2[2],c3[2],t[2],0,0,0,1);return matrix;}createIndexedTriangleGeometry(positions,indices){const geometryStart=this.getPerformanceNow?this.getPerformanceNow():Date.now();const geometry=new THREE.BufferGeometry();const attributeStart=this.getPerformanceNow?this.getPerformanceNow():Date.now();geometry.setAttribute('position',new THREE.BufferAttribute(positions,3));this.recordActiveViewerLoadPhase('geometry.setPositionAttribute',(this.getPerformanceNow?this.getPerformanceNow():Date.now())-attributeStart,null);const indexStart=this.getPerformanceNow?this.getPerformanceNow():Date.now();this.setGeometryIndexFast(geometry,indices);this.recordActiveViewerLoadPhase('geometry.setIndex',(this.getPerformanceNow?this.getPerformanceNow():Date.now())-indexStart,null);const boxStart=this.getPerformanceNow?this.getPerformanceNow():Date.now();geometry.computeBoundingBox();this.recordActiveViewerLoadPhase('geometry.computeBoundingBox',(this.getPerformanceNow?this.getPerformanceNow():Date.now())-boxStart,null);const sphereStart=this.getPerformanceNow?this.getPerformanceNow():Date.now();geometry.computeBoundingSphere();this.recordActiveViewerLoadPhase('geometry.computeBoundingSphere',(this.getPerformanceNow?this.getPerformanceNow():Date.now())-sphereStart,null);this.recordActiveViewerLoadPhase('geometry.createIndexedTriangleGeometry.total',(this.getPerformanceNow?this.getPerformanceNow():Date.now())-geometryStart,null);return geometry;}setGeometryIndexFast(geometry,indices){if(!geometry||!indices)return;const indexCount=getArrayLikeLength(indices);if(!indexCount)return;if(indices.isBufferAttribute){geometry.setIndex(indices);return;}if(ArrayBuffer.isView(indices)){geometry.setIndex(new THREE.BufferAttribute(indices,1));return;}if(Array.isArray(indices)){geometry.setIndex(indices);}}createExpandedTriangleGeometry(positions,indices){const count=getArrayLikeLength(indices);const expanded=new Float32Array(count*3);for(let i=0;i<count;i++){const vertexIndex=indices[i]||0;const source=vertexIndex*3;const target=i*3;expanded[target]=positions[source]||0;expanded[target+1]=positions[source+1]||0;expanded[target+2]=positions[source+2]||0;}const geometry=new THREE.BufferGeometry();geometry.setAttribute('position',new THREE.BufferAttribute(expanded,3));geometry.computeBoundingBox();geometry.computeBoundingSphere();return geometry;}shouldCreateEmbeddedLineObject(geometryData,element,hasTriangles){if(!geometryData||!element)return false;const lineIndexCount=getArrayLikeLength(geometryData.LineIndices);if(lineIndexCount<2)return false;if(!hasTriangles)return true;if(!this.performanceRenderMode)return true;if(this.isLowObjectBudgetCategory(element))return false;if(lineIndexCount>1800)return false;if(this.embeddedLineObjectCount>=this.embeddedLineObjectBudget)return false;return true;}createLineSegments(positions,lineIndices,element){if(!positions||!lineIndices||lineIndices.length<2)return null;const lineTotalStart=this.getPerformanceNow?this.getPerformanceNow():Date.now();const geometry=new THREE.BufferGeometry();const positionBuffer=positions instanceof Float32Array?positions:new Float32Array(positions);geometry.setAttribute('position',new THREE.BufferAttribute(positionBuffer,3));this.setGeometryIndexFast(geometry,lineIndices);const lineBoundsStart=this.getPerformanceNow?this.getPerformanceNow():Date.now();geometry.computeBoundingBox();geometry.computeBoundingSphere();this.recordActiveViewerLoadPhase('lineGeometry.computeBounds',(this.getPerformanceNow?this.getPerformanceNow():Date.now())-lineBoundsStart,null);const material=this.isNonRenderedDataElement(element)?this.createNonRenderedDataElementLineMaterial():this.createThreeLineMaterial(element.CategoryId,element.DisplayColor,element);const line=new THREE.LineSegments(geometry,material);line.userData.element=element;line.userData.elementKey=element.ModelId+':'+element.ElementId;line.userData.categoryId=element.CategoryId;line.userData.isNonRenderedDataElement=this.isNonRenderedDataElement(element);line.userData.isLineGeometry=true;this.recordActiveViewerLoadPhase('lineGeometry.createLineSegments.total',(this.getPerformanceNow?this.getPerformanceNow():Date.now())-lineTotalStart,null);return line;}createSafeOptimizedMaterial(element){const categoryId=element?element.CategoryId:null;const displayColor=element?element.DisplayColor:null;const color=this.resolveMaterialColor(null,displayColor,categoryId);return new THREE.MeshBasicMaterial({color,side:THREE.DoubleSide,transparent:false,opacity:1,depthTest:true,depthWrite:true,polygonOffset:false,polygonOffsetFactor:0,polygonOffsetUnits:0});}getStructuralRebarBaseColor(){return new THREE.Color(0x8f8f8f);}createThreeLineMaterial(categoryId,displayColor,element){const transparency=this.resolveTransparency(null,displayColor);const rebarGray=!!element&&this.isStructuralRebarCategory(element);const key='line:'+(categoryId||'default')+':'+(rebarGray?'rebar-gray':(displayColor?[displayColor.Red,displayColor.Green,displayColor.Blue,displayColor.Transparency].join(','):'category'));if(this.lineMaterialByKey.has(key))return this.lineMaterialByKey.get(key);const color=rebarGray?this.getStructuralRebarBaseColor():this.resolveMaterialColor(null,displayColor,categoryId);const opacity=this.opacityFromTransparency(transparency);const material=new THREE.LineBasicMaterial({color,transparent:opacity<1,opacity,depthTest:true,depthWrite:false});this.lineMaterialByKey.set(key,material);return material;}buildMaterials(materialIds,materialsById,element){const categoryId=element?element.CategoryId:null;const displayColor=element?element.DisplayColor:null;const materialIndexById=new Map();const materials=[];const useNonRenderedDataElement=this.isNonRenderedDataElement(element);const addMaterial=(materialId)=>{const hasRevitMaterial=!!(materialId&&materialsById&&materialsById.has(materialId));const key=useNonRenderedDataElement?'non-rendered-data-element':(hasRevitMaterial?materialId:'no-material');if(materialIndexById.has(key))return materialIndexById.get(key);const revitMaterial=hasRevitMaterial?materialsById.get(materialId):null;const material=useNonRenderedDataElement?this.createNonRenderedDataElementMaterial():this.createThreeMaterial(revitMaterial,categoryId,displayColor,hasRevitMaterial,element);const index=materials.length;materials.push(material);materialIndexById.set(key,index);if(!hasRevitMaterial||useNonRenderedDataElement){materialIndexById.set('category:default',index);if(categoryId)materialIndexById.set('category:'+categoryId,index);}return index;};if(!materialIds||!materialIds.length){addMaterial(null);}else{for(const id of materialIds)addMaterial(id||null);}return{materials,materialIndexById};}getMeshMaterialCacheKey(spec){const color=spec&&spec.color&&spec.color.isColor?spec.color.getHexString():new THREE.Color(Number(spec&&spec.color!=null?spec.color:0)).getHexString();const side=spec&&spec.side===THREE.FrontSide?'F':(spec&&spec.side===THREE.BackSide?'B':'D');const opacity=Math.max(0.02,Math.min(1,Number(spec&&spec.opacity==null?1:spec.opacity))).toFixed(3);const transparent=spec&&spec.transparent?'T':'O';const depthWrite=spec&&spec.depthWrite===false?'0':'1';const depthTest=spec&&spec.depthTest===false?'0':'1';const polygonOffset=spec&&spec.polygonOffset===false?'0':'1';const factor=Number(spec&&spec.polygonOffsetFactor==null?1:spec.polygonOffsetFactor).toFixed(3);const units=Number(spec&&spec.polygonOffsetUnits==null?1:spec.polygonOffsetUnits).toFixed(3);return['meshbasic',color,side,opacity,transparent,depthWrite,depthTest,polygonOffset,factor,units].join(':');}applyAmbientFormShading(material){return material;}setAmbientFormShadingStrength(strength,requestRender){return 0;}scheduleAmbientFormShadingRestore(){return false;}getNavigationPerformanceEdgeBatches(){const result=[];const seen=new Set();const collections=[this.normalChunkEdgeBatches,this.visualStateChunkEdgeBatches,this.groupedTransparentEdgeBatches];for(const collection of collections){for(const object of collection||[]){if(!object||seen.has(object))continue;seen.add(object);result.push(object);}}return result;}beginNavigationPerformanceMode(reason){if(!this.performanceRenderMode||!this.renderer)return false;if(this.navigationPerformanceRestoreTimer){clearTimeout(this.navigationPerformanceRestoreTimer);this.navigationPerformanceRestoreTimer=null;}if(this.navigationPerformanceActive)return true;this.navigationPerformanceActive=true;this.baseRendererPixelRatio=Math.min(window.devicePixelRatio||1,2);const navigationRatio=this.baseRendererPixelRatio;if(!this.renderer.getPixelRatio||Math.abs(this.renderer.getPixelRatio()-navigationRatio)>0.01){this.renderer.setPixelRatio(navigationRatio);}if(!this.navigationEdgeVisibilityStates)this.navigationEdgeVisibilityStates=new Map();this.navigationEdgeVisibilityStates.clear();if(this.recordPerformanceOperation){this.recordPerformanceOperation('navigationPerformance:begin',0,{reason:reason||'',pixelRatio:navigationRatio,hiddenEdgeBatches:0,transparencyPreserved:!!(this.visualMassState&&this.visualMassState.active),edgesPreserved:true});}this.requestRender&&this.requestRender('navigation-performance-begin');return true;}scheduleNavigationPerformanceRestore(){if(!this.navigationPerformanceActive)return false;if(this.navigationPerformanceRestoreTimer)clearTimeout(this.navigationPerformanceRestoreTimer);this.navigationPerformanceRestoreTimer=setTimeout(()=>{this.navigationPerformanceRestoreTimer=null;this.restoreNavigationPerformanceMode();},160);return true;}restoreNavigationPerformanceMode(){if(this.navigationPerformanceRestoreTimer){clearTimeout(this.navigationPerformanceRestoreTimer);this.navigationPerformanceRestoreTimer=null;}if(!this.navigationPerformanceActive)return false;this.navigationPerformanceActive=false;if(this.renderer){this.baseRendererPixelRatio=Math.min(window.devicePixelRatio||1,2);if(!this.renderer.getPixelRatio||Math.abs(this.renderer.getPixelRatio()-this.baseRendererPixelRatio)>0.01){this.renderer.setPixelRatio(this.baseRendererPixelRatio);}}for(const[edgeBatch,wasVisible]of this.navigationEdgeVisibilityStates||[]){if(edgeBatch)edgeBatch.visible=wasVisible;}if(this.navigationEdgeVisibilityStates)this.navigationEdgeVisibilityStates.clear();if(this.scheduleRaycastScreenCandidatePrewarm)this.scheduleRaycastScreenCandidatePrewarm('navigation-restored',70);this.requestRender&&this.requestRender('navigation-performance-restore');return true;}getCachedMeshBasicMaterial(spec){const key=this.getMeshMaterialCacheKey(spec||{});if(!this.baseMeshMaterialCache)this.baseMeshMaterialCache=new Map();if(!this.baseMeshMaterialCache.has(key)){const safeOpacity=Math.max(0.02,Math.min(1,Number(spec&&spec.opacity==null?1:spec.opacity)));const transparent=!!(spec&&spec.transparent)||safeOpacity<1;const polygonOffset=transparent&&spec&&spec.polygonOffset!==false;const material=new THREE.MeshBasicMaterial({color:spec&&spec.color&&spec.color.isColor?spec.color:Number(spec&&spec.color!=null?spec.color:0),side:spec&&spec.side!=null?spec.side:THREE.DoubleSide,transparent,opacity:safeOpacity,depthTest:spec&&spec.depthTest!==false,depthWrite:spec&&spec.depthWrite===false?false:!transparent,polygonOffset,polygonOffsetFactor:polygonOffset?Number(spec&&spec.polygonOffsetFactor==null?1:spec.polygonOffsetFactor):0,polygonOffsetUnits:polygonOffset?Number(spec&&spec.polygonOffsetUnits==null?1:spec.polygonOffsetUnits):0});material.userData=Object.assign({},material.userData||{},{sharedBaseCached:true,sharedMaterialKey:key});this.applyAmbientFormShading(material);this.baseMeshMaterialCache.set(key,material);}return this.baseMeshMaterialCache.get(key);}createThreeMaterial(revitMaterial,categoryId,displayColor,hasRevitMaterial,element){const isRebar=!!element&&this.isStructuralRebarCategory(element);if(!hasRevitMaterial){return this.getCachedMeshBasicMaterial({color:isRebar?this.getStructuralRebarBaseColor():this.noMaterialColor,side:THREE.DoubleSide,transparent:false,opacity:1,depthTest:true,depthWrite:true,polygonOffset:true,polygonOffsetFactor:1,polygonOffsetUnits:1});}const color=isRebar?this.getStructuralRebarBaseColor():this.resolveMaterialColor(revitMaterial,displayColor,categoryId);const opacity=this.opacityFromTransparency(this.resolveTransparency(revitMaterial,displayColor));const transparent=opacity<1;return this.getCachedMeshBasicMaterial({color,side:THREE.DoubleSide,transparent,opacity,depthTest:true,depthWrite:!transparent,polygonOffset:true,polygonOffsetFactor:1,polygonOffsetUnits:1});}createTransparentCurtainHostMaterial(){return this.createNonRenderedDataElementMaterial();}createNonRenderedDataElementMaterial(){if(!this.nonRenderedDataElementMaterial){const material=new THREE.MeshBasicMaterial({color:this.transparentCurtainHostColor,side:THREE.DoubleSide,transparent:true,opacity:0,depthTest:false,depthWrite:false,colorWrite:false});material.visible=false;material.userData=Object.assign({},material.userData||{},{isNonRenderedDataElementMaterial:true});this.nonRenderedDataElementMaterial=material;}return this.nonRenderedDataElementMaterial;}createNonRenderedDataElementLineMaterial(){if(!this.nonRenderedDataElementLineMaterial){const material=new THREE.LineBasicMaterial({color:this.transparentCurtainHostColor,transparent:true,opacity:0,depthTest:false,depthWrite:false,colorWrite:false});material.visible=false;material.userData=Object.assign({},material.userData||{},{isNonRenderedDataElementMaterial:true});this.nonRenderedDataElementLineMaterial=material;}return this.nonRenderedDataElementLineMaterial;}isNonRenderedDataElement(element){if(this.isVoidDataElement(element))return true;if(element&&typeof element.IsNonRenderedDataElement==='boolean'){return element.IsNonRenderedDataElement;}return this.isTelarDataElement(element)||this.isTransparentCurtainHostElement(element);}isVoidDataElement(element){if(!element)return false;const builtInCategory=String(element.BuiltInCategory||'').trim().toUpperCase();if(builtInCategory.includes('OPENING')||builtInCategory.includes('HOLE')||builtInCategory.includes('VOID')){return true;}const elementClass=String(element.ElementClass||'').trim().toLowerCase();return elementClass==='opening'||elementClass.endsWith('.opening');}isNonRenderedDataMesh(mesh){if(!mesh||!mesh.userData)return false;if(mesh.userData.isNonRenderedDataElement!=null)return mesh.userData.isNonRenderedDataElement===true;return this.isNonRenderedDataElement(mesh.userData.element);}isNonRenderedDataKey(key){if(!key)return false;const meshes=this.getMeshesForElementKey?this.getMeshesForElementKey(key):[];return!!(meshes&&meshes.some(mesh=>this.isNonRenderedDataMesh(mesh)));}isTelarDataElement(element){if(!element)return false;const builtInCategory=String(element.BuiltInCategory||'').trim().toUpperCase();if(builtInCategory==='OST_REVEALS')return true;const text=[element.CategoryName,element.SubcategoryName,element.FamilyName,element.TypeName,element.Name].map(value=>this.normalizeTextForSearch(value)).join(' ');const words=text.split(/[^a-z0-9]+/).filter(Boolean);return words.includes('telar')||words.includes('telares');}isTransparentCurtainHostElement(element){if(!element)return false;const builtInCategory=String(element.BuiltInCategory||'').trim().toUpperCase();const elementClass=String(element.ElementClass||'').trim().toLowerCase();const wallKind=String(element.WallKind||'').trim().toLowerCase();if(wallKind==='curtain'&&(!elementClass||elementClass==='wall'||elementClass.endsWith('.wall'))&&(!builtInCategory||builtInCategory==='OST_WALLS')){return true;}if(builtInCategory||elementClass||wallKind)return false;const categoryName=this.normalizeTextForSearch(element.CategoryName);const subcategoryName=this.normalizeTextForSearch(element.SubcategoryName);const familyName=this.normalizeTextForSearch(element.FamilyName);const typeName=this.normalizeTextForSearch(element.TypeName);const elementName=this.normalizeTextForSearch(element.Name);const componentCategoryText=[categoryName,subcategoryName].join(' ');if(componentCategoryText.includes('panel')||componentCategoryText.includes('paneles')||componentCategoryText.includes('montante')||componentCategoryText.includes('montantes')||componentCategoryText.includes('mullion')||componentCategoryText.includes('mullions')){return false;}const text=[categoryName,subcategoryName,familyName,typeName,elementName].join(' ');const isCurtain=text.includes('muro cortina')||text.includes('muros cortina')||text.includes('muro-cortina')||text.includes('curtain wall')||text.includes('curtainwall')||text.includes('curtain system');const isHostCategory=categoryName.includes('muro')||categoryName.includes('wall')||categoryName.includes('sistema')||categoryName.includes('system');return isCurtain&&isHostCategory;}normalizeTextForSearch(value){return String(value||'').normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase();}resolveMaterialColor(revitMaterial,displayColor,categoryId){if(revitMaterial){return new THREE.Color(this.colorChannel(revitMaterial.Red,200)/255,this.colorChannel(revitMaterial.Green,200)/255,this.colorChannel(revitMaterial.Blue,200)/255);}if(displayColor){return new THREE.Color(this.colorChannel(displayColor.Red,200)/255,this.colorChannel(displayColor.Green,200)/255,this.colorChannel(displayColor.Blue,200)/255);}return new THREE.Color(this.getCategoryColor(categoryId));}resolveTransparency(revitMaterial,displayColor){if(revitMaterial&&Number.isFinite(Number(revitMaterial.Transparency))){return Math.max(0,Math.min(100,Number(revitMaterial.Transparency)));}if(displayColor&&Number.isFinite(Number(displayColor.Transparency))){return Math.max(0,Math.min(100,Number(displayColor.Transparency)));}return 0;}opacityFromTransparency(transparency){const value=Math.max(0,Math.min(100,Number(transparency)||0));return Math.max(0.02,Math.min(1,1-value/100));}colorChannel(value,fallback){const number=Number(value);return Number.isFinite(number)?Math.max(0,Math.min(255,number)):fallback;}createOptimizedSingleMaterial(geometryData,element,materialsById){if(this.isNonRenderedDataElement(element)){return this.createNonRenderedDataElementMaterial();}const categoryId=element?element.CategoryId:null;const displayColor=element?element.DisplayColor:null;const materialId=this.getPrimaryGeometryMaterialId(geometryData,materialsById);const hasRevitMaterial=!!(materialId&&materialsById&&materialsById.has(materialId));const revitMaterial=hasRevitMaterial?materialsById.get(materialId):null;return this.createThreeMaterial(revitMaterial,categoryId,displayColor,hasRevitMaterial);}getPrimaryGeometryMaterialId(geometryData,materialsById){const candidates=[];if(geometryData&&Array.isArray(geometryData.MaterialRuns)){for(const run of geometryData.MaterialRuns){if(run&&run.materialId)candidates.push(run.materialId);}}if(geometryData&&Array.isArray(geometryData.MaterialRunIds)){for(const id of geometryData.MaterialRunIds)if(id)candidates.push(id);}if(geometryData&&geometryData.MaterialIds&&typeof geometryData.MaterialIds.length==='number'){const limit=Math.min(geometryData.MaterialIds.length,256);for(let i=0;i<limit;i++)if(geometryData.MaterialIds[i])candidates.push(geometryData.MaterialIds[i]);}for(const id of candidates){if(materialsById&&materialsById.has(id))return id;}return candidates.length?candidates[0]:null;}getGeometryMaterialIds(geometryData){if(geometryData&&Array.isArray(geometryData.MaterialRunIds)&&geometryData.MaterialRunIds.length){return geometryData.MaterialRunIds;}return geometryData&&geometryData.MaterialIds?geometryData.MaterialIds:[];}applyGeometryGroups(geometry,geometryData,materialIndexById){geometry.clearGroups();const position=geometry.getAttribute?geometry.getAttribute('position'):null;const indexCount=geometry.index?geometry.index.count:(position?position.count:0);const runs=geometryData&&Array.isArray(geometryData.MaterialRuns)?geometryData.MaterialRuns:null;if(runs&&runs.length){let coveredIndices=0;for(const run of runs){if(!run||!run.triangleCount)continue;const start=Math.max(0,Number(run.startTriangle)||0)*3;const count=Math.max(0,Number(run.triangleCount)||0)*3;if(!count||start>=indexCount)continue;const safeCount=Math.min(count,indexCount-start);const id=run.materialId||'category:default';geometry.addGroup(start,safeCount,materialIndexById.get(id)||0);coveredIndices+=safeCount;}if(coveredIndices>0){if(coveredIndices<indexCount&&geometry.groups.length===0)geometry.addGroup(0,indexCount,0);return;}geometry.clearGroups();}const materialIds=geometryData&&geometryData.MaterialIds?geometryData.MaterialIds:[];if(!materialIds||!materialIds.length){geometry.addGroup(0,indexCount,0);return;}let currentId=materialIds[0]||'category:default';let start=0;for(let tri=1;tri<materialIds.length;tri++){const id=materialIds[tri]||'category:default';if(id!==currentId){geometry.addGroup(start*3,(tri-start)*3,materialIndexById.get(currentId)||0);currentId=id;start=tri;}}geometry.addGroup(start*3,(materialIds.length-start)*3,materialIndexById.get(currentId)||0);}getEdgeDetectionAngleForElement(element){const visualDetailLevel=this.getVisualDetailLevel();if(this.isStructuralRebarCategory(element))return this.performanceRenderMode?70:(this.structuralRebarEdgeDetectionAngle||45);if(this.isLightweightTubularCurveElement(element)){return visualDetailLevel==='high'?40:62;}if(this.isOptimizedDenseMeshCategory(element)){return visualDetailLevel==='high'?(this.performanceRenderMode?24:16):(this.performanceRenderMode?32:22);}if(visualDetailLevel==='high'){if(this.isSpecialtyLowDetailCategory(element))return this.performanceRenderMode?36:(this.specialtyEdgeDetectionAngle||24);if(this.isReadableDetailEdgeCategory(element))return this.performanceRenderMode?16:10;if(this.isPrimaryReadableEdgeCategory(element))return this.performanceRenderMode?8:6;return Math.max(this.edgeDetectionAngle||2,6);}if(this.isPrimaryReadableEdgeCategory(element))return this.performanceRenderMode?18:12;if(this.isReadableDetailEdgeCategory(element))return this.performanceRenderMode?34:24;if(this.isSpecialtyLowDetailCategory(element))return this.performanceRenderMode?55:(this.specialtyEdgeDetectionAngle||32);return this.performanceRenderMode?28:Math.max(this.edgeDetectionAngle||2,12);}getGeneratedEdgeSegmentCount(edgeGeometry){if(!edgeGeometry||!edgeGeometry.getAttribute)return 0;const position=edgeGeometry.getAttribute('position');return position&&Number.isFinite(Number(position.count))?Math.floor(Number(position.count)/2):0;}hasPrecomputedEdgeTopology(geometryData){const edgeIndices=geometryData&&geometryData.EdgeLineIndices;return!!(edgeIndices&&typeof edgeIndices.length==='number'&&edgeIndices.length>=2);}getSemanticEdgeAngleProfile(geometryData){if(!geometryData||!this.hasPrecomputedEdgeTopology(geometryData))return null;if(!this.semanticEdgeAngleProfileCache)this.semanticEdgeAngleProfileCache=new WeakMap();const cached=this.semanticEdgeAngleProfileCache.get(geometryData);if(cached)return cached;const edgeIndices=geometryData.EdgeLineIndices;const edgeAngles=geometryData.EdgeAngles;const edgeCount=Math.floor(edgeIndices.length/2);if(!edgeAngles||typeof edgeAngles.length!=='number'||edgeAngles.length<edgeCount){const unavailable={available:false,curvedFacetTopology:false,recommendedThresholdAngle:0};this.semanticEdgeAngleProfileCache.set(geometryData,unavailable);return unavailable;}const sampleLimit=24000;const stride=Math.max(1,Math.floor(edgeCount/sampleLimit));const bins=new Map();let sampled=0;let boundaryCount=0;let finiteCount=0;let smoothFacetCount=0;let hardCount=0;for(let i=0;i<edgeCount;i+=stride){const encoded=Number(edgeAngles[i]);sampled++;if(encoded===65535){boundaryCount++;continue;}if(!Number.isFinite(encoded))continue;const degrees=encoded/100;if(degrees<1.5)continue;finiteCount++;if(degrees>=4&&degrees<=42){smoothFacetCount++;const bin=Math.max(2,Math.min(42,Math.round(degrees/2)*2));bins.set(bin,(bins.get(bin)||0)+1);}else if(degrees>42){hardCount++;}}let dominantFacetAngle=0;let dominantFacetCount=0;for(const[angle,count]of bins.entries()){if(count>dominantFacetCount){dominantFacetAngle=angle;dominantFacetCount=count;}}const meaningfulCount=Math.max(1,finiteCount+boundaryCount);const curvedFacetTopology=smoothFacetCount>=8&&dominantFacetCount>=6&&(dominantFacetCount/meaningfulCount>=0.07||smoothFacetCount/meaningfulCount>=0.28);const recommendedThresholdAngle=curvedFacetTopology?Math.max(32,Math.min(48,dominantFacetAngle+4)):0;const profile={available:true,sampled,boundaryCount,finiteCount,smoothFacetCount,hardCount,dominantFacetAngle,dominantFacetCount,curvedFacetTopology,recommendedThresholdAngle};this.semanticEdgeAngleProfileCache.set(geometryData,profile);return profile;}getSimplifiedExteriorEdgeSettings(mesh){if(!mesh||!mesh.userData||!mesh.userData.geometryData)return null;if(this.getVisualDetailLevel&&this.getVisualDetailLevel()!=='medium')return null;const element=mesh.userData.element;const geometryData=mesh.userData.geometryData;if(!element||!this.hasPrecomputedEdgeTopology(geometryData))return null;if(!(this.isPrimaryReadableEdgeCategory(element)||this.isReadableDetailEdgeCategory(element)||this.isOptimizedDenseMeshCategory(element)))return null;if(this.isLightweightTubularCurveElement(element)||this.isStructuralRebarCategory(element))return null;const triangleCount=this.getTriangleCountForEdgeBudget(geometryData);const dense=this.isOptimizedDenseMeshCategory(element);const semanticProfile=this.getSemanticEdgeAngleProfile?this.getSemanticEdgeAngleProfile(geometryData):null;const semanticMinAngle=semanticProfile&&semanticProfile.curvedFacetTopology?Math.max(this.performanceRenderMode?36:32,semanticProfile.recommendedThresholdAngle):0;return{triangleCount,minLengthRatio:dense?0.009:0.007,relaxedMinLengthRatio:dense?0.0045:0.0035,minAngle:Math.max(this.performanceRenderMode?16:10,semanticMinAngle),maxSegments:this.performanceRenderMode?360:720,minimumReadableSegments:8,preserveComponentDetails:true,semanticCurvedEdges:semanticMinAngle>0,preferDirectFallback:triangleCount>(this.performanceRenderMode?70000:120000)};}hasDetailedGeneratedEdge(mesh){return!!(mesh&&mesh.children&&mesh.children.some(child=>(child&&child.isLineSegments&&child.geometry&&child.userData&&child.userData.isGeneratedEdge)));}hasCompactFallbackEdge(mesh){const data=mesh&&mesh.userData;const geometry=data&&data.compactFallbackEdgeGeometry;const position=geometry&&geometry.getAttribute&&geometry.getAttribute('position');return!!(position&&position.count>=2);}hasReadableEdgeCoverageForMesh(mesh){return this.hasDetailedGeneratedEdge(mesh)||this.hasCompactFallbackEdge(mesh);}getCompactFallbackEdgeSettings(mesh){if(!mesh||!mesh.isMesh||!mesh.geometry||!mesh.userData)return null;const element=mesh.userData.element;if(!element||this.isNonRenderedDataElement(element))return null;const geometryData=mesh.userData.geometryData||null;const dense=this.isOptimizedDenseMeshCategory(element)||this.isReadableDetailEdgeCategory(element)||this.isSpecialtyLowDetailCategory(element)||this.isStructuralRebarCategory(element);const semanticProfile=this.getSemanticEdgeAngleProfile?this.getSemanticEdgeAngleProfile(geometryData):null;const semanticMinAngle=semanticProfile&&semanticProfile.curvedFacetTopology?Math.max(this.performanceRenderMode?36:32,semanticProfile.recommendedThresholdAngle):0;return{triangleCount:this.getTriangleCountForEdgeBudget(geometryData),minLengthRatio:dense?0.012:0.009,relaxedMinLengthRatio:dense?0.006:0.0045,minAngle:Math.max(this.performanceRenderMode?24:18,semanticMinAngle),maxSegments:this.performanceRenderMode?(dense?36:48):72,minimumReadableSegments:4,preserveComponentDetails:true,semanticCurvedEdges:semanticMinAngle>0,compactFallback:true};}getLowComplexityFullEdgeSettings(mesh){if(!mesh||!mesh.isMesh||!mesh.geometry||!mesh.userData)return null;const element=mesh.userData.element;const geometryData=mesh.userData.geometryData;if(!element||!this.hasPrecomputedEdgeTopology(geometryData))return null;if(this.isNonRenderedDataElement(element)||this.isLightweightTubularCurveElement(element)||this.isStructuralRebarCategory(element))return null;const topologyEdgeCount=Math.floor(geometryData.EdgeLineIndices.length/2);const triangleCount=this.getTriangleCountForEdgeBudget(geometryData);const maxTopologyEdges=this.performanceRenderMode?320:520;const maxTriangles=this.performanceRenderMode?4200:10000;if(!topologyEdgeCount||topologyEdgeCount>maxTopologyEdges)return null;if(triangleCount>maxTriangles)return null;const semanticProfile=this.getSemanticEdgeAngleProfile?this.getSemanticEdgeAngleProfile(geometryData):null;const semanticThreshold=semanticProfile&&semanticProfile.curvedFacetTopology?Math.max(this.performanceRenderMode?36:32,semanticProfile.recommendedThresholdAngle):0;return{topologyEdgeCount,triangleCount,thresholdAngle:semanticThreshold||(this.performanceRenderMode?2:1),semanticCurvedEdges:semanticThreshold>0};}createLowComplexityFullEdgeGeometry(mesh,settings){if(!mesh||!settings)return null;const geometry=this.getOrCreateGeneratedEdgeGeometry(mesh,settings.thresholdAngle);if(!geometry)return null;geometry.userData=Object.assign({},geometry.userData||{},{lowComplexityFullEdges:true,lowComplexityTopologyEdges:settings.topologyEdgeCount||0,semanticCurvedEdges:settings.semanticCurvedEdges===true});return geometry;}getSemanticCurvedEdgeSettings(mesh){if(!mesh||!mesh.isMesh||!mesh.geometry||!mesh.userData)return null;const element=mesh.userData.element;const geometryData=mesh.userData.geometryData;if(!element||!this.hasPrecomputedEdgeTopology(geometryData))return null;if(this.isNonRenderedDataElement(element)||this.isLightweightTubularCurveElement(element)||this.isStructuralRebarCategory(element))return null;const profile=this.getSemanticEdgeAngleProfile?this.getSemanticEdgeAngleProfile(geometryData):null;if(!profile||!profile.curvedFacetTopology)return null;const topologyEdgeCount=Math.floor(geometryData.EdgeLineIndices.length/2);const topologyScanLimit=this.performanceRenderMode?180000:520000;if(!topologyEdgeCount||topologyEdgeCount>topologyScanLimit)return null;const dense=this.isOptimizedDenseMeshCategory(element)||this.isReadableDetailEdgeCategory(element)||this.isSpecialtyLowDetailCategory(element);return{triangleCount:this.getTriangleCountForEdgeBudget(geometryData),topologyEdgeCount,minAngle:Math.max(this.performanceRenderMode?36:32,profile.recommendedThresholdAngle),minLengthRatio:dense?0.00045:0.0003,relaxedMinLengthRatio:dense?0.0002:0.00015,maxSegments:this.performanceRenderMode?(dense?520:720):(dense?1100:1500),minimumReadableSegments:4,preserveComponentDetails:true,semanticCurvedEdges:true};}createSemanticCurvedEdgeGeometry(mesh,settings){if(!mesh||!settings)return null;const baseGeometry=this.createSimplifiedExteriorEdgeGeometry?this.createSimplifiedExteriorEdgeGeometry(mesh,settings):null;const referenceGeometry=this.createTubularRepresentativeEdgeGeometry?this.createTubularRepresentativeEdgeGeometry(mesh,{forceCircular:true,longitudinalChains:2,includeTransverseEdges:false}):null;if(!baseGeometry&&!referenceGeometry)return null;if(!referenceGeometry){baseGeometry.userData=Object.assign({},baseGeometry.userData||{},{semanticCurvedEdges:true,semanticCurvedThresholdAngle:settings.minAngle||0});return baseGeometry;}const basePosition=baseGeometry&&baseGeometry.getAttribute?baseGeometry.getAttribute('position'):null;const referencePosition=referenceGeometry.getAttribute&&referenceGeometry.getAttribute('position');if(!referencePosition||!referencePosition.count)return baseGeometry;const baseArray=basePosition&&basePosition.array?basePosition.array:new Float32Array(0);const referenceArray=referencePosition.array;const geometryData=mesh.userData&&mesh.userData.geometryData;const geometryKey=(mesh.userData&&mesh.userData.sharedGeometryId)||(geometryData&&(geometryData.SharedGeometryId||geometryData.GeometryId))||(mesh.geometry&&mesh.geometry.uuid)||'';const cacheKey=geometryKey?['edgeSemanticCircularReferenceV1',geometryKey,settings.minAngle||0,settings.maxSegments||0].join('|'):'';if(!this.generatedEdgeGeometryCache)this.generatedEdgeGeometryCache=new Map();const cached=cacheKey?this.generatedEdgeGeometryCache.get(cacheKey):null;if(cached){this.generatedEdgeGeometryCacheHits=(this.generatedEdgeGeometryCacheHits||0)+1;return cached;}const positions=new Float32Array(baseArray.length+referenceArray.length);positions.set(baseArray,0);positions.set(referenceArray,baseArray.length);const geometry=new THREE.BufferGeometry();geometry.setAttribute('position',new THREE.BufferAttribute(positions,3));geometry.userData=Object.assign({},geometry.userData||{},{generatedEdgeCached:!!cacheKey,generatedEdgeCacheKey:cacheKey||'',semanticCurvedEdges:true,semanticCurvedThresholdAngle:settings.minAngle||0,circularParallelReferenceEdges:true,circularParallelReferenceSegments:this.getGeneratedEdgeSegmentCount(referenceGeometry)});if(geometry.computeBoundingBox)geometry.computeBoundingBox();if(geometry.computeBoundingSphere)geometry.computeBoundingSphere();if(cacheKey)this.generatedEdgeGeometryCache.set(cacheKey,geometry);this.generatedEdgeGeometryCacheMisses=(this.generatedEdgeGeometryCacheMisses||0)+1;return geometry;}getTubularRepresentativeEdgeGeometryCacheKey(mesh,longitudinalChains,includeTransverseEdges){if(!mesh||!mesh.geometry)return'';const geometryData=mesh.userData&&mesh.userData.geometryData;const sharedId=(mesh.userData&&mesh.userData.sharedGeometryId)||(geometryData&&(geometryData.SharedGeometryId||geometryData.GeometryId))||'';const position=mesh.geometry.getAttribute&&mesh.geometry.getAttribute('position');const index=mesh.geometry.index;const geometryKey=sharedId||mesh.geometry.uuid||'';if(!geometryKey||!position||!position.count)return'';return['edgeTubularRepresentativeV4',geometryKey,position.count,index&&index.count?index.count:0,Number.isFinite(Number(this.edgeDetectionAngle))?Number(this.edgeDetectionAngle):2,Math.max(0,Math.min(3,Number(longitudinalChains)||0)),includeTransverseEdges===false?0:1].join('|');}createTubularRepresentativeEdgeGeometry(mesh,options){if(!mesh||!mesh.geometry||!mesh.userData)return null;const element=mesh.userData.element;const geometryData=mesh.userData.geometryData;const forceCircular=!!(options&&options.forceCircular);if(!this.isLightweightTubularCurveElement(element)&&!forceCircular)return null;const positionAttr=mesh.geometry.getAttribute&&mesh.geometry.getAttribute('position');const source=positionAttr&&positionAttr.array;if(!source||source.length<6)return null;const requestedLongitudinalChains=options&&options.longitudinalChains!=null?Number(options.longitudinalChains):2;const maxLongitudinalChains=Math.max(0,Math.min(3,Number.isFinite(requestedLongitudinalChains)?requestedLongitudinalChains:2));const includeTransverseEdges=!(options&&options.includeTransverseEdges===false);const cacheKey=this.getTubularRepresentativeEdgeGeometryCacheKey(mesh,maxLongitudinalChains,includeTransverseEdges);if(!this.generatedEdgeGeometryCache)this.generatedEdgeGeometryCache=new Map();const cached=cacheKey?this.generatedEdgeGeometryCache.get(cacheKey):null;if(cached){this.generatedEdgeGeometryCacheHits=(this.generatedEdgeGeometryCacheHits||0)+1;return cached;}const vertexCount=Math.floor(source.length/3);const boundaryAngle=65535;const rawEdges=[];let longest=null;const emittedSourceEdges=new Set();const pointKey=(x,y,z)=>[x,y,z].map(value=>Math.round((Number(value)||0)*1e6)).join(',');const appendRawEdge=(ax,ay,az,bx,by,bz,angle)=>{const dx=bx-ax;const dy=by-ay;const dz=bz-az;const length=Math.sqrt(dx*dx+dy*dy+dz*dz);if(!(length>1e-9))return;const keyA=pointKey(ax,ay,az);const keyB=pointKey(bx,by,bz);const dedupeKey=keyA<keyB?keyA+'|'+keyB:keyB+'|'+keyA;if(emittedSourceEdges.has(dedupeKey))return;emittedSourceEdges.add(dedupeKey);const edge={ax,ay,az,bx,by,bz,dx,dy,dz,length,angle:Number.isFinite(Number(angle))?Number(angle):boundaryAngle};rawEdges.push(edge);if(!longest||length>longest.length)longest=edge;};const interactionGeometry=this.getGeneratedInteractionEdgeGeometry?this.getGeneratedInteractionEdgeGeometry(mesh):null;const interactionPosition=interactionGeometry&&interactionGeometry.getAttribute?interactionGeometry.getAttribute('position'):null;const interactionSource=interactionPosition&&interactionPosition.array;if(interactionSource&&interactionSource.length>=6){for(let i=0;i+5<interactionSource.length;i+=6){appendRawEdge(Number(interactionSource[i])||0,Number(interactionSource[i+1])||0,Number(interactionSource[i+2])||0,Number(interactionSource[i+3])||0,Number(interactionSource[i+4])||0,Number(interactionSource[i+5])||0,boundaryAngle);}}if(!rawEdges.length&&this.hasPrecomputedEdgeTopology(geometryData)){const edgeIndices=geometryData.EdgeLineIndices;const edgeCount=Math.floor(edgeIndices.length/2);const edgeAngles=geometryData.EdgeAngles;const hasAngles=edgeAngles&&edgeAngles.length>=edgeCount;const interactionAngle=Number.isFinite(Number(this.edgeDetectionAngle))?Number(this.edgeDetectionAngle):2;const thresholdHundredths=Math.max(0,Math.round(interactionAngle*100));for(let i=0;i<edgeCount;i++){const angle=hasAngles?Number(edgeAngles[i]):boundaryAngle;if(angle!==boundaryAngle&&angle<thresholdHundredths)continue;const a=edgeIndices[i*2];const b=edgeIndices[i*2+1];if(a==null||b==null||a<0||b<0||a>=vertexCount||b>=vertexCount)continue;const ai=a*3;const bi=b*3;appendRawEdge(Number(source[ai])||0,Number(source[ai+1])||0,Number(source[ai+2])||0,Number(source[bi])||0,Number(source[bi+1])||0,Number(source[bi+2])||0,angle);}}if(!longest||rawEdges.length<3)return null;const invAxisLength=1/longest.length;const axis={x:longest.dx*invAxisLength,y:longest.dy*invAxisLength,z:longest.dz*invAxisLength};const reference=Math.abs(axis.x)<=Math.abs(axis.y)&&Math.abs(axis.x)<=Math.abs(axis.z)?{x:1,y:0,z:0}:(Math.abs(axis.y)<=Math.abs(axis.z)?{x:0,y:1,z:0}:{x:0,y:0,z:1});let ux=axis.y*reference.z-axis.z*reference.y;let uy=axis.z*reference.x-axis.x*reference.z;let uz=axis.x*reference.y-axis.y*reference.x;const uLength=Math.sqrt(ux*ux+uy*uy+uz*uz)||1;ux/=uLength;uy/=uLength;uz/=uLength;const vx=axis.y*uz-axis.z*uy;const vy=axis.z*ux-axis.x*uz;const vz=axis.x*uy-axis.y*ux;let centerX=0;let centerY=0;let centerZ=0;let minProjection=Infinity;let maxProjection=-Infinity;for(let i=0;i<vertexCount;i++){const x=Number(source[i*3])||0;const y=Number(source[i*3+1])||0;const z=Number(source[i*3+2])||0;centerX+=x;centerY+=y;centerZ+=z;const projection=x*axis.x+y*axis.y+z*axis.z;minProjection=Math.min(minProjection,projection);maxProjection=Math.max(maxProjection,projection);}centerX/=vertexCount;centerY/=vertexCount;centerZ/=vertexCount;const axisSpan=maxProjection-minProjection;if(!(axisSpan>1e-9))return null;let maxRadial=0;for(let i=0;i<vertexCount;i++){const rx=(Number(source[i*3])||0)-centerX;const ry=(Number(source[i*3+1])||0)-centerY;const rz=(Number(source[i*3+2])||0)-centerZ;const radialU=rx*ux+ry*uy+rz*uz;const radialV=rx*vx+ry*vy+rz*vz;maxRadial=Math.max(maxRadial,Math.sqrt(radialU*radialU+radialV*radialV));}if(!(maxRadial>1e-9)||axisSpan<maxRadial*2.2)return null;const axialGroups=new Map();const transverse=[];const groupBins=48;for(const edge of rawEdges){const axialDelta=edge.dx*axis.x+edge.dy*axis.y+edge.dz*axis.z;const axialRatio=Math.abs(axialDelta)/edge.length;const midX=(edge.ax+edge.bx)*0.5-centerX;const midY=(edge.ay+edge.by)*0.5-centerY;const midZ=(edge.az+edge.bz)*0.5-centerZ;const midProjection=((edge.ax+edge.bx)*0.5*axis.x)+((edge.ay+edge.by)*0.5*axis.y)+((edge.az+edge.bz)*0.5*axis.z);if(axialRatio>=0.82){const radialU=midX*ux+midY*uy+midZ*uz;const radialV=midX*vx+midY*vy+midZ*vz;let angle=Math.atan2(radialV,radialU);if(angle<0)angle+=Math.PI*2;const bin=Math.round((angle/(Math.PI*2))*groupBins)%groupBins;let group=axialGroups.get(bin);if(!group){group={bin,angle:(bin/groupBins)*Math.PI*2,edges:[],coverage:0};axialGroups.set(bin,group);}group.edges.push(edge);group.coverage+=Math.abs(axialDelta);continue;}const normalizedProjection=(midProjection-minProjection)/axisSpan;const nearEnd=normalizedProjection<=0.1||normalizedProjection>=0.9;const isBoundary=edge.angle===boundaryAngle;const angleDegrees=isBoundary?180:edge.angle/100;const keepTransverse=maxLongitudinalChains===0?nearEnd:((nearEnd&&(isBoundary||angleDegrees>=6))||angleDegrees>=45);if(includeTransverseEdges&&keepTransverse){transverse.push({edge,score:(nearEnd?10:0)+angleDegrees/180+edge.length/axisSpan});}}const groups=Array.from(axialGroups.values());if(forceCircular&&groups.length<6)return null;groups.sort((a,b)=>b.coverage-a.coverage);const selectedGroups=maxLongitudinalChains>0&&groups.length?[groups[0]]:[];const angularDistance=(a,b)=>{const delta=Math.abs(a-b)%(Math.PI*2);return Math.min(delta,Math.PI*2-delta);};while(selectedGroups.length<maxLongitudinalChains&&selectedGroups.length<groups.length){let best=null;let bestScore=-Infinity;const bestCoverage=groups[0].coverage||1;for(const group of groups){if(selectedGroups.includes(group))continue;const separation=Math.min(...selectedGroups.map(selected=>angularDistance(group.angle,selected.angle)));const score=separation+(group.coverage/bestCoverage)*0.1;if(score>bestScore){best=group;bestScore=score;}}if(!best)break;selectedGroups.push(best);}const outputEdges=[];let longitudinalSegments=0;const maxSegmentsPerChain=this.performanceRenderMode?12:24;for(const group of selectedGroups){group.edges.sort((a,b)=>b.length-a.length);const selected=group.edges.slice(0,maxSegmentsPerChain);outputEdges.push(...selected);longitudinalSegments+=selected.length;}transverse.sort((a,b)=>b.score-a.score);const maxTransverseSegments=maxLongitudinalChains===0?(this.performanceRenderMode?128:192):(this.performanceRenderMode?48:72);for(const item of transverse.slice(0,maxTransverseSegments))outputEdges.push(item.edge);if(!outputEdges.length)return null;const positions=new Float32Array(outputEdges.length*6);let cursor=0;for(const edge of outputEdges){positions[cursor++]=edge.ax;positions[cursor++]=edge.ay;positions[cursor++]=edge.az;positions[cursor++]=edge.bx;positions[cursor++]=edge.by;positions[cursor++]=edge.bz;}const geometry=new THREE.BufferGeometry();geometry.setAttribute('position',new THREE.BufferAttribute(positions,3));geometry.userData=Object.assign({},geometry.userData||{},{generatedEdgeCached:!!cacheKey,generatedEdgeCacheKey:cacheKey||'',tubularRepresentativeEdges:this.isLightweightTubularCurveElement(element),circularParallelReferenceEdges:forceCircular||maxLongitudinalChains===2,tubularLongitudinalEdgeChains:selectedGroups.length,tubularLongitudinalEdgeSegments:longitudinalSegments,tubularTransverseEdgeSegments:outputEdges.length-longitudinalSegments});if(geometry.computeBoundingBox)geometry.computeBoundingBox();if(geometry.computeBoundingSphere)geometry.computeBoundingSphere();if(cacheKey)this.generatedEdgeGeometryCache.set(cacheKey,geometry);this.generatedEdgeGeometryCacheMisses=(this.generatedEdgeGeometryCacheMisses||0)+1;return geometry;}getCompactFallbackEdgeGeometryCacheKey(mesh,settings,kind){if(!mesh||!mesh.geometry||!settings)return'';const geometryData=mesh.userData&&mesh.userData.geometryData;const sharedId=(mesh.userData&&mesh.userData.sharedGeometryId)||(geometryData&&(geometryData.SharedGeometryId||geometryData.GeometryId))||'';const position=mesh.geometry.getAttribute&&mesh.geometry.getAttribute('position');const index=mesh.geometry.index;const geometryKey=sharedId||mesh.geometry.uuid||'';if(!geometryKey||!position||!position.count)return'';return['edgeCompact',kind||'topology',geometryKey,position.count,index&&index.count?index.count:0,settings.minAngle,settings.minLengthRatio,settings.maxSegments].join('|');}createCompactBoundingBoxEdgeGeometry(mesh,settings){if(!mesh||!mesh.geometry)return null;const geometry=mesh.geometry;if(!geometry.boundingBox&&geometry.computeBoundingBox)geometry.computeBoundingBox();const box=geometry.boundingBox;if(!box||!box.min||!box.max)return null;const cacheKey=this.getCompactFallbackEdgeGeometryCacheKey(mesh,settings,'bounds');if(!this.generatedEdgeGeometryCache)this.generatedEdgeGeometryCache=new Map();const cached=cacheKey?this.generatedEdgeGeometryCache.get(cacheKey):null;if(cached){this.generatedEdgeGeometryCacheHits=(this.generatedEdgeGeometryCacheHits||0)+1;return cached;}const min=box.min;const max=box.max;const corners=[[min.x,min.y,min.z],[max.x,min.y,min.z],[max.x,max.y,min.z],[min.x,max.y,min.z],[min.x,min.y,max.z],[max.x,min.y,max.z],[max.x,max.y,max.z],[min.x,max.y,max.z]];const pairs=[[0,1],[1,2],[2,3],[3,0],[4,5],[5,6],[6,7],[7,4],[0,4],[1,5],[2,6],[3,7]];const diagonal=Math.max(1e-9,Math.sqrt(Math.pow(max.x-min.x,2)+Math.pow(max.y-min.y,2)+Math.pow(max.z-min.z,2)));const values=[];const emitted=new Set();for(const pair of pairs){const a=corners[pair[0]];const b=corners[pair[1]];const dx=b[0]-a[0];const dy=b[1]-a[1];const dz=b[2]-a[2];if(Math.sqrt(dx*dx+dy*dy+dz*dz)<diagonal*1e-7)continue;const forward=a.join(',')+'|'+b.join(',');const reverse=b.join(',')+'|'+a.join(',');if(emitted.has(forward)||emitted.has(reverse))continue;emitted.add(forward);values.push(a[0],a[1],a[2],b[0],b[1],b[2]);}if(!values.length)return null;const edgeGeometry=new THREE.BufferGeometry();edgeGeometry.setAttribute('position',new THREE.BufferAttribute(new Float32Array(values),3));edgeGeometry.userData=Object.assign({},edgeGeometry.userData||{},{generatedEdgeCached:!!cacheKey,generatedEdgeCacheKey:cacheKey||'',compactFallbackEdges:true,compactFallbackKind:'bounds',compactFallbackEdgeSegments:Math.floor(values.length/6)});if(edgeGeometry.computeBoundingBox)edgeGeometry.computeBoundingBox();if(edgeGeometry.computeBoundingSphere)edgeGeometry.computeBoundingSphere();if(cacheKey)this.generatedEdgeGeometryCache.set(cacheKey,edgeGeometry);this.generatedEdgeGeometryCacheMisses=(this.generatedEdgeGeometryCacheMisses||0)+1;return edgeGeometry;}createCompactFallbackEdgeGeometry(mesh,settings){if(!mesh||!settings)return null;const geometryData=mesh.userData&&mesh.userData.geometryData;let geometry=null;geometry=this.createTubularRepresentativeEdgeGeometry?this.createTubularRepresentativeEdgeGeometry(mesh):null;if(geometry){geometry.userData=Object.assign({},geometry.userData||{},{compactFallbackEdges:true,compactFallbackKind:'tubular-representative'});return geometry;}const semanticSettings=this.getSemanticCurvedEdgeSettings?this.getSemanticCurvedEdgeSettings(mesh):null;if(semanticSettings){geometry=this.createSemanticCurvedEdgeGeometry?this.createSemanticCurvedEdgeGeometry(mesh,semanticSettings):null;if(geometry){geometry.userData=Object.assign({},geometry.userData||{},{compactFallbackEdges:true,compactFallbackKind:'semantic-curved'});}return geometry;}const lowComplexitySettings=this.getLowComplexityFullEdgeSettings?this.getLowComplexityFullEdgeSettings(mesh):null;if(lowComplexitySettings){geometry=this.createLowComplexityFullEdgeGeometry(mesh,lowComplexitySettings);if(geometry){geometry.userData=Object.assign({},geometry.userData||{},{compactFallbackEdges:true,compactFallbackKind:'low-complexity-full'});return geometry;}}const topologyEdgeCount=geometryData&&geometryData.EdgeLineIndices?Math.floor(geometryData.EdgeLineIndices.length/2):0;const topologyScanLimit=this.performanceRenderMode?240000:750000;if(this.hasPrecomputedEdgeTopology(geometryData)&&topologyEdgeCount<=topologyScanLimit){geometry=this.createSimplifiedExteriorEdgeGeometry(mesh,settings);if(geometry){geometry.userData=Object.assign({},geometry.userData||{},{compactFallbackEdges:true,compactFallbackKind:'topology'});}}return geometry||this.createCompactBoundingBoxEdgeGeometry(mesh,settings);}createCompactFallbackEdgeDescriptor(mesh,reason,options){const opts=options&&typeof options==='object'?options:{};if(!mesh||!mesh.isMesh||!mesh.userData)return null;if(opts.ignoreExistingDetailedEdge!==true&&this.hasDetailedGeneratedEdge(mesh))return null;const settings=this.getCompactFallbackEdgeSettings(mesh);if(!settings)return null;const geometry=this.createCompactFallbackEdgeGeometry(mesh,settings);if(!geometry)return null;return{geometry,material:this.getBaseEdgeMaterial(mesh),renderOrder:2,reason:reason||'detailed-edge-unavailable',segments:this.getGeneratedEdgeSegmentCount(geometry)};}applyCompactFallbackEdgeDescriptor(mesh,descriptor){if(!mesh||!mesh.userData||!descriptor||!descriptor.geometry||!descriptor.material)return false;const hadFallback=this.hasCompactFallbackEdge(mesh);const previousSegments=Number(mesh.userData.compactFallbackEdgeSegments)||0;mesh.userData.compactFallbackEdgeGeometry=descriptor.geometry;mesh.userData.compactFallbackEdgeMaterial=descriptor.material;mesh.userData.compactFallbackEdgeRenderOrder=descriptor.renderOrder||2;mesh.userData.compactFallbackEdgeReason=descriptor.reason||'';mesh.userData.compactFallbackEdgeSegments=Number(descriptor.segments)||this.getGeneratedEdgeSegmentCount(descriptor.geometry);if(!hadFallback)this.compactFallbackEdgeCount=(this.compactFallbackEdgeCount||0)+1;this.compactFallbackEdgeSegmentCount=Math.max(0,(this.compactFallbackEdgeSegmentCount||0)-previousSegments+mesh.userData.compactFallbackEdgeSegments);return true;}ensureCompactFallbackEdgeCoverage(mesh,reason){if(!mesh||this.hasDetailedGeneratedEdge(mesh))return false;if(this.hasCompactFallbackEdge(mesh))return true;const descriptor=this.createCompactFallbackEdgeDescriptor(mesh,reason);return this.applyCompactFallbackEdgeDescriptor(mesh,descriptor);}clearCompactFallbackEdgeForMesh(mesh){if(!mesh||!mesh.userData||!this.hasCompactFallbackEdge(mesh))return false;if(this.removeTransientCompactFallbackEdge)this.removeTransientCompactFallbackEdge(mesh,true);const segments=Number(mesh.userData.compactFallbackEdgeSegments)||0;mesh.userData.compactFallbackEdgeGeometry=null;mesh.userData.compactFallbackEdgeMaterial=null;mesh.userData.compactFallbackEdgeRenderOrder=0;mesh.userData.compactFallbackEdgeReason='';mesh.userData.compactFallbackEdgeSegments=0;this.compactFallbackEdgeCount=Math.max(0,(this.compactFallbackEdgeCount||0)-1);this.compactFallbackEdgeSegmentCount=Math.max(0,(this.compactFallbackEdgeSegmentCount||0)-segments);return true;}getTransientCompactFallbackEdge(mesh){if(!mesh||!mesh.children)return null;return mesh.children.find(child=>child&&child.isLineSegments&&child.userData&&child.userData.isTransientCompactFallbackEdge)||null;}removeTransientCompactFallbackEdge(mesh,clearRequest){if(!mesh||!mesh.userData)return false;const edge=this.getTransientCompactFallbackEdge?this.getTransientCompactFallbackEdge(mesh):null;if(edge&&edge.parent)edge.parent.remove(edge);if(edge)this.transientCompactFallbackEdgeCount=Math.max(0,(this.transientCompactFallbackEdgeCount||0)-1);if(clearRequest!==false){mesh.userData.transientCompactFallbackRequested=false;mesh.userData.transientCompactFallbackReason='';}return!!edge;}requestTransientCompactFallbackEdge(mesh,reason){if(!mesh||!mesh.isMesh||!mesh.userData)return false;if(this.hasDetailedGeneratedEdge&&this.hasDetailedGeneratedEdge(mesh))return false;if(!(this.hasCompactFallbackEdge&&this.hasCompactFallbackEdge(mesh)))return false;mesh.userData.transientCompactFallbackRequested=true;mesh.userData.transientCompactFallbackReason=reason||'chunk-detached';return true;}syncTransientCompactFallbackEdge(mesh,appearance){if(!mesh||!mesh.isMesh||!mesh.userData)return false;const data=mesh.userData;const isSuppressed=!!(data.normalChunkSuppressed||data.visualStateChunkSuppressed||data.massTransparencyChunkSuppressed);const hasDetailed=!!(this.hasDetailedGeneratedEdge&&this.hasDetailedGeneratedEdge(mesh));const hasCompact=!!(this.hasCompactFallbackEdge&&this.hasCompactFallbackEdge(mesh));if(isSuppressed||hasDetailed||!hasCompact||!data.transientCompactFallbackRequested){return this.removeTransientCompactFallbackEdge?this.removeTransientCompactFallbackEdge(mesh,isSuppressed||hasDetailed||!hasCompact):false;}const resolved=appearance||(this.resolveVisualAppearance?this.resolveVisualAppearance(mesh):null);const shouldShow=!!(resolved&&resolved.visible!==false&&data.baseVisible!==false&&mesh.visible!==false);let edge=this.getTransientCompactFallbackEdge?this.getTransientCompactFallbackEdge(mesh):null;if(!shouldShow){if(edge&&edge.visible!==false)edge.visible=false;return!!edge;}if(!edge){const material=resolved.edgeMaterial||data.compactFallbackEdgeMaterial||this.getBaseEdgeMaterial(mesh);edge=new THREE.LineSegments(data.compactFallbackEdgeGeometry,material);edge.userData=Object.assign({},edge.userData||{},{isTransientCompactFallbackEdge:true,sharedCompactFallbackGeometry:true,elementKey:this.getMeshKey?this.getMeshKey(mesh):data.elementKey,reason:data.transientCompactFallbackReason||'chunk-detached'});edge.frustumCulled=true;mesh.add(edge);this.transientCompactFallbackEdgeCount=(this.transientCompactFallbackEdgeCount||0)+1;}edge.material=resolved.edgeMaterial||data.compactFallbackEdgeMaterial||edge.material;edge.renderOrder=Number.isFinite(Number(resolved.edgeRenderOrder))?Number(resolved.edgeRenderOrder):(data.compactFallbackEdgeRenderOrder||2);edge.visible=true;return true;}activateTransientCompactFallbackEdgesForKeys(keys,reason){let activated=0;for(const key of new Set(keys||[])){const meshes=this.getMeshesForElementKey?this.getMeshesForElementKey(key):[];for(const mesh of meshes){if(!(this.requestTransientCompactFallbackEdge&&this.requestTransientCompactFallbackEdge(mesh,reason)))continue;if(this.updateVisualAppearanceForMesh)this.updateVisualAppearanceForMesh(mesh);activated++;}}return activated;}getNormalChunkEdgeSourcesForMesh(mesh){const sources=[];if(!mesh||!mesh.isMesh)return sources;for(const child of mesh.children||[]){if(!child||!child.isLineSegments||!child.geometry)continue;if(!(child.userData&&child.userData.isGeneratedEdge))continue;sources.push({geometry:child.geometry,material:child.material||this.edgeMaterial,renderOrder:child.renderOrder||1,transformObject:child,compactFallback:false});}if(!sources.length&&this.hasCompactFallbackEdge(mesh)){sources.push({geometry:mesh.userData.compactFallbackEdgeGeometry,material:mesh.userData.compactFallbackEdgeMaterial||this.getBaseEdgeMaterial(mesh),renderOrder:mesh.userData.compactFallbackEdgeRenderOrder||2,transformObject:mesh,compactFallback:true});}return sources;}getSimplifiedExteriorEdgeGeometryCacheKey(mesh,settings){if(!mesh||!mesh.geometry||!settings)return'';const geometryData=mesh.userData&&mesh.userData.geometryData?mesh.userData.geometryData:null;const sharedId=(mesh.userData&&mesh.userData.sharedGeometryId)||(geometryData&&(geometryData.SharedGeometryId||geometryData.GeometryId))||'';const position=mesh.geometry.getAttribute&&mesh.geometry.getAttribute('position');const index=mesh.geometry.index;const geometryKey=sharedId||mesh.geometry.uuid||'';if(!geometryKey||!position||!position.count)return'';return['edgeOuter',this.getVisualDetailLevel?this.getVisualDetailLevel():'medium',geometryKey,position.count,index&&index.count?index.count:0,settings.minLengthRatio,settings.minAngle,settings.maxSegments,settings.preserveComponentDetails===true?1:0,settings.semanticCurvedEdges===true?1:0].join('|');}createSimplifiedExteriorEdgeGeometry(mesh,settings){if(!mesh||!mesh.geometry||!settings)return null;const geometryData=mesh.userData&&mesh.userData.geometryData;const edgeIndices=geometryData&&geometryData.EdgeLineIndices;if(!edgeIndices||typeof edgeIndices.length!=='number'||edgeIndices.length<2)return null;const positionAttr=mesh.geometry.getAttribute&&mesh.geometry.getAttribute('position');const source=positionAttr&&positionAttr.array;if(!source||typeof source.length!=='number'||source.length<3)return null;if(!this.generatedEdgeGeometryCache)this.generatedEdgeGeometryCache=new Map();const cacheKey=this.getSimplifiedExteriorEdgeGeometryCacheKey(mesh,settings);const cached=cacheKey?this.generatedEdgeGeometryCache.get(cacheKey):null;if(cached){this.generatedEdgeGeometryCacheHits=(this.generatedEdgeGeometryCacheHits||0)+1;return cached;}const geometry=mesh.geometry;if(!geometry.boundingBox&&geometry.computeBoundingBox)geometry.computeBoundingBox();const box=geometry.boundingBox;if(!box)return null;const dx=(box.max.x||0)-(box.min.x||0);const dy=(box.max.y||0)-(box.min.y||0);const dz=(box.max.z||0)-(box.min.z||0);const diagonal=Math.sqrt(dx*dx+dy*dy+dz*dz);if(!(diagonal>1e-9))return null;const semanticExtents=[Math.abs(dx),Math.abs(dy),Math.abs(dz)];let semanticAxisIndex=0;if(semanticExtents[1]>semanticExtents[semanticAxisIndex])semanticAxisIndex=1;if(semanticExtents[2]>semanticExtents[semanticAxisIndex])semanticAxisIndex=2;const semanticAxisSpan=semanticExtents[semanticAxisIndex];const semanticCrossSpan=Math.max(...semanticExtents.filter((_,index)=>index!==semanticAxisIndex));const semanticElongated=settings.semanticCurvedEdges===true&&semanticAxisSpan>Math.max(1e-9,semanticCrossSpan)*1.6;const edgeAngles=geometryData.EdgeAngles;const edgeCount=Math.floor(edgeIndices.length/2);const hasAngles=edgeAngles&&typeof edgeAngles.length==='number'&&edgeAngles.length>=edgeCount;const boundaryAngle=65535;const thresholdHundredths=Math.max(0,Math.round((Number(settings.minAngle)||0)*100));const vertexCount=Math.floor(source.length/3);const maxSegments=Math.max(24,Number(settings.maxSegments)||720);const componentScanLimit=this.performanceRenderMode?8000:24000;const preserveComponentDetails=settings.preserveComponentDetails===true&&edgeCount<=componentScanLimit;const geometryPointTolerance=Math.max(1e-9,diagonal*1e-6);const geometryPointKey=(offset)=>[Math.round((Number(source[offset])||0)/geometryPointTolerance),Math.round((Number(source[offset+1])||0)/geometryPointTolerance),Math.round((Number(source[offset+2])||0)/geometryPointTolerance)].join(',');const coordinatePointKey=(x,y,z)=>[Math.round((Number(x)||0)/geometryPointTolerance),Math.round((Number(y)||0)/geometryPointTolerance),Math.round((Number(z)||0)/geometryPointTolerance)].join(',');let interactionEdgeKeys=null;const interactionValidationLimit=this.performanceRenderMode?8000:20000;if(settings.semanticCurvedEdges===true&&edgeCount<=interactionValidationLimit&&this.getGeneratedInteractionEdgeGeometry){const interactionGeometry=this.getGeneratedInteractionEdgeGeometry(mesh);const interactionPosition=interactionGeometry&&interactionGeometry.getAttribute?interactionGeometry.getAttribute('position'):null;const interactionSource=interactionPosition&&interactionPosition.array;if(interactionSource&&interactionSource.length>=6){interactionEdgeKeys=new Set();for(let i=0;i+5<interactionSource.length;i+=6){const keyA=coordinatePointKey(interactionSource[i],interactionSource[i+1],interactionSource[i+2]);const keyB=coordinatePointKey(interactionSource[i+3],interactionSource[i+4],interactionSource[i+5]);interactionEdgeKeys.add(keyA<keyB?keyA+'|'+keyB:keyB+'|'+keyA);}}}const selectWithComponentCoverage=(allCandidates)=>{if(!preserveComponentDetails||!allCandidates.length){allCandidates.sort((a,b)=>b.score-a.score);if(allCandidates.length>maxSegments)allCandidates.length=maxSegments;allCandidates.semanticComponentCount=0;return allCandidates;}const parent=new Int32Array(allCandidates.length);const rank=new Uint8Array(allCandidates.length);for(let i=0;i<parent.length;i++)parent[i]=i;const find=(value)=>{let root=value;while(parent[root]!==root)root=parent[root];while(parent[value]!==value){const next=parent[value];parent[value]=root;value=next;}return root;};const unite=(a,b)=>{let rootA=find(a);let rootB=find(b);if(rootA===rootB)return;if(rank[rootA]<rank[rootB]){const swap=rootA;rootA=rootB;rootB=swap;}parent[rootB]=rootA;if(rank[rootA]===rank[rootB])rank[rootA]++;};const endpointOwner=new Map();for(let i=0;i<allCandidates.length;i++){const candidate=allCandidates[i];const keys=[geometryPointKey(candidate.a*3),geometryPointKey(candidate.b*3)];for(const key of keys){const owner=endpointOwner.get(key);if(owner==null)endpointOwner.set(key,i);else unite(i,owner);}}const groups=new Map();for(let i=0;i<allCandidates.length;i++){const root=find(i);let group=groups.get(root);if(!group){group={candidates:[],bestScore:-Infinity};groups.set(root,group);}const candidate=allCandidates[i];group.candidates.push(candidate);group.bestScore=Math.max(group.bestScore,candidate.score);}const orderedGroups=Array.from(groups.values()).sort((a,b)=>b.bestScore-a.bestScore);const quota=Math.max(1,Math.min(6,Math.floor(maxSegments/Math.max(1,orderedGroups.length))));const selected=[];const selectedSet=new Set();for(const group of orderedGroups){if(selected.length>=maxSegments)break;group.candidates.sort((a,b)=>{if(a.preferredLength!==b.preferredLength)return a.preferredLength?-1:1;return b.score-a.score;});for(const candidate of group.candidates.slice(0,quota)){if(selected.length>=maxSegments)break;selected.push(candidate);selectedSet.add(candidate.edgeIndex);}}allCandidates.sort((a,b)=>{if(a.preferredLength!==b.preferredLength)return a.preferredLength?-1:1;return b.score-a.score;});for(const candidate of allCandidates){if(selected.length>=maxSegments)break;if(selectedSet.has(candidate.edgeIndex))continue;selected.push(candidate);selectedSet.add(candidate.edgeIndex);}selected.semanticComponentCount=orderedGroups.length;return selected;};const collectCandidates=(lengthRatio)=>{const minLength=diagonal*Math.max(0.0001,Number(lengthRatio)||0.005);const componentDetailMinLength=Math.max(diagonal*1e-6,minLength*0.06);const candidates=[];const shouldDedupeGeometryEdges=preserveComponentDetails||!!interactionEdgeKeys||edgeCount<=30000;const emittedGeometryEdges=shouldDedupeGeometryEdges?new Set():null;const trimAt=Math.max(maxSegments*4,256);const keepAfterTrim=Math.max(maxSegments*2,128);const trimCandidates=()=>{if(preserveComponentDetails)return;if(candidates.length<=trimAt)return;candidates.sort((a,b)=>b.score-a.score);candidates.length=Math.min(candidates.length,keepAfterTrim);};for(let i=0;i<edgeCount;i++){const a=edgeIndices[i*2];const b=edgeIndices[i*2+1];if(a==null||b==null||a<0||b<0||a>=vertexCount||b>=vertexCount)continue;const angle=hasAngles?edgeAngles[i]:boundaryAngle;const boundary=angle===boundaryAngle;if(!boundary&&angle<thresholdHundredths)continue;const ai=a*3;const bi=b*3;const ex=(source[bi]||0)-(source[ai]||0);const ey=(source[bi+1]||0)-(source[ai+1]||0);const ez=(source[bi+2]||0)-(source[ai+2]||0);const length=Math.sqrt(ex*ex+ey*ey+ez*ez);if(!(length>1e-9))continue;const axisDelta=semanticAxisIndex===0?ex:(semanticAxisIndex===1?ey:ez);if(semanticElongated&&boundary&&Math.abs(axisDelta)/length>=0.92&&length>=semanticAxisSpan*0.45)continue;const preferredLength=boundary?length>=minLength*0.45:length>=minLength;if(!preferredLength&&(!preserveComponentDetails||length<componentDetailMinLength))continue;if(emittedGeometryEdges||interactionEdgeKeys){const keyA=geometryPointKey(ai);const keyB=geometryPointKey(bi);const geometryEdgeKey=keyA<keyB?keyA+'|'+keyB:keyB+'|'+keyA;if(emittedGeometryEdges&&emittedGeometryEdges.has(geometryEdgeKey))continue;if(interactionEdgeKeys&&!interactionEdgeKeys.has(geometryEdgeKey))continue;if(emittedGeometryEdges)emittedGeometryEdges.add(geometryEdgeKey);}const normalizedLength=length/diagonal;const angleDegrees=boundary?180:Math.min(180,angle/100);const score=normalizedLength*120+(angleDegrees/180)*1.5+(boundary?3.0:0);candidates.push({a,b,score,edgeIndex:i,preferredLength});trimCandidates();}return selectWithComponentCoverage(candidates);};let candidates=collectCandidates(settings.minLengthRatio);if(candidates.length<(settings.minimumReadableSegments||8)){candidates=collectCandidates(settings.relaxedMinLengthRatio||settings.minLengthRatio*0.5);}if(!candidates.length)return null;const positions=new Float32Array(candidates.length*6);let cursor=0;for(const candidate of candidates){const ai=candidate.a*3;const bi=candidate.b*3;positions[cursor++]=source[ai]||0;positions[cursor++]=source[ai+1]||0;positions[cursor++]=source[ai+2]||0;positions[cursor++]=source[bi]||0;positions[cursor++]=source[bi+1]||0;positions[cursor++]=source[bi+2]||0;}const edgeGeometry=new THREE.BufferGeometry();edgeGeometry.setAttribute('position',new THREE.BufferAttribute(positions,3));edgeGeometry.userData=Object.assign({},edgeGeometry.userData||{},{generatedEdgeCached:!!cacheKey,generatedEdgeCacheKey:cacheKey||'',simplifiedExteriorEdges:true,simplifiedExteriorEdgeSegments:candidates.length,semanticCurvedEdges:settings.semanticCurvedEdges===true,semanticComponentCount:Number(candidates.semanticComponentCount)||0});if(edgeGeometry.computeBoundingBox)edgeGeometry.computeBoundingBox();if(edgeGeometry.computeBoundingSphere)edgeGeometry.computeBoundingSphere();if(cacheKey)this.generatedEdgeGeometryCache.set(cacheKey,edgeGeometry);this.generatedEdgeGeometryCacheMisses=(this.generatedEdgeGeometryCacheMisses||0)+1;this.recordActiveViewerLoadPhase('edges.simplifiedExteriorGeometry',0,{geometryId:geometryData.GeometryId||'',candidateEdges:edgeCount,emittedEdges:candidates.length,triangleCount:settings.triangleCount||0});return edgeGeometry;}getMediumAdaptiveEdgeSettings(mesh,baseEdgeGeometry,baseAngle){if(!mesh||this.getVisualDetailLevel()!=='medium')return null;const element=mesh.userData&&mesh.userData.element;const geometryData=mesh.userData&&mesh.userData.geometryData;if(!element||!geometryData)return null;if(this.isLightweightTubularCurveElement(element))return null;const triangleCount=this.getTriangleCountForEdgeBudget(geometryData);const baseSegments=this.getGeneratedEdgeSegmentCount(baseEdgeGeometry);const minimumReadableSegments=triangleCount>0?Math.min(48,Math.max(6,Math.ceil(Math.sqrt(triangleCount)*0.7))):6;if(baseSegments>=minimumReadableSegments)return null;const isRebar=this.isStructuralRebarCategory(element);const isOptimizedDenseMesh=this.isOptimizedDenseMeshCategory(element);const isSpecialty=this.isSpecialtyLowDetailCategory(element);const isReadableDetail=this.isReadableDetailEdgeCategory(element);let triangleLimit;let thresholdAngle;let segmentLimit;if(isRebar){triangleLimit=this.performanceRenderMode?8000:24000;thresholdAngle=this.performanceRenderMode?24:18;segmentLimit=this.performanceRenderMode?360:720;}else if(isOptimizedDenseMesh){triangleLimit=this.performanceRenderMode?30000:90000;thresholdAngle=this.performanceRenderMode?18:12;segmentLimit=this.performanceRenderMode?700:1400;}else if(isSpecialty){triangleLimit=this.performanceRenderMode?24000:80000;thresholdAngle=this.performanceRenderMode?22:16;segmentLimit=this.performanceRenderMode?600:1200;}else if(isReadableDetail){triangleLimit=this.performanceRenderMode?40000:120000;thresholdAngle=this.performanceRenderMode?18:12;segmentLimit=this.performanceRenderMode?800:1600;}else{triangleLimit=this.performanceRenderMode?12000:40000;thresholdAngle=this.performanceRenderMode?16:10;segmentLimit=this.performanceRenderMode?420:900;}if(triangleCount>triangleLimit)return null;const safeBaseAngle=Math.max(0,Number(baseAngle)||0);const safeThresholdAngle=Math.max(2,Math.min(safeBaseAngle-1,thresholdAngle));if(!(safeThresholdAngle<safeBaseAngle))return null;return{baseSegments,minimumReadableSegments,thresholdAngle:safeThresholdAngle,segmentLimit};}getElementSearchText(element){if(!element)return'';return this.normalizeTextForSearch([element.CategoryName,element.SubcategoryName,element.FamilyName,element.TypeName,element.Name].filter(Boolean).join(' '));}isStructuralRebarCategory(element){const text=this.getElementSearchText(element);if(!text)return false;const keywords=['armadura estructural','armaduras estructurales','structural rebar','rebar','reinforcement','reinforcing bar','barra de armadura','barras de armadura','acero de refuerzo','acero corrugado'];return keywords.some(keyword=>text.includes(keyword));}isLightweightTubularCurveElement(element){if(!element)return false;const builtInCategory=String(element.BuiltInCategory||'').trim().toUpperCase();return builtInCategory==='OST_PIPECURVES'||builtInCategory==='OST_CONDUIT';}isOptimizedDenseMeshCategory(element){if(!element)return false;const builtInCategory=String(element.BuiltInCategory||'').trim().toUpperCase();if(!builtInCategory)return false;const categories=['OST_PIPEFITTING','OST_PIPEACCESSORY','OST_DUCTFITTING','OST_DUCTACCESSORY','OST_CONDUITFITTING','OST_CABLETRAYFITTING','OST_SPRINKLERS','OST_DUCTTERMINAL','OST_PLUMBINGFIXTURES','OST_ELECTRICALFIXTURES','OST_LIGHTINGFIXTURES','OST_ELECTRICALEQUIPMENT','OST_MECHANICALEQUIPMENT','OST_COMMUNICATIONDEVICES','OST_DATADEVICES','OST_FIREALARMDEVICES','OST_SECURITYDEVICES','OST_NURSECALLDEVICES','OST_TELEPHONEDEVICES','OST_FURNITURE','OST_FURNITURESYSTEMS','OST_CASEWORK','OST_SPECIALITYEQUIPMENT','OST_GENERICMODEL'];return categories.includes(builtInCategory);}isSpecialtyLowDetailCategory(element){const text=this.getElementSearchText(element);if(!text)return false;const keywords=['tuberia','tuberias','tubo','tubos','pipe','pipes','union de tubo','uniones de tubo','union de tuberia','uniones de tuberia','pipe fitting','pipe fittings','pipe accessory','pipe accessories','accesorio de tuberia','accesorios de tuberia','aparato sanitario','aparatos sanitarios','plumbing fixture','plumbing fixtures','sanitary fixture','sanitary fixtures','aparato electrico','aparatos electricos','electrical fixture','electrical fixtures','electrical equipment','conducto','conductos','duct','ducts','duct fitting','duct fittings','air terminal','air terminals','bandeja portacables','bandejas portacables','cable tray','cable trays','conduit','conduit fitting','conduit fittings','mechanical equipment','equipo mecanico','equipos mecanicos','sprinkler','sprinklers'];return keywords.some(keyword=>text.includes(keyword));}isLowObjectBudgetCategory(element){return this.isStructuralRebarCategory(element)||this.isSpecialtyLowDetailCategory(element);}isPrimaryReadableEdgeCategory(element){const text=this.getElementSearchText(element);if(!text)return false;const keywords=['muro','muros','wall','walls','suelo','suelos','floor','floors','slab','slabs','cubierta','cubiertas','roof','roofs','techo','techos','ceiling','ceilings','armazon estructural','structural framing','viga','vigas','beam','beams','pilar estructural','pilares estructurales','structural column','structural columns','columna','columnas','column','columns','cimentacion estructural','foundation','foundations','zapata','zapatas','puerta','puertas','door','doors','ventana','ventanas','window','windows','barandilla','barandillas','railing','railings','escalera','escaleras','stair','stairs','montante','montantes','mullion','mullions','panel de muro cortina','curtain panel','curtain panels'];return keywords.some(keyword=>text.includes(keyword));}isReadableDetailEdgeCategory(element){const text=this.getElementSearchText(element);if(!text)return false;const keywords=['mobiliario','furniture','mesa','mesas','table','tables','silla','sillas','chair','chairs','modelo generico','modelos genericos','generic model','generic models','aparato sanitario','aparatos sanitarios','plumbing fixture','plumbing fixtures','sanitary fixture','sanitary fixtures','aparato electrico','aparatos electricos','electrical fixture','electrical fixtures','equipo electrico','equipos electricos','electrical equipment','dispositivo','dispositivos','device','devices','luminaria','luminarias','lighting fixture','lighting fixtures','equipo mecanico','equipos mecanicos','mechanical equipment','especialidad','specialty equipment'];return keywords.some(keyword=>text.includes(keyword));}shouldGenerateEdgesForGeometry(geometryData,element,model,totalGeometries){if(!geometryData||!element)return false;if(this.isNonRenderedDataElement(element))return false;const visualDetailLevel=this.getVisualDetailLevel();if(visualDetailLevel==='high'){if(!this.performanceRenderMode)return true;if(this.isStructuralRebarCategory(element))return false;return true;}if(!this.performanceRenderMode)return true;const triangleCount=this.getTriangleCountForEdgeBudget(geometryData);const primaryReadable=this.isPrimaryReadableEdgeCategory(element);const readableDetail=this.isReadableDetailEdgeCategory(element)||this.isOptimizedDenseMeshCategory(element);const structuralRebar=this.isStructuralRebarCategory(element);const specialtyLowDetail=this.isSpecialtyLowDetailCategory(element);if(structuralRebar){if(this.generatedEdgeObjectCount>=this.generatedEdgeObjectBudget)return false;return triangleCount<=8000;}if(primaryReadable){return triangleCount<=80000;}if(readableDetail){if(triangleCount<=100000)return true;if(triangleCount<=140000&&this.generatedEdgeObjectCount<this.generatedEdgeObjectBudget)return true;return this.hasPrecomputedEdgeTopology?this.hasPrecomputedEdgeTopology(geometryData):false;}if(this.generatedEdgeObjectCount>=this.generatedEdgeObjectBudget)return false;if(specialtyLowDetail)return triangleCount<=24000;if(triangleCount>20000)return false;return true;}getTriangleCountForEdgeBudget(geometryData){if(!geometryData)return 0;if(typeof geometryData.TriangleCount==='number'&&isFinite(geometryData.TriangleCount))return geometryData.TriangleCount;const indices=geometryData.Indices||geometryData.indices||geometryData.IndexBuffer||null;if(indices&&typeof indices.length==='number')return Math.floor(indices.length/3);return 0;}getKeySetFingerprint(keys){if(!keys)return'0:0';const arr=Array.from(keys||[]).filter(Boolean).map(String).sort();let hash=2166136261>>>0;for(const key of arr){for(let i=0;i<key.length;i++){hash^=key.charCodeAt(i);hash=Math.imul(hash,16777619)>>>0;}hash^=124;hash=Math.imul(hash,16777619)>>>0;}return arr.length+':'+hash.toString(16);}hasActiveColorOverrides(){return!!(this.visualColorOverrides&&this.visualColorOverrides.size);}isColorOverrideKey(key){return!!(key&&this.visualColorOverrides&&this.visualColorOverrides.has(key));}getMassTransparencyOwner(state){if(!state)return'';const owner=String(state.owner||state.sourceOwner||'').toLowerCase();if(owner==='viewer'||owner==='data')return owner;if(state.mode==='fade-unselected')return'viewer';if(state.mode==='data-transparency')return'data';return'data';}getMassTransparencyLayer(owner){const resolvedOwner=String(owner||'').toLowerCase()==='viewer'?'viewer':'data';return resolvedOwner==='viewer'?this.viewerMassTransparencyState:this.dataMassTransparencyState;}isViewerTransparencyActive(){return!!(this.viewerMassTransparencyState&&this.viewerMassTransparencyState.active);}getPreferredMassTransparencyState(){if(this.viewerMassTransparencyState&&this.viewerMassTransparencyState.active)return this.viewerMassTransparencyState;if(this.dataMassTransparencyState&&this.dataMassTransparencyState.active)return this.dataMassTransparencyState;return null;}syncMassTransparencyStateFromLayers(previousState){const nextState=this.getPreferredMassTransparencyState?this.getPreferredMassTransparencyState():null;this.visualMassState=nextState&&nextState.active?nextState:null;if(!this.fadedMeshKeys)this.fadedMeshKeys=new Set();const mass=this.visualMassState;const canSyncIncrementally=!!(mass&&mass.active&&mass.allKeys&&previousState&&previousState.active&&previousState.allKeys===mass.allKeys);if(canSyncIncrementally){const changed=new Set();for(const key of previousState.activeKeys||[])changed.add(key);for(const key of mass.activeKeys||[])changed.add(key);for(const key of changed){if(!key||!mass.allKeys.has(key)||(mass.activeKeys&&mass.activeKeys.has(key)))this.fadedMeshKeys.delete(key);else this.fadedMeshKeys.add(key);}}else{this.fadedMeshKeys.clear();if(mass&&mass.active&&mass.allKeys){const activeKeys=mass.activeKeys||new Set();for(const key of mass.allKeys||[])if(key&&!activeKeys.has(key))this.fadedMeshKeys.add(key);}}this.fadeActive=this.fadedMeshKeys.size>0;return this.visualMassState;}setMassTransparencyLayer(owner,state){const resolvedOwner=String(owner||'').toLowerCase()==='viewer'?'viewer':'data';const previousVisibleState=this.visualMassState&&this.visualMassState.active?this.visualMassState:null;const normalized=state&&state.active?state:null;if(normalized){normalized.owner=resolvedOwner;normalized.sourceOwner=resolvedOwner;if(!normalized.mode)normalized.mode=resolvedOwner==='viewer'?'fade-unselected':'data-transparency';normalized.activeKeys=normalized.activeKeys instanceof Set?normalized.activeKeys:new Set(normalized.activeKeys||[]);normalized.allKeys=normalized.allKeys instanceof Set?normalized.allKeys:new Set(normalized.allKeys||[]);}if(resolvedOwner==='viewer')this.viewerMassTransparencyState=normalized;else this.dataMassTransparencyState=normalized;return this.syncMassTransparencyStateFromLayers?this.syncMassTransparencyStateFromLayers(previousVisibleState):normalized;}clearMassTransparencyLayer(owner){const resolvedOwner=String(owner||'').toLowerCase()==='viewer'?'viewer':'data';const previousVisibleState=this.visualMassState&&this.visualMassState.active?this.visualMassState:null;if(resolvedOwner==='viewer')this.viewerMassTransparencyState=null;else this.dataMassTransparencyState=null;return this.syncMassTransparencyStateFromLayers?this.syncMassTransparencyStateFromLayers(previousVisibleState):null;}collectMassTransparencyKeysFromState(state,target){const changed=target||new Set();if(!state)return changed;if(state.activeKeys)for(const key of state.activeKeys)if(key)changed.add(key);if(state.allKeys)for(const key of state.allKeys)if(key)changed.add(key);return changed;}shouldUseGroupedTransparentEdges(){if(this.isolateActive)return false;if(this.massTransparencyUseStableEdges===true)return false;const edgeCoverageCount=(this.generatedEdgeObjectCount||0)+(this.compactFallbackEdgeCount||0);return!!(this.visualMassState&&this.visualMassState.active&&this.getVisualDetailLevel&&this.getVisualDetailLevel()!=='low'&&edgeCoverageCount>0);}isSpecialInteractiveEdgeMaterial(material){return!!material&&[this.selectionEdgeMaterial,this.hoverEdgeMaterial,this.rectanglePreviewEdgeMaterial].includes(material);}shouldGroupTransparentEdgeChild(child,material){const edgeMaterial=material||(child?child.material:null);if(!this.shouldUseGroupedTransparentEdges())return false;if(!child||!child.userData||!child.userData.isGeneratedEdge)return false;const parent=child.parent&&child.parent.isMesh?child.parent:null;const key=parent&&this.getMeshKey?this.getMeshKey(parent):null;const mass=this.visualMassState||null;if(key){if(mass&&mass.activeKeys&&mass.activeKeys.has(key))return false;const selectionUsesSource=this.selectionUsesSourceAppearance?this.selectionUsesSourceAppearance():this.visualSelectionOverlayOnly!==true;if(selectionUsesSource&&this.selectedKeys&&this.selectedKeys.has(key))return false;if(this.visualHoverKey&&this.visualHoverKey===key)return false;if(this.rectanglePreviewKeys&&this.rectanglePreviewKeys.has(key))return false;}if(!edgeMaterial||!edgeMaterial.transparent||Number(edgeMaterial.opacity)>=1)return false;if(this.isSpecialInteractiveEdgeMaterial(edgeMaterial))return false;return true;}getGroupedTransparentEdgeMaterialKey(material){const color=material&&material.color&&material.color.isColor?material.color.getHexString():'0';const opacity=Math.max(0.02,Math.min(1,Number(material&&material.opacity==null?1:material.opacity))).toFixed(3);const renderOrder=Number.isFinite(Number(material&&material.renderOrder))?Number(material.renderOrder):0;return['edge',color,opacity,material&&material.transparent?'T':'O',renderOrder].join(':');}clearGroupedTransparentEdges(dispose){const shouldDispose=dispose===true;let removed=0;const detachBatch=(batch,disposeGeometry)=>{if(!batch)return;if(batch.parent)batch.parent.remove(batch);const sharesNormalGeometry=!!(batch.userData&&batch.userData.sharedNormalEdgeGeometry);if(disposeGeometry&&!sharesNormalGeometry&&batch.geometry&&batch.geometry.dispose)batch.geometry.dispose();removed++;};const activeBatches=Array.isArray(this.groupedTransparentEdgeBatches)?this.groupedTransparentEdgeBatches.slice():[];if(activeBatches.length){for(const batch of activeBatches)detachBatch(batch,shouldDispose);}if(shouldDispose&&this.groupedTransparentEdgeCache&&this.groupedTransparentEdgeCache.size){for(const entry of this.groupedTransparentEdgeCache.values()){for(const batch of(entry&&entry.batches)||[]){if(!activeBatches.includes(batch))detachBatch(batch,true);}}this.groupedTransparentEdgeCache.clear();}this.groupedTransparentEdgeBatches=[];this.groupedTransparentEdgeCacheSignature=null;for(const mesh of this.meshes||[]){const parentVisible=!!(mesh&&mesh.visible!==false&&mesh.userData&&mesh.userData.baseVisible!==false&&!mesh.userData.normalChunkSuppressed&&!mesh.userData.massTransparencyChunkSuppressed&&!mesh.userData.visualStateChunkSuppressed);for(const child of mesh.children||[]){if(!child||!child.userData||!child.userData.groupedTransparentSuppressed)continue;child.userData.groupedTransparentSuppressed=false;child.visible=parentVisible;}}return removed;}async clearGroupedTransparentEdgesBatched(dispose,options){const opts=options&&typeof options==='object'?options:{};const shouldDispose=dispose===true;const report=typeof opts.onProgress==='function'?opts.onProgress:()=>{};const isCancelled=typeof opts.isCancelled==='function'?opts.isCancelled:()=>false;const batchSize=Math.max(500,Math.min(8000,Number(opts.batchSize)||3200));let removed=0;const activeBatches=Array.isArray(this.groupedTransparentEdgeBatches)?this.groupedTransparentEdgeBatches.slice():[];const detachBatch=(batch,disposeGeometry)=>{if(!batch)return;if(batch.parent)batch.parent.remove(batch);const sharesNormalGeometry=!!(batch.userData&&batch.userData.sharedNormalEdgeGeometry);if(disposeGeometry&&!sharesNormalGeometry&&batch.geometry&&batch.geometry.dispose)batch.geometry.dispose();removed++;};for(const batch of activeBatches)detachBatch(batch,shouldDispose);if(shouldDispose&&this.groupedTransparentEdgeCache&&this.groupedTransparentEdgeCache.size){for(const entry of this.groupedTransparentEdgeCache.values()){for(const batch of(entry&&entry.batches)||[]){if(!activeBatches.includes(batch))detachBatch(batch,true);}}this.groupedTransparentEdgeCache.clear();}this.groupedTransparentEdgeBatches=[];this.groupedTransparentEdgeCacheSignature=null;const meshes=this.meshes||[];for(let start=0;start<meshes.length;start+=batchSize){if(isCancelled())return false;const end=Math.min(meshes.length,start+batchSize);for(let index=start;index<end;index++){const mesh=meshes[index];const parentVisible=!!(mesh&&mesh.visible!==false&&mesh.userData&&mesh.userData.baseVisible!==false&&!mesh.userData.normalChunkSuppressed&&!mesh.userData.massTransparencyChunkSuppressed&&!mesh.userData.visualStateChunkSuppressed);for(const child of(mesh&&mesh.children)||[]){if(!child||!child.userData||!child.userData.groupedTransparentSuppressed)continue;child.userData.groupedTransparentSuppressed=false;child.visible=parentVisible;}}report(end,meshes.length,opts.progressMessage||'Restaurando bordes del modelo...');if(end<meshes.length&&opts.yieldBetweenBatches!==false){if(this.waitForResetProgressPaint)await this.waitForResetProgressPaint();else if(this.yieldDuringLoad)await this.yieldDuringLoad();else await new Promise(resolve=>setTimeout(resolve,0));}}return removed;}suppressGroupedTransparentSourceEdges(){const mass=this.visualMassState||null;for(const mesh of this.meshes||[]){if(!mesh||!mesh.isMesh)continue;const key=this.getMeshKey?this.getMeshKey(mesh):null;const includeFaded=!!(key&&this.fadedMeshKeys&&this.fadedMeshKeys.has(key));const isActiveMassKey=!!(key&&mass&&mass.activeKeys&&mass.activeKeys.has(key));const isColored=!!(this.isColorOverrideKey&&this.isColorOverrideKey(key));const baseVisible=!(mesh.userData&&mesh.userData.baseVisible===false);if(!baseVisible||!includeFaded||isActiveMassKey||isColored)continue;for(const child of mesh.children||[]){if(!child||!child.isLineSegments||!child.geometry)continue;if(!(child.userData&&child.userData.isGeneratedEdge))continue;if(this.isSpecialInteractiveEdgeMaterial(child.material))continue;child.userData.groupedTransparentSuppressed=true;child.visible=false;}}}getBaseVisibleKeyFingerprintForKeys(keys){const visibleKeys=new Set();for(const rawKey of keys||[]){const key=String(rawKey||'');if(!key)continue;const meshes=this.getMeshesForElementKey?this.getMeshesForElementKey(key):[];if(meshes.some(mesh=>mesh&&mesh.userData&&mesh.userData.baseVisible!==false))visibleKeys.add(key);}return this.getKeySetFingerprint?this.getKeySetFingerprint(visibleKeys):Array.from(visibleKeys).sort().join('|');}isElementKeyBaseVisibleForRender(key){if(!key)return false;const canResolveSource=typeof this.getMeshesForElementKey==='function';const meshes=canResolveSource?this.getMeshesForElementKey(key):[];if(!canResolveSource||!meshes||!meshes.length)return true;return meshes.some(mesh=>mesh&&mesh.userData&&mesh.userData.baseVisible!==false&&!(this.isNonRenderedDataMesh&&this.isNonRenderedDataMesh(mesh)));}getGroupedTransparentEdgesSignature(){if(!(this.visualMassState&&this.visualMassState.active))return null;const mass=this.visualMassState||{};const allKeysFingerprint=mass.allKeysFingerprint||(this.getKeySetFingerprint?this.getKeySetFingerprint(mass.allKeys||[]):((mass.allKeys&&mass.allKeys.size)||0));return['edges-v8-cached-transparent-base','detail',this.getVisualDetailLevel?this.getVisualDetailLevel():'default','base',allKeysFingerprint,'visibilityRevision',this.baseVisibilityRevision||0,'overridesRevision',this.visualColorOverridesRevision||0,'rev',this.normalChunkRevision||0,'edgeGeneration',this.normalChunkEdgeBuildGeneration||0,'color',mass.fadedColor==null?'default':Number(mass.fadedColor),'opacity',Number(mass.fadedOpacity==null?this.getConfiguredTransparencyOpacity(0.02):mass.fadedOpacity).toFixed(3),'edgeCount',this.generatedEdgeObjectCount||0,'compactEdgeCount',this.compactFallbackEdgeCount||0].join('|');}syncGroupedTransparentSourceEdgesIncremental(){const mass=this.visualMassState||{};const currentActive=new Set(mass.activeKeys||[]);const currentInteractive=new Set();if(!this.shouldUseSelectionOverlayBatches||!this.shouldUseSelectionOverlayBatches()){for(const key of this.selectedKeys||[])currentInteractive.add(key);}if(this.visualHoverKey)currentInteractive.add(this.visualHoverKey);for(const key of this.rectanglePreviewKeys||[])currentInteractive.add(key);const changed=new Set();for(const key of this.groupedTransparentEdgeActiveKeysSnapshot||[])changed.add(key);for(const key of currentActive)changed.add(key);for(const key of this.groupedTransparentEdgeInteractiveKeysSnapshot||[])changed.add(key);for(const key of currentInteractive)changed.add(key);for(const key of changed){const shouldSuppress=mass.allKeys&&mass.allKeys.has(key)&&!currentActive.has(key)&&!currentInteractive.has(key)&&!(this.isColorOverrideKey&&this.isColorOverrideKey(key));const meshes=this.getMeshesForElementKey?this.getMeshesForElementKey(key):[];for(const mesh of meshes){if(!mesh)continue;const parentVisible=!!(mesh.visible!==false&&mesh.userData&&mesh.userData.baseVisible!==false&&!mesh.userData.normalChunkSuppressed&&!mesh.userData.massTransparencyChunkSuppressed&&!mesh.userData.visualStateChunkSuppressed);for(const child of mesh.children||[]){if(!child||!child.isLineSegments||!(child.userData&&child.userData.isGeneratedEdge))continue;child.userData.groupedTransparentSuppressed=shouldSuppress;child.visible=shouldSuppress?false:parentVisible;}}}this.groupedTransparentEdgeActiveKeysSnapshot=currentActive;this.groupedTransparentEdgeInteractiveKeysSnapshot=currentInteractive;return changed.size;}buildGroupedTransparentEdgesFromNormalBatches(signature,groupedMaterial){const sourceBatches=Array.from(this.normalChunkEdgeBatches||[]).filter(batch=>batch&&batch.geometry);if(!sourceBatches.length)return null;for(const sourceBatch of sourceBatches){const position=sourceBatch.geometry.getAttribute&&sourceBatch.geometry.getAttribute('position');const ranges=sourceBatch.userData&&sourceBatch.userData.edgeFloatRangesByKey;const mapped=Number(sourceBatch.userData&&sourceBatch.userData.edgeMappedFloatValues)||0;if(!position||!position.array||!(ranges instanceof Map)||mapped<position.array.length)return null;}const mass=this.visualMassState||{};const allKeys=mass.allKeys||new Set();const activeKeys=mass.activeKeys||new Set();const interactiveKeys=new Set();if(!this.shouldUseSelectionOverlayBatches||!this.shouldUseSelectionOverlayBatches()){for(const key of this.selectedKeys||[])interactiveKeys.add(key);}if(this.visualHoverKey)interactiveKeys.add(this.visualHoverKey);for(const key of this.rectanglePreviewKeys||[])interactiveKeys.add(key);const includeKey=key=>!!(key&&allKeys.has(key)&&(!this.isElementKeyBaseVisibleForRender||this.isElementKeyBaseVisibleForRender(key))&&!activeKeys.has(key)&&!interactiveKeys.has(key)&&!(this.isColorOverrideKey&&this.isColorOverrideKey(key)));const batches=[];for(const sourceBatch of sourceBatches){const sourceGeometry=sourceBatch.geometry;const position=sourceGeometry.getAttribute('position');const sourceArray=position.array;const rangeMap=sourceBatch.userData.edgeFloatRangesByKey;const includedRanges=[];let floatCount=0;for(const[key,ranges]of rangeMap.entries()){if(!includeKey(key))continue;for(let i=0;i+1<ranges.length;i+=2){const start=Number(ranges[i])||0;const count=Number(ranges[i+1])||0;if(!(count>0)||start<0||start+count>sourceArray.length)continue;includedRanges.push({key,start,count});floatCount+=count;}}if(!floatCount)continue;const sharesNormalGeometry=floatCount===sourceArray.length;let geometry=sourceGeometry;const outputRanges=new Map();if(!sharesNormalGeometry){const positions=new Float32Array(floatCount);let cursor=0;for(const item of includedRanges){positions.set(sourceArray.subarray(item.start,item.start+item.count),cursor);if(this.appendEdgeFloatRange)this.appendEdgeFloatRange(outputRanges,item.key,cursor,item.count);cursor+=item.count;}geometry=new THREE.BufferGeometry();geometry.setAttribute('position',new THREE.BufferAttribute(positions,3));geometry.computeBoundingBox();geometry.computeBoundingSphere();}else{for(const item of includedRanges){if(this.appendEdgeFloatRange)this.appendEdgeFloatRange(outputRanges,item.key,item.start,item.count);}}const batch=new THREE.LineSegments(geometry,groupedMaterial);batch.renderOrder=sourceBatch.renderOrder||0;batch.frustumCulled=true;batch.userData=Object.assign({},batch.userData||{},{isGroupedTransparentEdgeBatch:true,derivedFromNormalEdgeBatch:true,sharedNormalEdgeGeometry:sharesNormalGeometry,renderProjectId:sourceBatch.userData?(sourceBatch.userData.renderProjectId||''):'',cacheSignature:signature,spatialChunkKey:sourceBatch.userData?(sourceBatch.userData.spatialChunkKey||'all'):'all',edgeFloatValues:floatCount,edgeFloatRangesByKey:outputRanges,edgeMappedFloatValues:floatCount});batch.visible=true;this.scene.add(batch);batches.push(batch);}return batches;}rebuildGroupedTransparentEdges(forceRebuild){const metricsStart=this.getPerformanceNow?this.getPerformanceNow():Date.now();const signature=this.getGroupedTransparentEdgesSignature?this.getGroupedTransparentEdgesSignature():null;if(!this.shouldUseGroupedTransparentEdges()){this.clearGroupedTransparentEdges(false);return 0;}if(!forceRebuild&&signature&&signature===this.groupedTransparentEdgeCacheSignature&&this.groupedTransparentEdgeBatches&&this.groupedTransparentEdgeBatches.length){const changedKeys=this.syncGroupedTransparentSourceEdgesIncremental?this.syncGroupedTransparentSourceEdgesIncremental():0;for(const batch of this.groupedTransparentEdgeBatches){if(!batch)continue;batch.visible=true;if(!batch.parent)this.scene.add(batch);}this.groupedTransparentEdgeReuseCount=(this.groupedTransparentEdgeReuseCount||0)+1;if(this.recordPerformanceOperation)this.recordPerformanceOperation('groupedTransparentEdges:reuse-incremental',(this.getPerformanceNow?this.getPerformanceNow():Date.now())-metricsStart,{batches:this.groupedTransparentEdgeBatches.length,changedKeys});return this.groupedTransparentEdgeBatches.length;}this.clearGroupedTransparentEdges(false);if(!this.groupedTransparentEdgeCache)this.groupedTransparentEdgeCache=new Map();const cached=!forceRebuild&&signature?this.groupedTransparentEdgeCache.get(signature):null;if(cached&&cached.batches&&cached.batches.length){this.groupedTransparentEdgeBatches=cached.batches;this.groupedTransparentEdgeCacheSignature=signature;this.suppressGroupedTransparentSourceEdges&&this.suppressGroupedTransparentSourceEdges();this.groupedTransparentEdgeActiveKeysSnapshot=new Set(this.visualMassState&&this.visualMassState.activeKeys||[]);this.groupedTransparentEdgeInteractiveKeysSnapshot=new Set();if(!this.shouldUseSelectionOverlayBatches||!this.shouldUseSelectionOverlayBatches()){for(const key of this.selectedKeys||[])this.groupedTransparentEdgeInteractiveKeysSnapshot.add(key);}if(this.visualHoverKey)this.groupedTransparentEdgeInteractiveKeysSnapshot.add(this.visualHoverKey);for(const key of this.rectanglePreviewKeys||[])this.groupedTransparentEdgeInteractiveKeysSnapshot.add(key);for(const batch of this.groupedTransparentEdgeBatches||[]){if(!batch)continue;batch.visible=true;this.scene.add(batch);}this.groupedTransparentEdgeReuseCount=(this.groupedTransparentEdgeReuseCount||0)+1;if(this.recordPerformanceOperation)this.recordPerformanceOperation('groupedTransparentEdges:reuse',(this.getPerformanceNow?this.getPerformanceNow():Date.now())-metricsStart,{batches:this.groupedTransparentEdgeBatches.length});return this.groupedTransparentEdgeBatches.length;}const groups=new Map();const temp=new THREE.Vector3();const mass=this.visualMassState||{};const groupedMaterial=this.getDarkerEdgeMaterialForColor(mass.fadedColor==null?this.getConfiguredTransparencyColor():Number(mass.fadedColor),Math.max(0.04,Number(mass.fadedOpacity==null?this.getConfiguredTransparencyOpacity(0.02):mass.fadedOpacity)),true);const groupedKey=this.getGroupedTransparentEdgeMaterialKey(groupedMaterial);const allKeys=mass.allKeys||null;const activeKeys=mass.activeKeys||null;const spatialGrid=this.getSpatialChunkGrid?this.getSpatialChunkGrid(this.meshes||[],{spatialMinimumMeshes:40}):null;const derivedBatches=this.buildGroupedTransparentEdgesFromNormalBatches?this.buildGroupedTransparentEdgesFromNormalBatches(signature,groupedMaterial):null;if(derivedBatches!==null){this.groupedTransparentEdgeBatches=derivedBatches;this.groupedTransparentEdgeCacheSignature=signature;this.groupedTransparentEdgeActiveKeysSnapshot=new Set(activeKeys||[]);this.groupedTransparentEdgeInteractiveKeysSnapshot=new Set();if(!this.shouldUseSelectionOverlayBatches||!this.shouldUseSelectionOverlayBatches()){for(const key of this.selectedKeys||[])this.groupedTransparentEdgeInteractiveKeysSnapshot.add(key);}if(this.visualHoverKey)this.groupedTransparentEdgeInteractiveKeysSnapshot.add(this.visualHoverKey);for(const key of this.rectanglePreviewKeys||[])this.groupedTransparentEdgeInteractiveKeysSnapshot.add(key);if(signature&&derivedBatches.length){this.groupedTransparentEdgeCache.set(signature,{batches:derivedBatches});const limit=Number(this.groupedTransparentEdgeCacheLimit||6);while(this.groupedTransparentEdgeCache.size>limit){const firstKey=this.groupedTransparentEdgeCache.keys().next().value;const entry=this.groupedTransparentEdgeCache.get(firstKey);this.groupedTransparentEdgeCache.delete(firstKey);for(const batch of(entry&&entry.batches)||[]){if(batch.parent)batch.parent.remove(batch);if(!(batch.userData&&batch.userData.sharedNormalEdgeGeometry)&&batch.geometry&&batch.geometry.dispose)batch.geometry.dispose();}}}this.suppressGroupedTransparentSourceEdges&&this.suppressGroupedTransparentSourceEdges();this.groupedTransparentEdgeBuildCount=(this.groupedTransparentEdgeBuildCount||0)+1;if(this.recordPerformanceOperation)this.recordPerformanceOperation('groupedTransparentEdges:derive-normal-batches',(this.getPerformanceNow?this.getPerformanceNow():Date.now())-metricsStart,{batches:derivedBatches.length,normalEdgeBatches:this.normalChunkEdgeBatches?this.normalChunkEdgeBatches.length:0});return derivedBatches.length;}for(const mesh of this.meshes||[]){if(!mesh||!mesh.isMesh)continue;if(this.isNonRenderedDataMesh(mesh))continue;const meshKey=this.getMeshKey?this.getMeshKey(mesh):null;const baseVisible=!(mesh.userData&&mesh.userData.baseVisible===false);const includeByMass=!!(meshKey&&allKeys&&allKeys.has(meshKey));const includeSuppressed=!!(mesh.userData&&(mesh.userData.massTransparencyChunkSuppressed||mesh.userData.normalChunkSuppressed));if(!baseVisible||!includeByMass||(!mesh.visible&&!includeSuppressed&&!(activeKeys&&activeKeys.has(meshKey))))continue;if(this.isColorOverrideKey&&this.isColorOverrideKey(meshKey))continue;mesh.updateWorldMatrix(true,false);const edgeSources=this.getNormalChunkEdgeSourcesForMesh?this.getNormalChunkEdgeSourcesForMesh(mesh):[];for(const edgeSource of edgeSources){if(!edgeSource||!edgeSource.geometry)continue;if(this.isSpecialInteractiveEdgeMaterial(edgeSource.material))continue;const position=edgeSource.geometry.getAttribute&&edgeSource.geometry.getAttribute('position');if(!position||!position.count)continue;const spatialKey=spatialGrid&&this.getSpatialChunkKeyForMesh?this.getSpatialChunkKeyForMesh(mesh,spatialGrid):'all';const projectId=this.getMeshRenderProjectId?this.getMeshRenderProjectId(mesh):'';const bucketKey=groupedKey+'|project:'+projectId+'|cell:'+spatialKey;let bucket=groups.get(bucketKey);if(!bucket){bucket={material:groupedMaterial,renderOrder:edgeSource.renderOrder||0,arrays:[],count:0,spatialKey,projectId};groups.set(bucketKey,bucket);}const transformObject=edgeSource.transformObject||mesh;transformObject.updateWorldMatrix&&transformObject.updateWorldMatrix(true,false);const transformed=this.buildTransformedLineSegmentPositions?this.buildTransformedLineSegmentPositions(edgeSource.geometry,transformObject.matrixWorld||mesh.matrixWorld,temp):new Float32Array(0);if(!transformed.length)continue;bucket.arrays.push({positions:transformed,sourceKey:meshKey});bucket.count+=transformed.length;}}const batches=[];for(const bucket of groups.values()){if(!bucket||!bucket.count)continue;const merged=new Float32Array(bucket.count);const rangeMap=new Map();let offset=0;for(const item of bucket.arrays){const arr=item.positions;merged.set(arr,offset);if(this.appendEdgeFloatRange)this.appendEdgeFloatRange(rangeMap,item.sourceKey,offset,arr.length);offset+=arr.length;}const geometry=new THREE.BufferGeometry();geometry.setAttribute('position',new THREE.BufferAttribute(merged,3));geometry.computeBoundingBox();geometry.computeBoundingSphere();const batch=new THREE.LineSegments(geometry,bucket.material);batch.renderOrder=bucket.renderOrder||0;batch.frustumCulled=true;batch.userData=Object.assign({},batch.userData||{},{isGroupedTransparentEdgeBatch:true,renderProjectId:bucket.projectId||'',cacheSignature:signature,spatialChunkKey:bucket.spatialKey||'all',edgeFloatValues:merged.length,edgeFloatRangesByKey:rangeMap,edgeMappedFloatValues:merged.length});batch.visible=true;this.scene.add(batch);batches.push(batch);}this.groupedTransparentEdgeBatches=batches;this.groupedTransparentEdgeCacheSignature=signature;this.groupedTransparentEdgeActiveKeysSnapshot=new Set(this.visualMassState&&this.visualMassState.activeKeys||[]);this.groupedTransparentEdgeInteractiveKeysSnapshot=new Set();if(!this.shouldUseSelectionOverlayBatches||!this.shouldUseSelectionOverlayBatches()){for(const key of this.selectedKeys||[])this.groupedTransparentEdgeInteractiveKeysSnapshot.add(key);}if(this.visualHoverKey)this.groupedTransparentEdgeInteractiveKeysSnapshot.add(this.visualHoverKey);for(const key of this.rectanglePreviewKeys||[])this.groupedTransparentEdgeInteractiveKeysSnapshot.add(key);if(signature&&batches.length){this.groupedTransparentEdgeCache.set(signature,{batches});const limit=Number(this.groupedTransparentEdgeCacheLimit||6);while(this.groupedTransparentEdgeCache.size>limit){const firstKey=this.groupedTransparentEdgeCache.keys().next().value;const entry=this.groupedTransparentEdgeCache.get(firstKey);this.groupedTransparentEdgeCache.delete(firstKey);for(const batch of(entry&&entry.batches)||[]){if(batch.parent)batch.parent.remove(batch);if(!(batch.userData&&batch.userData.sharedNormalEdgeGeometry)&&batch.geometry&&batch.geometry.dispose)batch.geometry.dispose();}}}this.suppressGroupedTransparentSourceEdges&&this.suppressGroupedTransparentSourceEdges();this.groupedTransparentEdgeBuildCount=(this.groupedTransparentEdgeBuildCount||0)+1;if(this.recordPerformanceOperation)this.recordPerformanceOperation('groupedTransparentEdges:build',(this.getPerformanceNow?this.getPerformanceNow():Date.now())-metricsStart,{batches:batches.length,groups:groups.size,fullBase:true});return batches.length;}clearNormalChunkEdges(dispose,preserveBuildGeneration){if(preserveBuildGeneration!==true)this.normalChunkEdgeBuildGeneration=(this.normalChunkEdgeBuildGeneration||0)+1;const shouldDispose=dispose===true;if(shouldDispose&&this.groupedTransparentEdgeCache&&this.groupedTransparentEdgeCache.size&&this.clearGroupedTransparentEdges){this.clearGroupedTransparentEdges(true);}let removed=0;if(this.normalChunkEdgeBatches&&this.normalChunkEdgeBatches.length){for(const batch of this.normalChunkEdgeBatches){if(!batch)continue;if(batch.parent)batch.parent.remove(batch);if(shouldDispose&&batch.geometry&&batch.geometry.dispose)batch.geometry.dispose();removed++;}}if(shouldDispose)this.normalChunkEdgeBatches=[];return removed;}removeNormalChunkEdgesForSpatialKeys(spatialKeys,dispose){const keySet=new Set(spatialKeys||[]);if(!keySet.size||!this.normalChunkEdgeBatches||!this.normalChunkEdgeBatches.length)return 0;const shouldDispose=dispose===true;const kept=[];let removed=0;for(const batch of this.normalChunkEdgeBatches){if(!batch)continue;const spatialKey=batch.userData?(batch.userData.spatialChunkKey||'all'):'all';if(!keySet.has(spatialKey)){kept.push(batch);continue;}if(batch.parent)batch.parent.remove(batch);if(shouldDispose&&batch.geometry&&batch.geometry.dispose)batch.geometry.dispose();removed++;}this.normalChunkEdgeBatches=kept;return removed;}removeNormalChunkEdgesForKeys(keys,dispose){const keySet=new Set(keys||[]);if(!keySet.size||!this.normalChunkEdgeBatches||!this.normalChunkEdgeBatches.length)return 0;const shouldDispose=dispose===true;const kept=[];let changed=0;for(const batch of this.normalChunkEdgeBatches){if(!batch)continue;const result=this.filterMappedEdgeBatchByExcludedKeys?this.filterMappedEdgeBatchByExcludedKeys(batch,keySet):{changed:false,complete:false,empty:false};if(!result.complete){return this.clearNormalChunkEdges?this.clearNormalChunkEdges(shouldDispose):0;}if(!result.changed){kept.push(batch);continue;}changed++;if(result.empty){if(batch.parent)batch.parent.remove(batch);if(shouldDispose&&batch.geometry&&batch.geometry.dispose)batch.geometry.dispose();}else{kept.push(batch);}}this.normalChunkEdgeBatches=kept;if(changed&&this.groupedTransparentEdgeCache instanceof Map){this.groupedTransparentEdgeCache.clear();this.groupedTransparentEdgeCacheSignature=null;}return changed;}getNormalChunkEdgeMaterialKey(material){const color=material&&material.color&&material.color.isColor?material.color.getHexString():'0';const opacity=Math.max(0.02,Math.min(1,Number(material&&material.opacity==null?1:material.opacity))).toFixed(3);const transparent=material&&material.transparent?'T':'O';return['normalEdge',color,opacity,transparent].join(':');}forEachTransformedLineSegmentPair(geometry,matrix,tempVector,callback){if(!geometry||!callback)return 0;const position=geometry.getAttribute&&geometry.getAttribute('position');if(!position||!position.count||!position.array)return 0;const index=geometry.index&&geometry.index.count>=2?geometry.index:null;const indexArray=index&&index.array?index.array:null;const referenceCount=index?Math.floor(index.count/2)*2:Math.floor(position.count/2)*2;if(referenceCount<2)return 0;const source=position.array;const temp=tempVector||new THREE.Vector3();const safeMatrix=matrix||new THREE.Matrix4();let emittedPairs=0;for(let reference=0;reference+1<referenceCount;reference+=2){const firstIndex=indexArray?Number(indexArray[reference]):reference;const secondIndex=indexArray?Number(indexArray[reference+1]):reference+1;if(!Number.isFinite(firstIndex)||!Number.isFinite(secondIndex)||firstIndex<0||secondIndex<0||firstIndex>=position.count||secondIndex>=position.count){continue;}let offset=firstIndex*3;temp.set(source[offset],source[offset+1],source[offset+2]).applyMatrix4(safeMatrix);const ax=temp.x;const ay=temp.y;const az=temp.z;offset=secondIndex*3;temp.set(source[offset],source[offset+1],source[offset+2]).applyMatrix4(safeMatrix);callback(ax,ay,az,temp.x,temp.y,temp.z);emittedPairs++;}return emittedPairs;}buildTransformedLineSegmentPositions(geometry,matrix,tempVector){if(!geometry)return new Float32Array(0);const position=geometry.getAttribute&&geometry.getAttribute('position');if(!position||!position.count)return new Float32Array(0);const index=geometry.index&&geometry.index.count>=2?geometry.index:null;const referenceCount=index?Math.floor(index.count/2)*2:Math.floor(position.count/2)*2;if(referenceCount<2)return new Float32Array(0);const transformed=new Float32Array(referenceCount*3);let cursor=0;this.forEachTransformedLineSegmentPair(geometry,matrix,tempVector,(ax,ay,az,bx,by,bz)=>{transformed[cursor++]=ax;transformed[cursor++]=ay;transformed[cursor++]=az;transformed[cursor++]=bx;transformed[cursor++]=by;transformed[cursor++]=bz;});return cursor===transformed.length?transformed:transformed.slice(0,cursor);}appendEdgeFloatRange(rangeMap,key,start,count){if(!(rangeMap instanceof Map)||!key||!(count>0))return;let ranges=rangeMap.get(key);if(!ranges){ranges=[];rangeMap.set(key,ranges);}const length=ranges.length;if(length>=2&&ranges[length-2]+ranges[length-1]===start){ranges[length-1]+=count;}else{ranges.push(start,count);}}filterMappedEdgeBatchByExcludedKeys(batch,excludedKeys){const keySet=new Set(excludedKeys||[]);if(!batch||!batch.geometry||!keySet.size)return{changed:false,complete:false,empty:false};const position=batch.geometry.getAttribute&&batch.geometry.getAttribute('position');const source=position&&position.array;const rangeMap=batch.userData&&batch.userData.edgeFloatRangesByKey;const mapped=Number(batch.userData&&batch.userData.edgeMappedFloatValues)||0;if(!source||!(rangeMap instanceof Map)||mapped<source.length){return{changed:false,complete:false,empty:false};}let keptFloatValues=0;let removedFloatValues=0;for(const[key,ranges]of rangeMap.entries()){for(let i=0;i+1<ranges.length;i+=2){const start=Number(ranges[i])||0;const count=Number(ranges[i+1])||0;if(!(count>0)||start<0||start+count>source.length)continue;if(keySet.has(key))removedFloatValues+=count;else keptFloatValues+=count;}}if(!removedFloatValues)return{changed:false,complete:true,empty:false};if(!keptFloatValues)return{changed:true,complete:true,empty:true};const positions=new Float32Array(keptFloatValues);const nextRanges=new Map();let cursor=0;for(const[key,ranges]of rangeMap.entries()){if(keySet.has(key))continue;for(let i=0;i+1<ranges.length;i+=2){const start=Number(ranges[i])||0;const count=Number(ranges[i+1])||0;if(!(count>0)||start<0||start+count>source.length)continue;positions.set(source.subarray(start,start+count),cursor);this.appendEdgeFloatRange(nextRanges,key,cursor,count);cursor+=count;}}const previousGeometry=batch.geometry;const sharedPreviousGeometry=!!(batch.userData&&batch.userData.sharedNormalEdgeGeometry);const geometry=new THREE.BufferGeometry();geometry.setAttribute('position',new THREE.BufferAttribute(positions,3));geometry.computeBoundingBox();geometry.computeBoundingSphere();batch.geometry=geometry;batch.userData.edgeFloatRangesByKey=nextRanges;batch.userData.edgeMappedFloatValues=positions.length;batch.userData.edgeFloatValues=positions.length;batch.userData.edgeSegments=Math.floor(positions.length/6);batch.userData.sharedNormalEdgeGeometry=false;if(!sharedPreviousGeometry&&previousGeometry&&previousGeometry.dispose)previousGeometry.dispose();return{changed:true,complete:true,empty:false,removedFloatValues};}removeGroupedTransparentEdgesForKeys(keys,dispose){const keySet=new Set(keys||[]);if(!keySet.size||!this.groupedTransparentEdgeBatches||!this.groupedTransparentEdgeBatches.length)return 0;const activeSignature=this.groupedTransparentEdgeCacheSignature;if(activeSignature&&this.groupedTransparentEdgeCache instanceof Map){this.groupedTransparentEdgeCache.delete(activeSignature);}const kept=[];let changed=0;for(const batch of this.groupedTransparentEdgeBatches){if(!batch)continue;const result=this.filterMappedEdgeBatchByExcludedKeys?this.filterMappedEdgeBatchByExcludedKeys(batch,keySet):{changed:false,complete:false,empty:false};if(!result.complete){return this.clearGroupedTransparentEdges?this.clearGroupedTransparentEdges(dispose===true):0;}if(!result.changed){kept.push(batch);continue;}changed++;if(result.empty){if(batch.parent)batch.parent.remove(batch);const sharedGeometry=!!(batch.userData&&batch.userData.sharedNormalEdgeGeometry);if(dispose===true&&!sharedGeometry&&batch.geometry&&batch.geometry.dispose)batch.geometry.dispose();}else{kept.push(batch);}}this.groupedTransparentEdgeBatches=kept;this.groupedTransparentEdgeCacheSignature=null;return changed;}removeVisualStateChunkEdgesForKeys(keys,dispose){const keySet=new Set(keys||[]);if(!keySet.size||!this.visualStateChunkEdgeBatches||!this.visualStateChunkEdgeBatches.length)return 0;const kept=[];let changed=0;let requiresFullRebuild=false;for(const batch of this.visualStateChunkEdgeBatches){if(!batch)continue;const result=this.filterMappedEdgeBatchByExcludedKeys?this.filterMappedEdgeBatchByExcludedKeys(batch,keySet):{changed:false,complete:false,empty:false};if(!result.complete){requiresFullRebuild=true;kept.push(batch);continue;}if(!result.changed){kept.push(batch);continue;}changed++;if(result.empty){if(batch.parent)batch.parent.remove(batch);if(batch.geometry&&batch.geometry.dispose)batch.geometry.dispose();}else{kept.push(batch);}}this.visualStateChunkEdgeBatches=kept;if(requiresFullRebuild)this.visualStateChunkCacheSignature=null;return changed;}rebuildNormalChunkEdgesFromMeshes(meshes,disposeExisting,options){const opts=options&&typeof options==='object'?options:{};const appendExisting=opts.appendExisting===true;const metricsStart=this.getPerformanceNow?this.getPerformanceNow():Date.now();if(!appendExisting)this.clearNormalChunkEdges(disposeExisting===true);if(!meshes||!meshes.length)return 0;const groups=new Map();const temp=new THREE.Vector3();const spatialGrid=!opts.forcedSpatialChunkKey&&this.getSpatialChunkGrid?this.getSpatialChunkGrid(meshes,{spatialMinimumMeshes:Number(this.spatialChunkMinimumMeshes||24),targetMeshesPerCell:150,maximumCells:Number(this.spatialChunkMaximumCells||512)}):null;for(const mesh of meshes){if(!mesh||!mesh.isMesh)continue;mesh.updateWorldMatrix(true,false);const edgeSources=this.getNormalChunkEdgeSourcesForMesh?this.getNormalChunkEdgeSourcesForMesh(mesh):[];for(const edgeSource of edgeSources){if(!edgeSource||!edgeSource.geometry)continue;const material=edgeSource.material||this.edgeMaterial;if(!material)continue;const position=edgeSource.geometry.getAttribute&&edgeSource.geometry.getAttribute('position');if(!position||!position.count)continue;const spatialKey=opts.forcedSpatialChunkKey||(spatialGrid&&this.getSpatialChunkKeyForMesh?this.getSpatialChunkKeyForMesh(mesh,spatialGrid):'all');const projectId=this.getMeshRenderProjectId?this.getMeshRenderProjectId(mesh):'';const key=this.getNormalChunkEdgeMaterialKey(material)+'|project:'+projectId+'|cell:'+spatialKey;let bucket=groups.get(key);if(!bucket){bucket={material,arrays:[],count:0,renderOrder:edgeSource.renderOrder||1,spatialKey,projectId,compactFallbackSources:0};groups.set(key,bucket);}if(edgeSource.compactFallback)bucket.compactFallbackSources++;const transformObject=edgeSource.transformObject||mesh;transformObject.updateWorldMatrix&&transformObject.updateWorldMatrix(true,false);const matrix=transformObject.matrixWorld||mesh.matrixWorld;const transformed=this.buildTransformedLineSegmentPositions?this.buildTransformedLineSegmentPositions(edgeSource.geometry,matrix,temp):new Float32Array(0);if(!transformed.length)continue;const sourceKey=this.getMeshKey?this.getMeshKey(mesh):(mesh.userData&&mesh.userData.elementKey);bucket.arrays.push({positions:transformed,sourceKey});bucket.count+=transformed.length;}}const batches=[];const maxFloatValues=Math.max(6000,Math.floor(Number(this.normalChunkEdgeMaxFloatValues||240000)/6)*6);const createBatch=(bucket,arrays,floatCount,batchIndex,rangeMap)=>{if(!floatCount)return;const merged=new Float32Array(floatCount);let offset=0;for(const arr of arrays){merged.set(arr,offset);offset+=arr.length;}const geometry=new THREE.BufferGeometry();geometry.setAttribute('position',new THREE.BufferAttribute(merged,3));geometry.computeBoundingBox();geometry.computeBoundingSphere();const batch=new THREE.LineSegments(geometry,bucket.material);batch.renderOrder=bucket.renderOrder||1;batch.frustumCulled=true;batch.userData=Object.assign({},batch.userData||{},{isNormalChunkEdgeBatch:true,stableBaseMaterial:bucket.material,renderProjectId:bucket.projectId||'',spatialChunkKey:bucket.spatialKey||'all',adaptiveEdgeBatchIndex:batchIndex,edgeFloatValues:floatCount,edgeSegments:Math.floor(floatCount/6),compactFallbackEdgeBatch:bucket.compactFallbackSources>0,compactFallbackSourceCount:bucket.compactFallbackSources||0,edgeFloatRangesByKey:rangeMap,edgeMappedFloatValues:Array.from(rangeMap?rangeMap.values():[]).reduce((total,ranges)=>{for(let i=1;i<ranges.length;i+=2)total+=Number(ranges[i])||0;return total;},0)});this.scene.add(batch);batches.push(batch);};for(const bucket of groups.values()){if(!bucket||!bucket.count)continue;let parts=[];let partCount=0;let batchIndex=0;let rangeMap=new Map();const flush=()=>{if(!partCount)return;createBatch(bucket,parts,partCount,batchIndex++,rangeMap);parts=[];partCount=0;rangeMap=new Map();};for(const sourceItem of bucket.arrays){const sourceArray=sourceItem&&sourceItem.positions?sourceItem.positions:sourceItem;const sourceKey=sourceItem&&sourceItem.sourceKey?sourceItem.sourceKey:'';let offset=0;while(offset<sourceArray.length){const remaining=sourceArray.length-offset;const available=maxFloatValues-partCount;const take=Math.min(remaining,available);const alignedTake=take===remaining?take:Math.max(6,Math.floor(take/6)*6);if(partCount&&alignedTake>available){flush();continue;}if(sourceKey&&this.appendEdgeFloatRange)this.appendEdgeFloatRange(rangeMap,sourceKey,partCount,alignedTake);parts.push(sourceArray.subarray(offset,offset+alignedTake));partCount+=alignedTake;offset+=alignedTake;if(partCount>=maxFloatValues)flush();}}flush();}this.normalChunkEdgeBatches=appendExisting?Array.from(this.normalChunkEdgeBatches||[]).concat(batches):batches;if(this.recordPerformanceOperation)this.recordPerformanceOperation('normalChunkEdges:adaptive-build',(this.getPerformanceNow?this.getPerformanceNow():Date.now())-metricsStart,{sourceMeshes:meshes.length,materialSpatialGroups:groups.size,edgeBatches:batches.length,maxFloatValues});return batches.length;}async rebuildNormalChunkEdgesFromMeshesAsync(meshes,options){const opts=options&&typeof options==='object'?options:{};const report=typeof opts.onProgress==='function'?opts.onProgress:()=>{};const metricsStart=this.getPerformanceNow?this.getPerformanceNow():Date.now();const buildGeneration=(this.normalChunkEdgeBuildGeneration||0)+1;this.normalChunkEdgeBuildGeneration=buildGeneration;const isCancelled=()=>this.normalChunkEdgeBuildGeneration!==buildGeneration;const list=Array.from(meshes||[]).filter(mesh=>mesh&&mesh.isMesh);if(!list.length){report(1,1,'Bordes visibles sincronizados.');return(this.normalChunkEdgeBatches||[]).filter(batch=>batch&&batch.parent).length;}const groups=new Map();const spatialGrid=this.getSpatialChunkGrid?this.getSpatialChunkGrid(list,{spatialMinimumMeshes:Number(this.spatialChunkMinimumMeshes||24),targetMeshesPerCell:150,maximumCells:Number(this.spatialChunkMaximumCells||512)}):null;const batchSize=Math.max(50,Number(opts.batchSize)||240);const maxFloatValues=Math.max(6000,Math.floor(Number(this.normalChunkEdgeMaxFloatValues||240000)/6)*6);const yieldFrame=async()=>{if(opts.yieldBetweenBatches===false)return;if(this.waitForResetProgressPaint)await this.waitForResetProgressPaint();else if(this.yieldDuringLoad)await this.yieldDuringLoad();else await new Promise(resolve=>{const schedule=typeof requestAnimationFrame==='function'?requestAnimationFrame:callback=>setTimeout(callback,0);schedule(()=>resolve());});};report(0,list.length,'Analizando bordes de los elementos visibles...');for(let start=0;start<list.length;start+=batchSize){const end=Math.min(list.length,start+batchSize);for(let meshIndex=start;meshIndex<end;meshIndex++){const mesh=list[meshIndex];mesh.updateWorldMatrix(true,false);const edgeSources=this.getNormalChunkEdgeSourcesForMesh?this.getNormalChunkEdgeSourcesForMesh(mesh):[];for(const edgeSource of edgeSources){if(!edgeSource||!edgeSource.geometry)continue;const material=edgeSource.material||this.edgeMaterial;if(!material)continue;const position=edgeSource.geometry.getAttribute&&edgeSource.geometry.getAttribute('position');if(!position||!position.count)continue;const spatialKey=spatialGrid&&this.getSpatialChunkKeyForMesh?this.getSpatialChunkKeyForMesh(mesh,spatialGrid):'all';const projectId=this.getMeshRenderProjectId?this.getMeshRenderProjectId(mesh):'';const key=this.getNormalChunkEdgeMaterialKey(material)+'|project:'+projectId+'|cell:'+spatialKey;let bucket=groups.get(key);if(!bucket){bucket={material,sources:[],renderOrder:edgeSource.renderOrder||1,spatialKey,projectId,compactFallbackSources:0};groups.set(key,bucket);}if(edgeSource.compactFallback)bucket.compactFallbackSources++;const sourceKey=this.getMeshKey?this.getMeshKey(mesh):(mesh.userData&&mesh.userData.elementKey);bucket.sources.push({mesh,edgeSource,position,sourceKey});}}report(end,list.length,'Analizando bordes de los elementos visibles...');if(end<list.length)await yieldFrame();if(isCancelled())return 0;}const batches=[];const disposePendingBatches=()=>{for(const batch of batches){if(batch&&batch.parent)batch.parent.remove(batch);if(batch&&batch.geometry&&batch.geometry.dispose)batch.geometry.dispose();}batches.length=0;};const buckets=Array.from(groups.values()).filter(bucket=>bucket&&bucket.sources&&bucket.sources.length);const totalBuckets=Math.max(1,buckets.length);const temp=new THREE.Vector3();const createBatch=(bucket,staging,floatCount,batchIndex,rangeMap)=>{if(!floatCount)return null;const finalPositions=floatCount===staging.length?staging:staging.slice(0,floatCount);const geometry=new THREE.BufferGeometry();geometry.setAttribute('position',new THREE.BufferAttribute(finalPositions,3));geometry.computeBoundingBox();geometry.computeBoundingSphere();const batch=new THREE.LineSegments(geometry,bucket.material);batch.renderOrder=bucket.renderOrder||1;batch.frustumCulled=true;batch.userData=Object.assign({},batch.userData||{},{isNormalChunkEdgeBatch:true,stableBaseMaterial:bucket.material,renderProjectId:bucket.projectId||'',spatialChunkKey:bucket.spatialKey||'all',adaptiveEdgeBatchIndex:batchIndex,edgeFloatValues:floatCount,edgeSegments:Math.floor(floatCount/6),compactFallbackEdgeBatch:bucket.compactFallbackSources>0,compactFallbackSourceCount:bucket.compactFallbackSources||0,edgeFloatRangesByKey:rangeMap,edgeMappedFloatValues:Array.from(rangeMap?rangeMap.values():[]).reduce((total,ranges)=>{for(let i=1;i<ranges.length;i+=2)total+=Number(ranges[i])||0;return total;},0),memorySafeChunk:true});batches.push(batch);return batch;};report(0,totalBuckets,'Construyendo lotes de bordes visibles...');for(let bucketIndex=0;bucketIndex<buckets.length;bucketIndex++){const bucket=buckets[bucketIndex];let staging=new Float32Array(maxFloatValues);let cursor=0;let batchIndex=0;let rangeMap=new Map();const flush=()=>{if(!cursor)return;createBatch(bucket,staging,cursor,batchIndex++,rangeMap);staging=new Float32Array(maxFloatValues);cursor=0;rangeMap=new Map();};for(const sourceItem of bucket.sources){const mesh=sourceItem.mesh;const edgeSource=sourceItem.edgeSource;const position=sourceItem.position;const sourceKey=sourceItem.sourceKey||'';if(!mesh||!edgeSource||!edgeSource.geometry||!position)continue;const transformObject=edgeSource.transformObject||mesh;transformObject.updateWorldMatrix&&transformObject.updateWorldMatrix(true,false);const matrix=transformObject.matrixWorld||mesh.matrixWorld;if(this.forEachTransformedLineSegmentPair){this.forEachTransformedLineSegmentPair(edgeSource.geometry,matrix,temp,(ax,ay,az,bx,by,bz)=>{if(cursor+6>staging.length)flush();if(sourceKey&&this.appendEdgeFloatRange)this.appendEdgeFloatRange(rangeMap,sourceKey,cursor,6);staging[cursor++]=ax;staging[cursor++]=ay;staging[cursor++]=az;staging[cursor++]=bx;staging[cursor++]=by;staging[cursor++]=bz;});}}flush();staging=null;report(bucketIndex+1,totalBuckets,'Construyendo lotes de bordes visibles...');if(bucketIndex+1<buckets.length)await yieldFrame();if(isCancelled()){disposePendingBatches();return 0;}}if(isCancelled()){disposePendingBatches();return 0;}const previousBatches=Array.from(this.normalChunkEdgeBatches||[]);if(opts.disposeExisting===true&&this.groupedTransparentEdgeCache&&this.groupedTransparentEdgeCache.size&&this.clearGroupedTransparentEdges){this.clearGroupedTransparentEdges(true);}for(const previous of previousBatches){if(!previous)continue;if(previous.parent)previous.parent.remove(previous);if(opts.disposeExisting===true&&previous.geometry&&previous.geometry.dispose)previous.geometry.dispose();}this.normalChunkEdgeBatches=batches;for(const batch of batches)if(batch&&!batch.parent)this.scene.add(batch);if(this.stableNormalEdgeAppearanceMode&&this.stableNormalEdgeAppearanceMode!=='normal'&&this.applyStableNormalChunkEdgeAppearance){this.applyStableNormalChunkEdgeAppearance(this.stableNormalEdgeAppearanceMode);}if(this.recordPerformanceOperation)this.recordPerformanceOperation('normalChunkEdges:adaptive-build-async',(this.getPerformanceNow?this.getPerformanceNow():Date.now())-metricsStart,{sourceMeshes:list.length,materialSpatialGroups:groups.size,edgeBatches:batches.length,maxFloatValues,memorySafe:true});report(1,1,'Bordes visibles sincronizados.');return batches.length;}getVisibleNormalChunkEdgeSourceMeshes(){const sources=[];const seen=new Set();if(!this.normalChunkVisible||!this.normalChunkMeshes||!this.normalChunkMeshes.length)return sources;for(const chunk of this.normalChunkMeshes||[]){if(!chunk||!chunk.parent)continue;const adaptiveEdgeOnly=!!(chunk.userData&&chunk.userData.adaptiveSurfaceSuppressed===true);if(chunk.visible===false&&!adaptiveEdgeOnly)continue;const chunkSources=chunk.userData&&Array.isArray(chunk.userData.normalChunkSourceMeshes)?chunk.userData.normalChunkSourceMeshes:[];for(const mesh of chunkSources){if(!mesh||!mesh.isMesh)continue;const key=this.getMeshKey?this.getMeshKey(mesh):(mesh.userData&&mesh.userData.elementKey);const id=key||mesh.uuid;if(seen.has(id))continue;if(mesh.userData&&mesh.userData.normalChunkSuppressed===true&&mesh.userData.baseVisible!==false){sources.push(mesh);seen.add(id);}}}return sources;}refreshNormalChunkEdgesForVisibleChunks(reason){try{if(!this.rebuildNormalChunkEdgesFromMeshes||!this.clearNormalChunkEdges)return 0;if(!this.normalChunkVisible||!this.normalChunkMeshes||!this.normalChunkMeshes.length){this.clearNormalChunkEdges(false);return 0;}const sources=this.getVisibleNormalChunkEdgeSourceMeshes?this.getVisibleNormalChunkEdgeSourceMeshes():[];const count=this.rebuildNormalChunkEdgesFromMeshes(sources,true);this.normalChunkEdgesVisibleOnly=true;if(this.recordPerformanceOperation)this.recordPerformanceOperation('normalChunkEdges:visible-refresh',0,{reason:reason||'visible-chunks-refresh',sourceMeshes:sources.length,edgeBatches:count||0});this.requestRender&&this.requestRender('normal-chunk-edges-visible-refresh');return count||0;}catch(error){if(typeof debugError==='function')debugError('viewer:normalChunkEdges:visibleRefresh:error',error,{reason:reason||''});return 0;}}cancelScheduledNormalChunkEdgesForVisibleChunks(){this.normalChunkEdgeRefreshSequence=(this.normalChunkEdgeRefreshSequence||0)+1;this.normalChunkEdgeAsyncRefreshSequence=(this.normalChunkEdgeAsyncRefreshSequence||0)+1;this.normalChunkEdgeBuildGeneration=(this.normalChunkEdgeBuildGeneration||0)+1;if(this.normalChunkEdgeAsyncRefreshTimer){clearTimeout(this.normalChunkEdgeAsyncRefreshTimer);this.normalChunkEdgeAsyncRefreshTimer=null;}this._pendingNormalChunkVisibleEdgeRefresh=false;this._pendingNormalChunkVisibleEdgeRefreshReason=null;}async refreshNormalChunkEdgesForVisibleChunksAsync(reason,options){const opts=options&&typeof options==='object'?options:{};if(!this.normalChunkVisible||!this.normalChunkMeshes||!this.normalChunkMeshes.length){this.clearNormalChunkEdges&&this.clearNormalChunkEdges(false);return 0;}this.cancelScheduledNormalChunkEdgesForVisibleChunks&&this.cancelScheduledNormalChunkEdgesForVisibleChunks();const sources=this.getVisibleNormalChunkEdgeSourceMeshes?this.getVisibleNormalChunkEdgeSourceMeshes():[];if(!this.rebuildNormalChunkEdgesFromMeshesAsync)return this.refreshNormalChunkEdgesForVisibleChunks(reason);const count=await this.rebuildNormalChunkEdgesFromMeshesAsync(sources,{disposeExisting:true,batchSize:Number(opts.batchSize)||240,yieldBetweenBatches:opts.yieldBetweenBatches!==false,onProgress:opts.onProgress});this.normalChunkEdgesVisibleOnly=true;if(this.recordPerformanceOperation)this.recordPerformanceOperation('normalChunkEdges:visible-refresh-async',0,{reason:reason||'visible-chunks-refresh',sourceMeshes:sources.length,edgeBatches:count||0});this.requestRender&&this.requestRender('normal-chunk-edges-visible-refresh-async');return count||0;}scheduleNormalChunkEdgesForVisibleChunks(reason){if(!this.normalChunkVisible||!this.normalChunkMeshes||!this.normalChunkMeshes.length)return;this._pendingNormalChunkVisibleEdgeRefreshReason=reason||this._pendingNormalChunkVisibleEdgeRefreshReason||'scheduled';if(this._pendingNormalChunkVisibleEdgeRefresh)return;this._pendingNormalChunkVisibleEdgeRefresh=true;const token=(this.normalChunkEdgeRefreshSequence||0)+1;this.normalChunkEdgeRefreshSequence=token;const run=()=>{if(this.normalChunkEdgeRefreshSequence!==token)return;this._pendingNormalChunkVisibleEdgeRefresh=false;const currentReason=this._pendingNormalChunkVisibleEdgeRefreshReason||'scheduled';this._pendingNormalChunkVisibleEdgeRefreshReason=null;this.refreshNormalChunkEdgesForVisibleChunks&&this.refreshNormalChunkEdgesForVisibleChunks(currentReason);};if(typeof requestAnimationFrame==='function')requestAnimationFrame(run);else setTimeout(run,0);}scheduleNormalChunkEdgesForVisibleChunksAsync(reason){if(!this.normalChunkVisible||!this.normalChunkMeshes||!this.normalChunkMeshes.length)return false;const token=(this.normalChunkEdgeAsyncRefreshSequence||0)+1;this.normalChunkEdgeAsyncRefreshSequence=token;if(this.normalChunkEdgeAsyncRefreshTimer){clearTimeout(this.normalChunkEdgeAsyncRefreshTimer);this.normalChunkEdgeAsyncRefreshTimer=null;}this.normalChunkEdgeAsyncRefreshTimer=setTimeout(async()=>{this.normalChunkEdgeAsyncRefreshTimer=null;if(this.normalChunkEdgeAsyncRefreshSequence!==token)return;if(!this.normalChunkVisible||!this.normalChunkMeshes||!this.normalChunkMeshes.length)return;try{if(this.refreshNormalChunkEdgesForVisibleChunksAsync){await this.refreshNormalChunkEdgesForVisibleChunksAsync(reason||'visibility-change',{batchSize:240,yieldBetweenBatches:true});}else if(this.refreshNormalChunkEdgesForVisibleChunks){this.refreshNormalChunkEdgesForVisibleChunks(reason||'visibility-change');}}catch(error){if(typeof debugError==='function')debugError('viewer:normalChunkEdges:scheduledAsync:error',error,{reason:reason||''});}},180);return true;}clearNormalChunks(dispose){if(this.restoreSelectionMaskedNormalChunks)this.restoreSelectionMaskedNormalChunks();if(this.restoreSelectionPeeledNormalChunks)this.restoreSelectionPeeledNormalChunks();const shouldDispose=dispose===true;this.clearNormalChunkEdges(shouldDispose);let removed=0;if(this.normalChunkMeshes&&this.normalChunkMeshes.length){for(const chunk of this.normalChunkMeshes){if(!chunk)continue;if(this.releaseAdaptiveMediumDepthProxy){this.releaseAdaptiveMediumDepthProxy(chunk,shouldDispose);}if(chunk.parent)chunk.parent.remove(chunk);if(shouldDispose&&chunk.geometry&&chunk.geometry.dispose)chunk.geometry.dispose();removed++;}}if(shouldDispose){this.normalChunkMeshes=[];this.normalChunkCacheSignature=null;this.normalChunksByElementKey=new Map();if(this.detachedNormalChunkCells)this.detachedNormalChunkCells.clear();if(this.normalChunkCellRefreshKeys)this.normalChunkCellRefreshKeys.clear();this.normalChunkCellRefreshGeneration=(this.normalChunkCellRefreshGeneration||0)+1;if(this.normalChunkCellRefreshTimer)clearTimeout(this.normalChunkCellRefreshTimer);this.normalChunkCellRefreshTimer=null;if(this.normalChunkCellRefreshIdleHandle&&typeof cancelIdleCallback==='function'){cancelIdleCallback(this.normalChunkCellRefreshIdleHandle);}this.normalChunkCellRefreshIdleHandle=null;}this.normalChunkVisible=false;for(const key of this.normalChunkHiddenKeys||[]){const meshes=this.getMeshesForElementKey?this.getMeshesForElementKey(key):[];for(const mesh of meshes){if(!mesh||!mesh.userData)continue;mesh.userData.normalChunkSuppressed=false;const sourceVisible=!mesh.userData.selectionOverlaySuppressed&&!(mesh.userData.massTransparencyChunkSuppressed||mesh.userData.visualStateChunkSuppressed)&&mesh.userData.baseVisible!==false;mesh.visible=sourceVisible;for(const child of mesh.children||[]){if(!child||!child.isLineSegments||!(child.userData&&child.userData.isGeneratedEdge))continue;const groupedSuppressed=!!(child.userData&&child.userData.groupedTransparentSuppressed);child.visible=sourceVisible&&!groupedSuppressed;}}}if(this.normalChunkHiddenKeys)this.normalChunkHiddenKeys.clear();return removed;}suspendNormalChunkSurfacesPreserveEdges(reason){if(this.restoreSelectionMaskedNormalChunks)this.restoreSelectionMaskedNormalChunks();if(this.restoreSelectionPeeledNormalChunks)this.restoreSelectionPeeledNormalChunks();let removed=0;for(const chunk of this.normalChunkMeshes||[]){if(!chunk)continue;if(this.releaseAdaptiveMediumDepthProxy)this.releaseAdaptiveMediumDepthProxy(chunk,false);if(chunk.parent)chunk.parent.remove(chunk);removed++;}this.normalChunkVisible=false;for(const key of this.normalChunkHiddenKeys||[]){const meshes=this.getMeshesForElementKey?this.getMeshesForElementKey(key):[];for(const mesh of meshes){if(!mesh||!mesh.userData)continue;mesh.userData.normalChunkSuppressed=false;const sourceVisible=!mesh.userData.selectionOverlaySuppressed&&!(mesh.userData.massTransparencyChunkSuppressed||mesh.userData.visualStateChunkSuppressed)&&mesh.userData.baseVisible!==false;mesh.visible=sourceVisible;}}if(this.normalChunkHiddenKeys)this.normalChunkHiddenKeys.clear();for(const batch of this.normalChunkEdgeBatches||[]){if(!batch)continue;if(!batch.userData)batch.userData={};if(!batch.userData.stableBaseMaterial)batch.userData.stableBaseMaterial=batch.material;if(!batch.parent&&this.scene)this.scene.add(batch);}if(this.recordPerformanceOperation)this.recordPerformanceOperation('normalChunks:suspend-surfaces-keep-edges',0,{reason:reason||'',removedChunks:removed,edgeBatches:this.normalChunkEdgeBatches?this.normalChunkEdgeBatches.length:0});return removed;}async suspendNormalChunkSurfacesPreserveEdgesBatched(reason,options){const opts=options&&typeof options==='object'?options:{};const isCancelled=typeof opts.isCancelled==='function'?opts.isCancelled:()=>false;const report=typeof opts.onProgress==='function'?opts.onProgress:()=>{};const batchSize=Math.max(300,Math.min(6000,Number(opts.batchSize)||2200));if(this.restoreSelectionMaskedNormalChunks)this.restoreSelectionMaskedNormalChunks();if(this.restoreSelectionPeeledNormalChunks)this.restoreSelectionPeeledNormalChunks();let removed=0;for(const chunk of this.normalChunkMeshes||[]){if(!chunk)continue;if(this.releaseAdaptiveMediumDepthProxy)this.releaseAdaptiveMediumDepthProxy(chunk,false);if(chunk.parent)chunk.parent.remove(chunk);removed++;}this.normalChunkVisible=false;const hiddenKeys=Array.from(this.normalChunkHiddenKeys||[]);for(let start=0;start<hiddenKeys.length;start+=batchSize){if(isCancelled())return false;const end=Math.min(hiddenKeys.length,start+batchSize);for(let index=start;index<end;index++){const key=hiddenKeys[index];const meshes=this.getMeshesForElementKey?this.getMeshesForElementKey(key):[];for(const mesh of meshes){if(!mesh||!mesh.userData)continue;mesh.userData.normalChunkSuppressed=false;const sourceVisible=!mesh.userData.selectionOverlaySuppressed&&!(mesh.userData.massTransparencyChunkSuppressed||mesh.userData.visualStateChunkSuppressed)&&mesh.userData.baseVisible!==false;mesh.visible=sourceVisible;}}report(end,hiddenKeys.length,'Preparando superficies para la visualización...');if(end<hiddenKeys.length&&opts.yieldBetweenBatches!==false){if(this.waitForResetProgressPaint)await this.waitForResetProgressPaint();else if(this.yieldDuringLoad)await this.yieldDuringLoad();else await new Promise(resolve=>setTimeout(resolve,0));}}if(this.normalChunkHiddenKeys)this.normalChunkHiddenKeys.clear();for(const batch of this.normalChunkEdgeBatches||[]){if(!batch)continue;if(!batch.userData)batch.userData={};if(!batch.userData.stableBaseMaterial)batch.userData.stableBaseMaterial=batch.material;if(!batch.parent&&this.scene)this.scene.add(batch);}if(this.recordPerformanceOperation)this.recordPerformanceOperation('normalChunks:suspend-surfaces-keep-edges-batched',0,{reason:reason||'',removedChunks:removed,sourceKeys:hiddenKeys.length});return removed;}async reactivateNormalChunkCacheBatched(options){const opts=options&&typeof options==='object'?options:{};if(!this.normalChunkMeshes||!this.normalChunkMeshes.length)return 0;const report=typeof opts.onProgress==='function'?opts.onProgress:()=>{};const isCancelled=typeof opts.isCancelled==='function'?opts.isCancelled:()=>false;const meshes=this.meshes||[];const batchSize=Math.max(500,Math.min(8000,Number(opts.batchSize)||2800));if(this.restoreSelectionMaskedNormalChunks)this.restoreSelectionMaskedNormalChunks();if(this.restoreSelectionPeeledNormalChunks)this.restoreSelectionPeeledNormalChunks();for(const chunk of this.normalChunkMeshes||[]){if(!chunk)continue;if(this.releaseAdaptiveMediumDepthProxy)this.releaseAdaptiveMediumDepthProxy(chunk,false);if(chunk.parent)chunk.parent.remove(chunk);}if(!this.normalChunkHiddenKeys)this.normalChunkHiddenKeys=new Set();else this.normalChunkHiddenKeys.clear();for(let start=0;start<meshes.length;start+=batchSize){if(isCancelled())return false;const end=Math.min(meshes.length,start+batchSize);for(let index=start;index<end;index++){const mesh=meshes[index];if(!mesh||!mesh.isMesh||!mesh.userData)continue;const key=this.getMeshKey?this.getMeshKey(mesh):mesh.userData.elementKey;const mappedChunks=key&&this.normalChunksByElementKey?this.normalChunksByElementKey.get(key):null;const chunkable=!!(key&&mesh.userData.baseVisible!==false&&!(this.isNormalChunkInteractiveKey&&this.isNormalChunkInteractiveKey(key))&&mappedChunks&&mappedChunks.size&&(!this.isNormalChunkRenderableMesh||this.isNormalChunkRenderableMesh(mesh)));if(chunkable){mesh.userData.normalChunkSuppressed=true;mesh.visible=false;if(this.removeTransientCompactFallbackEdge)this.removeTransientCompactFallbackEdge(mesh,true);this.normalChunkHiddenKeys.add(key);}else{mesh.userData.normalChunkSuppressed=false;if(!(mesh.userData.massTransparencyChunkSuppressed||mesh.userData.visualStateChunkSuppressed)){this.updateVisualAppearanceForMesh&&this.updateVisualAppearanceForMesh(mesh);}}}report(end,meshes.length,opts.progressMessage||'Recuperando bloques optimizados del modelo...');if(end<meshes.length&&opts.yieldBetweenBatches!==false){if(this.waitForResetProgressPaint)await this.waitForResetProgressPaint();else if(this.yieldDuringLoad)await this.yieldDuringLoad();else await new Promise(resolve=>setTimeout(resolve,0));}}if(isCancelled())return false;for(const chunk of this.normalChunkMeshes||[]){if(!chunk)continue;chunk.visible=true;if(!chunk.parent&&this.scene)this.scene.add(chunk);}for(const edgeBatch of this.normalChunkEdgeBatches||[]){if(!edgeBatch)continue;edgeBatch.visible=true;if(!edgeBatch.parent&&this.scene)this.scene.add(edgeBatch);}this.normalChunkVisible=true;this.normalChunkEdgesVisibleOnly=false;if(this.updateAdaptiveMediumChunkVisibility)this.updateAdaptiveMediumChunkVisibility();if(this.syncInteractiveNormalChunkPeel)this.syncInteractiveNormalChunkPeel();this.normalChunkReuseCount=(this.normalChunkReuseCount||0)+1;return this.normalChunkMeshes.length;}async restoreNormalChunksResponsive(options){const opts=options&&typeof options==='object'?options:{};const report=typeof opts.onProgress==='function'?opts.onProgress:()=>{};const isCancelled=typeof opts.isCancelled==='function'?opts.isCancelled:()=>false;if(isCancelled())return false;if(!this.shouldUseNormalChunks()){return 0;}const signature=this.getNormalChunkSignatureBatched?await this.getNormalChunkSignatureBatched({batchSize:opts.signatureBatchSize||3200,yieldBetweenBatches:true,isCancelled,onProgress:(current,total,message)=>report(current,total,message)}):this.getNormalChunkSignature();if(signature===false||isCancelled())return false;const sameSignature=!opts.forceRebuild&&signature&&signature===this.normalChunkCacheSignature&&this.normalChunkMeshes&&this.normalChunkMeshes.length;if(sameSignature&&this.reactivateNormalChunkCacheBatched){const reused=await this.reactivateNormalChunkCacheBatched({batchSize:opts.batchSize||2800,yieldBetweenBatches:true,isCancelled,progressMessage:opts.progressMessage||'Recuperando bloques optimizados del modelo...',onProgress:(current,total,message)=>report(current,total,message)});if(reused!==false&&this.recordPerformanceOperation){this.recordPerformanceOperation('normalChunks:responsive-reuse',0,{chunks:Number(reused)||0,meshes:this.meshes?this.meshes.length:0});}return reused;}if(isCancelled())return false;if(this.rebuildNormalChunksForLoad){return await this.rebuildNormalChunksForLoad(true,{precomputedSignature:signature,onProgress:(current,total,message)=>report(current,total,message),edgeBatchSize:opts.edgeBatchSize||180,isCancelled});}return this.rebuildNormalChunks?this.rebuildNormalChunks(true):0;}restoreStableNormalChunkEdgeAppearance(){let changed=0;for(const batch of this.normalChunkEdgeBatches||[]){if(!batch)continue;const baseMaterial=batch.userData&&batch.userData.stableBaseMaterial;if(baseMaterial&&batch.material!==baseMaterial){batch.material=baseMaterial;changed++;}}this.stableNormalEdgeAppearanceMode='normal';return changed;}applyStableNormalChunkEdgeAppearance(mode){if(!this.normalChunkEdgeBatches||!this.normalChunkEdgeBatches.length)return 0;const resolvedMode=mode==='mass'?'mass':(mode==='color'?'color':'normal');if(resolvedMode==='normal')return this.restoreStableNormalChunkEdgeAppearance?this.restoreStableNormalChunkEdgeAppearance():0;let targetMaterial=null;if(resolvedMode==='mass'){const mass=this.visualMassState||{};const color=mass.fadedColor==null?this.getConfiguredTransparencyColor():Number(mass.fadedColor);const opacity=Math.max(0.04,Number(mass.fadedOpacity==null?this.getConfiguredTransparencyOpacity(0.02):mass.fadedOpacity));targetMaterial=this.getDarkerEdgeMaterialForColor(color,opacity,true);}else{const descriptor=this.getUniformVisualStateEdgeDescriptor?this.getUniformVisualStateEdgeDescriptor():null;if(!descriptor){return this.restoreStableNormalChunkEdgeAppearance?this.restoreStableNormalChunkEdgeAppearance():0;}targetMaterial=this.getSimpleEdgeMaterial?this.getSimpleEdgeMaterial(descriptor.edgeColor,descriptor.edgeOpacity,descriptor.transparent):this.edgeMaterial;}if(!targetMaterial)return 0;let changed=0;for(const batch of this.normalChunkEdgeBatches||[]){if(!batch)continue;if(!batch.userData)batch.userData={};if(!batch.userData.stableBaseMaterial)batch.userData.stableBaseMaterial=batch.material;if(batch.material!==targetMaterial){batch.material=targetMaterial;changed++;}if(!batch.parent&&this.scene)this.scene.add(batch);}this.stableNormalEdgeAppearanceMode=resolvedMode;return changed;}getUniformVisualStateEdgeDescriptor(){if(!(this.visualColorOverrides&&this.visualColorOverrides.size))return null;let result=null;const visitedKeys=new Set();for(const mesh of this.meshes||[]){if(!mesh||!mesh.isMesh||!mesh.userData||mesh.userData.baseVisible===false)continue;if(this.isNonRenderedDataMesh&&this.isNonRenderedDataMesh(mesh))continue;const key=this.getMeshKey?this.getMeshKey(mesh):mesh.userData.elementKey;if(!key||visitedKeys.has(key))continue;visitedKeys.add(key);const state=this.visualColorOverrides.get(key);if(!state)return null;const color=Number(state.color==null?0xbec7d3:state.color);const opacity=Math.max(0.02,Math.min(1,Number(state.opacity==null?1:state.opacity)));const edgeColor=state.edgeColor==null?this.darkenColorValue(color,0.3):Number(state.edgeColor);const edgeOpacity=Math.max(0.02,Math.min(1,Number(state.edgeOpacity==null?(opacity<1?Math.max(0.04,opacity):1):state.edgeOpacity)));const descriptor={color,opacity,edgeColor,edgeOpacity,transparent:opacity<1||edgeOpacity<1};if(!result){result=descriptor;continue;}if(result.color!==descriptor.color||Math.abs(result.opacity-descriptor.opacity)>0.0001||result.edgeColor!==descriptor.edgeColor||Math.abs(result.edgeOpacity-descriptor.edgeOpacity)>0.0001)return null;}return visitedKeys.size?result:null;}shouldReuseStableNormalEdgesForVisualState(){return!!(this.normalChunkEdgeBatches&&this.normalChunkEdgeBatches.length&&this.visualColorOverrides&&this.visualColorOverrides.size&&!(this.visualWorksStates&&this.visualWorksStates.size)&&!(this.visualClashStates&&this.visualClashStates.size)&&!this.dataPanelFilterActive&&!this.baseIsolationActive&&!this.isolateActive&&this.getUniformVisualStateEdgeDescriptor&&this.getUniformVisualStateEdgeDescriptor());}shouldReuseStableNormalEdgesForMassTransparency(){return!!(this.normalChunkEdgeBatches&&this.normalChunkEdgeBatches.length&&this.visualMassState&&this.visualMassState.active&&!this.dataPanelFilterActive&&!this.baseIsolationActive&&!this.isolateActive);}shouldUseSelectionOverlayBatches(keys,meshes){if(this.visualSelectionOverlayOnly===true)return true;const keySet=keys||this.selectedKeys||[];const meshList=meshes||this.selectedMeshes||[];const keyCount=typeof keySet.size==='number'?keySet.size:Array.from(keySet||[]).length;const meshCount=typeof meshList.length==='number'?meshList.length:Array.from(meshList||[]).length;return keyCount>=Math.max(2,Number(this.selectionOverlayKeyThreshold)||24)||meshCount>=Math.max(2,Number(this.selectionOverlayMeshThreshold)||48);}selectionUsesSourceAppearance(){return!(this.shouldUseSelectionOverlayBatches&&this.shouldUseSelectionOverlayBatches());}getCurrentInteractiveElementKeys(){const keys=new Set();const selectionUsesSourceAppearance=this.selectionUsesSourceAppearance?this.selectionUsesSourceAppearance():this.visualSelectionOverlayOnly!==true;if(selectionUsesSourceAppearance&&this.selectedKeys&&this.selectedKeys.size){for(const key of this.selectedKeys)if(key)keys.add(key);}if(this.hoverVisualMutatesGeometry===true&&this.visualHoverKey)keys.add(this.visualHoverKey);const rectangleUsesSourceAppearance=this.visualInteractionOverlayOnly!==true;if(rectangleUsesSourceAppearance&&this.rectanglePreviewKeys&&this.rectanglePreviewKeys.size){for(const key of this.rectanglePreviewKeys)if(key)keys.add(key);}return keys;}getNormalChunkSourceKeySet(chunk){if(!chunk||!chunk.userData)return new Set();const dataKeys=chunk.userData.normalChunkSourceKeys;if(dataKeys&&typeof dataKeys.forEach==='function')return new Set(dataKeys);const keys=new Set();const sourceMeshes=Array.isArray(chunk.userData.normalChunkSourceMeshes)?chunk.userData.normalChunkSourceMeshes:[];for(const mesh of sourceMeshes){const key=this.getMeshKey?this.getMeshKey(mesh):(mesh&&mesh.userData&&mesh.userData.elementKey);if(key)keys.add(key);}return keys;}normalChunkContainsAnyKey(chunk,keySet){if(!chunk||!keySet||!keySet.size)return false;const sourceKeys=this.getNormalChunkSourceKeySet?this.getNormalChunkSourceKeySet(chunk):new Set();for(const key of keySet)if(sourceKeys.has(key))return true;return false;}getNormalChunkBlockedKeys(){const keys=this.getCurrentInteractiveElementKeys?this.getCurrentInteractiveElementKeys():new Set();const blocked=new Set(keys||[]);if(this.hiddenKeys&&this.hiddenKeys.size){for(const key of this.hiddenKeys)if(key)blocked.add(key);}if(this.normalChunkHiddenKeys&&this.normalChunkHiddenKeys.size){for(const key of this.normalChunkHiddenKeys){if(!key)continue;const sourceMeshes=this.getMeshesForElementKey?this.getMeshesForElementKey(key):[];if(sourceMeshes.some(sourceMesh=>sourceMesh&&sourceMesh.userData&&sourceMesh.userData.baseVisible===false))blocked.add(key);}}if(this.isolateActive&&this.isolatedKeys&&this.isolatedKeys.size&&this.normalChunkHiddenKeys&&this.normalChunkHiddenKeys.size){for(const key of this.normalChunkHiddenKeys){if(key&&!this.isolatedKeys.has(key))blocked.add(key);}}return blocked;}restoreSelectionPeeledNormalChunks(){const peeledChunks=this.selectionPeeledNormalChunks?Array.from(this.selectionPeeledNormalChunks):[];const restoredKeys=this.selectionChunkRestoredSources?Array.from(this.selectionChunkRestoredSources):[];const blockedKeys=this.getNormalChunkBlockedKeys?this.getNormalChunkBlockedKeys():new Set();const retainedChunks=new Set();const retainedSourceKeys=new Set();let restoredChunkBecameVisible=false;if(this.selectionPeeledNormalChunks)this.selectionPeeledNormalChunks.clear();if(this.selectionChunkRestoredSources)this.selectionChunkRestoredSources.clear();for(const chunk of peeledChunks){if(!chunk)continue;const mustStayPeeled=this.normalChunkContainsAnyKey&&this.normalChunkContainsAnyKey(chunk,blockedKeys);const wasVisible=chunk.visible!==false;chunk.visible=!mustStayPeeled;if(!wasVisible&&chunk.visible)restoredChunkBecameVisible=true;if(mustStayPeeled){retainedChunks.add(chunk);const sourceMeshes=chunk.userData&&Array.isArray(chunk.userData.normalChunkSourceMeshes)?chunk.userData.normalChunkSourceMeshes:[];for(const sourceMesh of sourceMeshes){const sourceKey=this.getMeshKey?this.getMeshKey(sourceMesh):(sourceMesh&&sourceMesh.userData&&sourceMesh.userData.elementKey);if(sourceKey)retainedSourceKeys.add(sourceKey);}}}for(const key of restoredKeys){const meshes=this.getMeshesForElementKey?this.getMeshesForElementKey(key):[];const mustStaySourceVisible=retainedSourceKeys.has(key);for(const mesh of meshes){if(!mesh||!mesh.userData)continue;const canReturnToChunk=!mustStaySourceVisible&&this.normalChunkVisible&&this.shouldUseNormalChunks&&this.shouldUseNormalChunks()&&mesh.isMesh&&mesh.userData.baseVisible!==false&&!this.isNormalChunkInteractiveKey(key)&&!(this.hiddenKeys&&this.hiddenKeys.has(key))&&!!(this.isNormalChunkRenderableMesh&&this.isNormalChunkRenderableMesh(mesh));if(canReturnToChunk){mesh.userData.normalChunkSuppressed=true;mesh.visible=false;}else{mesh.userData.normalChunkSuppressed=false;if(!(mesh.userData.massTransparencyChunkSuppressed||mesh.userData.visualStateChunkSuppressed))this.updateVisualAppearanceForMesh(mesh);}}}if(this.selectionPeeledNormalChunks){for(const chunk of retainedChunks)this.selectionPeeledNormalChunks.add(chunk);}if(this.selectionChunkRestoredSources){for(const key of retainedSourceKeys)this.selectionChunkRestoredSources.add(key);}if(restoredChunkBecameVisible&&this.normalChunkEdgesVisibleOnly&&this.scheduleNormalChunkEdgesForVisibleChunks){this.scheduleNormalChunkEdgesForVisibleChunks('peeled-chunk-restored');}}restoreSelectionMaskedNormalChunks(options){const opts=options&&typeof options==='object'?options:{};const keepSelected=new Set(opts.keepSelectedKeys||[]);const maskedChunks=this.selectionMaskedNormalChunks||new Map();const sourceKeys=this.selectionMaskedNormalChunkSourceKeys||new Set();for(const[chunk,state]of maskedChunks.entries()){if(!chunk||!state)continue;const geometry=chunk.geometry;if(geometry&&state.originalIndexAttribute&&geometry.index!==state.originalIndexAttribute)geometry.setIndex(state.originalIndexAttribute);if(state.originalVisible!=null)chunk.visible=state.originalVisible;if(chunk.userData)chunk.userData.selectionIndexMasked=false;}maskedChunks.clear();for(const key of sourceKeys){if(!key||keepSelected.has(key))continue;const chunks=this.normalChunksByElementKey&&this.normalChunksByElementKey.get(key);const hasLiveChunk=!!(chunks&&Array.from(chunks).some(chunk=>chunk&&chunk.parent));const meshes=this.getMeshesForElementKey?this.getMeshesForElementKey(key):[];for(const mesh of meshes){if(!mesh||!mesh.userData)continue;const canReturnToChunk=hasLiveChunk&&mesh.isMesh&&mesh.userData.baseVisible!==false&&!(this.hiddenKeys&&this.hiddenKeys.has(key))&&!(mesh.userData.massTransparencyChunkSuppressed||mesh.userData.visualStateChunkSuppressed);if(canReturnToChunk){mesh.userData.normalChunkSuppressed=true;mesh.visible=false;}else if(!(mesh.userData.massTransparencyChunkSuppressed||mesh.userData.visualStateChunkSuppressed)){mesh.userData.normalChunkSuppressed=false;if(this.updateVisualAppearanceForMesh)this.updateVisualAppearanceForMesh(mesh);}}}sourceKeys.clear();}applySelectionMaskToNormalChunk(chunk,selectedKeys){if(!chunk||!chunk.geometry||!chunk.geometry.index||!selectedKeys||!selectedKeys.size)return false;const rangesByKey=chunk.userData&&chunk.userData.normalChunkIndexRangesByKey;if(!rangesByKey||typeof rangesByKey.get!=='function')return false;const originalIndexAttribute=chunk.geometry.index;const originalArray=originalIndexAttribute.array;if(!originalArray||!originalArray.length)return false;const ranges=[];for(const key of selectedKeys){const keyRanges=rangesByKey.get(key);if(!Array.isArray(keyRanges))continue;for(const pair of keyRanges){const start=Math.max(0,Number(pair&&pair[0])||0);const count=Math.max(0,Number(pair&&pair[1])||0);if(count>0&&start<originalArray.length)ranges.push([start,Math.min(originalArray.length,start+count)]);}}if(!ranges.length)return false;ranges.sort((a,b)=>a[0]-b[0]);const merged=[];for(const range of ranges){if(!merged.length||range[0]>merged[merged.length-1][1])merged.push(range.slice());else merged[merged.length-1][1]=Math.max(merged[merged.length-1][1],range[1]);}let excluded=0;for(const range of merged)excluded+=Math.max(0,range[1]-range[0]);if(!excluded)return false;const nextLength=Math.max(0,originalArray.length-excluded);const ArrayType=originalArray.constructor||Uint32Array;const filtered=new ArrayType(nextLength);let sourceCursor=0;let targetCursor=0;for(const range of merged){if(range[0]>sourceCursor){const part=originalArray.subarray(sourceCursor,range[0]);filtered.set(part,targetCursor);targetCursor+=part.length;}sourceCursor=Math.max(sourceCursor,range[1]);}if(sourceCursor<originalArray.length){const part=originalArray.subarray(sourceCursor);filtered.set(part,targetCursor);}if(!this.selectionMaskedNormalChunks)this.selectionMaskedNormalChunks=new Map();this.selectionMaskedNormalChunks.set(chunk,{originalIndexAttribute,originalVisible:chunk.visible});if(chunk.userData)chunk.userData.selectionIndexMasked=true;if(nextLength>0)chunk.geometry.setIndex(new THREE.BufferAttribute(filtered,1));else chunk.visible=false;return true;}syncSelectionNormalChunkMasks(){if(this.cancelScheduledNormalChunkEdgesForVisibleChunks)this.cancelScheduledNormalChunkEdgesForVisibleChunks();const selectionUsesSource=this.selectionUsesSourceAppearance?this.selectionUsesSourceAppearance():this.visualSelectionOverlayOnly!==true;const selectedKeys=this.selectedKeys?new Set(this.selectedKeys):new Set();this.restoreSelectionMaskedNormalChunks&&this.restoreSelectionMaskedNormalChunks({keepSelectedKeys:selectedKeys});if(!selectedKeys.size||!this.normalChunkVisible||!this.normalChunksByElementKey)return new Set();const chunksToMask=new Set();const fallbackKeys=new Set();for(const key of selectedKeys){const chunks=this.normalChunksByElementKey.get(key);if(!chunks||!chunks.size)continue;let hasRangeMetadata=false;for(const chunk of chunks){if(!chunk||!chunk.parent)continue;const rangesByKey=chunk.userData&&chunk.userData.normalChunkIndexRangesByKey;if(rangesByKey&&typeof rangesByKey.get==='function'&&rangesByKey.get(key)){hasRangeMetadata=true;chunksToMask.add(chunk);}}if(!hasRangeMetadata)fallbackKeys.add(key);}const successfullyMaskedKeys=new Set();for(const chunk of chunksToMask){const chunkKeys=this.getNormalChunkSourceKeySet?this.getNormalChunkSourceKeySet(chunk):new Set();const selectedInChunk=new Set();for(const key of selectedKeys)if(chunkKeys.has(key))selectedInChunk.add(key);if(!selectedInChunk.size)continue;if(this.applySelectionMaskToNormalChunk&&this.applySelectionMaskToNormalChunk(chunk,selectedInChunk)){for(const key of selectedInChunk)successfullyMaskedKeys.add(key);}else{for(const key of selectedInChunk)fallbackKeys.add(key);}}if(!this.selectionMaskedNormalChunkSourceKeys)this.selectionMaskedNormalChunkSourceKeys=new Set();for(const key of successfullyMaskedKeys){if(fallbackKeys.has(key))continue;this.selectionMaskedNormalChunkSourceKeys.add(key);if(!selectionUsesSource)continue;const meshes=this.getMeshesForElementKey?this.getMeshesForElementKey(key):[];for(const mesh of meshes){if(!mesh||!mesh.userData||mesh.userData.baseVisible===false)continue;mesh.userData.normalChunkSuppressed=false;if(!(mesh.userData.massTransparencyChunkSuppressed||mesh.userData.visualStateChunkSuppressed)){if(this.updateVisualAppearanceForMesh)this.updateVisualAppearanceForMesh(mesh);}}}if(this.normalChunkVisible&&(!this.normalChunkEdgeBatches||!this.normalChunkEdgeBatches.length)&&this.scheduleNormalChunkEdgesForVisibleChunksAsync){this.scheduleNormalChunkEdgesForVisibleChunksAsync('selection-idle-edge-sync');}return selectionUsesSource?fallbackKeys:new Set();}syncInteractiveNormalChunkPeel(){if(this.clashModeActive===true){if(this.recordPerformanceOperation)this.recordPerformanceOperation('normalChunks:peel-skip-clash-mode',0,{});return;}if(!this.selectionPeeledNormalChunks)this.selectionPeeledNormalChunks=new Set();if(!this.selectionChunkRestoredSources)this.selectionChunkRestoredSources=new Set();this.restoreSelectionPeeledNormalChunks();if(!this.normalChunkVisible||!this.normalChunksByElementKey){this.restoreSelectionMaskedNormalChunks&&this.restoreSelectionMaskedNormalChunks();return;}const selectionFallbackKeys=this.syncSelectionNormalChunkMasks?this.syncSelectionNormalChunkMasks():new Set(this.selectedKeys||[]);const blockedKeys=this.getNormalChunkBlockedKeys?this.getNormalChunkBlockedKeys():new Set();if(this.selectedKeys&&this.selectedKeys.size){for(const key of this.selectedKeys){if(!selectionFallbackKeys||!selectionFallbackKeys.has(key))blockedKeys.delete(key);}}if(!blockedKeys.size)return;const chunksToPeel=new Set();const pendingChunks=[];const expandedSourceKeys=new Set();const addChunksForKey=key=>{const chunks=this.normalChunksByElementKey.get(key);if(!chunks)return;for(const chunk of chunks){if(!chunk||chunksToPeel.has(chunk))continue;chunksToPeel.add(chunk);pendingChunks.push(chunk);}};for(const key of blockedKeys)addChunksForKey(key);while(pendingChunks.length){const chunk=pendingChunks.pop();const sourceMeshes=chunk&&chunk.userData&&Array.isArray(chunk.userData.normalChunkSourceMeshes)?chunk.userData.normalChunkSourceMeshes:[];for(const sourceMesh of sourceMeshes){const sourceKey=this.getMeshKey?this.getMeshKey(sourceMesh):(sourceMesh&&sourceMesh.userData&&sourceMesh.userData.elementKey);if(!sourceKey||expandedSourceKeys.has(sourceKey))continue;expandedSourceKeys.add(sourceKey);addChunksForKey(sourceKey);}}for(const chunk of chunksToPeel){if(!chunk)continue;chunk.visible=false;this.selectionPeeledNormalChunks.add(chunk);const sourceMeshes=chunk.userData&&Array.isArray(chunk.userData.normalChunkSourceMeshes)?chunk.userData.normalChunkSourceMeshes:[];for(const sourceMesh of sourceMeshes){const sourceKey=this.getMeshKey?this.getMeshKey(sourceMesh):(sourceMesh&&sourceMesh.userData&&sourceMesh.userData.elementKey);if(!sourceKey||!sourceMesh||!sourceMesh.userData)continue;this.selectionChunkRestoredSources.add(sourceKey);sourceMesh.userData.normalChunkSuppressed=false;if(!(sourceMesh.userData.massTransparencyChunkSuppressed||sourceMesh.userData.visualStateChunkSuppressed))this.updateVisualAppearanceForMesh(sourceMesh);}}}invalidateNormalChunks(reason){this.normalChunkRevision=(this.normalChunkRevision||0)+1;this.normalChunkCacheSignature=null;this.clearNormalChunks(true);this.clearVisualStateChunks&&this.clearVisualStateChunks(true);if(this.recordPerformanceOperation)this.recordPerformanceOperation('normalChunks:invalidate',0,{reason:reason||'unknown',revision:this.normalChunkRevision});}isNormalChunkInteractiveKey(key){if(!key)return false;if(this.clashModeActive===true){return!!(this.clashPriorityKeys&&this.clashPriorityKeys.has(key));}const selectionUsesSource=this.selectionUsesSourceAppearance?this.selectionUsesSourceAppearance():this.visualSelectionOverlayOnly!==true;if(selectionUsesSource&&this.selectedKeys&&this.selectedKeys.has(key))return true;if(this.hoverVisualMutatesGeometry===true&&this.visualHoverKey&&this.visualHoverKey===key)return true;if(this.visualInteractionOverlayOnly!==true&&this.rectanglePreviewKeys&&this.rectanglePreviewKeys.has(key))return true;return false;}hasNormalChunkBlockingVisualState(){if(this.visualMassState&&this.visualMassState.active)return true;if(this.dataPanelFilterActive)return true;if(this.visualColorOverrides&&this.visualColorOverrides.size)return true;if(this.visualWorksStates&&this.visualWorksStates.size)return true;if(this.visualClashStates&&this.visualClashStates.size)return true;if(this.visualFadeStates&&this.visualFadeStates.size)return true;if(this.isolateActive)return true;return false;}shouldUseNormalChunks(){return this.dataSpatialGroupingActive!==true&&this.normalChunkEnabled!==false&&this.meshes&&this.meshes.length>1500&&!this.hasNormalChunkBlockingVisualState();}getNormalChunkSignature(){if(!this.shouldUseNormalChunks())return null;let baseVisibleCount=0;let hashA=2166136261>>>0;let hashB=5381>>>0;for(const mesh of this.meshes||[]){if(!mesh||!mesh.isMesh||!mesh.userData||mesh.userData.baseVisible===false)continue;baseVisibleCount++;const key=String(this.getMeshKey?(this.getMeshKey(mesh)||''):(mesh.userData.elementKey||''));for(let i=0;i<key.length;i++){const code=key.charCodeAt(i);hashA^=code;hashA=Math.imul(hashA,16777619)>>>0;hashB=(Math.imul(hashB,33)^code)>>>0;}hashA^=124;hashA=Math.imul(hashA,16777619)>>>0;hashB=(Math.imul(hashB,33)^124)>>>0;}return['normal-v2','rev',this.normalChunkRevision||0,'detail',this.getVisualDetailLevel?this.getVisualDetailLevel():'default','meshCount',this.meshes?this.meshes.length:0,'baseVisible',baseVisibleCount,'baseHash',baseVisibleCount+':'+hashA.toString(16)+':'+hashB.toString(16)].join('|');}async getNormalChunkSignatureBatched(options){if(!this.shouldUseNormalChunks())return null;const opts=options&&typeof options==='object'?options:{};const meshes=this.meshes||[];const batchSize=Math.max(500,Math.min(8000,Number(opts.batchSize)||3200));const report=typeof opts.onProgress==='function'?opts.onProgress:()=>{};const isCancelled=typeof opts.isCancelled==='function'?opts.isCancelled:()=>false;let baseVisibleCount=0;let hashA=2166136261>>>0;let hashB=5381>>>0;for(let start=0;start<meshes.length;start+=batchSize){if(isCancelled())return false;const end=Math.min(meshes.length,start+batchSize);for(let index=start;index<end;index++){const mesh=meshes[index];if(!mesh||!mesh.isMesh||!mesh.userData||mesh.userData.baseVisible===false)continue;baseVisibleCount++;const key=String(this.getMeshKey?(this.getMeshKey(mesh)||''):(mesh.userData.elementKey||''));for(let i=0;i<key.length;i++){const code=key.charCodeAt(i);hashA^=code;hashA=Math.imul(hashA,16777619)>>>0;hashB=(Math.imul(hashB,33)^code)>>>0;}hashA^=124;hashA=Math.imul(hashA,16777619)>>>0;hashB=(Math.imul(hashB,33)^124)>>>0;}report(end,meshes.length,opts.progressMessage||'Verificando bloques optimizados...');if(end<meshes.length&&opts.yieldBetweenBatches!==false){if(this.waitForResetProgressPaint)await this.waitForResetProgressPaint();else if(this.yieldDuringLoad)await this.yieldDuringLoad();else await new Promise(resolve=>setTimeout(resolve,0));}}if(isCancelled())return false;return['normal-v2','rev',this.normalChunkRevision||0,'detail',this.getVisualDetailLevel?this.getVisualDetailLevel():'default','meshCount',meshes.length,'baseVisible',baseVisibleCount,'baseHash',baseVisibleCount+':'+hashA.toString(16)+':'+hashB.toString(16)].join('|');}isNormalChunkMaterialSupported(material){if(!material||material.visible===false)return false;if(!material.isMeshBasicMaterial||material.vertexColors===true)return false;if(material.map||material.alphaMap||material.lightMap||material.aoMap||material.emissiveMap||material.bumpMap||material.normalMap||material.displacementMap||material.roughnessMap||material.metalnessMap||material.envMap)return false;return true;}getNormalChunkMaterialSegments(mesh){if(!mesh||!mesh.isMesh||mesh.isSkinnedMesh||!mesh.userData)return[];if(this.isNonRenderedDataMesh&&this.isNonRenderedDataMesh(mesh))return[];const geometry=this.getBaseGeometryForMesh?this.getBaseGeometryForMesh(mesh):mesh.geometry;if(!geometry||!geometry.isBufferGeometry)return[];const position=geometry.getAttribute&&geometry.getAttribute('position');if(!position||!position.count)return[];const baseMaterial=this.getBaseMaterialForMesh?this.getBaseMaterialForMesh(mesh):mesh.material;const materials=Array.isArray(baseMaterial)?baseMaterial:[baseMaterial];if(!materials.length)return[];const fullCount=geometry.index&&geometry.index.count?geometry.index.count:position.count;const groups=geometry.groups&&geometry.groups.length?geometry.groups:(materials.length===1?[{start:0,count:fullCount,materialIndex:0}]:[]);if(!groups.length)return[];const segments=[];for(const group of groups){const materialIndex=Math.max(0,Number(group&&group.materialIndex)||0);const material=materials[materialIndex]||materials[0];if(!(this.isNormalChunkMaterialSupported&&this.isNormalChunkMaterialSupported(material)))return[];const start=Math.max(0,Math.min(fullCount,Number(group&&group.start)||0));const requestedCount=Number(group&&group.count);const count=Math.max(0,Math.min(fullCount-start,Number.isFinite(requestedCount)?requestedCount:(fullCount-start)));if(!count)continue;segments.push({mesh,geometry,material,materialIndex,start,count});}return segments;}isNormalChunkRenderableMesh(mesh){return!!(this.getNormalChunkMaterialSegments&&this.getNormalChunkMaterialSegments(mesh).length);}getNormalChunkMaterialForMesh(mesh){const segments=this.getNormalChunkMaterialSegments?this.getNormalChunkMaterialSegments(mesh):[];return segments.length?segments[0].material:null;}getNormalChunkMaterialKey(material){if(!material)return null;if(material.userData&&material.userData.sharedMaterialKey)return material.userData.sharedMaterialKey;const color=material.color&&material.color.isColor?material.color.getHexString():'none';const opacity=Math.max(0.02,Math.min(1,Number(material.opacity==null?1:material.opacity))).toFixed(3);const side=material.side===THREE.FrontSide?'F':(material.side===THREE.BackSide?'B':'D');const transparent=material.transparent?'T':'O';const depthWrite=material.depthWrite===false?'0':'1';const depthTest=material.depthTest===false?'0':'1';const polygonOffset=material.polygonOffset?'1':'0';const factor=Number(material.polygonOffsetFactor||0).toFixed(3);const units=Number(material.polygonOffsetUnits||0).toFixed(3);return['normalmat',color,opacity,transparent,side,depthWrite,depthTest,polygonOffset,factor,units].join(':');}getAdaptiveMediumDetailClass(mesh){if(!mesh||!mesh.isMesh||this.getVisualDetailLevel()!=='medium'||!this.performanceRenderMode)return'full';const element=mesh.userData&&mesh.userData.element;const geometry=mesh.geometry;const position=geometry&&geometry.getAttribute?geometry.getAttribute('position'):null;const isDenseCategory=this.isOptimizedDenseMeshCategory(element)||this.isSpecialtyLowDetailCategory(element)||this.isStructuralRebarCategory(element);if(!isDenseCategory&&(!position||position.count<1200))return'full';return'compact';}getMeshWorldBoundingRadius(mesh){if(!mesh||!mesh.geometry)return 0;const geometry=mesh.geometry;if(!geometry.boundingSphere&&geometry.computeBoundingSphere)geometry.computeBoundingSphere();if(!geometry.boundingSphere)return 0;mesh.updateWorldMatrix(true,false);const scale=mesh.matrixWorld&&mesh.matrixWorld.getMaxScaleOnAxis?mesh.matrixWorld.getMaxScaleOnAxis():1;return Math.max(0,Number(geometry.boundingSphere.radius)||0)*Math.max(0.0001,Number(scale)||1);}getProjectedPixelRadius(center,radius){if(!this.camera||!this.canvas||!center||!(radius>0))return Number.POSITIVE_INFINITY;const height=Math.max(1,this.canvas.clientHeight||this.canvas.height||1);if(this.camera.isOrthographicCamera){const visibleHeight=Math.max(0.000001,Math.abs(this.camera.top-this.camera.bottom)/Math.max(0.000001,this.camera.zoom||1));return radius*height/visibleHeight;}const distance=Math.max(0.000001,this.camera.position.distanceTo(center));const fov=Math.max(1,Number(this.camera.fov)||50)*Math.PI/180;return radius*height/(2*distance*Math.tan(fov/2));}getDepthStableNormalChunkMaterial(material){if(!material||material.transparent===true||Number(material.opacity)<0.999)return material;if(!this.normalChunkDepthStableMaterialCache)this.normalChunkDepthStableMaterialCache=new WeakMap();let stable=this.normalChunkDepthStableMaterialCache.get(material);if(!stable){stable=material.clone();stable.polygonOffset=false;stable.polygonOffsetFactor=0;stable.polygonOffsetUnits=0;stable.depthTest=true;stable.depthWrite=true;stable.userData=Object.assign({},stable.userData||{},{normalChunkDepthStableMaterial:true,normalChunkSourceMaterialUuid:material.uuid||''});this.normalChunkDepthStableMaterialCache.set(material,stable);}return stable;}getAdaptiveMediumDepthMaterial(){if(!this.adaptiveMediumDepthMaterial){const material=new THREE.MeshBasicMaterial({color:0x000000,side:THREE.DoubleSide,transparent:false,opacity:1,depthTest:true,depthWrite:true,colorWrite:false,polygonOffset:false});material.blending=THREE.NoBlending;material.toneMapped=false;material.userData=Object.assign({},material.userData||{},{adaptiveMediumDepthOnly:true});this.adaptiveMediumDepthMaterial=material;}return this.adaptiveMediumDepthMaterial;}getAdaptiveMediumDepthBoxGeometry(){if(!this.adaptiveMediumDepthBoxGeometry){this.adaptiveMediumDepthBoxGeometry=new THREE.BoxGeometry(1,1,1);this.adaptiveMediumDepthBoxGeometry.userData=Object.assign({},this.adaptiveMediumDepthBoxGeometry.userData||{},{adaptiveMediumSharedDepthBox:true,sharedCached:true});}return this.adaptiveMediumDepthBoxGeometry;}getAdaptiveMediumSourceWorldBox(source){if(!source||!source.geometry)return null;const geometry=this.getBaseGeometryForMesh?(this.getBaseGeometryForMesh(source)||source.geometry):source.geometry;if(!geometry.boundingBox&&geometry.computeBoundingBox)geometry.computeBoundingBox();if(!geometry.boundingBox||geometry.boundingBox.isEmpty())return null;source.updateWorldMatrix&&source.updateWorldMatrix(true,false);const box=geometry.boundingBox.clone();if(source.matrixWorld)box.applyMatrix4(source.matrixWorld);return box.isEmpty()?null:box;}ensureAdaptiveMediumDepthProxy(chunk){if(!chunk||!chunk.geometry||!chunk.userData||chunk.userData.normalChunkTransparent===true)return null;let proxy=chunk.userData.adaptiveMediumDepthProxy||null;if(!proxy){const chunkSources=Array.isArray(chunk.userData.normalChunkSourceMeshes)&&chunk.userData.normalChunkSourceMeshes.length?chunk.userData.normalChunkSourceMeshes:[chunk];const boxes=[];for(const source of chunkSources){const box=this.getAdaptiveMediumSourceWorldBox?this.getAdaptiveMediumSourceWorldBox(source):null;if(box)boxes.push(box);}if(!boxes.length)return null;proxy=new THREE.InstancedMesh(this.getAdaptiveMediumDepthBoxGeometry(),this.getAdaptiveMediumDepthMaterial(),boxes.length);const matrix=new THREE.Matrix4();const center=new THREE.Vector3();const size=new THREE.Vector3();const quaternion=new THREE.Quaternion();for(let index=0;index<boxes.length;index++){boxes[index].getCenter(center);boxes[index].getSize(size);size.set(Math.max(0.0001,size.x),Math.max(0.0001,size.y),Math.max(0.0001,size.z));matrix.compose(center,quaternion,size);proxy.setMatrixAt(index,matrix);}if(proxy.instanceMatrix){if(proxy.instanceMatrix.setUsage&&THREE.StaticDrawUsage!=null){proxy.instanceMatrix.setUsage(THREE.StaticDrawUsage);}proxy.instanceMatrix.needsUpdate=true;}if(proxy.computeBoundingBox)proxy.computeBoundingBox();if(proxy.computeBoundingSphere)proxy.computeBoundingSphere();proxy.raycast=()=>{};proxy.name='ExportModel_Adaptive_Medium_Depth';proxy.frustumCulled=true;proxy.renderOrder=Math.max(1,Number(chunk.renderOrder||0)+1);proxy.userData=Object.assign({},proxy.userData||{},{isAdaptiveMediumDepthProxy:true,isNormalChunk:true,lightweightInstancedDepthProxy:true,adaptiveDepthProxyInstances:boxes.length,sourceNormalChunk:chunk,renderProjectId:chunk.userData.renderProjectId||'',spatialChunkKey:chunk.userData.spatialChunkKey||'all',normalChunkSourceMeshes:chunk.userData.normalChunkSourceMeshes||[],normalChunkSourceKeys:chunk.userData.normalChunkSourceKeys||new Set()});chunk.userData.adaptiveMediumDepthProxy=proxy;if(!this.adaptiveMediumDepthProxies)this.adaptiveMediumDepthProxies=new Set();this.adaptiveMediumDepthProxies.add(proxy);}if(!proxy.parent&&this.scene)this.scene.add(proxy);return proxy;}releaseAdaptiveMediumDepthProxy(chunk,releaseReference){const proxy=chunk&&chunk.userData?chunk.userData.adaptiveMediumDepthProxy:null;if(!proxy)return false;proxy.visible=false;if(proxy.parent)proxy.parent.remove(proxy);if(releaseReference===true){if(this.adaptiveMediumDepthProxies)this.adaptiveMediumDepthProxies.delete(proxy);chunk.userData.adaptiveMediumDepthProxy=null;}return true;}updateAdaptiveMediumChunkVisibility(){const chunks=this.normalChunkMeshes||[];if(!chunks.length)return 0;const adaptiveEnabled=this.getVisualDetailLevel()==='medium'&&this.performanceRenderMode;let suppressed=0;for(const chunk of chunks){if(!chunk||!chunk.userData||chunk.userData.adaptiveDetailClass!=='compact')continue;const peeled=!!(this.selectionPeeledNormalChunks&&this.selectionPeeledNormalChunks.has(chunk));if(!adaptiveEnabled||peeled){chunk.userData.adaptiveSurfaceSuppressed=false;if(chunk.parent&&!peeled)chunk.visible=true;if(this.releaseAdaptiveMediumDepthProxy)this.releaseAdaptiveMediumDepthProxy(chunk,false);continue;}let hideSurface=false;if(adaptiveEnabled&&chunk.userData.adaptiveHasEdgeCoverage!==false){const sphere=chunk.geometry&&chunk.geometry.boundingSphere;const radius=Number(chunk.userData.adaptiveMaxSourceRadius)||(sphere&&sphere.radius)||0;const center=sphere&&sphere.center;const pixels=this.getProjectedPixelRadius(center,radius);const threshold=Math.max(1,Number(this.adaptiveMediumPixelThreshold)||3.5);const hysteresis=Math.max(0,Number(this.adaptiveMediumPixelHysteresis)||0);const wasSuppressed=chunk.userData.adaptiveSurfaceSuppressed===true;hideSurface=pixels<(wasSuppressed?threshold+hysteresis:threshold);chunk.userData.adaptiveProjectedPixelRadius=pixels;}chunk.userData.adaptiveSurfaceSuppressed=hideSurface;chunk.visible=!hideSurface;if(hideSurface){suppressed++;const proxy=chunk.parent&&this.ensureAdaptiveMediumDepthProxy?this.ensureAdaptiveMediumDepthProxy(chunk):null;if(proxy)proxy.visible=true;}else if(this.releaseAdaptiveMediumDepthProxy){this.releaseAdaptiveMediumDepthProxy(chunk,false);}}this.adaptiveMediumSuppressedChunks=suppressed;return suppressed;}getMeshRenderProjectId(mesh){if(!mesh||!mesh.userData)return'';const element=mesh.userData.element||null;const value=(element&&element.ProjectId!=null)?element.ProjectId:mesh.userData.projectId;return value==null?'':String(value);}getMergedRenderObjectSourceMeshes(object){if(!object||!object.userData)return[];const data=object.userData;const sources=data.normalChunkSourceMeshes||data.visualStateChunkSourceMeshes||data.massTransparencyChunkSourceMeshes||null;return Array.isArray(sources)?sources:[];}isMergedRenderObjectValidForTreeVisibility(object){if(!object||!object.userData)return true;const sources=this.getMergedRenderObjectSourceMeshes?this.getMergedRenderObjectSourceMeshes(object):[];if(sources.length)return sources.every(mesh=>mesh&&mesh.userData&&mesh.userData.baseVisible!==false);const projectId=String(object.userData.renderProjectId||'');if(!projectId||!this.meshesByProjectId)return true;const projectMeshes=this.meshesByProjectId.get(projectId)||[];return projectMeshes.some(mesh=>mesh&&mesh.userData&&mesh.userData.baseVisible!==false);}enforceTreeVisibilityMergedRenderGuard(audit,reason){if(!this.treeVisibilityGuardActive)return 0;let hidden=0;const collections=[this.normalChunkMeshes,this.adaptiveMediumDepthProxies,this.normalChunkEdgeBatches,this.visualStateChunkMeshes,this.visualStateChunkEdgeBatches,this.massTransparencyChunkMeshes,this.groupedTransparentEdgeBatches];for(const collection of collections){for(const object of collection||[]){if(!object||!object.parent)continue;if(this.isMergedRenderObjectValidForTreeVisibility&&!this.isMergedRenderObjectValidForTreeVisibility(object)){if(object.visible!==false){object.visible=false;hidden++;}}}}if(audit===true&&this.recordPerformanceOperation)this.recordPerformanceOperation('treeVisibility:render-guard',0,{reason:reason||'tree-visibility',hiddenObjects:hidden,revision:this.treeVisibilityGuardRevision||0});return hidden;}collectNormalChunkSourceMeshesFromMeshes(sourceMeshes){const groups=new Map();if(!this.shouldUseNormalChunks())return groups;for(const mesh of sourceMeshes||[]){if(!mesh||!mesh.isMesh||!mesh.userData)continue;const key=this.getMeshKey?this.getMeshKey(mesh):(mesh.userData&&mesh.userData.elementKey);if(!key)continue;if(this.isNormalChunkInteractiveKey(key))continue;if(mesh.userData.baseVisible===false)continue;const segments=this.getNormalChunkMaterialSegments?this.getNormalChunkMaterialSegments(mesh):[];if(!segments.length)continue;const adaptiveDetailClass=this.getAdaptiveMediumDetailClass(mesh);for(const segment of segments){const material=segment.material;const materialKey=this.getNormalChunkMaterialKey(material);if(!materialKey)continue;const isTransparent=!!(material.transparent||Number(material.opacity)<1);const projectId=this.getMeshRenderProjectId?this.getMeshRenderProjectId(mesh):'';const groupKey=materialKey+'|project:'+projectId+'|adaptive:'+adaptiveDetailClass+'|transparency:'+(isTransparent?'T':'O');let bucket=groups.get(groupKey);if(!bucket){bucket={material,segments:[],meshes:[],meshSet:new Set(),adaptiveDetailClass,transparent:isTransparent,projectId};groups.set(groupKey,bucket);}bucket.segments.push(segment);if(!bucket.meshSet.has(mesh)){bucket.meshSet.add(mesh);bucket.meshes.push(mesh);}}}return groups;}collectNormalChunkSourceMeshes(){return this.collectNormalChunkSourceMeshesFromMeshes?this.collectNormalChunkSourceMeshesFromMeshes(this.meshes||[]):new Map();}buildChunkGeometriesFromSegments(segments,options){const chunks=[];const list=Array.isArray(segments)?segments.filter(segment=>segment&&segment.mesh&&segment.geometry):[];if(!list.length)return chunks;const opts=options||{};const maxVerticesPerChunk=Math.max(3,Number(opts.maxVerticesPerChunk||220000));const maxIndicesPerChunk=Math.max(3,Number(opts.maxIndicesPerChunk||850000));const sourceMeshes=Array.from(new Set(list.map(segment=>segment.mesh).filter(Boolean)));const forcedSpatialChunkKey=String(opts.forcedSpatialChunkKey||'');const spatialGrid=!forcedSpatialChunkKey&&this.getSpatialChunkGrid?this.getSpatialChunkGrid(sourceMeshes,{spatialMinimumMeshes:Number(opts.spatialMinimumMeshes||24),targetMeshesPerCell:Number(opts.targetMeshesPerCell||this.spatialChunkTargetMeshesPerCell||180),maximumCells:Number(opts.maximumCells||this.spatialChunkMaximumCells||512)}):null;const spatialBuckets=new Map();for(const segment of list){const spatialKey=forcedSpatialChunkKey||(spatialGrid&&this.getSpatialChunkKeyForMesh?this.getSpatialChunkKeyForMesh(segment.mesh,spatialGrid):'all');if(!spatialBuckets.has(spatialKey))spatialBuckets.set(spatialKey,[]);spatialBuckets.get(spatialKey).push(segment);}const temp=new THREE.Vector3();for(const[spatialKey,bucketSegments]of spatialBuckets.entries()){let pendingSegments=[];let estimatedVertices=0;let pendingIndexCount=0;const flushChunk=()=>{if(!pendingSegments.length||!pendingIndexCount){pendingSegments=[];estimatedVertices=0;pendingIndexCount=0;return;}const vertexCapacity=Math.max(1,estimatedVertices);const indexCapacity=Math.max(1,pendingIndexCount);const positions=new Float32Array(vertexCapacity*3);const use32=vertexCapacity>65535;const indices=use32?new Uint32Array(indexCapacity):new Uint16Array(indexCapacity);let vertexCursor=0;let indexCursor=0;const sourceParts=[];const sourceSet=new Set();const vertexMapsByMesh=new Map();const indexRangesByKey=new Map();for(const pending of pendingSegments){const mesh=pending.mesh;const geometry=pending.geometry;const position=geometry.getAttribute&&geometry.getAttribute('position');if(!mesh||!position||!position.count)continue;const source=position.array;const srcIndex=geometry.index&&geometry.index.count?geometry.index.array:null;mesh.updateWorldMatrix(true,false);let vertexMap=vertexMapsByMesh.get(mesh);if(!vertexMap){vertexMap=new Map();vertexMapsByMesh.set(mesh,vertexMap);}const rangeStart=indexCursor;for(let i=0;i<pending.count;i++){const sourceIndex=srcIndex?Number(srcIndex[pending.start+i]):pending.start+i;if(!Number.isFinite(sourceIndex)||sourceIndex<0||sourceIndex>=position.count)continue;let outputIndex=vertexMap.get(sourceIndex);if(outputIndex==null){outputIndex=vertexCursor++;vertexMap.set(sourceIndex,outputIndex);const srcOffset=sourceIndex*3;const dstOffset=outputIndex*3;temp.set(source[srcOffset],source[srcOffset+1],source[srcOffset+2]).applyMatrix4(mesh.matrixWorld);positions[dstOffset]=temp.x;positions[dstOffset+1]=temp.y;positions[dstOffset+2]=temp.z;}indices[indexCursor++]=outputIndex;}const rangeCount=indexCursor-rangeStart;const sourceKey=this.getMeshKey?this.getMeshKey(mesh):(mesh&&mesh.userData&&mesh.userData.elementKey);if(sourceKey&&rangeCount>0){let ranges=indexRangesByKey.get(sourceKey);if(!ranges){ranges=[];indexRangesByKey.set(sourceKey,ranges);}ranges.push([rangeStart,rangeCount]);}if(!sourceSet.has(mesh)){sourceSet.add(mesh);sourceParts.push(mesh);}}if(!vertexCursor||!indexCursor){pendingSegments=[];estimatedVertices=0;pendingIndexCount=0;return;}const geometry=new THREE.BufferGeometry();geometry.setAttribute('position',new THREE.BufferAttribute(positions.subarray(0,vertexCursor*3),3));geometry.setIndex(new THREE.BufferAttribute(indices.subarray(0,indexCursor),1));geometry.userData=Object.assign({},geometry.userData||{},{normalChunkSourceMeshes:sourceParts,normalChunkSourceKeys:sourceParts.map(mesh=>this.getMeshKey?this.getMeshKey(mesh):(mesh&&mesh.userData&&mesh.userData.elementKey)).filter(Boolean),spatialChunkKey:spatialKey||'all',materialSegmentChunk:true,memorySafeChunk:true,normalChunkIndexRangesByKey:indexRangesByKey});geometry.computeBoundingBox();geometry.computeBoundingSphere();chunks.push(geometry);pendingSegments=[];estimatedVertices=0;pendingIndexCount=0;};const appendSlice=(segment,start,count,localVertexCount)=>{if(!count)return;const sliceVertexEstimate=Math.max(1,Math.min(localVertexCount,count));if(pendingSegments.length&&(estimatedVertices+sliceVertexEstimate>maxVerticesPerChunk||pendingIndexCount+count>maxIndicesPerChunk))flushChunk();pendingSegments.push({mesh:segment.mesh,geometry:segment.geometry,start,count});estimatedVertices+=sliceVertexEstimate;pendingIndexCount+=count;if(estimatedVertices>=maxVerticesPerChunk||pendingIndexCount>=maxIndicesPerChunk)flushChunk();};for(const segment of bucketSegments){const geometry=segment.geometry;const position=geometry.getAttribute&&geometry.getAttribute('position');if(!position||!position.count)continue;const localVertexCount=position.count;const availableCount=geometry.index&&geometry.index.count?geometry.index.count:localVertexCount;const start=Math.max(0,Math.min(availableCount,Number(segment.start)||0));const requestedCount=Math.max(0,Math.min(availableCount-start,Number(segment.count)||0));if(!requestedCount)continue;const rawSliceLimit=Math.max(3,Math.min(maxIndicesPerChunk,maxVerticesPerChunk));const triangleAlignedLimit=Math.max(3,Math.floor(rawSliceLimit/3)*3);let consumed=0;while(consumed<requestedCount){const remaining=requestedCount-consumed;let count=Math.min(remaining,triangleAlignedLimit);if(remaining>count&&count%3!==0)count-=count%3;if(count<=0)count=remaining;appendSlice(segment,start+consumed,count,localVertexCount);consumed+=count;}}flushChunk();}if(spatialBuckets.size>1)this.spatialChunkBuildCount=(this.spatialChunkBuildCount||0)+1;return chunks;}getSpatialChunkGrid(meshes,options){const list=Array.isArray(meshes)?meshes.filter(Boolean):[];const opts=options||{};const minimumMeshes=Math.max(12,Number(opts.spatialMinimumMeshes||this.spatialChunkMinimumMeshes||24));if(this.spatialChunkingEnabled===false||opts.spatialPartition===false||list.length<minimumMeshes)return null;if(!this.performanceRenderMode&&(this.meshes||[]).length<2500)return null;const bounds=this.getFullModelBounds?this.getFullModelBounds():null;if(!bounds||bounds.isEmpty())return null;const size=bounds.getSize(new THREE.Vector3());const totalMeshes=Math.max(list.length,(this.meshes&&this.meshes.length)||0);const targetMeshesPerCell=Math.max(80,Number(opts.targetMeshesPerCell||this.spatialChunkTargetMeshesPerCell||180));const maximumCells=Math.max(64,Number(opts.maximumCells||this.spatialChunkMaximumCells||512));const targetCells=Math.max(8,Math.min(maximumCells,Math.ceil(totalMeshes/targetMeshesPerCell)));const verticalWeight=Math.max(0.35,Math.min(1.5,size.y/Math.max(1,Math.max(size.x,size.z))));const verticalDivisions=Math.max(1,Math.min(12,Math.ceil(Math.cbrt(targetCells)*verticalWeight)));const horizontalTarget=Math.max(1,targetCells/verticalDivisions);const aspect=Math.max(0.25,Math.min(4,size.x/Math.max(0.001,size.z)));const divisionsX=Math.max(3,Math.min(24,Math.ceil(Math.sqrt(horizontalTarget*aspect))));const divisionsZ=Math.max(3,Math.min(24,Math.ceil(horizontalTarget/Math.max(1,divisionsX))));const cellX=Math.max(4,size.x/Math.max(1,divisionsX));const cellZ=Math.max(4,size.z/Math.max(1,divisionsZ));const cellY=Math.max(3,size.y/Math.max(1,verticalDivisions));const signature=[bounds.min.x.toFixed(3),bounds.min.y.toFixed(3),bounds.min.z.toFixed(3),cellX.toFixed(3),cellY.toFixed(3),cellZ.toFixed(3)].join('|');return{origin:bounds.min.clone(),cellX,cellY,cellZ,divisionsX,divisionsY:verticalDivisions,divisionsZ,targetCells,signature};}getSpatialChunkKeyForMesh(mesh,grid){if(!mesh||!grid)return'all';const cached=mesh.userData&&mesh.userData.spatialChunkCache;if(cached&&cached.signature===grid.signature)return cached.key;const geometry=mesh.geometry;if(!geometry||!geometry.isBufferGeometry)return'all';if(!geometry.boundingBox&&geometry.computeBoundingBox)geometry.computeBoundingBox();const center=geometry.boundingBox&&!geometry.boundingBox.isEmpty()?geometry.boundingBox.getCenter(new THREE.Vector3()):new THREE.Vector3();mesh.updateWorldMatrix(true,false);center.applyMatrix4(mesh.matrixWorld);const ix=Math.floor((center.x-grid.origin.x)/grid.cellX);const iy=Math.floor((center.y-grid.origin.y)/grid.cellY);const iz=Math.floor((center.z-grid.origin.z)/grid.cellZ);const key=ix+':'+iy+':'+iz;if(mesh.userData)mesh.userData.spatialChunkCache={signature:grid.signature,key};return key;}partitionMeshesForSpatialChunks(meshes,options){const list=Array.isArray(meshes)?meshes.filter(Boolean):[];if(!list.length)return[];const grid=this.getSpatialChunkGrid(list,options);if(!grid)return[{key:'all',meshes:list}];const buckets=new Map();for(const mesh of list){const key=this.getSpatialChunkKeyForMesh(mesh,grid);if(!buckets.has(key))buckets.set(key,[]);buckets.get(key).push(mesh);}if(buckets.size<=1)return[{key:'all',meshes:list}];return Array.from(buckets,([key,bucketMeshes])=>({key,meshes:bucketMeshes}));}buildChunkGeometriesFromMeshes(meshes,options){const chunks=[];if(!meshes||!meshes.length)return chunks;const opts=options||{};const maxVerticesPerChunk=Number(opts.maxVerticesPerChunk||240000);const maxIndicesPerChunk=Number(opts.maxIndicesPerChunk||900000);let positionParts=[];let indexParts=[];let sourceParts=[];let indexRangesByKey=new Map();let vertexCount=0;let indexCount=0;const temp=new THREE.Vector3();const flushChunk=()=>{if(!positionParts.length)return;const positions=new Float32Array(vertexCount*3);let pOffset=0;for(const arr of positionParts){positions.set(arr,pOffset);pOffset+=arr.length;}const geometry=new THREE.BufferGeometry();geometry.setAttribute('position',new THREE.BufferAttribute(positions,3));if(indexCount>0){const indexArray=vertexCount>65535?new Uint32Array(indexCount):new Uint16Array(indexCount);let iOffset=0;for(const arr of indexParts){indexArray.set(arr,iOffset);iOffset+=arr.length;}geometry.setIndex(new THREE.BufferAttribute(indexArray,1));}geometry.userData=Object.assign({},geometry.userData||{},{normalChunkSourceMeshes:sourceParts.slice(),normalChunkSourceKeys:sourceParts.map(mesh=>this.getMeshKey?this.getMeshKey(mesh):(mesh&&mesh.userData&&mesh.userData.elementKey)).filter(Boolean),normalChunkIndexRangesByKey:new Map(Array.from(indexRangesByKey.entries()).map(([key,ranges])=>[key,ranges.map(pair=>pair.slice())]))});geometry.computeBoundingSphere();chunks.push(geometry);positionParts=[];indexParts=[];sourceParts=[];indexRangesByKey=new Map();vertexCount=0;indexCount=0;};const spatialBuckets=this.partitionMeshesForSpatialChunks?this.partitionMeshesForSpatialChunks(meshes,opts):[{key:'all',meshes:Array.from(meshes||[])}];for(const spatialBucket of spatialBuckets){const spatialKey=spatialBucket&&spatialBucket.key?spatialBucket.key:'all';for(const mesh of(spatialBucket&&spatialBucket.meshes)||[]){const geometry=opts.useSingleDraw!==false&&this.getSingleDrawGeometryForMesh?this.getSingleDrawGeometryForMesh(mesh):(mesh.geometry||null);if(!geometry||!geometry.isBufferGeometry)continue;const position=geometry.getAttribute&&geometry.getAttribute('position');if(!position||!position.count)continue;const localVertexCount=position.count;const localIndexCount=geometry.index?geometry.index.count:localVertexCount;if(vertexCount>0&&(vertexCount+localVertexCount>maxVerticesPerChunk||indexCount+localIndexCount>maxIndicesPerChunk))flushChunk();mesh.updateWorldMatrix(true,false);const transformed=new Float32Array(localVertexCount*3);const source=position.array;for(let i=0;i<localVertexCount;i++){const srcOffset=i*3;temp.set(source[srcOffset],source[srcOffset+1],source[srcOffset+2]).applyMatrix4(mesh.matrixWorld);transformed[srcOffset]=temp.x;transformed[srcOffset+1]=temp.y;transformed[srcOffset+2]=temp.z;}positionParts.push(transformed);const sourceKey=this.getMeshKey?this.getMeshKey(mesh):(mesh&&mesh.userData&&mesh.userData.elementKey);const sourceIndexStart=indexCount;if(geometry.index&&geometry.index.count){const srcIndex=geometry.index.array;const use32=(vertexCount+localVertexCount)>65535;const localIndices=use32?new Uint32Array(srcIndex.length):new Uint16Array(srcIndex.length);for(let i=0;i<srcIndex.length;i++)localIndices[i]=Number(srcIndex[i])+vertexCount;indexParts.push(localIndices);indexCount+=localIndices.length;}else{const use32=(vertexCount+localVertexCount)>65535;const localIndices=use32?new Uint32Array(localVertexCount):new Uint16Array(localVertexCount);for(let i=0;i<localVertexCount;i++)localIndices[i]=i+vertexCount;indexParts.push(localIndices);indexCount+=localIndices.length;}if(sourceKey&&indexCount>sourceIndexStart){let ranges=indexRangesByKey.get(sourceKey);if(!ranges){ranges=[];indexRangesByKey.set(sourceKey,ranges);}ranges.push([sourceIndexStart,indexCount-sourceIndexStart]);}sourceParts.push(mesh);vertexCount+=localVertexCount;}if(positionParts.length){const lastChunkIndex=chunks.length;flushChunk();for(let i=lastChunkIndex;i<chunks.length;i++){chunks[i].userData=Object.assign({},chunks[i].userData||{},{spatialChunkKey:spatialKey});if(chunks[i].computeBoundingBox)chunks[i].computeBoundingBox();}}}if(spatialBuckets.length>1)this.spatialChunkBuildCount=(this.spatialChunkBuildCount||0)+1;return chunks;}async rebuildNormalChunksForLoad(forceRebuild,options){const opts=options&&typeof options==='object'?options:{};const report=typeof opts.onProgress==='function'?opts.onProgress:()=>{};const operationId=Number(opts.operationId||0);const treeVisibilityRevisionAtBuildStart=this.treeVisibilityGuardRevision||0;const externalCancelled=typeof opts.isCancelled==='function'?opts.isCancelled:()=>false;const isCancelled=()=>externalCancelled()||(operationId>0&&operationId!==this.loadOperationId)||(this.treeVisibilityGuardRevision||0)!==treeVisibilityRevisionAtBuildStart;if(!this.shouldUseNormalChunks()){this.clearNormalChunks(false);report(1,1,'Optimización del visor no necesaria.');return 0;}const metricsStart=this.getPerformanceNow?this.getPerformanceNow():Date.now();const signature=opts.precomputedSignature||this.getNormalChunkSignature();const sameSignature=!forceRebuild&&signature&&signature===this.normalChunkCacheSignature&&this.normalChunkMeshes&&this.normalChunkMeshes.length;if(sameSignature)return this.rebuildNormalChunks(false);this.clearNormalChunks(true);const groups=this.collectNormalChunkSourceMeshes();const buckets=Array.from(groups.values()).filter(bucket=>bucket&&bucket.meshes&&bucket.meshes.length&&bucket.segments&&bucket.segments.length);if(!buckets.length){report(1,1,'Optimización del visor completada.');return 0;}const chunks=[];const allChunkSourceMeshSet=new Set();let transparentChunkCount=0;let opaqueChunkCount=0;this.normalChunksByElementKey=new Map();this.normalChunkMeshes=chunks;this.normalChunkEdgeBatches=[];const cleanupBuildState=()=>{try{for(const chunk of chunks||[]){if(!chunk)continue;if(this.releaseAdaptiveMediumDepthProxy)this.releaseAdaptiveMediumDepthProxy(chunk,true);if(chunk.parent)chunk.parent.remove(chunk);if(chunk.geometry&&chunk.geometry.dispose)chunk.geometry.dispose();}if(this.normalChunkMeshes===chunks){this.normalChunkMeshes=[];this.normalChunkVisible=false;this.normalChunkCacheSignature=null;}}catch(_){}};const cleanupFallback=error=>{cleanupBuildState();this.normalChunkVisible=false;this.normalChunkCacheSignature=null;this.normalChunkLoadFallbackCount=(this.normalChunkLoadFallbackCount||0)+1;if(typeof debugError==='function')debugError('viewer:normalChunks:loadFallback',error,{buckets:buckets.length,builtChunks:chunks.length,fallbackCount:this.normalChunkLoadFallbackCount});if(this.recordPerformanceOperation)this.recordPerformanceOperation('normalChunks:load-fallback',(this.getPerformanceNow?this.getPerformanceNow():Date.now())-metricsStart,{buckets:buckets.length,builtChunks:chunks.length,error:error&&error.message?String(error.message):String(error||'')});};try{report(0,buckets.length,'Optimizando geometría del visor 3D...');for(let bucketIndex=0;bucketIndex<buckets.length;bucketIndex++){if(isCancelled()){cleanupBuildState();return false;}const bucket=buckets[bucketIndex];for(const sourceMesh of bucket.meshes)if(sourceMesh)allChunkSourceMeshSet.add(sourceMesh);const geometries=this.buildChunkGeometriesFromSegments(bucket.segments,{maxVerticesPerChunk:bucket.transparent?Number(this.normalChunkTransparentMaxVertices||60000):Number(this.normalChunkOpaqueMaxVertices||120000),maxIndicesPerChunk:bucket.transparent?Number(this.normalChunkTransparentMaxIndices||180000):Number(this.normalChunkOpaqueMaxIndices||360000),spatialMinimumMeshes:bucket.transparent?12:Number(this.spatialChunkMinimumMeshes||24),targetMeshesPerCell:bucket.transparent?120:Number(this.spatialChunkTargetMeshesPerCell||180)});for(const geometry of geometries){const chunkMaterial=this.getDepthStableNormalChunkMaterial?this.getDepthStableNormalChunkMaterial(bucket.material):bucket.material;const chunk=new THREE.Mesh(geometry,chunkMaterial);chunk.renderOrder=0;chunk.frustumCulled=true;if(bucket.transparent)transparentChunkCount++;else opaqueChunkCount++;const sourceMeshes=geometry&&geometry.userData&&Array.isArray(geometry.userData.normalChunkSourceMeshes)?geometry.userData.normalChunkSourceMeshes.slice():[];const sourceKeys=new Set((geometry&&geometry.userData&&Array.isArray(geometry.userData.normalChunkSourceKeys))?geometry.userData.normalChunkSourceKeys:[]);let adaptiveMaxSourceRadius=0;let adaptiveHasEdgeCoverage=true;if(bucket.adaptiveDetailClass==='compact'){for(const sourceMesh of sourceMeshes){adaptiveMaxSourceRadius=Math.max(adaptiveMaxSourceRadius,this.getMeshWorldBoundingRadius(sourceMesh));const hasEdgeCoverage=this.hasReadableEdgeCoverageForMesh?this.hasReadableEdgeCoverageForMesh(sourceMesh):!!(sourceMesh&&sourceMesh.children&&sourceMesh.children.some(child=>child&&child.isLineSegments&&child.geometry&&child.userData&&child.userData.isGeneratedEdge));if(!hasEdgeCoverage)adaptiveHasEdgeCoverage=false;}}chunk.userData=Object.assign({},chunk.userData||{},{isNormalChunk:true,renderProjectId:bucket.projectId||'',normalChunkSourceMeshes:sourceMeshes,normalChunkSourceKeys:sourceKeys,normalChunkIndexRangesByKey:geometry&&geometry.userData?(geometry.userData.normalChunkIndexRangesByKey||null):null,spatialChunkKey:geometry&&geometry.userData?(geometry.userData.spatialChunkKey||'all'):'all',adaptiveDetailClass:bucket.adaptiveDetailClass||'full',adaptiveMaxSourceRadius,adaptiveHasEdgeCoverage,normalChunkTransparent:!!bucket.transparent,materialSegmentChunk:true,memorySafeChunk:true});for(const sourceMesh of sourceMeshes){const sourceKey=this.getMeshKey?this.getMeshKey(sourceMesh):(sourceMesh&&sourceMesh.userData&&sourceMesh.userData.elementKey);if(!sourceKey)continue;let list=this.normalChunksByElementKey.get(sourceKey);if(!list){list=new Set();this.normalChunksByElementKey.set(sourceKey,list);}list.add(chunk);}chunks.push(chunk);}report(bucketIndex+1,buckets.length,'Optimizando geometría del visor 3D...');if(bucketIndex+1<buckets.length&&this.yieldDuringLoad)await this.yieldDuringLoad();}if(isCancelled()){cleanupBuildState();return false;}for(const chunk of chunks)if(chunk)this.scene.add(chunk);for(const mesh of allChunkSourceMeshSet){const key=this.getMeshKey?this.getMeshKey(mesh):(mesh&&mesh.userData&&mesh.userData.elementKey);if(!key||!mesh||!mesh.userData)continue;mesh.userData.normalChunkSuppressed=true;mesh.visible=false;if(this.removeTransientCompactFallbackEdge)this.removeTransientCompactFallbackEdge(mesh,true);this.normalChunkHiddenKeys.add(key);}const allChunkSourceMeshes=Array.from(allChunkSourceMeshSet);if(this.rebuildNormalChunkEdgesFromMeshesAsync){await this.rebuildNormalChunkEdgesFromMeshesAsync(allChunkSourceMeshes,{disposeExisting:true,batchSize:Number(opts.edgeBatchSize)||220,yieldBetweenBatches:true,onProgress:(current,total,message)=>report(current,total,message||'Optimizando bordes del visor 3D...','edges')});}else if(this.rebuildNormalChunkEdgesFromMeshes){this.rebuildNormalChunkEdgesFromMeshes(allChunkSourceMeshes,true);}if(isCancelled()){cleanupBuildState();return false;}this.normalChunkEdgesVisibleOnly=false;this.normalChunkOpaqueMeshCount=opaqueChunkCount;this.normalChunkTransparentMeshCount=transparentChunkCount;this.normalChunkSourceMeshCount=allChunkSourceMeshes.length;this.normalChunkCacheSignature=signature;this.normalChunkVisible=chunks.length>0;this.updateAdaptiveMediumChunkVisibility&&this.updateAdaptiveMediumChunkVisibility();if(this.syncInteractiveNormalChunkPeel)this.syncInteractiveNormalChunkPeel();this.normalChunkBuildCount=(this.normalChunkBuildCount||0)+1;if(this.recordPerformanceOperation)this.recordPerformanceOperation('normalChunks:build-load-async',(this.getPerformanceNow?this.getPerformanceNow():Date.now())-metricsStart,{chunks:chunks.length,opaqueChunks:opaqueChunkCount,transparentChunks:transparentChunkCount,sourceMeshes:allChunkSourceMeshes.length,groups:groups.size,hiddenKeys:this.normalChunkHiddenKeys?this.normalChunkHiddenKeys.size:0,memorySafe:true});report(1,1,'Optimización del visor completada.','complete');return chunks.length;}catch(error){cleanupFallback(error);report(1,1,'El visor continuará sin bloques optimizados.','fallback');return 0;}}rebuildNormalChunks(forceRebuild){if(!this.shouldUseNormalChunks()){this.clearNormalChunks(false);return 0;}const metricsStart=this.getPerformanceNow?this.getPerformanceNow():Date.now();const signature=this.getNormalChunkSignature();const sameSignature=!forceRebuild&&signature&&signature===this.normalChunkCacheSignature&&this.normalChunkMeshes&&this.normalChunkMeshes.length;this.clearNormalChunks(sameSignature?false:true);if(sameSignature){for(const key of this.normalChunkHiddenKeys||[]){const meshes=this.getMeshesForElementKey?this.getMeshesForElementKey(key):[];for(const mesh of meshes){if(mesh&&mesh.userData){mesh.userData.normalChunkSuppressed=false;mesh.visible=mesh.userData.baseVisible!==false;}}}this.normalChunkHiddenKeys.clear();for(const mesh of this.meshes||[]){if(!mesh||!mesh.isMesh||!mesh.userData)continue;const key=this.getMeshKey(mesh);if(!key||this.isNormalChunkInteractiveKey(key)||mesh.userData.baseVisible===false)continue;if(!(this.isNormalChunkRenderableMesh&&this.isNormalChunkRenderableMesh(mesh)))continue;mesh.userData.normalChunkSuppressed=true;mesh.visible=false;if(this.removeTransientCompactFallbackEdge)this.removeTransientCompactFallbackEdge(mesh,true);this.normalChunkHiddenKeys.add(key);}for(const chunk of this.normalChunkMeshes){if(!chunk)continue;chunk.visible=true;this.scene.add(chunk);}for(const edgeBatch of this.normalChunkEdgeBatches||[]){if(!edgeBatch)continue;edgeBatch.visible=true;this.scene.add(edgeBatch);}this.normalChunkVisible=true;if(this.updateAdaptiveMediumChunkVisibility)this.updateAdaptiveMediumChunkVisibility();if(this.syncInteractiveNormalChunkPeel)this.syncInteractiveNormalChunkPeel();this.normalChunkReuseCount=(this.normalChunkReuseCount||0)+1;if(this.recordPerformanceOperation)this.recordPerformanceOperation('normalChunks:reuse',(this.getPerformanceNow?this.getPerformanceNow():Date.now())-metricsStart,{chunks:this.normalChunkMeshes.length});return this.normalChunkMeshes.length;}const groups=this.collectNormalChunkSourceMeshes();const chunks=[];const allChunkSourceMeshSet=new Set();let transparentChunkCount=0;let opaqueChunkCount=0;this.normalChunksByElementKey=new Map();for(const bucket of groups.values()){if(!bucket||!bucket.meshes||!bucket.meshes.length||!bucket.segments||!bucket.segments.length)continue;for(const sourceMesh of bucket.meshes)if(sourceMesh)allChunkSourceMeshSet.add(sourceMesh);const geometries=this.buildChunkGeometriesFromSegments(bucket.segments,{maxVerticesPerChunk:bucket.transparent?Number(this.normalChunkTransparentMaxVertices||60000):Number(this.normalChunkOpaqueMaxVertices||120000),maxIndicesPerChunk:bucket.transparent?Number(this.normalChunkTransparentMaxIndices||180000):Number(this.normalChunkOpaqueMaxIndices||360000),spatialMinimumMeshes:bucket.transparent?12:Number(this.spatialChunkMinimumMeshes||24),targetMeshesPerCell:bucket.transparent?120:Number(this.spatialChunkTargetMeshesPerCell||180)});for(const geometry of geometries){const chunkMaterial=this.getDepthStableNormalChunkMaterial?this.getDepthStableNormalChunkMaterial(bucket.material):bucket.material;const chunk=new THREE.Mesh(geometry,chunkMaterial);chunk.renderOrder=0;chunk.frustumCulled=true;if(bucket.transparent)transparentChunkCount++;else opaqueChunkCount++;const sourceMeshes=geometry&&geometry.userData&&Array.isArray(geometry.userData.normalChunkSourceMeshes)?geometry.userData.normalChunkSourceMeshes.slice():[];const sourceKeys=new Set((geometry&&geometry.userData&&Array.isArray(geometry.userData.normalChunkSourceKeys))?geometry.userData.normalChunkSourceKeys:[]);let adaptiveMaxSourceRadius=0;let adaptiveHasEdgeCoverage=true;if(bucket.adaptiveDetailClass==='compact'){for(const sourceMesh of sourceMeshes){adaptiveMaxSourceRadius=Math.max(adaptiveMaxSourceRadius,this.getMeshWorldBoundingRadius(sourceMesh));const hasEdgeCoverage=this.hasReadableEdgeCoverageForMesh?this.hasReadableEdgeCoverageForMesh(sourceMesh):!!(sourceMesh&&sourceMesh.children&&sourceMesh.children.some(child=>child&&child.isLineSegments&&child.geometry&&child.userData&&child.userData.isGeneratedEdge));if(!hasEdgeCoverage)adaptiveHasEdgeCoverage=false;}}chunk.userData=Object.assign({},chunk.userData||{},{isNormalChunk:true,renderProjectId:bucket.projectId||'',normalChunkSourceMeshes:sourceMeshes,normalChunkSourceKeys:sourceKeys,normalChunkIndexRangesByKey:geometry&&geometry.userData?(geometry.userData.normalChunkIndexRangesByKey||null):null,spatialChunkKey:geometry&&geometry.userData?(geometry.userData.spatialChunkKey||'all'):'all',adaptiveDetailClass:bucket.adaptiveDetailClass||'full',adaptiveMaxSourceRadius,adaptiveHasEdgeCoverage,normalChunkTransparent:!!bucket.transparent,materialSegmentChunk:true});for(const sourceMesh of sourceMeshes){const sourceKey=this.getMeshKey?this.getMeshKey(sourceMesh):(sourceMesh&&sourceMesh.userData&&sourceMesh.userData.elementKey);if(!sourceKey)continue;let list=this.normalChunksByElementKey.get(sourceKey);if(!list){list=new Set();this.normalChunksByElementKey.set(sourceKey,list);}list.add(chunk);}this.scene.add(chunk);chunks.push(chunk);}for(const mesh of bucket.meshes){const key=this.getMeshKey(mesh);if(!key||!mesh.userData)continue;mesh.userData.normalChunkSuppressed=true;mesh.visible=false;if(this.removeTransientCompactFallbackEdge)this.removeTransientCompactFallbackEdge(mesh,true);this.normalChunkHiddenKeys.add(key);}}this.normalChunkMeshes=chunks;const allChunkSourceMeshes=Array.from(allChunkSourceMeshSet);this.rebuildNormalChunkEdgesFromMeshes&&this.rebuildNormalChunkEdgesFromMeshes(allChunkSourceMeshes,true);this.normalChunkEdgesVisibleOnly=false;this.normalChunkOpaqueMeshCount=opaqueChunkCount;this.normalChunkTransparentMeshCount=transparentChunkCount;this.normalChunkSourceMeshCount=allChunkSourceMeshes.length;this.normalChunkCacheSignature=signature;this.normalChunkVisible=chunks.length>0;this.updateAdaptiveMediumChunkVisibility&&this.updateAdaptiveMediumChunkVisibility();if(this.syncInteractiveNormalChunkPeel)this.syncInteractiveNormalChunkPeel();this.normalChunkBuildCount=(this.normalChunkBuildCount||0)+1;if(this.recordPerformanceOperation)this.recordPerformanceOperation('normalChunks:build',(this.getPerformanceNow?this.getPerformanceNow():Date.now())-metricsStart,{chunks:chunks.length,opaqueChunks:opaqueChunkCount,transparentChunks:transparentChunkCount,sourceMeshes:allChunkSourceMeshes.length,groups:groups.size,hiddenKeys:this.normalChunkHiddenKeys?this.normalChunkHiddenKeys.size:0});return chunks.length;}syncNormalChunksForKeys(keys){if(this.clashModeActive===true){const skipped=keys&&typeof keys.size==='number'?keys.size:(Array.isArray(keys)?keys.length:0);if(this.recordPerformanceOperation)this.recordPerformanceOperation('normalChunks:sync-skip-clash-mode',0,{changedKeys:skipped});return;}if(!this.normalChunkVisible||!this.normalChunkHiddenKeys)return;const keySet=new Set(keys||[]);if(!keySet.size)return;if(this.hasChunkKeyIntersection&&this.hasChunkKeyIntersection(keySet,this.normalChunkHiddenKeys)){this.clearNormalChunks(false);if(!(this.visualMassState&&this.visualMassState.active)){if(this.deferNormalChunkRefresh)this.deferNormalChunkRefresh('normal-chunk-source-keys-changed');else if(this.rebuildNormalChunks)this.rebuildNormalChunks(false);}if(this.recordPerformanceOperation)this.recordPerformanceOperation('normalChunks:clear-source-key-change',0,{changedKeys:keySet.size});return;}let needsFullRefresh=false;for(const key of keySet){const meshes=this.getMeshesForElementKey?this.getMeshesForElementKey(key):[];if(!meshes.length)continue;const existingChunks=this.normalChunksByElementKey&&this.normalChunksByElementKey.get(key);const hasExistingChunk=!!(existingChunks&&existingChunks.size);let suppressAny=false;for(const mesh of meshes){if(!mesh||!mesh.userData)continue;const chunkable=this.shouldUseNormalChunks()&&mesh.isMesh&&mesh.userData.baseVisible!==false&&!this.isNormalChunkInteractiveKey(key)&&!!(this.isNormalChunkRenderableMesh&&this.isNormalChunkRenderableMesh(mesh));const canSuppressWithCurrentChunks=chunkable&&hasExistingChunk;if(canSuppressWithCurrentChunks){mesh.userData.normalChunkSuppressed=true;mesh.visible=false;suppressAny=true;}else{mesh.userData.normalChunkSuppressed=false;if(!(mesh.userData.massTransparencyChunkSuppressed||mesh.userData.visualStateChunkSuppressed))this.updateVisualAppearanceForMesh(mesh);if(chunkable&&!hasExistingChunk)needsFullRefresh=true;}}if(suppressAny)this.normalChunkHiddenKeys.add(key);else this.normalChunkHiddenKeys.delete(key);}if(needsFullRefresh&&!(this.visualMassState&&this.visualMassState.active)){this.normalChunkCacheSignature=null;if(this.deferNormalChunkRefresh)this.deferNormalChunkRefresh('normal-chunk-new-visible-keys');else if(this.rebuildNormalChunks)this.rebuildNormalChunks(false);}this.requestRender&&this.requestRender('normal-chunks-sync-keys');}hasChunkKeyIntersection(keys,chunkKeys){if(!keys||!chunkKeys||!chunkKeys.size)return false;for(const key of keys){if(chunkKeys.has(key))return true;}return false;}removeNormalChunksContainingKeys(keys,options){const keySet=new Set(keys||[]);if(!keySet.size||!this.normalChunkMeshes||!this.normalChunkMeshes.length)return 0;const opts=options&&typeof options==='object'?options:{};const directlyAffectedChunks=new Set();for(const key of keySet){const chunks=this.normalChunksByElementKey&&this.normalChunksByElementKey.get(key);if(chunks&&chunks.size){for(const chunk of chunks)if(chunk)directlyAffectedChunks.add(chunk);}}for(const chunk of this.normalChunkMeshes||[]){const sourceKeys=chunk&&chunk.userData&&chunk.userData.normalChunkSourceKeys;if(!sourceKeys||!sourceKeys.size)continue;for(const key of keySet){if(sourceKeys.has(key)){directlyAffectedChunks.add(chunk);break;}}}if(!directlyAffectedChunks.size)return 0;const impactedSpatialKeys=new Set();for(const chunk of directlyAffectedChunks){impactedSpatialKeys.add(chunk&&chunk.userData?(chunk.userData.spatialChunkKey||'all'):'all');}const chunksToRemove=new Set();for(const chunk of this.normalChunkMeshes||[]){const spatialKey=chunk&&chunk.userData?(chunk.userData.spatialChunkKey||'all'):'all';if(impactedSpatialKeys.has(spatialKey))chunksToRemove.add(chunk);}const restoredKeys=new Set();if(!this.detachedNormalChunkCells)this.detachedNormalChunkCells=new Map();for(const chunk of chunksToRemove){if(!chunk)continue;const spatialKey=chunk.userData?(chunk.userData.spatialChunkKey||'all'):'all';let detachedSources=this.detachedNormalChunkCells.get(spatialKey);if(!detachedSources){detachedSources=new Set();this.detachedNormalChunkCells.set(spatialKey,detachedSources);}if(this.releaseAdaptiveMediumDepthProxy)this.releaseAdaptiveMediumDepthProxy(chunk,true);if(chunk.parent)chunk.parent.remove(chunk);if(opts.dispose===true&&chunk.geometry&&chunk.geometry.dispose)chunk.geometry.dispose();const sourceMeshes=chunk.userData&&Array.isArray(chunk.userData.normalChunkSourceMeshes)?chunk.userData.normalChunkSourceMeshes:[];for(const mesh of sourceMeshes){if(mesh)detachedSources.add(mesh);const sourceKey=this.getMeshKey?this.getMeshKey(mesh):(mesh&&mesh.userData&&mesh.userData.elementKey);if(!sourceKey||!mesh||!mesh.userData)continue;restoredKeys.add(sourceKey);mesh.userData.normalChunkSuppressed=false;if(this.requestTransientCompactFallbackEdge){this.requestTransientCompactFallbackEdge(mesh,'normal-chunk-cell-detached');}if(this.normalChunkHiddenKeys)this.normalChunkHiddenKeys.delete(sourceKey);if(this.normalChunksByElementKey){const mapped=this.normalChunksByElementKey.get(sourceKey);if(mapped){for(const removedChunk of chunksToRemove)mapped.delete(removedChunk);if(!mapped.size)this.normalChunksByElementKey.delete(sourceKey);}}}}this.normalChunkMeshes=(this.normalChunkMeshes||[]).filter(chunk=>!chunksToRemove.has(chunk));this.normalChunkVisible=this.normalChunkMeshes.length>0;this.normalChunkCacheSignature=null;if(this.removeNormalChunkEdgesForSpatialKeys){this.removeNormalChunkEdgesForSpatialKeys(impactedSpatialKeys,opts.dispose===true);}else if(this.clearNormalChunkEdges){this.clearNormalChunkEdges(opts.dispose===true);}for(const key of restoredKeys){const meshes=this.getMeshesForElementKey?this.getMeshesForElementKey(key):[];for(const mesh of meshes){if(!mesh||!mesh.userData)continue;if(keySet.has(key)||(this.hiddenKeys&&this.hiddenKeys.has(key))){mesh.userData.baseVisible=false;mesh.visible=false;for(const child of mesh.children||[])if(child)child.visible=false;}else if(!(mesh.userData.massTransparencyChunkSuppressed||mesh.userData.visualStateChunkSuppressed)){this.updateVisualAppearanceForMesh(mesh);}}}if(opts.deferRebuild===true&&this.deferNormalChunkRefresh&&this.normalChunkEnabled!==false){this.deferNormalChunkRefresh('normal-chunk-remove-hidden-keys');}if(this.recordPerformanceOperation)this.recordPerformanceOperation('normalChunks:remove-spatial-cells',0,{hiddenKeys:keySet.size,removedChunks:chunksToRemove.size,impactedSpatialCells:impactedSpatialKeys.size,restoredKeys:restoredKeys.size});return chunksToRemove.size;}getDetachedNormalChunkCellKeysForElementKeys(keys){const keySet=new Set(keys||[]);const result=new Set();if(!keySet.size||!this.detachedNormalChunkCells||!this.detachedNormalChunkCells.size)return result;for(const[spatialKey,sourceMeshes]of this.detachedNormalChunkCells.entries()){for(const mesh of sourceMeshes||[]){const sourceKey=this.getMeshKey?this.getMeshKey(mesh):(mesh&&mesh.userData&&mesh.userData.elementKey);if(sourceKey&&keySet.has(sourceKey)){result.add(spatialKey);break;}}}return result;}scheduleDetachedNormalChunkCellRefresh(keys,reason,delayMs){if(!this.normalChunkEnabled||!this.detachedNormalChunkCells||!this.detachedNormalChunkCells.size)return false;const spatialKeys=this.getDetachedNormalChunkCellKeysForElementKeys?this.getDetachedNormalChunkCellKeysForElementKeys(keys):new Set();if(!spatialKeys.size)return false;if(!this.normalChunkCellRefreshKeys)this.normalChunkCellRefreshKeys=new Set();for(const key of spatialKeys)this.normalChunkCellRefreshKeys.add(key);const generation=(this.normalChunkCellRefreshGeneration||0)+1;this.normalChunkCellRefreshGeneration=generation;if(this.normalChunkCellRefreshTimer)clearTimeout(this.normalChunkCellRefreshTimer);if(this.normalChunkCellRefreshIdleHandle&&typeof cancelIdleCallback==='function'){cancelIdleCallback(this.normalChunkCellRefreshIdleHandle);}this.normalChunkCellRefreshIdleHandle=null;const queueAttempt=wait=>{this.normalChunkCellRefreshTimer=setTimeout(()=>{this.normalChunkCellRefreshTimer=null;if(this.normalChunkCellRefreshGeneration!==generation)return;if(this.navigationPerformanceActive||this.dragging){queueAttempt(180);return;}const execute=()=>{this.normalChunkCellRefreshIdleHandle=null;if(this.normalChunkCellRefreshGeneration!==generation)return;if(this.navigationPerformanceActive||this.dragging){queueAttempt(180);return;}const pending=new Set(this.normalChunkCellRefreshKeys||[]);if(this.normalChunkCellRefreshKeys)this.normalChunkCellRefreshKeys.clear();if(pending.size&&this.rebuildDetachedNormalChunkCells){this.rebuildDetachedNormalChunkCells(pending,reason||'restore-hidden-local');}};if(typeof requestIdleCallback==='function'){this.normalChunkCellRefreshIdleHandle=requestIdleCallback(execute,{timeout:900});}else{execute();}},Math.max(0,Number(wait)||0));};queueAttempt(Math.max(80,Number(delayMs)||180));return true;}rebuildDetachedNormalChunkCells(spatialKeys,reason){const keySet=new Set(spatialKeys||[]);if(!keySet.size||!this.detachedNormalChunkCells||!this.detachedNormalChunkCells.size)return 0;if(!this.shouldUseNormalChunks||!this.shouldUseNormalChunks())return 0;if(this.visualMassState&&this.visualMassState.active)return 0;const metricsStart=this.getPerformanceNow?this.getPerformanceNow():Date.now();let createdChunks=0;let rebuiltCells=0;let skippedLargeCells=0;const maximumLocalSources=Math.max(240,Number(this.normalChunkLocalRefreshMaximumSources||900));for(const spatialKey of keySet){const stored=this.detachedNormalChunkCells.get(spatialKey);if(!stored||!stored.size){this.detachedNormalChunkCells.delete(spatialKey);continue;}const sourceMeshes=Array.from(stored).filter(mesh=>mesh&&mesh.isMesh&&mesh.userData);if(sourceMeshes.length>maximumLocalSources){skippedLargeCells++;continue;}const eligibleMeshes=sourceMeshes.filter(mesh=>{const sourceKey=this.getMeshKey?this.getMeshKey(mesh):mesh.userData.elementKey;return sourceKey&&mesh.userData.baseVisible!==false&&!(this.isNormalChunkInteractiveKey&&this.isNormalChunkInteractiveKey(sourceKey))&&(!this.isNormalChunkRenderableMesh||this.isNormalChunkRenderableMesh(mesh));});if(!eligibleMeshes.length)continue;const groups=this.collectNormalChunkSourceMeshesFromMeshes?this.collectNormalChunkSourceMeshesFromMeshes(eligibleMeshes):new Map();const cellSourceSet=new Set();const cellChunks=[];for(const bucket of groups.values()){if(!bucket||!bucket.meshes||!bucket.meshes.length||!bucket.segments||!bucket.segments.length)continue;const geometries=this.buildChunkGeometriesFromSegments(bucket.segments,{maxVerticesPerChunk:bucket.transparent?Number(this.normalChunkTransparentMaxVertices||60000):Number(this.normalChunkOpaqueMaxVertices||120000),maxIndicesPerChunk:bucket.transparent?Number(this.normalChunkTransparentMaxIndices||180000):Number(this.normalChunkOpaqueMaxIndices||360000),spatialPartition:false,forcedSpatialChunkKey:spatialKey});for(const geometry of geometries){const chunkMaterial=this.getDepthStableNormalChunkMaterial?this.getDepthStableNormalChunkMaterial(bucket.material):bucket.material;const chunk=new THREE.Mesh(geometry,chunkMaterial);chunk.renderOrder=0;chunk.frustumCulled=true;const chunkSources=geometry&&geometry.userData&&Array.isArray(geometry.userData.normalChunkSourceMeshes)?geometry.userData.normalChunkSourceMeshes.slice():[];const chunkKeys=new Set((geometry&&geometry.userData&&Array.isArray(geometry.userData.normalChunkSourceKeys))?geometry.userData.normalChunkSourceKeys:[]);let adaptiveMaxSourceRadius=0;let adaptiveHasEdgeCoverage=true;if(bucket.adaptiveDetailClass==='compact'){for(const sourceMesh of chunkSources){adaptiveMaxSourceRadius=Math.max(adaptiveMaxSourceRadius,this.getMeshWorldBoundingRadius(sourceMesh));const hasCoverage=this.hasReadableEdgeCoverageForMesh?this.hasReadableEdgeCoverageForMesh(sourceMesh):!!(sourceMesh&&sourceMesh.children&&sourceMesh.children.some(child=>child&&child.isLineSegments));if(!hasCoverage)adaptiveHasEdgeCoverage=false;}}chunk.userData=Object.assign({},chunk.userData||{},{isNormalChunk:true,renderProjectId:bucket.projectId||'',normalChunkSourceMeshes:chunkSources,normalChunkSourceKeys:chunkKeys,normalChunkIndexRangesByKey:geometry&&geometry.userData?(geometry.userData.normalChunkIndexRangesByKey||null):null,spatialChunkKey:spatialKey||'all',adaptiveDetailClass:bucket.adaptiveDetailClass||'full',adaptiveMaxSourceRadius,adaptiveHasEdgeCoverage,normalChunkTransparent:!!bucket.transparent,materialSegmentChunk:true,locallyRebuiltChunk:true});for(const sourceMesh of chunkSources){cellSourceSet.add(sourceMesh);const sourceKey=this.getMeshKey?this.getMeshKey(sourceMesh):(sourceMesh.userData&&sourceMesh.userData.elementKey);if(!sourceKey)continue;let mapped=this.normalChunksByElementKey&&this.normalChunksByElementKey.get(sourceKey);if(!mapped){mapped=new Set();if(!this.normalChunksByElementKey)this.normalChunksByElementKey=new Map();this.normalChunksByElementKey.set(sourceKey,mapped);}mapped.add(chunk);}this.scene.add(chunk);cellChunks.push(chunk);createdChunks++;}for(const mesh of bucket.meshes){const sourceKey=this.getMeshKey?this.getMeshKey(mesh):(mesh.userData&&mesh.userData.elementKey);if(!sourceKey||!mesh.userData)continue;mesh.userData.normalChunkSuppressed=true;mesh.visible=false;if(this.removeTransientCompactFallbackEdge)this.removeTransientCompactFallbackEdge(mesh,true);if(!this.normalChunkHiddenKeys)this.normalChunkHiddenKeys=new Set();this.normalChunkHiddenKeys.add(sourceKey);}}if(cellChunks.length){if(!this.normalChunkMeshes)this.normalChunkMeshes=[];this.normalChunkMeshes.push(...cellChunks);if(this.rebuildNormalChunkEdgesFromMeshes&&cellSourceSet.size){this.rebuildNormalChunkEdgesFromMeshes(Array.from(cellSourceSet),false,{appendExisting:true,forcedSpatialChunkKey:spatialKey});}this.detachedNormalChunkCells.delete(spatialKey);rebuiltCells++;}}this.normalChunkCacheSignature=null;this.normalChunkVisible=!!(this.normalChunkMeshes&&this.normalChunkMeshes.length);this.normalChunkSourceMeshCount=this.normalChunkHiddenKeys?this.normalChunkHiddenKeys.size:0;this.normalChunkEdgesVisibleOnly=false;if(this.updateAdaptiveMediumChunkVisibility)this.updateAdaptiveMediumChunkVisibility();if(this.applyStableNormalChunkEdgeAppearance&&this.stableNormalEdgeAppearanceMode){this.applyStableNormalChunkEdgeAppearance(this.stableNormalEdgeAppearanceMode);}if(this.requestRender)this.requestRender('normal-chunks-local-cell-refresh');if(this.recordPerformanceOperation)this.recordPerformanceOperation('normalChunks:local-cell-refresh',(this.getPerformanceNow?this.getPerformanceNow():Date.now())-metricsStart,{reason:reason||'',requestedCells:keySet.size,rebuiltCells,createdChunks,skippedLargeCells});return createdChunks;}removeVisualStateChunksContainingKeys(keys,options){const keySet=new Set(keys||[]);if(!keySet.size||!this.visualStateChunkMeshes||!this.visualStateChunkMeshes.length)return 0;const opts=options&&typeof options==='object'?options:{};const chunksToRemove=new Set();for(const chunk of this.visualStateChunkMeshes||[]){const sourceMeshes=chunk&&chunk.userData&&Array.isArray(chunk.userData.visualStateChunkSourceMeshes)?chunk.userData.visualStateChunkSourceMeshes:[];for(const mesh of sourceMeshes){const key=this.getMeshKey?this.getMeshKey(mesh):(mesh&&mesh.userData&&mesh.userData.elementKey);if(key&&keySet.has(key)){chunksToRemove.add(chunk);break;}}}if(!chunksToRemove.size)return 0;const restoredKeys=new Set();for(const chunk of chunksToRemove){if(!chunk)continue;if(chunk.parent)chunk.parent.remove(chunk);const sourceMeshes=chunk.userData&&Array.isArray(chunk.userData.visualStateChunkSourceMeshes)?chunk.userData.visualStateChunkSourceMeshes:[];for(const mesh of sourceMeshes){const sourceKey=this.getMeshKey?this.getMeshKey(mesh):(mesh&&mesh.userData&&mesh.userData.elementKey);if(!sourceKey||!mesh||!mesh.userData)continue;restoredKeys.add(sourceKey);mesh.userData.visualStateChunkSuppressed=false;if(this.requestTransientCompactFallbackEdge){this.requestTransientCompactFallbackEdge(mesh,'visual-state-chunk-detached');}if(this.visualStateChunkHiddenKeys)this.visualStateChunkHiddenKeys.delete(sourceKey);}}this.visualStateChunkMeshes=(this.visualStateChunkMeshes||[]).filter(chunk=>!chunksToRemove.has(chunk));if(this.removeVisualStateChunkEdgesForKeys){this.removeVisualStateChunkEdgesForKeys(restoredKeys,opts.dispose===true);}this.visualStateChunkVisible=this.visualStateChunkMeshes.length>0;this.visualStateChunkCacheSignature=null;for(const key of restoredKeys){const meshes=this.getMeshesForElementKey?this.getMeshesForElementKey(key):[];for(const mesh of meshes){if(!mesh||!mesh.userData)continue;if(keySet.has(key)||(this.hiddenKeys&&this.hiddenKeys.has(key))){mesh.userData.baseVisible=false;mesh.visible=false;if(mesh.children&&mesh.children.length){for(const child of mesh.children)if(child)child.visible=false;}}else if(!(mesh.userData.normalChunkSuppressed||mesh.userData.massTransparencyChunkSuppressed)){this.updateVisualAppearanceForMesh(mesh);}}}return chunksToRemove.size;}purgeMergedRenderResiduesForHiddenKeys(keys,options){const keySet=new Set(keys||[]);if(!keySet.size)return{normal:0,visualState:0,massTransparency:0,groupedTransparentEdges:0};const opts=options&&typeof options==='object'?options:{};const normal=this.removeNormalChunksContainingKeys?this.removeNormalChunksContainingKeys(keySet,opts):0;const normalEdges=this.removeNormalChunkEdgesForKeys?this.removeNormalChunkEdgesForKeys(keySet,opts.dispose===true):0;const visualState=this.removeVisualStateChunksContainingKeys?this.removeVisualStateChunksContainingKeys(keySet,opts):0;let massTransparency=0;let groupedTransparentEdges=0;const mass=this.visualMassState||null;const touchesMassBase=!!(mass&&mass.active&&mass.allKeys&&this.hasChunkKeyIntersection&&this.hasChunkKeyIntersection(keySet,mass.allKeys));const touchesMassChunks=!!(this.massTransparencyChunkHiddenKeys&&this.hasChunkKeyIntersection&&this.hasChunkKeyIntersection(keySet,this.massTransparencyChunkHiddenKeys));if(touchesMassBase||touchesMassChunks){if(opts.preserveMassTransparencyChunks===true&&this.maskMassTransparencyChunksForHiddenKeys){const maskResult=this.maskMassTransparencyChunksForHiddenKeys(keySet);massTransparency=Number(maskResult&&maskResult.changedChunks||0);if(maskResult&&maskResult.requiresRebuild){const removedMass=this.clearMassTransparencyChunks?this.clearMassTransparencyChunks(true):0;const rebuiltMass=this.rebuildMassTransparencyChunks?this.rebuildMassTransparencyChunks(true):0;massTransparency=Math.max(massTransparency,removedMass,rebuiltMass);}}else{massTransparency=this.clearMassTransparencyChunks?this.clearMassTransparencyChunks(true):0;this.massTransparencyChunkCacheSignature=null;}groupedTransparentEdges=this.removeGroupedTransparentEdgesForKeys?this.removeGroupedTransparentEdgesForKeys(keySet,true):(this.clearGroupedTransparentEdges?this.clearGroupedTransparentEdges(true):0);if(opts.preserveMassTransparencyChunks===true&&this.groupedTransparentEdgeBatches&&this.groupedTransparentEdgeBatches.length){this.groupedTransparentEdgeCacheSignature=this.getGroupedTransparentEdgesSignature?this.getGroupedTransparentEdgesSignature():null;for(const batch of this.groupedTransparentEdgeBatches){if(batch&&batch.userData)batch.userData.cacheSignature=this.groupedTransparentEdgeCacheSignature;}}else{this.groupedTransparentEdgeCacheSignature=null;}}return{normal,normalEdges,visualState,massTransparency,groupedTransparentEdges};}handleChunkBaseVisibilityChanges(changedKeys,reason){const keySet=new Set(changedKeys||[]);const result={clearedNormal:false,rebuiltMass:false,rebuiltVisualState:false};if(!keySet.size)return result;const metricsStart=this.getPerformanceNow?this.getPerformanceNow():Date.now();if(this.normalChunkVisible&&this.hasChunkKeyIntersection(keySet,this.normalChunkHiddenKeys)){this.clearNormalChunks(false);result.clearedNormal=true;}if(this.visualMassState&&this.visualMassState.active){const touchesMass=this.hasChunkKeyIntersection(keySet,this.massTransparencyChunkHiddenKeys)||this.hasChunkKeyIntersection(keySet,this.visualMassState.allKeys||new Set());if(touchesMass){const restoredKeys=new Set();for(const key of keySet){const meshes=this.getMeshesForElementKey?this.getMeshesForElementKey(key):[];if(meshes.some(mesh=>mesh&&mesh.userData&&mesh.userData.baseVisible!==false))restoredKeys.add(key);}if(this.clearMassTransparencyChunks)this.clearMassTransparencyChunks(true);this.massTransparencyChunkCacheSignature=null;if(this.rebuildMassTransparencyChunks)this.rebuildMassTransparencyChunks(true);if(restoredKeys.size){if(this.clearGroupedTransparentEdges)this.clearGroupedTransparentEdges(true);this.groupedTransparentEdgeCacheSignature=null;if(this.rebuildGroupedTransparentEdges)this.rebuildGroupedTransparentEdges(true);}result.rebuiltMass=true;}}if(this.visualStateChunkVisible&&this.hasChunkKeyIntersection(keySet,this.visualStateChunkHiddenKeys)){if(this.clearVisualStateChunks)this.clearVisualStateChunks(true);if(this.rebuildVisualStateChunks)this.rebuildVisualStateChunks(true);result.rebuiltVisualState=true;}if(result.clearedNormal&&!(this.visualMassState&&this.visualMassState.active)){if(this.deferNormalChunkRefresh)this.deferNormalChunkRefresh(reason||'base-visibility-changed');else if(this.rebuildNormalChunks)this.rebuildNormalChunks(false);}if(this.recordPerformanceOperation&&(result.clearedNormal||result.rebuiltMass||result.rebuiltVisualState)){this.recordPerformanceOperation('chunkVisibility:base-change',(this.getPerformanceNow?this.getPerformanceNow():Date.now())-metricsStart,{reason:reason||'base-visibility-changed',changedKeys:keySet.size,clearedNormal:result.clearedNormal,rebuiltMass:result.rebuiltMass,rebuiltVisualState:result.rebuiltVisualState});}return result;}hasVisualStateChunkableState(){return!!((this.visualColorOverrides&&this.visualColorOverrides.size)||(this.visualWorksStates&&this.visualWorksStates.size)||(this.visualClashStates&&this.visualClashStates.size));}shouldUseVisualStateChunks(){const massActive=!!(this.visualMassState&&this.visualMassState.active);const hasColorOverrides=!!(this.visualColorOverrides&&this.visualColorOverrides.size);return this.dataSpatialGroupingActive!==true&&this.normalChunkEnabled!==false&&this.meshes&&this.meshes.length>1500&&!this.isolateActive&&this.hasVisualStateChunkableState&&this.hasVisualStateChunkableState()&&(!massActive||hasColorOverrides);}isVisualStateChunkInteractiveKey(key){if(!key)return false;const selectionUsesSource=this.selectionUsesSourceAppearance?this.selectionUsesSourceAppearance():this.visualSelectionOverlayOnly!==true;if(selectionUsesSource&&this.selectedKeys&&this.selectedKeys.has(key))return true;if(this.visualHoverKey&&this.visualHoverKey===key)return true;if(this.rectanglePreviewKeys&&this.rectanglePreviewKeys.has(key))return true;if(this.clashPriorityKeys&&this.clashPriorityKeys.has(key))return true;return false;}getVisualStateEntrySignature(prefix,map){if(!map||!map.size)return prefix+':0:0';let hash=2166136261>>>0;let count=0;const entries=Array.from(map.entries()).sort((a,b)=>String(a[0]).localeCompare(String(b[0])));for(const[key,state]of entries){count++;const text=[prefix,key,state&&state.hidden?'hidden':'shown',state&&state.color==null?'':Number(state.color),state&&state.opacity==null?'':Number(state.opacity).toFixed(3),state&&state.edgeColor==null?'':Number(state.edgeColor),state&&state.edgeOpacity==null?'':Number(state.edgeOpacity).toFixed(3),state&&state.renderOrder==null?'':Number(state.renderOrder)].join(':');for(let i=0;i<text.length;i++){hash^=text.charCodeAt(i);hash=Math.imul(hash,16777619)>>>0;}hash^=124;hash=Math.imul(hash,16777619)>>>0;}return prefix+':'+count+':'+hash.toString(16);}getVisualStateChunkSignature(){if(!(this.shouldUseVisualStateChunks&&this.shouldUseVisualStateChunks()))return null;const baseVisibleKeys=new Set();for(const mesh of this.meshes||[]){if(!mesh||!mesh.isMesh||!mesh.userData||mesh.userData.baseVisible===false)continue;const key=this.getMeshKey?this.getMeshKey(mesh):mesh.userData.elementKey;if(key)baseVisibleKeys.add(key);}return['visual-state-v4-stable-edges','stableEdges',this.visualStateUseStableEdges===true?1:0,'rev',this.normalChunkRevision||0,'detail',this.getVisualDetailLevel?this.getVisualDetailLevel():'default','meshCount',this.meshes?this.meshes.length:0,'base',this.getKeySetFingerprint?this.getKeySetFingerprint(baseVisibleKeys):baseVisibleKeys.size,'interactive',this.getKeySetFingerprint?this.getKeySetFingerprint(this.getCurrentInteractiveElementKeys?this.getCurrentInteractiveElementKeys():[]):0,'colors',(this.visualColorOverrides&&this.visualColorOverrides.size)||0,'colorRev',this.visualColorOverridesRevision||0,'works',this.getVisualStateEntrySignature('works',this.visualWorksStates),'clash',this.getVisualStateEntrySignature('clash',this.visualClashStates),'force',this.getKeySetFingerprint?this.getKeySetFingerprint(this.visualClashForceVisibleKeys||[]):((this.visualClashForceVisibleKeys&&this.visualClashForceVisibleKeys.size)||0),'hidden',this.getKeySetFingerprint?this.getKeySetFingerprint(this.visualClashHiddenKeys||[]):((this.visualClashHiddenKeys&&this.visualClashHiddenKeys.size)||0)].join('|');}getVisualStateChunkDescriptor(mesh){if(!mesh||!mesh.isMesh||!mesh.userData)return null;if(this.isNonRenderedDataMesh(mesh))return null;const key=this.getMeshKey?this.getMeshKey(mesh):mesh.userData.elementKey;if(!key)return null;if(mesh.userData.baseVisible===false)return null;if(this.dataPanelFilterActive&&this.dataPanelFilterKeys&&!this.dataPanelFilterKeys.has(key))return null;if(this.visualClashHiddenKeys&&this.visualClashHiddenKeys.has(key))return null;if(this.isVisualStateChunkInteractiveKey&&this.isVisualStateChunkInteractiveKey(key))return null;const works=this.visualWorksStates&&this.visualWorksStates.get(key);if(works&&works.hidden)return null;const clash=this.visualClashStates&&this.visualClashStates.get(key);const data=this.visualColorOverrides&&this.visualColorOverrides.get(key);const massActive=!!(this.visualMassState&&this.visualMassState.active);const state=massActive?data:(clash||works||data);if(!state)return null;const color=Number(state.color==null?0xbec7d3:state.color);const opacity=Math.max(0.02,Math.min(1,Number(state.opacity==null?1:state.opacity)));const transparent=opacity<1;const edgeColor=state.edgeColor==null?this.darkenColorValue(color,0.3):Number(state.edgeColor);const edgeOpacity=Math.max(0.02,Math.min(1,Number(state.edgeOpacity==null?(opacity<1?Math.max(0.04,opacity):1):state.edgeOpacity)));const baseRenderOrder=mesh.userData.baseRenderOrder||0;const renderOrder=Number.isFinite(Number(state.renderOrder))?Number(state.renderOrder):(state===data&&!works&&!clash?baseRenderOrder+2:baseRenderOrder);const materialKey=['vstate',color.toString(16),opacity.toFixed(3),transparent?'T':'O',edgeColor.toString(16),edgeOpacity.toFixed(3),renderOrder].join(':');const projectId=this.getMeshRenderProjectId?this.getMeshRenderProjectId(mesh):'';return{materialKey,color,opacity,transparent,edgeColor,edgeOpacity,renderOrder,projectId};}collectVisualStateChunkSourceMeshes(){const groups=new Map();if(!(this.shouldUseVisualStateChunks&&this.shouldUseVisualStateChunks()))return groups;for(const mesh of this.meshes||[]){const descriptor=this.getVisualStateChunkDescriptor?this.getVisualStateChunkDescriptor(mesh):null;if(!descriptor)continue;const groupKey=descriptor.materialKey+'|project:'+(descriptor.projectId||'');let bucket=groups.get(groupKey);if(!bucket){bucket={descriptor,meshes:[],projectId:descriptor.projectId||''};groups.set(groupKey,bucket);}bucket.meshes.push(mesh);}return groups;}clearVisualStateChunks(dispose){if(this.restoreSelectionMaskedVisualStateChunks)this.restoreSelectionMaskedVisualStateChunks();const shouldDispose=dispose===true;let removed=0;if(this.visualStateChunkMeshes&&this.visualStateChunkMeshes.length){for(const chunk of this.visualStateChunkMeshes){if(!chunk)continue;if(chunk.parent)chunk.parent.remove(chunk);if(shouldDispose&&chunk.geometry&&chunk.geometry.dispose)chunk.geometry.dispose();removed++;}}if(this.visualStateChunkEdgeBatches&&this.visualStateChunkEdgeBatches.length){for(const batch of this.visualStateChunkEdgeBatches){if(!batch)continue;if(batch.parent)batch.parent.remove(batch);if(shouldDispose&&batch.geometry&&batch.geometry.dispose)batch.geometry.dispose();removed++;}}if(shouldDispose){this.visualStateChunkMeshes=[];this.visualStateChunkEdgeBatches=[];this.visualStateChunkCacheSignature=null;this.visualStateChunksByElementKey=new Map();}this.visualStateChunkVisible=false;for(const key of this.visualStateChunkHiddenKeys||[]){const meshes=this.getMeshesForElementKey?this.getMeshesForElementKey(key):[];for(const mesh of meshes){if(!mesh||!mesh.userData)continue;mesh.userData.visualStateChunkSuppressed=false;if(mesh.userData.selectionOverlaySuppressed)mesh.visible=false;else if(!(mesh.userData.normalChunkSuppressed||mesh.userData.massTransparencyChunkSuppressed))this.updateVisualAppearanceForMesh(mesh);}}if(this.visualStateChunkHiddenKeys)this.visualStateChunkHiddenKeys.clear();return removed;}async clearVisualStateChunksBatched(dispose,options){const opts=options&&typeof options==='object'?options:{};const shouldDispose=dispose===true;const report=typeof opts.onProgress==='function'?opts.onProgress:()=>{};const isCancelled=typeof opts.isCancelled==='function'?opts.isCancelled:()=>false;const batchSize=Math.max(300,Math.min(6000,Number(opts.batchSize)||2200));if(this.restoreSelectionMaskedVisualStateChunks)this.restoreSelectionMaskedVisualStateChunks();let removed=0;for(const chunk of this.visualStateChunkMeshes||[]){if(!chunk)continue;if(chunk.parent)chunk.parent.remove(chunk);if(shouldDispose&&chunk.geometry&&chunk.geometry.dispose)chunk.geometry.dispose();removed++;}for(const batch of this.visualStateChunkEdgeBatches||[]){if(!batch)continue;if(batch.parent)batch.parent.remove(batch);if(shouldDispose&&batch.geometry&&batch.geometry.dispose)batch.geometry.dispose();removed++;}if(shouldDispose){this.visualStateChunkMeshes=[];this.visualStateChunkEdgeBatches=[];this.visualStateChunkCacheSignature=null;this.visualStateChunksByElementKey=new Map();}this.visualStateChunkVisible=false;const updatedKeysTarget=opts.updatedKeysTarget&&typeof opts.updatedKeysTarget.add==='function'?opts.updatedKeysTarget:null;const hiddenKeys=Array.from(this.visualStateChunkHiddenKeys||[]);for(let start=0;start<hiddenKeys.length;start+=batchSize){if(isCancelled())return false;const end=Math.min(hiddenKeys.length,start+batchSize);for(let index=start;index<end;index++){const key=hiddenKeys[index];const meshes=this.getMeshesForElementKey?this.getMeshesForElementKey(key):[];for(const mesh of meshes){if(!mesh||!mesh.userData)continue;mesh.userData.visualStateChunkSuppressed=false;if(mesh.userData.selectionOverlaySuppressed)mesh.visible=false;else if(!(mesh.userData.normalChunkSuppressed||mesh.userData.massTransparencyChunkSuppressed))this.updateVisualAppearanceForMesh(mesh);}if(updatedKeysTarget)updatedKeysTarget.add(key);}report(end,hiddenKeys.length,opts.progressMessage||'Restaurando superficies coloreadas...');if(end<hiddenKeys.length&&opts.yieldBetweenBatches!==false){if(this.waitForResetProgressPaint)await this.waitForResetProgressPaint();else if(this.yieldDuringLoad)await this.yieldDuringLoad();else await new Promise(resolve=>setTimeout(resolve,0));}}if(this.visualStateChunkHiddenKeys)this.visualStateChunkHiddenKeys.clear();return removed;}buildVisualStateChunkEdgesFromNormalBatches(groups){const sourceBatches=Array.from(this.normalChunkEdgeBatches||[]).filter(batch=>batch&&batch.geometry);if(!groups||!groups.size||!sourceBatches.length)return null;for(const sourceBatch of sourceBatches){const position=sourceBatch.geometry.getAttribute&&sourceBatch.geometry.getAttribute('position');const ranges=sourceBatch.userData&&sourceBatch.userData.edgeFloatRangesByKey;const mapped=Number(sourceBatch.userData&&sourceBatch.userData.edgeMappedFloatValues)||0;if(!position||!position.array||!(ranges instanceof Map)||mapped<position.array.length)return null;}const descriptorByKey=new Map();for(const[groupKey,bucket]of groups.entries()){if(!bucket||!bucket.descriptor)continue;for(const mesh of bucket.meshes||[]){const key=this.getMeshKey?this.getMeshKey(mesh):(mesh&&mesh.userData&&mesh.userData.elementKey);if(key)descriptorByKey.set(key,{groupKey,bucket});}}const outputGroups=new Map();const coveredKeys=new Set();for(const sourceBatch of sourceBatches){const position=sourceBatch.geometry.getAttribute('position');const sourceArray=position.array;const spatialKey=sourceBatch.userData?(sourceBatch.userData.spatialChunkKey||'all'):'all';const projectId=sourceBatch.userData?(sourceBatch.userData.renderProjectId||''):'';for(const[key,ranges]of sourceBatch.userData.edgeFloatRangesByKey.entries()){const target=descriptorByKey.get(key);if(!target)continue;const outputKey=target.groupKey+'|project:'+projectId+'|cell:'+spatialKey;let output=outputGroups.get(outputKey);if(!output){output={descriptor:target.bucket.descriptor,projectId,spatialKey,arrays:[],count:0};outputGroups.set(outputKey,output);}for(let i=0;i+1<ranges.length;i+=2){const start=Number(ranges[i])||0;const count=Number(ranges[i+1])||0;if(!(count>0)||start<0||start+count>sourceArray.length)continue;output.arrays.push({positions:sourceArray.subarray(start,start+count),sourceKey:key});output.count+=count;coveredKeys.add(key);}}}const batches=[];for(const output of outputGroups.values()){if(!output||!output.count)continue;const positions=new Float32Array(output.count);const rangeMap=new Map();let cursor=0;for(const item of output.arrays){const array=item.positions;positions.set(array,cursor);if(item.sourceKey&&this.appendEdgeFloatRange)this.appendEdgeFloatRange(rangeMap,item.sourceKey,cursor,array.length);cursor+=array.length;}const geometry=new THREE.BufferGeometry();geometry.setAttribute('position',new THREE.BufferAttribute(positions,3));geometry.computeBoundingBox();geometry.computeBoundingSphere();const descriptor=output.descriptor;const material=this.getSimpleEdgeMaterial(descriptor.edgeColor,descriptor.edgeOpacity,descriptor.edgeOpacity<1||descriptor.transparent);const batch=new THREE.LineSegments(geometry,material);batch.renderOrder=(descriptor.renderOrder||0)+1;batch.frustumCulled=true;batch.userData=Object.assign({},batch.userData||{},{isVisualStateChunkEdgeBatch:true,derivedFromNormalEdgeBatch:true,renderProjectId:output.projectId||'',spatialChunkKey:output.spatialKey||'all',edgeFloatValues:output.count,edgeSegments:Math.floor(output.count/6),edgeFloatRangesByKey:rangeMap,edgeMappedFloatValues:output.count});this.scene.add(batch);batches.push(batch);}const missingKeys=new Set();for(const key of descriptorByKey.keys()){if(!coveredKeys.has(key))missingKeys.add(key);}if(missingKeys.size&&this.buildVisualStateChunkEdgesFromSourceGroups){const fallbackBatches=this.buildVisualStateChunkEdgesFromSourceGroups(groups,missingKeys);for(const batch of fallbackBatches)batches.push(batch);}this.lastVisualStateEdgeCoverage={desiredKeys:descriptorByKey.size,reusedKeys:coveredKeys.size,fallbackKeys:missingKeys.size,reusedBatches:batches.filter(batch=>batch&&batch.userData&&batch.userData.derivedFromNormalEdgeBatch).length,fallbackBatches:batches.filter(batch=>batch&&batch.userData&&batch.userData.derivedFromSourceEdges).length};return batches;}buildVisualStateChunkEdgesFromSourceGroups(groups,includeKeys){const batches=[];if(!groups||!groups.size||!this.getVisualDetailLevel)return batches;const requestedKeys=includeKeys&&typeof includeKeys.has==='function'?includeKeys:null;const temp=new THREE.Vector3();for(const bucket of groups.values()){if(!bucket||!bucket.meshes||!bucket.meshes.length||!bucket.descriptor)continue;const sourceMeshes=requestedKeys?bucket.meshes.filter(mesh=>{const key=this.getMeshKey?this.getMeshKey(mesh):(mesh&&mesh.userData&&mesh.userData.elementKey);return!!(key&&requestedKeys.has(key));}):bucket.meshes;if(!sourceMeshes.length)continue;const spatialBuckets=this.partitionMeshesForSpatialChunks?this.partitionMeshesForSpatialChunks(sourceMeshes,{spatialMinimumMeshes:40}):[{key:'all',meshes:sourceMeshes}];for(const spatialBucket of spatialBuckets){const arrays=[];let count=0;for(const mesh of spatialBucket.meshes||[]){if(!mesh||!mesh.isMesh)continue;mesh.updateWorldMatrix(true,false);const edgeSources=this.getNormalChunkEdgeSourcesForMesh?this.getNormalChunkEdgeSourcesForMesh(mesh):[];for(const edgeSource of edgeSources){if(!edgeSource||!edgeSource.geometry)continue;const position=edgeSource.geometry.getAttribute&&edgeSource.geometry.getAttribute('position');if(!position||!position.count)continue;const transformObject=edgeSource.transformObject||mesh;transformObject.updateWorldMatrix&&transformObject.updateWorldMatrix(true,false);const transformed=this.buildTransformedLineSegmentPositions?this.buildTransformedLineSegmentPositions(edgeSource.geometry,transformObject.matrixWorld||mesh.matrixWorld,temp):new Float32Array(0);if(!transformed.length)continue;const sourceKey=this.getMeshKey?this.getMeshKey(mesh):(mesh.userData&&mesh.userData.elementKey);arrays.push({positions:transformed,sourceKey});count+=transformed.length;}}if(!count)continue;const merged=new Float32Array(count);const rangeMap=new Map();let offset=0;for(const item of arrays){const arr=item.positions;merged.set(arr,offset);if(item.sourceKey&&this.appendEdgeFloatRange)this.appendEdgeFloatRange(rangeMap,item.sourceKey,offset,arr.length);offset+=arr.length;}const geometry=new THREE.BufferGeometry();geometry.setAttribute('position',new THREE.BufferAttribute(merged,3));geometry.computeBoundingBox();geometry.computeBoundingSphere();const descriptor=bucket.descriptor;const material=this.getSimpleEdgeMaterial(descriptor.edgeColor,descriptor.edgeOpacity,descriptor.edgeOpacity<1||descriptor.transparent);const batch=new THREE.LineSegments(geometry,material);batch.renderOrder=(descriptor.renderOrder||0)+1;batch.frustumCulled=true;batch.userData=Object.assign({},batch.userData||{},{isVisualStateChunkEdgeBatch:true,derivedFromSourceEdges:true,fallbackMissingEdges:!!requestedKeys,renderProjectId:bucket.projectId||'',spatialChunkKey:spatialBucket.key||'all',edgeFloatValues:count,edgeSegments:Math.floor(count/6),edgeFloatRangesByKey:rangeMap,edgeMappedFloatValues:count});if(this.navigationPerformanceActive&&this.getVisualDetailLevel()!=='medium'){if(!this.navigationEdgeVisibilityStates)this.navigationEdgeVisibilityStates=new Map();this.navigationEdgeVisibilityStates.set(batch,true);batch.visible=false;}this.scene.add(batch);batches.push(batch);}}return batches;}buildVisualStateChunkEdgesFromGroups(groups){const derivedBatches=this.buildVisualStateChunkEdgesFromNormalBatches?this.buildVisualStateChunkEdgesFromNormalBatches(groups):null;if(derivedBatches!==null)return derivedBatches;const batches=this.buildVisualStateChunkEdgesFromSourceGroups?this.buildVisualStateChunkEdgesFromSourceGroups(groups):[];let desiredKeys=0;if(groups&&groups.size){const keys=new Set();for(const bucket of groups.values()){for(const mesh of(bucket&&bucket.meshes)||[]){const key=this.getMeshKey?this.getMeshKey(mesh):(mesh&&mesh.userData&&mesh.userData.elementKey);if(key)keys.add(key);}}desiredKeys=keys.size;}this.lastVisualStateEdgeCoverage={desiredKeys,reusedKeys:0,fallbackKeys:desiredKeys,reusedBatches:0,fallbackBatches:batches.length};return batches;}rebuildVisualStateChunks(forceRebuild){const metricsStart=this.getPerformanceNow?this.getPerformanceNow():Date.now();if(!(this.shouldUseVisualStateChunks&&this.shouldUseVisualStateChunks())){this.clearVisualStateChunks(false);return 0;}const signature=this.getVisualStateChunkSignature?this.getVisualStateChunkSignature():null;const sameSignature=!forceRebuild&&signature&&signature===this.visualStateChunkCacheSignature&&this.visualStateChunkMeshes&&this.visualStateChunkMeshes.length;this.clearVisualStateChunks(sameSignature?false:true);if(sameSignature){for(const chunk of this.visualStateChunkMeshes||[]){if(!chunk)continue;const sourceMeshes=chunk.userData&&Array.isArray(chunk.userData.visualStateChunkSourceMeshes)?chunk.userData.visualStateChunkSourceMeshes:[];for(const mesh of sourceMeshes){const key=this.getMeshKey?this.getMeshKey(mesh):(mesh&&mesh.userData&&mesh.userData.elementKey);if(!key||!mesh||!mesh.userData)continue;if(this.isVisualStateChunkInteractiveKey&&this.isVisualStateChunkInteractiveKey(key))continue;mesh.userData.visualStateChunkSuppressed=true;mesh.visible=false;if(this.removeTransientCompactFallbackEdge)this.removeTransientCompactFallbackEdge(mesh,true);this.visualStateChunkHiddenKeys.add(key);}chunk.visible=true;this.scene.add(chunk);}for(const batch of this.visualStateChunkEdgeBatches||[]){if(!batch)continue;batch.visible=true;this.scene.add(batch);}this.visualStateChunkVisible=true;this.visualStateChunkReuseCount=(this.visualStateChunkReuseCount||0)+1;if(this.recordPerformanceOperation)this.recordPerformanceOperation('visualStateChunks:reuse',(this.getPerformanceNow?this.getPerformanceNow():Date.now())-metricsStart,{chunks:this.visualStateChunkMeshes.length,edges:this.visualStateChunkEdgeBatches?this.visualStateChunkEdgeBatches.length:0});return this.visualStateChunkMeshes.length;}const groups=this.collectVisualStateChunkSourceMeshes?this.collectVisualStateChunkSourceMeshes():new Map();const chunks=[];this.visualStateChunksByElementKey=new Map();for(const bucket of groups.values()){if(!bucket||!bucket.meshes||!bucket.meshes.length||!bucket.descriptor)continue;const descriptor=bucket.descriptor;const material=this.getSimpleColorMaterial(descriptor.color,descriptor.opacity,descriptor.transparent);const geometries=this.buildChunkGeometriesFromMeshes(bucket.meshes,{useSingleDraw:true,maxVerticesPerChunk:120000,maxIndicesPerChunk:360000,spatialMinimumMeshes:24,targetMeshesPerCell:180,maximumCells:Number(this.spatialChunkMaximumCells||512)});for(const geometry of geometries){const chunk=new THREE.Mesh(geometry,material);chunk.renderOrder=descriptor.renderOrder||0;chunk.frustumCulled=true;const sourceMeshes=geometry&&geometry.userData&&Array.isArray(geometry.userData.normalChunkSourceMeshes)?geometry.userData.normalChunkSourceMeshes.slice():[];chunk.userData=Object.assign({},chunk.userData||{},{isVisualStateChunk:true,renderProjectId:bucket.projectId||'',visualStateChunkSourceMeshes:sourceMeshes,visualStateChunkIndexRangesByKey:geometry&&geometry.userData?(geometry.userData.normalChunkIndexRangesByKey||null):null,visualStateChunkDescriptor:descriptor,spatialChunkKey:geometry&&geometry.userData?(geometry.userData.spatialChunkKey||'all'):'all'});for(const sourceMesh of sourceMeshes){const sourceKey=this.getMeshKey?this.getMeshKey(sourceMesh):(sourceMesh&&sourceMesh.userData&&sourceMesh.userData.elementKey);if(!sourceKey)continue;let list=this.visualStateChunksByElementKey.get(sourceKey);if(!list){list=new Set();this.visualStateChunksByElementKey.set(sourceKey,list);}list.add(chunk);}this.scene.add(chunk);chunks.push(chunk);}for(const mesh of bucket.meshes){const key=this.getMeshKey?this.getMeshKey(mesh):(mesh&&mesh.userData&&mesh.userData.elementKey);if(!key||!mesh||!mesh.userData)continue;if(this.isVisualStateChunkInteractiveKey&&this.isVisualStateChunkInteractiveKey(key))continue;mesh.userData.visualStateChunkSuppressed=true;mesh.visible=false;if(this.removeTransientCompactFallbackEdge)this.removeTransientCompactFallbackEdge(mesh,true);this.visualStateChunkHiddenKeys.add(key);}}this.visualStateChunkMeshes=chunks;this.visualStateChunkEdgeBatches=this.visualStateUseStableEdges===true?[]:(this.buildVisualStateChunkEdgesFromGroups?this.buildVisualStateChunkEdgesFromGroups(groups):[]);this.visualStateChunkCacheSignature=signature;this.visualStateChunkVisible=chunks.length>0;this.visualStateChunkBuildCount=(this.visualStateChunkBuildCount||0)+1;if(this.recordPerformanceOperation)this.recordPerformanceOperation('visualStateChunks:build',(this.getPerformanceNow?this.getPerformanceNow():Date.now())-metricsStart,{chunks:chunks.length,edgeBatches:this.visualStateChunkEdgeBatches?this.visualStateChunkEdgeBatches.length:0,groups:groups.size,hiddenKeys:this.visualStateChunkHiddenKeys?this.visualStateChunkHiddenKeys.size:0});return chunks.length;}restoreSelectionMaskedVisualStateChunks(options){const opts=options&&typeof options==='object'?options:{};const keepSelected=new Set(opts.keepSelectedKeys||[]);const maskedChunks=this.selectionMaskedVisualStateChunks||new Map();const sourceKeys=this.selectionMaskedVisualStateSourceKeys||new Set();for(const[chunk,state]of maskedChunks.entries()){if(!chunk||!state||!chunk.geometry)continue;if(state.originalIndexAttribute&&chunk.geometry.index!==state.originalIndexAttribute)chunk.geometry.setIndex(state.originalIndexAttribute);if(state.originalVisible!=null)chunk.visible=state.originalVisible;if(chunk.userData)chunk.userData.selectionIndexMasked=false;}maskedChunks.clear();for(const key of sourceKeys){if(!key||keepSelected.has(key))continue;const chunks=this.visualStateChunksByElementKey&&this.visualStateChunksByElementKey.get(key);const hasLiveChunk=!!(chunks&&Array.from(chunks).some(chunk=>chunk&&chunk.parent));const meshes=this.getMeshesForElementKey?this.getMeshesForElementKey(key):[];for(const mesh of meshes){if(!mesh||!mesh.userData)continue;if(hasLiveChunk&&mesh.userData.baseVisible!==false&&!(this.hiddenKeys&&this.hiddenKeys.has(key))){mesh.userData.visualStateChunkSuppressed=true;mesh.visible=false;if(this.visualStateChunkHiddenKeys)this.visualStateChunkHiddenKeys.add(key);}else if(!(mesh.userData.normalChunkSuppressed||mesh.userData.massTransparencyChunkSuppressed)){mesh.userData.visualStateChunkSuppressed=false;this.updateVisualAppearanceForMesh&&this.updateVisualAppearanceForMesh(mesh);}}}sourceKeys.clear();}applySelectionMaskToVisualStateChunk(chunk,selectedKeys){if(!chunk||!chunk.geometry||!chunk.geometry.index||!selectedKeys||!selectedKeys.size)return false;const rangesByKey=chunk.userData&&chunk.userData.visualStateChunkIndexRangesByKey;if(!rangesByKey||typeof rangesByKey.get!=='function')return false;const originalIndexAttribute=chunk.geometry.index;const originalArray=originalIndexAttribute.array;if(!originalArray||!originalArray.length)return false;const ranges=[];for(const key of selectedKeys){const keyRanges=rangesByKey.get(key);if(!Array.isArray(keyRanges))continue;for(const pair of keyRanges){const start=Math.max(0,Number(pair&&pair[0])||0);const count=Math.max(0,Number(pair&&pair[1])||0);if(count>0&&start<originalArray.length)ranges.push([start,Math.min(originalArray.length,start+count)]);}}if(!ranges.length)return false;ranges.sort((a,b)=>a[0]-b[0]);const mergedRanges=[];for(const range of ranges){if(!mergedRanges.length||range[0]>mergedRanges[mergedRanges.length-1][1])mergedRanges.push(range.slice());else mergedRanges[mergedRanges.length-1][1]=Math.max(mergedRanges[mergedRanges.length-1][1],range[1]);}let excluded=0;for(const range of mergedRanges)excluded+=Math.max(0,range[1]-range[0]);const nextLength=Math.max(0,originalArray.length-excluded);const ArrayType=originalArray.constructor||Uint32Array;const filtered=new ArrayType(nextLength);let sourceCursor=0;let targetCursor=0;for(const range of mergedRanges){if(range[0]>sourceCursor){const part=originalArray.subarray(sourceCursor,range[0]);filtered.set(part,targetCursor);targetCursor+=part.length;}sourceCursor=Math.max(sourceCursor,range[1]);}if(sourceCursor<originalArray.length)filtered.set(originalArray.subarray(sourceCursor),targetCursor);if(!this.selectionMaskedVisualStateChunks)this.selectionMaskedVisualStateChunks=new Map();this.selectionMaskedVisualStateChunks.set(chunk,{originalIndexAttribute,originalVisible:chunk.visible});if(chunk.userData)chunk.userData.selectionIndexMasked=true;if(nextLength>0)chunk.geometry.setIndex(new THREE.BufferAttribute(filtered,1));else chunk.visible=false;return true;}syncSelectionVisualStateChunkMasks(){const selectionUsesSource=this.selectionUsesSourceAppearance?this.selectionUsesSourceAppearance():this.visualSelectionOverlayOnly!==true;const selectedKeys=this.selectedKeys?new Set(this.selectedKeys):new Set();this.restoreSelectionMaskedVisualStateChunks&&this.restoreSelectionMaskedVisualStateChunks({keepSelectedKeys:selectedKeys});if(!selectedKeys.size||!this.visualStateChunkVisible||!this.visualStateChunksByElementKey)return new Set();const chunksToMask=new Set();const fallbackKeys=new Set();for(const key of selectedKeys){const chunks=this.visualStateChunksByElementKey.get(key);if(!chunks||!chunks.size)continue;let hasMetadata=false;for(const chunk of chunks){if(!chunk||!chunk.parent)continue;const ranges=chunk.userData&&chunk.userData.visualStateChunkIndexRangesByKey;if(ranges&&typeof ranges.get==='function'&&ranges.get(key)){hasMetadata=true;chunksToMask.add(chunk);}}if(!hasMetadata)fallbackKeys.add(key);}const maskedKeys=new Set();for(const chunk of chunksToMask){const sourceMeshes=chunk.userData&&Array.isArray(chunk.userData.visualStateChunkSourceMeshes)?chunk.userData.visualStateChunkSourceMeshes:[];const selectedInChunk=new Set();for(const mesh of sourceMeshes){const key=this.getMeshKey?this.getMeshKey(mesh):(mesh&&mesh.userData&&mesh.userData.elementKey);if(key&&selectedKeys.has(key))selectedInChunk.add(key);}if(!selectedInChunk.size)continue;if(this.applySelectionMaskToVisualStateChunk&&this.applySelectionMaskToVisualStateChunk(chunk,selectedInChunk)){for(const key of selectedInChunk)maskedKeys.add(key);}else{for(const key of selectedInChunk)fallbackKeys.add(key);}}if(!this.selectionMaskedVisualStateSourceKeys)this.selectionMaskedVisualStateSourceKeys=new Set();for(const key of maskedKeys){if(fallbackKeys.has(key))continue;this.selectionMaskedVisualStateSourceKeys.add(key);if(!selectionUsesSource)continue;if(this.visualStateChunkHiddenKeys)this.visualStateChunkHiddenKeys.delete(key);const meshes=this.getMeshesForElementKey?this.getMeshesForElementKey(key):[];for(const mesh of meshes){if(!mesh||!mesh.userData||mesh.userData.baseVisible===false)continue;mesh.userData.visualStateChunkSuppressed=false;if(!(mesh.userData.normalChunkSuppressed||mesh.userData.massTransparencyChunkSuppressed))this.updateVisualAppearanceForMesh&&this.updateVisualAppearanceForMesh(mesh);}}return selectionUsesSource?fallbackKeys:new Set();}refreshChunkRenderSystems(changedKeys,forceNormalRebuild){if(this.clashModeActive===true){if(this.shouldUseVisualStateChunks&&this.shouldUseVisualStateChunks()){if(this.suspendNormalChunksForClashMode)this.suspendNormalChunksForClashMode('refresh-chunk-render-systems');if(forceNormalRebuild||!this.visualStateChunkVisible||!this.visualStateChunkMeshes||!this.visualStateChunkMeshes.length){this.rebuildVisualStateChunks&&this.rebuildVisualStateChunks(!!forceNormalRebuild);}}if(this.recordPerformanceOperation)this.recordPerformanceOperation('normalChunks:refresh-skip-clash-mode',0,{changedKeys:changedKeys&&typeof changedKeys.size==='number'?changedKeys.size:(Array.isArray(changedKeys)?changedKeys.length:0),forceNormalRebuild:!!forceNormalRebuild});return;}const changedKeySet=new Set(changedKeys||[]);let visualStateSelectionHandledIncrementally=false;if(this.visualStateChunkVisible&&this.hasChunkKeyIntersection&&this.hasChunkKeyIntersection(changedKeySet,this.visualStateChunkHiddenKeys)){const previouslyMasked=new Set(this.selectionMaskedVisualStateSourceKeys||[]);let selectionOnly=changedKeySet.size>0;for(const key of changedKeySet){if(!(this.selectedKeys&&this.selectedKeys.has(key))&&!previouslyMasked.has(key)){selectionOnly=false;break;}}if(selectionOnly&&this.syncSelectionVisualStateChunkMasks){const fallback=this.syncSelectionVisualStateChunkMasks();if(fallback&&fallback.size)this.clearVisualStateChunks(false);else visualStateSelectionHandledIncrementally=true;}else{this.clearVisualStateChunks(false);}}else if(this.visualStateChunkVisible&&this.selectionMaskedVisualStateSourceKeys&&this.selectionMaskedVisualStateSourceKeys.size&&this.syncSelectionVisualStateChunkMasks){const fallback=this.syncSelectionVisualStateChunkMasks();visualStateSelectionHandledIncrementally=!(fallback&&fallback.size);}if(this.visualMassState&&this.visualMassState.active){const useHybridColorChunks=!!(this.shouldUseVisualStateChunks&&this.shouldUseVisualStateChunks());if(!useHybridColorChunks)this.clearVisualStateChunks&&this.clearVisualStateChunks(false);const useStableEdges=this.shouldReuseStableNormalEdgesForMassTransparency&&this.shouldReuseStableNormalEdgesForMassTransparency();this.massTransparencyUseStableEdges=!!useStableEdges;this.visualStateUseStableEdges=false;if(useStableEdges){this.suspendNormalChunkSurfacesPreserveEdges&&this.suspendNormalChunkSurfacesPreserveEdges('mass-transparency');if(this.groupedTransparentEdgeBatches&&this.groupedTransparentEdgeBatches.length)this.clearGroupedTransparentEdges&&this.clearGroupedTransparentEdges(false);this.applyStableNormalChunkEdgeAppearance&&this.applyStableNormalChunkEdgeAppearance('mass');}else{this.clearNormalChunks(false);}if(this.rebuildMassTransparencyChunks)this.rebuildMassTransparencyChunks(false);if(useHybridColorChunks&&this.rebuildVisualStateChunks&&!(visualStateSelectionHandledIncrementally&&this.visualStateChunkVisible)){this.rebuildVisualStateChunks(!!forceNormalRebuild);}if(!useStableEdges&&this.rebuildGroupedTransparentEdges)this.rebuildGroupedTransparentEdges(false);return;}this.massTransparencyUseStableEdges=false;this.clearMassTransparencyChunks(false);if(this.groupedTransparentEdgeBatches&&this.groupedTransparentEdgeBatches.length)this.clearGroupedTransparentEdges&&this.clearGroupedTransparentEdges(false);if(this.shouldUseVisualStateChunks&&this.shouldUseVisualStateChunks()){if(visualStateSelectionHandledIncrementally&&this.visualStateChunkVisible){return;}const useStableEdges=this.shouldReuseStableNormalEdgesForVisualState&&this.shouldReuseStableNormalEdgesForVisualState();this.visualStateUseStableEdges=!!useStableEdges;if(useStableEdges){this.suspendNormalChunkSurfacesPreserveEdges&&this.suspendNormalChunkSurfacesPreserveEdges('visual-color');this.applyStableNormalChunkEdgeAppearance&&this.applyStableNormalChunkEdgeAppearance('color');}else{this.clearNormalChunks(false);}this.rebuildVisualStateChunks&&this.rebuildVisualStateChunks(!!forceNormalRebuild);return;}this.visualStateUseStableEdges=false;this.restoreStableNormalChunkEdgeAppearance&&this.restoreStableNormalChunkEdgeAppearance();this.clearVisualStateChunks&&this.clearVisualStateChunks(false);if(!this.shouldUseNormalChunks()){this.clearNormalChunks(false);return;}if(forceNormalRebuild||!this.normalChunkVisible||!this.normalChunkMeshes||!this.normalChunkMeshes.length)this.rebuildNormalChunks(!!forceNormalRebuild);else this.syncNormalChunksForKeys(changedKeys);}rebuildMassTransparencyChunkElementIndex(){const index=new Map();for(const chunk of this.massTransparencyChunkMeshes||[]){if(!chunk)continue;const keys=chunk.userData&&Array.isArray(chunk.userData.massTransparencyChunkSourceKeys)?chunk.userData.massTransparencyChunkSourceKeys:(chunk.geometry&&chunk.geometry.userData&&Array.isArray(chunk.geometry.userData.massTransparencyChunkSourceKeys)?chunk.geometry.userData.massTransparencyChunkSourceKeys:[]);for(const key of keys){if(!key)continue;let chunks=index.get(key);if(!chunks){chunks=new Set();index.set(key,chunks);}chunks.add(chunk);}}this.massTransparencyChunksByElementKey=index;return index;}getMassTransparencyChunksForElementKeys(keys){const keySet=new Set(keys||[]);const chunks=new Set();if(!keySet.size||!this.massTransparencyChunkMeshes||!this.massTransparencyChunkMeshes.length)return chunks;if(!this.massTransparencyChunksByElementKey||typeof this.massTransparencyChunksByElementKey.get!=='function'||!this.massTransparencyChunksByElementKey.size){this.rebuildMassTransparencyChunkElementIndex&&this.rebuildMassTransparencyChunkElementIndex();}for(const key of keySet){const mapped=this.massTransparencyChunksByElementKey&&this.massTransparencyChunksByElementKey.get(key);if(mapped&&mapped.size)for(const chunk of mapped)if(chunk)chunks.add(chunk);}return chunks;}applyMassTransparencyChunkHiddenMask(chunk,maskKeys){if(!chunk||!chunk.geometry)return{changed:false,complete:false,empty:false};if(!chunk.userData)chunk.userData={};const geometry=chunk.geometry;const sourceKeys=new Set(chunk.userData.massTransparencyChunkSourceKeys||[]);const requestedMask=new Set();for(const key of maskKeys||[])if(key&&sourceKeys.has(key))requestedMask.add(key);const originalIndexAttribute=chunk.userData.massTransparencyOriginalIndexAttribute||geometry.index;const originalArray=originalIndexAttribute&&originalIndexAttribute.array;const rangesByKey=chunk.userData.massTransparencyChunkIndexRangesByKey||(geometry.userData&&geometry.userData.normalChunkIndexRangesByKey)||null;if(!originalIndexAttribute||!originalArray||!originalArray.length||!rangesByKey||typeof rangesByKey.get!=='function'){return{changed:false,complete:requestedMask.size===0,empty:false};}chunk.userData.massTransparencyOriginalIndexAttribute=originalIndexAttribute;chunk.userData.massTransparencyChunkIndexRangesByKey=rangesByKey;const previousMask=chunk.userData.massTransparencyHiddenMaskKeys instanceof Set?chunk.userData.massTransparencyHiddenMaskKeys:new Set(chunk.userData.massTransparencyHiddenMaskKeys||[]);let sameMask=previousMask.size===requestedMask.size;if(sameMask)for(const key of requestedMask)if(!previousMask.has(key)){sameMask=false;break;}if(sameMask)return{changed:false,complete:true,empty:chunk.visible===false&&requestedMask.size>0};if(!requestedMask.size){if(geometry.index!==originalIndexAttribute)geometry.setIndex(originalIndexAttribute);chunk.userData.massTransparencyHiddenMaskKeys=new Set();chunk.userData.massTransparencyBaseVisibilityMasked=false;chunk.visible=true;return{changed:true,complete:true,empty:false};}const ranges=[];for(const key of requestedMask){const keyRanges=rangesByKey.get(key);if(!Array.isArray(keyRanges)||!keyRanges.length){return{changed:false,complete:false,empty:false};}for(const pair of keyRanges){const start=Math.max(0,Number(pair&&pair[0])||0);const count=Math.max(0,Number(pair&&pair[1])||0);if(count>0&&start<originalArray.length)ranges.push([start,Math.min(originalArray.length,start+count)]);}}if(!ranges.length)return{changed:false,complete:false,empty:false};ranges.sort((a,b)=>a[0]-b[0]);const merged=[];for(const range of ranges){if(!merged.length||range[0]>merged[merged.length-1][1])merged.push(range.slice());else merged[merged.length-1][1]=Math.max(merged[merged.length-1][1],range[1]);}let excluded=0;for(const range of merged)excluded+=Math.max(0,range[1]-range[0]);const nextLength=Math.max(0,originalArray.length-excluded);const ArrayType=originalArray.constructor||Uint32Array;const filtered=new ArrayType(nextLength);let sourceCursor=0;let targetCursor=0;for(const range of merged){if(range[0]>sourceCursor){const part=originalArray.subarray(sourceCursor,range[0]);filtered.set(part,targetCursor);targetCursor+=part.length;}sourceCursor=Math.max(sourceCursor,range[1]);}if(sourceCursor<originalArray.length){const part=originalArray.subarray(sourceCursor);filtered.set(part,targetCursor);}geometry.setIndex(new THREE.BufferAttribute(filtered,1));chunk.userData.massTransparencyHiddenMaskKeys=requestedMask;chunk.userData.massTransparencyBaseVisibilityMasked=true;chunk.visible=nextLength>0;return{changed:true,complete:true,empty:nextLength===0,excludedIndices:excluded};}maskMassTransparencyChunksForHiddenKeys(keys){const keySet=new Set(keys||[]);const result={changedChunks:0,affectedChunks:0,requiresRebuild:false,excludedIndices:0};if(!keySet.size||!this.massTransparencyChunkMeshes||!this.massTransparencyChunkMeshes.length)return result;const chunks=this.getMassTransparencyChunksForElementKeys?this.getMassTransparencyChunksForElementKeys(keySet):new Set();result.affectedChunks=chunks.size;for(const chunk of chunks){if(!chunk||!chunk.userData)continue;const nextMask=new Set(chunk.userData.massTransparencyHiddenMaskKeys||[]);const sourceKeys=new Set(chunk.userData.massTransparencyChunkSourceKeys||[]);for(const key of keySet)if(sourceKeys.has(key))nextMask.add(key);const masked=this.applyMassTransparencyChunkHiddenMask?this.applyMassTransparencyChunkHiddenMask(chunk,nextMask):{complete:false};if(!masked.complete){result.requiresRebuild=true;break;}if(masked.changed)result.changedChunks++;result.excludedIndices+=Number(masked.excludedIndices||0);}if(!result.requiresRebuild&&result.changedChunks){this.massTransparencyChunkVisible=(this.massTransparencyChunkMeshes||[]).some(chunk=>chunk&&chunk.parent&&chunk.visible!==false);this.massTransparencyChunkCacheSignature=this.getMassTransparencyChunkSignature?this.getMassTransparencyChunkSignature():null;}return result;}restoreMassTransparencyChunkKeys(keys){const keySet=new Set(keys||[]);const result={changedChunks:0,affectedChunks:0,requiresRebuild:false};if(!keySet.size||!(this.visualMassState&&this.visualMassState.active))return result;const chunks=this.getMassTransparencyChunksForElementKeys?this.getMassTransparencyChunksForElementKeys(keySet):new Set();result.affectedChunks=chunks.size;for(const key of keySet){if(this.isColorOverrideKey&&this.isColorOverrideKey(key))continue;const mapped=this.massTransparencyChunksByElementKey&&this.massTransparencyChunksByElementKey.get(key);if(!mapped||!mapped.size){const mass=this.visualMassState||{};if(mass.allKeys&&mass.allKeys.has(key))result.requiresRebuild=true;}}if(result.requiresRebuild)return result;for(const chunk of chunks){if(!chunk||!chunk.userData)continue;const nextMask=new Set(chunk.userData.massTransparencyHiddenMaskKeys||[]);for(const key of keySet)nextMask.delete(key);const restored=this.applyMassTransparencyChunkHiddenMask?this.applyMassTransparencyChunkHiddenMask(chunk,nextMask):{complete:false};if(!restored.complete){result.requiresRebuild=true;break;}if(restored.changed)result.changedChunks++;}if(!result.requiresRebuild&&result.changedChunks){this.massTransparencyChunkVisible=(this.massTransparencyChunkMeshes||[]).some(chunk=>chunk&&chunk.parent&&chunk.visible!==false);this.massTransparencyChunkCacheSignature=this.getMassTransparencyChunkSignature?this.getMassTransparencyChunkSignature():null;}return result;}removeHiddenKeysFromMassTransparencyActiveLayers(keys){const keySet=new Set(keys||[]);if(!keySet.size)return 0;const previousVisibleState=this.visualMassState&&this.visualMassState.active?this.visualMassState:null;let changed=0;const cloneLayer=layer=>{if(!layer||!layer.active)return layer;const activeKeys=new Set(layer.activeKeys||[]);let layerChanged=false;for(const key of keySet)if(activeKeys.delete(key))layerChanged=true;if(!layerChanged)return layer;changed++;return Object.assign({},layer,{activeKeys,allKeys:layer.allKeys instanceof Set?layer.allKeys:new Set(layer.allKeys||[]),baseVisibilityRevision:this.baseVisibilityRevision||0});};this.viewerMassTransparencyState=cloneLayer(this.viewerMassTransparencyState);this.dataMassTransparencyState=cloneLayer(this.dataMassTransparencyState);if(changed&&this.syncMassTransparencyStateFromLayers)this.syncMassTransparencyStateFromLayers(previousVisibleState);if(this.visualMassState&&this.visualMassState.active)this.visualMassState.baseVisibilityRevision=this.baseVisibilityRevision||0;return changed;}clearMassTransparencyChunks(dispose){const shouldDispose=dispose===true;let removed=0;if(this.massTransparencyChunkMeshes&&this.massTransparencyChunkMeshes.length){for(const mesh of this.massTransparencyChunkMeshes){if(!mesh)continue;if(mesh.parent)mesh.parent.remove(mesh);if(shouldDispose&&mesh.geometry&&mesh.geometry.dispose)mesh.geometry.dispose();removed++;}}if(shouldDispose){this.massTransparencyChunkMeshes=[];this.massTransparencyChunkCacheSignature=null;this.massTransparencyChunksByElementKey=new Map();}this.massTransparencyChunkVisible=false;for(const key of this.massTransparencyChunkHiddenKeys||[]){const meshes=this.getMeshesForElementKey?this.getMeshesForElementKey(key):[];for(const mesh of meshes){if(!mesh||!mesh.userData)continue;mesh.userData.massTransparencyChunkSuppressed=false;mesh.visible=(mesh.userData.normalChunkSuppressed||mesh.userData.visualStateChunkSuppressed)?false:mesh.userData.baseVisible!==false;}}if(this.massTransparencyChunkHiddenKeys)this.massTransparencyChunkHiddenKeys.clear();if(this.massTransparencyChunkActiveKeysSnapshot)this.massTransparencyChunkActiveKeysSnapshot.clear();if(this.massTransparencyChunkInteractiveKeysSnapshot)this.massTransparencyChunkInteractiveKeysSnapshot.clear();return removed;}async clearMassTransparencyChunksBatched(dispose,options){const opts=options&&typeof options==='object'?options:{};const shouldDispose=dispose===true;const report=typeof opts.onProgress==='function'?opts.onProgress:()=>{};const isCancelled=typeof opts.isCancelled==='function'?opts.isCancelled:()=>false;const batchSize=Math.max(300,Math.min(6000,Number(opts.batchSize)||2200));let removed=0;for(const mesh of this.massTransparencyChunkMeshes||[]){if(!mesh)continue;if(mesh.parent)mesh.parent.remove(mesh);if(shouldDispose&&mesh.geometry&&mesh.geometry.dispose)mesh.geometry.dispose();removed++;}if(shouldDispose){this.massTransparencyChunkMeshes=[];this.massTransparencyChunkCacheSignature=null;this.massTransparencyChunksByElementKey=new Map();}this.massTransparencyChunkVisible=false;const hiddenKeys=Array.from(this.massTransparencyChunkHiddenKeys||[]);for(let start=0;start<hiddenKeys.length;start+=batchSize){if(isCancelled())return false;const end=Math.min(hiddenKeys.length,start+batchSize);for(let index=start;index<end;index++){const key=hiddenKeys[index];const meshes=this.getMeshesForElementKey?this.getMeshesForElementKey(key):[];for(const mesh of meshes){if(!mesh||!mesh.userData)continue;mesh.userData.massTransparencyChunkSuppressed=false;mesh.visible=(mesh.userData.normalChunkSuppressed||mesh.userData.visualStateChunkSuppressed)?false:mesh.userData.baseVisible!==false;}}report(end,hiddenKeys.length,opts.progressMessage||'Restaurando superficies transparentes...');if(end<hiddenKeys.length&&opts.yieldBetweenBatches!==false){if(this.waitForResetProgressPaint)await this.waitForResetProgressPaint();else if(this.yieldDuringLoad)await this.yieldDuringLoad();else await new Promise(resolve=>setTimeout(resolve,0));}}if(this.massTransparencyChunkHiddenKeys)this.massTransparencyChunkHiddenKeys.clear();if(this.massTransparencyChunkActiveKeysSnapshot)this.massTransparencyChunkActiveKeysSnapshot.clear();if(this.massTransparencyChunkInteractiveKeysSnapshot)this.massTransparencyChunkInteractiveKeysSnapshot.clear();return removed;}getMassTransparencyChunkSignature(){if(!(this.visualMassState&&this.visualMassState.active))return null;const mass=this.visualMassState||{};const allKeysFingerprint=mass.allKeysFingerprint||(this.getKeySetFingerprint?this.getKeySetFingerprint(mass.allKeys||[]):((mass.allKeys&&mass.allKeys.size)||0));return['mass-v6-cached-base','detail',this.getVisualDetailLevel?this.getVisualDetailLevel():'default','base',allKeysFingerprint,'visibilityRevision',this.baseVisibilityRevision||0,'overridesRevision',this.visualColorOverridesRevision||0,'color',mass.fadedColor==null?'default':Number(mass.fadedColor),'opacity',Number(mass.fadedOpacity==null?this.getConfiguredTransparencyOpacity(0.02):mass.fadedOpacity).toFixed(3)].join('|');}shouldUseMassTransparencyChunks(){return this.dataSpatialGroupingActive!==true&&!this.isolateActive&&!!(this.visualMassState&&this.visualMassState.active&&this.visualMassState.allKeys&&this.visualMassState.allKeys.size>0);}syncMassTransparencyRenderForSectionBox(reason){if(this.clashModeActive===true)return false;const mass=this.visualMassState&&this.visualMassState.active?this.visualMassState:null;if(!mass)return false;if(this.renderer){this.renderer.clippingPlanes=this.getActiveClippingPlanes?this.getActiveClippingPlanes():(this.sectionPlanes||[]);}if(this.recordPerformanceOperation)this.recordPerformanceOperation('massTransparency:section-sync',0,{reason:reason||'',sectionBoxActive:this.sectionBoxActive===true,keys:mass.allKeys?mass.allKeys.size:0,chunks:this.massTransparencyChunkMeshes?this.massTransparencyChunkMeshes.length:0,preservedOptimizedRender:true});if(this.requestRender)this.requestRender(reason||'mass-transparency-section-sync');return true;}isMassTransparencyInteractiveKey(key){if(!key)return false;const selectionUsesSource=this.selectionUsesSourceAppearance?this.selectionUsesSourceAppearance():this.visualSelectionOverlayOnly!==true;if(selectionUsesSource&&this.selectedKeys&&this.selectedKeys.has(key))return true;if(this.visualHoverKey&&this.visualHoverKey===key)return true;if(this.rectanglePreviewKeys&&this.rectanglePreviewKeys.has(key))return true;return false;}collectMassTransparencyChunkSourceMeshes(){const result=[];const seen=new Set();if(!this.shouldUseMassTransparencyChunks())return result;const mass=this.visualMassState||{};const keys=mass.allKeys&&mass.allKeys.size?mass.allKeys:(this.fadedMeshKeys||[]);for(const key of keys){if(!key)continue;if(this.isColorOverrideKey&&this.isColorOverrideKey(key))continue;const meshes=this.getMeshesForElementKey?this.getMeshesForElementKey(key):(this.meshByElementKey&&this.meshByElementKey.get(key)?[this.meshByElementKey.get(key)]:[]);for(const mesh of meshes){if(!mesh||!mesh.isMesh||seen.has(mesh))continue;if(this.isNonRenderedDataMesh(mesh))continue;const baseVisible=!(mesh.userData&&mesh.userData.baseVisible===false);if(!baseVisible)continue;result.push(mesh);seen.add(mesh);}}return result;}buildMassTransparencyChunkGeometryFromMeshes(meshes){const result=[];const byProject=new Map();for(const mesh of meshes||[]){if(!mesh)continue;const projectId=this.getMeshRenderProjectId?this.getMeshRenderProjectId(mesh):'';let list=byProject.get(projectId);if(!list){list=[];byProject.set(projectId,list);}list.push(mesh);}for(const[projectId,projectMeshes]of byProject.entries()){const chunks=this.buildChunkGeometriesFromMeshes?this.buildChunkGeometriesFromMeshes(projectMeshes,{useSingleDraw:true,maxVerticesPerChunk:120000,maxIndicesPerChunk:360000,spatialMinimumMeshes:24,targetMeshesPerCell:180,maximumCells:Number(this.spatialChunkMaximumCells||512)}):[];for(const geometry of chunks){const sourceMeshes=geometry&&geometry.userData&&Array.isArray(geometry.userData.normalChunkSourceMeshes)?geometry.userData.normalChunkSourceMeshes.slice():[];geometry.userData=Object.assign({},geometry.userData||{},{renderProjectId:projectId||'',massTransparencyChunkSourceMeshes:sourceMeshes,massTransparencyChunkSourceKeys:sourceMeshes.map(mesh=>this.getMeshKey?this.getMeshKey(mesh):(mesh&&mesh.userData&&mesh.userData.elementKey)).filter(Boolean)});result.push(geometry);}}return result;}rebuildMassTransparencyChunks(forceRebuild){const metricsStart=this.getPerformanceNow?this.getPerformanceNow():Date.now();if(!this.shouldUseMassTransparencyChunks()){this.clearMassTransparencyChunks(false);return 0;}const signature=this.getMassTransparencyChunkSignature();const sameSignature=!forceRebuild&&signature&&signature===this.massTransparencyChunkCacheSignature&&this.massTransparencyChunkMeshes&&this.massTransparencyChunkMeshes.length;if(sameSignature){const currentActive=new Set(this.visualMassState&&this.visualMassState.activeKeys||[]);const currentInteractive=new Set();if(!this.shouldUseSelectionOverlayBatches||!this.shouldUseSelectionOverlayBatches()){for(const key of this.selectedKeys||[])currentInteractive.add(key);}if(this.visualHoverKey)currentInteractive.add(this.visualHoverKey);for(const key of this.rectanglePreviewKeys||[])currentInteractive.add(key);const changed=new Set();for(const key of this.massTransparencyChunkActiveKeysSnapshot||[])changed.add(key);for(const key of currentActive)changed.add(key);for(const key of this.massTransparencyChunkInteractiveKeysSnapshot||[])changed.add(key);for(const key of currentInteractive)changed.add(key);for(const key of changed){if(!key)continue;const shouldHide=this.visualMassState&&this.visualMassState.allKeys&&this.visualMassState.allKeys.has(key)&&!currentActive.has(key)&&!currentInteractive.has(key)&&!(this.isColorOverrideKey&&this.isColorOverrideKey(key));const meshes=this.getMeshesForElementKey?this.getMeshesForElementKey(key):[];let touched=false;for(const mesh of meshes){if(!mesh||!mesh.userData)continue;mesh.userData.massTransparencyChunkSuppressed=shouldHide;if(shouldHide){mesh.visible=false;if(this.removeTransientCompactFallbackEdge)this.removeTransientCompactFallbackEdge(mesh,true);}else if(!(mesh.userData.normalChunkSuppressed||mesh.userData.visualStateChunkSuppressed))this.updateVisualAppearanceForMesh(mesh);touched=true;}if(touched&&shouldHide)this.massTransparencyChunkHiddenKeys.add(key);else this.massTransparencyChunkHiddenKeys.delete(key);}this.massTransparencyChunkActiveKeysSnapshot=currentActive;this.massTransparencyChunkInteractiveKeysSnapshot=currentInteractive;for(const chunk of this.massTransparencyChunkMeshes){if(!chunk)continue;chunk.visible=true;if(!chunk.parent)this.scene.add(chunk);}this.massTransparencyChunkVisible=true;this.massTransparencyChunkReuseCount=(this.massTransparencyChunkReuseCount||0)+1;if(this.recordPerformanceOperation)this.recordPerformanceOperation('massTransparencyChunks:reuse-incremental',(this.getPerformanceNow?this.getPerformanceNow():Date.now())-metricsStart,{chunks:this.massTransparencyChunkMeshes.length,changedKeys:changed.size,activeKeys:currentActive.size});return this.massTransparencyChunkMeshes.length;}this.clearMassTransparencyChunks(true);const sourceMeshes=this.collectMassTransparencyChunkSourceMeshes();if(!sourceMeshes.length){this.massTransparencyChunkCacheSignature=signature;this.massTransparencyChunkVisible=false;return 0;}const fadedMaterial=this.getSimpleColorMaterial(this.visualMassState&&this.visualMassState.fadedColor!=null?this.visualMassState.fadedColor:this.getConfiguredTransparencyColor(),this.visualMassState&&this.visualMassState.fadedOpacity!=null?this.visualMassState.fadedOpacity:this.getConfiguredTransparencyOpacity(0.02),true);const fadedGeometries=this.buildMassTransparencyChunkGeometryFromMeshes(sourceMeshes);const chunks=[];for(const geometry of fadedGeometries){const chunk=new THREE.Mesh(geometry,fadedMaterial);chunk.renderOrder=-1;chunk.frustumCulled=true;chunk.userData=Object.assign({},chunk.userData||{},{isMassTransparencyChunk:true,renderProjectId:geometry&&geometry.userData?(geometry.userData.renderProjectId||''):'',massTransparencyChunkSourceMeshes:geometry&&geometry.userData&&Array.isArray(geometry.userData.massTransparencyChunkSourceMeshes)?geometry.userData.massTransparencyChunkSourceMeshes.slice():[],massTransparencyChunkSourceKeys:geometry&&geometry.userData&&Array.isArray(geometry.userData.massTransparencyChunkSourceKeys)?geometry.userData.massTransparencyChunkSourceKeys.slice():[],massTransparencyChunkIndexRangesByKey:geometry&&geometry.userData?(geometry.userData.normalChunkIndexRangesByKey||null):null,massTransparencyOriginalIndexAttribute:geometry&&geometry.index?geometry.index:null,massTransparencyHiddenMaskKeys:new Set(),spatialChunkKey:geometry&&geometry.userData?(geometry.userData.spatialChunkKey||'all'):'all'});this.scene.add(chunk);chunks.push(chunk);}this.massTransparencyChunkMeshes=chunks;if(this.rebuildMassTransparencyChunkElementIndex)this.rebuildMassTransparencyChunkElementIndex();this.massTransparencyChunkCacheSignature=signature;this.massTransparencyChunkVisible=chunks.length>0;for(const mesh of sourceMeshes){const key=this.getMeshKey(mesh);if(!key||!mesh.userData)continue;if(this.visualMassState&&this.visualMassState.activeKeys&&this.visualMassState.activeKeys.has(key))continue;if(this.isMassTransparencyInteractiveKey&&this.isMassTransparencyInteractiveKey(key))continue;if(this.isColorOverrideKey&&this.isColorOverrideKey(key))continue;mesh.userData.massTransparencyChunkSuppressed=true;mesh.visible=false;if(this.removeTransientCompactFallbackEdge)this.removeTransientCompactFallbackEdge(mesh,true);this.massTransparencyChunkHiddenKeys.add(key);}this.massTransparencyChunkActiveKeysSnapshot=new Set(this.visualMassState&&this.visualMassState.activeKeys||[]);this.massTransparencyChunkInteractiveKeysSnapshot=new Set();if(!this.shouldUseSelectionOverlayBatches||!this.shouldUseSelectionOverlayBatches()){for(const key of this.selectedKeys||[])this.massTransparencyChunkInteractiveKeysSnapshot.add(key);}if(this.visualHoverKey)this.massTransparencyChunkInteractiveKeysSnapshot.add(this.visualHoverKey);for(const key of this.rectanglePreviewKeys||[])this.massTransparencyChunkInteractiveKeysSnapshot.add(key);this.massTransparencyChunkBuildCount=(this.massTransparencyChunkBuildCount||0)+1;if(this.recordPerformanceOperation)this.recordPerformanceOperation('massTransparencyChunks:build',(this.getPerformanceNow?this.getPerformanceNow():Date.now())-metricsStart,{chunks:chunks.length,sourceMeshes:sourceMeshes.length});return chunks.length;}getGeneratedEdgeGeometryCacheKey(mesh,thresholdAngle){if(!mesh||!mesh.geometry)return'';const geometry=mesh.geometry;const geometryData=mesh.userData&&mesh.userData.geometryData?mesh.userData.geometryData:null;const sharedId=(mesh.userData&&mesh.userData.sharedGeometryId)||(geometryData&&(geometryData.SharedGeometryId||geometryData.GeometryId))||'';const position=geometry.getAttribute&&geometry.getAttribute('position');const index=geometry.index;const positionCount=position&&position.count?position.count:0;const indexCount=index&&index.count?index.count:0;const geometryKey=sharedId||geometry.uuid||'';if(!geometryKey||!positionCount)return'';return['edgeGeom',this.getVisualDetailLevel?this.getVisualDetailLevel():'default',Number(thresholdAngle||0),geometryKey,positionCount,indexCount].join('|');}cacheGeneratedEdgeGeometry(mesh,thresholdAngle,edgeGeometry){if(!mesh||!edgeGeometry)return edgeGeometry||null;if(!this.generatedEdgeGeometryCache)this.generatedEdgeGeometryCache=new Map();const cacheKey=this.getGeneratedEdgeGeometryCacheKey?this.getGeneratedEdgeGeometryCacheKey(mesh,thresholdAngle):'';if(!cacheKey)return edgeGeometry;const cached=this.generatedEdgeGeometryCache.get(cacheKey);if(cached){if(cached!==edgeGeometry&&edgeGeometry.dispose&&!(edgeGeometry.userData&&edgeGeometry.userData.generatedEdgeCached))edgeGeometry.dispose();return cached;}edgeGeometry.userData=Object.assign({},edgeGeometry.userData||{},{generatedEdgeCached:true,generatedEdgeCacheKey:cacheKey});this.generatedEdgeGeometryCache.set(cacheKey,edgeGeometry);return edgeGeometry;}getOrCreateGeneratedEdgeGeometry(mesh,thresholdAngle,options){if(!mesh||!mesh.geometry)return null;if(!this.generatedEdgeGeometryCache)this.generatedEdgeGeometryCache=new Map();const shouldCacheResult=!options||options.cacheResult!==false;const cacheKey=this.getGeneratedEdgeGeometryCacheKey?this.getGeneratedEdgeGeometryCacheKey(mesh,thresholdAngle):'';const cached=cacheKey?this.generatedEdgeGeometryCache.get(cacheKey):null;if(cached){this.generatedEdgeGeometryCacheHits=(this.generatedEdgeGeometryCacheHits||0)+1;if(this.activeViewerLoadBreakdown)this.activeViewerLoadBreakdown.counters.edgeGeometryCacheHits=(this.activeViewerLoadBreakdown.counters.edgeGeometryCacheHits||0)+1;this.recordActiveViewerLoadPhase('edges.edgeGeometryCacheHit',0,{thresholdAngle,cacheKey});return cached;}const precomputedEdgeGeometry=this.createPrecomputedGeneratedEdgeGeometry?this.createPrecomputedGeneratedEdgeGeometry(mesh,thresholdAngle):null;if(precomputedEdgeGeometry){this.generatedEdgeGeometryCacheMisses=(this.generatedEdgeGeometryCacheMisses||0)+1;if(this.activeViewerLoadBreakdown){this.activeViewerLoadBreakdown.counters.edgeGeometryCacheMisses=(this.activeViewerLoadBreakdown.counters.edgeGeometryCacheMisses||0)+1;this.activeViewerLoadBreakdown.counters.precomputedEdgeGeometryMisses=(this.activeViewerLoadBreakdown.counters.precomputedEdgeGeometryMisses||0)+1;}if(cacheKey&&shouldCacheResult){precomputedEdgeGeometry.userData=Object.assign({},precomputedEdgeGeometry.userData||{},{generatedEdgeCached:true,generatedEdgeCacheKey:cacheKey,precomputedEdges:true});this.generatedEdgeGeometryCache.set(cacheKey,precomputedEdgeGeometry);}return precomputedEdgeGeometry;}const edgeGeometryStart=this.getPerformanceNow?this.getPerformanceNow():Date.now();const edgeGeometry=new THREE.EdgesGeometry(mesh.geometry,thresholdAngle);const edgeGeometryMs=(this.getPerformanceNow?this.getPerformanceNow():Date.now())-edgeGeometryStart;this.generatedEdgeGeometryCacheMisses=(this.generatedEdgeGeometryCacheMisses||0)+1;if(this.activeViewerLoadBreakdown)this.activeViewerLoadBreakdown.counters.edgeGeometryCacheMisses=(this.activeViewerLoadBreakdown.counters.edgeGeometryCacheMisses||0)+1;this.recordActiveViewerLoadPhase('edges.newEdgesGeometry',edgeGeometryMs,{thresholdAngle,geometryId:mesh.userData&&mesh.userData.geometryData?mesh.userData.geometryData.GeometryId||'':'',cacheKey:cacheKey||''});if(!edgeGeometry||!edgeGeometry.getAttribute('position')||edgeGeometry.getAttribute('position').count===0){if(edgeGeometry&&edgeGeometry.dispose)edgeGeometry.dispose();return null;}if(cacheKey&&shouldCacheResult){edgeGeometry.userData=Object.assign({},edgeGeometry.userData||{},{generatedEdgeCached:true,generatedEdgeCacheKey:cacheKey});this.generatedEdgeGeometryCache.set(cacheKey,edgeGeometry);}return edgeGeometry;}createPrecomputedGeneratedEdgeGeometry(mesh,thresholdAngle){const geometryData=mesh&&mesh.userData?mesh.userData.geometryData:null;const edgeIndices=geometryData&&geometryData.EdgeLineIndices;if(!edgeIndices||typeof edgeIndices.length!=='number'||edgeIndices.length<2)return null;if(!mesh.geometry||!mesh.geometry.getAttribute)return null;const positionAttr=mesh.geometry.getAttribute('position');const source=positionAttr&&positionAttr.array;if(!source||typeof source.length!=='number'||source.length<3)return null;const edgeAngles=geometryData.EdgeAngles;const hasAngles=edgeAngles&&typeof edgeAngles.length==='number'&&edgeAngles.length>=Math.floor(edgeIndices.length/2);const thresholdHundredths=Math.max(0,Math.round((Number(thresholdAngle)||0)*100));const boundaryAngle=65535;const edgeCount=Math.floor(edgeIndices.length/2);let included=0;for(let i=0;i<edgeCount;i++){const angle=hasAngles?edgeAngles[i]:boundaryAngle;if(angle===boundaryAngle||angle>=thresholdHundredths)included++;}if(!included)return null;const start=this.getPerformanceNow?this.getPerformanceNow():Date.now();const positions=new Float32Array(included*2*3);let cursor=0;const vertexCount=Math.floor(source.length/3);for(let i=0;i<edgeCount;i++){const angle=hasAngles?edgeAngles[i]:boundaryAngle;if(!(angle===boundaryAngle||angle>=thresholdHundredths))continue;const a=edgeIndices[i*2];const b=edgeIndices[i*2+1];if(a==null||b==null||a<0||b<0||a>=vertexCount||b>=vertexCount)continue;const ai=a*3;const bi=b*3;positions[cursor++]=source[ai]||0;positions[cursor++]=source[ai+1]||0;positions[cursor++]=source[ai+2]||0;positions[cursor++]=source[bi]||0;positions[cursor++]=source[bi+1]||0;positions[cursor++]=source[bi+2]||0;}const finalPositions=cursor===positions.length?positions:positions.slice(0,cursor);if(!finalPositions.length)return null;const edgeGeometry=new THREE.BufferGeometry();edgeGeometry.setAttribute('position',new THREE.BufferAttribute(finalPositions,3));const ms=(this.getPerformanceNow?this.getPerformanceNow():Date.now())-start;this.recordActiveViewerLoadPhase('edges.precomputedEdgeGeometry',ms,{thresholdAngle,geometryId:geometryData?geometryData.GeometryId||'':'',candidateEdges:edgeCount,emittedEdges:Math.floor(finalPositions.length/6)});return edgeGeometry;}disposeGeneratedEdgeGeometryCache(){if(!this.generatedEdgeGeometryCache)return;for(const geometry of this.generatedEdgeGeometryCache.values())if(geometry&&geometry.dispose)geometry.dispose();this.generatedEdgeGeometryCache.clear();this.generatedEdgeGeometryCacheHits=0;this.generatedEdgeGeometryCacheMisses=0;}createEdges(mesh){if(!mesh||!mesh.isMesh)return null;try{const baseThresholdAngle=this.getEdgeDetectionAngleForElement(mesh.userData&&mesh.userData.element);const exteriorSettings=this.getSimplifiedExteriorEdgeSettings?this.getSimplifiedExteriorEdgeSettings(mesh):null;let baseEdgeGeometry=null;let edgeGeometry=null;let thresholdAngle=baseThresholdAngle;let adaptiveMediumEdges=false;let simplifiedExteriorEdges=false;let tubularRepresentativeEdges=false;let lowComplexityFullEdges=false;let semanticCurvedEdges=false;let semanticCurvedClassified=false;edgeGeometry=this.createTubularRepresentativeEdgeGeometry?this.createTubularRepresentativeEdgeGeometry(mesh):null;if(edgeGeometry){thresholdAngle=-2;tubularRepresentativeEdges=true;}if(!edgeGeometry){const semanticSettings=this.getSemanticCurvedEdgeSettings?this.getSemanticCurvedEdgeSettings(mesh):null;if(semanticSettings){semanticCurvedClassified=true;edgeGeometry=this.createSemanticCurvedEdgeGeometry(mesh,semanticSettings);if(edgeGeometry){thresholdAngle=semanticSettings.minAngle;simplifiedExteriorEdges=true;semanticCurvedEdges=true;}}}if(!edgeGeometry&&semanticCurvedClassified)return null;if(!edgeGeometry){const lowComplexitySettings=this.getLowComplexityFullEdgeSettings?this.getLowComplexityFullEdgeSettings(mesh):null;if(lowComplexitySettings){edgeGeometry=this.createLowComplexityFullEdgeGeometry(mesh,lowComplexitySettings);if(edgeGeometry){thresholdAngle=lowComplexitySettings.thresholdAngle;lowComplexityFullEdges=true;}}}if(!edgeGeometry&&exteriorSettings&&exteriorSettings.preferDirectFallback){edgeGeometry=this.createSimplifiedExteriorEdgeGeometry(mesh,exteriorSettings);simplifiedExteriorEdges=!!edgeGeometry;}if(!edgeGeometry){baseEdgeGeometry=this.getOrCreateGeneratedEdgeGeometry(mesh,baseThresholdAngle);edgeGeometry=baseEdgeGeometry;const adaptive=this.getMediumAdaptiveEdgeSettings?this.getMediumAdaptiveEdgeSettings(mesh,baseEdgeGeometry,baseThresholdAngle):null;if(adaptive){const midpointAngle=Math.round((adaptive.thresholdAngle+baseThresholdAngle)/2);const candidateAngles=[adaptive.thresholdAngle];if(midpointAngle>adaptive.thresholdAngle&&midpointAngle<baseThresholdAngle)candidateAngles.push(midpointAngle);for(const candidateAngle of candidateAngles){const candidateGeometry=this.getOrCreateGeneratedEdgeGeometry(mesh,candidateAngle,{cacheResult:false});const candidateSegments=this.getGeneratedEdgeSegmentCount(candidateGeometry);if(candidateSegments<=adaptive.baseSegments||candidateSegments>adaptive.segmentLimit){if(candidateGeometry&&candidateGeometry.dispose&&!(candidateGeometry.userData&&candidateGeometry.userData.generatedEdgeCached))candidateGeometry.dispose();continue;}edgeGeometry=this.cacheGeneratedEdgeGeometry?this.cacheGeneratedEdgeGeometry(mesh,candidateAngle,candidateGeometry):candidateGeometry;thresholdAngle=candidateAngle;adaptiveMediumEdges=true;break;}}if(exteriorSettings){const currentSegments=this.getGeneratedEdgeSegmentCount(edgeGeometry);if(currentSegments<exteriorSettings.minimumReadableSegments){const exteriorGeometry=this.createSimplifiedExteriorEdgeGeometry(mesh,exteriorSettings);if(exteriorGeometry){edgeGeometry=exteriorGeometry;thresholdAngle=-1;adaptiveMediumEdges=false;simplifiedExteriorEdges=true;}}}}if(!edgeGeometry)return null;const material=this.getBaseEdgeMaterial(mesh);const edge=new THREE.LineSegments(edgeGeometry,material);edge.userData.isGeneratedEdge=true;edge.userData.generatedEdgeGeometryShared=!!(edgeGeometry.userData&&edgeGeometry.userData.generatedEdgeCached);edge.userData.generatedEdgeSegmentCount=this.getGeneratedEdgeSegmentCount(edgeGeometry);edge.userData.generatedEdgeThresholdAngle=thresholdAngle;edge.userData.mediumAdaptiveEdges=adaptiveMediumEdges;edge.userData.simplifiedExteriorEdges=simplifiedExteriorEdges||!!(edgeGeometry.userData&&edgeGeometry.userData.simplifiedExteriorEdges);edge.userData.tubularRepresentativeEdges=tubularRepresentativeEdges||!!(edgeGeometry.userData&&edgeGeometry.userData.tubularRepresentativeEdges);edge.userData.lowComplexityFullEdges=lowComplexityFullEdges||!!(edgeGeometry.userData&&edgeGeometry.userData.lowComplexityFullEdges);edge.userData.semanticCurvedEdges=semanticCurvedEdges||!!(edgeGeometry.userData&&edgeGeometry.userData.semanticCurvedEdges);edge.renderOrder=2;if(!edgeGeometry.boundingBox&&edgeGeometry.computeBoundingBox)edgeGeometry.computeBoundingBox();if(!edgeGeometry.boundingSphere&&edgeGeometry.computeBoundingSphere)edgeGeometry.computeBoundingSphere();edge.frustumCulled=true;return edge;}catch(error){if(typeof debugError==='function')debugError('viewer:createEdges:failed',error,{elementKey:mesh.userData&&mesh.userData.elementKey,geometryId:mesh.userData&&mesh.userData.geometryData&&mesh.userData.geometryData.GeometryId});return null;}}removeGeneratedEdges(){this.invalidateNormalChunks&&this.invalidateNormalChunks('removeGeneratedEdges');this.clearMassTransparencyChunks(true);this.clearGroupedTransparentEdges(true);this.clearVisualStateChunks&&this.clearVisualStateChunks(true);let removed=0;for(const mesh of this.meshes||[]){const children=Array.isArray(mesh.children)?mesh.children.slice():[];for(const child of children){if(!child||!child.userData||!child.userData.isGeneratedEdge)continue;mesh.remove(child);if(child.geometry&&child.geometry.dispose&&!(child.geometry.userData&&child.geometry.userData.generatedEdgeCached))child.geometry.dispose();removed++;}if(this.clearCompactFallbackEdgeForMesh)this.clearCompactFallbackEdgeForMesh(mesh);}this.edges=(this.edges||[]).filter(edge=>!(edge&&edge.userData&&edge.userData.isGeneratedEdge));this.generatedEdgeObjectCount=0;return removed;}collectVisualStateKeysForEdgeRefresh(){const keys=new Set();if(!this.shouldUseSelectionOverlayBatches||!this.shouldUseSelectionOverlayBatches()){for(const key of this.selectedKeys||[])keys.add(key);}for(const key of this.fadedMeshKeys||[])keys.add(key);for(const key of(this.visualColorOverrides?this.visualColorOverrides.keys():[]))keys.add(key);for(const key of(this.visualWorksStates?this.visualWorksStates.keys():[]))keys.add(key);for(const key of(this.visualClashStates?this.visualClashStates.keys():[]))keys.add(key);if(this.visualMassState&&this.visualMassState.active){for(const key of this.visualMassState.allKeys||[])keys.add(key);}if(this.visualHoverKey)keys.add(this.visualHoverKey);return keys;}setVisualDetailLevelSilently(level){const normalized=this.normalizeVisualDetailLevel?this.normalizeVisualDetailLevel(level):(level||'medium');this.visualDetailLevel=normalized;if(window.BimGeneralSettings&&window.BimGeneralSettings.getSettings&&window.BimGeneralSettings.applySettings){try{const settings=window.BimGeneralSettings.getSettings();settings.visualDetailLevel=normalized;window.BimGeneralSettings.applySettings(settings,{silent:true});}catch(_){}}}getVisualDetailLabel(level){const normalized=this.normalizeVisualDetailLevel?this.normalizeVisualDetailLevel(level):String(level||'medium');if(normalized==='high')return'Alto';return'Medio';}createVisualDetailAbortError(){const error=new Error('Cambio de nivel de visualización cancelado.');error.name='AbortError';return error;}async removeGeneratedEdgesAsync(operation,onProgress){this.invalidateNormalChunks&&this.invalidateNormalChunks('removeGeneratedEdgesAsync');this.clearMassTransparencyChunks(true);this.clearGroupedTransparentEdges(true);this.clearVisualStateChunks&&this.clearVisualStateChunks(true);let removed=0;const meshes=this.meshes||[];const total=Math.max(1,meshes.length);const progressInterval=Math.max(20,Math.floor(total/80));for(let i=0;i<meshes.length;i++){if(operation&&operation.cancelled)throw this.createVisualDetailAbortError();const mesh=meshes[i];const children=Array.isArray(mesh&&mesh.children)?mesh.children.slice():[];for(const child of children){if(!child||!child.userData||!child.userData.isGeneratedEdge)continue;mesh.remove(child);if(child.geometry&&child.geometry.dispose&&!(child.geometry.userData&&child.geometry.userData.generatedEdgeCached))child.geometry.dispose();removed++;}if(this.clearCompactFallbackEdgeForMesh)this.clearCompactFallbackEdgeForMesh(mesh);if(i%progressInterval===0){if(onProgress)onProgress(i,total);await this.yieldDuringLoad();}}this.edges=(this.edges||[]).filter(edge=>!(edge&&edge.userData&&edge.userData.isGeneratedEdge));this.generatedEdgeObjectCount=0;if(onProgress)onProgress(total,total);return removed;}disposeGeneratedEdgeBatch(edgeBatch){for(const item of edgeBatch||[]){const edge=item&&item.edge;if(edge&&edge.geometry&&edge.geometry.dispose&&!(edge.geometry.userData&&edge.geometry.userData.generatedEdgeCached))edge.geometry.dispose();}}rebuildGeneratedEdgesForVisualDetail(previousVisualDetailLevel){this.rebuildGeneratedEdgesForVisualDetailAsync(previousVisualDetailLevel).catch(error=>{if(error&&error.name==='AbortError')return;console.error(error);});}async rebuildGeneratedEdgesForVisualDetailAsync(previousVisualDetailLevel){if(!this.meshes||!this.meshes.length)return;const ui=window.__exportModelUI||null;const targetLevel=this.getVisualDetailLevel();const previousLevel=this.normalizeVisualDetailLevel?this.normalizeVisualDetailLevel(previousVisualDetailLevel||targetLevel):(previousVisualDetailLevel||targetLevel);const operationId=(this.visualDetailRebuildOperationId||0)+1;this.visualDetailRebuildOperationId=operationId;const operation={id:operationId,cancelled:false};const totalMeshes=Math.max(1,this.meshes.length);const label=this.getVisualDetailLabel(targetLevel);const previousStatus=ui&&ui.nodes&&ui.nodes.loadStatus?ui.nodes.loadStatus.textContent:'';let edgeBatch=[];let compactFallbackBatch=[];const isCancelled=()=>operation.cancelled||this.visualDetailRebuildOperationId!==operationId;const setProgress=(message,current,total)=>{if(ui&&ui.setStatus)ui.setStatus(message);if(ui&&ui.setLoadingProgress)ui.setLoadingProgress(message,current,total,true);};if(ui&&ui.setLoadingCancelHandler){ui.setLoadingCancelHandler(()=>{operation.cancelled=true;if(ui.setStatus)ui.setStatus('Cancelando cambio de nivel de visualización...');if(ui.setLoadingProgress)ui.setLoadingProgress('Cancelando cambio de nivel de visualización...',1,1,true);});}const finishProgress=(message)=>{if(ui&&ui.hideLoadingProgress)ui.hideLoadingProgress();if(ui&&ui.setStatus)ui.setStatus(message||previousStatus||('Nivel de visualización: '+label));};try{setProgress('Aplicando nivel de visualización '+label+'...',0,totalMeshes);const model=this.currentModel||{geometries:[],elements:[]};this.configureRenderPerformanceForModel(model,true);this.generatedEdgeObjectCount=0;edgeBatch=[];const progressInterval=Math.max(10,Math.floor(totalMeshes/120));for(let i=0;i<this.meshes.length;i++){if(isCancelled())throw this.createVisualDetailAbortError();const mesh=this.meshes[i];if(!mesh||!mesh.isMesh)continue;const element=mesh.userData&&mesh.userData.element;const geometryData=mesh.userData&&mesh.userData.geometryData;let nextDetailedEdge=null;if(this.shouldGenerateEdgesForGeometry(geometryData,element,model,totalMeshes)){const edge=this.createEdges(mesh);if(edge){edgeBatch.push({mesh,edge});nextDetailedEdge=edge;this.generatedEdgeObjectCount++;}}if(!nextDetailedEdge&&this.createCompactFallbackEdgeDescriptor){const descriptor=this.createCompactFallbackEdgeDescriptor(mesh,'visual-detail-rebuild',{ignoreExistingDetailedEdge:true});if(descriptor)compactFallbackBatch.push({mesh,descriptor});}if(i%progressInterval===0){setProgress('Generando bordes '+label+': '+(i+1)+'/'+totalMeshes,i+1,totalMeshes);await this.yieldDuringLoad();}}if(isCancelled())throw this.createVisualDetailAbortError();setProgress('Reemplazando bordes...',totalMeshes,totalMeshes);await this.removeGeneratedEdgesAsync(operation);if(isCancelled())throw this.createVisualDetailAbortError();this.edges=(this.edges||[]).filter(edge=>!(edge&&edge.userData&&edge.userData.isGeneratedEdge));for(const item of edgeBatch){if(!item||!item.mesh||!item.edge)continue;item.mesh.add(item.edge);this.edges.push(item.edge);}for(const item of compactFallbackBatch){if(!item||!item.mesh||!item.descriptor)continue;this.applyCompactFallbackEdgeDescriptor(item.mesh,item.descriptor);}this.generatedEdgeObjectCount=edgeBatch.length;const refreshKeys=this.collectVisualStateKeysForEdgeRefresh();if(refreshKeys.size&&this.updateVisualAppearanceForKeys)this.updateVisualAppearanceForKeys(refreshKeys);this.clearMassTransparencyChunks(true);if(this.rebuildGroupedTransparentEdges)this.rebuildGroupedTransparentEdges(true);if(this.rebuildMassTransparencyChunks&&this.visualMassState&&this.visualMassState.active)this.rebuildMassTransparencyChunks(true);if(this.rebuildNormalChunks&&!(this.visualMassState&&this.visualMassState.active)&&this.clashModeActive!==true)this.rebuildNormalChunks(true);this.renderOnce();finishProgress('Nivel de visualización '+label+' aplicado.');return true;}catch(error){this.disposeGeneratedEdgeBatch(edgeBatch);if(error&&error.name==='AbortError'){operation.cancelled=true;this.setVisualDetailLevelSilently(previousLevel);this.generatedEdgeObjectCount=this.countGeneratedEdgeObjects();finishProgress('Cambio de nivel de visualización cancelado.');this.renderOnce();return false;}finishProgress('Error al aplicar nivel de visualización.');throw error;}}clear(){if(this.navigationPerformanceRestoreTimer)clearTimeout(this.navigationPerformanceRestoreTimer);this.navigationPerformanceRestoreTimer=null;this.navigationPerformanceActive=false;if(this.navigationEdgeVisibilityStates)this.navigationEdgeVisibilityStates.clear();if(this.renderer){this.baseRendererPixelRatio=Math.min(window.devicePixelRatio||1,2);this.renderer.setPixelRatio(this.baseRendererPixelRatio);}if(typeof this.restoreDataSpatialGrouping==='function')this.restoreDataSpatialGrouping('viewer-clear');if(this.selectionController)this.selectionController.cancelAll(this,'viewer-clear');this.restoreFadedElements();this.clearMassTransparencyHighlight();this.clearColorOverrides();this.disableSectionBox();this.clearMeasurement();this.clearElevationMarker();for(const mesh of this.meshes){this.removeMeshFromCategoryGroup(mesh);mesh.traverse(child=>{if(child.geometry&&!(child.geometry.userData&&child.geometry.userData.sharedCached))child.geometry.dispose();if(child.material){if(Array.isArray(child.material))child.material.forEach(material=>this.disposeMaterialIfOwned(material));else this.disposeMaterialIfOwned(child.material);}});}this.meshes=[];this.edges=[];this.treeVisibilityGuardActive=false;this.treeVisibilityGuardRevision=0;this.baseVisibilityRevision=(this.baseVisibilityRevision||0)+1;if(this.visualColorOverrides)this.visualColorOverrides.clear();this.viewerMassTransparencyState=null;this.dataMassTransparencyState=null;this.visualMassState=null;this.aiVisualState=null;if(this.visualWorksStates)this.visualWorksStates.clear();if(this.visualClashStates)this.visualClashStates.clear();if(this.visualFadeStates)this.visualFadeStates.clear();this.visualSelectionPreserveAppearance=false;this.visualHoverKey=null;if(this.clearAllInteractionOverlays)this.clearAllInteractionOverlays();if(this.rectanglePreviewMeshes)this.rectanglePreviewMeshes.clear();if(this.rectanglePreviewKeys)this.rectanglePreviewKeys.clear();if(this.fadedMeshMaterialStates)this.fadedMeshMaterialStates.clear();this.generatedEdgeObjectCount=0;this.compactFallbackEdgeCount=0;this.compactFallbackEdgeSegmentCount=0;this.embeddedLineObjectCount=0;this.meshByElementKey.clear();if(this.meshesByElementKey)this.meshesByElementKey.clear();this.meshesByProjectId=new Map();this.meshesByCategoryUiKey=new Map();this.meshesByProjectCategoryUiKey=new Map();this.elementKeyByCategoryUiKey=new Map();this.elementKeyByProjectId=new Map();for(const group of this.categoryGroups.values()){if(group&&group.parent)group.parent.remove(group);}this.categoryGroups=new Map();this.disposeMaterialMap(this.lineMaterialByKey);this.disposeGeneratedEdgeGeometryCache();this.disposeSharedGeometryCache();this.selectedMesh=null;this.selectedMeshes=[];this.selectedOriginalMaterials=new Map();this.selectedKey=null;this.selectedKeys=new Set();this.orbitPivot=null;this.lastSelectionOrbitPivot=null;this.lastSelectedElementKey=null;this.isolationRenderSnapshot=null;this.selectionCandidateCache=null;this.selectionCandidateCacheRevision=(this.selectionCandidateCacheRevision||0)+1;this.raycastScreenCandidateCache=null;this.hoveredMesh=null;this.hoveredOriginalMaterials=null;this.isolatedKey=null;this.isolateActive=false;this.isolatedKeys=new Set();this.hiddenKeys=new Set();this.visibilityPredicate=null;this.dataPanelFilterActive=false;this.dataPanelFilterKeys=new Set();this.baseIsolationActive=false;this.baseIsolationKeys=new Set();this.restoreRectanglePreview&&this.restoreRectanglePreview();this.clearClashHighlight&&this.clearClashHighlight();this.updateLevelGridToModel();if(this.selectionController)this.selectionController.syncState(this,'viewer-clear');}cancelCurrentLoad(clearScene){this.loadOperationId++;if(clearScene!==false){try{this.clear();}catch(error){console.error('Error al cancelar la carga del visor:',error);}}return true;}getCategoryColor(categoryId){if(this.materialByKey.has(categoryId))return this.materialByKey.get(categoryId);const color=fallbackPalette[this.materialByKey.size%fallbackPalette.length];this.materialByKey.set(categoryId,color);return color;}setCategoryVisibility(categoryId,visible){if(visible)this.visibleCategories.add(categoryId);else this.visibleCategories.delete(categoryId);const group=this.categoryGroups&&this.categoryGroups.get(categoryId||'__sin_categoria__');if(group)group.visible=visible;for(const mesh of this.meshes){if(mesh.userData.categoryId===categoryId)this.setMeshBaseVisibility(mesh,visible);}}getSubcategoryUiNameForElement(element){if(!element)return'';const value=element.SubcategoryName||element.SubCategoryName||element.SubCategory||element.Subcategoria||'';const text=String(value||'').trim();if(!text||text.toLowerCase()==='general')return'';return text;}getCategoryUiKeyForElement(element){if(!element)return'Sin categoría||';const category=element.CategoryName||'Sin categoría';const subcategory=this.getSubcategoryUiNameForElement(element);return category+'||'+subcategory;}getMeshesForElementKeys(keys){const result=[];const seen=new Set();for(const key of keys||[]){const meshes=this.getMeshesForElementKey?this.getMeshesForElementKey(key):[];for(const mesh of meshes){if(!mesh||seen.has(mesh))continue;seen.add(mesh);result.push(mesh);}}return result;}getMeshesForCategoryUiKeys(categoryKeys,visibleProjectIds){const result=[];const seen=new Set();const categories=Array.from(new Set(categoryKeys||[])).filter(Boolean);const projects=visibleProjectIds?Array.from(new Set(visibleProjectIds||[])).filter(Boolean):null;for(const categoryKey of categories){const lists=[];if(projects&&projects.length&&this.meshesByProjectCategoryUiKey){for(const projectId of projects)lists.push(this.meshesByProjectCategoryUiKey.get(projectId+'||'+categoryKey)||[]);}else if(this.meshesByCategoryUiKey){lists.push(this.meshesByCategoryUiKey.get(categoryKey)||[]);}for(const list of lists){for(const mesh of list||[]){if(!mesh||seen.has(mesh))continue;seen.add(mesh);result.push(mesh);}}}return result;}primeTreeVisibilityPredicate(predicate,reason){this.visibilityPredicate=predicate||null;this.treeVisibilityGuardActive=!!this.visibilityPredicate;this.treeVisibilityGuardRevision=(this.treeVisibilityGuardRevision||0)+1;this.normalChunkCacheSignature=null;this.visualStateChunkCacheSignature=null;this.massTransparencyChunkCacheSignature=null;this.groupedTransparentEdgeCacheSignature=null;if(this.cancelScheduledNormalChunkEdgesForVisibleChunks)this.cancelScheduledNormalChunkEdgesForVisibleChunks();if(this.recordPerformanceOperation)this.recordPerformanceOperation('treeVisibility:predicate-primed',0,{reason:reason||'tree-visibility',revision:this.treeVisibilityGuardRevision});return this.treeVisibilityGuardRevision;}setVisibilityFilter(predicate,affectedProjectIds,options){const opts=options&&typeof options==='object'?options:{};this.primeTreeVisibilityPredicate(predicate,opts.reason||'visibility-filter');const projectIds=Array.from(new Set(affectedProjectIds||[])).filter(Boolean);const categoryKeys=Array.from(new Set(opts.affectedCategoryKeys||[])).filter(Boolean);const strictTreeVisibility=projectIds.length>0||categoryKeys.length>0;let targetMeshes=null;if(projectIds.length){targetMeshes=null;}else if(opts.affectedElementKeys&&opts.affectedElementKeys.length){targetMeshes=this.getMeshesForElementKeys(opts.affectedElementKeys);}else if(categoryKeys.length){targetMeshes=this.getMeshesForCategoryUiKeys(categoryKeys,opts.visibleProjectIds||null);}if(strictTreeVisibility&&this.suspendMergedRenderSystemsForTreeVisibility){this.suspendMergedRenderSystemsForTreeVisibility(opts.reason||'tree-visibility');}if(strictTreeVisibility){this.treeVisibilityGuardActive=true;}const updateOptions={forceChunkRebuild:strictTreeVisibility||opts.forceChunkRebuild===true,skipChunkVisibilitySync:strictTreeVisibility,strictTreeVisibility,reason:opts.reason||'visibility-filter',async:opts.async!==false,onProgress:typeof opts.onProgress==='function'?opts.onProgress:null};const count=targetMeshes?targetMeshes.length:((this.meshes&&this.meshes.length)||0);if(updateOptions.async&&count>3500&&this.applyVisibilityStateBatched){return this.applyVisibilityStateBatched(targetMeshes,updateOptions);}else{return this.applyVisibilityState(targetMeshes,updateOptions);}}suspendMergedRenderSystemsForTreeVisibility(reason){if(this.normalChunkRefreshDeferTimer){clearTimeout(this.normalChunkRefreshDeferTimer);this.normalChunkRefreshDeferTimer=null;}if(this.cancelScheduledNormalChunkEdgesForVisibleChunks)this.cancelScheduledNormalChunkEdgesForVisibleChunks();const removed={normal:this.clearNormalChunks?this.clearNormalChunks(false):0,visualState:this.clearVisualStateChunks?this.clearVisualStateChunks(false):0,massTransparency:this.clearMassTransparencyChunks?this.clearMassTransparencyChunks(false):0,transparentEdges:this.clearGroupedTransparentEdges?this.clearGroupedTransparentEdges(false):0};this.normalChunkCacheSignature=null;this.visualStateChunkCacheSignature=null;this.massTransparencyChunkCacheSignature=null;this.groupedTransparentEdgeCacheSignature=null;if(this.recordPerformanceOperation)this.recordPerformanceOperation('treeVisibility:suspend-merged-render-systems',0,{reason:reason||'tree-visibility',normal:removed.normal||0,visualState:removed.visualState||0,massTransparency:removed.massTransparency||0,transparentEdges:removed.transparentEdges||0});return removed;}finalizeStrictTreeVisibility(targetMeshes,changedKeys,reason){const meshes=Array.from(targetMeshes||[]);const hiddenKeys=new Set();let hiddenMeshes=0;for(const mesh of meshes){if(!mesh||!mesh.userData||mesh.userData.baseVisible!==false)continue;hiddenMeshes++;const key=mesh.userData.elementKey;if(key)hiddenKeys.add(key);mesh.visible=false;for(const child of mesh.children||[])if(child)child.visible=false;}if(hiddenKeys.size){let selectionChanged=false;if(this.selectedKeys&&this.selectedKeys.size){for(const key of hiddenKeys)if(this.selectedKeys.delete(key))selectionChanged=true;}if(this.selectedKey&&hiddenKeys.has(this.selectedKey)){this.selectedKey=null;selectionChanged=true;}if(this.visualHoverKey&&hiddenKeys.has(this.visualHoverKey)){if(this.restoreHoveredMaterial)this.restoreHoveredMaterial();this.visualHoverKey=null;}if(this.clearInteractionOverlayForKey){for(const key of hiddenKeys)this.clearInteractionOverlayForKey(key);}if(selectionChanged){this.selectedMeshes=(this.selectedMeshes||[]).filter(sourceMesh=>{const key=sourceMesh&&sourceMesh.userData?sourceMesh.userData.elementKey:null;return!key||!hiddenKeys.has(key);});this.selectedMesh=this.selectedMeshes.length===1?this.selectedMeshes[0]:null;if(this.clearSelectionOverlayBatches)this.clearSelectionOverlayBatches(true);if(this.refreshSelectionOverlayBatches&&this.selectedKeys&&this.selectedKeys.size)this.refreshSelectionOverlayBatches(true);if(this.onSelect&&(!this.selectedKeys||!this.selectedKeys.size))this.onSelect(null);}}if(this.syncInteractiveNormalChunkPeel)this.syncInteractiveNormalChunkPeel();if(this.enforceTreeVisibilityMergedRenderGuard)this.enforceTreeVisibilityMergedRenderGuard(true,reason||'tree-visibility-finalize');if(this.scheduleNormalChunkEdgesForVisibleChunks)this.scheduleNormalChunkEdgesForVisibleChunks('tree-visibility-finalize');if(this.invalidateSelectionCandidateCache)this.invalidateSelectionCandidateCache('tree-visibility-finalize');if(this.invalidateRaycastScreenCandidateCache)this.invalidateRaycastScreenCandidateCache('tree-visibility-finalize');if(this.requestRender)this.requestRender('tree-visibility-finalize');if(this.recordPerformanceOperation)this.recordPerformanceOperation('treeVisibility:finalize',0,{reason:reason||'tree-visibility',changedKeys:changedKeys&&typeof changedKeys.size==='number'?changedKeys.size:0,hiddenKeys:hiddenKeys.size,hiddenMeshes});}suspendMergedRenderSystemsForBaseIsolation(reason){if(this.normalChunkRefreshDeferTimer){clearTimeout(this.normalChunkRefreshDeferTimer);this.normalChunkRefreshDeferTimer=null;}if(this.cancelScheduledNormalChunkEdgesForVisibleChunks)this.cancelScheduledNormalChunkEdgesForVisibleChunks();const removed={normal:this.clearNormalChunks?this.clearNormalChunks(false):0,visualState:this.clearVisualStateChunks?this.clearVisualStateChunks(false):0,massTransparency:this.clearMassTransparencyChunks?this.clearMassTransparencyChunks(false):0,transparentEdges:this.clearGroupedTransparentEdges?this.clearGroupedTransparentEdges(false):0};this.normalChunkCacheSignature=null;this.visualStateChunkCacheSignature=null;this.massTransparencyChunkCacheSignature=null;this.groupedTransparentEdgeCacheSignature=null;if(this.recordPerformanceOperation)this.recordPerformanceOperation('baseIsolation:suspend-previous-render-systems',0,{reason:reason||'base-isolation',normal:removed.normal||0,visualState:removed.visualState||0,massTransparency:removed.massTransparency||0,transparentEdges:removed.transparentEdges||0});return removed;}async suspendMergedRenderSystemsForBaseIsolationResponsive(reason,options){const opts=options&&typeof options==='object'?options:{};const isCancelled=typeof opts.isCancelled==='function'?opts.isCancelled:()=>false;const report=typeof opts.onProgress==='function'?opts.onProgress:()=>{};if(this.normalChunkRefreshDeferTimer){clearTimeout(this.normalChunkRefreshDeferTimer);this.normalChunkRefreshDeferTimer=null;}if(this.cancelScheduledNormalChunkEdgesForVisibleChunks)this.cancelScheduledNormalChunkEdgesForVisibleChunks();const originalNormalSignature=this.normalChunkCacheSignature;const removed={normal:0,visualState:0,massTransparency:0,transparentEdges:0};if(this.suspendNormalChunkSurfacesPreserveEdgesBatched){const value=await this.suspendNormalChunkSurfacesPreserveEdgesBatched(reason||'base-isolation-responsive',{isCancelled,onProgress:report,batchSize:opts.batchSize||2200});if(value===false&&isCancelled())return false;removed.normal=Number(value)||0;}else if(this.suspendNormalChunkSurfacesPreserveEdges){removed.normal=this.suspendNormalChunkSurfacesPreserveEdges(reason||'base-isolation-responsive')||0;}if(isCancelled())return false;if(this.clearVisualStateChunksBatched){const value=await this.clearVisualStateChunksBatched(false,{isCancelled,onProgress:report,batchSize:opts.batchSize||2200});if(value===false&&isCancelled())return false;removed.visualState=Number(value)||0;}if(isCancelled())return false;if(this.clearMassTransparencyChunksBatched){const value=await this.clearMassTransparencyChunksBatched(false,{isCancelled,onProgress:report,batchSize:opts.batchSize||2200});if(value===false&&isCancelled())return false;removed.massTransparency=Number(value)||0;}if(isCancelled())return false;const needsGroupedEdgeCleanup=!!((this.groupedTransparentEdgeBatches&&this.groupedTransparentEdgeBatches.length)||((this.visualMassState&&this.visualMassState.active)&&this.massTransparencyUseStableEdges!==true));if(needsGroupedEdgeCleanup&&this.clearGroupedTransparentEdgesBatched){const value=await this.clearGroupedTransparentEdgesBatched(false,{isCancelled,onProgress:report,batchSize:opts.batchSize||2600});if(value===false&&isCancelled())return false;removed.transparentEdges=Number(value)||0;}this.normalChunkCacheSignature=originalNormalSignature;this.visualStateChunkCacheSignature=null;this.massTransparencyChunkCacheSignature=null;this.groupedTransparentEdgeCacheSignature=null;if(this.recordPerformanceOperation)this.recordPerformanceOperation('baseIsolation:suspend-previous-render-systems-responsive',0,{reason:reason||'base-isolation-responsive',normal:removed.normal,visualState:removed.visualState,massTransparency:removed.massTransparency,transparentEdges:removed.transparentEdges,normalCachePreserved:!!originalNormalSignature});return removed;}getIsolationStateFingerprint(keys){const values=new Set(keys||[]);return this.getKeySetFingerprint?this.getKeySetFingerprint(values):Array.from(values).map(String).sort().join('\u001f');}captureIsolationRenderSnapshot(reason){const collectionNames=['normalChunkMeshes','adaptiveMediumDepthProxies','normalChunkEdgeBatches','visualStateChunkMeshes','visualStateChunkEdgeBatches','massTransparencyChunkMeshes','groupedTransparentEdgeBatches'];const objectStates=[];const seen=new Set();for(const name of collectionNames){for(const object of this[name]||[]){if(!object||seen.has(object))continue;seen.add(object);objectStates.push({object,parent:object.parent||null,visible:object.visible!==false});}}return{reason:reason||'isolation',objectStates,normalChunkVisible:this.normalChunkVisible===true,visualStateChunkVisible:this.visualStateChunkVisible===true,massTransparencyChunkVisible:this.massTransparencyChunkVisible===true,hiddenFingerprint:this.getIsolationStateFingerprint(this.hiddenKeys),dataFilterFingerprint:this.getIsolationStateFingerprint(this.dataPanelFilterKeys),baseIsolationFingerprint:this.getIsolationStateFingerprint(this.baseIsolationKeys),baseVisibilityRevision:Number(this.baseVisibilityRevision)||0,visualColorOverridesRevision:Number(this.visualColorOverridesRevision)||0,normalChunkRevision:Number(this.normalChunkRevision)||0,dataPanelFilterActive:this.dataPanelFilterActive===true,baseIsolationActive:this.baseIsolationActive===true,visualMassState:this.visualMassState||null,visualWorksStatesSize:this.visualWorksStates?this.visualWorksStates.size:0,visualClashStatesSize:this.visualClashStates?this.visualClashStates.size:0};}canRestoreIsolationRenderSnapshot(snapshot){if(!snapshot||!Array.isArray(snapshot.objectStates))return false;return snapshot.hiddenFingerprint===this.getIsolationStateFingerprint(this.hiddenKeys)&&snapshot.dataFilterFingerprint===this.getIsolationStateFingerprint(this.dataPanelFilterKeys)&&snapshot.baseIsolationFingerprint===this.getIsolationStateFingerprint(this.baseIsolationKeys)&&snapshot.baseVisibilityRevision===(Number(this.baseVisibilityRevision)||0)&&snapshot.visualColorOverridesRevision===(Number(this.visualColorOverridesRevision)||0)&&snapshot.normalChunkRevision===(Number(this.normalChunkRevision)||0)&&snapshot.dataPanelFilterActive===(this.dataPanelFilterActive===true)&&snapshot.baseIsolationActive===(this.baseIsolationActive===true)&&snapshot.visualMassState===(this.visualMassState||null)&&snapshot.visualWorksStatesSize===(this.visualWorksStates?this.visualWorksStates.size:0)&&snapshot.visualClashStatesSize===(this.visualClashStates?this.visualClashStates.size:0);}restoreIsolationRenderSnapshotFast(reason){const snapshot=this.isolationRenderSnapshot;if(!this.canRestoreIsolationRenderSnapshot(snapshot)){this.isolationRenderSnapshot=null;if(snapshot&&this.suspendMergedRenderSystemsForIsolationLegacy){this.suspendMergedRenderSystemsForIsolationLegacy('isolation-cache-invalid');}return false;}if(this.endStrictIsolationVisibilityGuard)this.endStrictIsolationVisibilityGuard(reason||'isolation-fast-restore');for(const state of snapshot.objectStates){const object=state&&state.object;if(!object)continue;object.visible=state.visible!==false;if(!object.parent&&state.parent&&state.parent.add)state.parent.add(object);}this.normalChunkVisible=snapshot.normalChunkVisible===true;this.visualStateChunkVisible=snapshot.visualStateChunkVisible===true;this.massTransparencyChunkVisible=snapshot.massTransparencyChunkVisible===true;this.isolationRenderSnapshot=null;if(this.invalidateRaycastScreenCandidateCache)this.invalidateRaycastScreenCandidateCache(reason||'isolation-fast-restore');if(this.invalidateMeasurementPickCache)this.invalidateMeasurementPickCache(reason||'isolation-fast-restore');if(this.requestRender)this.requestRender(reason||'isolation-fast-restore');if(this.recordPerformanceOperation)this.recordPerformanceOperation('isolation:restore-cached-render-state',0,{restoredObjects:snapshot.objectStates.length,reason:reason||'isolation-fast-restore'});return true;}suspendMergedRenderSystemsForIsolation(reason){if(this.normalChunkRefreshDeferTimer){clearTimeout(this.normalChunkRefreshDeferTimer);this.normalChunkRefreshDeferTimer=null;}if(this.cancelScheduledNormalChunkEdgesForVisibleChunks)this.cancelScheduledNormalChunkEdgesForVisibleChunks();const snapshot=this.captureIsolationRenderSnapshot?this.captureIsolationRenderSnapshot(reason||'isolation'):null;if(!snapshot)return this.suspendMergedRenderSystemsForIsolationLegacy(reason);this.isolationRenderSnapshot=snapshot;const removed={normal:0,visualState:0,massTransparency:0,transparentEdges:0};for(const state of snapshot.objectStates){const object=state&&state.object;if(!object||!object.parent)continue;if(object.userData&&object.userData.isNormalChunk)removed.normal++;else if(object.userData&&object.userData.isVisualStateChunk)removed.visualState++;else if(object.userData&&object.userData.isMassTransparencyChunk)removed.massTransparency++;else if(object.userData&&object.userData.isGroupedTransparentEdgeBatch)removed.transparentEdges++;object.parent.remove(object);}this.normalChunkVisible=false;this.visualStateChunkVisible=false;this.massTransparencyChunkVisible=false;if(this.recordPerformanceOperation)this.recordPerformanceOperation('isolation:suspend-cached-render-state',0,{reason:reason||'isolation',detachedObjects:snapshot.objectStates.length});return removed;}suspendMergedRenderSystemsForIsolationLegacy(reason){if(this.normalChunkRefreshDeferTimer){clearTimeout(this.normalChunkRefreshDeferTimer);this.normalChunkRefreshDeferTimer=null;}if(this.cancelScheduledNormalChunkEdgesForVisibleChunks)this.cancelScheduledNormalChunkEdgesForVisibleChunks();const removed={normal:this.clearNormalChunks?this.clearNormalChunks(false):0,visualState:this.clearVisualStateChunks?this.clearVisualStateChunks(false):0,massTransparency:this.clearMassTransparencyChunks?this.clearMassTransparencyChunks(false):0,transparentEdges:this.clearGroupedTransparentEdges?this.clearGroupedTransparentEdges(false):0};if(this.recordPerformanceOperation)this.recordPerformanceOperation('isolation:suspend-merged-render-systems',0,{reason:reason||'isolation',normal:removed.normal||0,visualState:removed.visualState||0,massTransparency:removed.massTransparency||0,transparentEdges:removed.transparentEdges||0});return removed;}isStrictIsolationMergedRenderObject(object){if(!object||!object.userData)return false;const data=object.userData;return!!(data.isNormalChunk||data.isAdaptiveMediumDepthProxy||data.isNormalChunkEdgeBatch||data.isVisualStateChunk||data.isVisualStateChunkEdgeBatch||data.isMassTransparencyChunk||data.isGroupedTransparentEdgeBatch);}getStrictIsolationObjectElementKey(object){if(!object||!object.userData)return null;const data=object.userData;return data.elementKey||data.interactionOverlaySourceKey||data.selectionOverlaySourceKey||data.rectanglePreviewSourceKey||null;}beginStrictIsolationVisibilityGuard(reason){this.strictIsolationGuardActive=true;if(!(this.strictIsolationForcedVisibility instanceof Map))this.strictIsolationForcedVisibility=new Map();else this.strictIsolationForcedVisibility.clear();this.strictIsolationLastSceneAuditAt=0;this.enforceStrictIsolationVisibilityGuard(true,reason||'isolation-start');}endStrictIsolationVisibilityGuard(reason){this.strictIsolationGuardActive=false;const forced=this.strictIsolationForcedVisibility instanceof Map?Array.from(this.strictIsolationForcedVisibility.entries()):[];this.strictIsolationForcedVisibility=new Map();this.strictIsolationLastSceneAuditAt=0;for(const[object,previousVisible]of forced){if(!object||this.isStrictIsolationMergedRenderObject(object))continue;const key=this.getStrictIsolationObjectElementKey(object);if(key&&object.userData&&object.userData.baseVisible===false)continue;object.visible=previousVisible!==false;}if(this.recordPerformanceOperation)this.recordPerformanceOperation('isolation:visibility-guard-end',0,{reason:reason||'isolation-end',restoredAuxiliaryObjects:forced.length});}forceHideForStrictIsolation(object,captureOriginal){if(!object)return false;if(captureOriginal&&object.visible!==false&&this.strictIsolationForcedVisibility instanceof Map&&!this.strictIsolationForcedVisibility.has(object)){this.strictIsolationForcedVisibility.set(object,true);}if(object.visible!==false)object.visible=false;return true;}enforceStrictIsolationVisibilityGuard(auditScene,reason){if(!this.strictIsolationGuardActive||!this.isolateActive||!this.isolatedKeys||!this.isolatedKeys.size)return 0;const allowed=this.isolatedKeys;let hiddenObjects=0;const mergedCollections=[this.normalChunkMeshes,this.adaptiveMediumDepthProxies,this.normalChunkEdgeBatches,this.visualStateChunkMeshes,this.visualStateChunkEdgeBatches,this.massTransparencyChunkMeshes,this.groupedTransparentEdgeBatches];for(const collection of mergedCollections){for(const object of collection||[]){if(!object)continue;if(!object.parent)continue;if(this.forceHideForStrictIsolation(object,false))hiddenObjects++;}}if(this.strictIsolationForcedVisibility instanceof Map){for(const object of this.strictIsolationForcedVisibility.keys()){if(!object)continue;if(object.visible!==false){object.visible=false;hiddenObjects++;}}}for(const mesh of this.meshes||[]){if(!mesh||!mesh.userData)continue;const key=this.getMeshKey?this.getMeshKey(mesh):mesh.userData.elementKey;const allowedMesh=!!(key&&allowed.has(key)&&mesh.userData.baseVisible!==false&&!(this.hiddenKeys&&this.hiddenKeys.has(key)));if(!allowedMesh){if(this.forceHideForStrictIsolation(mesh,auditScene===true))hiddenObjects++;for(const child of mesh.children||[]){if(!child)continue;if(this.forceHideForStrictIsolation(child,auditScene===true))hiddenObjects++;}}}const now=this.getPerformanceNow?this.getPerformanceNow():Date.now();const shouldAuditScene=auditScene===true||!this.strictIsolationLastSceneAuditAt||(now-this.strictIsolationLastSceneAuditAt)>250;if(shouldAuditScene&&this.scene&&typeof this.scene.traverse==='function'){this.strictIsolationLastSceneAuditAt=now;this.scene.traverse(object=>{if(!object||object===this.scene)return;if(this.isStrictIsolationMergedRenderObject(object)){if(this.forceHideForStrictIsolation(object,auditScene===true))hiddenObjects++;return;}const key=this.getStrictIsolationObjectElementKey(object);if(key&&!allowed.has(key)){if(this.forceHideForStrictIsolation(object,auditScene===true))hiddenObjects++;}});}if(auditScene===true&&this.recordPerformanceOperation)this.recordPerformanceOperation('isolation:visibility-guard',0,{reason:reason||'isolation',isolatedKeys:allowed.size,forcedHiddenObjects:hiddenObjects});return hiddenObjects;}ensureOptimizedVisualRenderState(reason){if(this.isolateActive||this.clashModeActive===true)return false;let repaired=false;const colorExpected=!!(this.shouldUseVisualStateChunks&&this.shouldUseVisualStateChunks());const massExpected=!!(this.shouldUseMassTransparencyChunks&&this.shouldUseMassTransparencyChunks());if(massExpected&&(!this.massTransparencyChunkMeshes||!this.massTransparencyChunkMeshes.length)){this.rebuildMassTransparencyChunks&&this.rebuildMassTransparencyChunks(true);repaired=true;}if(colorExpected&&(!this.visualStateChunkMeshes||!this.visualStateChunkMeshes.length)){this.rebuildVisualStateChunks&&this.rebuildVisualStateChunks(true);repaired=true;}if(massExpected&&this.massTransparencyUseStableEdges!==true&&this.shouldUseGroupedTransparentEdges&&this.shouldUseGroupedTransparentEdges()&&(!this.groupedTransparentEdgeBatches||!this.groupedTransparentEdgeBatches.length)){this.rebuildGroupedTransparentEdges&&this.rebuildGroupedTransparentEdges(true);repaired=true;}if(!massExpected&&!colorExpected&&this.shouldUseNormalChunks&&this.shouldUseNormalChunks()&&(!this.normalChunkMeshes||!this.normalChunkMeshes.length)){this.rebuildNormalChunks&&this.rebuildNormalChunks(false);repaired=true;}if(this.recordPerformanceOperation)this.recordPerformanceOperation('renderOptimization:validate',0,{reason:reason||'',repaired,colorExpected,massExpected,normalChunks:(this.normalChunkMeshes||[]).length,visualStateChunks:(this.visualStateChunkMeshes||[]).length,massTransparencyChunks:(this.massTransparencyChunkMeshes||[]).length});if(repaired&&this.requestRender)this.requestRender('render-optimization-validated');return repaired;}scheduleOptimizedVisualRenderStateValidation(reason){this.optimizedVisualRenderValidationReason=reason||this.optimizedVisualRenderValidationReason||'';if(this.optimizedVisualRenderValidationTimer)return true;this.optimizedVisualRenderValidationTimer=setTimeout(()=>{this.optimizedVisualRenderValidationTimer=null;const pendingReason=this.optimizedVisualRenderValidationReason||'';this.optimizedVisualRenderValidationReason='';this.ensureOptimizedVisualRenderState&&this.ensureOptimizedVisualRenderState(pendingReason);},0);return true;}getAvailableElementKeysIgnoringBaseIsolation(){const keys=new Set();for(const mesh of this.meshes||[]){if(!mesh||!mesh.userData)continue;const key=this.getMeshKey?this.getMeshKey(mesh):mesh.userData.elementKey;if(!key)continue;const passesFilter=this.visibilityPredicate?!!this.visibilityPredicate(mesh.userData.element):true;const passesIsolation=this.isolateActive&&this.isolatedKeys&&this.isolatedKeys.size?this.isolatedKeys.has(key):true;if(passesFilter&&passesIsolation&&!(this.hiddenKeys&&this.hiddenKeys.has(key)))keys.add(key);}return keys;}setBaseIsolationKeys(keys,options){const opts=options&&typeof options==='object'?options:{};let nextKeys=new Set(keys||[]);if(nextKeys.size&&this.getAvailableElementKeysIgnoringBaseIsolation){const availableKeys=this.getAvailableElementKeysIgnoringBaseIsolation();if(availableKeys.size===nextKeys.size){let coversAllAvailable=true;for(const key of availableKeys){if(!nextKeys.has(key)){coversAllAvailable=false;break;}}if(coversAllAvailable)nextKeys=new Set();}}const currentKeys=this.baseIsolationKeys||new Set();let sameKeys=this.baseIsolationActive===(nextKeys.size>0)&&currentKeys.size===nextKeys.size;if(sameKeys){for(const key of nextKeys){if(!currentKeys.has(key)){sameKeys=false;break;}}}if(sameKeys)return false;const preservedMassState=opts.preserveVisualAppearance===true&&this.visualMassState&&this.visualMassState.active?this.visualMassState:null;this.baseIsolationKeys=nextKeys;this.baseIsolationActive=nextKeys.size>0;if(this.baseIsolationActive&&this.suspendMergedRenderSystemsForBaseIsolation){this.suspendMergedRenderSystemsForBaseIsolation('base-isolation');}this.applyVisibilityState(null,{reason:opts.source==='ai-panel'?'ai-base-isolation':'base-isolation',forceChunkRebuild:false,skipChunkVisibilitySync:!!preservedMassState,skipChunkRefresh:!!preservedMassState,skipSectionRefresh:!!preservedMassState});if(preservedMassState){preservedMassState.baseVisibilityRevision=this.baseVisibilityRevision||0;this.massTransparencyUseStableEdges=false;if(this.visualTransactionDepth&&this.visualTransactionDepth>0){this.visualTransactionRefreshAllChunks=true;this.visualTransactionRenderReason=this.visualTransactionRenderReason||'base-isolation-preserve-appearance';}else if(this.refreshChunkRenderSystems){this.refreshChunkRenderSystems(null,false);}}else if(this.baseIsolationActive&&this.activateTransientCompactFallbackEdgesForKeys){this.activateTransientCompactFallbackEdgesForKeys(this.baseIsolationKeys,'data-base-isolation');}if(preservedMassState){}else if(this.visualTransactionDepth&&this.visualTransactionDepth>0){if(this.scheduleOptimizedVisualRenderStateValidation)this.scheduleOptimizedVisualRenderStateValidation('base-isolation');}else if(this.ensureOptimizedVisualRenderState){this.ensureOptimizedVisualRenderState('base-isolation');}return true;}clearBaseIsolationKeys(){if(!this.baseIsolationActive&&(!this.baseIsolationKeys||!this.baseIsolationKeys.size))return false;this.baseIsolationKeys=new Set();this.baseIsolationActive=false;this.applyVisibilityState();if(this.visualTransactionDepth&&this.visualTransactionDepth>0){if(this.scheduleOptimizedVisualRenderStateValidation)this.scheduleOptimizedVisualRenderStateValidation('base-isolation-clear');}else if(this.ensureOptimizedVisualRenderState){this.ensureOptimizedVisualRenderState('base-isolation-clear');}return true;}async setBaseIsolationKeysResponsive(keys,options){const opts=options&&typeof options==='object'?options:{};const nextKeys=new Set(keys||[]);const currentKeys=this.baseIsolationKeys||new Set();let sameKeys=this.baseIsolationActive===(nextKeys.size>0)&&currentKeys.size===nextKeys.size;if(sameKeys){for(const key of nextKeys){if(!currentKeys.has(key)){sameKeys=false;break;}}}if(sameKeys)return false;const changedKeys=new Set();const preservedMassState=opts.preserveVisualAppearance===true&&this.visualMassState&&this.visualMassState.active?this.visualMassState:null;this.baseIsolationKeys=nextKeys;this.baseIsolationActive=nextKeys.size>0;if(this.baseIsolationActive){if(this.suspendMergedRenderSystemsForBaseIsolationResponsive){const suspended=await this.suspendMergedRenderSystemsForBaseIsolationResponsive('base-isolation-responsive',{isCancelled:opts.isCancelled,onProgress:opts.onProgress,batchSize:opts.batchSize||2200});if(suspended===false&&typeof opts.isCancelled==='function'&&opts.isCancelled())return false;}else if(this.suspendMergedRenderSystemsForBaseIsolation){this.suspendMergedRenderSystemsForBaseIsolation('base-isolation-responsive');}}const visibilityResult=this.applyVisibilityStateBatched?await this.applyVisibilityStateBatched(null,{reason:opts.source==='ai-panel'?'ai-base-isolation-responsive':'base-isolation-responsive',forceChunkRebuild:false,skipChunkVisibilitySync:true,skipChunkRefresh:true,skipSectionRefresh:true,skipVisualUpdate:true,batchSize:opts.batchSize||2600,changedKeysTarget:changedKeys,isCancelled:opts.isCancelled,onProgress:(current,total,message)=>{if(typeof opts.onProgress==='function')opts.onProgress(current,total,message);}}):this.applyVisibilityState(null,{skipChunkVisibilitySync:true,skipChunkRefresh:true,skipSectionRefresh:true,skipVisualUpdate:true});if(visibilityResult===false||(typeof opts.isCancelled==='function'&&opts.isCancelled()))return false;if(changedKeys.size&&this.updateVisualAppearanceForKeysBatched){const appearanceResult=await this.updateVisualAppearanceForKeysBatched(changedKeys,{skipChunkRefresh:true,skipSectionRefresh:true,batchSize:opts.appearanceBatchSize||2200,isCancelled:opts.isCancelled,progressMessage:opts.appearanceMessage||'Actualizando elementos visibles...',onProgress:(current,total,message)=>{if(typeof opts.onProgress==='function')opts.onProgress(current,total,message);}});if(appearanceResult&&appearanceResult.cancelled)return false;}if(preservedMassState)preservedMassState.baseVisibilityRevision=this.baseVisibilityRevision||0;if(this.visualTransactionDepth&&this.visualTransactionDepth>0){this.visualTransactionRefreshAllChunks=true;this.visualTransactionRenderReason=this.visualTransactionRenderReason||'base-isolation-responsive';}return true;}async clearBaseIsolationKeysResponsive(options){const opts=options&&typeof options==='object'?options:{};if(!this.baseIsolationActive&&(!this.baseIsolationKeys||!this.baseIsolationKeys.size))return false;this.baseIsolationKeys=new Set();this.baseIsolationActive=false;const changedKeys=new Set();const visibilityResult=this.applyVisibilityStateBatched?await this.applyVisibilityStateBatched(null,{reason:'base-isolation-clear-responsive',forceChunkRebuild:false,skipChunkVisibilitySync:true,skipChunkRefresh:true,skipSectionRefresh:true,skipVisualUpdate:true,batchSize:opts.batchSize||2600,changedKeysTarget:changedKeys,isCancelled:opts.isCancelled,onProgress:(current,total,message)=>{if(typeof opts.onProgress==='function')opts.onProgress(current,total,message);}}):this.applyVisibilityState(null,{skipChunkVisibilitySync:true,skipChunkRefresh:true,skipSectionRefresh:true,skipVisualUpdate:true});if(visibilityResult===false||(typeof opts.isCancelled==='function'&&opts.isCancelled()))return false;if(changedKeys.size&&this.updateVisualAppearanceForKeysBatched){const appearanceResult=await this.updateVisualAppearanceForKeysBatched(changedKeys,{skipChunkRefresh:true,skipSectionRefresh:true,batchSize:opts.appearanceBatchSize||2200,isCancelled:opts.isCancelled,progressMessage:opts.appearanceMessage||'Restaurando alcance visible del modelo...',onProgress:(current,total,message)=>{if(typeof opts.onProgress==='function')opts.onProgress(current,total,message);}});if(appearanceResult&&appearanceResult.cancelled)return false;}if(this.visualTransactionDepth&&this.visualTransactionDepth>0){this.visualTransactionRefreshAllChunks=true;this.visualTransactionRenderReason=this.visualTransactionRenderReason||'base-isolation-clear-responsive';}return true;}setDataPanelFilterKeys(keys){const nextKeys=new Set(keys||[]);const previousKeys=new Set(this.dataPanelFilterKeys||[]);const wasActive=!!this.dataPanelFilterActive;const willBeActive=nextKeys.size>0;this.dataPanelFilterKeys=nextKeys;this.dataPanelFilterActive=willBeActive;if(this.invalidateSelectionCandidateCache)this.invalidateSelectionCandidateCache('data-panel-filter');if(wasActive!==willBeActive){this.updateVisualAppearanceForAll();return;}const changedKeys=new Set();for(const key of previousKeys)if(!nextKeys.has(key))changedKeys.add(key);for(const key of nextKeys)if(!previousKeys.has(key))changedKeys.add(key);if(changedKeys.size)this.updateVisualAppearanceForKeys(changedKeys);}clearDataPanelFilterKeys(){if(!this.dataPanelFilterActive&&(!this.dataPanelFilterKeys||!this.dataPanelFilterKeys.size))return;this.dataPanelFilterKeys=new Set();this.dataPanelFilterActive=false;if(this.invalidateSelectionCandidateCache)this.invalidateSelectionCandidateCache('data-panel-filter-clear');this.updateVisualAppearanceForAll();}isElementVisibleForDataScope(element){if(!element||!this.meshByElementKey)return false;const key=element.ModelId+':'+element.ElementId;const meshes=this.getMeshesForElementKey?this.getMeshesForElementKey(key):[this.meshByElementKey.get(key)].filter(Boolean);if(!meshes.length)return false;return meshes.some(mesh=>mesh&&mesh.userData&&mesh.userData.baseVisible!==false);}computeBaseVisibilityForMesh(mesh){if(!mesh||!mesh.userData)return false;const key=mesh.userData.elementKey;const passesFilter=this.visibilityPredicate?!!this.visibilityPredicate(mesh.userData.element):true;const passesBaseIsolation=this.baseIsolationActive&&this.baseIsolationKeys&&this.baseIsolationKeys.size?this.baseIsolationKeys.has(key):true;const passesIsolation=this.isolateActive&&this.isolatedKeys&&this.isolatedKeys.size?this.isolatedKeys.has(key):true;return passesFilter&&passesBaseIsolation&&passesIsolation&&!this.hiddenKeys.has(key);}applyVisibilityState(targetMeshes,options){const opts=options&&typeof options==='object'?options:{};const meshes=targetMeshes||this.meshes;const changedKeys=new Set();const hiddenKeys=new Set();const total=meshes&&typeof meshes.length==='number'?meshes.length:0;if(opts.onProgress)opts.onProgress(0,total,'Actualizando elementos visibles...');for(const mesh of meshes){if(!mesh||!mesh.userData)continue;const key=mesh.userData.elementKey;const nextVisible=this.computeBaseVisibilityForMesh(mesh);if(!nextVisible&&key)hiddenKeys.add(key);if(mesh.userData.baseVisible!==nextVisible){mesh.userData.baseVisible=nextVisible;if(key)changedKeys.add(key);}else if(opts.forceChunkRebuild&&key){changedKeys.add(key);}}if(changedKeys.size)this.baseVisibilityRevision=(this.baseVisibilityRevision||0)+1;if(changedKeys.size&&this.invalidateSelectionCandidateCache)this.invalidateSelectionCandidateCache('visibility-state');if(changedKeys.size&&this.sectionBoxActive&&opts.skipSectionRefresh!==true&&this.clashModeActive!==true){this.visualDirtySectionPending=true;}if(hiddenKeys.size&&opts.skipChunkVisibilitySync!==true&&this.clashModeActive!==true&&this.purgeMergedRenderResiduesForHiddenKeys){this.purgeMergedRenderResiduesForHiddenKeys(hiddenKeys,{dispose:true,deferRebuild:false});}if(changedKeys.size&&opts.skipChunkVisibilitySync!==true&&this.clashModeActive!==true&&this.handleChunkBaseVisibilityChanges){this.handleChunkBaseVisibilityChanges(changedKeys,opts.reason||'visibility-state');}if(changedKeys.size&&opts.skipVisualUpdate!==true)this.updateVisualAppearanceForKeys(changedKeys,{forceChunkRebuild:!!opts.forceChunkRebuild,skipChunkRefresh:opts.skipChunkRefresh===true,skipSectionRefresh:opts.skipSectionRefresh===true});if(opts.strictTreeVisibility&&this.finalizeStrictTreeVisibility){this.finalizeStrictTreeVisibility(meshes,changedKeys,opts.reason||'tree-visibility');}if(opts.onProgress)opts.onProgress(total,total,'Visibilidad actualizada.');return{changedKeys:changedKeys.size,total};}applyVisibilityStateBatched(targetMeshes,options){const opts=options&&typeof options==='object'?options:{};const meshes=Array.from(targetMeshes||this.meshes||[]);const changedKeys=new Set();const hiddenKeys=new Set();const token=(this.visibilityFilterUpdateToken||0)+1;this.visibilityFilterUpdateToken=token;const externalCancelled=typeof opts.isCancelled==='function'?opts.isCancelled:()=>false;const changedKeysTarget=opts.changedKeysTarget&&typeof opts.changedKeysTarget.add==='function'?opts.changedKeysTarget:null;const batchSize=Math.max(500,Math.min(6000,Number(opts.batchSize)||2800));let index=0;return new Promise(resolve=>{if(opts.onProgress)opts.onProgress(0,meshes.length,'Actualizando elementos visibles...');const step=()=>{if(this.visibilityFilterUpdateToken!==token||externalCancelled()){resolve(false);return;}const end=Math.min(meshes.length,index+batchSize);for(;index<end;index++){const mesh=meshes[index];if(!mesh||!mesh.userData)continue;const key=mesh.userData.elementKey;const nextVisible=this.computeBaseVisibilityForMesh(mesh);if(!nextVisible&&key)hiddenKeys.add(key);if(mesh.userData.baseVisible!==nextVisible){mesh.userData.baseVisible=nextVisible;if(key)changedKeys.add(key);}else if(opts.forceChunkRebuild&&key){changedKeys.add(key);}}if(opts.onProgress)opts.onProgress(index,meshes.length,'Actualizando elementos visibles...');if(index<meshes.length){const schedule=typeof requestAnimationFrame==='function'?requestAnimationFrame:callback=>setTimeout(callback,0);schedule(step);return;}if(this.visibilityFilterUpdateToken!==token||externalCancelled()){resolve(false);return;}if(opts.onProgress)opts.onProgress(Math.max(0,meshes.length-1),meshes.length,'Actualizando representación gráfica...');if(changedKeysTarget)for(const key of changedKeys)changedKeysTarget.add(key);if(changedKeys.size)this.baseVisibilityRevision=(this.baseVisibilityRevision||0)+1;if(changedKeys.size&&this.invalidateSelectionCandidateCache)this.invalidateSelectionCandidateCache('visibility-state-batched');if(changedKeys.size&&this.sectionBoxActive&&opts.skipSectionRefresh!==true&&this.clashModeActive!==true){this.visualDirtySectionPending=true;}if(hiddenKeys.size&&opts.skipChunkVisibilitySync!==true&&this.clashModeActive!==true&&this.purgeMergedRenderResiduesForHiddenKeys){this.purgeMergedRenderResiduesForHiddenKeys(hiddenKeys,{dispose:true,deferRebuild:false});}if(changedKeys.size&&opts.skipChunkVisibilitySync!==true&&this.clashModeActive!==true&&this.handleChunkBaseVisibilityChanges){this.handleChunkBaseVisibilityChanges(changedKeys,opts.reason||'visibility-state-batched');}if(changedKeys.size&&opts.skipVisualUpdate!==true)this.updateVisualAppearanceForKeys(changedKeys,{forceChunkRebuild:!!opts.forceChunkRebuild,skipChunkRefresh:opts.skipChunkRefresh===true,skipSectionRefresh:opts.skipSectionRefresh===true});if(opts.strictTreeVisibility&&this.finalizeStrictTreeVisibility){this.finalizeStrictTreeVisibility(meshes,changedKeys,opts.reason||'tree-visibility-batched');}if(opts.onProgress)opts.onProgress(meshes.length,meshes.length,'Visibilidad actualizada.');resolve({changedKeys:changedKeys.size,total:meshes.length});};step();});}showAll(){const hadIsolation=!!(this.isolateActive||(this.isolatedKeys&&this.isolatedKeys.size));const hiddenBefore=new Set(this.hiddenKeys||[]);if(!hadIsolation){if(hiddenBefore.size)this.restoreHiddenKeysIncrementally(hiddenBefore);this.syncViewerToolButtons();return;}this.isolateActive=false;this.isolatedKeys.clear();if(this.restoreIsolationRenderSnapshotFast&&this.restoreIsolationRenderSnapshotFast('show-all-fast')){if(hiddenBefore.size)this.restoreHiddenKeysIncrementally(hiddenBefore);this.syncViewerToolButtons();return;}this.hiddenKeys.clear();this.applyVisibilityState(null,{forceChunkRebuild:true,reason:'show-all'});if(this.endStrictIsolationVisibilityGuard)this.endStrictIsolationVisibilityGuard('show-all');if(this.syncInteractiveNormalChunkPeel)this.syncInteractiveNormalChunkPeel();if(this.scheduleNormalChunkEdgesForVisibleChunks)this.scheduleNormalChunkEdgesForVisibleChunks('show-all');this.syncViewerToolButtons();}showHiddenElements(){const hiddenBefore=new Set(this.hiddenKeys||[]);if(hiddenBefore.size)this.restoreHiddenKeysIncrementally(hiddenBefore);this.syncViewerToolButtons();}toggleHideSelected(){if(!this.selectedKeys||!this.selectedKeys.size){this.showHiddenElements();return false;}this.hideSelected();return this.hiddenKeys.size>0;}hideSelected(){if(!this.selectedKeys||!this.selectedKeys.size)return false;const keysToHide=new Set(this.selectedKeys||[]);if(!keysToHide.size)return false;if(!this.hiddenKeys)this.hiddenKeys=new Set();const metricsStart=this.getPerformanceNow?this.getPerformanceNow():Date.now();const hadMassTransparency=!!(this.visualMassState&&this.visualMassState.active);const hadViewerTransparencyMode=!!(this.isViewerTransparencyActive&&this.isViewerTransparencyActive());for(const key of keysToHide)this.hiddenKeys.add(key);if(this.restoreHoveredMaterial)this.restoreHoveredMaterial();const targetMeshes=this.getMeshesForElementKeys?this.getMeshesForElementKeys(keysToHide):[];let changedBaseVisibility=false;for(const mesh of targetMeshes||[]){if(!mesh||!mesh.userData)continue;if(mesh.userData.baseVisible!==false)changedBaseVisibility=true;mesh.userData.baseVisible=false;mesh.userData.normalChunkSuppressed=false;mesh.userData.visualStateChunkSuppressed=false;mesh.userData.massTransparencyChunkSuppressed=false;mesh.visible=false;for(const child of mesh.children||[])if(child)child.visible=false;}if(changedBaseVisibility)this.baseVisibilityRevision=(this.baseVisibilityRevision||0)+1;if(hadMassTransparency&&this.removeHiddenKeysFromMassTransparencyActiveLayers){this.removeHiddenKeysFromMassTransparencyActiveLayers(keysToHide);}if(this.visualMassState&&this.visualMassState.active){this.visualMassState.baseVisibilityRevision=this.baseVisibilityRevision||0;}if(this.restoreSelectionPeeledNormalChunks)this.restoreSelectionPeeledNormalChunks();if(this.cancelScheduledNormalChunkEdgesForVisibleChunks)this.cancelScheduledNormalChunkEdgesForVisibleChunks();const normalEdgeBatchesBefore=(this.normalChunkEdgeBatches||[]).filter(batch=>batch&&batch.parent).length;const purgeResult=this.purgeMergedRenderResiduesForHiddenKeys?this.purgeMergedRenderResiduesForHiddenKeys(keysToHide,{dispose:false,deferRebuild:false,preserveMassTransparencyChunks:hadMassTransparency}):null;if(this.visualMassState&&this.visualMassState.active&&this.massTransparencyChunkMeshes&&this.massTransparencyChunkMeshes.length){this.massTransparencyChunkCacheSignature=this.getMassTransparencyChunkSignature?this.getMassTransparencyChunkSignature():this.massTransparencyChunkCacheSignature;}if(this.clearInteractionOverlayForKey){for(const key of keysToHide)this.clearInteractionOverlayForKey(key);}if(this.clearSelectionOverlayBatches)this.clearSelectionOverlayBatches(true);this.selectedMesh=null;this.selectedMeshes=[];this.selectedOriginalMaterials=new Map();this.selectedKey=null;this.selectedKeys=new Set();this.visualSelectionPreserveAppearance=false;this._forceSelectionSourceAppearanceUpdate=false;if(this.selectionPeeledNormalChunks)this.selectionPeeledNormalChunks.clear();if(this.selectionChunkRestoredSources)this.selectionChunkRestoredSources.clear();if(this.visualMassState&&this.visualMassState.active){this.massTransparencyChunkActiveKeysSnapshot=new Set(this.visualMassState.activeKeys||[]);this.massTransparencyChunkInteractiveKeysSnapshot=new Set();this.groupedTransparentEdgeActiveKeysSnapshot=new Set(this.visualMassState.activeKeys||[]);this.groupedTransparentEdgeInteractiveKeysSnapshot=new Set();}if(this.updateSelectionIndexesForVisibilityChange){this.updateSelectionIndexesForVisibilityChange(targetMeshes,false,'hide-selected-fast');}if(this.invalidateMeasurementPickCache)this.invalidateMeasurementPickCache('hide-selected-fast');if(this.invalidateSelectionCandidateCache)this.invalidateSelectionCandidateCache('hide-selected-fast');if(this.invalidateRaycastScreenCandidateCache)this.invalidateRaycastScreenCandidateCache('hide-selected-fast');if(this.onSelect)this.onSelect(null);if(this.requestRender)this.requestRender('hide-selected-fast');const liveNormalEdgeBatches=(this.normalChunkEdgeBatches||[]).filter(batch=>batch&&batch.parent).length;if(!hadMassTransparency&&this.normalChunkVisible&&this.scheduleNormalChunkEdgesForVisibleChunksAsync&&liveNormalEdgeBatches===0){this.scheduleNormalChunkEdgesForVisibleChunksAsync('hide-selected-visible-edges');}if(this.syncViewerToolButtons)this.syncViewerToolButtons();if(this.recordPerformanceOperation)this.recordPerformanceOperation('hideSelected:incremental',(this.getPerformanceNow?this.getPerformanceNow():Date.now())-metricsStart,{hiddenKeys:keysToHide.size,targetMeshes:targetMeshes.length,baseVisibilityRevision:this.baseVisibilityRevision||0,transparencyPreserved:hadMassTransparency,viewerTransparencyPreserved:hadViewerTransparencyMode,normalEdgeBatchesBefore,normalEdgeBatchesPreserved:liveNormalEdgeBatches,removedNormalChunks:purgeResult&&purgeResult.normal||0,removedNormalEdges:purgeResult&&purgeResult.normalEdges||0,removedVisualStateChunks:purgeResult&&purgeResult.visualState||0,maskedMassTransparencyChunks:purgeResult&&purgeResult.massTransparency||0,removedGroupedTransparentEdges:purgeResult&&purgeResult.groupedTransparentEdges||0});return true;}async restoreIsolationWithProgress(options){const opts=options&&typeof options==='object'?options:{};const report=typeof opts.onProgress==='function'?opts.onProgress:()=>{};const hadIsolation=!!(this.isolateActive||(this.isolatedKeys&&this.isolatedKeys.size));if(!hadIsolation)return false;const meshes=Array.isArray(this.meshes)?this.meshes:[];const totalMeshes=Math.max(1,meshes.length);this.isolateActive=false;if(!this.isolatedKeys)this.isolatedKeys=new Set();this.isolatedKeys.clear();const hadCachedIsolationSnapshot=!!this.isolationRenderSnapshot;const cachedObjectCount=this.isolationRenderSnapshot&&Array.isArray(this.isolationRenderSnapshot.objectStates)?this.isolationRenderSnapshot.objectStates.length:0;if(this.restoreIsolationRenderSnapshotFast&&this.restoreIsolationRenderSnapshotFast('restore-isolation-fast')){report({message:'Aislamiento restablecido desde cache.',current:Math.max(1,cachedObjectCount),total:Math.max(1,cachedObjectCount)});this.syncViewerToolButtons();return false;}report({message:'Restaurando los elementos aislados...',current:0,total:totalMeshes});if(opts.yieldBeforeWork!==false&&this.waitForResetProgressPaint){await this.waitForResetProgressPaint();}const visibilityOptions={forceChunkRebuild:hadCachedIsolationSnapshot,reason:'restore-isolation',batchSize:Math.max(500,Number(opts.visibilityBatchSize)||3500),onProgress:(current,total,phase)=>{report({message:phase||'Restaurando los elementos aislados...',current:Math.max(0,Number(current)||0),total:Math.max(1,Number(total)||totalMeshes)});}};if(this.applyVisibilityStateBatched){await this.applyVisibilityStateBatched(null,visibilityOptions);}else{this.applyVisibilityState(null,visibilityOptions);report({message:'Visibilidad restaurada.',current:totalMeshes,total:totalMeshes});}if(this.endStrictIsolationVisibilityGuard)this.endStrictIsolationVisibilityGuard('restore-isolation');report({message:'Sincronizando bloques optimizados y bordes visibles...',current:0,total:0});if(this.waitForResetProgressPaint)await this.waitForResetProgressPaint();if(this.syncInteractiveNormalChunkPeel)this.syncInteractiveNormalChunkPeel();if(this.refreshNormalChunkEdgesForVisibleChunksAsync){await this.refreshNormalChunkEdgesForVisibleChunksAsync('restore-isolation',{onProgress:(current,total,phase)=>report({message:phase||'Sincronizando bordes visibles...',current:Math.max(0,Number(current)||0),total:Math.max(0,Number(total)||0)})});}else if(this.scheduleNormalChunkEdgesForVisibleChunks){this.scheduleNormalChunkEdgesForVisibleChunks('restore-isolation');}if(this.invalidateSelectionCandidateCache)this.invalidateSelectionCandidateCache('restore-isolation');if(this.invalidateRaycastScreenCandidateCache)this.invalidateRaycastScreenCandidateCache('restore-isolation');if(this.invalidateMeasurementPickCache)this.invalidateMeasurementPickCache('restore-isolation');if(this.requestRender)this.requestRender('restore-isolation');this.syncViewerToolButtons();report({message:'Aislamiento restablecido.',current:1,total:1});return false;}toggleIsolateSelected(){if(!this.selectedKeys||!this.selectedKeys.size||this.isolateActive){const hadIsolation=!!(this.isolateActive||(this.isolatedKeys&&this.isolatedKeys.size));const hadCachedIsolationSnapshot=!!this.isolationRenderSnapshot;this.isolateActive=false;this.isolatedKeys.clear();if(hadIsolation&&this.restoreIsolationRenderSnapshotFast&&this.restoreIsolationRenderSnapshotFast('toggle-isolation-off-fast')){this.syncViewerToolButtons();return false;}this.applyVisibilityState(null,{forceChunkRebuild:hadCachedIsolationSnapshot,reason:'toggle-isolation-off'});if(this.endStrictIsolationVisibilityGuard)this.endStrictIsolationVisibilityGuard('toggle-isolation-off');if(this.syncInteractiveNormalChunkPeel)this.syncInteractiveNormalChunkPeel();if(this.scheduleNormalChunkEdgesForVisibleChunks)this.scheduleNormalChunkEdgesForVisibleChunks('toggle-isolation-off');this.syncViewerToolButtons();return false;}return this.isolateSelected();}toggleSectionBox(){if(this.sectionBoxActive)this.disableSectionBox();else this.enableSectionBox();this.syncViewerToolButtons();return this.sectionBoxActive;}getSectionBoxTargetMeshes(){const hasRealSelection=!!(this.selectedKeys&&this.selectedKeys.size);this.sectionBoxLastTargetHadSelection=hasRealSelection;if(hasRealSelection){const selected=this.getMeshesForElementKeys?this.getMeshesForElementKeys(this.selectedKeys):(this.selectedMeshes||[]);return this.filterSectionBoxScopeMeshes(selected,{respectDataPanelFilter:false});}return this.filterSectionBoxScopeMeshes(this.meshes||[],{respectDataPanelFilter:false});}computeSectionScopeBoxFromMeshes(meshes){const box=new THREE.Box3();box.makeEmpty();const localBox=new THREE.Box3();let hasGeometry=false;for(const mesh of meshes||[]){if(!mesh||!mesh.isMesh||!mesh.geometry)continue;const geometry=mesh.geometry;if(!geometry.boundingBox){try{geometry.computeBoundingBox();}catch(_){}}if(!geometry.boundingBox)continue;mesh.updateWorldMatrix(true,false);localBox.copy(geometry.boundingBox).applyMatrix4(mesh.matrixWorld);if(!Number.isFinite(localBox.min.x)||!Number.isFinite(localBox.max.x))continue;box.union(localBox);hasGeometry=true;}return hasGeometry&&!box.isEmpty()?box:null;}cancelDeferredSectionCutEdges(){if(!this.sectionCutEdgeDeferredTimer)return;try{if(this.sectionCutEdgeDeferredTimerType==='raf'&&typeof cancelAnimationFrame==='function'){cancelAnimationFrame(this.sectionCutEdgeDeferredTimer);}else{clearTimeout(this.sectionCutEdgeDeferredTimer);}}catch(_){}this.sectionCutEdgeDeferredTimer=null;this.sectionCutEdgeDeferredTimerType=null;}scheduleDeferredSectionCutEdges(reason){this.cancelDeferredSectionCutEdges();const token=(this.sectionCutEdgeDeferredToken||0)+1;this.sectionCutEdgeDeferredToken=token;const run=()=>{this.sectionCutEdgeDeferredTimer=null;this.sectionCutEdgeDeferredTimerType=null;if(token!==this.sectionCutEdgeDeferredToken)return;if(!this.sectionBoxActive||!this.sectionBounds||this.clashModeActive===true)return;if(this.updateSectionCutEdges)this.updateSectionCutEdges();if(this.requestRender)this.requestRender(reason||'section-cut-edges-deferred');};if(typeof requestAnimationFrame==='function'){this.sectionCutEdgeDeferredTimerType='raf';this.sectionCutEdgeDeferredTimer=requestAnimationFrame(run);}else{this.sectionCutEdgeDeferredTimerType='timeout';this.sectionCutEdgeDeferredTimer=setTimeout(run,0);}}filterSectionBoxScopeMeshes(meshes,options){const opts=options&&typeof options==='object'?options:{};const respectDataPanelFilter=opts.respectDataPanelFilter===true;const result=[];const seen=new Set();for(const mesh of meshes||[]){if(!this.isMeshInSectionBoxScope(mesh,{respectDataPanelFilter}))continue;if(seen.has(mesh))continue;seen.add(mesh);result.push(mesh);}return result;}isMeshInSectionBoxScope(mesh,options){if(!mesh||!mesh.isMesh||!mesh.userData)return false;if(!mesh.geometry||!mesh.geometry.getAttribute||!mesh.geometry.getAttribute('position'))return false;if(mesh.userData.isSectionHelper)return false;if(mesh.userData.baseVisible===false)return false;const opts=options&&typeof options==='object'?options:{};const key=this.getMeshKey?this.getMeshKey(mesh):mesh.userData.elementKey;if(key){if(this.hiddenKeys&&this.hiddenKeys.has(key))return false;if(opts.respectDataPanelFilter===true&&this.dataPanelFilterActive&&this.dataPanelFilterKeys&&!this.dataPanelFilterKeys.has(key))return false;const works=this.visualWorksStates&&this.visualWorksStates.get(key);if(works&&works.hidden)return false;if(this.visualClashHiddenKeys&&this.visualClashHiddenKeys.has(key))return false;}return true;}isOrientedSectionBox(){return!!(this.sectionBoxFrame&&this.sectionLocalBounds);}clearSectionBoxOrientation(){this.sectionLocalBounds=null;this.sectionBoxFrame=null;this.sectionBoxSource=null;}getSectionWorkingBounds(){return this.isOrientedSectionBox()?this.sectionLocalBounds:this.sectionBounds;}getSectionBoxWorldBox(){if(!this.sectionBounds)return null;return new THREE.Box3(this.sectionBounds.min.clone(),this.sectionBounds.max.clone());}getSectionBoxLocalSize(target){const result=target||new THREE.Vector3();const bounds=this.getSectionWorkingBounds();if(!bounds)return result.set(0,0,0);return result.subVectors(bounds.max,bounds.min);}sectionLocalToWorld(point,target){const source=point&&point.isVector3?point:new THREE.Vector3();const result=target||new THREE.Vector3();if(!this.isOrientedSectionBox())return result.copy(source);const frame=this.sectionBoxFrame;return result.copy(frame.origin).addScaledVector(frame.right,source.x).addScaledVector(frame.up,source.y).addScaledVector(frame.normal,source.z);}sectionWorldToLocal(point,target){const source=point&&point.isVector3?point:new THREE.Vector3();const result=target||new THREE.Vector3();if(!this.isOrientedSectionBox())return result.copy(source);const delta=source.clone().sub(this.sectionBoxFrame.origin);return result.set(delta.dot(this.sectionBoxFrame.right),delta.dot(this.sectionBoxFrame.up),delta.dot(this.sectionBoxFrame.normal));}getSectionBoxCornersWorld(){const bounds=this.getSectionWorkingBounds();if(!bounds)return[];const min=bounds.min;const max=bounds.max;const localCorners=[new THREE.Vector3(min.x,min.y,min.z),new THREE.Vector3(max.x,min.y,min.z),new THREE.Vector3(max.x,max.y,min.z),new THREE.Vector3(min.x,max.y,min.z),new THREE.Vector3(min.x,min.y,max.z),new THREE.Vector3(max.x,min.y,max.z),new THREE.Vector3(max.x,max.y,max.z),new THREE.Vector3(min.x,max.y,max.z)];return localCorners.map(point=>this.sectionLocalToWorld(point,new THREE.Vector3()));}updateSectionWorldBoundsFromLocal(){if(!this.isOrientedSectionBox())return this.sectionBounds;const worldBox=new THREE.Box3();worldBox.makeEmpty();for(const point of this.getSectionBoxCornersWorld())worldBox.expandByPoint(point);this.sectionBounds={min:worldBox.min.clone(),max:worldBox.max.clone()};return this.sectionBounds;}createSectionBoxFrame(originValue,rightValue,upValue,normalValue){const asVector=(value,fallback)=>{if(value&&value.isVector3)return value.clone();if(Array.isArray(value))return new THREE.Vector3(Number(value[0])||0,Number(value[1])||0,Number(value[2])||0);return fallback.clone();};const origin=asVector(originValue,new THREE.Vector3());const right=asVector(rightValue,new THREE.Vector3(1,0,0));const up=asVector(upValue,new THREE.Vector3(0,1,0));const exportedNormal=asVector(normalValue,new THREE.Vector3(0,0,1));if(right.lengthSq()<1e-12)right.set(1,0,0);right.normalize();up.addScaledVector(right,-up.dot(right));if(up.lengthSq()<1e-12){const alternative=Math.abs(right.y)<0.9?new THREE.Vector3(0,1,0):new THREE.Vector3(0,0,1);up.copy(alternative.addScaledVector(right,-alternative.dot(right)));}up.normalize();const normal=new THREE.Vector3().crossVectors(right,up);if(normal.lengthSq()<1e-12)normal.copy(exportedNormal);if(normal.lengthSq()<1e-12)normal.set(0,0,1);normal.normalize();if(exportedNormal.lengthSq()>1e-12&&normal.dot(exportedNormal.normalize())<0){up.multiplyScalar(-1);normal.multiplyScalar(-1);}return{origin,right,up,normal};}getSectionFrameFromMeshModelTransform(mesh){const element=mesh&&mesh.userData&&mesh.userData.element;const transform=element&&element.ModelTransform;if(!Array.isArray(transform)||transform.length<16)return null;const matrix=this.createThreeMatrixFromRevitTransform(transform);const right=new THREE.Vector3().setFromMatrixColumn(matrix,0);const up=new THREE.Vector3().setFromMatrixColumn(matrix,1);const normal=new THREE.Vector3().setFromMatrixColumn(matrix,2);return this.createSectionBoxFrame(new THREE.Vector3(),right,up,normal);}getDominantSectionModelFrame(meshes,origin){const candidates=new Map();for(const mesh of meshes||[]){const frame=this.getSectionFrameFromMeshModelTransform(mesh);if(!frame)continue;const values=[frame.right.x,frame.right.y,frame.right.z,frame.up.x,frame.up.y,frame.up.z];const key=values.map(value=>Math.round(value*10000)).join('|');const entry=candidates.get(key)||{count:0,frame};entry.count++;candidates.set(key,entry);}let dominant=null;for(const entry of candidates.values()){if(!dominant||entry.count>dominant.count)dominant=entry;}const frame=dominant?dominant.frame:this.createSectionBoxFrame(new THREE.Vector3(),new THREE.Vector3(1,0,0),new THREE.Vector3(0,1,0),new THREE.Vector3(0,0,1));frame.origin.copy(origin||new THREE.Vector3());return frame;}refineSectionFrameForSelectedMeshes(frame,meshes){if(!frame||!meshes||!meshes.length)return frame;let totalVertices=0;for(const mesh of meshes){const position=mesh&&mesh.geometry&&mesh.geometry.getAttribute&&mesh.geometry.getAttribute('position');totalVertices+=position?position.count:0;}if(totalVertices<3)return frame;const maxSamples=12000;const stride=Math.max(1,Math.ceil(totalVertices/maxSamples));const point=new THREE.Vector3();const delta=new THREE.Vector3();let count=0;let sumU=0;let sumV=0;let sumUU=0;let sumUV=0;let sumVV=0;for(const mesh of meshes){const position=mesh&&mesh.geometry&&mesh.geometry.getAttribute&&mesh.geometry.getAttribute('position');if(!position)continue;mesh.updateWorldMatrix(true,false);for(let index=0;index<position.count;index+=stride){point.fromBufferAttribute(position,index).applyMatrix4(mesh.matrixWorld);delta.copy(point).sub(frame.origin);const u=delta.dot(frame.right);const v=delta.dot(frame.normal);if(!Number.isFinite(u)||!Number.isFinite(v))continue;count++;sumU+=u;sumV+=v;sumUU+=u*u;sumUV+=u*v;sumVV+=v*v;}}if(count<3)return frame;const meanU=sumU/count;const meanV=sumV/count;const covarianceUU=Math.max(0,sumUU/count-meanU*meanU);const covarianceUV=sumUV/count-meanU*meanV;const covarianceVV=Math.max(0,sumVV/count-meanV*meanV);const trace=covarianceUU+covarianceVV;const anisotropy=Math.hypot(covarianceUU-covarianceVV,2*covarianceUV);if(!Number.isFinite(trace)||trace<1e-10||anisotropy<trace*0.015)return frame;const angle=0.5*Math.atan2(2*covarianceUV,covarianceUU-covarianceVV);const right=frame.right.clone().multiplyScalar(Math.cos(angle)).add(frame.normal.clone().multiplyScalar(Math.sin(angle))).normalize();if(right.dot(frame.right)<0)right.multiplyScalar(-1);const normal=new THREE.Vector3().crossVectors(right,frame.up).normalize();return this.createSectionBoxFrame(frame.origin,right,frame.up,normal);}computeSectionLocalBoundsFromMeshes(meshes,frame,exactVertices){if(!frame)return null;const bounds=new THREE.Box3();bounds.makeEmpty();const worldPoint=new THREE.Vector3();const localPoint=new THREE.Vector3();const delta=new THREE.Vector3();const localBox=new THREE.Box3();const corners=Array.from({length:8},()=>new THREE.Vector3());let hasPoint=false;const expandWorldPoint=point=>{delta.copy(point).sub(frame.origin);localPoint.set(delta.dot(frame.right),delta.dot(frame.up),delta.dot(frame.normal));if(!Number.isFinite(localPoint.x)||!Number.isFinite(localPoint.y)||!Number.isFinite(localPoint.z))return;bounds.expandByPoint(localPoint);hasPoint=true;};let totalVertices=0;for(const mesh of meshes||[]){const position=mesh&&mesh.geometry&&mesh.geometry.getAttribute&&mesh.geometry.getAttribute('position');totalVertices+=position?position.count:0;}const useExactVertices=exactVertices===true&&totalVertices>0&&totalVertices<=300000;for(const mesh of meshes||[]){if(!mesh||!mesh.geometry)continue;mesh.updateWorldMatrix(true,false);const position=mesh.geometry.getAttribute&&mesh.geometry.getAttribute('position');if(useExactVertices&&position){for(let index=0;index<position.count;index++){worldPoint.fromBufferAttribute(position,index).applyMatrix4(mesh.matrixWorld);expandWorldPoint(worldPoint);}continue;}if(!mesh.geometry.boundingBox){try{mesh.geometry.computeBoundingBox();}catch(_){}}if(!mesh.geometry.boundingBox)continue;localBox.copy(mesh.geometry.boundingBox);const min=localBox.min;const max=localBox.max;corners[0].set(min.x,min.y,min.z);corners[1].set(max.x,min.y,min.z);corners[2].set(max.x,max.y,min.z);corners[3].set(min.x,max.y,min.z);corners[4].set(min.x,min.y,max.z);corners[5].set(max.x,min.y,max.z);corners[6].set(max.x,max.y,max.z);corners[7].set(min.x,max.y,max.z);for(const corner of corners)expandWorldPoint(corner.applyMatrix4(mesh.matrixWorld));}return hasPoint&&!bounds.isEmpty()?bounds:null;}createAlignedSectionBoxFromMeshes(meshes,options){const targets=Array.isArray(meshes)?meshes.filter(Boolean):[];if(!targets.length)return null;const opts=options&&typeof options==='object'?options:{};const worldBox=this.computeSectionScopeBoxFromMeshes(targets);if(!worldBox||worldBox.isEmpty())return null;let frame=this.getDominantSectionModelFrame(targets,worldBox.getCenter(new THREE.Vector3()));if(opts.alignToSelection===true)frame=this.refineSectionFrameForSelectedMeshes(frame,targets);const exactVertices=Object.prototype.hasOwnProperty.call(opts,'exactVertices')?opts.exactVertices===true:opts.alignToSelection===true;const localBounds=this.computeSectionLocalBoundsFromMeshes(targets,frame,exactVertices);if(!localBounds||localBounds.isEmpty())return null;const size=localBounds.getSize(new THREE.Vector3());const requestedMarginFactor=Number(opts.marginFactor);const margin=Number.isFinite(requestedMarginFactor)&&requestedMarginFactor>=0?Math.max(Math.max(size.x,size.y,size.z,1)*requestedMarginFactor,requestedMarginFactor>0?0.15:0):Math.max(size.length()*0.015,0.02);localBounds.min.addScalar(-margin);localBounds.max.addScalar(margin);return{frame,localBounds};}getSectionScopeProjectionDepth(origin,normal){const targets=this.getSectionBoxTargetMeshes?this.getSectionBoxTargetMeshes():(this.meshes||[]);const box=this.computeSectionScopeBoxFromMeshes?this.computeSectionScopeBoxFromMeshes(targets):null;if(!box||box.isEmpty())return 0;const corners=[new THREE.Vector3(box.min.x,box.min.y,box.min.z),new THREE.Vector3(box.min.x,box.min.y,box.max.z),new THREE.Vector3(box.min.x,box.max.y,box.min.z),new THREE.Vector3(box.min.x,box.max.y,box.max.z),new THREE.Vector3(box.max.x,box.min.y,box.min.z),new THREE.Vector3(box.max.x,box.min.y,box.max.z),new THREE.Vector3(box.max.x,box.max.y,box.min.z),new THREE.Vector3(box.max.x,box.max.y,box.max.z)];let minProjection=Infinity;for(const corner of corners)minProjection=Math.min(minProjection,corner.clone().sub(origin).dot(normal));if(!Number.isFinite(minProjection))return 0;const margin=Math.max(box.getSize(new THREE.Vector3()).length()*0.01,0.05);return Math.max(0,-minProjection+margin);}capturePlansPanelViewState(){const cloneVector=value=>value&&typeof value.clone==='function'?value.clone():null;const cloneBounds=bounds=>bounds&&bounds.min&&bounds.max?{min:cloneVector(bounds.min),max:cloneVector(bounds.max)}:null;const cloneFrame=frame=>frame?{origin:cloneVector(frame.origin),right:cloneVector(frame.right),up:cloneVector(frame.up),normal:cloneVector(frame.normal)}:null;return{camera:{projectionMode:this.projectionMode,navigationMode:this.navigationMode,position:cloneVector(this.camera&&this.camera.position),quaternion:this.camera&&this.camera.quaternion&&this.camera.quaternion.clone?this.camera.quaternion.clone():null,up:cloneVector(this.camera&&this.camera.up),target:cloneVector(this.target),orbitPivot:cloneVector(this.orbitPivot),distance:Number(this.distance),orthoScale:Number(this.orthoScale),theta:Number(this.theta),phi:Number(this.phi)},sectionBox:{active:this.sectionBoxActive===true,bounds:cloneBounds(this.sectionBounds),localBounds:cloneBounds(this.sectionLocalBounds),frame:cloneFrame(this.sectionBoxFrame),source:this.sectionBoxSource==null?null:String(this.sectionBoxSource)}};}restorePlansPanelViewState(snapshot){if(!snapshot||typeof snapshot!=='object')return false;const section=snapshot.sectionBox||{};if(section.active){if(this.cancelDeferredSectionCutEdges)this.cancelDeferredSectionCutEdges();if(this.clearPlansOverlayCutPlane)this.clearPlansOverlayCutPlane();const cloneVector=value=>value&&typeof value.clone==='function'?value.clone():null;const cloneBounds=bounds=>bounds&&bounds.min&&bounds.max?{min:cloneVector(bounds.min),max:cloneVector(bounds.max)}:null;const cloneFrame=frame=>frame?{origin:cloneVector(frame.origin),right:cloneVector(frame.right),up:cloneVector(frame.up),normal:cloneVector(frame.normal)}:null;this.sectionBoxFrame=cloneFrame(section.frame);this.sectionLocalBounds=cloneBounds(section.localBounds);this.sectionBounds=cloneBounds(section.bounds);this.sectionBoxSource=section.source==null?null:String(section.source);if(this.sectionBoxFrame&&this.sectionLocalBounds&&this.updateSectionWorldBoundsFromLocal){this.updateSectionWorldBoundsFromLocal();}this.sectionBoxActive=true;this.sectionActiveFace=null;this.sectionDrag=null;this.sectionHandleBaseSize=null;this.updateSectionPlanes();if(this.syncMassTransparencyRenderForSectionBox)this.syncMassTransparencyRenderForSectionBox('plans-panel-section-restore');this.buildSectionCaps();this.buildSectionBoxVisual();if(this.clearSectionCutEdges)this.clearSectionCutEdges();if(this.scheduleDeferredSectionCutEdges)this.scheduleDeferredSectionCutEdges('plans-panel-restore');else if(this.updateSectionCutEdges)this.updateSectionCutEdges();}else if(this.sectionBoxActive&&this.disableSectionBox){this.disableSectionBox();}const camera=snapshot.camera||{};if(camera.projectionMode&&camera.projectionMode!==this.projectionMode&&this.switchProjectionMode){this.switchProjectionMode(camera.projectionMode);}if(camera.navigationMode)this.navigationMode=camera.navigationMode;if(this.target&&camera.target)this.target.copy(camera.target);if(camera.orbitPivot&&camera.orbitPivot.clone)this.orbitPivot=camera.orbitPivot.clone();else this.orbitPivot=null;if(Number.isFinite(camera.distance))this.distance=camera.distance;if(Number.isFinite(camera.orthoScale))this.orthoScale=camera.orthoScale;if(Number.isFinite(camera.theta))this.theta=camera.theta;if(Number.isFinite(camera.phi))this.phi=camera.phi;if(this.camera){if(camera.position)this.camera.position.copy(camera.position);if(camera.up)this.camera.up.copy(camera.up);if(camera.quaternion)this.camera.quaternion.copy(camera.quaternion);this.camera.updateMatrixWorld(true);}if(this.updateCameraClipRange)this.updateCameraClipRange();if(this.updateCameraProjection)this.updateCameraProjection();if(this.updateViewCubeOrientation)this.updateViewCubeOrientation();if(this.syncViewerToolButtons)this.syncViewerToolButtons();if(this.requestRender)this.requestRender('plans-panel-view-restored');return true;}getSectionBoxCentroid(){if(!this.sectionBoxActive||typeof THREE==='undefined')return null;if(this.sectionBoxFrame&&this.sectionLocalBounds&&this.sectionLocalBounds.min&&this.sectionLocalBounds.max){const frame=this.sectionBoxFrame;const localCenter=this.sectionLocalBounds.min.clone().add(this.sectionLocalBounds.max).multiplyScalar(0.5);return frame.origin.clone().add(frame.right.clone().multiplyScalar(localCenter.x)).add(frame.up.clone().multiplyScalar(localCenter.y)).add(frame.normal.clone().multiplyScalar(localCenter.z));}if(this.sectionBounds&&this.sectionBounds.min&&this.sectionBounds.max){return this.sectionBounds.min.clone().add(this.sectionBounds.max).multiplyScalar(0.5);}return null;}setOrbitPivotToSectionBoxCenter(){const center=this.getSectionBoxCentroid();if(!center)return false;this.orbitPivot=center.clone();if(this.requestRender)this.requestRender('native-view-section-box-pivot');return center;}enableSectionBoxForNativeView(spatial,basis,options){if(!spatial||!basis||typeof THREE==='undefined')return false;if(this.cancelDeferredSectionCutEdges)this.cancelDeferredSectionCutEdges();if(this.clearPlansOverlayCutPlane)this.clearPlansOverlayCutPlane();const numberOr=(value,fallback)=>{const number=Number(value);return Number.isFinite(number)?number:fallback;};const vectorFrom=(value,fallback)=>{if(value&&value.isVector3)return value.clone();if(Array.isArray(value))return new THREE.Vector3(numberOr(value[0],0),numberOr(value[1],0),numberOr(value[2],0));return fallback.clone();};const planePoint=vectorFrom(spatial.modelCropCenter||spatial.cutPlanePoint||spatial.center,new THREE.Vector3());const frame=this.createSectionBoxFrame(planePoint,basis.right,basis.up,basis.normal);const width=Math.max(0.01,numberOr(spatial.modelCropWidth,numberOr(spatial.width,1)));const height=Math.max(0.01,numberOr(spatial.modelCropHeight,numberOr(spatial.height,1)));const cropMin=Array.isArray(spatial.cropMin)?spatial.cropMin:[0,0,0];const cropMax=Array.isArray(spatial.cropMax)?spatial.cropMax:[0,0,0];const exportedDepth=Math.abs(numberOr(cropMax[2],0)-numberOr(cropMin[2],0));const projectedDepth=this.getSectionScopeProjectionDepth(frame.origin,frame.normal);const minimumDepth=Math.max(Math.min(width,height)*0.015,0.08);const requestedDepth=numberOr(options&&options.depthBehindViewMeters,0);const depth=requestedDepth>0?Math.max(minimumDepth,requestedDepth):Math.max(minimumDepth,exportedDepth>0.01?exportedDepth:projectedDepth,0.25);this.sectionBoxFrame=frame;this.sectionLocalBounds={min:new THREE.Vector3(-width*0.5,-height*0.5,-depth),max:new THREE.Vector3(width*0.5,height*0.5,0)};this.sectionBoxSource=String(options&&options.source||'native-view');this.updateSectionWorldBoundsFromLocal();this.sectionHandleBaseSize=null;this.sectionEdgeReferenceOrthoScale=Math.max(0.05,Number(this.orthoScale)||1);this.sectionEdgeReferenceRadius=null;this.sectionEdgeTargetPixels=null;this.sectionBoxActive=true;this.sectionActiveFace=null;this.sectionBoxLastTargetHadSelection=false;this.updateSectionPlanes();if(this.syncMassTransparencyRenderForSectionBox)this.syncMassTransparencyRenderForSectionBox('native-view-section-enable');this.buildSectionCaps();this.buildSectionBoxVisual();if(this.clearSectionCutEdges)this.clearSectionCutEdges();if(this.scheduleDeferredSectionCutEdges)this.scheduleDeferredSectionCutEdges('native-view-section-cut-edges');else if(this.updateSectionCutEdges)this.updateSectionCutEdges();if(this.syncViewerToolButtons)this.syncViewerToolButtons();if(this.requestRender)this.requestRender('native-view-section-box');return true;}enableSectionBox(){const targets=this.getSectionBoxTargetMeshes?this.getSectionBoxTargetMeshes():(this.selectedMeshes&&this.selectedMeshes.length?this.selectedMeshes:this.meshes.filter(mesh=>mesh.visible));if(!targets.length)return;const aligned=this.createAlignedSectionBoxFromMeshes(targets,{alignToSelection:this.sectionBoxLastTargetHadSelection===true});if(!aligned)return;this.sectionBoxFrame=aligned.frame;this.sectionLocalBounds={min:aligned.localBounds.min.clone(),max:aligned.localBounds.max.clone()};this.sectionBoxSource=this.sectionBoxLastTargetHadSelection?'selection-aligned':'model-aligned';this.updateSectionWorldBoundsFromLocal();this.sectionHandleBaseSize=null;this.sectionEdgeReferenceOrthoScale=Math.max(0.05,Number(this.orthoScale)||1);this.sectionEdgeReferenceRadius=null;this.sectionEdgeTargetPixels=null;this.sectionBoxActive=true;this.updateSectionPlanes();if(this.syncMassTransparencyRenderForSectionBox)this.syncMassTransparencyRenderForSectionBox('section-enable');this.buildSectionCaps();this.buildSectionBoxVisual();if(this.sectionBoxLastTargetHadSelection&&this.scheduleDeferredSectionCutEdges){this.clearSectionCutEdges&&this.clearSectionCutEdges();if(this.requestRender)this.requestRender('section-enable-selected-fast');this.scheduleDeferredSectionCutEdges('section-enable-selected-cut-edges');}else{if(this.cancelDeferredSectionCutEdges)this.cancelDeferredSectionCutEdges();this.updateSectionCutEdges();}this.syncViewerToolButtons();}enableSectionBoxForElementKeys(keys,options){const keySet=new Set(keys||[]);if(!keySet.size)return false;const candidates=this.getMeshesForElementKeys?this.getMeshesForElementKeys(keySet):(this.meshes||[]).filter(mesh=>{const key=this.getMeshKey?this.getMeshKey(mesh):(mesh&&mesh.userData&&mesh.userData.elementKey);return key&&keySet.has(key);});const targets=this.filterSectionBoxScopeMeshes?this.filterSectionBoxScopeMeshes(candidates,{respectDataPanelFilter:false}):candidates;if(!targets.length)return false;const aligned=this.createAlignedSectionBoxFromMeshes(targets,{alignToSelection:true});if(!aligned)return false;if(this.cancelDeferredSectionCutEdges)this.cancelDeferredSectionCutEdges();this.sectionBoxFrame=aligned.frame;this.sectionLocalBounds={min:aligned.localBounds.min.clone(),max:aligned.localBounds.max.clone()};this.sectionBoxSource=String(options&&options.source||'element-keys-aligned');this.updateSectionWorldBoundsFromLocal();this.sectionHandleBaseSize=null;this.sectionEdgeReferenceOrthoScale=Math.max(0.05,Number(this.orthoScale)||1);this.sectionEdgeReferenceRadius=null;this.sectionEdgeTargetPixels=null;this.sectionBoxActive=true;this.sectionActiveFace=null;this.sectionBoxLastTargetHadSelection=false;this.updateSectionPlanes();if(this.syncMassTransparencyRenderForSectionBox)this.syncMassTransparencyRenderForSectionBox('section-enable-element-keys');this.buildSectionCaps();this.buildSectionBoxVisual();if(this.clearSectionCutEdges)this.clearSectionCutEdges();if(this.scheduleDeferredSectionCutEdges)this.scheduleDeferredSectionCutEdges('section-enable-element-keys');else if(this.updateSectionCutEdges)this.updateSectionCutEdges();if(this.syncViewerToolButtons)this.syncViewerToolButtons();if(this.requestRender)this.requestRender('section-enable-element-keys');return true;}enableSectionBoxForBox(sourceBox,marginFactor,options){if(this.cancelDeferredSectionCutEdges)this.cancelDeferredSectionCutEdges();if(!sourceBox||sourceBox.isEmpty())return null;const opts=options&&typeof options==='object'?options:{};const box=sourceBox.clone();const size=box.getSize(new THREE.Vector3());const margin=Math.max(Math.max(size.x,size.y,size.z,1)*(Number(marginFactor)||0.16),0.15);box.min.addScalar(-margin);box.max.addScalar(margin);const center=box.getCenter(new THREE.Vector3());this.sectionBoxFrame=this.createSectionBoxFrame(center,new THREE.Vector3(1,0,0),new THREE.Vector3(0,1,0),new THREE.Vector3(0,0,1));this.sectionLocalBounds={min:box.min.clone().sub(center),max:box.max.clone().sub(center)};this.sectionBoxSource=String(opts.source||'world-box');this.updateSectionWorldBoundsFromLocal();this.sectionHandleBaseSize=null;this.sectionEdgeReferenceOrthoScale=Math.max(0.05,Number(this.orthoScale)||1);this.sectionEdgeReferenceRadius=null;this.sectionEdgeTargetPixels=null;this.sectionBoxActive=true;this.sectionActiveFace=null;this.updateSectionPlanes();if(this.syncMassTransparencyRenderForSectionBox)this.syncMassTransparencyRenderForSectionBox('section-enable-box');if(opts.fast===true||opts.skipCapsAndCutEdges===true){if(this.clearSectionCaps)this.clearSectionCaps();if(this.clearSectionCutEdges)this.clearSectionCutEdges();this.buildSectionBoxVisual();}else{this.buildSectionCaps();this.buildSectionBoxVisual();this.updateSectionCutEdges();}this.syncViewerToolButtons();return box;}enableSectionBoxForBoxFastForClash(sourceBox,marginFactor){return this.enableSectionBoxForBox(sourceBox,marginFactor,{fast:true,skipCapsAndCutEdges:true});}enableSectionBoxForMeshesFastForClash(meshes,marginFactor){if(this.cancelDeferredSectionCutEdges)this.cancelDeferredSectionCutEdges();const targets=Array.isArray(meshes)?meshes.filter(Boolean):[];if(!targets.length||!this.createAlignedSectionBoxFromMeshes)return null;const aligned=this.createAlignedSectionBoxFromMeshes(targets,{alignToSelection:true,exactVertices:false,marginFactor:Number.isFinite(Number(marginFactor))?Number(marginFactor):0.16});if(!aligned)return null;this.sectionBoxFrame=aligned.frame;this.sectionLocalBounds={min:aligned.localBounds.min.clone(),max:aligned.localBounds.max.clone()};this.sectionBoxSource='clash-pair-aligned';this.updateSectionWorldBoundsFromLocal();this.sectionHandleBaseSize=null;this.sectionEdgeReferenceOrthoScale=Math.max(0.05,Number(this.orthoScale)||1);this.sectionEdgeReferenceRadius=null;this.sectionEdgeTargetPixels=null;this.sectionBoxActive=true;this.sectionActiveFace=null;this.updateSectionPlanes();if(this.clearSectionCaps)this.clearSectionCaps();if(this.clearSectionCutEdges)this.clearSectionCutEdges();this.buildSectionBoxVisual();this.syncViewerToolButtons();return this.getSectionBoxWorldBox?this.getSectionBoxWorldBox():null;}getActiveClippingPlanes(){const planes=Array.isArray(this.sectionPlanes)?this.sectionPlanes.slice():[];if(this.plansOverlayCutPlane)planes.push(this.plansOverlayCutPlane);return planes;}setPlansOverlayCutPlane(point,viewNormal){if(!point||!viewNormal||typeof THREE==='undefined'){return this.clearPlansOverlayCutPlane();}const planePoint=point.isVector3?point.clone():new THREE.Vector3(Number(point.x)||0,Number(point.y)||0,Number(point.z)||0);const normal=viewNormal.isVector3?viewNormal.clone():new THREE.Vector3(Number(viewNormal.x)||0,Number(viewNormal.y)||0,Number(viewNormal.z)||0);if(normal.lengthSq()<1e-12)return false;normal.normalize().multiplyScalar(-1);this.plansOverlayCutPlane=new THREE.Plane().setFromNormalAndCoplanarPoint(normal,planePoint);if(this.renderer)this.renderer.clippingPlanes=this.getActiveClippingPlanes();if(this.requestRender)this.requestRender('plans-overlay-cut-plane');return true;}clearPlansOverlayCutPlane(){const changed=!!this.plansOverlayCutPlane;this.plansOverlayCutPlane=null;if(this.renderer)this.renderer.clippingPlanes=this.getActiveClippingPlanes();if(changed&&this.requestRender)this.requestRender('plans-overlay-cut-clear');return changed;}disableSectionBox(){if(this.cancelDeferredSectionCutEdges)this.cancelDeferredSectionCutEdges();this.sectionCutEdgeDeferredToken=(this.sectionCutEdgeDeferredToken||0)+1;this.sectionBoxActive=false;this.sectionBounds=null;this.clearSectionBoxOrientation();this.sectionPlanes=[];this.renderer.clippingPlanes=this.getActiveClippingPlanes();if(this.syncMassTransparencyRenderForSectionBox)this.syncMassTransparencyRenderForSectionBox('section-disable');if(this.sectionBoxGroup){this.sectionOverlayScene.remove(this.sectionBoxGroup);this.sectionBoxGroup.traverse(child=>{if(child.geometry)child.geometry.dispose&&child.geometry.dispose();if(child.material)child.material.dispose&&child.material.dispose();});}this.sectionBoxGroup=null;this.sectionHandles=[];this.sectionRotationHandles=[];this.sectionDrag=null;this.sectionActiveFace=null;this.sectionActiveRotationAxis=null;this.sectionHandleBaseSize=null;this.sectionEdgeReferenceOrthoScale=null;this.sectionEdgeReferenceRadius=null;this.sectionEdgeTargetPixels=null;this.clearSectionCaps();this.clearSectionCutEdges();this.syncViewerToolButtons();}updateSectionPlanes(){if(!this.sectionBounds)return;const faces=['xMin','xMax','yMin','yMax','zMin','zMax'];if(this.isOrientedSectionBox()){const definitions=faces.map(face=>{const inwardNormal=this.getOrientedSectionFaceNormal(face).multiplyScalar(-1);const point=this.getSectionFaceCenter(face);return new THREE.Plane().setFromNormalAndCoplanarPoint(inwardNormal,point);});if(!this.sectionPlanes||this.sectionPlanes.length!==definitions.length)this.sectionPlanes=definitions;else{for(let i=0;i<definitions.length;i++)this.sectionPlanes[i].copy(definitions[i]);}}else{const min=this.sectionBounds.min;const max=this.sectionBounds.max;const definitions=[[new THREE.Vector3(1,0,0),-min.x],[new THREE.Vector3(-1,0,0),max.x],[new THREE.Vector3(0,1,0),-min.y],[new THREE.Vector3(0,-1,0),max.y],[new THREE.Vector3(0,0,1),-min.z],[new THREE.Vector3(0,0,-1),max.z]];if(!this.sectionPlanes||this.sectionPlanes.length!==definitions.length){this.sectionPlanes=definitions.map(([normal,constant])=>new THREE.Plane(normal,constant));}else{for(let i=0;i<definitions.length;i++){this.sectionPlanes[i].normal.copy(definitions[i][0]);this.sectionPlanes[i].constant=definitions[i][1];}}}this.renderer.clippingPlanes=this.getActiveClippingPlanes();this.updateSectionCapPlanes();}buildSectionCaps(){this.clearSectionCaps();}collectSectionRenderMeshes(){const result=[];const seen=new Set();const addDirect=mesh=>{if(!mesh||!mesh.isMesh||seen.has(mesh))return;if(mesh.userData&&mesh.userData.isSectionHelper)return;if(this.isNonRenderedDataMesh(mesh))return;if(!mesh.visible)return;if(!mesh.geometry||!mesh.geometry.getAttribute||!mesh.geometry.getAttribute('position'))return;seen.add(mesh);result.push(mesh);};const addSource=mesh=>{if(!mesh||!mesh.isMesh||seen.has(mesh))return;if(mesh.userData&&mesh.userData.isSectionHelper)return;if(this.isNonRenderedDataMesh(mesh))return;if(!this.isSourceMeshVisibleForSectionCut(mesh))return;if(!mesh.geometry||!mesh.geometry.getAttribute||!mesh.geometry.getAttribute('position'))return;seen.add(mesh);result.push(mesh);};const addChunkOrSources=chunk=>{if(!chunk||!chunk.visible)return;const data=chunk.userData||{};const sources=Array.isArray(data.normalChunkSourceMeshes)?data.normalChunkSourceMeshes:(Array.isArray(data.visualStateChunkSourceMeshes)?data.visualStateChunkSourceMeshes:(Array.isArray(data.massTransparencyChunkSourceMeshes)?data.massTransparencyChunkSourceMeshes:null));if(sources&&sources.length){for(const source of sources)addSource(source);return;}addDirect(chunk);};for(const mesh of this.normalChunkMeshes||[])addChunkOrSources(mesh);for(const mesh of this.visualStateChunkMeshes||[])addChunkOrSources(mesh);for(const mesh of this.massTransparencyChunkMeshes||[])addChunkOrSources(mesh);for(const mesh of this.meshes||[])addDirect(mesh);return result;}isSourceMeshVisibleForSectionCut(mesh){if(!mesh||!mesh.userData||mesh.userData.baseVisible===false)return false;const key=mesh.userData.elementKey||(this.getMeshKey?this.getMeshKey(mesh):null);if(key){if(this.hiddenKeys&&this.hiddenKeys.has(key))return false;if(this.dataPanelFilterActive&&this.dataPanelFilterKeys&&!this.dataPanelFilterKeys.has(key))return false;const works=this.visualWorksStates&&this.visualWorksStates.get(key);if(works&&works.hidden)return false;if(this.visualClashHiddenKeys&&this.visualClashHiddenKeys.has(key))return false;}return true;}collectMeshSectionSegments(mesh,planeIndex){const segments=[];const plane=this.sectionPlanes&&this.sectionPlanes[planeIndex];if(!mesh||!mesh.geometry||!plane)return segments;const geometry=mesh.geometry;const position=geometry.getAttribute?geometry.getAttribute('position'):null;if(!position||position.count<3)return segments;mesh.updateMatrixWorld(true);const index=geometry.index;const triangleCount=index?Math.floor(index.count/3):Math.floor(position.count/3);const a=new THREE.Vector3();const b=new THREE.Vector3();const c=new THREE.Vector3();const outA=new THREE.Vector3();const outB=new THREE.Vector3();for(let triangle=0;triangle<triangleCount;triangle++){const offset=triangle*3;const ia=index?index.getX(offset):offset;const ib=index?index.getX(offset+1):offset+1;const ic=index?index.getX(offset+2):offset+2;a.fromBufferAttribute(position,ia).applyMatrix4(mesh.matrixWorld);b.fromBufferAttribute(position,ib).applyMatrix4(mesh.matrixWorld);c.fromBufferAttribute(position,ic).applyMatrix4(mesh.matrixWorld);const segment=this.getTrianglePlaneIntersection(a,b,c,plane,outA,outB);if(!segment)continue;if(!this.isPointInsideOtherSectionPlanes(segment[0],planeIndex))continue;if(!this.isPointInsideOtherSectionPlanes(segment[1],planeIndex))continue;segments.push([segment[0],segment[1]]);}return segments;}createSectionCapGeometryFromSegments(segments,plane,state){const loops=this.buildSectionLoopsFromSegments(segments);if(!loops.length)return null;const basis=this.getSectionPlaneBasis(plane);const positions=[];const indices=[];const offset=plane.normal.clone().normalize().multiplyScalar(this.getSectionGeometryOffset());for(const loop of loops){if(!loop||loop.length<3)continue;const origin=loop[0];const points2D=loop.map(point=>{const local=point.clone().sub(origin);return new THREE.Vector2(local.dot(basis.u),local.dot(basis.v));});const triangles=THREE.ShapeUtils.triangulateShape(points2D,[]);if(!triangles||!triangles.length)continue;const baseIndex=positions.length/3;for(const point of loop){const shifted=point.clone().add(offset);positions.push(shifted.x,shifted.y,shifted.z);}for(const triangle of triangles){indices.push(baseIndex+triangle[0],baseIndex+triangle[1],baseIndex+triangle[2]);}}if(!positions.length||!indices.length)return null;const geometry=new THREE.BufferGeometry();geometry.setAttribute('position',new THREE.Float32BufferAttribute(positions,3));geometry.setIndex(indices);geometry.computeBoundingBox();geometry.computeBoundingSphere();const opacity=Math.max(0.02,Math.min(1,Number(state&&state.opacity!=null?state.opacity:1)));const material=new THREE.MeshBasicMaterial({color:state&&state.color&&state.color.isColor?state.color.clone():new THREE.Color(this.sectionCutFillColor),side:THREE.DoubleSide,transparent:opacity<1,opacity,depthTest:true,depthWrite:opacity>=0.999,polygonOffset:true,polygonOffsetFactor:-1,polygonOffsetUnits:-1});const cap=new THREE.Mesh(geometry,material);cap.userData.isSectionHelper=true;cap.renderOrder=0;cap.frustumCulled=false;return cap;}buildSectionLoopsFromSegments(segments){const remaining=(segments||[]).filter(segment=>segment&&segment[0]&&segment[1]&&segment[0].distanceToSquared(segment[1])>0.00000001).map(segment=>[segment[0].clone(),segment[1].clone()]);const loops=[];const maxIterations=remaining.length*4+100;while(remaining.length){const first=remaining.shift();const path=[first[0],first[1]];let changed=true;let iterations=0;while(changed&&iterations++<maxIterations){changed=false;for(let i=0;i<remaining.length;i++){const segment=remaining[i];const start=path[0];const end=path[path.length-1];if(this.pointsAlmostEqual(end,segment[0])){path.push(segment[1]);}else if(this.pointsAlmostEqual(end,segment[1])){path.push(segment[0]);}else if(this.pointsAlmostEqual(start,segment[1])){path.unshift(segment[0]);}else if(this.pointsAlmostEqual(start,segment[0])){path.unshift(segment[1]);}else{continue;}remaining.splice(i,1);changed=true;break;}}if(path.length>=4&&this.pointsAlmostEqual(path[0],path[path.length-1]))path.pop();if(path.length>=3)loops.push(this.removeDuplicateLoopPoints(path));}return loops.filter(loop=>loop&&loop.length>=3);}removeDuplicateLoopPoints(points){const result=[];for(const point of points||[]){if(!result.length||!this.pointsAlmostEqual(result[result.length-1],point))result.push(point);}if(result.length>2&&this.pointsAlmostEqual(result[0],result[result.length-1]))result.pop();return result;}pointsAlmostEqual(a,b){return!!a&&!!b&&a.distanceToSquared(b)<=0.00000025;}getSectionPlaneBasis(plane){const normal=plane.normal.clone().normalize();const reference=Math.abs(normal.dot(new THREE.Vector3(0,0,1)))>0.95?new THREE.Vector3(1,0,0):new THREE.Vector3(0,0,1);const u=new THREE.Vector3().crossVectors(normal,reference).normalize();const v=new THREE.Vector3().crossVectors(normal,u).normalize();return{u,v};}getSectionGeometryOffset(){if(!this.sectionBounds)return 0.0005;const size=new THREE.Box3(this.sectionBounds.min.clone(),this.sectionBounds.max.clone()).getSize(new THREE.Vector3());return Math.max(size.length()*0.00001,0.0005);}getSectionCutMargin(){if(!this.sectionBounds)return 0.02;const size=new THREE.Box3(this.sectionBounds.min.clone(),this.sectionBounds.max.clone()).getSize(new THREE.Vector3());return Math.max(size.length()*0.0015,0.015);}getSectionOtherPlanesWithMargin(excludedPlaneIndex){const margin=this.getSectionCutMargin();return(this.sectionPlanes||[]).map((plane,index)=>{if(index===excludedPlaneIndex)return null;const expanded=plane.clone();expanded.constant+=margin;return expanded;}).filter(Boolean);}updateSectionCutEdges(){this.clearSectionCutEdges();if(!this.sectionBoxActive||!this.sectionBounds||!this.sectionPlanes||!this.sectionPlanes.length)return;const buckets=this.collectSectionCutEdgeBuckets();if(!buckets||!buckets.length)return;const group=new THREE.Group();group.name='ExportModel_SectionCutEdges';group.userData.isSectionHelper=true;for(const bucket of buckets){if(!bucket||!bucket.positions||bucket.positions.length<6)continue;const geometry=new THREE.BufferGeometry();geometry.setAttribute('position',new THREE.BufferAttribute(new Float32Array(bucket.positions),3));geometry.computeBoundingSphere();const color=bucket.color&&bucket.color.isColor?bucket.color.getHex():this.sectionCutEdgeColor;const opacity=Math.max(0.04,Math.min(1,Number(bucket.opacity==null?0.45:bucket.opacity)));const material=this.getSimpleEdgeMaterial?this.getSimpleEdgeMaterial(color,opacity,opacity<1):new THREE.LineBasicMaterial({color,transparent:opacity<1,opacity,depthTest:true,depthWrite:false});const line=new THREE.LineSegments(geometry,material);line.userData.isSectionHelper=true;line.renderOrder=2;line.frustumCulled=false;group.add(line);}if(!group.children.length){group.clear();return;}this.sectionCutEdgeGroup=group;this.sectionCapScene.add(group);}clearSectionCutEdges(){if(!this.sectionCutEdgeGroup)return;if(this.sectionCutEdgeGroup.parent)this.sectionCutEdgeGroup.parent.remove(this.sectionCutEdgeGroup);this.sectionCutEdgeGroup.traverse(child=>{if(child.geometry)child.geometry.dispose&&child.geometry.dispose();if(child.material)child.material.dispose&&child.material.dispose();});this.sectionCutEdgeGroup=null;}collectSectionCutEdgeBuckets(){const bucketsByKey=new Map();if(!this.sectionPlanes||!this.sectionPlanes.length)return[];const a=new THREE.Vector3();const b=new THREE.Vector3();const c=new THREE.Vector3();const outA=new THREE.Vector3();const outB=new THREE.Vector3();const maxPositions=Math.max(2,this.sectionCutEdgeMaxSegments*2)*3;let totalPositions=0;const renderMeshes=this.collectSectionRenderMeshes?this.collectSectionRenderMeshes():this.meshes;for(const mesh of renderMeshes){if(!this.isMeshCandidateForSectionCut(mesh))continue;const geometry=mesh.geometry;const position=geometry&&geometry.getAttribute?geometry.getAttribute('position'):null;if(!position||position.count<3)continue;mesh.updateMatrixWorld(true);const edgeState=this.getSectionCutEdgeStateForMesh(mesh);const bucketKey=this.getSectionStateKey(edgeState);let bucket=bucketsByKey.get(bucketKey);if(!bucket){bucket={color:edgeState.color,opacity:edgeState.opacity,positions:[]};bucketsByKey.set(bucketKey,bucket);}const index=geometry.index;const triangleCount=index?Math.floor(index.count/3):Math.floor(position.count/3);for(let planeIndex=0;planeIndex<this.sectionPlanes.length;planeIndex++){const plane=this.sectionPlanes[planeIndex];if(!this.meshIntersectsSectionPlane(mesh,plane,planeIndex))continue;for(let triangle=0;triangle<triangleCount;triangle++){const offset=triangle*3;const ia=index?index.getX(offset):offset;const ib=index?index.getX(offset+1):offset+1;const ic=index?index.getX(offset+2):offset+2;a.fromBufferAttribute(position,ia).applyMatrix4(mesh.matrixWorld);b.fromBufferAttribute(position,ib).applyMatrix4(mesh.matrixWorld);c.fromBufferAttribute(position,ic).applyMatrix4(mesh.matrixWorld);const segment=this.getTrianglePlaneIntersection(a,b,c,plane,outA,outB);if(!segment)continue;const clippedSegment=this.clipSegmentToOtherSectionPlanes(segment[0],segment[1],planeIndex);if(!clippedSegment)continue;const lineOffset=plane.normal.clone().normalize().multiplyScalar(this.getSectionGeometryOffset()*1.5);const lineStart=clippedSegment[0].clone().add(lineOffset);const lineEnd=clippedSegment[1].clone().add(lineOffset);bucket.positions.push(lineStart.x,lineStart.y,lineStart.z);bucket.positions.push(lineEnd.x,lineEnd.y,lineEnd.z);totalPositions+=6;if(totalPositions>=maxPositions)return Array.from(bucketsByKey.values());}}}return Array.from(bucketsByKey.values());}clipSegmentToOtherSectionPlanes(start,end,excludedPlaneIndex){if(!start||!end||!this.sectionPlanes||!this.sectionPlanes.length)return null;let a=start.clone();let b=end.clone();const epsilon=0.00001;const margin=this.getSectionCutMargin();for(let i=0;i<this.sectionPlanes.length;i++){if(i===excludedPlaneIndex)continue;const plane=this.sectionPlanes[i];const da=plane.distanceToPoint(a)+margin;const db=plane.distanceToPoint(b)+margin;if(da>=-epsilon&&db>=-epsilon)continue;if(da<-epsilon&&db<-epsilon)return null;const denominator=da-db;if(Math.abs(denominator)<epsilon)return null;const t=Math.max(0,Math.min(1,da/denominator));const point=new THREE.Vector3().lerpVectors(a,b,t);if(da<-epsilon)a=point;else b=point;}if(a.distanceToSquared(b)<0.00000001)return null;return[a,b];}getSectionCapStateForMesh(mesh){const material=this.getPrimaryMeshMaterial(mesh);return{color:this.getMaterialColor(material),opacity:this.getMaterialOpacity(material)};}getSectionCutEdgeStateForMesh(mesh){const capState=this.getSectionCapStateForMesh(mesh);const key=mesh&&mesh.userData?(mesh.userData.elementKey||(this.getMeshKey?this.getMeshKey(mesh):null)):null;const mass=this.visualMassState&&this.visualMassState.active?this.visualMassState:null;const isMassFadedKey=!!(key&&mass&&mass.allKeys&&mass.allKeys.has(key)&&!(mass.activeKeys&&mass.activeKeys.has(key)));const hasExplicitFade=!!(key&&this.visualFadeStates&&this.visualFadeStates.has(key));const isTrackedFadedKey=!!(key&&this.fadedMeshKeys&&this.fadedMeshKeys.has(key));const materialOpacity=Math.max(0.02,Math.min(1,Number(capState.opacity==null?1:capState.opacity)));const isTransparentState=materialOpacity<0.999||isMassFadedKey||hasExplicitFade||isTrackedFadedKey;if(isTransparentState)return this.getSoftSectionCutEdgeStateForTransparentMesh(mesh,capState,key);const edgeColor=capState.color&&capState.color.isColor?capState.color.clone():new THREE.Color(this.sectionCutEdgeColor);edgeColor.multiplyScalar(0.58);return{color:edgeColor,opacity:0.48};}getSoftSectionCutEdgeStateForTransparentMesh(mesh,capState,key){const visualOpacity=this.getSectionCutVisualOpacityForTransparentMesh(mesh,capState,key);const mass=this.visualMassState&&this.visualMassState.active?this.visualMassState:null;const isMassFadedKey=!!(key&&mass&&mass.allKeys&&mass.allKeys.has(key)&&!(mass.activeKeys&&mass.activeKeys.has(key)));let sourceColor=capState&&capState.color&&capState.color.isColor?capState.color.clone():new THREE.Color(this.sectionCutEdgeColor);if(isMassFadedKey&&mass&&mass.fadedColor!=null){sourceColor=new THREE.Color(Number(mass.fadedColor));}else if(key&&this.visualFadeStates&&this.visualFadeStates.has(key)){const fade=this.visualFadeStates.get(key);if(fade&&fade.color!=null)sourceColor=new THREE.Color(Number(fade.color));}const luminance=(sourceColor.r*0.2126)+(sourceColor.g*0.7152)+(sourceColor.b*0.0722);const edgeColor=sourceColor.clone();if(luminance>0.72)edgeColor.multiplyScalar(0.44);else if(luminance>0.48)edgeColor.multiplyScalar(0.56);else edgeColor.lerp(new THREE.Color(0x6b7280),0.35);const opacity=Math.max(0.055,Math.min(0.20,visualOpacity*1.65));return{color:edgeColor,opacity};}getSectionCutVisualOpacityForTransparentMesh(mesh,capState,key){const materialOpacity=Math.max(0.02,Math.min(1,Number(capState&&capState.opacity!=null?capState.opacity:1)));let visualOpacity=materialOpacity;if(key&&this.visualFadeStates&&this.visualFadeStates.has(key)){const fade=this.visualFadeStates.get(key);if(fade&&fade.opacity!=null)visualOpacity=Math.min(visualOpacity,Math.max(0.02,Math.min(1,Number(fade.opacity))));}const mass=this.visualMassState&&this.visualMassState.active?this.visualMassState:null;const isMassFadedKey=!!(key&&mass&&mass.allKeys&&mass.allKeys.has(key)&&!(mass.activeKeys&&mass.activeKeys.has(key)));if(isMassFadedKey&&mass&&mass.fadedOpacity!=null){visualOpacity=Math.min(visualOpacity,Math.max(0.02,Math.min(1,Number(mass.fadedOpacity))));}else if(key&&this.fadedMeshKeys&&this.fadedMeshKeys.has(key)&&mass&&mass.fadedOpacity!=null){visualOpacity=Math.min(visualOpacity,Math.max(0.02,Math.min(1,Number(mass.fadedOpacity))));}return Math.max(0.02,Math.min(1,visualOpacity));}getSectionStateKey(state){const colorHex=state&&state.color&&state.color.isColor?state.color.getHexString():'a8a8a8';const rawOpacity=state&&state.opacity!=null?state.opacity:1;const opacity=Math.max(0.02,Math.min(1,Number(rawOpacity))).toFixed(3);return colorHex+':'+opacity;}isMeshCandidateForSectionCut(mesh){if(!mesh||!mesh.isMesh||!mesh.geometry)return false;if(mesh.userData&&mesh.userData.isSectionHelper)return false;return mesh.visible||this.isSourceMeshVisibleForSectionCut(mesh);}meshIntersectsSectionPlane(mesh,plane,excludedPlaneIndex){if(!mesh||!mesh.geometry||!plane||!this.sectionBounds)return false;const geometry=mesh.geometry;if(!geometry.boundingBox)geometry.computeBoundingBox();if(!geometry.boundingBox)return false;mesh.updateMatrixWorld(true);const box=geometry.boundingBox.clone().applyMatrix4(mesh.matrixWorld);const sectionBox=new THREE.Box3(this.sectionBounds.min.clone(),this.sectionBounds.max.clone());if(!box.intersectsBox(sectionBox))return false;if(!this.boxIntersectsPlane(box,plane))return false;const center=box.getCenter(new THREE.Vector3());return this.isPointInsideOtherSectionPlanes(center,excludedPlaneIndex)||sectionBox.containsPoint(center)||box.intersectsBox(sectionBox);}boxIntersectsPlane(box,plane){const corners=[new THREE.Vector3(box.min.x,box.min.y,box.min.z),new THREE.Vector3(box.min.x,box.min.y,box.max.z),new THREE.Vector3(box.min.x,box.max.y,box.min.z),new THREE.Vector3(box.min.x,box.max.y,box.max.z),new THREE.Vector3(box.max.x,box.min.y,box.min.z),new THREE.Vector3(box.max.x,box.min.y,box.max.z),new THREE.Vector3(box.max.x,box.max.y,box.min.z),new THREE.Vector3(box.max.x,box.max.y,box.max.z)];let hasPositive=false;let hasNegative=false;for(const corner of corners){const distance=plane.distanceToPoint(corner);if(distance>=-0.00001)hasPositive=true;if(distance<=0.00001)hasNegative=true;if(hasPositive&&hasNegative)return true;}return false;}getTrianglePlaneIntersection(a,b,c,plane,outA,outB){const points=[];this.addEdgePlaneIntersection(a,b,plane,points);this.addEdgePlaneIntersection(b,c,plane,points);this.addEdgePlaneIntersection(c,a,plane,points);if(points.length<2)return null;outA.copy(points[0]);outB.copy(points[1]);if(outA.distanceToSquared(outB)<0.00000001)return null;return[outA.clone(),outB.clone()];}addEdgePlaneIntersection(a,b,plane,points){const da=plane.distanceToPoint(a);const db=plane.distanceToPoint(b);const epsilon=0.000001;if(Math.abs(da)<epsilon&&Math.abs(db)<epsilon)return;if((da>0&&db>0)||(da<0&&db<0))return;const denominator=da-db;if(Math.abs(denominator)<epsilon)return;const t=da/denominator;if(t<-epsilon||t>1+epsilon)return;points.push(new THREE.Vector3().lerpVectors(a,b,Math.max(0,Math.min(1,t))));}isPointInsideOtherSectionPlanes(point,excludedPlaneIndex){const tolerance=0.00001;for(let i=0;i<this.sectionPlanes.length;i++){if(i===excludedPlaneIndex)continue;if(this.sectionPlanes[i].distanceToPoint(point)<-tolerance)return false;}return true;}createSectionCapMaterialFromState(state,otherPlanes){const safeState=state||{color:new THREE.Color(0xa8a8a8),opacity:1};const opacity=Math.max(0.02,Math.min(1,Number(safeState.opacity)||1));return new THREE.MeshBasicMaterial({color:safeState.color?safeState.color.clone():new THREE.Color(0xa8a8a8),side:THREE.DoubleSide,transparent:opacity<1,opacity,depthWrite:false,depthTest:true,clippingPlanes:otherPlanes,stencilWrite:true,stencilRef:0,stencilFunc:THREE.NotEqualStencilFunc,stencilFail:THREE.ReplaceStencilOp,stencilZFail:THREE.ReplaceStencilOp,stencilZPass:THREE.ReplaceStencilOp});}getPrimaryMeshMaterial(mesh){if(!mesh)return null;const material=mesh.material;if(Array.isArray(material))return material.find(item=>item&&item.visible!==false)||material[0]||null;return material||null;}getMaterialColor(material){if(material&&material.color&&material.color.isColor)return material.color.clone();return new THREE.Color(0xa8a8a8);}getMaterialOpacity(material){if(material&&Number.isFinite(Number(material.opacity))){return Math.max(0.02,Math.min(1,Number(material.opacity)));}return 1;}createSectionStencilGroup(geometry,plane,otherPlanes,renderOrder){const group=new THREE.Group();const baseMaterial=new THREE.MeshBasicMaterial({depthWrite:false,depthTest:false,colorWrite:false,stencilWrite:true,stencilFunc:THREE.AlwaysStencilFunc});const backMaterial=baseMaterial.clone();backMaterial.side=THREE.BackSide;backMaterial.clippingPlanes=[plane,...otherPlanes];backMaterial.stencilFail=THREE.IncrementWrapStencilOp;backMaterial.stencilZFail=THREE.IncrementWrapStencilOp;backMaterial.stencilZPass=THREE.IncrementWrapStencilOp;const frontMaterial=baseMaterial.clone();frontMaterial.side=THREE.FrontSide;frontMaterial.clippingPlanes=[plane,...otherPlanes];frontMaterial.stencilFail=THREE.DecrementWrapStencilOp;frontMaterial.stencilZFail=THREE.DecrementWrapStencilOp;frontMaterial.stencilZPass=THREE.DecrementWrapStencilOp;const backMesh=new THREE.Mesh(geometry,backMaterial);backMesh.renderOrder=renderOrder;const frontMesh=new THREE.Mesh(geometry,frontMaterial);frontMesh.renderOrder=renderOrder;group.add(backMesh,frontMesh);return group;}clearSectionCaps(){if(this.sectionCapGroup){if(this.sectionCapGroup.parent)this.sectionCapGroup.parent.remove(this.sectionCapGroup);this.sectionCapGroup.traverse(child=>{if(child.geometry&&child.userData&&child.userData.sectionOwnedGeometry){child.geometry.dispose&&child.geometry.dispose();}if(child.material){if(Array.isArray(child.material))child.material.forEach(m=>m.dispose&&m.dispose());else child.material.dispose&&child.material.dispose();}});}this.sectionCapGroup=null;this.sectionCapPlaneMeshes=[];}updateSectionCapPlanes(){if(!this.sectionBounds||!this.sectionCapPlaneMeshes||!this.sectionCapPlaneMeshes.length)return;const size=this.getSectionBoxLocalSize(new THREE.Vector3());for(const capPlane of this.sectionCapPlaneMeshes){const face=this.getSectionFaceNameByIndex(capPlane.userData.sectionPlaneIndex);const normal=this.getOrientedSectionFaceNormal(face);const position=this.getSectionFaceCenter(face).sub(normal.clone().multiplyScalar(Math.max(size.length()*0.001,0.0035)));const dimensions=this.getSectionFaceDimensions(face,size);const matrix=this.getSectionFaceMatrix(face,position);capPlane.position.copy(position);capPlane.quaternion.setFromRotationMatrix(matrix);capPlane.scale.set(dimensions.width,dimensions.height,1);}}getSectionFaceNameByIndex(index){return['xMin','xMax','yMin','yMax','zMin','zMax'][index]||'zMax';}getSectionFaceDimensions(face,size){if(face==='xMin'||face==='xMax')return{width:size.z,height:size.y};if(face==='yMin'||face==='yMax')return{width:size.x,height:size.z};return{width:size.x,height:size.y};}getSectionFaceRotation(face){const matrix=this.getSectionFaceMatrix(face,new THREE.Vector3());return new THREE.Euler().setFromRotationMatrix(matrix);}getSectionFaceMatrix(face,position){const frame=this.isOrientedSectionBox()?this.sectionBoxFrame:{right:new THREE.Vector3(1,0,0),up:new THREE.Vector3(0,1,0),normal:new THREE.Vector3(0,0,1)};let axisU;let axisV;let outward;if(face==='xMin'){axisU=frame.normal.clone();axisV=frame.up.clone();outward=frame.right.clone().negate();}else if(face==='xMax'){axisU=frame.normal.clone().negate();axisV=frame.up.clone();outward=frame.right.clone();}else if(face==='yMin'){axisU=frame.right.clone();axisV=frame.normal.clone();outward=frame.up.clone().negate();}else if(face==='yMax'){axisU=frame.right.clone();axisV=frame.normal.clone().negate();outward=frame.up.clone();}else if(face==='zMin'){axisU=frame.right.clone().negate();axisV=frame.up.clone();outward=frame.normal.clone().negate();}else{axisU=frame.right.clone();axisV=frame.up.clone();outward=frame.normal.clone();}const matrix=new THREE.Matrix4().makeBasis(axisU.normalize(),axisV.normalize(),outward.normalize());if(position)matrix.setPosition(position);return matrix;}buildSectionBoxVisual(){if(this.sectionBoxGroup)this.sectionOverlayScene.remove(this.sectionBoxGroup);this.sectionHandles=[];this.sectionRotationHandles=[];this.sectionBoxGroup=new THREE.Group();this.sectionBoxGroup.name='ExportModel_SectionBox';this.sectionOverlayScene.add(this.sectionBoxGroup);this.updateSectionBoxVisual();}updateSectionBoxVisual(options){if(!this.sectionBoxGroup||!this.sectionBounds)return;const fast=!!(options&&options.fast);while(this.sectionBoxGroup.children.length){const child=this.sectionBoxGroup.children.pop();child.traverse&&child.traverse(item=>{if(item.geometry)item.geometry.dispose&&item.geometry.dispose();if(item.material){if(Array.isArray(item.material))item.material.forEach(m=>m.dispose&&m.dispose());else item.material.dispose&&item.material.dispose();}});}this.sectionHandles=[];this.sectionRotationHandles=[];this.sectionDragFastVisual=null;this.sectionDragFastVisualActive=false;const worldBox=this.getSectionBoxWorldBox();const size=this.getSectionBoxLocalSize(new THREE.Vector3());const sectionColor=0x0078d4;const lineMaterial=new THREE.MeshBasicMaterial({color:sectionColor,transparent:true,opacity:1,depthTest:false});if(fast){this.addSectionBoxFastEdges(worldBox,sectionColor);if(this.sectionActiveFace)this.addActiveSectionFace(worldBox,this.sectionActiveFace);return;}this.addSectionBoxEdges(worldBox,lineMaterial);if(this.sectionActiveFace)this.addActiveSectionFace(worldBox,this.sectionActiveFace);const handleSize=this.computeSectionHandleSize(size);for(const face of['xMin','xMax','yMin','yMax','zMin','zMax']){this.addSectionFaceHandle(face,this.getSectionFaceCenter(face),this.getOrientedSectionFaceNormal(face),handleSize);}this.addSectionRotationCornerHandles(handleSize);}addSectionBoxFastEdges(box,color){if(!box||!this.sectionBoxGroup)return;const corners=this.getSectionBoxCornersWorld();if(corners.length!==8)return;const pairs=[[0,1],[1,2],[2,3],[3,0],[4,5],[5,6],[6,7],[7,4],[0,4],[1,5],[2,6],[3,7]];const values=[];for(const pair of pairs){const a=corners[pair[0]],b=corners[pair[1]];values.push(a.x,a.y,a.z,b.x,b.y,b.z);}const geometry=new THREE.BufferGeometry();geometry.setAttribute('position',new THREE.BufferAttribute(new Float32Array(values),3));const material=new THREE.LineBasicMaterial({color:color||0x0078d4,transparent:true,opacity:1,depthTest:false,depthWrite:false});const lines=new THREE.LineSegments(geometry,material);lines.renderOrder=10;lines.userData.isSectionHelper=true;this.sectionBoxGroup.add(lines);}computeSectionHandleSize(size){const targetPixels=18;const worldBox=this.getSectionBoxWorldBox();const referencePoint=worldBox?worldBox.getCenter(new THREE.Vector3()):null;const screenBasedSize=this.getWorldPerPixelAtPoint(referencePoint)*targetPixels;const minimumSize=Math.max((size?size.length():1)*0.0008,0.006);return Math.max(screenBasedSize,minimumSize);}addActiveSectionFace(box,face){const size=this.getSectionBoxLocalSize(new THREE.Vector3());const normal=this.getOrientedSectionFaceNormal(face);const position=this.getSectionFaceCenter(face).sub(normal.clone().multiplyScalar(Math.max(size.length()*0.001,0.004)));const dimensions=this.getSectionFaceDimensions(face,size);const material=new THREE.MeshBasicMaterial({color:0x22c55e,transparent:true,opacity:0.28,side:THREE.DoubleSide,depthTest:false,depthWrite:false});const plane=new THREE.Mesh(new THREE.PlaneGeometry(dimensions.width,dimensions.height),material);plane.position.copy(position);plane.quaternion.setFromRotationMatrix(this.getSectionFaceMatrix(face,position));plane.renderOrder=28;this.sectionBoxGroup.add(plane);}updateSectionDragFastVisual(){if(!this.sectionBoxGroup||!this.sectionBounds||!this.sectionActiveFace)return;if(!this.sectionDragFastVisual){const material=new THREE.MeshBasicMaterial({color:0x22c55e,transparent:true,opacity:0.36,side:THREE.DoubleSide,depthTest:false,depthWrite:false});this.sectionDragFastVisual=new THREE.Mesh(new THREE.PlaneGeometry(1,1),material);this.sectionDragFastVisual.renderOrder=60;this.sectionDragFastVisual.userData.isSectionHelper=true;this.sectionBoxGroup.add(this.sectionDragFastVisual);}const face=this.sectionActiveFace;const size=this.getSectionBoxLocalSize(new THREE.Vector3());const normal=this.getOrientedSectionFaceNormal(face);const position=this.getSectionFaceCenter(face).sub(normal.clone().multiplyScalar(Math.max(size.length()*0.001,0.004)));const dimensions=this.getSectionFaceDimensions(face,size);this.sectionDragFastVisual.position.copy(position);this.sectionDragFastVisual.quaternion.setFromRotationMatrix(this.getSectionFaceMatrix(face,position));this.sectionDragFastVisual.scale.set(Math.max(0.001,dimensions.width),Math.max(0.001,dimensions.height),1);this.sectionDragFastVisual.visible=true;this.sectionDragFastVisualActive=true;}clearSectionDragFastVisual(){if(this.sectionDragFastVisual){if(this.sectionDragFastVisual.parent)this.sectionDragFastVisual.parent.remove(this.sectionDragFastVisual);if(this.sectionDragFastVisual.geometry&&this.sectionDragFastVisual.geometry.dispose)this.sectionDragFastVisual.geometry.dispose();if(this.sectionDragFastVisual.material&&this.sectionDragFastVisual.material.dispose)this.sectionDragFastVisual.material.dispose();}this.sectionDragFastVisual=null;this.sectionDragFastVisualActive=false;}addSectionFaceHandle(face,faceCenter,normal,handleSize){const boxSize=this.getSectionBoxLocalSize(new THREE.Vector3());const insideOffset=Math.max(boxSize.length()*0.006,handleSize*0.22);const base=faceCenter.clone().sub(normal.clone().multiplyScalar(insideOffset));const group=new THREE.Group();group.position.copy(base);group.userData.sectionFace=face;group.renderOrder=30;const fill=new THREE.MeshBasicMaterial({color:0x0ea5e9,transparent:true,opacity:0.95,depthTest:false,depthWrite:false,side:THREE.DoubleSide});const edge=new THREE.LineBasicMaterial({color:0x005a9e,transparent:true,opacity:1,depthTest:false});const coneGeometry=new THREE.ConeGeometry(handleSize*0.28,handleSize*0.52,3);const makeCone=(direction,offset)=>{const cone=new THREE.Mesh(coneGeometry.clone(),fill.clone());cone.position.copy(direction.clone().multiplyScalar(offset));cone.quaternion.setFromUnitVectors(new THREE.Vector3(0,1,0),direction.clone().normalize());cone.userData.sectionFace=face;cone.renderOrder=32;const coneEdges=new THREE.LineSegments(new THREE.EdgesGeometry(cone.geometry),edge.clone());coneEdges.userData.sectionFace=face;coneEdges.renderOrder=33;cone.add(coneEdges);return cone;};group.add(makeCone(normal.clone(),handleSize*0.20));group.add(makeCone(normal.clone().negate(),handleSize*0.58));const hitGeometry=new THREE.PlaneGeometry(handleSize*0.85,handleSize*0.85);const hitMaterial=new THREE.MeshBasicMaterial({transparent:true,opacity:0,depthWrite:false,depthTest:false,side:THREE.DoubleSide});const hit=new THREE.Mesh(hitGeometry,hitMaterial);hit.quaternion.setFromUnitVectors(new THREE.Vector3(0,0,1),normal.clone().normalize());hit.userData.sectionFace=face;hit.renderOrder=35;group.add(hit);this.sectionHandles.push(group);this.sectionBoxGroup.add(group);}addSectionRotationCornerHandles(handleSize){if(!this.sectionBoxGroup||!this.isOrientedSectionBox())return;const corners=this.getSectionBoxCornersWorld();if(corners.length!==8)return;const axisLabel=String(this.sectionRotationAxis||'Z').toUpperCase();const colorByAxis={X:0xef4444,Y:0x22c55e,Z:0x3b82f6};const color=colorByAxis[axisLabel]||colorByAxis.Z;const axisWorld=this.getSectionRotationAxisWorld(axisLabel);const radius=Math.max(handleSize*0.34,0.004);const tube=Math.max(radius*0.18,0.0015);corners.forEach((corner,index)=>{const group=new THREE.Group();group.position.copy(corner);group.userData.sectionRotationAxis=axisLabel;group.userData.sectionCornerIndex=index;group.renderOrder=42;const material=new THREE.MeshBasicMaterial({color,transparent:true,opacity:0.96,depthTest:false,depthWrite:false,side:THREE.DoubleSide});const ring=new THREE.Mesh(new THREE.TorusGeometry(radius,tube,8,24),material);ring.quaternion.setFromUnitVectors(new THREE.Vector3(0,0,1),axisWorld.clone().normalize());ring.userData.sectionRotationAxis=axisLabel;ring.userData.sectionCornerIndex=index;ring.renderOrder=43;group.add(ring);const hit=new THREE.Mesh(new THREE.SphereGeometry(radius*1.35,8,6),new THREE.MeshBasicMaterial({transparent:true,opacity:0,depthTest:false,depthWrite:false}));hit.userData.sectionRotationAxis=axisLabel;hit.userData.sectionCornerIndex=index;hit.renderOrder=44;group.add(hit);this.sectionRotationHandles.push(group);this.sectionHandles.push(group);this.sectionBoxGroup.add(group);});}createSectionHandleHitGeometry(face,size){return new THREE.PlaneGeometry(size,size);}computeSectionEdgeRadius(box){if(!box||box.isEmpty())return 0.0025;const worldPerPixel=this.getWorldPerPixelAtPoint(box.getCenter(new THREE.Vector3()));const boxSize=this.getSectionBoxLocalSize(new THREE.Vector3());const baseRadius=Math.max(boxSize.length()*0.000425,0.0025);if(!this.sectionEdgeTargetPixels||!Number.isFinite(this.sectionEdgeTargetPixels)){const initialPixels=worldPerPixel>0?(baseRadius*2)/worldPerPixel:4;this.sectionEdgeTargetPixels=Math.max(2.5,Math.min(7,initialPixels));}const targetPixels=Math.max(0.75,Math.min(4,(Number(this.sectionEdgeTargetPixels)||4)*0.5));return Math.max(0.00005,worldPerPixel*targetPixels*0.5);}addSectionBoxEdges(box,material){const corners=this.getSectionBoxCornersWorld();if(corners.length!==8)return;const pairs=[[0,1],[1,2],[2,3],[3,0],[4,5],[5,6],[6,7],[7,4],[0,4],[1,5],[2,6],[3,7]];const radius=this.computeSectionEdgeRadius(box);for(const pair of pairs)this.sectionBoxGroup.add(this.createCylinderBetween(corners[pair[0]],corners[pair[1]],radius,material.clone()));}createCylinderBetween(a,b,radius,material){const direction=new THREE.Vector3().subVectors(b,a);const length=direction.length();const geometry=new THREE.CylinderGeometry(radius,radius,length,8);const cylinder=new THREE.Mesh(geometry,material);cylinder.position.copy(a).add(b).multiplyScalar(0.5);cylinder.quaternion.setFromUnitVectors(new THREE.Vector3(0,1,0),direction.normalize());cylinder.renderOrder=10;return cylinder;}pickSectionHandle(x,y){if(!this.sectionHandles||!this.sectionHandles.length)return null;const rect=this.canvas.getBoundingClientRect();this.pointer.x=(x/rect.width)*2-1;this.pointer.y=-(y/rect.height)*2+1;this.raycaster.setFromCamera(this.pointer,this.camera);const hits=this.raycaster.intersectObjects(this.sectionHandles,true);if(!hits.length)return null;let object=hits[0].object;while(object&&!object.userData.sectionFace&&!object.userData.sectionRotationAxis&&object.parent)object=object.parent;return object&&(object.userData.sectionFace||object.userData.sectionRotationAxis)?object:null;}ensureOrientedSectionBox(){if(this.isOrientedSectionBox())return true;if(!this.sectionBounds)return false;const box=new THREE.Box3(this.sectionBounds.min.clone(),this.sectionBounds.max.clone());if(box.isEmpty())return false;const center=box.getCenter(new THREE.Vector3());this.sectionBoxFrame=this.createSectionBoxFrame(center,new THREE.Vector3(1,0,0),new THREE.Vector3(0,1,0),new THREE.Vector3(0,0,1));this.sectionLocalBounds={min:box.min.clone().sub(center),max:box.max.clone().sub(center)};this.sectionBoxSource=this.sectionBoxSource||'legacy-oriented';return true;}getSectionRotationAxisWorld(axis){const normalized=String(axis||this.sectionRotationAxis||'Z').toUpperCase();const frame=this.isOrientedSectionBox()?this.sectionBoxFrame:null;if(normalized==='Z')return new THREE.Vector3(0,1,0);if(normalized==='X'){const right=frame?frame.right.clone():new THREE.Vector3(1,0,0);if(right.lengthSq()>1e-12)return right.normalize();return new THREE.Vector3(1,0,0);}const xAxis=frame?frame.right.clone():new THREE.Vector3(1,0,0);const vertical=new THREE.Vector3(0,1,0);const yAxis=new THREE.Vector3().crossVectors(vertical,xAxis);if(yAxis.lengthSq()<1e-12&&frame)yAxis.copy(frame.normal).multiplyScalar(-1);if(yAxis.lengthSq()<1e-12)yAxis.set(0,0,-1);return yAxis.normalize();}getSectionRotationVectorFromScreen(x,y,center,axis){if(!this.canvas||!this.camera||!this.raycaster)return null;const rect=this.canvas.getBoundingClientRect();const width=Math.max(1,rect.width);const height=Math.max(1,rect.height);this.pointer.x=(x/width)*2-1;this.pointer.y=-(y/height)*2+1;this.raycaster.setFromCamera(this.pointer,this.camera);const plane=new THREE.Plane().setFromNormalAndCoplanarPoint(axis.clone().normalize(),center);const point=this.raycaster.ray.intersectPlane(plane,new THREE.Vector3());if(!point)return null;const vector=point.sub(center);vector.addScaledVector(axis,-vector.dot(axis));if(vector.lengthSq()<1e-10)return null;return vector.normalize();}getSectionRotationScreenAngle(x,y,center){if(!this.canvas||!this.camera||!center)return 0;const rect=this.canvas.getBoundingClientRect();const projected=center.clone().project(this.camera);const centerX=(projected.x*0.5+0.5)*Math.max(1,rect.width);const centerY=(-projected.y*0.5+0.5)*Math.max(1,rect.height);return Math.atan2(y-centerY,x-centerX);}beginSectionRotation(axis,x,y){if(!this.ensureOrientedSectionBox())return false;const normalized=String(axis||this.sectionRotationAxis||'Z').toUpperCase();const center=this.getSectionBoxCentroid();if(!center)return false;const axisWorld=this.getSectionRotationAxisWorld(normalized);const startVector=this.getSectionRotationVectorFromScreen(x,y,center,axisWorld);this.sectionActiveFace=null;this.sectionActiveRotationAxis=normalized;this.sectionDrag={mode:'rotate',axis:normalized,axisWorld:axisWorld.clone(),center:center.clone(),startVector:startVector?startVector.clone():null,startScreenAngle:this.getSectionRotationScreenAngle(x,y,center),startFrame:{origin:this.sectionBoxFrame.origin.clone(),right:this.sectionBoxFrame.right.clone(),up:this.sectionBoxFrame.up.clone(),normal:this.sectionBoxFrame.normal.clone()},fastStarted:false};return true;}rotateSectionBoxFrameAroundCenter(frame,bounds,axis,angle,center){if(!frame||!bounds||!axis||!center||!Number.isFinite(angle))return null;const quaternion=new THREE.Quaternion().setFromAxisAngle(axis.clone().normalize(),angle);const right=frame.right.clone().applyQuaternion(quaternion).normalize();const up=frame.up.clone().applyQuaternion(quaternion).normalize();const normal=frame.normal.clone().applyQuaternion(quaternion).normalize();const localCenter=new THREE.Vector3().addVectors(bounds.min,bounds.max).multiplyScalar(0.5);const origin=center.clone().addScaledVector(right,-localCenter.x).addScaledVector(up,-localCenter.y).addScaledVector(normal,-localCenter.z);return this.createSectionBoxFrame(origin,right,up,normal);}dragSectionRotation(x,y){const drag=this.sectionDrag;if(!drag||drag.mode!=='rotate'||!this.sectionLocalBounds)return;const currentVector=this.getSectionRotationVectorFromScreen(x,y,drag.center,drag.axisWorld);let angle;if(drag.startVector&&currentVector){const cross=new THREE.Vector3().crossVectors(drag.startVector,currentVector);angle=Math.atan2(drag.axisWorld.dot(cross),THREE.MathUtils.clamp(drag.startVector.dot(currentVector),-1,1));}else{angle=this.getSectionRotationScreenAngle(x,y,drag.center)-drag.startScreenAngle;}if(!Number.isFinite(angle))return;const rotated=this.rotateSectionBoxFrameAroundCenter(drag.startFrame,this.sectionLocalBounds,drag.axisWorld,angle,drag.center);if(!rotated)return;this.sectionBoxFrame=rotated;this.sectionBoxSource='manual-rotation';this.updateSectionWorldBoundsFromLocal();this.updateSectionPlanes();if(!drag.fastStarted){this.clearSectionCaps();this.clearSectionCutEdges();drag.fastStarted=true;}this.updateSectionBoxVisual({fast:true});if(this.requestRender)this.requestRender('section-rotate');}finishSectionBoxInteraction(){this.sectionDrag=null;this.sectionActiveFace=null;this.sectionActiveRotationAxis=null;this.clearSectionDragFastVisual&&this.clearSectionDragFastVisual();this.buildSectionCaps();this.updateSectionBoxVisual();this.updateSectionCutEdges();this.forceSectionDragRender&&this.forceSectionDragRender('section-end');}dragSectionFace(dx,dy){if(!this.sectionDrag||!this.sectionBounds)return;const face=this.sectionDrag.face;const normal=this.getOrientedSectionFaceNormal(face);const center=this.getSectionFaceCenter(face);const screenDirection=this.getScreenDirectionForNormal(center,normal);const worldPerPixel=this.getWorldPerPixelAtPoint(center);const deltaAlongNormal=(dx*screenDirection.x+dy*screenDirection.y)*worldPerPixel;const minGap=Math.max(worldPerPixel*4,0.01);const b=this.getSectionWorkingBounds();if(face==='xMin')b.min.x=Math.min(b.min.x-deltaAlongNormal,b.max.x-minGap);else if(face==='xMax')b.max.x=Math.max(b.max.x+deltaAlongNormal,b.min.x+minGap);else if(face==='yMin')b.min.y=Math.min(b.min.y-deltaAlongNormal,b.max.y-minGap);else if(face==='yMax')b.max.y=Math.max(b.max.y+deltaAlongNormal,b.min.y+minGap);else if(face==='zMin')b.min.z=Math.min(b.min.z-deltaAlongNormal,b.max.z-minGap);else if(face==='zMax')b.max.z=Math.max(b.max.z+deltaAlongNormal,b.min.z+minGap);if(this.isOrientedSectionBox())this.updateSectionWorldBoundsFromLocal();this.updateSectionPlanes();if(!this.sectionDragFastVisualActive){this.clearSectionCaps();this.clearSectionCutEdges();this.sectionDragFastVisualActive=true;}this.updateSectionBoxVisual({fast:true});this.requestRender&&this.requestRender('section-drag');}getSectionFaceCenter(face){const b=this.getSectionWorkingBounds();const c=new THREE.Vector3().addVectors(b.min,b.max).multiplyScalar(0.5);if(face==='xMin')c.x=b.min.x;else if(face==='xMax')c.x=b.max.x;else if(face==='yMin')c.y=b.min.y;else if(face==='yMax')c.y=b.max.y;else if(face==='zMin')c.z=b.min.z;else if(face==='zMax')c.z=b.max.z;return this.sectionLocalToWorld(c,new THREE.Vector3());}getScreenDirectionForNormal(point,normal){const width=Math.max(1,this.renderer.domElement.clientWidth);const height=Math.max(1,this.renderer.domElement.clientHeight);const a=point.clone().project(this.camera);const b=point.clone().add(normal).project(this.camera);const ax=(a.x*0.5+0.5)*width;const ay=(-a.y*0.5+0.5)*height;const bx=(b.x*0.5+0.5)*width;const by=(-b.y*0.5+0.5)*height;const dir={x:bx-ax,y:by-ay};const length=Math.hypot(dir.x,dir.y)||1;return{x:dir.x/length,y:dir.y/length};}getOrientedSectionFaceNormal(face){const frame=this.isOrientedSectionBox()?this.sectionBoxFrame:null;if(frame){if(face==='xMin')return frame.right.clone().negate();if(face==='xMax')return frame.right.clone();if(face==='yMin')return frame.up.clone().negate();if(face==='yMax')return frame.up.clone();if(face==='zMin')return frame.normal.clone().negate();return frame.normal.clone();}if(face==='xMin')return new THREE.Vector3(-1,0,0);if(face==='xMax')return new THREE.Vector3(1,0,0);if(face==='yMin')return new THREE.Vector3(0,-1,0);if(face==='yMax')return new THREE.Vector3(0,1,0);if(face==='zMin')return new THREE.Vector3(0,0,-1);return new THREE.Vector3(0,0,1);}getSectionFaceNormal(face){if(face==='xMin')return new THREE.Vector3(-1,0,0);if(face==='xMax')return new THREE.Vector3(1,0,0);if(face==='yMin')return new THREE.Vector3(0,-1,0);if(face==='yMax')return new THREE.Vector3(0,1,0);if(face==='zMin')return new THREE.Vector3(0,0,-1);return new THREE.Vector3(0,0,1);}fadeUnselected(transparency){const run=()=>this.fadeUnselectedCore?this.fadeUnselectedCore(transparency):false;return this.runVisualTransaction?this.runVisualTransaction('fade-unselected',run):run();}fadeUnselectedWithProgress(transparency,options){const run=()=>this.fadeUnselectedCoreWithProgress?this.fadeUnselectedCoreWithProgress(transparency,options):this.fadeUnselected(transparency);return this.runVisualTransaction?this.runVisualTransaction('fade-unselected-progress',run):run();}async fadeUnselectedCoreWithProgress(transparency,options){const opts=options&&typeof options==='object'?options:{};const report=typeof opts.onProgress==='function'?opts.onProgress:()=>{};const waitForPaint=async()=>{if(opts.yieldBetweenStages===false)return;if(this.waitForResetProgressPaint)await this.waitForResetProgressPaint();};const metricsStart=this.getPerformanceNow?this.getPerformanceNow():Date.now();if(this.isViewerTransparencyActive&&this.isViewerTransparencyActive()){const changedKeys=new Set();report({message:'Retirando la transparencia del visor...',current:0,total:0});await waitForPaint();if(this.clearMassTransparencyHighlight){this.clearMassTransparencyHighlight({owner:'viewer',disposeCache:true,fullReset:true,skipNormalChunkRefresh:true,skipVisualUpdate:true,changedKeysTarget:changedKeys});}if(changedKeys.size&&this.updateVisualAppearanceForKeysBatched){await this.updateVisualAppearanceForKeysBatched(changedKeys,{skipChunkRefresh:true,progressMessage:'Restaurando la apariencia original...',onProgress:(current,total,message)=>report({message,current,total})});}report({message:'Recuperando los bloques optimizados del modelo...',current:0,total:0});await waitForPaint();if(this.rebuildNormalChunks&&!(this.visualMassState&&this.visualMassState.active))this.rebuildNormalChunks(true);report({message:'Transparencia retirada correctamente.',current:1,total:1});if(this.recordPerformanceOperation)this.recordPerformanceOperation('fadeUnselected:toggleOff-progress',(this.getPerformanceNow?this.getPerformanceNow():Date.now())-metricsStart,{restoredKeys:changedKeys.size});this.syncViewerToolButtons&&this.syncViewerToolButtons();return false;}report({message:'Preparando la transparencia del visor...',current:0,total:0});await waitForPaint();this.clearClashHighlight();this.clearWorksProgressVisualization();if(this.clearMassTransparencyHighlight)this.clearMassTransparencyHighlight({owner:'viewer',disposeCache:true,skipNormalChunkRefresh:true,skipVisualUpdate:true});if(!this.selectedKeys||!this.selectedKeys.size)return false;const rawTransparency=Number(transparency);const fallbackPercent=Number.isFinite(rawTransparency)?(rawTransparency<=1?rawTransparency*100:rawTransparency):85;const opacity=this.getConfiguredTransparencyOpacity(0.03,fallbackPercent);const fadeColor=this.getConfiguredTransparencyColor();const allKeys=this.getDisplayVisibleElementKeySet?this.getDisplayVisibleElementKeySet():(this.getBaseVisibleElementKeySet?this.getBaseVisibleElementKeySet():new Set());const activeKeys=new Set();for(const key of this.selectedKeys||[])if(allKeys.has(key))activeKeys.add(key);if(!activeKeys.size)return false;const fadedKeys=new Set();for(const key of allKeys)if(!activeKeys.has(key))fadedKeys.add(key);if(!fadedKeys.size)return false;report({message:'Definiendo '+fadedKeys.size+' elementos transparentes...',current:activeKeys.size,total:Math.max(1,allKeys.size)});await waitForPaint();const previousActiveState=this.visualMassState&&this.visualMassState.active?this.visualMassState:null;const state={active:true,owner:'viewer',sourceOwner:'viewer',mode:'fade-unselected',activeKeys,allKeys,preserveActiveAppearance:true,highlightColor:0x5DA3F6,highlightOpacity:0.7,fadedColor:fadeColor,fadedOpacity:opacity};if(this.setMassTransparencyLayer)this.setMassTransparencyLayer('viewer',state);else this.visualMassState=state;const useStableEdges=this.shouldReuseStableNormalEdgesForMassTransparency&&this.shouldReuseStableNormalEdgesForMassTransparency();this.massTransparencyUseStableEdges=!!useStableEdges;report({message:'Construyendo superficies transparentes optimizadas...',current:0,total:0});await waitForPaint();if(useStableEdges){this.suspendNormalChunkSurfacesPreserveEdges&&this.suspendNormalChunkSurfacesPreserveEdges('viewer-transparency');this.clearGroupedTransparentEdges&&this.clearGroupedTransparentEdges(false);this.applyStableNormalChunkEdgeAppearance&&this.applyStableNormalChunkEdgeAppearance('mass');}else if(this.clearNormalChunks){this.clearNormalChunks(false);}const chunkCount=this.rebuildMassTransparencyChunks?this.rebuildMassTransparencyChunks(false):0;if(!useStableEdges&&this.rebuildGroupedTransparentEdges){report({message:'Sincronizando los bordes transparentes...',current:0,total:0});await waitForPaint();this.rebuildGroupedTransparentEdges(false);}const changedKeys=new Set(activeKeys);if(this.collectMassTransparencyKeysFromState){this.collectMassTransparencyKeysFromState(previousActiveState,changedKeys);this.collectMassTransparencyKeysFromState(this.visualMassState,changedKeys);}if(changedKeys.size){await this.updateVisualAppearanceForKeysBatched(changedKeys,{forceChunkRebuild:true,progressMessage:'Aplicando transparencia a superficies y bordes...',onProgress:(current,total,message)=>report({message,current,total})});}report({message:'Transparencia aplicada correctamente.',current:Math.max(1,allKeys.size),total:Math.max(1,allKeys.size)});if(this.recordPerformanceOperation)this.recordPerformanceOperation('fadeUnselected:on-progress',(this.getPerformanceNow?this.getPerformanceNow():Date.now())-metricsStart,{allKeys:allKeys.size,fadedKeys:fadedKeys.size,selectedKeys:this.selectedKeys?this.selectedKeys.size:0,activeKeys:activeKeys.size,chunks:chunkCount,opacity,transparencyPercent:fallbackPercent});if(this.recordModelPerformanceSnapshot)this.recordModelPerformanceSnapshot('fadeUnselected:on');this.syncViewerToolButtons&&this.syncViewerToolButtons();return!!(this.isViewerTransparencyActive&&this.isViewerTransparencyActive());}fadeUnselectedCore(transparency){const metricsStart=this.getPerformanceNow?this.getPerformanceNow():Date.now();if(this.isViewerTransparencyActive&&this.isViewerTransparencyActive()){if(this.clearMassTransparencyHighlight)this.clearMassTransparencyHighlight({owner:'viewer',disposeCache:true,fullReset:true,skipNormalChunkRefresh:true});if(this.rebuildNormalChunks&&!(this.visualMassState&&this.visualMassState.active))this.rebuildNormalChunks(true);if(this.recordPerformanceOperation)this.recordPerformanceOperation('fadeUnselected:toggleOff',(this.getPerformanceNow?this.getPerformanceNow():Date.now())-metricsStart,{});this.syncViewerToolButtons&&this.syncViewerToolButtons();return false;}this.clearClashHighlight();this.clearWorksProgressVisualization();if(this.clearMassTransparencyHighlight)this.clearMassTransparencyHighlight({owner:'viewer',disposeCache:true,skipNormalChunkRefresh:true,skipVisualUpdate:true});if(!this.selectedKeys||!this.selectedKeys.size){if(this.recordPerformanceOperation)this.recordPerformanceOperation('fadeUnselected:noSelection',(this.getPerformanceNow?this.getPerformanceNow():Date.now())-metricsStart,{});return false;}const rawTransparency=Number(transparency);const fallbackPercent=Number.isFinite(rawTransparency)?(rawTransparency<=1?rawTransparency*100:rawTransparency):85;const opacity=this.getConfiguredTransparencyOpacity(0.03,fallbackPercent);const fadeColor=this.getConfiguredTransparencyColor();const allKeys=this.getDisplayVisibleElementKeySet?this.getDisplayVisibleElementKeySet():(this.getBaseVisibleElementKeySet?this.getBaseVisibleElementKeySet():new Set());const activeKeys=new Set();for(const key of this.selectedKeys||[]){if(allKeys.has(key))activeKeys.add(key);}if(!activeKeys.size){if(this.recordPerformanceOperation)this.recordPerformanceOperation('fadeUnselected:noVisibleSelection',(this.getPerformanceNow?this.getPerformanceNow():Date.now())-metricsStart,{selectedKeys:this.selectedKeys?this.selectedKeys.size:0,visibleKeys:allKeys.size});return false;}const fadedKeys=new Set();for(const key of allKeys){if(!activeKeys.has(key))fadedKeys.add(key);}if(!fadedKeys.size)return false;const previousActiveState=this.visualMassState&&this.visualMassState.active?this.visualMassState:null;const state={active:true,owner:'viewer',sourceOwner:'viewer',mode:'fade-unselected',activeKeys,allKeys,preserveActiveAppearance:true,highlightColor:0x5DA3F6,highlightOpacity:0.7,fadedColor:fadeColor,fadedOpacity:opacity};if(this.setMassTransparencyLayer)this.setMassTransparencyLayer('viewer',state);else this.visualMassState=state;const useStableEdges=this.shouldReuseStableNormalEdgesForMassTransparency&&this.shouldReuseStableNormalEdgesForMassTransparency();this.massTransparencyUseStableEdges=!!useStableEdges;if(useStableEdges){this.suspendNormalChunkSurfacesPreserveEdges&&this.suspendNormalChunkSurfacesPreserveEdges('viewer-transparency');this.clearGroupedTransparentEdges&&this.clearGroupedTransparentEdges(false);this.applyStableNormalChunkEdgeAppearance&&this.applyStableNormalChunkEdgeAppearance('mass');}else if(this.clearNormalChunks){this.clearNormalChunks(false);}if(this.rebuildMassTransparencyChunks)this.rebuildMassTransparencyChunks(false);if(!useStableEdges&&this.rebuildGroupedTransparentEdges)this.rebuildGroupedTransparentEdges(false);const changedKeys=new Set(activeKeys);if(this.collectMassTransparencyKeysFromState){this.collectMassTransparencyKeysFromState(previousActiveState,changedKeys);this.collectMassTransparencyKeysFromState(this.visualMassState,changedKeys);}if(changedKeys.size)this.updateVisualAppearanceForKeys(changedKeys,{forceChunkRebuild:true});if(this.recordPerformanceOperation)this.recordPerformanceOperation('fadeUnselected:on',(this.getPerformanceNow?this.getPerformanceNow():Date.now())-metricsStart,{allKeys:allKeys.size,fadedKeys:this.fadedMeshKeys?this.fadedMeshKeys.size:fadedKeys.size,selectedKeys:this.selectedKeys?this.selectedKeys.size:0,activeKeys:activeKeys.size,opacity,transparencyPercent:fallbackPercent});if(this.recordModelPerformanceSnapshot)this.recordModelPerformanceSnapshot('fadeUnselected:on');this.syncViewerToolButtons&&this.syncViewerToolButtons();return!!(this.isViewerTransparencyActive&&this.isViewerTransparencyActive());}applyFadeToMesh(mesh,opacity){const key=this.getMeshKey(mesh);if(!key)return;this.visualFadeStates.set(key,{opacity:Math.max(0.02,Math.min(1,Number(opacity)||0.1))});this.fadedMeshKeys.add(key);this.fadeActive=true;this.updateVisualAppearanceForKeys([key],{skipSectionRefresh:true});}applyFadeToMaterial(material,opacity){const materials=Array.isArray(material)?material:[material];for(const item of materials){if(!item||this.fadedMaterialStates.has(item))continue;this.fadedMaterialStates.set(item,{transparent:item.transparent,opacity:item.opacity,depthWrite:item.depthWrite});item.transparent=true;item.opacity=opacity;item.depthWrite=false;item.needsUpdate=true;}}restoreFadedElements(options){const opts=options&&typeof options==='object'?options:{};const metricsStart=this.getPerformanceNow?this.getPerformanceNow():Date.now();const changedKeys=new Set(this.visualFadeStates?this.visualFadeStates.keys():[]);if(this.fadedMeshKeys){for(const key of this.fadedMeshKeys)changedKeys.add(key);}if(this.visualFadeStates)this.visualFadeStates.clear();if(this.fadedMeshMaterialStates&&this.fadedMeshMaterialStates.size){for(const[mesh]of this.fadedMeshMaterialStates.entries()){const key=this.getMeshKey(mesh);if(key)changedKeys.add(key);}this.fadedMeshMaterialStates.clear();}if(this.fadedMaterialStates&&this.fadedMaterialStates.size){for(const[material,state]of this.fadedMaterialStates.entries()){material.transparent=state.transparent;material.opacity=state.opacity;material.depthWrite=state.depthWrite;material.needsUpdate=true;}this.fadedMaterialStates.clear();}if(this.fadedEdgeStates&&this.fadedEdgeStates.size){this.fadedEdgeStates.clear();}this.fadedMeshKeys.clear();this.fadeActive=false;if(opts.skipVisualUpdate!==true)this.updateVisualAppearanceForKeys(changedKeys,{skipChunkRefresh:opts.skipChunkRefresh===true,skipSectionRefresh:opts.skipSectionRefresh===true});if(this.recordPerformanceOperation)this.recordPerformanceOperation('restoreFadedElements',(this.getPerformanceNow?this.getPerformanceNow():Date.now())-metricsStart,{changedKeys:changedKeys.size});if(this.recordModelPerformanceSnapshot)this.recordModelPerformanceSnapshot('restoreFadedElements');}applyColorOverrides(colorByElementKey){return this.runVisualTransaction?this.runVisualTransaction('apply-color-overrides',()=>this.applyColorOverridesCore(colorByElementKey)):this.applyColorOverridesCore(colorByElementKey);}applyColorOverridesCore(colorByElementKey){const metricsStart=this.getPerformanceNow?this.getPerformanceNow():Date.now();if(!colorByElementKey||!colorByElementKey.size){if(this.recordPerformanceOperation)this.recordPerformanceOperation('applyColorOverrides:noop-empty',(this.getPerformanceNow?this.getPerformanceNow():Date.now())-metricsStart,{});return false;}const previous=this.visualColorOverrides||new Map();const next=new Map();for(const[key,color]of colorByElementKey.entries()){if(!key)continue;next.set(key,{color:Number(color||0)});}const changedKeys=new Set();for(const[key,value]of previous.entries()){const nextValue=next.get(key);if(!nextValue||Number(nextValue.color)!==Number(value&&value.color))changedKeys.add(key);}for(const[key,value]of next.entries()){const oldValue=previous.get(key);if(!oldValue||Number(oldValue.color)!==Number(value&&value.color))changedKeys.add(key);}const hasClashState=!!((this.visualClashStates&&this.visualClashStates.size)||(this.clashMaterialStates&&this.clashMaterialStates.size)||this.clashModeActive===true);const hasWorksState=!!((this.visualWorksStates&&this.visualWorksStates.size)||(this.worksProgressMaterialStates&&this.worksProgressMaterialStates.size)||(this.worksProgressVisibilityStates&&this.worksProgressVisibilityStates.size)||(this.worksProgressFadedMeshKeys&&this.worksProgressFadedMeshKeys.size));const expectsColorChunks=this.shouldUseVisualStateChunks?this.shouldUseVisualStateChunks():false;const colorBuffersReady=!expectsColorChunks||!!(this.visualStateChunkMeshes&&this.visualStateChunkMeshes.length);if(!changedKeys.size&&colorBuffersReady&&!hasClashState&&!hasWorksState){if(this.recordPerformanceOperation)this.recordPerformanceOperation('applyColorOverrides:noop-unchanged',(this.getPerformanceNow?this.getPerformanceNow():Date.now())-metricsStart,{colorKeys:next.size});return false;}this.clearClashHighlight();this.clearWorksProgressVisualization();this.visualColorOverrides=next;if(changedKeys.size)this.visualColorOverridesRevision=(this.visualColorOverridesRevision||0)+1;if(changedKeys.size)this.updateVisualAppearanceForKeys(changedKeys);else if(!colorBuffersReady){if(this.visualTransactionDepth&&this.visualTransactionDepth>0){this.visualTransactionRefreshAllChunks=true;this.visualTransactionRenderReason=this.visualTransactionRenderReason||'apply-color-overrides-repair';}else if(this.refreshChunkRenderSystems){this.refreshChunkRenderSystems(null,false);}}if(this.scheduleOptimizedVisualRenderStateValidation)this.scheduleOptimizedVisualRenderStateValidation('apply-color-overrides');if(this.recordPerformanceOperation)this.recordPerformanceOperation('applyColorOverrides',(this.getPerformanceNow?this.getPerformanceNow():Date.now())-metricsStart,{colorKeys:next.size,changedKeys:changedKeys.size,withTransparency:!!(this.visualMassState&&this.visualMassState.active)});return true;}darkenColorValue(color,factor){const colorObject=new THREE.Color(Number(color||0));const safeFactor=Number.isFinite(Number(factor))?Math.max(0,Math.min(1,Number(factor))):0.3;colorObject.r*=safeFactor;colorObject.g*=safeFactor;colorObject.b*=safeFactor;return colorObject.getHex();}getDarkerEdgeMaterialForColor(color,opacity,transparent){return this.getSimpleEdgeMaterial(this.darkenColorValue(color,0.3),opacity,transparent);}getDarkerEdgeMaterialForMesh(mesh,opacity,transparent,fallbackColor){const material=this.getPrimaryMeshMaterial(mesh);const color=material&&material.color&&material.color.isColor?material.color.getHex():(fallbackColor==null?0xbebebe:fallbackColor);return this.getDarkerEdgeMaterialForColor(color,opacity,transparent);}getSimpleColorMaterial(color,opacity,transparent){const safeOpacity=Math.max(0.02,Math.min(1,Number(opacity==null?1:opacity)));const isTransparent=!!transparent||safeOpacity<1;const key=Number(color||0).toString(16)+':'+safeOpacity.toFixed(3)+':'+(isTransparent?'t':'o');if(!this.simpleColorMaterialCache.has(key)){const material=new THREE.MeshBasicMaterial({color:Number(color||0),side:THREE.DoubleSide,transparent:isTransparent,opacity:safeOpacity,depthWrite:!isTransparent,depthTest:true,polygonOffset:isTransparent,polygonOffsetFactor:isTransparent?1:0,polygonOffsetUnits:isTransparent?1:0});this.applyAmbientFormShading(material);this.simpleColorMaterialCache.set(key,material);}return this.simpleColorMaterialCache.get(key);}getSimpleEdgeMaterial(color,opacity,transparent){const rawOpacity=Number(opacity==null?1:opacity);const safeOpacity=rawOpacity<=0?0:Math.max(0.02,Math.min(1,rawOpacity));const isTransparent=!!transparent||safeOpacity<1;const key=Number(color||0).toString(16)+':'+safeOpacity.toFixed(3)+':'+(isTransparent?'t':'o');if(!this.simpleEdgeMaterialCache.has(key)){this.simpleEdgeMaterialCache.set(key,new THREE.LineBasicMaterial({color:Number(color||0),transparent:isTransparent,opacity:safeOpacity,depthTest:true,depthWrite:false}));}return this.simpleEdgeMaterialCache.get(key);}createColorOverrideMaterial(originalMaterial,color){return this.getSimpleColorMaterial(color,1,false);}scheduleVisualSectionRefresh(){this.visualDirtySectionPending=true;}flushVisualSectionRefresh(options){const opts=options&&typeof options==='object'?options:{};if(!this.visualDirtySectionPending&&opts.force!==true)return;this.visualDirtySectionPending=false;if(!this.sectionBoxActive||!this.sectionBounds)return;if(this.clashModeActive===true||opts.skipForClash===true)return;if(this.buildSectionCaps)this.buildSectionCaps();if(this.updateSectionBoxVisual)this.updateSectionBoxVisual();if(this.updateSectionCutEdges)this.updateSectionCutEdges();}getMeshKey(mesh){return mesh&&mesh.userData?mesh.userData.elementKey:null;}getBaseMaterialForMesh(mesh){return mesh&&mesh.userData&&mesh.userData.baseMaterial?mesh.userData.baseMaterial:(mesh?mesh.material:null);}getBaseGeometryForMesh(mesh){if(!mesh||!mesh.isMesh)return mesh?mesh.geometry:null;if(!mesh.userData)mesh.userData={};if(!mesh.userData.baseGeometry){mesh.userData.baseGeometry=mesh.geometry;}return mesh.userData.baseGeometry||mesh.geometry||null;}getSingleDrawGeometryForMesh(mesh){const baseGeometry=this.getBaseGeometryForMesh(mesh);if(!baseGeometry||!baseGeometry.isBufferGeometry)return baseGeometry;return this.getSingleDrawGeometryForGeometry(baseGeometry);}getSingleDrawGeometryForGeometry(baseGeometry){if(!baseGeometry||!baseGeometry.isBufferGeometry)return baseGeometry;if(!this.singleDrawGeometryCache)this.singleDrawGeometryCache=new Map();const key=baseGeometry.uuid||baseGeometry.id||String(this.singleDrawGeometryCache.size);const cached=this.singleDrawGeometryCache.get(key);if(cached)return cached;const geometry=new THREE.BufferGeometry();geometry.name=(baseGeometry.name||'')+':single-draw-view';geometry.userData=Object.assign({},baseGeometry.userData||{},{singleDrawCached:true,singleDrawBaseUuid:baseGeometry.uuid||null});if(baseGeometry.index)geometry.setIndex(baseGeometry.index);const attributes=baseGeometry.attributes||{};for(const name of Object.keys(attributes)){geometry.setAttribute(name,attributes[name]);}if(baseGeometry.morphAttributes)geometry.morphAttributes=baseGeometry.morphAttributes;geometry.morphTargetsRelative=!!baseGeometry.morphTargetsRelative;geometry.drawRange.start=baseGeometry.drawRange?baseGeometry.drawRange.start:0;geometry.drawRange.count=baseGeometry.drawRange?baseGeometry.drawRange.count:Infinity;geometry.boundingBox=baseGeometry.boundingBox?baseGeometry.boundingBox.clone():null;geometry.boundingSphere=baseGeometry.boundingSphere?baseGeometry.boundingSphere.clone():null;this.singleDrawGeometryCache.set(key,geometry);return geometry;}applyGeometryForAppearance(mesh,appearance){if(!mesh||!mesh.isMesh||!appearance)return false;const baseGeometry=this.getBaseGeometryForMesh(mesh);if(!baseGeometry)return false;const targetGeometry=appearance.useSingleDrawGeometry?this.getSingleDrawGeometryForMesh(mesh):baseGeometry;if(targetGeometry&&mesh.geometry!==targetGeometry){mesh.geometry=targetGeometry;return true;}return false;}getOriginalBaseEdgeMaterial(mesh){const element=mesh&&mesh.userData?mesh.userData.element:null;if(this.isNonRenderedDataElement(element)){return this.createNonRenderedDataElementLineMaterial();}return this.edgeMaterial;}setMeshBaseVisibility(mesh,visible){if(!mesh||!mesh.userData)return;mesh.userData.baseVisible=visible!==false;if(this.invalidateSelectionCandidateCache)this.invalidateSelectionCandidateCache('mesh-base-visible');this.updateVisualAppearanceForMesh(mesh);}resolveVisualAppearance(mesh){const key=this.getMeshKey(mesh);const baseVisible=!(mesh&&mesh.userData&&mesh.userData.baseVisible===false);const baseRenderOrder=mesh&&mesh.userData?(mesh.userData.baseRenderOrder||0):0;let visible=baseVisible;let material=this.getBaseMaterialForMesh(mesh);let edgeMaterial=this.getOriginalBaseEdgeMaterial(mesh);let forceEdges=false;let useSingleDrawGeometry=false;let renderOrder=baseRenderOrder;let edgeRenderOrder=baseRenderOrder+1;if(!key){return{visible,material,edgeMaterial,forceEdges,useSingleDrawGeometry,renderOrder,edgeRenderOrder};}const works=this.visualWorksStates&&this.visualWorksStates.get(key);const normalChunkSuppressed=!!(mesh&&mesh.userData&&mesh.userData.normalChunkSuppressed);const normalChunkInteractive=this.isNormalChunkInteractiveKey&&this.isNormalChunkInteractiveKey(key);if(normalChunkSuppressed&&!normalChunkInteractive)visible=false;const chunkSuppressed=!!(mesh&&mesh.userData&&mesh.userData.massTransparencyChunkSuppressed);const chunkInteractive=this.isMassTransparencyInteractiveKey&&this.isMassTransparencyInteractiveKey(key);if(chunkSuppressed&&!chunkInteractive)visible=false;const visualStateChunkSuppressed=!!(mesh&&mesh.userData&&mesh.userData.visualStateChunkSuppressed);const visualStateChunkInteractive=this.isVisualStateChunkInteractiveKey&&this.isVisualStateChunkInteractiveKey(key);if(visualStateChunkSuppressed&&!visualStateChunkInteractive)visible=false;if(this.dataPanelFilterActive&&this.dataPanelFilterKeys&&!this.dataPanelFilterKeys.has(key))visible=false;if(works&&works.hidden)visible=false;const hiddenByUserViewAction=(this.hiddenKeys&&this.hiddenKeys.has(key))||(this.isolateActive&&this.isolatedKeys&&this.isolatedKeys.size&&!this.isolatedKeys.has(key));if(this.visualClashForceVisibleKeys&&this.visualClashForceVisibleKeys.has(key)&&!hiddenByUserViewAction)visible=true;if(this.visualClashHiddenKeys&&this.visualClashHiddenKeys.has(key))visible=false;if(this.isNonRenderedDataMesh(mesh)){return{visible,material:mesh&&mesh.isLineSegments?this.createNonRenderedDataElementLineMaterial():this.createNonRenderedDataElementMaterial(),edgeMaterial:this.createNonRenderedDataElementLineMaterial(),forceEdges:false,useSingleDrawGeometry:false,renderOrder:baseRenderOrder,edgeRenderOrder:baseRenderOrder+1};}const applyColor=(color,opacity,transparent,edgeOpacity,order)=>{const safeOpacity=Math.max(0.02,Math.min(1,Number(opacity==null?1:opacity)));const isTransparent=!!transparent||safeOpacity<1;const numericColor=Number(color==null?0xbec7d3:color);material=this.getSimpleColorMaterial(numericColor,safeOpacity,isTransparent);const safeEdgeOpacity=edgeOpacity==null?(isTransparent?Math.max(0.04,safeOpacity):1):edgeOpacity;edgeMaterial=this.getDarkerEdgeMaterialForColor(numericColor,safeEdgeOpacity,safeEdgeOpacity<1||isTransparent);forceEdges=true;useSingleDrawGeometry=mesh&&mesh.isMesh;renderOrder=Number.isFinite(Number(order))?Number(order):baseRenderOrder;edgeRenderOrder=renderOrder+1;};if(visible){const fade=this.visualFadeStates&&this.visualFadeStates.get(key);if(fade){const baseColor=fade.color!=null?Number(fade.color):this.getPrimaryMeshColorValueFromMaterial(material,0xbebebe);applyColor(baseColor,fade.opacity,true,Math.max(0.04,fade.opacity),baseRenderOrder);}const mass=this.visualMassState;const massKeepsActiveSourceAppearance=!!(mass&&mass.active&&mass.preserveActiveAppearance===true&&mass.activeKeys&&mass.activeKeys.has(key));if(mass&&mass.active){if(mass.activeKeys&&mass.activeKeys.has(key)){if(!massKeepsActiveSourceAppearance){applyColor(mass.highlightColor,mass.highlightOpacity,true,mass.highlightOpacity,baseRenderOrder);}else{edgeMaterial=this.getOriginalBaseEdgeMaterial(mesh);forceEdges=true;edgeRenderOrder=Math.max(edgeRenderOrder,baseRenderOrder+2);}}else{applyColor(mass.fadedColor,mass.fadedOpacity,true,Math.max(0.04,mass.fadedOpacity),baseRenderOrder);}}const data=this.visualColorOverrides&&this.visualColorOverrides.get(key);if(data){applyColor(data.color,1,false,1,Math.max(baseRenderOrder,renderOrder+2));visible=baseVisible;}if(works&&!works.hidden){applyColor(works.color,works.opacity,works.opacity<1,works.edgeOpacity,baseRenderOrder);}const clash=this.visualClashStates&&this.visualClashStates.get(key);if(clash){applyColor(clash.color,clash.opacity,true,clash.edgeOpacity,clash.renderOrder);if(clash.edgeColor!=null){edgeMaterial=this.getSimpleEdgeMaterial(clash.edgeColor,clash.edgeOpacity,true);forceEdges=true;}}const overlayOnly=this.visualInteractionOverlayOnly===true;const selectionUsesSource=this.selectionUsesSourceAppearance?this.selectionUsesSourceAppearance():this.visualSelectionOverlayOnly!==true;const isSelected=this.selectedKeys&&this.selectedKeys.has(key)&&this.visualSelectionPreserveAppearance!==true&&selectionUsesSource;const isRectanglePreview=!overlayOnly&&this.rectanglePreviewKeys&&this.rectanglePreviewKeys.has(key);if(isSelected){material=mesh&&mesh.isLineSegments?this.selectionEdgeMaterial:this.selectionMaterial;edgeMaterial=this.selectionEdgeMaterial;forceEdges=true;useSingleDrawGeometry=false;renderOrder=Math.max(renderOrder,50);edgeRenderOrder=renderOrder+1;}else if(isRectanglePreview){if(mesh&&mesh.isLineSegments)material=this.rectanglePreviewEdgeMaterial;edgeMaterial=this.rectanglePreviewEdgeMaterial;forceEdges=true;edgeRenderOrder=Math.max(edgeRenderOrder,49);}else if(!overlayOnly&&this.visualHoverKey===key&&this.hoverVisualMutatesGeometry===true){if(mesh&&mesh.isLineSegments)material=this.hoverEdgeMaterial;edgeMaterial=this.hoverEdgeMaterial;forceEdges=true;edgeRenderOrder=Math.max(edgeRenderOrder,46);}}if(normalChunkSuppressed&&!normalChunkInteractive)visible=false;if(chunkSuppressed&&!chunkInteractive)visible=false;if(visualStateChunkSuppressed&&!visualStateChunkInteractive)visible=false;return{visible,material,edgeMaterial,forceEdges,useSingleDrawGeometry,renderOrder,edgeRenderOrder};}getPrimaryMeshColorValueFromMaterial(material,fallbackColor){const primary=Array.isArray(material)?material[0]:material;if(primary&&primary.color&&primary.color.isColor)return primary.color.getHex();return fallbackColor==null?0xbec7d3:fallbackColor;}updateVisualAppearanceForMesh(mesh){if(!mesh||!mesh.userData)return;const appearance=this.resolveVisualAppearance(mesh);let changed=false;if(mesh.visible!==appearance.visible){mesh.visible=appearance.visible;changed=true;}if(mesh.material!==appearance.material){mesh.material=appearance.material;changed=true;}const nextRenderOrder=appearance.renderOrder||0;if(mesh.renderOrder!==nextRenderOrder){mesh.renderOrder=nextRenderOrder;changed=true;}if(this.applyGeometryForAppearance(mesh,appearance))changed=true;if(this.setEdgeMaterial(mesh,appearance.edgeMaterial,appearance.forceEdges,appearance.edgeRenderOrder))changed=true;if(this.syncTransientCompactFallbackEdge&&this.syncTransientCompactFallbackEdge(mesh,appearance))changed=true;if(changed)this.scheduleVisualSectionRefresh();}updateVisualAppearanceForKeys(keys,options){const metricsStart=this.getPerformanceNow?this.getPerformanceNow():Date.now();const opts=options&&typeof options==='object'?options:{};const keySet=new Set(keys||[]);if(!keySet.size)return;if(this.sectionBoxActive&&opts.skipSectionRefresh!==true&&this.clashModeActive!==true){this.visualDirtySectionPending=true;}let meshHits=0;for(const key of keySet){const meshes=this.getMeshesForElementKey?this.getMeshesForElementKey(key):(this.meshByElementKey&&this.meshByElementKey.get(key)?[this.meshByElementKey.get(key)]:[]);for(const mesh of meshes){if(!mesh)continue;meshHits++;this.updateVisualAppearanceForMesh(mesh);}}if(this.shouldUseSelectionOverlayBatches&&this.shouldUseSelectionOverlayBatches()&&this.selectedKeys&&this.selectedKeys.size){let touchesSelection=false;for(const key of keySet){if(this.selectedKeys.has(key)){touchesSelection=true;break;}}if(touchesSelection&&this.refreshSelectionOverlayBatches)this.refreshSelectionOverlayBatches(true);}if(this.visualTransactionDepth&&this.visualTransactionDepth>0){if(!this.visualTransactionChangedKeys)this.visualTransactionChangedKeys=new Set();for(const key of keySet)this.visualTransactionChangedKeys.add(key);this.visualTransactionRenderReason=this.visualTransactionRenderReason||'visual-appearance-keys';}else{if(opts.skipSectionRefresh===true)this.visualDirtySectionPending=false;else this.flushVisualSectionRefresh();if(opts.skipChunkRefresh!==true)this.refreshChunkRenderSystems&&this.refreshChunkRenderSystems(keySet,!!opts.forceChunkRebuild);this.requestRender&&this.requestRender('visual-appearance-keys');}if(this.recordVisualUpdateMetric)this.recordVisualUpdateMetric('updateVisualAppearanceForKeys',(this.getPerformanceNow?this.getPerformanceNow():Date.now())-metricsStart,{keyCount:keySet.size,meshHits,batched:!!(this.visualTransactionDepth&&this.visualTransactionDepth>0)});}async updateVisualAppearanceForKeysBatched(keys,options){const metricsStart=this.getPerformanceNow?this.getPerformanceNow():Date.now();const opts=options&&typeof options==='object'?options:{};const keyList=Array.from(new Set(keys||[]));if(!keyList.length)return{keys:0,meshes:0};const report=typeof opts.onProgress==='function'?opts.onProgress:()=>{};const isCancelled=typeof opts.isCancelled==='function'?opts.isCancelled:()=>false;const requestedBatchSize=Number(opts.batchSize);const batchSize=Number.isFinite(requestedBatchSize)&&requestedBatchSize>0?Math.max(100,Math.floor(requestedBatchSize)):Math.max(600,Math.ceil(keyList.length/12));if(this.sectionBoxActive&&opts.skipSectionRefresh!==true&&this.clashModeActive!==true){this.visualDirtySectionPending=true;}let meshHits=0;for(let start=0;start<keyList.length;start+=batchSize){if(isCancelled())return{keys:start,meshes:meshHits,cancelled:true};const end=Math.min(keyList.length,start+batchSize);for(let index=start;index<end;index++){const key=keyList[index];const meshes=this.getMeshesForElementKey?this.getMeshesForElementKey(key):(this.meshByElementKey&&this.meshByElementKey.get(key)?[this.meshByElementKey.get(key)]:[]);for(const mesh of meshes){if(!mesh)continue;meshHits++;this.updateVisualAppearanceForMesh(mesh);}}report(end,keyList.length,opts.progressMessage||'Actualizando superficies y bordes...');if(end<keyList.length&&opts.yieldBetweenBatches!==false){if(this.waitForResetProgressPaint)await this.waitForResetProgressPaint();else if(this.yieldDuringLoad)await this.yieldDuringLoad();}}if(isCancelled())return{keys:keyList.length,meshes:meshHits,cancelled:true};if(this.shouldUseSelectionOverlayBatches&&this.shouldUseSelectionOverlayBatches()&&this.selectedKeys&&this.selectedKeys.size){let touchesSelection=false;for(const key of keyList){if(this.selectedKeys.has(key)){touchesSelection=true;break;}}if(touchesSelection&&this.refreshSelectionOverlayBatches)this.refreshSelectionOverlayBatches(true);}const keySet=new Set(keyList);if(this.visualTransactionDepth&&this.visualTransactionDepth>0){if(!this.visualTransactionChangedKeys)this.visualTransactionChangedKeys=new Set();for(const key of keySet)this.visualTransactionChangedKeys.add(key);this.visualTransactionRenderReason=this.visualTransactionRenderReason||'visual-appearance-keys-batched';}else{if(opts.skipSectionRefresh===true)this.visualDirtySectionPending=false;else this.flushVisualSectionRefresh();if(opts.skipChunkRefresh!==true)this.refreshChunkRenderSystems&&this.refreshChunkRenderSystems(keySet,!!opts.forceChunkRebuild);this.requestRender&&this.requestRender('visual-appearance-keys-batched');}if(this.recordVisualUpdateMetric)this.recordVisualUpdateMetric('updateVisualAppearanceForKeysBatched',(this.getPerformanceNow?this.getPerformanceNow():Date.now())-metricsStart,{keyCount:keyList.length,meshHits,batchSize,batched:true});return{keys:keyList.length,meshes:meshHits};}ensureSelectionOverlayGroup3D(){if(this.selectionOverlayGroup3D)return this.selectionOverlayGroup3D;const group=new THREE.Group();group.name='BIM_SelectionOverlay3D';group.renderOrder=92;group.userData=Object.assign({},group.userData||{},{isSelectionOverlayGroup:true});this.selectionOverlayGroup3D=group;if(this.scene)this.scene.add(group);return group;}clearSelectionOverlayBatches(dispose){const shouldDispose=dispose!==false;for(const object of this.selectionOverlayObjects3D||[]){if(!object)continue;if(object.parent)object.parent.remove(object);if(shouldDispose&&object.userData&&object.userData.ownsSelectionOverlayGeometry&&object.geometry&&object.geometry.dispose)object.geometry.dispose();}this.selectionOverlayObjects3D=[];if(this.selectionOverlayKeys3D)this.selectionOverlayKeys3D.clear();this.selectionOverlaySignature=null;return 0;}getVisualOverlaySourceMeshesForKeys(keys){const result=[];const seen=new Set();for(const rawKey of keys||[]){const key=String(rawKey||'');if(!key)continue;const meshes=this.getMeshesForElementKey?this.getMeshesForElementKey(key):[];for(const mesh of meshes||[]){if(!mesh||!mesh.geometry||seen.has(mesh))continue;if(!mesh.userData||mesh.userData.baseVisible===false)continue;if(this.hiddenKeys&&this.hiddenKeys.has(key))continue;if(this.isolateActive&&this.isolatedKeys&&this.isolatedKeys.size&&!this.isolatedKeys.has(key))continue;if(this.dataPanelFilterActive&&this.dataPanelFilterKeys&&!this.dataPanelFilterKeys.has(key))continue;const works=this.visualWorksStates&&this.visualWorksStates.get(key);if(works&&works.hidden)continue;seen.add(mesh);result.push(mesh);}}return result;}getSelectionOverlaySourceMeshes(){return this.getVisualOverlaySourceMeshesForKeys?this.getVisualOverlaySourceMeshesForKeys(this.selectedKeys||[]):[];}buildSelectionOverlayEdgeBatchesFromMeshes(meshes,options){const opts=options&&typeof options==='object'?options:{};const groups=new Map();const temp=new THREE.Vector3();const spatialGrid=this.getSpatialChunkGrid?this.getSpatialChunkGrid(meshes,{spatialMinimumMeshes:Number(opts.spatialMinimumMeshes)||12,targetMeshesPerCell:Number(opts.targetMeshesPerCell)||80,maximumCells:Number(opts.maximumCells)||256}):null;const appendGeometry=(sourceObject,sourceMesh,spatialKey)=>{if(!sourceObject||!sourceObject.geometry)return;const position=sourceObject.geometry.getAttribute&&sourceObject.geometry.getAttribute('position');if(!position||!position.count)return;sourceObject.updateWorldMatrix&&sourceObject.updateWorldMatrix(true,false);const matrix=sourceObject.matrixWorld||sourceMesh.matrixWorld;const transformed=this.buildTransformedLineSegmentPositions?this.buildTransformedLineSegmentPositions(sourceObject.geometry,matrix,temp):new Float32Array(0);if(!transformed.length)return;let bucket=groups.get(spatialKey);if(!bucket){bucket={arrays:[],count:0,spatialKey};groups.set(spatialKey,bucket);}bucket.arrays.push(transformed);bucket.count+=transformed.length;};for(const mesh of meshes||[]){if(!mesh||!mesh.geometry)continue;mesh.updateWorldMatrix&&mesh.updateWorldMatrix(true,false);const spatialKey=spatialGrid&&this.getSpatialChunkKeyForMesh?this.getSpatialChunkKeyForMesh(mesh,spatialGrid):'all';let edgeCount=0;for(const child of mesh.children||[]){if(!child||!child.isLineSegments||!child.geometry)continue;appendGeometry(child,mesh,spatialKey);edgeCount++;}if(!edgeCount&&mesh.isMesh&&this.getGeneratedInteractionEdgeGeometry){const geometry=this.getGeneratedInteractionEdgeGeometry(mesh);if(geometry){const proxy={geometry,matrixWorld:mesh.matrixWorld,updateWorldMatrix(){}};appendGeometry(proxy,mesh,spatialKey);}}}const objects=[];const maxFloatValues=Math.max(6000,Number(opts.maxFloatValues)||180000);const overlayMaterial=opts.material||this.selectionEdgeMaterial;const overlayType=opts.overlayType||'selection';const renderOrder=Number.isFinite(Number(opts.renderOrder))?Number(opts.renderOrder):93;for(const bucket of groups.values()){let parts=[];let floatCount=0;const flush=()=>{if(!floatCount)return;const merged=new Float32Array(floatCount);let offset=0;for(const part of parts){merged.set(part,offset);offset+=part.length;}const geometry=new THREE.BufferGeometry();geometry.setAttribute('position',new THREE.BufferAttribute(merged,3));geometry.computeBoundingBox();geometry.computeBoundingSphere();const object=new THREE.LineSegments(geometry,overlayMaterial);object.frustumCulled=true;object.renderOrder=renderOrder;object.userData=Object.assign({},object.userData||{},{isSelectionOverlay:overlayType==='selection',isSelectionEdgeBatch:overlayType==='selection',isRectanglePreviewOverlay:overlayType==='rectangle-preview',isRectanglePreviewEdgeBatch:overlayType==='rectangle-preview',ownsSelectionOverlayGeometry:true,overlayType,spatialChunkKey:bucket.spatialKey||'all'});objects.push(object);parts=[];floatCount=0;};for(const array of bucket.arrays){if(floatCount&&floatCount+array.length>maxFloatValues)flush();if(array.length<=maxFloatValues){parts.push(array);floatCount+=array.length;continue;}let offset=0;while(offset<array.length){const take=Math.min(maxFloatValues,array.length-offset);const aligned=take===array.length-offset?take:Math.max(6,Math.floor(take/6)*6);parts.push(array.subarray(offset,offset+aligned));floatCount+=aligned;offset+=aligned;flush();}}flush();}return objects;}ensureRectanglePreviewOverlayGroup3D(){if(this.rectanglePreviewOverlayGroup3D)return this.rectanglePreviewOverlayGroup3D;const group=new THREE.Group();group.name='BIM_RectanglePreviewOverlay3D';group.renderOrder=88;group.userData=Object.assign({},group.userData||{},{isRectanglePreviewOverlayGroup:true});this.rectanglePreviewOverlayGroup3D=group;if(this.scene)this.scene.add(group);return group;}clearRectanglePreviewOverlayBatches(dispose){const shouldDispose=dispose!==false;for(const object of this.rectanglePreviewOverlayObjects3D||[]){if(!object)continue;if(object.parent)object.parent.remove(object);if(shouldDispose&&object.userData&&object.userData.ownsSelectionOverlayGeometry&&object.geometry&&object.geometry.dispose)object.geometry.dispose();}this.rectanglePreviewOverlayObjects3D=[];this.rectanglePreviewOverlaySignature=null;return 0;}refreshRectanglePreviewOverlayBatches(force){if(this.rectanglePreviewUsesBatch!==true){this.clearRectanglePreviewOverlayBatches(true);return 0;}const keys=new Set(this.rectanglePreviewKeys||[]);const fingerprint=this.getKeySetFingerprint?this.getKeySetFingerprint(keys):Array.from(keys).sort().join('\u001f');const signature=['rectangle-preview-overlay-v1',fingerprint,this.normalChunkRevision||0,keys.size].join('|');if(!force&&signature===this.rectanglePreviewOverlaySignature)return(this.rectanglePreviewOverlayObjects3D||[]).length;const metricsStart=this.getPerformanceNow?this.getPerformanceNow():Date.now();this.clearRectanglePreviewOverlayBatches(true);this.rectanglePreviewOverlaySignature=signature;if(!keys.size)return 0;const sourceMeshes=this.getVisualOverlaySourceMeshesForKeys?this.getVisualOverlaySourceMeshesForKeys(keys):[];if(!sourceMeshes.length)return 0;const group=this.ensureRectanglePreviewOverlayGroup3D?this.ensureRectanglePreviewOverlayGroup3D():null;if(!group)return 0;const objects=this.buildSelectionOverlayEdgeBatchesFromMeshes?this.buildSelectionOverlayEdgeBatchesFromMeshes(sourceMeshes,{material:this.rectanglePreviewEdgeMaterial,overlayType:'rectangle-preview',renderOrder:89,maxFloatValues:150000,targetMeshesPerCell:70,maximumCells:256}):[];for(const object of objects)group.add(object);this.rectanglePreviewOverlayObjects3D=objects;if(this.recordVisualUpdateMetric)this.recordVisualUpdateMetric('rectanglePreviewOverlay:build',(this.getPerformanceNow?this.getPerformanceNow():Date.now())-metricsStart,{selectedKeys:keys.size,sourceMeshes:sourceMeshes.length,overlayObjects:objects.length,batched:true});this.requestRender&&this.requestRender('rectangle-preview-overlay');return objects.length;}refreshSelectionOverlayBatches(force){const useBatchedOverlay=this.shouldUseSelectionOverlayBatches?this.shouldUseSelectionOverlayBatches():this.visualSelectionOverlayOnly===true;if(!useBatchedOverlay||this.visualSelectionPreserveAppearance===true){this.clearSelectionOverlayBatches(true);return 0;}const keys=new Set(this.selectedKeys||[]);const fingerprint=this.getKeySetFingerprint?this.getKeySetFingerprint(keys):Array.from(keys).sort().join('\u001f');const signature=['selection-overlay-v2',fingerprint,this.normalChunkRevision||0,keys.size].join('|');if(!force&&signature===this.selectionOverlaySignature)return(this.selectionOverlayObjects3D||[]).length;const metricsStart=this.getPerformanceNow?this.getPerformanceNow():Date.now();this.clearSelectionOverlayBatches(true);this.selectionOverlaySignature=signature;if(!keys.size)return 0;const sourceMeshes=this.getSelectionOverlaySourceMeshes?this.getSelectionOverlaySourceMeshes():[];if(!sourceMeshes.length)return 0;const group=this.ensureSelectionOverlayGroup3D?this.ensureSelectionOverlayGroup3D():null;if(!group)return 0;const objects=[];const directLimit=Math.max(1,Number(this.selectionOverlayDirectMeshLimit||12));if(sourceMeshes.length<=directLimit){const state={type:'selected',material:this.selectionMaterial,edgeMaterial:this.selectionEdgeMaterial,renderOrder:92,edgeOnly:false};for(const mesh of sourceMeshes){const meshOverlay=this.createInteractionOverlayForObject?this.createInteractionOverlayForObject(mesh,this.getMeshKey(mesh),state,false):null;if(meshOverlay)objects.push(meshOverlay);let edgeCount=0;for(const child of mesh.children||[]){if(!child||!child.isLineSegments||!child.geometry)continue;const edgeOverlay=this.createInteractionOverlayForObject?this.createInteractionOverlayForObject(child,this.getMeshKey(mesh),state,true):null;if(edgeOverlay){objects.push(edgeOverlay);edgeCount++;}}if(!edgeCount&&this.createGeneratedEdgeInteractionOverlay){const edgeOverlay=this.createGeneratedEdgeInteractionOverlay(mesh,this.getMeshKey(mesh),state);if(edgeOverlay)objects.push(edgeOverlay);}}}else{const geometries=this.buildChunkGeometriesFromMeshes?this.buildChunkGeometriesFromMeshes(sourceMeshes,{useSingleDraw:false,maxVerticesPerChunk:Number(this.selectionOverlayMaxVerticesPerChunk||120000),maxIndicesPerChunk:Number(this.selectionOverlayMaxIndicesPerChunk||420000),spatialMinimumMeshes:12,targetMeshesPerCell:80,maximumCells:256}):[];for(const geometry of geometries){const object=new THREE.Mesh(geometry,this.selectionMaterial);object.frustumCulled=true;object.renderOrder=92;object.userData=Object.assign({},object.userData||{},{isSelectionOverlay:true,isSelectionSurfaceBatch:true,ownsSelectionOverlayGeometry:true});objects.push(object);}for(const edgeObject of this.buildSelectionOverlayEdgeBatchesFromMeshes(sourceMeshes))objects.push(edgeObject);}for(const object of objects)group.add(object);this.selectionOverlayObjects3D=objects;this.selectionOverlayKeys3D=keys;const duration=(this.getPerformanceNow?this.getPerformanceNow():Date.now())-metricsStart;if(this.recordVisualUpdateMetric)this.recordVisualUpdateMetric('selectionOverlay:build',duration,{selectedKeys:keys.size,sourceMeshes:sourceMeshes.length,overlayObjects:objects.length,batched:sourceMeshes.length>directLimit});if(this.recordPerformanceOperation)this.recordPerformanceOperation('selectionOverlay:build',duration,{selectedKeys:keys.size,sourceMeshes:sourceMeshes.length,overlayObjects:objects.length,batched:sourceMeshes.length>directLimit});return objects.length;}ensureInteractionOverlayGroup(){if(this.interactionOverlayGroup3D)return this.interactionOverlayGroup3D;const group=new THREE.Group();group.name='BIM_InteractionOverlay3D';group.renderOrder=90;group.userData=Object.assign({},group.userData||{},{isInteractionOverlayGroup:true});this.interactionOverlayGroup3D=group;if(this.scene)this.scene.add(group);return group;}clearInteractionOverlayForKey(key){if(!key||!this.interactionOverlayObjectsByKey)return;const objects=this.interactionOverlayObjectsByKey.get(key);if(!objects||!objects.length){this.interactionOverlayObjectsByKey.delete(key);if(this.interactionOverlayActiveKeys)this.interactionOverlayActiveKeys.delete(key);return;}for(const object of objects){if(!object)continue;if(object.parent)object.parent.remove(object);}this.interactionOverlayObjectsByKey.delete(key);if(this.interactionOverlayActiveKeys)this.interactionOverlayActiveKeys.delete(key);}clearAllInteractionOverlays(){if(this.clearRectanglePreviewOverlayBatches)this.clearRectanglePreviewOverlayBatches(true);if(this.rectanglePreviewOverlayGroup3D&&this.rectanglePreviewOverlayGroup3D.parent)this.rectanglePreviewOverlayGroup3D.parent.remove(this.rectanglePreviewOverlayGroup3D);this.rectanglePreviewOverlayGroup3D=null;this.rectanglePreviewUsesBatch=false;if(this.clearSelectionOverlayBatches)this.clearSelectionOverlayBatches(true);if(this.selectionOverlayGroup3D&&this.selectionOverlayGroup3D.parent)this.selectionOverlayGroup3D.parent.remove(this.selectionOverlayGroup3D);this.selectionOverlayGroup3D=null;if(this.interactionOverlayObjectsByKey){for(const key of Array.from(this.interactionOverlayObjectsByKey.keys()))this.clearInteractionOverlayForKey(key);this.interactionOverlayObjectsByKey.clear();}if(this.interactionOverlayActiveKeys)this.interactionOverlayActiveKeys.clear();if(this.interactionOverlayGroup3D&&this.interactionOverlayGroup3D.parent)this.interactionOverlayGroup3D.parent.remove(this.interactionOverlayGroup3D);this.interactionOverlayGroup3D=null;}getInteractionOverlayStateForKey(key){if(!key)return null;const isNonRenderedDataElement=this.isNonRenderedDataKey&&this.isNonRenderedDataKey(key);if(isNonRenderedDataElement&&this.selectedKeys&&this.selectedKeys.has(key)){return{type:'selected',material:null,edgeMaterial:this.selectionEdgeMaterial,renderOrder:92,edgeOnly:true};}if(this.selectedKeys&&this.selectedKeys.has(key)&&this.visualSelectionPreserveAppearance!==true){return null;}if(this.rectanglePreviewKeys&&this.rectanglePreviewKeys.has(key)&&this.rectanglePreviewUsesBatch!==true){return{type:'preview',material:null,edgeMaterial:this.rectanglePreviewEdgeMaterial,renderOrder:88,edgeOnly:true};}if(this.visualHoverKey&&this.visualHoverKey===key){return{type:'hover',material:null,edgeMaterial:this.hoverEdgeMaterial,renderOrder:86,edgeOnly:true};}return null;}getGeneratedInteractionEdgeGeometry(source){if(!source||!source.geometry||!source.isMesh)return null;if(!this.interactionGeneratedEdgeGeometryCache)this.interactionGeneratedEdgeGeometryCache=new WeakMap();const cached=this.interactionGeneratedEdgeGeometryCache.get(source.geometry);if(cached)return cached;try{const angle=Number.isFinite(Number(this.edgeDetectionAngle))?Number(this.edgeDetectionAngle):2;const geometry=this.getOrCreateGeneratedEdgeGeometry?this.getOrCreateGeneratedEdgeGeometry(source,angle):new THREE.EdgesGeometry(source.geometry,angle);if(!geometry)return null;this.interactionGeneratedEdgeGeometryCache.set(source.geometry,geometry);return geometry;}catch(_){return null;}}createInteractionOverlayForObject(source,key,state,isEdge){if(!source||!source.geometry||!state)return null;const material=isEdge?state.edgeMaterial:state.material;if(!material)return null;source.updateWorldMatrix&&source.updateWorldMatrix(true,false);const overlay=isEdge?new THREE.LineSegments(source.geometry,material):new THREE.Mesh(source.geometry,material);overlay.matrix.copy(source.matrixWorld);overlay.matrixAutoUpdate=false;overlay.frustumCulled=true;overlay.renderOrder=(state.renderOrder||80)+(isEdge?1:0);overlay.userData=Object.assign({},overlay.userData||{},{isInteractionOverlay:true,sourceKey:key,overlayType:state.type});return overlay;}createGeneratedEdgeInteractionOverlay(source,key,state){if(!source||!source.isMesh||!state||!state.edgeMaterial)return null;const geometry=this.getGeneratedInteractionEdgeGeometry?this.getGeneratedInteractionEdgeGeometry(source):null;if(!geometry)return null;source.updateWorldMatrix&&source.updateWorldMatrix(true,false);const overlay=new THREE.LineSegments(geometry,state.edgeMaterial);overlay.matrix.copy(source.matrixWorld);overlay.matrixAutoUpdate=false;overlay.frustumCulled=true;overlay.renderOrder=(state.renderOrder||80)+1;overlay.userData=Object.assign({},overlay.userData||{},{isInteractionOverlay:true,isGeneratedInteractionEdgeOverlay:true,sourceKey:key,overlayType:state.type});return overlay;}buildInteractionOverlayObjectsForKey(key){const state=this.getInteractionOverlayStateForKey?this.getInteractionOverlayStateForKey(key):null;if(!state)return[];const meshes=this.getMeshesForElementKey?this.getMeshesForElementKey(key):[];const objects=[];for(const mesh of meshes||[]){if(!mesh||!mesh.userData)continue;if(mesh.userData.baseVisible===false)continue;if(this.dataPanelFilterActive&&this.dataPanelFilterKeys&&!this.dataPanelFilterKeys.has(key))continue;const works=this.visualWorksStates&&this.visualWorksStates.get(key);if(works&&works.hidden)continue;if(mesh.isMesh){if(state.edgeOnly!==true){const meshOverlay=this.createInteractionOverlayForObject(mesh,key,state,false);if(meshOverlay)objects.push(meshOverlay);}let edgeCount=0;for(const child of mesh.children||[]){if(!child||!child.isLineSegments||!child.geometry)continue;const edgeOverlay=this.createInteractionOverlayForObject(child,key,state,true);if(edgeOverlay){objects.push(edgeOverlay);edgeCount++;}}if(edgeCount===0&&state.edgeOnly===true){const generatedEdgeOverlay=this.createGeneratedEdgeInteractionOverlay?this.createGeneratedEdgeInteractionOverlay(mesh,key,state):null;if(generatedEdgeOverlay)objects.push(generatedEdgeOverlay);}}else if(mesh.isLineSegments){const lineOverlay=this.createInteractionOverlayForObject(mesh,key,state,true);if(lineOverlay)objects.push(lineOverlay);}}return objects;}updateInteractionOverlayForKeys(keys){const keySet=new Set(keys||[]);if(!keySet.size)return;if(this.rectanglePreviewUsesBatch===true&&this.refreshRectanglePreviewOverlayBatches)this.refreshRectanglePreviewOverlayBatches(false);if(this.refreshSelectionOverlayBatches)this.refreshSelectionOverlayBatches(false);const group=this.ensureInteractionOverlayGroup?this.ensureInteractionOverlayGroup():null;if(!group)return;if(!this.interactionOverlayObjectsByKey)this.interactionOverlayObjectsByKey=new Map();if(!this.interactionOverlayActiveKeys)this.interactionOverlayActiveKeys=new Set();for(const key of keySet){this.clearInteractionOverlayForKey(key);const objects=this.buildInteractionOverlayObjectsForKey?this.buildInteractionOverlayObjectsForKey(key):[];if(!objects.length)continue;for(const object of objects)group.add(object);this.interactionOverlayObjectsByKey.set(key,objects);this.interactionOverlayActiveKeys.add(key);}}updateInteractionAppearanceForKeys(keys){const metricsStart=this.getPerformanceNow?this.getPerformanceNow():Date.now();const keySet=new Set(keys||[]);if(!keySet.size)return;const selectionUsesSourceAppearance=this.selectionUsesSourceAppearance?this.selectionUsesSourceAppearance():this.visualSelectionOverlayOnly!==true;const explicitSourceKeys=this._selectionSourceAppearanceKeys instanceof Set?new Set(this._selectionSourceAppearanceKeys):null;const sourceAppearanceKeys=explicitSourceKeys||keySet;const keySetTouchesCurrentSelection=selectionUsesSourceAppearance&&this.selectedKeys&&Array.from(sourceAppearanceKeys).some(key=>this.selectedKeys.has(key));const needsSourceSelectionUpdate=sourceAppearanceKeys.size>0&&(this._forceSelectionSourceAppearanceUpdate===true||keySetTouchesCurrentSelection);const chunkAppearanceKeys=this._selectionChunkAppearanceKeys instanceof Set?new Set(this._selectionChunkAppearanceKeys):keySet;const needsBatchedSelectionMaskSync=!selectionUsesSourceAppearance&&chunkAppearanceKeys.size>0;const selectionOverlayModeChanged=this._selectionOverlayModeChanged===true;let meshHits=0;const restoreSuppressedSourceWithoutEdgeRebuild=mesh=>{if(!mesh||!mesh.userData)return false;const key=this.getMeshKey?this.getMeshKey(mesh):mesh.userData.elementKey;const suppressed=!!(mesh.userData.normalChunkSuppressed||mesh.userData.massTransparencyChunkSuppressed||mesh.userData.visualStateChunkSuppressed);const interactive=!!((this.isNormalChunkInteractiveKey&&this.isNormalChunkInteractiveKey(key))||(this.isMassTransparencyInteractiveKey&&this.isMassTransparencyInteractiveKey(key))||(this.isVisualStateChunkInteractiveKey&&this.isVisualStateChunkInteractiveKey(key)));if(!suppressed||interactive)return false;const appearance=this.resolveVisualAppearance?this.resolveVisualAppearance(mesh):null;if(appearance){mesh.material=appearance.material;mesh.renderOrder=appearance.renderOrder||0;if(this.applyGeometryForAppearance)this.applyGeometryForAppearance(mesh,appearance);for(const child of mesh.children||[]){if(!child||!child.isLineSegments)continue;child.material=appearance.edgeMaterial||child.material;child.renderOrder=appearance.edgeRenderOrder||0;child.visible=false;}}mesh.visible=false;return true;};if(this.visualInteractionOverlayOnly===true){if(needsSourceSelectionUpdate||selectionOverlayModeChanged||needsBatchedSelectionMaskSync){if(needsSourceSelectionUpdate&&this.ensureInteractiveMeshesVisibleForKeys&&this.selectedKeys&&this.selectedKeys.size){this.ensureInteractiveMeshesVisibleForKeys(this.selectedKeys);}if(this.syncInteractiveNormalChunkPeel)this.syncInteractiveNormalChunkPeel();if(needsSourceSelectionUpdate){for(const key of sourceAppearanceKeys){const meshes=this.getMeshesForElementKey?this.getMeshesForElementKey(key):(this.meshByElementKey&&this.meshByElementKey.get(key)?[this.meshByElementKey.get(key)]:[]);for(const mesh of meshes){if(!mesh)continue;meshHits++;if(restoreSuppressedSourceWithoutEdgeRebuild(mesh))continue;this.updateVisualAppearanceForMesh(mesh);}}}if(chunkAppearanceKeys.size&&this.visualStateChunkVisible&&this.refreshChunkRenderSystems)this.refreshChunkRenderSystems(chunkAppearanceKeys,false);if(this.visualClashStates&&this.visualClashStates.size)this.visualDirtySectionPending=false;else this.flushVisualSectionRefresh&&this.flushVisualSectionRefresh();}this._forceSelectionSourceAppearanceUpdate=false;this._selectionSourceAppearanceKeys=null;this._selectionChunkAppearanceKeys=null;this._selectionOverlayModeChanged=false;this.updateInteractionOverlayForKeys&&this.updateInteractionOverlayForKeys(keySet);this.requestRender&&this.requestRender(needsSourceSelectionUpdate?'interaction-source-overlay':'interaction-overlay');if(this.recordVisualUpdateMetric)this.recordVisualUpdateMetric('updateInteractionAppearanceForKeys:hybrid',(this.getPerformanceNow?this.getPerformanceNow():Date.now())-metricsStart,{keyCount:keySet.size,meshHits,sourceSelection:needsSourceSelectionUpdate,batchedSelection:!selectionUsesSourceAppearance,maskSync:needsBatchedSelectionMaskSync,modeChanged:selectionOverlayModeChanged});return;}for(const key of keySet){const meshes=this.getMeshesForElementKey?this.getMeshesForElementKey(key):(this.meshByElementKey&&this.meshByElementKey.get(key)?[this.meshByElementKey.get(key)]:[]);for(const mesh of meshes){if(!mesh)continue;meshHits++;if(restoreSuppressedSourceWithoutEdgeRebuild(mesh))continue;this.updateVisualAppearanceForMesh(mesh);}}if(this.syncInteractiveNormalChunkPeel)this.syncInteractiveNormalChunkPeel();if(this.visualStateChunkVisible&&this.refreshChunkRenderSystems)this.refreshChunkRenderSystems(keySet,false);if(this.visualClashStates&&this.visualClashStates.size)this.visualDirtySectionPending=false;else this.flushVisualSectionRefresh();this.requestRender&&this.requestRender('interaction-appearance');if(this.recordVisualUpdateMetric)this.recordVisualUpdateMetric('updateInteractionAppearanceForKeys',(this.getPerformanceNow?this.getPerformanceNow():Date.now())-metricsStart,{keyCount:keySet.size,meshHits});}updateVisualAppearanceForAll(options){const metricsStart=this.getPerformanceNow?this.getPerformanceNow():Date.now();const opts=options&&typeof options==='object'?options:{};let meshCount=0;for(const mesh of this.meshes||[]){meshCount++;this.updateVisualAppearanceForMesh(mesh);}if(this.shouldUseSelectionOverlayBatches&&this.shouldUseSelectionOverlayBatches()&&this.selectedKeys&&this.selectedKeys.size&&this.refreshSelectionOverlayBatches){this.refreshSelectionOverlayBatches(true);}if(this.visualTransactionDepth&&this.visualTransactionDepth>0){this.visualTransactionRefreshAllChunks=opts.skipChunkRefresh!==true;this.visualTransactionRenderReason=this.visualTransactionRenderReason||'visual-appearance-all';}else{if(opts.skipSectionRefresh===true)this.visualDirtySectionPending=false;else this.flushVisualSectionRefresh();if(opts.skipChunkRefresh!==true){this.refreshChunkRenderSystems&&this.refreshChunkRenderSystems(null,false);}if(opts.skipRender!==true)this.requestRender&&this.requestRender('visual-appearance-all');}if(this.recordVisualUpdateMetric)this.recordVisualUpdateMetric('updateVisualAppearanceForAll',(this.getPerformanceNow?this.getPerformanceNow():Date.now())-metricsStart,{meshCount,batched:!!(this.visualTransactionDepth&&this.visualTransactionDepth>0),skipChunkRefresh:opts.skipChunkRefresh===true});}async refreshChunkRenderSystemsResponsive(changedKeys,forceNormalRebuild,options){const opts=options&&typeof options==='object'?options:{};const isCancelled=typeof opts.isCancelled==='function'?opts.isCancelled:()=>false;const report=typeof opts.onProgress==='function'?opts.onProgress:()=>{};if(isCancelled())return false;if(this.clashModeActive===true||(this.visualMassState&&this.visualMassState.active)||(this.shouldUseVisualStateChunks&&this.shouldUseVisualStateChunks())){this.refreshChunkRenderSystems&&this.refreshChunkRenderSystems(changedKeys,!!forceNormalRebuild);return true;}this.massTransparencyUseStableEdges=false;if((this.massTransparencyChunkVisible||(this.massTransparencyChunkHiddenKeys&&this.massTransparencyChunkHiddenKeys.size))&&this.clearMassTransparencyChunksBatched){const massClear=await this.clearMassTransparencyChunksBatched(false,{isCancelled,batchSize:opts.batchSize||2400,progressMessage:'Finalizando superficies transparentes...',onProgress:report});if(massClear===false&&isCancelled())return false;}else if(this.massTransparencyChunkVisible&&this.clearMassTransparencyChunks){this.clearMassTransparencyChunks(false);}if(isCancelled())return false;if((this.groupedTransparentEdgeBatches&&this.groupedTransparentEdgeBatches.length)&&this.clearGroupedTransparentEdgesBatched){const edgeClear=await this.clearGroupedTransparentEdgesBatched(false,{isCancelled,batchSize:opts.batchSize||2800,progressMessage:'Finalizando bordes transparentes...',onProgress:report});if(edgeClear===false&&isCancelled())return false;}this.visualStateUseStableEdges=false;this.restoreStableNormalChunkEdgeAppearance&&this.restoreStableNormalChunkEdgeAppearance();if((this.visualStateChunkVisible||(this.visualStateChunkHiddenKeys&&this.visualStateChunkHiddenKeys.size))&&this.clearVisualStateChunksBatched){const colorClear=await this.clearVisualStateChunksBatched(false,{isCancelled,batchSize:opts.batchSize||2200,progressMessage:'Finalizando bloques de color...',onProgress:report});if(colorClear===false&&isCancelled())return false;}else if(this.visualStateChunkVisible&&this.clearVisualStateChunks){this.clearVisualStateChunks(false);}if(isCancelled())return false;if(!this.shouldUseNormalChunks()){this.clearNormalChunks&&this.clearNormalChunks(false);return true;}if(!!forceNormalRebuild||!this.normalChunkVisible||!this.normalChunkMeshes||!this.normalChunkMeshes.length){const restored=this.restoreNormalChunksResponsive?await this.restoreNormalChunksResponsive({forceRebuild:!!forceNormalRebuild,isCancelled,batchSize:opts.batchSize||2800,signatureBatchSize:opts.signatureBatchSize||3400,progressMessage:'Recuperando bloques optimizados del modelo...',onProgress:report}):(this.rebuildNormalChunks?this.rebuildNormalChunks(!!forceNormalRebuild):0);return restored!==false;}return true;}runVisualTransactionResponsive(label,callback,options){const opts=options&&typeof options==='object'?options:{};const token=(this.responsiveVisualOperationSequence||0)+1;this.responsiveVisualOperationSequence=token;this.activeResponsiveVisualOperationToken=token;const previous=this.responsiveVisualTransactionPromise;const isCancelled=()=>token!==this.responsiveVisualOperationSequence;const execute=async()=>{if(previous){try{await previous;}catch(_){}}if(isCancelled())return false;const outer=!this.visualTransactionDepth;if(outer){this.visualTransactionChangedKeys=new Set();this.visualTransactionRefreshAllChunks=false;this.visualTransactionRenderReason=label||'responsive-visual-transaction';this.visualTransactionStartedMs=this.getPerformanceNow?this.getPerformanceNow():Date.now();}this.visualTransactionDepth=(this.visualTransactionDepth||0)+1;if(!this.visualTransactionLabelStack)this.visualTransactionLabelStack=[];this.visualTransactionLabelStack.push(label||'responsive-visual-transaction');let result;let caughtError=null;try{result=callback?await callback({token,isCancelled}):undefined;}catch(error){caughtError=error;}finally{this.visualTransactionLabelStack.pop();this.visualTransactionDepth=Math.max(0,(this.visualTransactionDepth||1)-1);if(this.visualTransactionDepth===0){const changed=new Set(this.visualTransactionChangedKeys||[]);const refreshAll=!!this.visualTransactionRefreshAllChunks;const reason=this.visualTransactionRenderReason||label||'responsive-visual-transaction';this.visualTransactionChangedKeys=new Set();this.visualTransactionRefreshAllChunks=false;this.visualTransactionRenderReason=null;if(!isCancelled()&&(refreshAll||changed.size)){this.flushVisualSectionRefresh&&this.flushVisualSectionRefresh();if(this.refreshChunkRenderSystemsResponsive){await this.refreshChunkRenderSystemsResponsive(refreshAll?null:changed,false,{isCancelled,batchSize:opts.batchSize||2600,signatureBatchSize:opts.signatureBatchSize||3400,onProgress:opts.onProgress});}else if(this.refreshChunkRenderSystems){this.refreshChunkRenderSystems(refreshAll?null:changed,false);}if(!isCancelled())this.requestRender&&this.requestRender(reason);}if(this.recordVisualUpdateMetric)this.recordVisualUpdateMetric('runVisualTransactionResponsive',(this.getPerformanceNow?this.getPerformanceNow():Date.now())-(this.visualTransactionStartedMs||0),{label:label||'',changedKeys:changed.size,refreshAll,cancelled:isCancelled()});}}if(caughtError)throw caughtError;return isCancelled()?false:result;};const promise=execute();this.responsiveVisualTransactionPromise=promise;promise.finally(()=>{if(this.responsiveVisualTransactionPromise===promise)this.responsiveVisualTransactionPromise=null;if(this.activeResponsiveVisualOperationToken===token)this.activeResponsiveVisualOperationToken=0;}).catch(()=>{});return promise;}runVisualTransaction(label,callback){const outer=!this.visualTransactionDepth;if(outer){this.visualTransactionChangedKeys=new Set();this.visualTransactionRefreshAllChunks=false;this.visualTransactionRenderReason=label||'visual-transaction';this.visualTransactionStartedMs=this.getPerformanceNow?this.getPerformanceNow():Date.now();}this.visualTransactionDepth=(this.visualTransactionDepth||0)+1;if(!this.visualTransactionLabelStack)this.visualTransactionLabelStack=[];this.visualTransactionLabelStack.push(label||'visual-transaction');let finalized=false;const finalize=()=>{if(finalized)return;finalized=true;this.visualTransactionLabelStack.pop();this.visualTransactionDepth=Math.max(0,(this.visualTransactionDepth||1)-1);if(this.visualTransactionDepth===0){const changedKeys=new Set(this.visualTransactionChangedKeys||[]);const refreshAll=!!this.visualTransactionRefreshAllChunks;const reason=this.visualTransactionRenderReason||label||'visual-transaction';this.visualTransactionChangedKeys=new Set();this.visualTransactionRefreshAllChunks=false;this.visualTransactionRenderReason=null;if(refreshAll||changedKeys.size){this.flushVisualSectionRefresh();if(refreshAll){this.refreshChunkRenderSystems&&this.refreshChunkRenderSystems(null,false);}else{this.refreshChunkRenderSystems&&this.refreshChunkRenderSystems(changedKeys,false);}this.requestRender&&this.requestRender(reason);}if(this.recordVisualUpdateMetric)this.recordVisualUpdateMetric('runVisualTransaction',(this.getPerformanceNow?this.getPerformanceNow():Date.now())-(this.visualTransactionStartedMs||0),{label:label||'',changedKeys:changedKeys.size,refreshAll});}};try{const result=callback?callback():undefined;if(result&&typeof result.then==='function'){return Promise.resolve(result).finally(finalize);}finalize();return result;}catch(error){finalize();throw error;}}clearDataPanelGraphicsForModeSwitch(options){const run=()=>this.clearDataPanelGraphicsForModeSwitchCore?this.clearDataPanelGraphicsForModeSwitchCore(options):false;return this.runVisualTransaction?this.runVisualTransaction('clear-data-panel-graphics-mode-switch',run):run();}clearDataPanelGraphicsForModeSwitchCore(options){const opts=options&&typeof options==='object'?options:{};const metricsStart=this.getPerformanceNow?this.getPerformanceNow():Date.now();const dataMassState=(this.getMassTransparencyLayer?this.getMassTransparencyLayer('data'):null)||(this.visualMassState&&this.visualMassState.active&&(!this.getMassTransparencyOwner||this.getMassTransparencyOwner(this.visualMassState)==='data')?this.visualMassState:null);const hadMassTransparency=!!dataMassState;const hadColorOverrides=!!(this.visualColorOverrides&&this.visualColorOverrides.size)||!!(this.colorOverrideStates&&this.colorOverrideStates.size);const hadDataFilter=!!this.dataPanelFilterActive||!!(this.dataPanelFilterKeys&&this.dataPanelFilterKeys.size);const aiVisualState=this.aiVisualState&&this.aiVisualState.active?this.aiVisualState:null;const hadAiBaseIsolation=!!(aiVisualState&&aiVisualState.isolationKeys&&aiVisualState.isolationKeys.size);const hadAiVisualState=!!aiVisualState;const hadPendingNormalRefresh=!!this.normalChunkRefreshDeferTimer;if(!hadMassTransparency&&!hadColorOverrides&&!hadDataFilter&&!hadAiVisualState&&!hadPendingNormalRefresh){if(this.recordPerformanceOperation)this.recordPerformanceOperation('dataGraphics:modeSwitchNoop',(this.getPerformanceNow?this.getPerformanceNow():Date.now())-metricsStart,{targetMode:opts.targetMode||''});return false;}const changedKeys=new Set();if(this.collectMassTransparencyKeysFromState)this.collectMassTransparencyKeysFromState(dataMassState,changedKeys);if(this.visualColorOverrides){for(const key of this.visualColorOverrides.keys())if(key)changedKeys.add(key);}if(this.colorOverrideStates&&this.colorOverrideStates.size){for(const[mesh]of this.colorOverrideStates.entries()){const key=this.getMeshKey?this.getMeshKey(mesh):null;if(key)changedKeys.add(key);}}if(aiVisualState){for(const key of aiVisualState.colorKeys||[])if(key)changedKeys.add(key);for(const key of aiVisualState.isolationKeys||[])if(key)changedKeys.add(key);for(const key of aiVisualState.transparencyKeys||[])if(key)changedKeys.add(key);}if(this.normalChunkRefreshDeferTimer){clearTimeout(this.normalChunkRefreshDeferTimer);this.normalChunkRefreshDeferTimer=null;}if(hadMassTransparency&&this.clearMassTransparencyHighlight){this.clearMassTransparencyHighlight({owner:'data',skipNormalChunkRefresh:true,skipVisualUpdate:true,disposeCache:false});}if(hadDataFilter&&this.clearVisualStateChunks)this.clearVisualStateChunks(false);if(hadAiBaseIsolation){this.baseIsolationKeys=new Set();this.baseIsolationActive=false;this.applyVisibilityState&&this.applyVisibilityState(null,{reason:'ai-graphics-mode-switch',forceChunkRebuild:false,skipChunkRefresh:true,skipSectionRefresh:true,skipVisualUpdate:true});}if(this.visualColorOverrides)this.visualColorOverrides.clear();if(this.colorOverrideStates)this.colorOverrideStates.clear();if(hadAiVisualState)this.aiVisualState=null;if(hadDataFilter){this.dataPanelFilterKeys=new Set();this.dataPanelFilterActive=false;if(this.invalidateSelectionCandidateCache)this.invalidateSelectionCandidateCache('data-panel-graphics-mode-switch');}const skipVisualUpdate=opts.skipVisualUpdate===true;const fullUpdate=opts.forceFullUpdate===true||((hadDataFilter||hadAiBaseIsolation)&&opts.skipFullVisualUpdate!==true);if(!skipVisualUpdate){if(fullUpdate&&this.updateVisualAppearanceForAll){this.updateVisualAppearanceForAll();}else if(changedKeys.size&&this.updateVisualAppearanceForKeys){this.updateVisualAppearanceForKeys(changedKeys,{skipChunkRefresh:opts.skipChunkRefresh!==false,skipSectionRefresh:true});}else{this.requestRender&&this.requestRender('data-graphics-mode-switch');}}if(opts.rebuildNormalChunks===true&&this.rebuildNormalChunks&&!(this.visualMassState&&this.visualMassState.active)){this.rebuildNormalChunks(false);}if(this.recordPerformanceOperation)this.recordPerformanceOperation('dataGraphics:modeSwitchClear',(this.getPerformanceNow?this.getPerformanceNow():Date.now())-metricsStart,{targetMode:opts.targetMode||'',hadMassTransparency,hadColorOverrides,hadDataFilter,hadAiVisualState,hadAiBaseIsolation,changedKeys:changedKeys.size,fullUpdate,skipVisualUpdate});return true;}applyMassTransparencyHighlight(activeElementKeys,highlightColor,fadedColor,transparency,options){return this.runVisualTransaction?this.runVisualTransaction('apply-mass-transparency',()=>this.applyMassTransparencyHighlightCore(activeElementKeys,highlightColor,fadedColor,transparency,options)):this.applyMassTransparencyHighlightCore(activeElementKeys,highlightColor,fadedColor,transparency,options);}applyMassTransparencyHighlightCore(activeElementKeys,highlightColor,fadedColor,transparency,options){const metricsStart=this.getPerformanceNow?this.getPerformanceNow():Date.now();const opts=options&&typeof options==='object'?options:{};const owner=String(opts.owner||opts.sourceOwner||'data').toLowerCase()==='viewer'?'viewer':'data';const previousLayer=this.getMassTransparencyLayer?this.getMassTransparencyLayer(owner):(this.visualMassState&&this.visualMassState.active?this.visualMassState:null);const activeKeys=new Set(activeElementKeys||[]);let allKeys=opts.allElementKeys!=null?new Set(opts.allElementKeys||[]):(this.getBaseVisibleElementKeySet?this.getBaseVisibleElementKeySet():new Set());if(previousLayer&&previousLayer.allKeys&&previousLayer.allKeys.size===allKeys.size){let sameBase=true;for(const key of allKeys){if(!previousLayer.allKeys.has(key)){sameBase=false;break;}}if(sameBase)allKeys=previousLayer.allKeys;}for(const key of Array.from(activeKeys)){if(!allKeys.has(key))activeKeys.delete(key);}if(!activeKeys.size&&opts.allowEmptyActive!==true){if(this.recordPerformanceOperation)this.recordPerformanceOperation('applyMassTransparencyHighlight:noop-empty',(this.getPerformanceNow?this.getPerformanceNow():Date.now())-metricsStart,{});return false;}const percent=transparency==null?this.getConfiguredTransparencyPercent(85):Number(transparency);const opacity=this.getConfiguredTransparencyOpacity(0.02,percent);const resolvedFadedColor=fadedColor==null?this.getConfiguredTransparencyColor():Number(fadedColor||0xbebebe);const resolvedHighlightColor=Number(highlightColor||0x5DA3F6);const resolvedHighlightOpacity=opts.highlightOpacity==null?0.7:Number(opts.highlightOpacity);const currentBaseVisibilityRevision=this.baseVisibilityRevision||0;const sameActiveKeys=!!(previousLayer&&previousLayer.activeKeys&&previousLayer.activeKeys.size===activeKeys.size&&Array.from(activeKeys).every(key=>previousLayer.activeKeys.has(key)));const sameAllKeys=!!(previousLayer&&previousLayer.allKeys&&previousLayer.allKeys.size===allKeys.size&&Array.from(allKeys).every(key=>previousLayer.allKeys.has(key)));const sameAppearance=!!(previousLayer&&Number(previousLayer.highlightColor)===resolvedHighlightColor&&Number(previousLayer.highlightOpacity)===resolvedHighlightOpacity&&Number(previousLayer.fadedColor)===resolvedFadedColor&&Number(previousLayer.fadedOpacity)===opacity&&previousLayer.preserveActiveAppearance===(opts.preserveActiveAppearance===true));const sameVisibilityRevision=!!(previousLayer&&Number(previousLayer.baseVisibilityRevision||0)===Number(currentBaseVisibilityRevision));const layerIsCurrentlyVisible=!!(this.visualMassState&&this.visualMassState.active&&(!this.getMassTransparencyOwner||this.getMassTransparencyOwner(this.visualMassState)===owner));const expectedMassChunks=this.shouldUseMassTransparencyChunks?this.shouldUseMassTransparencyChunks():true;const expectsVisibleEdges=!!(layerIsCurrentlyVisible&&this.getVisualDetailLevel&&this.getVisualDetailLevel()!=='low'&&this.generatedEdgeObjectCount>0);const edgeBuffersReady=!expectsVisibleEdges||(this.massTransparencyUseStableEdges===true?!!(this.normalChunkEdgeBatches&&this.normalChunkEdgeBatches.length):!!(this.groupedTransparentEdgeBatches&&this.groupedTransparentEdgeBatches.length));const visualBuffersReady=(!layerIsCurrentlyVisible||!expectedMassChunks||!!(this.massTransparencyChunkMeshes&&this.massTransparencyChunkMeshes.length))&&edgeBuffersReady;const hasClashState=!!((this.visualClashStates&&this.visualClashStates.size)||this.clashModeActive===true);const hasWorksState=!!((this.visualWorksStates&&this.visualWorksStates.size)||(this.worksProgressMaterialStates&&this.worksProgressMaterialStates.size)||(this.worksProgressVisibilityStates&&this.worksProgressVisibilityStates.size));if(sameActiveKeys&&sameAllKeys&&sameAppearance&&sameVisibilityRevision&&visualBuffersReady&&!hasClashState&&!hasWorksState){if(this.recordPerformanceOperation)this.recordPerformanceOperation('applyMassTransparencyHighlight:noop-unchanged',(this.getPerformanceNow?this.getPerformanceNow():Date.now())-metricsStart,{owner,activeKeys:activeKeys.size,allKeys:allKeys.size});return false;}this.clearClashHighlight();this.clearWorksProgressVisualization();const previousActiveState=this.visualMassState&&this.visualMassState.active?this.visualMassState:null;const previousActiveKeys=previousLayer&&previousLayer.activeKeys?new Set(previousLayer.activeKeys):new Set();const state={active:true,owner,sourceOwner:owner,mode:owner==='viewer'?'fade-unselected':'data-transparency',activeKeys,allKeys,allKeysFingerprint:previousLayer&&previousLayer.allKeys===allKeys&&previousLayer.allKeysFingerprint?previousLayer.allKeysFingerprint:(this.getKeySetFingerprint?this.getKeySetFingerprint(allKeys):String(allKeys.size)),baseVisibilityRevision:currentBaseVisibilityRevision,preserveActiveAppearance:opts.preserveActiveAppearance===true,highlightColor:resolvedHighlightColor,highlightOpacity:resolvedHighlightOpacity,fadedColor:resolvedFadedColor,fadedOpacity:opacity};if(this.setMassTransparencyLayer)this.setMassTransparencyLayer(owner,state);else this.visualMassState=state;const visibleLayerIsThisOwner=this.visualMassState===state||(this.getMassTransparencyOwner&&this.getMassTransparencyOwner(this.visualMassState)===owner);const changedKeys=new Set();for(const key of activeKeys)changedKeys.add(key);for(const key of previousActiveKeys)changedKeys.add(key);const previousAllKeys=previousLayer&&previousLayer.allKeys?previousLayer.allKeys:new Set();if(previousAllKeys!==allKeys){for(const key of allKeys)if(!previousAllKeys.has(key))changedKeys.add(key);for(const key of previousAllKeys)if(!allKeys.has(key))changedKeys.add(key);}if(this.visualColorOverrides&&this.visualColorOverrides.size){for(const key of this.visualColorOverrides.keys())changedKeys.add(key);}if(visibleLayerIsThisOwner){if(changedKeys.size)this.updateVisualAppearanceForKeys(changedKeys,{skipChunkRefresh:true,skipSectionRefresh:true});if(this.visualTransactionDepth&&this.visualTransactionDepth>0){this.visualTransactionRefreshAllChunks=true;this.visualTransactionRenderReason=this.visualTransactionRenderReason||'apply-mass-transparency';}else if(this.refreshChunkRenderSystems){this.refreshChunkRenderSystems(changedKeys,false);}}else if(this.requestRender){this.requestRender('apply-mass-transparency-layer-stored');}if(this.scheduleOptimizedVisualRenderStateValidation)this.scheduleOptimizedVisualRenderStateValidation('apply-mass-transparency');if(this.recordPerformanceOperation)this.recordPerformanceOperation('applyMassTransparencyHighlight',(this.getPerformanceNow?this.getPerformanceNow():Date.now())-metricsStart,{owner,activeKeys:activeKeys.size,allKeys:allKeys.size,fadedKeys:this.fadedMeshKeys?this.fadedMeshKeys.size:0,colorKeys:this.visualColorOverrides?this.visualColorOverrides.size:0,updatedKeys:visibleLayerIsThisOwner?changedKeys.size:0,storedBehindViewerLayer:!visibleLayerIsThisOwner,cachePrewarmed:!!this.transparencyCachePrewarmDone,opacity});if(this.recordModelPerformanceSnapshot)this.recordModelPerformanceSnapshot('applyMassTransparencyHighlight');return true;}createMassTransparencyMaterial(originalMaterial,color,opacity){return this.getSimpleColorMaterial(color,opacity,true);}clearMassTransparencyHighlight(options){return this.runVisualTransaction?this.runVisualTransaction('clear-mass-transparency',()=>this.clearMassTransparencyHighlightCore(options)):this.clearMassTransparencyHighlightCore(options);}clearMassTransparencyHighlightCore(options){const opts=options&&typeof options==='object'?options:{};const metricsStart=this.getPerformanceNow?this.getPerformanceNow():Date.now();const targetOwnerRaw=opts.owner||opts.targetOwner||opts.onlyOwner||'';const targetOwner=targetOwnerRaw?(String(targetOwnerRaw).toLowerCase()==='viewer'?'viewer':'data'):'';const activeBefore=this.visualMassState&&this.visualMassState.active?this.visualMassState:null;const activeBeforeOwner=this.getMassTransparencyOwner?this.getMassTransparencyOwner(activeBefore):'';const targetState=targetOwner?(this.getMassTransparencyLayer?this.getMassTransparencyLayer(targetOwner):(activeBeforeOwner===targetOwner?activeBefore:null)):activeBefore;const hadAnyLayer=!!((this.viewerMassTransparencyState&&this.viewerMassTransparencyState.active)||(this.dataMassTransparencyState&&this.dataMassTransparencyState.active)||activeBefore);const hadMassTransparency=hadAnyLayer||!!(this.massTransparencyMaterialStates&&this.massTransparencyMaterialStates.size)||!!(this.massTransparencyEdgeStates&&this.massTransparencyEdgeStates.size);if(targetOwner&&!targetState&&!(activeBefore&&activeBeforeOwner===targetOwner)){return false;}const disposeMassCaches=opts.disposeCache===true||opts.fullReset===true;const clearingActiveLayer=!targetOwner||!activeBefore||activeBeforeOwner===targetOwner;const changedKeys=new Set();if(this.collectMassTransparencyKeysFromState){if(targetOwner)this.collectMassTransparencyKeysFromState(targetState||activeBefore,changedKeys);else{this.collectMassTransparencyKeysFromState(this.viewerMassTransparencyState,changedKeys);this.collectMassTransparencyKeysFromState(this.dataMassTransparencyState,changedKeys);this.collectMassTransparencyKeysFromState(activeBefore,changedKeys);}}if(clearingActiveLayer&&opts.skipMergedChunkClear!==true){this.clearGroupedTransparentEdges&&this.clearGroupedTransparentEdges(disposeMassCaches);this.clearMassTransparencyChunks&&this.clearMassTransparencyChunks(disposeMassCaches);}if(targetOwner){if(this.clearMassTransparencyLayer)this.clearMassTransparencyLayer(targetOwner);else this.visualMassState=null;}else{this.viewerMassTransparencyState=null;this.dataMassTransparencyState=null;this.visualMassState=null;if(this.fadedMeshKeys)this.fadedMeshKeys.clear();this.fadeActive=false;}const activeAfter=this.visualMassState&&this.visualMassState.active?this.visualMassState:null;if(this.collectMassTransparencyKeysFromState)this.collectMassTransparencyKeysFromState(activeAfter,changedKeys);if(opts.changedKeysTarget&&typeof opts.changedKeysTarget.add==='function'){for(const key of changedKeys)opts.changedKeysTarget.add(key);}if(clearingActiveLayer){if(this.massTransparencyMaterialStates&&this.massTransparencyMaterialStates.size){for(const[mesh]of this.massTransparencyMaterialStates.entries()){const key=this.getMeshKey(mesh);if(key)changedKeys.add(key);}this.massTransparencyMaterialStates.clear();}if(this.massTransparencyEdgeStates&&this.massTransparencyEdgeStates.size){this.massTransparencyEdgeStates.clear();}}if(!opts.skipVisualUpdate)this.updateVisualAppearanceForKeys(changedKeys);if(!opts.skipNormalChunkRefresh&&!this.skipNormalChunkRefreshDuringMassApply){if(this.visualTransactionDepth&&this.visualTransactionDepth>0){this.visualTransactionRefreshAllChunks=true;this.visualTransactionRenderReason=this.visualTransactionRenderReason||'clear-mass-transparency';}else if(!(this.visualMassState&&this.visualMassState.active)){if(this.refreshChunkRenderSystems)this.refreshChunkRenderSystems(changedKeys,false);else this.rebuildNormalChunks&&this.rebuildNormalChunks(false);}}if(hadMassTransparency||changedKeys.size){if(this.recordPerformanceOperation)this.recordPerformanceOperation('clearMassTransparencyHighlight',(this.getPerformanceNow?this.getPerformanceNow():Date.now())-metricsStart,{owner:targetOwner||'all',changedKeys:changedKeys.size,restoredLayer:activeAfter?(this.getMassTransparencyOwner?this.getMassTransparencyOwner(activeAfter):''):''});if(this.recordModelPerformanceSnapshot)this.recordModelPerformanceSnapshot('clearMassTransparencyHighlight');}}async clearMassTransparencyHighlightResponsive(options){const opts=options&&typeof options==='object'?options:{};const targetOwnerRaw=opts.owner||opts.targetOwner||opts.onlyOwner||'';const targetOwner=targetOwnerRaw?(String(targetOwnerRaw).toLowerCase()==='viewer'?'viewer':'data'):'';const activeBefore=this.visualMassState&&this.visualMassState.active?this.visualMassState:null;const activeBeforeOwner=this.getMassTransparencyOwner?this.getMassTransparencyOwner(activeBefore):'';const clearingActiveLayer=!targetOwner||!activeBefore||activeBeforeOwner===targetOwner;const hadStableMassEdges=this.massTransparencyUseStableEdges===true;const disposeMassCaches=opts.disposeCache===true||opts.fullReset===true;const changedKeys=new Set();const isCancelled=typeof opts.isCancelled==='function'?opts.isCancelled:()=>false;if(isCancelled())return false;const result=this.clearMassTransparencyHighlightCore(Object.assign({},opts,{skipMergedChunkClear:true,skipNormalChunkRefresh:true,skipVisualUpdate:true,changedKeysTarget:changedKeys}));if(result===false&&!changedKeys.size)return false;if(isCancelled())return false;if(clearingActiveLayer){if(!hadStableMassEdges&&this.clearGroupedTransparentEdgesBatched){const edgeResult=await this.clearGroupedTransparentEdgesBatched(disposeMassCaches,{batchSize:opts.batchSize||2600,isCancelled,progressMessage:'Restaurando bordes transparentes...',onProgress:opts.onProgress});if(edgeResult===false&&isCancelled())return false;}else if(!hadStableMassEdges&&this.clearGroupedTransparentEdges){this.clearGroupedTransparentEdges(disposeMassCaches);}if(this.clearMassTransparencyChunksBatched){const chunkResult=await this.clearMassTransparencyChunksBatched(disposeMassCaches,{batchSize:opts.batchSize||2200,isCancelled,progressMessage:'Restaurando superficies transparentes...',onProgress:opts.onProgress});if(chunkResult===false&&isCancelled())return false;}else if(this.clearMassTransparencyChunks){this.clearMassTransparencyChunks(disposeMassCaches);}}if(isCancelled())return false;if(changedKeys.size&&this.updateVisualAppearanceForKeysBatched){const appearanceResult=await this.updateVisualAppearanceForKeysBatched(changedKeys,{skipChunkRefresh:true,skipSectionRefresh:true,batchSize:opts.appearanceBatchSize||2200,isCancelled,progressMessage:opts.progressMessage||'Restaurando apariencia original...',onProgress:opts.onProgress});if(appearanceResult&&appearanceResult.cancelled)return false;}if(this.visualTransactionDepth&&this.visualTransactionDepth>0){this.visualTransactionRefreshAllChunks=true;this.visualTransactionRenderReason=this.visualTransactionRenderReason||'clear-mass-transparency-responsive';}return true;}clearColorOverrides(options){return this.runVisualTransaction?this.runVisualTransaction('clear-color-overrides',()=>this.clearColorOverridesCore(options)):this.clearColorOverridesCore(options);}clearColorOverridesCore(options){const opts=options&&typeof options==='object'?options:{};const changedKeys=new Set(this.visualColorOverrides?this.visualColorOverrides.keys():[]);const hasStoredStates=!!(this.colorOverrideStates&&this.colorOverrideStates.size);if(!changedKeys.size&&!hasStoredStates)return false;if(this.visualColorOverrides)this.visualColorOverrides.clear();if(this.colorOverrideStates&&this.colorOverrideStates.size){for(const[mesh]of this.colorOverrideStates.entries()){const key=this.getMeshKey(mesh);if(key)changedKeys.add(key);}this.colorOverrideStates.clear();}if(changedKeys.size)this.visualColorOverridesRevision=(this.visualColorOverridesRevision||0)+1;if(opts.skipVisualUpdate!==true){this.updateVisualAppearanceForKeys(changedKeys,{skipChunkRefresh:opts.skipChunkRefresh===true,skipSectionRefresh:opts.skipSectionRefresh===true});}return true;}async clearColorOverridesResponsive(options){const opts=options&&typeof options==='object'?options:{};const changedKeys=new Set(this.visualColorOverrides?this.visualColorOverrides.keys():[]);const hasStoredStates=!!(this.colorOverrideStates&&this.colorOverrideStates.size);if(!changedKeys.size&&!hasStoredStates)return false;const isCancelled=typeof opts.isCancelled==='function'?opts.isCancelled:()=>false;if(this.colorOverrideStates&&this.colorOverrideStates.size){for(const[mesh]of this.colorOverrideStates.entries()){const key=this.getMeshKey?this.getMeshKey(mesh):null;if(key)changedKeys.add(key);}}if(this.visualColorOverrides)this.visualColorOverrides.clear();if(this.colorOverrideStates)this.colorOverrideStates.clear();if(changedKeys.size)this.visualColorOverridesRevision=(this.visualColorOverridesRevision||0)+1;const appearanceAlreadyUpdated=new Set();if(this.visualStateChunkVisible||(this.visualStateChunkHiddenKeys&&this.visualStateChunkHiddenKeys.size)){if(this.clearVisualStateChunksBatched){const cleared=await this.clearVisualStateChunksBatched(false,{batchSize:opts.batchSize||2200,isCancelled,progressMessage:'Retirando bloques de color...',onProgress:opts.onProgress,updatedKeysTarget:appearanceAlreadyUpdated});if(cleared===false&&isCancelled())return false;}else if(this.clearVisualStateChunks){this.clearVisualStateChunks(false);}}if(isCancelled())return false;const remainingAppearanceKeys=appearanceAlreadyUpdated.size?new Set(Array.from(changedKeys).filter(key=>!appearanceAlreadyUpdated.has(key))):changedKeys;if(remainingAppearanceKeys.size&&this.updateVisualAppearanceForKeysBatched){const appearanceResult=await this.updateVisualAppearanceForKeysBatched(remainingAppearanceKeys,{skipChunkRefresh:true,skipSectionRefresh:true,batchSize:opts.appearanceBatchSize||2200,isCancelled,progressMessage:opts.progressMessage||'Restaurando elementos sin color...',onProgress:opts.onProgress});if(appearanceResult&&appearanceResult.cancelled)return false;}if(this.visualTransactionDepth&&this.visualTransactionDepth>0){this.visualTransactionRefreshAllChunks=true;this.visualTransactionRenderReason=this.visualTransactionRenderReason||'clear-color-overrides-responsive';}return true;}async applyColorOverridesResponsive(colorByElementKey,options){const opts=options&&typeof options==='object'?options:{};if(!colorByElementKey||!colorByElementKey.size)return this.clearColorOverridesResponsive(opts);const isCancelled=typeof opts.isCancelled==='function'?opts.isCancelled:()=>false;const previous=this.visualColorOverrides||new Map();const next=new Map();for(const[key,color]of colorByElementKey.entries()){if(key)next.set(key,{color:Number(color||0)});}const changedKeys=new Set();for(const[key,value]of previous.entries()){const nextValue=next.get(key);if(!nextValue||Number(nextValue.color)!==Number(value&&value.color))changedKeys.add(key);}for(const[key,value]of next.entries()){const oldValue=previous.get(key);if(!oldValue||Number(oldValue.color)!==Number(value&&value.color))changedKeys.add(key);}if(!changedKeys.size)return false;this.clearClashHighlight&&this.clearClashHighlight();this.clearWorksProgressVisualization&&this.clearWorksProgressVisualization();this.visualColorOverrides=next;this.visualColorOverridesRevision=(this.visualColorOverridesRevision||0)+1;if(this.updateVisualAppearanceForKeysBatched){const appearanceResult=await this.updateVisualAppearanceForKeysBatched(changedKeys,{skipChunkRefresh:true,skipSectionRefresh:true,batchSize:opts.appearanceBatchSize||2200,isCancelled,progressMessage:opts.progressMessage||'Actualizando colores...',onProgress:opts.onProgress});if(appearanceResult&&appearanceResult.cancelled)return false;}if(this.visualTransactionDepth&&this.visualTransactionDepth>0){this.visualTransactionRefreshAllChunks=true;this.visualTransactionRenderReason=this.visualTransactionRenderReason||'apply-color-overrides-responsive';}return true;}applyWorksProgressVisualization(colorByElementKey,focusedElementKeys,fadedOpacity,options){this.clearClashHighlight();this.clearMassTransparencyHighlight();this.clearColorOverrides();this.clearWorksProgressVisualization();if(!colorByElementKey||!colorByElementKey.size)return false;const focusedKeys=new Set(focusedElementKeys||[]);const focusMode=focusedKeys.size>0;const faded=Math.max(0.02,Math.min(1,Number(fadedOpacity==null?0.10:fadedOpacity)));const opts=options||{};const opacityByElementKey=opts.opacityByElementKey instanceof Map?opts.opacityByElementKey:new Map();const hiddenElementKeys=opts.hiddenElementKeys instanceof Set?opts.hiddenElementKeys:new Set();const unmappedFadedColor=Number.isFinite(Number(opts.unmappedFadedColor))?Number(opts.unmappedFadedColor):0xbec7d3;this.worksSelectionAppearanceLock=false;const changedKeys=new Set();const targetMeshes=focusMode?(this.meshes||[]):[];if(!focusMode){for(const key of colorByElementKey.keys()){const mesh=this.meshByElementKey.get(key);if(mesh)targetMeshes.push(mesh);}for(const key of opacityByElementKey.keys()){const mesh=this.meshByElementKey.get(key);if(mesh&&!targetMeshes.includes(mesh))targetMeshes.push(mesh);}}for(const key of hiddenElementKeys){const mesh=this.meshByElementKey.get(key);if(mesh&&!targetMeshes.includes(mesh))targetMeshes.push(mesh);}this.worksProgressFadedMeshKeys.clear();for(const mesh of targetMeshes){const key=this.getMeshKey(mesh);if(!key)continue;changedKeys.add(key);if(hiddenElementKeys.has(key)){this.visualWorksStates.set(key,{hidden:true});continue;}const mappedColor=colorByElementKey.has(key)?colorByElementKey.get(key):null;const isFocused=focusedKeys.has(key);const hasExplicitOpacity=opacityByElementKey.has(key);let opacity=hasExplicitOpacity?Number(opacityByElementKey.get(key)):1;if(!Number.isFinite(opacity))opacity=1;opacity=Math.max(0.02,Math.min(1,opacity));if(focusMode){opacity=isFocused?(hasExplicitOpacity?opacity:1):faded;if(!isFocused){if(this.fadedMeshKeys)this.fadedMeshKeys.add(key);if(this.worksProgressFadedMeshKeys)this.worksProgressFadedMeshKeys.add(key);this.fadeActive=true;}}const color=mappedColor==null?(focusMode?unmappedFadedColor:this.getPrimaryMeshColorValue(mesh,0xbec7d3)):Number(mappedColor||0);const edgeOpacity=opacity<1?Math.max(0.04,opacity):1;this.visualWorksStates.set(key,{color,opacity,edgeOpacity,hidden:false});}this.updateVisualAppearanceForKeys(changedKeys);if(this.invalidateSelectionCandidateCache)this.invalidateSelectionCandidateCache('works-progress-visualization');else if(this.invalidateRaycastScreenCandidateCache)this.invalidateRaycastScreenCandidateCache('works-progress-visualization');return true;}getPrimaryMeshColorValue(mesh,fallbackColor){const material=this.getPrimaryMeshMaterial(mesh);if(material&&material.color&&material.color.isColor)return material.color.getHex();return fallbackColor==null?0xbec7d3:fallbackColor;}setWorksSelectionAppearanceLock(enabled){this.worksSelectionAppearanceLock=enabled===true;}clearWorksProgressVisualization(options){const opts=options&&typeof options==='object'?options:{};this.worksSelectionAppearanceLock=false;if(!this.visualWorksStates)this.visualWorksStates=new Map();if(!this.worksProgressMaterialStates)this.worksProgressMaterialStates=new Map();if(!this.worksProgressEdgeStates)this.worksProgressEdgeStates=new Map();if(!this.worksProgressVisibilityStates)this.worksProgressVisibilityStates=new Map();if(!this.worksProgressFadedMeshKeys)this.worksProgressFadedMeshKeys=new Set();if(!this.fadedMeshKeys)this.fadedMeshKeys=new Set();const hasWorksState=!!(this.visualWorksStates.size||this.worksProgressMaterialStates.size||this.worksProgressEdgeStates.size||this.worksProgressVisibilityStates.size||this.worksProgressFadedMeshKeys.size);if(!hasWorksState)return false;const changedKeys=new Set(this.visualWorksStates.keys());this.visualWorksStates.clear();if(this.worksProgressMaterialStates&&this.worksProgressMaterialStates.size){for(const[mesh]of this.worksProgressMaterialStates.entries()){const key=this.getMeshKey(mesh);if(key)changedKeys.add(key);}this.worksProgressMaterialStates.clear();}if(this.worksProgressEdgeStates&&this.worksProgressEdgeStates.size){this.worksProgressEdgeStates.clear();}if(this.worksProgressVisibilityStates&&this.worksProgressVisibilityStates.size){for(const[mesh]of this.worksProgressVisibilityStates.entries()){const key=this.getMeshKey(mesh);if(key)changedKeys.add(key);}this.worksProgressVisibilityStates.clear();}if(this.worksProgressFadedMeshKeys&&this.worksProgressFadedMeshKeys.size){for(const key of this.worksProgressFadedMeshKeys){if(this.fadedMeshKeys)this.fadedMeshKeys.delete(key);changedKeys.add(key);}this.worksProgressFadedMeshKeys.clear();if(this.fadedMeshKeys&&!this.fadedMeshKeys.size)this.fadeActive=false;}if(opts.skipVisualUpdate!==true){if(this.visibilityPredicate||this.baseIsolationActive||this.isolateActive||(this.hiddenKeys&&this.hiddenKeys.size)){if(opts.skipVisibilityUpdate!==true)this.applyVisibilityState(null,{skipChunkRefresh:opts.skipChunkRefresh===true,skipSectionRefresh:true});}else{this.updateVisualAppearanceForKeys(changedKeys,{skipChunkRefresh:opts.skipChunkRefresh===true,skipSectionRefresh:true});}}if(this.invalidateSelectionCandidateCache)this.invalidateSelectionCandidateCache('works-progress-clear');else if(this.invalidateRaycastScreenCandidateCache)this.invalidateRaycastScreenCandidateCache('works-progress-clear');return true;}isolateSelected(){if(!this.selectedKeys||!this.selectedKeys.size)return false;this.isolateActive=true;this.isolatedKeys=new Set(this.selectedKeys);if(this.suspendMergedRenderSystemsForIsolation){this.suspendMergedRenderSystemsForIsolation('viewer-isolate-selected');}const isolatedSourceMeshes=this.getMeshesForElementKeys?this.getMeshesForElementKeys(this.isolatedKeys):(this.selectedMeshes||[]);if(this.ensureInteractiveMeshesVisibleForKeys)this.ensureInteractiveMeshesVisibleForKeys(this.isolatedKeys);this.applyVisibilityState(isolatedSourceMeshes,{reason:'viewer-isolate-selected',forceChunkRebuild:false,skipChunkVisibilitySync:true,skipChunkRefresh:true});if(this.activateTransientCompactFallbackEdgesForKeys){this.activateTransientCompactFallbackEdgesForKeys(this.isolatedKeys,'viewer-isolation');}if(this.beginStrictIsolationVisibilityGuard)this.beginStrictIsolationVisibilityGuard('viewer-isolate-selected');if(this.invalidateRaycastScreenCandidateCache)this.invalidateRaycastScreenCandidateCache('viewer-isolate-selected');if(this.invalidateMeasurementPickCache)this.invalidateMeasurementPickCache('viewer-isolate-selected');if(this.requestRender)this.requestRender('viewer-isolate-selected');this.fitToSelectedOrModel();this.syncViewerToolButtons();return true;}toggleElevationMode(forceState){const nextState=typeof forceState==='boolean'?forceState:!this.elevationActive;if(nextState===this.elevationActive)return this.elevationActive;if(nextState&&this.measureActive){this.toggleMeasureMode(false);}this.elevationActive=nextState;this.elevationPlacementActive=nextState;this.measureSnapCycleIndex=0;this.measureLastMouse=null;this.restoreHoveredMaterial();this.restoreMeasureReferenceHighlight();this.clearSelection(false);if(this.elevationActive){const currentMarker=this.getActiveElevationMarker(false);if(!currentMarker||currentMarker.point){this.addElevationMarker();}}this.canvas.style.cursor=this.elevationPlacementActive?'crosshair':(this.measureActive?'crosshair':'default');return this.elevationActive;}scheduleMeasurementPreviewFromScreen(x,y,mode){this.measurePreviewPending={x,y,mode:mode==='elevation'?'elevation':'measure'};if(this.measurePreviewTimer)return true;const now=this.getPerformanceNow?this.getPerformanceNow():Date.now();const delay=Math.max(0,this.measureRaycastThrottleMs-(now-(this.measurePreviewLastRun||0)));this.measurePreviewTimer=setTimeout(()=>{this.measurePreviewTimer=null;const pending=this.measurePreviewPending;this.measurePreviewPending=null;if(!pending)return;this.measurePreviewLastRun=this.getPerformanceNow?this.getPerformanceNow():Date.now();if(pending.mode==='elevation')this.updateElevationPreviewFromScreen(pending.x,pending.y);else this.updateMeasurePreviewFromScreen(pending.x,pending.y);},delay);return true;}cancelScheduledMeasurementPreview(){if(this.measurePreviewTimer)clearTimeout(this.measurePreviewTimer);this.measurePreviewTimer=null;this.measurePreviewPending=null;}updateElevationPreviewFromScreen(x,y,preserveCycle){if(!this.elevationActive||!this.elevationPlacementActive)return;if(!preserveCycle&&(!this.measureLastMouse||Math.abs(this.measureLastMouse.x-x)>2||Math.abs(this.measureLastMouse.y-y)>2)){this.measureSnapCycleIndex=0;}this.measureLastMouse={x,y};const snap=this.pickMeasurePointFromScreen(x,y);this.setMeasureReferenceHighlight(snap);}commitElevationFromScreen(x,y){if(!this.elevationActive||!this.elevationPlacementActive)return;const snap=this.pickMeasurePointFromScreen(x,y);if(!snap||!snap.point)return;const marker=this.getActiveElevationMarker(true);if(!marker)return;marker.point=snap.point.clone();marker.snap=snap;this.elevationPlacementActive=false;this.restoreMeasureReferenceHighlight();this.updateElevationOverlay();this.canvas.style.cursor=this.measureActive?'crosshair':'default';}getActiveElevationMarker(createIfMissing){if(!Array.isArray(this.elevationMarkers))this.elevationMarkers=[];if(this.activeElevationIndex<0||this.activeElevationIndex>=this.elevationMarkers.length){this.activeElevationIndex=this.elevationMarkers.length?this.elevationMarkers.length-1:-1;}if(this.activeElevationIndex<0&&createIfMissing){this.addElevationMarker();}return this.activeElevationIndex>=0?this.elevationMarkers[this.activeElevationIndex]:null;}addElevationMarker(){if(!Array.isArray(this.elevationMarkers))this.elevationMarkers=[];const current=this.getActiveElevationMarker(false);if(current&&!current.point)return this.activeElevationIndex;const marker={point:null,snap:null,overlay:this.createElevationOverlay()};this.elevationMarkers.push(marker);this.activeElevationIndex=this.elevationMarkers.length-1;this.elevationActive=true;this.elevationPlacementActive=true;this.measureSnapCycleIndex=0;this.measureLastMouse=null;this.updateElevationOverlay();this.canvas.style.cursor='crosshair';return this.activeElevationIndex;}removeLastElevationMarker(){if(!Array.isArray(this.elevationMarkers)||!this.elevationMarkers.length)return 0;const marker=this.elevationMarkers.pop();if(marker&&marker.overlay&&marker.overlay.root&&marker.overlay.root.parentElement){marker.overlay.root.parentElement.removeChild(marker.overlay.root);}this.elevationDragIndex=null;this.activeElevationIndex=this.elevationMarkers.length-1;if(!this.elevationMarkers.length){this.elevationActive=false;this.elevationPlacementActive=false;this.restoreMeasureReferenceHighlight();}this.updateElevationOverlay();return this.elevationMarkers.length;}clearElevationMarker(){if(Array.isArray(this.elevationMarkers)){for(const marker of this.elevationMarkers){if(marker&&marker.overlay&&marker.overlay.root&&marker.overlay.root.parentElement){marker.overlay.root.parentElement.removeChild(marker.overlay.root);}}}this.elevationMarkers=[];this.activeElevationIndex=-1;this.elevationDragIndex=null;this.elevationActive=false;this.elevationPlacementActive=false;}getElevationMarkerHitFromScreen(x,y){if(!Array.isArray(this.elevationMarkers)||!this.elevationMarkers.length)return null;let best=null;let bestDistance=Infinity;for(let elevationIndex=0;elevationIndex<this.elevationMarkers.length;elevationIndex++){const marker=this.elevationMarkers[elevationIndex];const point=marker&&marker.point;if(!point)continue;const distance=this.worldToScreenDistance(point,x,y);if(distance<bestDistance){bestDistance=distance;best={elevationIndex};}}return bestDistance<=28?best:null;}dragElevationMarkerFromScreen(x,y,preserveCycle){const index=this.elevationDragIndex!=null&&this.elevationDragIndex>=0?this.elevationDragIndex:this.activeElevationIndex;if(!Array.isArray(this.elevationMarkers)||index<0||index>=this.elevationMarkers.length)return;if(!preserveCycle&&(!this.measureLastMouse||Math.abs(this.measureLastMouse.x-x)>2||Math.abs(this.measureLastMouse.y-y)>2)){this.measureSnapCycleIndex=0;}this.measureLastMouse={x,y};const snap=this.pickMeasurePointFromScreen(x,y);if(!snap||!snap.point)return;const marker=this.elevationMarkers[index];if(!marker)return;marker.point=snap.point.clone();marker.snap=snap;this.activeElevationIndex=index;this.setMeasureReferenceHighlight(snap);this.updateElevationOverlay();}updateElevationOverlay(){if(!Array.isArray(this.elevationMarkers))return;for(const marker of this.elevationMarkers){const overlay=marker&&marker.overlay;const point=marker&&marker.point;if(!overlay||!overlay.root)continue;if(!point){overlay.root.style.display='none';overlay.root.classList.add('hidden');continue;}const projected=point.clone().project(this.camera);const width=Math.max(1,this.renderer.domElement.clientWidth);const height=Math.max(1,this.renderer.domElement.clientHeight);const sx=(projected.x*0.5+0.5)*width;const sy=(-projected.y*0.5+0.5)*height;overlay.root.style.left=sx+'px';overlay.root.style.top=sy+'px';overlay.root.style.display='grid';overlay.root.classList.remove('hidden');overlay.value.textContent=this.formatElevation(point.y);}}formatElevation(value){const number=Number(value)||0;const abs=Math.abs(number);let decimals=3;if(abs>=10)decimals=2;if(abs>=100)decimals=1;return'Z '+number.toFixed(decimals)+' m';}toggleMeasureMode(forceState){const nextState=typeof forceState==='boolean'?forceState:!this.measureActive;if(nextState===this.measureActive)return this.measureActive;this.measureActive=nextState;this.restoreHoveredMaterial();this.restoreMeasureReferenceHighlight();this.clearMeasurePreview();this.clearSelection(false);if(this.measureActive&&!this.measurements.length){this.addMeasurement();}else{this.syncActiveMeasurement();this.updateMeasurementOverlay();}this.canvas.style.cursor=(this.measureActive||this.elevationPlacementActive)?'crosshair':'default';return this.measureActive;}addMeasurement(){if(!Array.isArray(this.measurements))this.measurements=[];this.measurements.push({points:[],snaps:[],frame:null});this.activeMeasurementIndex=this.measurements.length-1;this.syncActiveMeasurement();this.measurePreview=null;this.measureDragIndex=null;this.measureDragMeasurementIndex=null;this.rebuildMeasurementGraphics();this.updateMeasurementOverlay();return this.activeMeasurementIndex;}removeLastMeasurement(){if(!Array.isArray(this.measurements)||!this.measurements.length)return 0;this.restoreMeasureReferenceHighlight();this.clearMeasurePreview();this.measurements.pop();if(!this.measurements.length){this.activeMeasurementIndex=-1;}else{this.activeMeasurementIndex=Math.min(this.activeMeasurementIndex,this.measurements.length-1);if(this.activeMeasurementIndex<0)this.activeMeasurementIndex=this.measurements.length-1;}this.syncActiveMeasurement();this.measureDragIndex=null;this.measureDragMeasurementIndex=null;this.rebuildMeasurementGraphics();this.updateMeasurementOverlay();return this.measurements.length;}syncActiveMeasurement(){if(!Array.isArray(this.measurements))this.measurements=[];if(!this.measurements.length){this.activeMeasurementIndex=-1;this.measurePoints=[];return null;}if(this.activeMeasurementIndex<0||this.activeMeasurementIndex>=this.measurements.length){this.activeMeasurementIndex=this.measurements.length-1;}const measurement=this.measurements[this.activeMeasurementIndex];if(!Array.isArray(measurement.points))measurement.points=[];if(!Array.isArray(measurement.snaps))measurement.snaps=[];if(!measurement.frame&&measurement.snaps[0])measurement.frame=this.createMeasurementCoordinateFrame(measurement.snaps[0],measurement.points[0]);this.measurePoints=measurement.points;return measurement;}getActiveMeasurement(){return this.syncActiveMeasurement();}isMeasurePlacementActive(){if(!this.measureActive)return false;const measurement=this.getActiveMeasurement();return!!(measurement&&measurement.points.length<2);}clearMeasurement(){this.restoreMeasureReferenceHighlight();this.measureDragIndex=null;this.measureDragMeasurementIndex=null;this.measurements=[];this.activeMeasurementIndex=-1;this.measurePoints=[];this.measurePreview=null;this.measurePickCache=null;this.measureSurfaceCandidateCache=null;if(this.measureGroup){this.measureScene.remove(this.measureGroup);this.measureGroup.traverse(child=>{if(child.geometry&&child.geometry.dispose)child.geometry.dispose();if(child.material){if(Array.isArray(child.material))child.material.forEach(material=>material&&material.dispose&&material.dispose());else if(child.material.dispose)child.material.dispose();}});}this.measureGroup=null;this.measureLine=null;this.measureMarkers=[];this.measurementOverlayLastSignature='';this.measurementOverlayLastViewRevision=-1;this.updateMeasurementOverlay(true);}restoreMeasureReferenceHighlight(){if(this.measureReferenceGroup){this.measureScene.remove(this.measureReferenceGroup);this.measureReferenceGroup.traverse(child=>{if(child.geometry&&child.geometry.dispose)child.geometry.dispose();if(child.material){if(Array.isArray(child.material))child.material.forEach(material=>material&&material.dispose&&material.dispose());else if(child.material.dispose)child.material.dispose();}});}this.measureReferenceGroup=null;this.measureReferenceSignature='';for(const state of this.measureHighlightStates||[]){if(!state||!state.object)continue;state.object.material=state.material;}this.measureHighlightTarget=null;this.measureHighlightStates=[];}getMeasureReferenceSignature(snap){if(!snap||!snap.point)return'';const objectId=snap.object?(snap.object.uuid||snap.object.id||''):'';if(snap.kind==='face'){if(snap.faceIndex!=null)return['face',objectId,snap.faceIndex].join('|');const points=Array.isArray(snap.facePoints)?snap.facePoints:[];return['face',objectId].concat(points.map(point=>point?[point.x,point.y,point.z].map(value=>Number(value).toFixed(5)).join(','):'')).join('|');}if(snap.kind==='edge'&&snap.edgeStart&&snap.edgeEnd){return['edge',objectId,snap.edgeStart.x.toFixed(5),snap.edgeStart.y.toFixed(5),snap.edgeStart.z.toFixed(5),snap.edgeEnd.x.toFixed(5),snap.edgeEnd.y.toFixed(5),snap.edgeEnd.z.toFixed(5)].join('|');}return['vertex',objectId,snap.point.x.toFixed(5),snap.point.y.toFixed(5),snap.point.z.toFixed(5)].join('|');}setMeasureReferenceHighlight(snap){if(!snap||!snap.point){this.restoreMeasureReferenceHighlight();return;}const signature=this.getMeasureReferenceSignature(snap);if(signature&&signature===this.measureReferenceSignature&&this.measureReferenceGroup){const marker=this.measureReferenceGroup.children.find(child=>child&&child.userData&&child.userData.measureSnapMarker);if(marker)marker.position.copy(snap.point);this.requestRender&&this.requestRender('measurement-highlight-reuse');return;}this.restoreMeasureReferenceHighlight();const group=new THREE.Group();group.renderOrder=120;const fillMaterial=new THREE.MeshBasicMaterial({color:0x90ee90,transparent:true,opacity:0.30,depthTest:false,depthWrite:false,side:THREE.DoubleSide});const lineMaterial=new THREE.LineBasicMaterial({color:0x90ee90,transparent:true,opacity:0.92,depthTest:false,depthWrite:false});const addSnapMarker=(opacity)=>{const radius=Math.max(this.getWorldPerPixelAtPoint(snap.point)*5,0.005);const geometry=new THREE.SphereGeometry(radius,16,16);const material=fillMaterial.clone();material.opacity=opacity==null?0.82:opacity;const marker=new THREE.Mesh(geometry,material);marker.position.copy(snap.point);marker.userData=Object.assign({},marker.userData||{},{measureSnapMarker:true});marker.renderOrder=123;group.add(marker);};if(snap.kind==='face'&&Array.isArray(snap.facePoints)&&snap.facePoints.length>=3){const geometry=new THREE.BufferGeometry();const positions=new Float32Array([snap.facePoints[0].x,snap.facePoints[0].y,snap.facePoints[0].z,snap.facePoints[1].x,snap.facePoints[1].y,snap.facePoints[1].z,snap.facePoints[2].x,snap.facePoints[2].y,snap.facePoints[2].z]);geometry.setAttribute('position',new THREE.BufferAttribute(positions,3));geometry.setIndex([0,1,2]);const mesh=new THREE.Mesh(geometry,fillMaterial);mesh.renderOrder=121;group.add(mesh);const edgeGeometry=new THREE.BufferGeometry();edgeGeometry.setAttribute('position',new THREE.BufferAttribute(new Float32Array([snap.facePoints[0].x,snap.facePoints[0].y,snap.facePoints[0].z,snap.facePoints[1].x,snap.facePoints[1].y,snap.facePoints[1].z,snap.facePoints[1].x,snap.facePoints[1].y,snap.facePoints[1].z,snap.facePoints[2].x,snap.facePoints[2].y,snap.facePoints[2].z,snap.facePoints[2].x,snap.facePoints[2].y,snap.facePoints[2].z,snap.facePoints[0].x,snap.facePoints[0].y,snap.facePoints[0].z]),3));const edges=new THREE.LineSegments(edgeGeometry,lineMaterial);edges.renderOrder=122;group.add(edges);addSnapMarker(0.78);}else if(snap.kind==='edge'&&snap.edgeStart&&snap.edgeEnd){const geometry=new THREE.BufferGeometry();geometry.setAttribute('position',new THREE.BufferAttribute(new Float32Array([snap.edgeStart.x,snap.edgeStart.y,snap.edgeStart.z,snap.edgeEnd.x,snap.edgeEnd.y,snap.edgeEnd.z]),3));const edge=new THREE.LineSegments(geometry,lineMaterial);edge.renderOrder=122;group.add(edge);addSnapMarker(0.88);}else{lineMaterial.dispose();const radius=Math.max(this.getWorldPerPixelAtPoint(snap.point)*5,0.005);const geometry=new THREE.SphereGeometry(radius,16,16);const marker=new THREE.Mesh(geometry,fillMaterial);marker.position.copy(snap.point);marker.userData=Object.assign({},marker.userData||{},{measureSnapMarker:true});marker.renderOrder=122;group.add(marker);}this.measureReferenceGroup=group;this.measureReferenceSignature=signature;this.measureScene.add(group);}clearMeasurePreview(){this.measurePreview=null;this.updateMeasurementOverlay();}updateMeasurePreviewFromScreen(x,y,preserveCycle){if(!this.measureActive||!this.isMeasurePlacementActive())return;if(!preserveCycle&&(!this.measureLastMouse||Math.abs(this.measureLastMouse.x-x)>2||Math.abs(this.measureLastMouse.y-y)>2)){this.measureSnapCycleIndex=0;}this.measureLastMouse={x,y};const snap=this.pickMeasurePointFromScreen(x,y);this.measurePreview=snap?snap.point.clone():null;this.setMeasureReferenceHighlight(snap);this.updateMeasurementOverlay();}commitMeasurePointFromScreen(x,y){if(!this.isMeasurePlacementActive())return;const snap=this.pickMeasurePointFromScreen(x,y);if(!snap||!snap.point)return;const measurement=this.getActiveMeasurement();if(!measurement)return;measurement.points.push(snap.point.clone());if(!Array.isArray(measurement.snaps))measurement.snaps=[];measurement.snaps.push(this.cloneMeasurementSnap(snap));if(measurement.points.length>2)measurement.points=measurement.points.slice(0,2);if(measurement.snaps.length>2)measurement.snaps=measurement.snaps.slice(0,2);if(measurement.points.length===1)measurement.frame=this.createMeasurementCoordinateFrame(snap,measurement.points[0]);this.syncActiveMeasurement();this.measurePreview=null;this.setMeasureReferenceHighlight(snap);this.rebuildMeasurementGraphics();this.updateMeasurementOverlay();}getMeasurePointHitFromScreen(x,y){if(!Array.isArray(this.measurements)||!this.measurements.length)return null;let best=null;let bestDistance=Infinity;for(let measurementIndex=0;measurementIndex<this.measurements.length;measurementIndex++){const measurement=this.measurements[measurementIndex];const points=measurement&&Array.isArray(measurement.points)?measurement.points:[];for(let pointIndex=0;pointIndex<points.length;pointIndex++){const distance=this.worldToScreenDistance(points[pointIndex],x,y);if(distance<bestDistance){bestDistance=distance;best={measurementIndex,pointIndex};}}}return bestDistance<=22?best:null;}getMeasurePointIndexFromScreen(x,y){const hit=this.getMeasurePointHitFromScreen(x,y);return hit?hit.pointIndex:-1;}dragMeasurePointFromScreen(x,y){if(this.measureDragIndex==null||this.measureDragIndex<0)return;if(this.measureDragMeasurementIndex==null||this.measureDragMeasurementIndex<0)return;if(!this.measureLastMouse||Math.abs(this.measureLastMouse.x-x)>2||Math.abs(this.measureLastMouse.y-y)>2){this.measureSnapCycleIndex=0;}this.measureLastMouse={x,y};const snap=this.pickMeasurePointFromScreen(x,y);if(!snap||!snap.point)return;const measurement=this.measurements[this.measureDragMeasurementIndex];if(!measurement||!Array.isArray(measurement.points))return;measurement.points[this.measureDragIndex]=snap.point.clone();if(!Array.isArray(measurement.snaps))measurement.snaps=[];measurement.snaps[this.measureDragIndex]=this.cloneMeasurementSnap(snap);if(this.measureDragIndex===0)measurement.frame=this.createMeasurementCoordinateFrame(snap,measurement.points[0]);this.activeMeasurementIndex=this.measureDragMeasurementIndex;this.syncActiveMeasurement();this.measurePreview=null;this.setMeasureReferenceHighlight(snap);this.rebuildMeasurementGraphics();this.updateMeasurementOverlay();}cloneMeasurementSnap(snap){if(!snap)return null;return{point:snap.point&&snap.point.clone?snap.point.clone():null,kind:snap.kind||'face',object:snap.object||null,facePoints:Array.isArray(snap.facePoints)?snap.facePoints.map(point=>point&&point.clone?point.clone():point):null,faceNormal:snap.faceNormal&&snap.faceNormal.clone?snap.faceNormal.clone():null,faceIndex:snap.faceIndex==null?null:Number(snap.faceIndex),edgeStart:snap.edgeStart&&snap.edgeStart.clone?snap.edgeStart.clone():null,edgeEnd:snap.edgeEnd&&snap.edgeEnd.clone?snap.edgeEnd.clone():null};}getMeasurementPickRevision(){const bounds=this.sectionBounds;const sectionSignature=this.sectionBoxActive&&bounds&&bounds.min&&bounds.max?[bounds.min.x,bounds.min.y,bounds.min.z,bounds.max.x,bounds.max.y,bounds.max.z].map(value=>Number(value||0).toFixed(3)).join(','):'none';return[this.selectionCandidateCacheRevision||0,this.raycastViewRevision||0,sectionSignature].join('|');}pickMeasurePointFromScreen(x,y){const metricsStart=this.getPerformanceNow?this.getPerformanceNow():Date.now();const revision=this.getMeasurementPickRevision();const now=this.getPerformanceNow?this.getPerformanceNow():Date.now();const cached=this.measurePickCache;if(cached&&cached.revision===revision&&cached.snap&&Math.abs(cached.x-x)<=1.5&&Math.abs(cached.y-y)<=1.5&&now-cached.time<=90){if(this.recordPerformanceOperation)this.recordPerformanceOperation('measurementPick:cache-hit',now-metricsStart,{kind:cached.snap.kind||''});return this.cloneMeasurementSnap(cached.snap);}const hit=this.pickRaycastHitFromScreen(x,y);if(!hit||!hit.point){this.measurePickCache={revision,x,y,time:now,snap:null};return null;}const snap=this.resolveMeasurementSnap(hit,x,y);this.measurePickCache={revision,x,y,time:now,snap:this.cloneMeasurementSnap(snap)};if(this.recordPerformanceOperation)this.recordPerformanceOperation('measurementPick:resolve',(this.getPerformanceNow?this.getPerformanceNow():Date.now())-metricsStart,{kind:snap&&snap.kind||'',cached:false});return snap;}cycleMeasureSnapCandidate(){if(!this.measureLastMouse)return;const hits=this.getMeasureRaycastHitsFromScreen(this.measureLastMouse.x,this.measureLastMouse.y);if(!hits||hits.length<=1)return;this.measureSnapCycleIndex=(this.measureSnapCycleIndex+1)%hits.length;this.measurePickCache=null;if(this.mouseMode==='elevation-drag'){this.dragElevationMarkerFromScreen(this.measureLastMouse.x,this.measureLastMouse.y,true);}else if(this.elevationActive&&this.elevationPlacementActive){this.updateElevationPreviewFromScreen(this.measureLastMouse.x,this.measureLastMouse.y,true);}else if(this.mouseMode==='measure-drag'){this.dragMeasurePointFromScreen(this.measureLastMouse.x,this.measureLastMouse.y);}else{this.updateMeasurePreviewFromScreen(this.measureLastMouse.x,this.measureLastMouse.y,true);}}pickRaycastHitFromScreen(x,y){const hits=this.getMeasureRaycastHitsFromScreen(x,y);if(!hits||!hits.length)return null;const index=Math.max(0,this.measureSnapCycleIndex||0)%hits.length;return hits[index];}getMeasureRaycastHitsFromScreen(x,y){const metricsStart=this.getPerformanceNow?this.getPerformanceNow():Date.now();const rect=this.canvas.getBoundingClientRect();const width=Math.max(1,rect.width||this.canvas.clientWidth||1);const height=Math.max(1,rect.height||this.canvas.clientHeight||1);this.pointer.x=(x/width)*2-1;this.pointer.y=-(y/height)*2+1;this.raycaster.setFromCamera(this.pointer,this.camera);const surfaceObjects=this.getMeasureSurfaceObjectsForSnap();let surfaceCandidates=this.getPointRaycastCandidates?this.getPointRaycastCandidates(x,y,surfaceObjects):surfaceObjects;if(this.sectionBoxActive&&this.sectionBounds&&this.sectionBounds.min&&this.sectionBounds.max){const sectionAabb=new THREE.Box3(this.sectionBounds.min.clone(),this.sectionBounds.max.clone());surfaceCandidates=surfaceCandidates.filter(mesh=>{const box=this.getMeshSelectionWorldBox?this.getMeshSelectionWorldBox(mesh):null;return!box||box.isEmpty()||box.intersectsBox(sectionAabb);});}if(this.isMeshSelectableForUser)surfaceCandidates=surfaceCandidates.filter(mesh=>this.isMeshSelectableForUser(mesh));const surfaceHits=this.filterRaycastHitsBySection(this.raycaster.intersectObjects(surfaceCandidates,false));const lineObjects=this.getMeasureLineObjectsForSnap(surfaceCandidates);const previousThreshold=this.raycaster.params&&this.raycaster.params.Line?this.raycaster.params.Line.threshold:null;if(this.raycaster.params&&this.raycaster.params.Line){const referencePoint=surfaceHits&&surfaceHits.length&&surfaceHits[0].point?surfaceHits[0].point:this.target;this.raycaster.params.Line.threshold=this.getMeasureLineRaycastThreshold(referencePoint);}const lineHits=lineObjects.length?this.filterRaycastHitsBySection(this.raycaster.intersectObjects(lineObjects,false).filter(hit=>hit&&hit.object&&hit.object.isLineSegments)):[];if(previousThreshold!=null&&this.raycaster.params&&this.raycaster.params.Line){this.raycaster.params.Line.threshold=previousThreshold;}const hits=[...(surfaceHits||[]),...(lineHits||[])].filter(hit=>hit&&hit.point).sort((a,b)=>{const distanceA=Number.isFinite(a.distance)?a.distance:Infinity;const distanceB=Number.isFinite(b.distance)?b.distance:Infinity;return distanceA-distanceB;});if(this.recordPerformanceOperation)this.recordPerformanceOperation('measurementRaycast:indexed',(this.getPerformanceNow?this.getPerformanceNow():Date.now())-metricsStart,{sourceSurfaces:surfaceObjects.length,surfaceCandidates:surfaceCandidates.length,lineCandidates:lineObjects.length,surfaceHits:surfaceHits.length,lineHits:lineHits.length,sectionBoxActive:!!this.sectionBoxActive});return hits;}isMeasureMeshCandidate(mesh){if(!mesh||!mesh.isMesh||!mesh.userData)return false;if(this.isMeshSelectableForUser&&!this.isMeshSelectableForUser(mesh)){return false;}const chunkSuppressed=!!(mesh.userData.normalChunkSuppressed||mesh.userData.massTransparencyChunkSuppressed||mesh.userData.visualStateChunkSuppressed);return mesh.visible||chunkSuppressed;}getMeasureSurfaceObjectsForSnap(){const revision=this.selectionCandidateCacheRevision||0;if(this.measureSurfaceCandidateCache&&this.measureSurfaceCandidateCacheRevision===revision){return this.measureSurfaceCandidateCache;}const candidates=this.getSelectableMeshCandidates?this.getSelectableMeshCandidates():(this.meshes||[]);const result=candidates.filter(mesh=>this.isMeasureMeshCandidate(mesh));this.measureSurfaceCandidateCache=result;this.measureSurfaceCandidateCacheRevision=revision;return result;}getMeasureLineObjectsForSnap(surfaceCandidates){const result=[];const seen=new Set();const addLine=line=>{if(!line||!line.isLineSegments||seen.has(line))return;if(!this.isMeasureObjectVisible(line))return;seen.add(line);result.push(line);};for(const object of surfaceCandidates||[]){if(!object)continue;for(const child of object.children||[])addLine(child);}const revision=this.selectionCandidateCacheRevision||0;if(!this.measureDirectLineCandidateCache||this.measureDirectLineCandidateCacheRevision!==revision){this.measureDirectLineCandidateCache=(this.meshes||[]).filter(object=>object&&object.isLineSegments&&!(object.userData&&object.userData.isGeneratedEdge));this.measureDirectLineCandidateCacheRevision=revision;}for(const object of this.measureDirectLineCandidateCache||[])addLine(object);return result;}isMeasureObjectVisible(object){let current=object;while(current){if(current.visible===false){const userData=current.userData||{};const suppressedByChunk=!!(userData.normalChunkSuppressed||userData.massTransparencyChunkSuppressed||userData.visualStateChunkSuppressed);const suppressedGroupedEdge=!!userData.groupedTransparentSuppressed;if(current.isMesh){if(!this.isMeasureMeshCandidate(current))return false;}else if(!suppressedByChunk&&!suppressedGroupedEdge){return false;}}current=current.parent;}return true;}getMeasureLineRaycastThreshold(referencePoint){const modelDepth=Math.max(1,this.getFullModelDepthSize?this.getFullModelDepthSize():1);const worldPerPixel=Math.max(0.000001,this.getWorldPerPixelAtPoint(referencePoint||this.target||new THREE.Vector3()));return THREE.MathUtils.clamp(worldPerPixel*14,modelDepth*0.00001,modelDepth*0.0025);}getMeasureSnapPixelTolerances(){let coarsePointer=false;try{coarsePointer=typeof window!=='undefined'&&window.matchMedia&&window.matchMedia('(pointer: coarse)').matches;}catch(_){}return coarsePointer?{vertex:30,edge:24}:{vertex:22,edge:16};}resolveMeasurementSnap(hit,screenX,screenY){const object=hit.object;if(!object||!object.geometry)return{point:hit.point.clone(),kind:'face',object};const geometry=object.geometry;const positionAttr=geometry.getAttribute&&geometry.getAttribute('position');if(!positionAttr||!positionAttr.count){return{point:hit.point.clone(),kind:object.isLineSegments?'edge':'face',object};}const matrixWorld=object.matrixWorld;const face=hit.face;let faceNormal=null;if(face&&face.normal){const normalMatrix=new THREE.Matrix3().getNormalMatrix(matrixWorld);faceNormal=face.normal.clone().applyMatrix3(normalMatrix).normalize();}if(object.isLineSegments){return this.resolveMeasurementLineSnap(hit,screenX,screenY);}const candidates=[];let facePoints=null;const tolerances=this.getMeasureSnapPixelTolerances();const generatedEdge=this.findBestMeasurementLineCandidate(object,hit.point,screenX,screenY);if(generatedEdge){if(generatedEdge.start)candidates.push({point:generatedEdge.start,kind:'vertex',px:this.worldToScreenDistance(generatedEdge.start,screenX,screenY)});if(generatedEdge.end)candidates.push({point:generatedEdge.end,kind:'vertex',px:this.worldToScreenDistance(generatedEdge.end,screenX,screenY)});if(generatedEdge.px<=tolerances.edge){candidates.push({point:generatedEdge.point,kind:'edge',start:generatedEdge.start,end:generatedEdge.end,px:generatedEdge.px});}}const pushVertex=(idx)=>{const v=new THREE.Vector3().fromBufferAttribute(positionAttr,idx).applyMatrix4(matrixWorld);candidates.push({point:v,kind:'vertex',px:this.worldToScreenDistance(v,screenX,screenY)});return v;};if(face){const va=pushVertex(face.a);const vb=pushVertex(face.b);const vc=pushVertex(face.c);facePoints=[va,vb,vc];const edgeAB=this.closestPointOnSegment(hit.point,va,vb);const edgeBC=this.closestPointOnSegment(hit.point,vb,vc);const edgeCA=this.closestPointOnSegment(hit.point,vc,va);candidates.push({point:edgeAB,kind:'edge',start:va,end:vb,px:this.worldToScreenDistance(edgeAB,screenX,screenY)});candidates.push({point:edgeBC,kind:'edge',start:vb,end:vc,px:this.worldToScreenDistance(edgeBC,screenX,screenY)});candidates.push({point:edgeCA,kind:'edge',start:vc,end:va,px:this.worldToScreenDistance(edgeCA,screenX,screenY)});}let bestVertex=candidates.filter(c=>c.kind==='vertex'&&c.px<=tolerances.vertex).sort((a,b)=>a.px-b.px)[0];if(bestVertex)return{point:bestVertex.point.clone(),kind:'vertex',object,facePoints,faceNormal,faceIndex:hit.faceIndex};let bestEdge=candidates.filter(c=>c.kind==='edge'&&c.px<=tolerances.edge).sort((a,b)=>a.px-b.px)[0];if(bestEdge){return{point:bestEdge.point.clone(),kind:'edge',object,edgeStart:bestEdge.start?bestEdge.start.clone():null,edgeEnd:bestEdge.end?bestEdge.end.clone():null,facePoints,faceNormal,faceIndex:hit.faceIndex};}return{point:hit.point.clone(),kind:'face',object,facePoints,faceNormal,faceIndex:hit.faceIndex};}resolveMeasurementLineSnap(hit,screenX,screenY){const object=hit&&hit.object;const point=hit&&hit.point?hit.point:null;if(!object||!point)return null;const tolerances=this.getMeasureSnapPixelTolerances();const candidate=this.findBestMeasurementLineCandidate(object,point,screenX,screenY);if(!candidate)return{point:point.clone(),kind:'edge',object};const startPx=candidate.start?this.worldToScreenDistance(candidate.start,screenX,screenY):Infinity;const endPx=candidate.end?this.worldToScreenDistance(candidate.end,screenX,screenY):Infinity;if(startPx<=tolerances.vertex||endPx<=tolerances.vertex){const vertex=startPx<=endPx?candidate.start:candidate.end;return{point:vertex.clone(),kind:'vertex',object};}return{point:candidate.point.clone(),kind:'edge',object,edgeStart:candidate.start?candidate.start.clone():null,edgeEnd:candidate.end?candidate.end.clone():null};}findBestMeasurementLineCandidate(object,referencePoint,screenX,screenY){if(!object||!referencePoint)return null;const lines=[];const addLine=line=>{if(line&&line.isLineSegments&&this.isMeasureObjectVisible(line))lines.push(line);};if(object.isLineSegments)addLine(object);else for(const child of object.children||[])addLine(child);let best=null;const start=new THREE.Vector3();const end=new THREE.Vector3();for(const line of lines){const geometry=line.geometry;const position=geometry&&geometry.getAttribute?geometry.getAttribute('position'):null;if(!position||position.count<2)continue;const matrixWorld=line.matrixWorld;const segmentCount=Math.floor(position.count/2);for(let i=0;i<segmentCount;i++){start.fromBufferAttribute(position,i*2).applyMatrix4(matrixWorld);end.fromBufferAttribute(position,i*2+1).applyMatrix4(matrixWorld);if(start.distanceToSquared(end)<=1e-12)continue;const point=this.closestPointOnSegment(referencePoint,start,end);const px=this.worldToScreenDistance(point,screenX,screenY);if(!best||px<best.px){best={point:point.clone(),start:start.clone(),end:end.clone(),px,object:line};}}}return best;}closestPointOnSegment(point,start,end){const segment=end.clone().sub(start);const lengthSq=segment.lengthSq();if(lengthSq<=1e-12)return start.clone();const t=THREE.MathUtils.clamp(point.clone().sub(start).dot(segment)/lengthSq,0,1);return start.clone().add(segment.multiplyScalar(t));}worldToScreenDistance(point,x,y){if(!point)return Infinity;const projected=point.clone().project(this.camera);const width=Math.max(1,this.renderer.domElement.clientWidth);const height=Math.max(1,this.renderer.domElement.clientHeight);const sx=(projected.x*0.5+0.5)*width;const sy=(-projected.y*0.5+0.5)*height;const dx=sx-x;const dy=sy-y;return Math.sqrt(dx*dx+dy*dy);}createMeasurementCoordinateFrame(snap,origin){const worldUp=new THREE.Vector3(0,1,0);const epsilon=1e-8;const result={origin:origin&&origin.clone?origin.clone():(snap&&snap.point&&snap.point.clone?snap.point.clone():new THREE.Vector3()),x:new THREE.Vector3(1,0,0),y:new THREE.Vector3(0,0,1),z:worldUp.clone(),mode:'global'};const horizontal=vector=>{if(!vector)return null;const projected=vector.clone().sub(worldUp.clone().multiplyScalar(vector.dot(worldUp)));return projected.lengthSq()>epsilon?projected.normalize():null;};const faceNormal=snap&&snap.faceNormal?horizontal(snap.faceNormal):null;let longestFaceEdge=null;if(snap&&Array.isArray(snap.facePoints)&&snap.facePoints.length>=2){for(let i=0;i<snap.facePoints.length;i++){const a=snap.facePoints[i];const b=snap.facePoints[(i+1)%snap.facePoints.length];if(!a||!b)continue;const direction=horizontal(b.clone().sub(a));if(!direction)continue;const lengthSq=a.distanceToSquared(b);if(!longestFaceEdge||lengthSq>longestFaceEdge.lengthSq)longestFaceEdge={direction,lengthSq};}}if(faceNormal){result.y.copy(faceNormal);result.x.copy(result.y).cross(worldUp).normalize();if(longestFaceEdge&&result.x.dot(longestFaceEdge.direction)<0)result.x.multiplyScalar(-1);result.mode='face';return result;}const edgeDirection=snap&&snap.edgeStart&&snap.edgeEnd?horizontal(snap.edgeEnd.clone().sub(snap.edgeStart)):(longestFaceEdge?longestFaceEdge.direction:null);if(edgeDirection){result.x.copy(edgeDirection);result.y.copy(worldUp).cross(result.x).normalize();result.mode='edge';return result;}const object=snap&&snap.object;if(object&&object.matrixWorld){const objectX=horizontal(new THREE.Vector3().setFromMatrixColumn(object.matrixWorld,0));if(objectX){result.x.copy(objectX);result.y.copy(worldUp).cross(result.x).normalize();result.mode='element';}}return result;}getMeasurementFrame(measurement,fallbackStart){if(measurement&&measurement.frame&&measurement.frame.x&&measurement.frame.y&&measurement.frame.z)return measurement.frame;const snap=measurement&&Array.isArray(measurement.snaps)?measurement.snaps[0]:null;const frame=this.createMeasurementCoordinateFrame(snap,fallbackStart);if(measurement)measurement.frame=frame;return frame;}getMeasurementProjectionSegments(start,end,frame){if(!start||!end)return[];const basis=frame&&frame.x&&frame.y&&frame.z?frame:{x:new THREE.Vector3(1,0,0),y:new THREE.Vector3(0,0,1),z:new THREE.Vector3(0,1,0),mode:'global'};const delta=end.clone().sub(start);const dx=delta.dot(basis.x);const dy=delta.dot(basis.y);const dz=delta.dot(basis.z);const xPoint=start.clone().addScaledVector(basis.x,dx);const yPoint=xPoint.clone().addScaledVector(basis.y,dy);return[{axis:'x',label:'X',color:0xef4444,start:start.clone(),end:xPoint,value:Math.abs(dx),signedValue:dx,frameMode:basis.mode||'global'},{axis:'y',label:'Y',color:0x22c55e,start:xPoint,end:yPoint,value:Math.abs(dy),signedValue:dy,frameMode:basis.mode||'global'},{axis:'z',label:'Z',color:0x38bdf8,start:yPoint,end:end.clone(),value:Math.abs(dz),signedValue:dz,frameMode:basis.mode||'global'}];}addMeasurementLineSegment(group,start,end,color,opacity,renderOrder){if(!group||!start||!end||start.distanceToSquared(end)<=1e-12)return null;const positions=new Float32Array([start.x,start.y,start.z,end.x,end.y,end.z]);const geometry=new THREE.BufferGeometry();geometry.setAttribute('position',new THREE.BufferAttribute(positions,3));const material=new THREE.LineBasicMaterial({color,transparent:true,opacity:opacity==null?0.96:opacity,depthTest:false,depthWrite:false});const line=new THREE.LineSegments(geometry,material);line.renderOrder=renderOrder||100;group.add(line);return line;}rebuildMeasurementGraphics(){if(this.measureGroup){this.measureScene.remove(this.measureGroup);this.measureGroup.traverse(child=>{if(child.geometry&&child.geometry.dispose)child.geometry.dispose();if(child.material){if(Array.isArray(child.material))child.material.forEach(material=>material&&material.dispose&&material.dispose());else if(child.material.dispose)child.material.dispose();}});}this.measureGroup=null;this.measureLine=null;this.measureMarkers=[];if(!Array.isArray(this.measurements)||!this.measurements.length)return;const group=new THREE.Group();group.renderOrder=100;const firstVisiblePoint=this.measurements.map(item=>item&&Array.isArray(item.points)?item.points[0]:null).find(Boolean)||this.target||new THREE.Vector3();const markerRadius=Math.max(this.getWorldPerPixelAtPoint(firstVisiblePoint)*6,0.006);for(let i=0;i<this.measurements.length;i++){const measurement=this.measurements[i];const points=measurement&&Array.isArray(measurement.points)?measurement.points:[];if(!points.length)continue;const markerGeometry=new THREE.SphereGeometry(markerRadius,16,16);const markerMaterial=new THREE.MeshBasicMaterial({color:0xff00ff,depthTest:false,depthWrite:false,transparent:true,opacity:0.92});for(const point of points){const marker=new THREE.Mesh(markerGeometry.clone(),markerMaterial.clone());marker.position.copy(point);marker.renderOrder=101;group.add(marker);this.measureMarkers.push(marker);}markerGeometry.dispose();markerMaterial.dispose();const frame=this.getMeasurementFrame(measurement,points[0]);if(points[0]&&frame){const axisLength=Math.max(markerRadius*5,this.getWorldPerPixelAtPoint(points[0])*38);this.addMeasurementLineSegment(group,points[0],points[0].clone().addScaledVector(frame.x,axisLength),0xef4444,0.88,102);this.addMeasurementLineSegment(group,points[0],points[0].clone().addScaledVector(frame.y,axisLength),0x22c55e,0.88,102);this.addMeasurementLineSegment(group,points[0],points[0].clone().addScaledVector(frame.z,axisLength),0x38bdf8,0.88,102);}if(points.length>=2){const line=this.addMeasurementLineSegment(group,points[0],points[1],0xff00ff,0.98,100);if(i===this.activeMeasurementIndex)this.measureLine=line;const projectionSegments=this.getMeasurementProjectionSegments(points[0],points[1],this.getMeasurementFrame(measurement,points[0]));for(const segment of projectionSegments){if(!segment||segment.value<=1e-9)continue;this.addMeasurementLineSegment(group,segment.start,segment.end,segment.color,0.92,99);}}}this.measureGroup=group;this.measureScene.add(group);}getMeasurementOverlaySignature(){const parts=[this.activeMeasurementIndex||0];for(const measurement of this.measurements||[]){const points=measurement&&Array.isArray(measurement.points)?measurement.points:[];parts.push(points.length);for(const point of points){if(!point){parts.push('null');continue;}parts.push(Number(point.x).toFixed(5),Number(point.y).toFixed(5),Number(point.z).toFixed(5));}const frame=measurement&&measurement.frame;if(frame&&frame.x&&frame.y){parts.push(frame.mode||'',frame.x.x.toFixed(4),frame.x.z.toFixed(4),frame.y.x.toFixed(4),frame.y.z.toFixed(4));}}if(this.measurePreview)parts.push('preview',this.measurePreview.x.toFixed(5),this.measurePreview.y.toFixed(5),this.measurePreview.z.toFixed(5));return parts.join('|');}updateMeasurementOverlay(force){const metricsStart=this.getPerformanceNow?this.getPerformanceNow():Date.now();const overlay=this.measureOverlay;if(!overlay)return;const signature=this.getMeasurementOverlaySignature();const viewRevision=this.raycastViewRevision||0;const contentChanged=force===true||signature!==this.measurementOverlayLastSignature;const viewChanged=force===true||viewRevision!==this.measurementOverlayLastViewRevision;if(!contentChanged&&!viewChanged)return;this.measurementOverlayLastViewRevision=viewRevision;if(contentChanged){this.measurementOverlayLastSignature=signature;this.clearMeasureDynamicLabels(overlay);this.clearMeasureProjectionOverlayLines(overlay);}this.updateMeasureProjectionOverlaySize(overlay);const measurement=this.getActiveMeasurement();const points=measurement&&Array.isArray(measurement.points)?measurement.points:[];const activePreview=this.measurePreview&&points.length===1?this.measurePreview:null;const secondPoint=points.length>=2?points[1]:activePreview;this.positionOverlayNode(overlay.pointA,points[0]);this.positionOverlayNode(overlay.pointB,secondPoint,points.length>=2?'2':'');if(overlay.distance)overlay.distance.classList.add('hidden');if(overlay.axes)overlay.axes.classList.add('hidden');if(contentChanged){if(points.length===1&&activePreview){this.drawMeasureProjectionOverlayLines(overlay,points[0],activePreview,0.60,true,this.getMeasurementFrame(measurement,points[0]));}if(Array.isArray(this.measurements)&&this.measurements.length){for(let i=0;i<this.measurements.length;i++){const item=this.measurements[i];const measurePoints=item&&Array.isArray(item.points)?item.points:[];if(!measurePoints.length)continue;const p1=measurePoints[0];const frame=this.getMeasurementFrame(item,p1);if(frame&&p1){const axisLength=Math.max(this.getWorldPerPixelAtPoint(p1)*38,0.02);this.createMeasureDynamicLabel(p1.clone().addScaledVector(frame.x,axisLength),'X','measure-local-axis-label measure-local-axis-x');this.createMeasureDynamicLabel(p1.clone().addScaledVector(frame.y,axisLength),'Y','measure-local-axis-label measure-local-axis-y');this.createMeasureDynamicLabel(p1.clone().addScaledVector(frame.z,axisLength),'Z','measure-local-axis-label measure-local-axis-z');}if(measurePoints.length<2)continue;const p2=measurePoints[1];const midpoint=p1.clone().lerp(p2,0.5);this.createMeasureDynamicLabel(midpoint,this.formatDistance(p1.distanceTo(p2)),'measure-total-dynamic');this.drawMeasureProjectionOverlayLines(overlay,p1,p2,0.96,false,this.getMeasurementFrame(item,p1));const projectionSegments=this.getMeasurementProjectionSegments(p1,p2,this.getMeasurementFrame(item,p1));for(const segment of projectionSegments){if(!segment||segment.value<=1e-9)continue;const segmentMidpoint=segment.start.clone().lerp(segment.end,0.5);this.createMeasureDynamicLabel(segmentMidpoint,segment.label+' '+this.formatDistance(segment.value),'measure-projection-label measure-projection-'+segment.axis);}}}}else{this.refreshMeasureDynamicLabelPositions(overlay);this.refreshMeasureProjectionOverlayLines(overlay);}const duration=(this.getPerformanceNow?this.getPerformanceNow():Date.now())-metricsStart;if(this.recordPerformanceOperation&&(contentChanged||duration>=4)){this.recordPerformanceOperation('measurementOverlay:update-persistent',duration,{contentChanged,viewChanged,measurements:Array.isArray(this.measurements)?this.measurements.length:0,dynamicLabels:overlay.root?overlay.root.querySelectorAll('.measure-dynamic-label').length:0,projectionLines:overlay.axisSvg?overlay.axisSvg.children.length:0});}}clearMeasureDynamicLabels(overlay){if(!overlay||!overlay.root)return;const labels=overlay.root.querySelectorAll('.measure-dynamic-label');labels.forEach(label=>label.remove());}refreshMeasureDynamicLabelPositions(overlay){if(!overlay||!overlay.root)return;const labels=overlay.root.querySelectorAll('.measure-dynamic-label');labels.forEach(label=>this.positionOverlayNode(label,label.__measureWorldPoint||null));}clearMeasureProjectionOverlayLines(overlay){if(!overlay||!overlay.axisSvg)return;while(overlay.axisSvg.firstChild)overlay.axisSvg.removeChild(overlay.axisSvg.firstChild);}refreshMeasureProjectionOverlayLines(overlay){if(!overlay||!overlay.axisSvg)return;const size=this.updateMeasureProjectionOverlaySize(overlay);for(const line of overlay.axisSvg.children||[]){const a=this.projectMeasurePointToOverlay(line.__measureStart,size.width,size.height);const b=this.projectMeasurePointToOverlay(line.__measureEnd,size.width,size.height);if(!a||!b){line.setAttribute('visibility','hidden');continue;}line.removeAttribute('visibility');line.setAttribute('x1',String(a.x));line.setAttribute('y1',String(a.y));line.setAttribute('x2',String(b.x));line.setAttribute('y2',String(b.y));}}updateMeasureProjectionOverlaySize(overlay){if(!overlay||!overlay.axisSvg)return{width:0,height:0};const width=Math.max(1,Math.floor(this.renderer.domElement.clientWidth||this.canvas.clientWidth||1));const height=Math.max(1,Math.floor(this.renderer.domElement.clientHeight||this.canvas.clientHeight||1));overlay.axisSvg.setAttribute('viewBox','0 0 '+width+' '+height);overlay.axisSvg.setAttribute('width',String(width));overlay.axisSvg.setAttribute('height',String(height));return{width,height};}projectMeasurePointToOverlay(point,width,height){if(!point||!Number.isFinite(width)||!Number.isFinite(height))return null;const projected=point.clone().project(this.camera);if(!Number.isFinite(projected.x)||!Number.isFinite(projected.y)||!Number.isFinite(projected.z))return null;if(projected.z<-1||projected.z>1)return null;return{x:(projected.x*0.5+0.5)*width,y:(-projected.y*0.5+0.5)*height};}addMeasureProjectionOverlayLine(overlay,segment,width,height,opacity,dashed){if(!overlay||!overlay.axisSvg||!segment||!segment.start||!segment.end)return;if(segment.start.distanceToSquared(segment.end)<=1e-12)return;const a=this.projectMeasurePointToOverlay(segment.start,width,height);const b=this.projectMeasurePointToOverlay(segment.end,width,height);const colorMap={x:'#ef4444',y:'#22c55e',z:'#38bdf8'};const color=colorMap[segment.axis]||'#f472b6';const ns='http://www.w3.org/2000/svg';const addLine=(stroke,strokeWidth,lineOpacity)=>{const line=document.createElementNS(ns,'line');line.__measureStart=segment.start.clone();line.__measureEnd=segment.end.clone();if(a&&b){line.setAttribute('x1',String(a.x));line.setAttribute('y1',String(a.y));line.setAttribute('x2',String(b.x));line.setAttribute('y2',String(b.y));}else{line.setAttribute('visibility','hidden');}line.setAttribute('stroke',stroke);line.setAttribute('stroke-width',String(strokeWidth));line.setAttribute('stroke-linecap','round');line.setAttribute('stroke-linejoin','round');line.setAttribute('vector-effect','non-scaling-stroke');line.setAttribute('opacity',String(lineOpacity));if(dashed)line.setAttribute('stroke-dasharray','8 6');overlay.axisSvg.appendChild(line);};addLine('rgba(15,23,42,0.58)',7,Math.min(0.46,opacity||0.46));addLine(color,3.5,opacity==null?0.96:opacity);}drawMeasureProjectionOverlayLines(overlay,start,end,opacity,dashed,frame){if(!overlay||!start||!end)return;const size=this.updateMeasureProjectionOverlaySize(overlay);const segments=this.getMeasurementProjectionSegments(start,end,frame);for(const segment of segments){if(!segment||segment.value<=1e-9)continue;this.addMeasureProjectionOverlayLine(overlay,segment,size.width,size.height,opacity,dashed);}}createMeasureDynamicLabel(point,text,className){const overlay=this.measureOverlay;if(!overlay||!overlay.root||!point)return null;const node=document.createElement('div');node.className='measure-dynamic-label '+(className||'');node.textContent=text||'';node.__measureWorldPoint=point.clone?point.clone():point;overlay.root.appendChild(node);this.positionOverlayNode(node,node.__measureWorldPoint);return node;}positionOverlayNode(node,point,text){if(!node)return;if(!point){node.classList.add('hidden');return;}if(typeof text==='string')node.textContent=text;const projected=point.clone().project(this.camera);if(projected.z<-1||projected.z>1){node.classList.add('hidden');return;}const width=Math.max(1,this.renderer.domElement.clientWidth);const height=Math.max(1,this.renderer.domElement.clientHeight);node.style.left=((projected.x*0.5+0.5)*width)+'px';node.style.top=((-projected.y*0.5+0.5)*height)+'px';node.classList.remove('hidden');}formatDistance(value){const abs=Math.abs(Number(value)||0);let decimals=3;if(abs>=10)decimals=2;if(abs>=100)decimals=1;return abs.toFixed(decimals)+' m';}waitForResetProgressPaint(){return new Promise(resolve=>{const schedule=typeof requestAnimationFrame==='function'?requestAnimationFrame:callback=>setTimeout(callback,0);schedule(()=>setTimeout(resolve,0));});}async resetAllViewModifications(options){const opts=options&&typeof options==='object'?options:{};const report=typeof opts.onProgress==='function'?opts.onProgress:()=>{};const metricsStart=this.getPerformanceNow?this.getPerformanceNow():Date.now();const resetToken=(this.resetAllViewSequence||0)+1;this.resetAllViewSequence=resetToken;this.resetAllViewActive=true;const ensureCurrent=()=>{if(this.resetAllViewSequence!==resetToken){const error=new Error('Restablecimiento sustituido por una operación más reciente.');error.name='AbortError';throw error;}};const update=async(message,current,total,details)=>{ensureCurrent();const safeTotal=Number.isFinite(Number(total))?Math.max(0,Number(total)):0;const safeCurrent=Number.isFinite(Number(current))?Math.max(0,Number(current)):0;report({message,current:safeCurrent,total:safeTotal,details:details||null});if(opts.yieldBetweenStages!==false)await this.waitForResetProgressPaint();};let chunkMode='none';let chunkCount=0;let visibilityResult={changedKeys:0,total:this.meshes?this.meshes.length:0};const hiddenBeforeReset=new Set(this.hiddenKeys||[]);const hadIsolationBeforeReset=!!(this.isolateActive||(this.isolatedKeys&&this.isolatedKeys.size));const hadSpatialGroupingBeforeReset=!!(this.dataSpatialGroupingActive||this.baseIsolationActive);const viewerMassBeforeReset=this.viewerMassTransparencyState&&this.viewerMassTransparencyState.active?this.viewerMassTransparencyState:null;const aiVisualBeforeReset=this.aiVisualState&&this.aiVisualState.active?this.aiVisualState:null;const colorOverrideKeysBeforeReset=new Set(this.visualColorOverrides?this.visualColorOverrides.keys():[]);if(this.colorOverrideStates&&this.colorOverrideStates.size){for(const[mesh]of this.colorOverrideStates.entries()){const key=this.getMeshKey?this.getMeshKey(mesh):null;if(key)colorOverrideKeysBeforeReset.add(key);}}const visualStateChunkKeysBeforeReset=new Set(this.visualStateChunkHiddenKeys||[]);const hadColorOverridesBeforeReset=colorOverrideKeysBeforeReset.size>0||!!(this.colorOverrideStates&&this.colorOverrideStates.size);const hadVisualStateRenderChunksBeforeReset=!!(this.visualStateChunkVisible||(this.visualStateChunkMeshes&&this.visualStateChunkMeshes.some(chunk=>chunk&&chunk.parent))||(this.visualStateChunkEdgeBatches&&this.visualStateChunkEdgeBatches.some(batch=>batch&&batch.parent)));const resetAppearanceKeys=new Set(hiddenBeforeReset);const appendResetKeys=keys=>{for(const key of keys||[])if(key)resetAppearanceKeys.add(key);};appendResetKeys(colorOverrideKeysBeforeReset);appendResetKeys(visualStateChunkKeysBeforeReset);if(viewerMassBeforeReset)appendResetKeys(viewerMassBeforeReset.allKeys||viewerMassBeforeReset.activeKeys);if(aiVisualBeforeReset){appendResetKeys(aiVisualBeforeReset.colorKeys);appendResetKeys(aiVisualBeforeReset.isolationKeys);appendResetKeys(aiVisualBeforeReset.transparencyKeys);}if(this.visualWorksStates&&this.visualWorksStates.size)appendResetKeys(this.visualWorksStates.keys());if(this.visualClashStates&&this.visualClashStates.size)appendResetKeys(this.visualClashStates.keys());if(this.visualFadeStates&&this.visualFadeStates.size)appendResetKeys(this.visualFadeStates.keys());const needsGlobalVisibilityReset=hadIsolationBeforeReset||hadSpatialGroupingBeforeReset;const hadGlobalTemporaryAppearance=!!(viewerMassBeforeReset||aiVisualBeforeReset||hadColorOverridesBeforeReset||hadVisualStateRenderChunksBeforeReset||(this.visualWorksStates&&this.visualWorksStates.size)||(this.visualClashStates&&this.visualClashStates.size)||(this.visualFadeStates&&this.visualFadeStates.size));try{await update('Preparando el restablecimiento del visor...',0,0);if(typeof this.restoreDataSpatialGrouping==='function')this.restoreDataSpatialGrouping('reset-all-view');if(this.normalChunkRefreshDeferTimer){clearTimeout(this.normalChunkRefreshDeferTimer);this.normalChunkRefreshDeferTimer=null;}this._pendingNormalChunkVisibleEdgeRefresh=false;this._pendingNormalChunkVisibleEdgeRefreshReason=null;await update('Limpiando selección, aislamiento y elementos ocultos...',0,0);this.isolateActive=false;if(!this.isolatedKeys)this.isolatedKeys=new Set();this.isolatedKeys.clear();if(!this.hiddenKeys)this.hiddenKeys=new Set();this.hiddenKeys.clear();if(this.baseIsolationActive||(this.baseIsolationKeys&&this.baseIsolationKeys.size)){this.baseIsolationKeys=new Set();this.baseIsolationActive=false;}await update('Restaurando colores y transparencias...',0,0);this.clearMassTransparencyHighlight&&this.clearMassTransparencyHighlight({owner:'viewer',disposeCache:false,fullReset:true,skipNormalChunkRefresh:true,skipVisualUpdate:true});if(aiVisualBeforeReset&&aiVisualBeforeReset.transparencyKeys&&aiVisualBeforeReset.transparencyKeys.size){this.clearMassTransparencyHighlight&&this.clearMassTransparencyHighlight({owner:'data',disposeCache:false,fullReset:true,skipNormalChunkRefresh:true,skipVisualUpdate:true});}if(hadColorOverridesBeforeReset){this.clearColorOverrides&&this.clearColorOverrides({skipVisualUpdate:true,skipChunkRefresh:true,skipSectionRefresh:true});}this.aiVisualState=null;if(!(this.visualMassState&&this.visualMassState.active&&this.getMassTransparencyOwner&&this.getMassTransparencyOwner(this.visualMassState)==='data')){this.restoreFadedElements&&this.restoreFadedElements({skipVisualUpdate:true,skipChunkRefresh:true,skipSectionRefresh:true});}if(!(this.visualMassState&&this.visualMassState.active)){this.clearGroupedTransparentEdges&&this.clearGroupedTransparentEdges(false);this.clearMassTransparencyChunks&&this.clearMassTransparencyChunks(false);}await update('Desactivando caja de sección y herramientas temporales...',0,0);this.disableSectionBox();this.clearMeasurement();this.clearElevationMarker();this.clearWorksProgressVisualization&&this.clearWorksProgressVisualization({skipVisualUpdate:true,skipVisibilityUpdate:true,skipChunkRefresh:true});this.clearClashHighlight({skipNormalChunkRebuild:true,skipSectionRefresh:true});this.clearVisualStateChunks&&this.clearVisualStateChunks(false);this.visualStateUseStableEdges=false;this.restoreStableNormalChunkEdgeAppearance&&this.restoreStableNormalChunkEdgeAppearance();this.elevationActive=false;this.elevationPlacementActive=false;await update('Restaurando la visibilidad de los elementos...',0,0);const visibilityOptions={forceChunkRebuild:false,reason:'reset-all-view',skipChunkVisibilitySync:true,skipChunkRefresh:true,skipSectionRefresh:true,skipVisualUpdate:true,batchSize:Number(opts.visibilityBatchSize)||5000,onProgress:(current,total,phase)=>{const safeTotal=Math.max(1,Number(total)||1);const safeCurrent=Math.max(0,Math.min(safeTotal,Number(current)||0));report({message:phase||'Restaurando la visibilidad de los elementos...',current:safeCurrent,total:safeTotal,details:{current:safeCurrent,total:safeTotal}});}};const visibilityTargetMeshes=needsGlobalVisibilityReset?null:(hiddenBeforeReset.size&&this.getMeshesForElementKeys?this.getMeshesForElementKeys(hiddenBeforeReset):[]);if(needsGlobalVisibilityReset||(visibilityTargetMeshes&&visibilityTargetMeshes.length)){visibilityResult=this.applyVisibilityStateBatched?await this.applyVisibilityStateBatched(visibilityTargetMeshes,visibilityOptions):this.applyVisibilityState(visibilityTargetMeshes,visibilityOptions);}else{visibilityResult={changedKeys:0,total:0};report({message:'Visibilidad sin cambios.',current:1,total:1});}ensureCurrent();await update('Restaurando la apariencia original del modelo...',0,0);if(needsGlobalVisibilityReset){this.updateVisualAppearanceForAll&&this.updateVisualAppearanceForAll({skipChunkRefresh:true,skipSectionRefresh:true,skipRender:true});}else if(resetAppearanceKeys.size&&this.updateVisualAppearanceForKeys){this.updateVisualAppearanceForKeys(resetAppearanceKeys,{skipChunkRefresh:true,skipSectionRefresh:true});}if(this.endStrictIsolationVisibilityGuard)this.endStrictIsolationVisibilityGuard('reset-all-view');await update('Recuperando los bloques optimizados del modelo...',0,0);const signatureBefore=this.getNormalChunkSignature?this.getNormalChunkSignature():null;const cachedSignature=this.normalChunkCacheSignature||null;const canReuseCachedChunks=!!(signatureBefore&&cachedSignature&&signatureBefore===cachedSignature&&this.normalChunkMeshes&&this.normalChunkMeshes.length);const deferSmallHiddenRecovery=!needsGlobalVisibilityReset&&!hadGlobalTemporaryAppearance&&hiddenBeforeReset.size>0&&hiddenBeforeReset.size<=256;const hasAnyResetRenderChange=needsGlobalVisibilityReset||hadGlobalTemporaryAppearance||hiddenBeforeReset.size>0;if(deferSmallHiddenRecovery){chunkCount=(this.normalChunkMeshes||[]).length;chunkMode='deferred-incremental';if(this.deferNormalChunkRefresh&&!(this.visualMassState&&this.visualMassState.active)){this.deferNormalChunkRefresh('reset-all-small-hidden',1600);}}else if(!hasAnyResetRenderChange){chunkCount=(this.normalChunkMeshes||[]).length;chunkMode='unchanged';}else if(this.rebuildNormalChunks&&this.clashModeActive!==true){chunkCount=this.rebuildNormalChunks(false);chunkMode=canReuseCachedChunks?'reused':'rebuilt';}if(chunkCount>0&&(!this.normalChunkEdgeBatches||!this.normalChunkEdgeBatches.length)&&this.refreshNormalChunkEdgesForVisibleChunks){await update('Restaurando los bordes visibles...',0,0);this.refreshNormalChunkEdgesForVisibleChunks('reset-all-view-missing-edges');}else{await update(chunkMode==='reused'?'Bloques y bordes recuperados desde la caché.':(chunkMode==='deferred-incremental'?'Elementos restaurados; reagrupación diferida hasta que el visor quede libre.':(chunkMode==='unchanged'?'La representación gráfica ya estaba restaurada.':'Bloques y bordes reconstruidos.')),0,0);}if(this.invalidateSelectionCandidateCache)this.invalidateSelectionCandidateCache('reset-all-view');if(this.invalidateRaycastScreenCandidateCache)this.invalidateRaycastScreenCandidateCache('reset-all-view');if(this.invalidateMeasurementPickCache)this.invalidateMeasurementPickCache('reset-all-view');this.syncViewerToolButtons&&this.syncViewerToolButtons();this.requestRender&&this.requestRender('reset-all-view');await update('Finalizando el restablecimiento...',0,0);if(this.recordPerformanceOperation)this.recordPerformanceOperation('resetAllView:complete',(this.getPerformanceNow?this.getPerformanceNow():Date.now())-metricsStart,{changedKeys:visibilityResult&&visibilityResult.changedKeys?visibilityResult.changedKeys:0,totalMeshes:visibilityResult&&visibilityResult.total?visibilityResult.total:(this.meshes?this.meshes.length:0),chunks:chunkCount,chunkMode,duplicateEdgeRefreshAvoided:true});report({message:'Vista restablecida correctamente.',current:100,total:100,details:{chunkMode,chunks:chunkCount}});return{changedKeys:visibilityResult&&visibilityResult.changedKeys?visibilityResult.changedKeys:0,total:visibilityResult&&visibilityResult.total?visibilityResult.total:0,chunks:chunkCount,chunkMode};}catch(error){if(this.recordPerformanceOperation)this.recordPerformanceOperation('resetAllView:error',(this.getPerformanceNow?this.getPerformanceNow():Date.now())-metricsStart,{message:error&&error.message?error.message:String(error)});throw error;}finally{if(this.resetAllViewSequence===resetToken)this.resetAllViewActive=false;}}getElementPropertyValue(element,names){if(!element)return'';for(const name of names||[]){if(Object.prototype.hasOwnProperty.call(element,name)&&element[name]!=null&&String(element[name]).trim()!==''){return element[name];}}const normalizeName=value=>this.normalizeTextForSearch(value).replace(/[^a-z0-9]+/g,'');const targetNames=new Set((names||[]).map(normalizeName));const containers=[element.Parameters,element.ParametersByName,element.Properties,element.Parametros,element.Propiedades];for(const container of containers){if(!container)continue;if(Array.isArray(container)){for(const item of container){if(!item)continue;const itemName=item.Name||item.name||item.ParameterName||item.parameterName||item.Key||item.key;if(!itemName||!targetNames.has(normalizeName(itemName)))continue;const value=item.Value??item.value??item.DisplayValue??item.displayValue??item.AsString??item.asString;if(value!=null&&String(value).trim()!=='')return value;}}else if(typeof container==='object'){for(const[key,value]of Object.entries(container)){if(!targetNames.has(normalizeName(key)))continue;if(value!=null&&typeof value==='object'){const nested=value.Value??value.value??value.DisplayValue??value.displayValue??value.AsString??value.asString;if(nested!=null&&String(nested).trim()!=='')return nested;}else if(value!=null&&String(value).trim()!==''){return value;}}}}return'';}normalizeTypeKeyPart(value){return this.selectionController.dispatch('state','normalizeTypeKeyPart',this,arguments);}stripProjectPrefixForTypeKey(value,element){return this.selectionController.dispatch('state','stripProjectPrefixForTypeKey',this,arguments);}getPortableElementPropertyValue(element,names){return this.selectionController.dispatch('state','getPortableElementPropertyValue',this,arguments);}getElementTypeSelectionKey(element,options){return this.selectionController.dispatch('state','getElementTypeSelectionKey',this,arguments);}getElementTypeSelectionKeys(element,options){return this.selectionController.dispatch('state','getElementTypeSelectionKeys',this,arguments);}selectSameTypesFromSelection(notify,options){return this.selectionController.dispatch('state','selectSameTypesFromSelection',this,arguments);}selectElement(elementKey,notify){return this.selectionController.dispatch('state','selectElement',this,arguments);}selectElementKeys(elementKeys,notify,options){return this.selectionController.dispatch('state','selectElementKeys',this,arguments);}invalidateSelectionCandidateCache(reason){return this.selectionController.dispatch('raycast','invalidateSelectionCandidateCache',this,arguments);}updateSelectionIndexesForVisibilityChange(meshes,visible,reason){return this.selectionController.dispatch('raycast','updateSelectionIndexesForVisibilityChange',this,arguments);}isMeshSelectableForUser(mesh){return this.selectionController.dispatch('raycast','isMeshSelectableForUser',this,arguments);}isMeshSelectableForSimilarSelection(mesh,options){return this.selectionController.dispatch('raycast','isMeshSelectableForSimilarSelection',this,arguments);}getSelectableMeshCandidates(){return this.selectionController.dispatch('raycast','getSelectableMeshCandidates',this,arguments);}invalidateRaycastScreenCandidateCache(reason){return this.selectionController.dispatch('raycast','invalidateRaycastScreenCandidateCache',this,arguments);}getRaycastScreenCandidateSignature(candidates,width,height){return this.selectionController.dispatch('raycast','getRaycastScreenCandidateSignature',this,arguments);}buildRaycastScreenCandidateIndex(candidates,width,height){return this.selectionController.dispatch('raycast','buildRaycastScreenCandidateIndex',this,arguments);}getPointRaycastCandidates(x,y,candidates){return this.selectionController.dispatch('raycast','getPointRaycastCandidates',this,arguments);}ensureInteractiveMeshesVisibleForKeys(keys){return this.selectionController.dispatch('raycast','ensureInteractiveMeshesVisibleForKeys',this,arguments);}schedulePreselectFromScreen(x,y){return this.selectionController.dispatch('hover','schedulePreselectFromScreen',this,arguments);}cancelScheduledPreselect(){return this.selectionController.dispatch('hover','cancelScheduledPreselect',this,arguments);}preselectFromScreen(x,y){return this.selectionController.dispatch('hover','preselectFromScreen',this,arguments);}pickMeshFromScreen(x,y){return this.selectionController.dispatch('raycast','pickMeshFromScreen',this,arguments);}filterRaycastHitsBySection(hits){return this.selectionController.dispatch('raycast','filterRaycastHitsBySection',this,arguments);}isPointInsideCurrentSection(point){const tolerance=0.00001;for(const plane of this.sectionPlanes||[]){if(plane.distanceToPoint(point)<-tolerance)return false;}return true;}getPreferredRaycastHit(hits){return this.selectionController.dispatch('raycast','getPreferredRaycastHit',this,arguments);}isFadedObject(object){const key=object&&object.userData?object.userData.elementKey:null;return!!key&&this.fadedMeshKeys&&this.fadedMeshKeys.has(key);}restoreHoveredMaterial(){return this.selectionController.dispatch('hover','restoreHoveredMaterial',this,arguments);}getBaseEdgeMaterial(mesh){return this.getOriginalBaseEdgeMaterial(mesh);}setEdgeMaterial(mesh,material,forceVisible,renderOrder){if(!mesh||!material)return false;const order=Number.isFinite(Number(renderOrder))?Number(renderOrder):null;let changed=false;if(mesh.isLineSegments){if(mesh.material!==material){mesh.material=material;changed=true;}if(order!=null&&mesh.renderOrder!==order){mesh.renderOrder=order;changed=true;}if(forceVisible&&mesh.visible!==true){mesh.visible=true;changed=true;}return changed;}for(const child of mesh.children||[]){if(!child||!child.isLineSegments)continue;if(child.material!==material){child.material=material;changed=true;}if(order!=null&&child.renderOrder!==order){child.renderOrder=order;changed=true;}const suppressGroupedEdge=this.shouldGroupTransparentEdgeChild(child,material);const sourceEdgeVisible=!!(mesh.visible!==false&&mesh.userData&&mesh.userData.baseVisible!==false&&!mesh.userData.normalChunkSuppressed&&!mesh.userData.massTransparencyChunkSuppressed&&!mesh.userData.visualStateChunkSuppressed);const targetVisible=suppressGroupedEdge?false:(forceVisible||sourceEdgeVisible);if(suppressGroupedEdge){if(!child.userData)child.userData={};child.userData.groupedTransparentSuppressed=true;}else if(child.userData&&child.userData.groupedTransparentSuppressed){child.userData.groupedTransparentSuppressed=false;}if(child.visible!==targetVisible){child.visible=targetVisible;changed=true;}}return changed;}selectFromScreen(x,y){return this.selectionController.dispatch('state','selectFromScreen',this,arguments);}selectMesh(mesh,notify){return this.selectionController.dispatch('state','selectMesh',this,arguments);}applySelectionOperation(meshes,notify){return this.selectionController.dispatch('state','applySelectionOperation',this,arguments);}selectMeshes(meshes,notify,options){return this.selectionController.dispatch('state','selectMeshes',this,arguments);}scheduleRectanglePreview(rect){return this.selectionController.dispatch('box','scheduleRectanglePreview',this,arguments);}cancelRectanglePreview(restore){return this.selectionController.dispatch('box','cancelRectanglePreview',this,arguments);}getVisibleMeshCountForRectangleSelection(){return this.selectionController.dispatch('box','getVisibleMeshCountForRectangleSelection',this,arguments);}previewRectangleSelection(rect){return this.selectionController.dispatch('box','previewRectangleSelection',this,arguments);}restoreRectanglePreview(){return this.selectionController.dispatch('box','restoreRectanglePreview',this,arguments);}selectByRectangle(rect,notify){return this.selectionController.dispatch('box','selectByRectangle',this,arguments);}getMeshesInRectangle(rect,options){return this.selectionController.dispatch('box','getMeshesInRectangle',this,arguments);}getProjectedRectOverlapRatio(projectedBox,rect){return this.selectionController.dispatch('box','getProjectedRectOverlapRatio',this,arguments);}getMeshSelectionWorldBox(mesh){return this.selectionController.dispatch('box','getMeshSelectionWorldBox',this,arguments);}projectBoxToScreen(box,width,height,points){return this.selectionController.dispatch('box','projectBoxToScreen',this,arguments);}meshHasProjectedGeometryInRect(mesh,rect,width,height,options){return this.selectionController.dispatch('box','meshHasProjectedGeometryInRect',this,arguments);}projectedPointInRect(point,rect,width,height){return this.selectionController.dispatch('box','projectedPointInRect',this,arguments);}updateOrbitPivotFromSelection(){return this.selectionController.dispatch('state','updateOrbitPivotFromSelection',this,arguments);}restoreSelectedMaterial(){return this.selectionController.dispatch('state','restoreSelectedMaterial',this,arguments);}isSelectedMesh(mesh){return this.selectionController.dispatch('state','isSelectedMesh',this,arguments);}clearSelection(notify){return this.selectionController.dispatch('state','clearSelection',this,arguments);}getMeshWorldBoxVolume(mesh){if(!mesh)return Number.POSITIVE_INFINITY;try{const box=new THREE.Box3().setFromObject(mesh);if(!box||box.isEmpty())return Number.POSITIVE_INFINITY;const size=box.getSize(new THREE.Vector3());return Math.max(0,size.x)*Math.max(0,size.y)*Math.max(0,size.z);}catch{return Number.POSITIVE_INFINITY;}}getClashMeshProjectId(mesh){const data=mesh&&mesh.userData?mesh.userData:null;const element=data?data.element:null;return String((data&&(data.projectId||data.ProjectId))||(element&&element.ProjectId)||'');}getClashContextSignature(contextProjectIds){const values=Array.from(contextProjectIds||[]).map(id=>String(id||'')).filter(Boolean).sort();return values.length?('projects:'+values.join(',')):'projects:__all__';}createClashVisualStateFromMaterials(material,edgeMaterial,renderOrder,generalTransparency){const color=material&&material.color&&material.color.isColor?material.color.getHex():0xbec7d3;const opacity=material&&Number.isFinite(Number(material.opacity))?Number(material.opacity):1;const edgeColor=edgeMaterial&&edgeMaterial.color&&edgeMaterial.color.isColor?edgeMaterial.color.getHex():this.darkenColorValue(color,0.3);const edgeOpacity=edgeMaterial&&Number.isFinite(Number(edgeMaterial.opacity))?Number(edgeMaterial.opacity):opacity;return{color,opacity,edgeColor,edgeOpacity,generalTransparency:generalTransparency===true,renderOrder:Number.isFinite(Number(renderOrder))?Number(renderOrder):0};}setClashStateForKey(key,state,edgeMaterial,changedKeys){if(!key||!state)return;this.visualClashStates.set(key,state);if(this.clashEdgeMaterialByKey)this.clashEdgeMaterialByKey.set(key,edgeMaterial||null);if(changedKeys)changedKeys.add(key);}releaseClashKeyFromVisualChunk(key){if(!key||!this.getMeshesForElementKey)return;const meshes=this.getMeshesForElementKey(key)||[];for(const mesh of meshes){if(mesh&&mesh.userData&&mesh.userData.visualStateChunkSuppressed===true){mesh.userData.visualStateChunkSuppressed=false;}}if(!this.activeClashReleasedKeys)this.activeClashReleasedKeys=new Set();this.activeClashReleasedKeys.add(key);}buildClashContextState(keyA,keyB,contextProjectIds,states){const metricsStart=this.getPerformanceNow?this.getPerformanceNow():Date.now();if(!this.visualClashForceVisibleKeys)this.visualClashForceVisibleKeys=new Set();if(!this.visualClashHiddenKeys)this.visualClashHiddenKeys=new Set();if(!this.activeClashContextVisibleKeys)this.activeClashContextVisibleKeys=new Set();if(!this.activeClashReleasedKeys)this.activeClashReleasedKeys=new Set();this.visualClashForceVisibleKeys.clear();this.visualClashHiddenKeys.clear();this.visualClashStates.clear();if(this.clashEdgeMaterialByKey)this.clashEdgeMaterialByKey.clear();this.activeClashContextVisibleKeys.clear();this.activeClashReleasedKeys.clear();const changedKeys=new Set();const hasContextFilter=contextProjectIds&&contextProjectIds.size>0;let contextKeys=0;let hiddenKeys=0;let fadedKeys=0;for(const mesh of this.meshes||[]){if(!mesh||!mesh.userData)continue;const key=mesh.userData.elementKey;if(!key)continue;const projectId=this.getClashMeshProjectId?this.getClashMeshProjectId(mesh):'';const isContextProject=!hasContextFilter||contextProjectIds.has(projectId);const isClashTarget=key===keyA||key===keyB;const baseVisible=!(mesh.userData&&mesh.userData.baseVisible===false);if(!isContextProject||(!baseVisible&&!isClashTarget)){this.visualClashHiddenKeys.add(key);changedKeys.add(key);hiddenKeys++;continue;}contextKeys++;if(baseVisible)this.activeClashContextVisibleKeys.add(key);if(isClashTarget)this.visualClashForceVisibleKeys.add(key);if(key===keyA){this.setClashStateForKey(key,states.a,states.edgeA,changedKeys);}else if(key===keyB){this.setClashStateForKey(key,states.b,states.edgeB,changedKeys);}else{this.setClashStateForKey(key,states.faded,states.edgeFaded,changedKeys);fadedKeys++;}}this.clashPriorityKeys=new Set([keyA,keyB]);this.activeClashPairKeys=new Set([keyA,keyB]);this.updateVisualAppearanceForKeys(changedKeys,{forceChunkRebuild:true,skipSectionRefresh:true});if(this.recordPerformanceOperation)this.recordPerformanceOperation('clash:context-build',(this.getPerformanceNow?this.getPerformanceNow():Date.now())-metricsStart,{changedKeys:changedKeys.size,contextKeys,hiddenKeys,fadedKeys,projectFilter:hasContextFilter?contextProjectIds.size:0});}enterExclusiveClashMode(){if(this.clashModeActive===true)return;this.clashModeActive=true;this.clashPreviousVisualSelectionOverlayOnly=this.visualSelectionOverlayOnly;this.clashPreviousVisualInteractionOverlayOnly=this.visualInteractionOverlayOnly;this.clashPreviousHoverVisualMutatesGeometry=this.hoverVisualMutatesGeometry;this.visualSelectionOverlayOnly=true;this.visualInteractionOverlayOnly=true;this.hoverVisualMutatesGeometry=false;if(this.recordPerformanceOperation)this.recordPerformanceOperation('clash:exclusive-enter',0,{});}suspendNormalChunksForClashMode(reason){const metricsStart=this.getPerformanceNow?this.getPerformanceNow():Date.now();let removedChunks=0;let removedEdges=0;if(this.normalChunkMeshes&&this.normalChunkMeshes.length){for(const chunk of this.normalChunkMeshes){if(!chunk)continue;if(this.releaseAdaptiveMediumDepthProxy)this.releaseAdaptiveMediumDepthProxy(chunk,false);if(chunk.parent){chunk.parent.remove(chunk);removedChunks++;}}}if(this.clearNormalChunkEdges)removedEdges=this.clearNormalChunkEdges(false)||0;this.normalChunkVisible=false;this.clashNormalChunksSuspended=true;if(this.recordPerformanceOperation)this.recordPerformanceOperation('normalChunks:suspend-clash-mode',(this.getPerformanceNow?this.getPerformanceNow():Date.now())-metricsStart,{reason:reason||'clash',removedChunks,removedEdges,hiddenKeys:this.normalChunkHiddenKeys?this.normalChunkHiddenKeys.size:0});}exitExclusiveClashMode(options){const opts=options&&typeof options==='object'?options:{};if(this.clashModeActive!==true&&this.clashNormalChunksSuspended!==true)return;const metricsStart=this.getPerformanceNow?this.getPerformanceNow():Date.now();this.clashModeActive=false;if(this.clashPreviousVisualSelectionOverlayOnly!==null)this.visualSelectionOverlayOnly=this.clashPreviousVisualSelectionOverlayOnly;if(this.clashPreviousVisualInteractionOverlayOnly!==null)this.visualInteractionOverlayOnly=this.clashPreviousVisualInteractionOverlayOnly;if(this.clashPreviousHoverVisualMutatesGeometry!==null)this.hoverVisualMutatesGeometry=this.clashPreviousHoverVisualMutatesGeometry;this.clashPreviousVisualSelectionOverlayOnly=null;this.clashPreviousVisualInteractionOverlayOnly=null;this.clashPreviousHoverVisualMutatesGeometry=null;const hadSuspendedNormalChunks=this.clashNormalChunksSuspended===true;this.clashNormalChunksSuspended=false;if(opts.rebuildNormalChunks!==false&&hadSuspendedNormalChunks){this.normalChunkCacheSignature=null;if(this.deferNormalChunkRefresh)this.deferNormalChunkRefresh('clash-mode-exit');}if(this.recordPerformanceOperation)this.recordPerformanceOperation('clash:exclusive-exit',(this.getPerformanceNow?this.getPerformanceNow():Date.now())-metricsStart,{rebuildNormalChunks:opts.rebuildNormalChunks!==false,hadSuspendedNormalChunks});}prepareViewerForClashMode(){const metricsStart=this.getPerformanceNow?this.getPerformanceNow():Date.now();if(this.enterExclusiveClashMode)this.enterExclusiveClashMode();let clearedTimers=0;if(this.normalChunkRefreshDeferTimer){clearTimeout(this.normalChunkRefreshDeferTimer);this.normalChunkRefreshDeferTimer=null;clearedTimers++;}if(this.suspendNormalChunksForClashMode)this.suspendNormalChunksForClashMode('prepare-viewer');if(this.clearMassTransparencyChunks)this.clearMassTransparencyChunks(false);if(this.clearGroupedTransparentEdges)this.clearGroupedTransparentEdges(false);this.viewerMassTransparencyState=null;this.dataMassTransparencyState=null;if(this.visualMassState&&this.visualMassState.active)this.visualMassState=null;if(this.fadedMeshKeys)this.fadedMeshKeys.clear();this.fadeActive=false;if(this.recordPerformanceOperation)this.recordPerformanceOperation('clash:prepare-viewer',(this.getPerformanceNow?this.getPerformanceNow():Date.now())-metricsStart,{clearedTimers,normalChunks:this.normalChunkMeshes?this.normalChunkMeshes.length:0,visualChunks:this.visualStateChunkMeshes?this.visualStateChunkMeshes.length:0});}updateClashPairOnly(keyA,keyB,states){const metricsStart=this.getPerformanceNow?this.getPerformanceNow():Date.now();if(!this.visualClashForceVisibleKeys)this.visualClashForceVisibleKeys=new Set();if(!this.visualClashHiddenKeys)this.visualClashHiddenKeys=new Set();if(!this.activeClashPairKeys)this.activeClashPairKeys=new Set();if(!this.activeClashContextVisibleKeys)this.activeClashContextVisibleKeys=new Set();const previous=new Set(this.activeClashPairKeys||[]);const next=new Set([keyA,keyB]);const changedKeys=new Set();for(const key of previous){if(!key||next.has(key))continue;this.visualClashForceVisibleKeys.delete(key);if(this.activeClashContextVisibleKeys.has(key)){this.setClashStateForKey(key,states.faded,states.edgeFaded,changedKeys);this.visualClashHiddenKeys.delete(key);}else{this.visualClashStates.delete(key);if(this.clashEdgeMaterialByKey)this.clashEdgeMaterialByKey.delete(key);this.visualClashHiddenKeys.add(key);changedKeys.add(key);}}this.visualClashHiddenKeys.delete(keyA);this.visualClashHiddenKeys.delete(keyB);this.visualClashForceVisibleKeys.clear();this.visualClashForceVisibleKeys.add(keyA);this.visualClashForceVisibleKeys.add(keyB);this.clashPriorityKeys=new Set([keyA,keyB]);this.setClashStateForKey(keyA,states.a,states.edgeA,changedKeys);this.setClashStateForKey(keyB,states.b,states.edgeB,changedKeys);this.activeClashPairKeys=next;if(changedKeys.size)this.updateVisualAppearanceForKeys(changedKeys,{skipChunkRefresh:true,skipSectionRefresh:true});if(this.recordPerformanceOperation)this.recordPerformanceOperation('clash:pair-swap',(this.getPerformanceNow?this.getPerformanceNow():Date.now())-metricsStart,{changedKeys:changedKeys.size,previousKeys:previous.size,nextKeys:next.size,releasedKeys:this.activeClashReleasedKeys?this.activeClashReleasedKeys.size:0});}clearClashContactZone(){const group=this.clashContactZoneGroup;if(!group)return;if(group.parent)group.parent.remove(group);group.traverse(object=>{if(object.geometry&&object.geometry.dispose)object.geometry.dispose();const materials=Array.isArray(object.material)?object.material:[object.material];for(const material of materials){if(material&&material.dispose)material.dispose();}});this.clashContactZoneGroup=null;if(this.requestRender)this.requestRender('clash-contact-zone-clear');}showClashContactZone(contactZone){this.clearClashContactZone();if(!contactZone||!this.scene||typeof THREE==='undefined')return false;const group=new THREE.Group();group.name='ExportModel_Clash_Contact_Zone';group.userData.isClashContactZone=true;const purple=0xc084fc;const darkPurple=0xa855f7;let rendered=false;if(contactZone.mode==='volume'&&contactZone.exact===true&&Array.isArray(contactZone.positions)&&contactZone.positions.length>=12){const geometry=new THREE.BufferGeometry();geometry.setAttribute('position',new THREE.Float32BufferAttribute(contactZone.positions,3));if(Array.isArray(contactZone.indices)&&contactZone.indices.length>=12)geometry.setIndex(contactZone.indices);geometry.computeVertexNormals();geometry.computeBoundingBox();geometry.computeBoundingSphere();const material=new THREE.MeshBasicMaterial({color:purple,side:THREE.DoubleSide,transparent:true,opacity:0.46,depthTest:true,depthWrite:false,polygonOffset:true,polygonOffsetFactor:-1,polygonOffsetUnits:-1});material.toneMapped=false;const surface=new THREE.Mesh(geometry,material);surface.name='ExportModel_Clash_Contact_Volume';surface.renderOrder=75;surface.frustumCulled=true;group.add(surface);const edgeGeometry=new THREE.EdgesGeometry(geometry,38);const edgeMaterial=new THREE.LineBasicMaterial({color:darkPurple,transparent:true,opacity:0.78,depthTest:true,depthWrite:false});edgeMaterial.toneMapped=false;const edges=new THREE.LineSegments(edgeGeometry,edgeMaterial);edges.name='ExportModel_Clash_Contact_Edges';edges.renderOrder=76;edges.frustumCulled=true;group.add(edges);rendered=true;}else if(contactZone.mode==='contour'&&Array.isArray(contactZone.positions)&&contactZone.positions.length>=6){const contourGeometry=new THREE.BufferGeometry();contourGeometry.setAttribute('position',new THREE.Float32BufferAttribute(contactZone.positions,3));contourGeometry.computeBoundingBox();contourGeometry.computeBoundingSphere();const contourMaterial=new THREE.LineBasicMaterial({color:darkPurple,transparent:true,opacity:0.94,depthTest:true,depthWrite:false});contourMaterial.toneMapped=false;const contour=new THREE.LineSegments(contourGeometry,contourMaterial);contour.name='ExportModel_Clash_Contact_Contour';contour.renderOrder=76;contour.frustumCulled=true;group.add(contour);rendered=true;}if(!rendered){group.traverse(object=>{if(object.geometry&&object.geometry.dispose)object.geometry.dispose();if(object.material&&object.material.dispose)object.material.dispose();});return false;}this.scene.add(group);this.clashContactZoneGroup=group;if(this.requestRender)this.requestRender('clash-contact-zone-show');return true;}showClashPair(elementKeyA,elementKeyB,options){const keyA=String(elementKeyA||'');const keyB=String(elementKeyB||'');if(!keyA||!keyB||!this.meshByElementKey)return false;const meshA=this.meshByElementKey.get(keyA);const meshB=this.meshByElementKey.get(keyB);if(!meshA||!meshB)return false;const metricsStart=this.getPerformanceNow?this.getPerformanceNow():Date.now();const opts=options&&typeof options==='object'?options:{};const evaluatedProjectIds=new Set([...(Array.isArray(opts.groupAProjectIds)?opts.groupAProjectIds:[]),...(Array.isArray(opts.groupBProjectIds)?opts.groupBProjectIds:[]),...(Array.isArray(opts.evaluatedProjectIds)?opts.evaluatedProjectIds:[])].map(id=>String(id||'')).filter(Boolean));const contextProjectIds=new Set([...Array.from(evaluatedProjectIds),...(Array.isArray(opts.contextVisibleProjectIds)?opts.contextVisibleProjectIds:[])].map(id=>String(id||'')).filter(Boolean));const contextSignature=this.getClashContextSignature?this.getClashContextSignature(contextProjectIds):String(contextProjectIds.size);const clashGreen=0x86efac;const clashOrange=0xf97316;const transparentColor=this.getConfiguredTransparencyColor(0xbec7d3);const transparentOpacity=this.getConfiguredTransparencyOpacity(0.02);const greenMaterial=this.getSimpleColorMaterial(clashGreen,0.56,true);const orangeMaterial=this.getSimpleColorMaterial(clashOrange,0.74,true);const fadedMaterial=this.getSimpleColorMaterial(transparentColor,transparentOpacity,true);const greenEdge=this.getDarkerEdgeMaterialForColor(clashGreen,0.95,true);const orangeEdge=this.getDarkerEdgeMaterialForColor(clashOrange,1,true);const fadedEdge=this.getDarkerEdgeMaterialForColor(transparentColor,Math.max(0.04,transparentOpacity),true);const volumeA=this.getMeshWorldBoxVolume(meshA);const volumeB=this.getMeshWorldBoxVolume(meshB);const renderOrderA=volumeA<=volumeB?42:41;const renderOrderB=volumeB<volumeA?42:41;const states={a:this.createClashVisualStateFromMaterials(greenMaterial,greenEdge,renderOrderA,false),b:this.createClashVisualStateFromMaterials(orangeMaterial,orangeEdge,renderOrderB,false),faded:this.createClashVisualStateFromMaterials(fadedMaterial,fadedEdge,0,true),edgeA:greenEdge,edgeB:orangeEdge,edgeFaded:fadedEdge};if(this.prepareViewerForClashMode)this.prepareViewerForClashMode();const sameContext=this.activeClashContextSignature===contextSignature&&this.visualClashStates&&this.visualClashStates.size&&this.activeClashContextVisibleKeys;if(!sameContext){this.restoreFadedElements();this.clearWorksProgressVisualization();this.clearClashHighlight({keepClashModeActive:true,skipNormalChunkRebuild:true});this.activeClashContextSignature=contextSignature;this.activeClashContextProjectIds=new Set(contextProjectIds);this.clearSelection(false);this.buildClashContextState(keyA,keyB,contextProjectIds,states);}else{this.clearSelection(false);this.updateClashPairOnly(keyA,keyB,states);}if(this.invalidateSelectionCandidateCache)this.invalidateSelectionCandidateCache('clash-pair-active');if(this.showClashContactZone)this.showClashContactZone(opts.contactZone||null);const box=this.getRawFitBoxForMeshes([meshA,meshB]);if(box&&!box.isEmpty()){const sectionBox=this.enableSectionBoxForMeshesFastForClash?this.enableSectionBoxForMeshesFastForClash([meshA,meshB],0.16):(this.enableSectionBoxForBoxFastForClash?this.enableSectionBoxForBoxFastForClash(box,0.16):this.enableSectionBoxForBox(box,0.16,{fast:true,skipCapsAndCutEdges:true}));this.fitToBox(sectionBox||box);const sectionCenter=this.getSectionBoxCentroid?this.getSectionBoxCentroid():null;this.orbitPivot=sectionCenter||box.getCenter(new THREE.Vector3());}if(this.recordPerformanceOperation)this.recordPerformanceOperation('clash:section-refresh-skip-fast',0,{active:!!this.sectionBoxActive});if(this.recordPerformanceOperation)this.recordPerformanceOperation('clash:showPairCached',(this.getPerformanceNow?this.getPerformanceNow():Date.now())-metricsStart,{sameContext:!!sameContext,contextProjects:contextProjectIds.size,pairA:keyA,pairB:keyB});return true;}applyClashMaterial(mesh,material,edgeMaterial,renderOrder){if(!mesh||!material)return;const key=this.getMeshKey(mesh);if(!key)return;const descriptor=this.extractColorOpacityFromMaterial?this.extractColorOpacityFromMaterial(material,0xbec7d3,1):{color:material.color&&material.color.isColor?material.color:new THREE.Color(0xbec7d3),opacity:material.opacity==null?1:material.opacity};const edgeDescriptor=edgeMaterial&&edgeMaterial.color&&edgeMaterial.color.isColor?{color:edgeMaterial.color.getHex(),opacity:edgeMaterial.opacity==null?descriptor.opacity:edgeMaterial.opacity}:{color:this.darkenColorValue(descriptor.color.getHex(),0.3),opacity:descriptor.opacity};this.visualClashStates.set(key,{color:descriptor.color.getHex(),opacity:Math.max(0.02,Math.min(1,Number(descriptor.opacity==null?1:descriptor.opacity))),edgeColor:edgeDescriptor.color,edgeOpacity:Math.max(0.02,Math.min(1,Number(edgeDescriptor.opacity==null?descriptor.opacity:edgeDescriptor.opacity))),renderOrder:Number.isFinite(Number(renderOrder))?Number(renderOrder):0});this.clashEdgeMaterialByKey.set(key,edgeMaterial||null);this.updateVisualAppearanceForKeys([key],{skipChunkRefresh:this.clashModeActive===true,skipSectionRefresh:this.clashModeActive===true});}clearClashHighlight(options){const opts=options&&typeof options==='object'?options:{};const keepClashModeActive=opts.keepClashModeActive===true;if(this.clearClashContactZone)this.clearClashContactZone();const changedKeys=new Set(this.visualClashStates?this.visualClashStates.keys():[]);if(this.activeClashReleasedKeys&&this.activeClashReleasedKeys.size){for(const key of this.activeClashReleasedKeys)changedKeys.add(key);this.activeClashReleasedKeys.clear();}this.activeClashContextSignature=null;if(this.activeClashContextProjectIds)this.activeClashContextProjectIds.clear();if(this.activeClashContextVisibleKeys)this.activeClashContextVisibleKeys.clear();if(this.activeClashPairKeys)this.activeClashPairKeys.clear();if(this.visualClashForceVisibleKeys&&this.visualClashForceVisibleKeys.size){for(const key of this.visualClashForceVisibleKeys)changedKeys.add(key);this.visualClashForceVisibleKeys.clear();}if(this.visualClashHiddenKeys&&this.visualClashHiddenKeys.size){for(const key of this.visualClashHiddenKeys)changedKeys.add(key);this.visualClashHiddenKeys.clear();}this.visualClashStates.clear();if(this.clashMaterialStates&&this.clashMaterialStates.size){for(const[mesh]of this.clashMaterialStates.entries()){const key=this.getMeshKey(mesh);if(key)changedKeys.add(key);}this.clashMaterialStates.clear();}if(this.clashRenderOrderStates&&this.clashRenderOrderStates.size){for(const[mesh]of this.clashRenderOrderStates.entries()){const key=this.getMeshKey(mesh);if(key)changedKeys.add(key);}this.clashRenderOrderStates.clear();}if(this.clashEdgeStates&&this.clashEdgeStates.size){this.clashEdgeStates.clear();}if(this.clashEdgeMaterialByKey)this.clashEdgeMaterialByKey.clear();if(this.clashPriorityKeys)this.clashPriorityKeys.clear();if(this.clashBoxHelper){this.scene.remove(this.clashBoxHelper);if(this.clashBoxHelper.geometry&&this.clashBoxHelper.geometry.dispose)this.clashBoxHelper.geometry.dispose();if(this.clashBoxHelper.material&&this.clashBoxHelper.material.dispose)this.clashBoxHelper.material.dispose();this.clashBoxHelper=null;}if(!keepClashModeActive&&this.exitExclusiveClashMode){this.exitExclusiveClashMode({rebuildNormalChunks:opts.skipNormalChunkRebuild!==true});}this.updateVisualAppearanceForKeys(changedKeys,{forceChunkRebuild:!keepClashModeActive,skipChunkRefresh:keepClashModeActive||opts.skipNormalChunkRebuild===true,skipSectionRefresh:keepClashModeActive||opts.skipSectionRefresh===true});}setClashBoxHelper(box){if(!box||box.isEmpty())return;if(this.clashBoxHelper){this.scene.remove(this.clashBoxHelper);if(this.clashBoxHelper.geometry&&this.clashBoxHelper.geometry.dispose)this.clashBoxHelper.geometry.dispose();if(this.clashBoxHelper.material&&this.clashBoxHelper.material.dispose)this.clashBoxHelper.material.dispose();this.clashBoxHelper=null;}const helper=new THREE.Box3Helper(box,0xfacc15);helper.name='ExportModel_Clash_Box';helper.renderOrder=998;if(helper.material){helper.material.depthTest=false;helper.material.depthWrite=false;helper.material.transparent=true;helper.material.opacity=0.95;}this.scene.add(helper);this.clashBoxHelper=helper;}fitToSelectedOrModel(){if(this.selectedMeshes&&this.selectedMeshes.length){const box=this.getFitBoxForMeshes(this.selectedMeshes);const pivot=this.lastSelectionOrbitPivot&&this.lastSelectionOrbitPivot.clone?this.lastSelectionOrbitPivot.clone():(box&&!box.isEmpty()?box.getCenter(new THREE.Vector3()):null);this.fitToBox(box);if(pivot)this.orbitPivot=pivot;}else this.fitToModel();}setStandardView(view){const targets=this.selectedMeshes&&this.selectedMeshes.length?this.selectedMeshes:this.meshes.filter(mesh=>mesh.visible);const box=this.getFitBoxForMeshes(targets);if(box&&!box.isEmpty()){const size=box.getSize(new THREE.Vector3());this.target.copy(box.getCenter(new THREE.Vector3()));const maxSize=Math.max(size.x,size.y,size.z,1);const depthSize=Math.max(maxSize,this.getFullModelDepthSize());this.distance=this.getFitCameraDistance(maxSize,depthSize);this.orthoScale=maxSize*1.12;}const direction=this.getStandardViewDirection(view);if(!direction){this.theta=-0.75;this.phi=1.1;this.orbitPivot=null;this.applyCamera();return;}const up=this.getNearestStandardViewUp(direction,view);this.orbitPivot=null;this.applyStandardViewCamera(direction,up);}getStandardViewDirection(view){if(view==='top')return new THREE.Vector3(0,-1,0);if(view==='bottom')return new THREE.Vector3(0,1,0);if(view==='front')return new THREE.Vector3(0,0,-1);if(view==='back')return new THREE.Vector3(0,0,1);if(view==='right')return new THREE.Vector3(-1,0,0);if(view==='left')return new THREE.Vector3(1,0,0);return null;}getNearestStandardViewUp(direction,view){const forward=direction.clone().normalize();if(view==='front'||view==='back'||view==='right'||view==='left'){return new THREE.Vector3(0,1,0);}let referenceUp=this.camera&&this.camera.up?this.camera.up.clone().normalize():new THREE.Vector3(0,1,0);let projected=referenceUp.clone().sub(forward.clone().multiplyScalar(referenceUp.dot(forward)));if(projected.lengthSq()<1e-8&&this.camera){const currentForward=this.camera.getWorldDirection(new THREE.Vector3()).normalize();projected=currentForward.sub(forward.clone().multiplyScalar(currentForward.dot(forward)));}if(projected.lengthSq()<1e-8){projected=Math.abs(forward.y)>0.9?new THREE.Vector3(0,0,forward.y<0?1:-1):new THREE.Vector3(0,1,0);}projected.normalize();const baseCandidates=Math.abs(forward.y)>0.9?[new THREE.Vector3(1,0,0),new THREE.Vector3(-1,0,0),new THREE.Vector3(0,0,1),new THREE.Vector3(0,0,-1)]:[new THREE.Vector3(0,1,0),new THREE.Vector3(0,-1,0)];const candidates=baseCandidates.filter(candidate=>Math.abs(candidate.dot(forward))<0.999);let best=candidates[0]||new THREE.Vector3(0,1,0);let bestDot=-Infinity;for(const candidate of candidates){const value=candidate.dot(projected);if(value>bestDot){bestDot=value;best=candidate;}}return best.clone().normalize();}applyStandardViewCamera(direction,up){const forward=direction.clone().normalize();const safeUp=up&&up.lengthSq&&up.lengthSq()>1e-8?up.clone().normalize():this.getNearestStandardViewUp(forward,null);this.camera.position.copy(this.target).sub(forward.multiplyScalar(Math.max(this.distance,0.05)));this.camera.up.copy(safeUp);this.camera.lookAt(this.target);this.syncOrbitAnglesFromCamera();this.syncFreeLookAnglesFromCamera();this.updateCameraClipRange();this.updateCameraProjection();this.updateViewCubeOrientation();this.requestRender&&this.requestRender('camera-free-look');}toggleLevelGrid(){this.levelGridVisible=!this.levelGridVisible;this.updateLevelGridToModel();return this.levelGridVisible;}updateLevelGridToModel(){if(this.levelGrid){this.scene.remove(this.levelGrid);this.levelGrid.traverse&&this.levelGrid.traverse(child=>{if(child.geometry)child.geometry.dispose&&child.geometry.dispose();if(child.material)child.material.dispose&&child.material.dispose();});this.levelGrid=null;}if(!this.levelGridVisible||!this.meshes.length)return;const box=new THREE.Box3();for(const mesh of this.meshes)box.expandByObject(mesh);if(box.isEmpty())return;const size=box.getSize(new THREE.Vector3());const center=box.getCenter(new THREE.Vector3());const gridSize=Math.max(size.x,size.z,10)*1.35;const divisions=Math.max(20,Math.min(220,Math.round(gridSize)));const grid=new THREE.GridHelper(gridSize,divisions,0xcbd5e1,0xe2e8f0);grid.name='ExportModel_Level00_Grid';grid.position.set(center.x,0,center.z);grid.renderOrder=-10;this.levelGrid=grid;this.scene.add(grid);}fitToModel(){if(!this.meshes.length)return;const visibleMeshes=this.meshes.filter(mesh=>mesh&&mesh.visible);const targets=visibleMeshes.length?visibleMeshes:this.meshes;const box=this.getFitBoxForMeshes(targets);this.fitToBox(box);}getRawFitBoxForMeshes(meshes){const box=new THREE.Box3();for(const mesh of meshes||[]){if(mesh)box.expandByObject(mesh);}return box.isEmpty()?null:box;}getFitBoxForMeshes(meshes){const box=this.getRawFitBoxForMeshes(meshes);if(!box||box.isEmpty())return this.getSectionFitBox();return this.applySectionBoundsToFitBox(box);}getSectionFitBox(){if(!this.sectionBoxActive||!this.sectionBounds)return null;return new THREE.Box3(this.sectionBounds.min.clone(),this.sectionBounds.max.clone());}applySectionBoundsToFitBox(box){if(!box||box.isEmpty()||!this.sectionBoxActive||!this.sectionBounds)return box;const sectionBox=new THREE.Box3(this.sectionBounds.min.clone(),this.sectionBounds.max.clone());const clipped=box.clone().intersect(sectionBox);return clipped&&!clipped.isEmpty()?clipped:sectionBox;}fitToObject(object){const box=new THREE.Box3().setFromObject(object);this.fitToBox(box);}getFitCameraDistance(maxSize,depthSize){const size=Math.max(1,Number(maxSize)||1);if(this.isPerspectiveMode()){const fov=THREE.MathUtils.degToRad((this.camera&&this.camera.fov)||this.perspectiveFov||55);const byFov=(size*1.18)/(2*Math.tan(fov/2));return Math.max(size*1.45,byFov,0.5);}const depth=Math.max(size,Number(depthSize)||size);return Math.max(size*2.6,depth*2.6);}fitToBox(box){if(!box||box.isEmpty())return;const size=box.getSize(new THREE.Vector3());const center=box.getCenter(new THREE.Vector3());const maxSize=Math.max(size.x,size.y,size.z,1);const depthSize=Math.max(maxSize,this.getFullModelDepthSize());this.target.copy(center);this.distance=this.getFitCameraDistance(maxSize,depthSize);this.orthoScale=maxSize*1.12;this.orbitPivot=null;this.applyCamera();}}window.BimCanvasViewer=BimCanvasViewer;})();