(function(){class BimGroupingService{constructor(store){this.store=store;}buildTree(elements,groupKeys){const activeKeys=(groupKeys||[]).filter(Boolean);if(!activeKeys.length){return[{name:'Elementos visibles',count:elements.length,elements,children:[]}];}const root={name:'root',count:0,elements:[],children:[],childMap:new Map()};for(const element of elements){let current=root;current.count++;current.elements.push(element);for(let groupIndex=0;groupIndex<activeKeys.length;groupIndex++){const key=activeKeys[groupIndex];const value=this.store.getValue(element,key)||'<Sin valor>';if(!current.childMap.has(value)){const node={name:value,count:0,elements:[],children:[],childMap:new Map(),groupKey:key,groupIndex:groupIndex};current.childMap.set(value,node);current.children.push(node);}current=current.childMap.get(value);current.count++;current.elements.push(element);}}cleanup(root);return root.children;}}function cleanup(node){delete node.childMap;node.children.sort((a,b)=>a.name.localeCompare(b.name));for(const child of node.children)cleanup(child);}window.BimGroupingService=BimGroupingService;})();